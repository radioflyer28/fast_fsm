---
phase: 18-safe-ownership-concurrency
plan: 04
type: execute
wave: 3
depends_on: [18-03]
files_modified:
  - src/fast_fsm/core.py
  - .specify/memory/spr-core-api.md
  - tests/test_ownership_concurrency.py
  - tests/test_advanced_functionality.py
  - tests/test_listeners.py
  - tests/test_builder.py
  - tests/test_mypyc_guard.py
autonomous: true
requirements: [OWN-01, OWN-02, OWN-03, OWN-04, OWN-05, OWN-06, OWN-07]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Per D-14, every exact public topology, history, listener, callback, failure-observer, and async-callback registrar participates in the same one-entry ownership policy as trigger/control operations."
    - "Batch and bidirectional graph writes own the complete Phase 16 normalize/preflight/commit operation exactly once, so rejection leaves no prefix mutation and success advances graph version once."
    - "Registration attempted from an owned callback raises before mutation, while external registration between completed operations preserves order and snapshot semantics."
    - "Per D-15, reads, builders before publication, factories, and independent clones gain no cross-field snapshot promise and never share ownership state."
  artifacts:
    - {path: "src/fast_fsm/core.py", provides: "Complete public write-family ownership coverage"}
    - {path: "tests/test_mypyc_guard.py", provides: "AST inventory proving every public writer enters once and delegations use private bodies"}
    - {path: "tests/test_listeners.py", provides: "Registration reentry and between-operation snapshot coverage"}
  key_links:
    - {from: "public graph/history/registrar methods", to: "sync admission seam", via: "one operation-labelled envelope", pattern: "_acquire_sync_ownership"}
    - {from: "batch graph writers", to: "_commit_transition_plan", via: "one owned preflight and atomic commit", pattern: "_commit_transition_plan"}
    - {from: "FSMBuilder/clone", to: "machine ownership", via: "fresh constructor-owned primitive/metadata", pattern: "clone|build"}
  prohibitions:
    - "[UNVERIFIED - no SPEC] Do not turn per-machine serialization into a global lock, hidden fair queue, timeout, or scheduler-order promise."
    - "[UNVERIFIED - no SPEC] Do not silently rebind an async machine to another event loop or implicitly offload synchronous callbacks to worker threads."
    - "[UNVERIFIED - no SPEC] Do not disclose trigger arguments, keyword values, callback payloads, stored causes, or arbitrary exception text in ownership errors or logs."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow cancellation, and do not roll back Phase 17 state/history after commit."
---

<objective>
Close ownership coverage over every public machine write while preserving graph, builder, history, observer, and clone contracts.

Purpose: Ensure OWN-05 is complete rather than trigger-only, with structural proof against future bypasses.
Output: Owned topology/history/registration operations, builder/clone independence, and a comprehensive public-writer AST/behavior matrix.
</objective>

## Artifacts this phase produces

- New private runtime symbols in `src/fast_fsm/core.py`: causal/prepared-marker ContextVars; base and async ownership slots/helpers; private trigger/control bodies.
- New files: `tests/test_ownership_concurrency.py`, `18-PERFORMANCE-EVIDENCE.md`, and `ADR-005-safe-ownership-concurrency.md`.
- Extended `phase18` verification suite and structural write-family inventory; no public symbol, dependency, schema, or external integration.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md
@.planning/phases/18-safe-ownership-concurrency/18-RESEARCH.md
@.planning/phases/18-safe-ownership-concurrency/18-PATTERNS.md
@.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md
@.planning/phases/18-safe-ownership-concurrency/18-03-SUMMARY.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-VERIFICATION.md
@src/fast_fsm/core.py

<interfaces>
- Exact write inventory: `add_state`; `add_transition`, `add_transitions`, `add_bidirectional_transition`, `add_emergency_transition`; `enable_history`, `disable_history`; `add_listener`, `on_enter`, `on_exit`, `after_transition`, `on_failed`, `on_trigger`; `on_enter_async`, `on_exit_async`; plus trigger and direct control completed by Plans 02-03.
- Each public method owns its validation/preflight and final mutation as one operation; batch helpers and public delegations are private already-owned paths rather than nested public calls.
- `FSMBuilder` staging remains outside published machine ownership. Each built machine, quick-built machine, factory result, and clone initializes independent lock/loop/owner/root metadata.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Own every topology and history mutation as one atomic public operation</name>
  <files>src/fast_fsm/core.py, tests/test_ownership_concurrency.py, tests/test_advanced_functionality.py, tests/test_mypyc_guard.py</files>
  <read_first>src/fast_fsm/core.py:896-965, src/fast_fsm/core.py:1094-1260, tests/test_graph_invariants.py, tests/test_advanced_functionality.py:586-958, tests/test_mypyc_guard.py:306-490</read_first>
  <behavior>
    - `add_state`, all four transition-graph families, and history enable/disable reject callback-originated reentry before validation and mutation.
    - Independent threads serialize complete validation/preflight/commit; invalid batch/bidirectional/emergency requests leave topology and graph version unchanged.
    - Async machines apply the D-12 unbound/bound-thread/idle policy to inherited sync graph/history writers.
  </behavior>
  <action>Remove the `RED until Plan 18-04` strict-xfail markers for topology/history rows and run them to record RED. Wrap each topology and history public method in the established sync ownership entry, with a stable operation label and `try/finally` release. Where a public batch/convenience method currently reaches another public writer, extract or reuse private already-owned normalization/commit bodies so D-14 yields exactly one admission for the entire request. Preserve Phase 16 canonical identity, full preflight, all-or-nothing mutation, no-op/version behavior, bounded deque validation/eviction, and copy-on-read semantics. Apply the AsyncStateMachine D-12 policy automatically through the shared overridable admission seam. Replace Wave 0's staged writer-name inventory with full enforcement: the parameterized behavior matrix and AST guard must prove every exact public writer enters once, releases once, and does not call a sibling acquiring public writer. Add Event/Barrier contention, callback reentry, invalid-input cleanup, graph-version, and history state assertions. Do not add ownership to read-only snapshot/current-state/history access per D-15.</action>
  <acceptance_criteria>
    - Every topology/history writer has one admission and one release path with no public-to-public double entry.
    - Rejected operations leave topology/version/history unchanged and do not leak ownership; contenders proceed afterward.
    - Existing Phase 16 graph/history suites remain unchanged in meaning and pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_advanced_functionality.py tests/test_mypyc_guard.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_advanced_functionality.py tests/test_mypyc_guard.py</automated>
    <automated>uv run pytest tests/test_ownership_concurrency.py tests/test_graph_invariants.py tests/test_advanced_functionality.py tests/test_mypyc_guard.py -x -q -k "write_family or topology or history or graph_version or ownership"</automated>
    <automated>task typecheck-mypy</automated>
  </verify>
  <done>Topology and history writes are serialized per machine as complete Phase 16 operations, reentry cannot mutate them, and structural inventory prevents bypass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Own all registrars and prove builder, factory, and clone independence</name>
  <files>src/fast_fsm/core.py, tests/test_ownership_concurrency.py, tests/test_listeners.py, tests/test_builder.py, .specify/memory/spr-core-api.md</files>
  <read_first>src/fast_fsm/core.py:1307-1430, src/fast_fsm/core.py:1982-2052, src/fast_fsm/core.py:2746-2802, src/fast_fsm/core.py:3830-3979, tests/test_listeners.py:1-190, tests/test_builder.py:1350-1555, tests/test_builder.py:2019-2050</read_first>
  <behavior>
    - Every sync/async listener and callback registrar rejects mutation from an owned callback; external registration after completion is visible in exact registration order on the next operation.
    - Failure-observer snapshot iteration remains defensive, but callback-time registration is now rejected and isolated/classified by the outer Phase 17 stage.
    - Builder staging stays mutable/frozen exactly as Phase 16 specified, and every published machine/clone owns distinct primitive, loop binding, owner, and root state.
  </behavior>
  <action>Remove the remaining `RED until Plan 18-04` strict-xfail markers for registrar and construction/clone rows and run them to record RED. Apply the shared ownership envelope to `add_listener`, `on_enter`, `on_exit`, `after_transition`, `on_failed`, `on_trigger`, `on_enter_async`, and `on_exit_async` per D-14, including validation and registry append inside the envelope. Update the Phase 17 observer-mutation regressions: a registrar call made from an owned callback must raise and be isolated/classified at the callback's current stage; separately register after the operation completes and prove the next tuple snapshot sees it in order. Keep tuple snapshot iteration itself. Leave `FSMBuilder` staging and factories outside published-machine ownership per D-15, but assert each machine created by builder/quick build/from-dict/from-transitions and each clone has independent ownership primitives/metadata and no copied active loop/owner/root. Preserve builder freeze, auto-async selection, callbacks, topology, and clone registry semantics. Finish the full AST gate for every D-14 writer and update the SPR with complete public write coverage and factory/clone independence.</action>
  <acceptance_criteria>
    - All eight registrar families reject callback-time mutation before append and release ownership after validation/callback failures.
    - Between-operation registration remains ordered and snapshot-safe; no current-pass duplication occurs.
    - Factories/builders/clones share no ownership primitive or live owner/loop/root state and retain Phase 16 behavior.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_listeners.py tests/test_builder.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_listeners.py tests/test_builder.py</automated>
    <automated>uv run pytest tests/test_ownership_concurrency.py tests/test_listeners.py tests/test_builder.py tests/test_transition_lifecycle.py -x -q -k "registration or listener or callback or observer or builder or clone or ownership"</automated>
    <automated>uv run python tools/release_evidence.py slots-policy --json</automated>
  </verify>
  <done>Every registrar is ownership-safe, snapshot behavior remains defensible between operations, and all construction/clone paths produce independent machines without widening read semantics.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | writer reentry bypass | high | mitigate | Complete behavior/AST inventory and pre-mutation rejection for every public writer. |
| T-18-02 | DoS | lock leak on invalid/batch writes | high | mitigate | One finally release plus post-failure contender/reuse proof. |
| T-18-05 | Tampering | registry/observer mutation | high | mitigate | Callback-time registrar rejection plus external-between-operation snapshot tests. |
| T-18-08 | Information Disclosure | operation errors | high | mitigate | Stable operation/category messages contain no graph, callback, or payload values. |
| T-18-09 | DoS | global coupling/fairness | medium | accept | Construction paths prove independent primitives; no fairness, timeout, or ordering promise. |
</threat_model>

<verification>Run the public-writer matrix, Phase 16 graph/history/builder suites, Phase 17 observer regressions, slots, Ruff, and mypy.</verification>
<success_criteria>OWN-05 covers every exact writer, each public operation enters once, registration snapshot behavior is preserved outside ownership, and construction/clone paths never share mutable ownership state.</success_criteria>
<output>Create `.planning/phases/18-safe-ownership-concurrency/18-04-SUMMARY.md` when done.</output>
