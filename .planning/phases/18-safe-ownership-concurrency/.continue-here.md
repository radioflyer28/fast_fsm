---
context: phase
phase: 18-safe-ownership-concurrency
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-01T21:33:28.366Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

The resuming agent must explain each constraint and its prevention mechanism before continuing:

- **Commit isolation:** unrelated user-owned files are already staged. Every project commit must use `git commit --only` with explicit owned paths.
- **Fresh-origin verification:** checkout-native `.so` shadows are intentionally retained and stale. Do not delete or overwrite them; use `tools/phase16_isolated_verify.py`.
- **Wave 2 is not closed:** Plan 18-03 is committed, but its independent post-wave gate failed. Verify and commit the existing AST-based guard repair before Plan 18-04.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Broad commit from a dirty index | A prior Phase 17 commit accidentally included pre-staged Beads files before it was repaired. The current index still contains unrelated user work. | blocking | Use `git commit --only <explicit owned paths>` for every commit; inspect `git diff --cached --name-only` before and after. |
| Checkout-native test execution | Plain `uv run pytest` imported the retained stale `src/fast_fsm/core.cpython-312-darwin.so` and failed collection against current source. | blocking | Preserve native shadows and run pure/fresh-native tests through `tools/phase16_isolated_verify.py`. |
| Closing a wave after executor self-check only | Plan 18-03 focused checks passed, but the independent Phase 18 suite caught an over-broad structural assertion (`source.count("threading.Lock()") == 1`). | blocking | Require the post-wave fresh-origin suite to pass; review the uncommitted AST-based invariant and commit it before Wave 3. |

<current_state>
Phase 18 is executing sequentially because the isolation base check found HEAD ahead of `origin/HEAD`. Plans 18-01 and 18-02 are fully complete. Plan 18-03 implementation and summary are committed at `792f1d5`, and STATE points to Plan 4 of 7, but Wave 2 remains open until its post-wave repair passes. `tests/test_mypyc_guard.py` contains the interrupted executor's uncommitted AST-based repair. No executor or verification process is running.
</current_state>

<completed_work>

- Plan 18-01: sync ownership tracer, deterministic inventory, native probe, and CI matrix — complete.
- Plan 18-02: synchronous trigger/direct-control ownership coverage — complete.
- Plan 18-03 implementation: permanent async loop affinity, non-blocking serialization, and causal child-task reentry rejection — committed.
- Wave 0 and Wave 1 fresh-origin pure/native suites passed.
</completed_work>

<remaining_work>

- Review the uncommitted `tests/test_mypyc_guard.py` repair. It replaces a global string count with AST checks for no module-global locks and exactly one per-instance sync and async admission lock.
- Run the focused structural test in an asserted-pure temporary export.
- Run `uv run python tools/phase16_isolated_verify.py --suite phase18`; it must pass before Wave 2 is closed.
- Commit only `tests/test_mypyc_guard.py`, then run Wave 2 schema/UI hooks and emit the checkpoint.
- Execute Plans 18-04 through 18-07, then Phase 18 code review/fix, security, Nyquist, regression, and independent verification gates.
- Continue the approved milestone workflow through Phases 19 and 20 and milestone completion.
</remaining_work>

<decisions_made>

- Use sequential execution after the canonical worktree base-check degraded isolation to `none`.
- Preserve native checkout shadows and rely on asserted fresh-origin pure/compiled exports.
- Treat the Wave 2 structural failure as a real integration regression; do not advance until repaired and independently verified.
</decisions_made>

<blockers>
- Technical: the Phase 18 suite currently fails `tests/test_mypyc_guard.py::test_phase18_sync_tracer_keeps_slots_private_body_and_no_global_lock`. A candidate repair is uncommitted and unverified.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable current state and first resume action.
2. `.planning/phases/18-safe-ownership-concurrency/18-03-PLAN.md` — async ownership acceptance criteria.
3. `.planning/phases/18-safe-ownership-concurrency/18-03-SUMMARY.md` — committed implementation details.
4. `.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md` — required pure/native and post-wave gates.
5. `.planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md` — locked ownership decisions.

## Critical Anti-Patterns (do NOT repeat these)

- Do not use plain `git commit`, `git add .`, or `git add -A`; exact-path commits prevent staged user changes from leaking.
- Do not delete or rebuild checkout-native `.so` shadows; verify from temporary exports.
- Do not trust a plan's focused self-check as wave completion; the full Phase 18 suite is the gate.

## Infrastructure State

- Branch: `feat/release-baseline-evidence`.
- Execution isolation: `none` after automatic `head-diverged-from-fork` degradation.
- Background processes: none.
- Async external jobs: none.
- Beads: Phase 18 issue `fast_fsm-z2k` remains in progress; preserve `.beads` working/index state.
- User-owned dirty state: extensive `.beads`, `.github/get-shit-done`, `.github/agents`, `.github/skills`, `.gitignore`, `.planning/config.json`, and related harness changes.

<context>
The implementation direction is sound. The only open Wave 2 issue is that a Phase 18 structural test encoded the pre-async assumption that the entire file contained one `threading.Lock()`. Plan 18-03 intentionally introduced `_async_admission_lock` as another per-machine instance lock. The drafted fix checks the actual invariant with AST: no module-level lock and exactly one initializer assignment for each slotted per-machine lock.
</context>

<next_action>
Start with: inspect `git diff -- tests/test_mypyc_guard.py`, run the focused fresh-origin structural test, then rerun `uv run python tools/phase16_isolated_verify.py --suite phase18`. If green, commit only the guard repair and close Wave 2 before Plan 18-04.
</next_action>
