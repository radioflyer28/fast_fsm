---
phase: 18-safe-ownership-concurrency
plan: 05
type: execute
wave: 4
depends_on: [18-04]
files_modified:
  - src/fast_fsm/core.py
  - .specify/memory/spr-core-api.md
  - tests/test_ownership_concurrency.py
  - tests/test_boundary_negative.py
  - tests/test_transition_lifecycle.py
  - tests/test_mypyc_guard.py
autonomous: true
requirements: [OWN-01, OWN-02, OWN-03, OWN-04, OWN-05, OWN-06, OWN-07]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Per D-01, D-02, D-04, and D-14, `safe_trigger()` performs ownership admission before its ordinary exception-conversion barrier, so ownership/loop RuntimeError escapes while ordinary post-admission Exception compatibility remains value-returning."
    - "Per D-03 and D-07, safe-trigger reentry inside a callback never double-enters and an uncaught nested ownership error is classified only at the outer callback's existing Phase 17 stage."
    - "The shared `_prepared_declarative_guards` dictionary is removed and replaced by context-local, machine-qualified state with exact set/reset nesting across independent machines, threads, and tasks."
    - "Per D-16, safe-trigger and declarative compatibility run against pure and freshly compiled origins with redacted errors and no concurrency-induced marker collision."
  artifacts:
    - {path: "src/fast_fsm/core.py", provides: "Ownership-safe `safe_trigger()` and context-local machine-qualified declarative prepared marker"}
    - {path: "tests/test_boundary_negative.py", provides: "Ownership-precondition versus ordinary safe-trigger exception contract"}
    - {path: "tests/test_mypyc_guard.py", provides: "No shared marker dictionary and one-entry safe-trigger structural guards"}
  key_links:
    - {from: "StateMachine.safe_trigger", to: "_trigger_owned", via: "ownership admission outside ordinary Exception catch", pattern: "safe_trigger|_trigger_owned"}
    - {from: "declarative guard preparation", to: "handler invocation", via: "ContextVar marker containing machine/source/trigger/target identity", pattern: "_prepared_declarative_guard"}
  prohibitions:
    - "[UNVERIFIED - no SPEC] Do not turn per-machine serialization into a global lock, hidden fair queue, timeout, or scheduler-order promise."
    - "[UNVERIFIED - no SPEC] Do not silently rebind an async machine to another event loop or implicitly offload synchronous callbacks to worker threads."
    - "[UNVERIFIED - no SPEC] Do not disclose trigger arguments, keyword values, callback payloads, stored causes, or arbitrary exception text in ownership errors or logs."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow cancellation, and do not roll back Phase 17 state/history after commit."
---

<objective>
Resolve the two critical compatibility seams: `safe_trigger()` exception scope and shared declarative prepared-guard state.

Purpose: Prevent ownership violations from being downgraded and prevent independent machines/contexts from corrupting declarative exactly-once dispatch.
Output: One-entry safe-trigger flow, context-local machine-qualified marker state, and pure/native concurrency regressions.
</objective>

## Artifacts this phase produces

- New private runtime symbols in `src/fast_fsm/core.py`: `_ownership_root`, `_prepared_declarative_guard`, base/async ownership slots and helpers, private trigger/control bodies.
- New files: `tests/test_ownership_concurrency.py`, `18-PERFORMANCE-EVIDENCE.md`, and `ADR-005-safe-ownership-concurrency.md`.
- Extended `phase18` verification suite; no new public symbol, dependency, schema, or external integration.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md
@.planning/phases/18-safe-ownership-concurrency/18-RESEARCH.md
@.planning/phases/18-safe-ownership-concurrency/18-PATTERNS.md
@.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md
@.planning/phases/18-safe-ownership-concurrency/18-04-SUMMARY.md
@.planning/phases/17-atomic-transition-lifecycle/17-VERIFICATION.md
@src/fast_fsm/core.py

<interfaces>
- `safe_trigger(trigger, *args, **kwargs) -> TransitionResult` retains conversion of ordinary Exceptions that escape after successful ownership admission; `RuntimeError` from ownership/loop admission occurs before that catch and propagates.
- `safe_trigger()` and `trigger()` each enter ownership exactly once and call the same `_trigger_owned()` implementation; neither calls the other public method.
- `_prepared_declarative_guard` is a module-level `ContextVar` whose value includes machine identity plus source/trigger/target identity; scopes use explicit token reset for Python 3.10 compatibility.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move safe-trigger ownership admission outside its compatibility catch</name>
  <reversibility rating="costly">D-01 changes the observable `safe_trigger()` control-flow boundary for ownership misuse while preserving ordinary compatibility.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_ownership_concurrency.py, tests/test_boundary_negative.py, tests/test_transition_lifecycle.py, .specify/memory/spr-core-api.md</files>
  <read_first>src/fast_fsm/core.py:2458-2659, tests/test_boundary_negative.py:243-280, tests/test_boundary_negative.py:400-425, tests/test_transition_lifecycle.py:115-220, .planning/phases/18-safe-ownership-concurrency/18-04-SUMMARY.md</read_first>
  <behavior>
    - Direct, callback-originated, cross-loop, causal-root, and D-12 mixed-mode ownership errors from `safe_trigger()` raise before the broad compatibility catch and contain only stable metadata.
    - Ordinary condition/internal Exceptions after admission still become the established failed `TransitionResult` shape; Phase 17 staged lifecycle results remain unchanged.
    - `safe_trigger()` owns once and calls `_trigger_owned()` directly, so it neither self-reenters through public `trigger()` nor bypasses ownership.
  </behavior>
  <action>Remove the `RED until Plan 18-05` strict-xfail markers for safe-trigger admission/redaction rows and run them to record RED. Refactor `safe_trigger()` so its ownership admission and outer `try/finally` occur before the existing ordinary Exception-conversion `try/except`, then invoke `_trigger_owned()` inside that inner compatibility barrier. This is the resolved research decision required by D-01 and D-14: ownership, loop, causal-root, foreign-thread, and busy preconditions are outside the catch and raise, while ordinary exceptions after admission retain safe-trigger value conversion. Preserve D-02, D-03, D-04, and D-07 by ensuring no preparation precedes admission, uncaught callback-originated nested errors are handled only by the outer lifecycle stage, and no trigger/payload/cause/arbitrary exception text enters ownership messages. Keep the legacy ordinary error/result behavior to the extent compatible with the locked boundary, including the first five `TransitionResult` fields and Phase 17 structured fields. Add direct and callback reentry, async-machine context misuse, secret-sentinel, one-entry AST, ordinary-exception conversion, and immediate-reuse tests. Update the SPR's formerly absolute safe-trigger claim to the exact ownership-precondition exception boundary in the same commit.</action>
  <acceptance_criteria>
    - Ownership `RuntimeError` is never returned as `TransitionResult`; ordinary post-admission Exception compatibility remains covered.
    - Safe-trigger has one admission and one private-body call, with release after converted results and every raising path.
    - Error text/repr/logs contain none of the unique trigger, args, kwargs, callback, or cause sentinels.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_boundary_negative.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_boundary_negative.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run pytest tests/test_ownership_concurrency.py tests/test_boundary_negative.py tests/test_transition_lifecycle.py -x -q -k "safe_trigger or ownership_error or redaction"</automated>
    <automated>task typecheck-mypy</automated>
  </verify>
  <done>`safe_trigger()` preserves ordinary safety conversion without swallowing or double-entering the ownership contract, and its documented compatibility boundary is exact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the shared declarative marker with context-local machine-qualified state</name>
  <files>src/fast_fsm/core.py, tests/test_ownership_concurrency.py, tests/test_transition_lifecycle.py, tests/test_mypyc_guard.py, .specify/memory/spr-core-api.md</files>
  <read_first>src/fast_fsm/core.py:46-132, src/fast_fsm/core.py:1507-1569, tests/test_transition_lifecycle.py:642-806, tests/test_mypyc_guard.py:306-490</read_first>
  <behavior>
    - Independent synchronous threads, independent async tasks, and nested operations on different machines cannot overwrite or consume another machine's prepared declarative marker.
    - Marker scopes include machine/source/trigger/target identity, restore exactly with ContextVar token reset, and keep declarative handlers exactly once in Phase 17 order.
    - No module-global mutable dictionary or `None` key remains, and fresh pure/native compiled semantics agree.
  </behavior>
  <action>Remove the remaining `RED until Plan 18-05` strict-xfail markers for declarative-marker rows and run them to record RED. Remove `_prepared_declarative_guards` and `_prepared_guard_scope_key()`. Introduce one module-level `_prepared_declarative_guard` `ContextVar` with a default empty value and a machine-qualified marker tuple; pass or derive `id(self)` along with canonical source, trigger, and target identity at preparation/consumption. Use explicit `token = set(...)` and `reset(token)` in `finally` so nested scopes restore correctly on Python 3.10 through 3.14. Preserve the Phase 16 one-guard-lookup and declarative exactly-once behavior and the Phase 17 handler stage/order/result contract. Add deterministic concurrent independent-machine tests for sync threads and async tasks, nested different-machine scopes, exception/cancellation restoration, and same-machine trigger ownership. Strengthen AST guards so the old shared dictionary/scope-key architecture cannot return and the ContextVar marker is machine-qualified. Update the SPR with the context-local marker invariant. At task completion the Wave 0 table has no remaining strict-xfail ownership rows.</action>
  <acceptance_criteria>
    - Concurrent independent machines execute their own declarative handler exactly once with no marker collision in sync or async modes.
    - Nested and exceptional scopes restore the previous marker exactly; later operations remain correct.
    - Structural tests prove there is no shared mutable marker registry and the compiled core still has the same public API/single-unit boundary.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_transition_lifecycle.py tests/test_mypyc_guard.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_transition_lifecycle.py tests/test_mypyc_guard.py</automated>
    <automated>uv run pytest tests/test_ownership_concurrency.py tests/test_transition_lifecycle.py tests/test_mypyc_guard.py -x -q -k "declarative or prepared_guard or context_local or machine_qualified"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_ownership_concurrency.py --include tests/test_transition_lifecycle.py --include tests/test_boundary_negative.py --include tests/test_mypyc_guard.py --include tools/phase16_isolated_verify.py -- uv run pytest tests/test_ownership_concurrency.py tests/test_transition_lifecycle.py tests/test_boundary_negative.py tests/test_mypyc_guard.py -x -q -k "safe_trigger or declarative or ownership"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_ownership_concurrency.py --include tests/test_transition_lifecycle.py --include tests/test_boundary_negative.py --include tests/test_mypyc_guard.py --include tools/phase16_isolated_verify.py -- uv run pytest tests/test_ownership_concurrency.py tests/test_transition_lifecycle.py tests/test_boundary_negative.py tests/test_mypyc_guard.py -x -q -k "safe_trigger or declarative or ownership"</automated>
  </verify>
  <done>Declarative preparation is context-local and machine-qualified in pure/native execution, and ownership errors remain outside safe-trigger result conversion.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | safe-trigger reentry | high | mitigate | Admission before catch/preparation and one-entry AST/behavior tests. |
| T-18-05 | Tampering | shared declarative marker | high | mitigate | ContextVar with machine identity plus concurrent independent-machine tests. |
| T-18-06 | Repudiation/Tampering | ownership downgrade | high | mitigate | Ownership RuntimeError escape versus ordinary result-conversion matrix. |
| T-18-07 | DoS/Tampering | marker cancellation cleanup | high | mitigate | Token reset in finally and cancellation restoration tests. |
| T-18-08 | Information Disclosure | error/result/log output | high | mitigate | Secret-sentinel assertions across ownership errors, result repr, and logs. |
| T-18-09 | DoS | hidden scheduling semantics | medium | accept | Safe-trigger and marker changes add no queue, timeout, fairness, or offload contract. |
</threat_model>

<verification>Run safe-trigger boundary, ownership, declarative, lifecycle, AST, blocking mypy, and asserted pure/native task suites.</verification>
<success_criteria>The severe safe-trigger conflict is resolved exactly, shared declarative state is eliminated, independent contexts cannot collide, and both changes preserve Phase 16/17 semantics in pure and native modes.</success_criteria>
<output>Create `.planning/phases/18-safe-ownership-concurrency/18-05-SUMMARY.md` when done.</output>
