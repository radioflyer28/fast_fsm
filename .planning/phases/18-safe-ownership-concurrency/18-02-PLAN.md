---
phase: 18-safe-ownership-concurrency
plan: 02
type: execute
wave: 1
depends_on: [18-01]
files_modified:
  - src/fast_fsm/core.py
  - .specify/memory/spr-core-api.md
  - tests/test_ownership_concurrency.py
  - tests/test_transition_lifecycle.py
  - tests/test_advanced_functionality.py
autonomous: true
requirements: [OWN-01, OWN-02, OWN-03, OWN-04, OWN-05, OWN-06, OWN-07]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Per D-01, D-02, D-03, and D-04, every synchronous same-owner write raises a stable redacted RuntimeError before preparation; uncaught callback reentry is classified only by the outer Phase 17 stage and caught reentry may let the outer operation continue."
    - "Per D-05, D-06, D-07, and D-08, independent threads serialize the full sync write envelope per machine and every success, Exception, BaseException, and callback failure releases it at a coherent Phase 17 boundary."
    - "Direct control uses one ownership entry and a private already-owned body, preserving its best-effort callback semantics without false reset/restore reentry."
  artifacts:
    - {path: "src/fast_fsm/core.py", provides: "Complete synchronous ownership envelope and private trigger/control bodies"}
    - {path: "tests/test_ownership_concurrency.py", provides: "Deterministic sync reentry, contention, cleanup, and direct-control matrix"}
    - {path: ".specify/memory/spr-core-api.md", provides: "Living sync ownership and direct-control invariants"}
  key_links:
    - {from: "trigger/safe trigger/direct control", to: "private owned bodies", via: "one public admission per operation", pattern: "_trigger_owned|_force_state_owned"}
    - {from: "ownership envelope", to: "Phase 17 commit/finalizer", via: "release only after result/failure observation completes", pattern: "finally"}
  prohibitions:
    - "[UNVERIFIED - no SPEC] Do not turn per-machine serialization into a global lock, hidden fair queue, timeout, or scheduler-order promise."
    - "[UNVERIFIED - no SPEC] Do not silently rebind an async machine to another event loop or implicitly offload synchronous callbacks to worker threads."
    - "[UNVERIFIED - no SPEC] Do not disclose trigger arguments, keyword values, callback payloads, stored causes, or arbitrary exception text in ownership errors or logs."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow cancellation, and do not roll back Phase 17 state/history after commit."
---

<objective>
Complete synchronous ownership semantics across ordinary transitions and direct control operations.

Purpose: Expand the tracer into the full thread-safe, exception-safe sync contract while retaining the exact Phase 17 transaction and control behavior.
Output: Complete sync admission/release helpers, one-entry private bodies, and deterministic lifecycle/control regressions.
</objective>

## Artifacts this phase produces

- New private runtime symbols in `src/fast_fsm/core.py`: `_ownership_root`, `_prepared_declarative_guard`, sync/async ownership slots, admission/release helpers, and private already-owned trigger/control bodies.
- New files: `tests/test_ownership_concurrency.py`, `.planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md`, `.specify/decisions/ADR-005-safe-ownership-concurrency.md`.
- Extended `phase18` verification suite; no new public symbol, dependency, schema, or external integration.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md
@.planning/phases/18-safe-ownership-concurrency/18-RESEARCH.md
@.planning/phases/18-safe-ownership-concurrency/18-PATTERNS.md
@.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md
@.planning/phases/18-safe-ownership-concurrency/18-01-SUMMARY.md
@.planning/phases/17-atomic-transition-lifecycle/17-VERIFICATION.md
@src/fast_fsm/core.py

<interfaces>
- Plan 18-01 provides `_acquire_sync_ownership(operation)`, `_release_sync_ownership(owner_thread_id)`, and `_trigger_owned(trigger, args, kwargs)`; extend these seams rather than adding another ownership mechanism.
- Preserve `force_state(name) -> None`, `reset() -> None`, and `restore(snapshot) -> None`, including the synthetic `__force__` best-effort callback chain and existing validation errors.
- Public `reset()` and `restore()` must validate/admit under their own stable operation labels and invoke `_force_state_owned()` directly; they must not call the acquiring public `force_state()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish the synchronous trigger envelope and callback-originated reentry matrix</name>
  <reversibility rating="costly">D-01 through D-08 establish observable exception and scheduling behavior across all synchronous call sites.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_ownership_concurrency.py, tests/test_transition_lifecycle.py, .specify/memory/spr-core-api.md</files>
  <read_first>src/fast_fsm/core.py:2053-2140, src/fast_fsm/core.py:2458-2659, tests/test_transition_lifecycle.py:115-220, tests/test_transition_lifecycle.py:356-447, .planning/phases/18-safe-ownership-concurrency/18-01-SUMMARY.md</read_first>
  <behavior>
    - Reentry from each Phase 17 callback region is rejected before nested preparation; uncaught errors become the outer callback-stage failure and caught errors leave the outer transition free to complete per D-03.
    - Ownership remains held through guard evaluation, callbacks, commit, declarative/trigger/after callbacks, failure observers, and final result construction per D-07.
    - Failure observers that raise `BaseException` do not leak the owner, and a second independent thread proceeds after the complete outer finalizer per D-08.
  </behavior>
  <action>Remove the `RED until Plan 18-02` strict-xfail markers only from the synchronous trigger/callback/release rows in the Wave 0 contract table, run the focused selection to record deterministic RED, then expand the sync tracer tests to exercise direct and callback-originated trigger reentry at pre-commit and post-commit stages, including deliberately caught and uncaught nested `RuntimeError`. Preserve the one public `trigger()` entry and private `_trigger_owned()` body; do not move any Phase 17 preparation, callbacks, commit, finalization, or result construction outside the envelope. Enforce D-01, D-02, D-03, D-04, D-07, and D-08 exactly: reentry checks precede lock acquisition and `_prepare_transition()`, fixed operation/category error text excludes user data, the outer lifecycle alone classifies uncaught callback failure, and release occurs after `_finalize_failure()` completes even if an observer raises `KeyboardInterrupt` or `SystemExit`. Extend deterministic two-thread tests so the contender cannot enter any lifecycle slot until the owner's finalizer finishes. Update the SPR incrementally with the now-complete synchronous trigger contract; leave all Plan 18-03 through 18-05 strict-xfail rows intact.</action>
  <acceptance_criteria>
    - No nested trigger reaches preparation, guards, callbacks, state, history, or observers before raising.
    - Caught and uncaught callback reentry produce the locked outer-operation outcomes at exact Phase 17 stages.
    - A blocked independent thread proceeds after every outer success/failure/BaseException path and observes coherent state/history.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run pytest tests/test_ownership_concurrency.py tests/test_transition_lifecycle.py -x -q -k "sync and (reentrant or thread or release or observer)"</automated>
    <automated>task typecheck-mypy</automated>
  </verify>
  <done>The complete synchronous trigger transaction is one ownership operation, callback reentry cannot mutate it, and all exit paths hand the machine cleanly to the next independent caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give force, reset, and restore one direct-control ownership entry</name>
  <files>src/fast_fsm/core.py, tests/test_ownership_concurrency.py, tests/test_advanced_functionality.py, .specify/memory/spr-core-api.md</files>
  <read_first>src/fast_fsm/core.py:1901-1980, src/fast_fsm/core.py:1982-2052, tests/test_advanced_functionality.py:586-958, tests/test_advanced_functionality.py:1024-1090</read_first>
  <behavior>
    - `force_state`, `reset`, and `restore` each acquire exactly once and call a private already-owned control body, never another acquiring public method.
    - Existing validation, synthetic trigger, callback ordering, catch-and-continue behavior, state/history semantics, and clone behavior remain unchanged except for ownership rejection.
    - Callback-originated direct-control reentry raises before nested validation/mutation, and all ordinary/BaseException callback paths release ownership.
  </behavior>
  <action>Remove the `RED until Plan 18-02` strict-xfail markers from the force/reset/restore rows, run them to record RED, then create `_force_state_owned(state_name)` containing the existing direct-control transition body. Make `force_state()`, `reset()`, and `restore()` each perform their existing public validation within one sync ownership envelope and route directly to that private body, eliminating the current `reset -> force_state` and `restore -> force_state` public delegation that would falsely reenter. Apply D-01 through D-08 without converting direct control to `TransitionResult`, without changing best-effort callback continuation, and without changing snapshot validation or synthetic trigger semantics. Add table-driven reentry, two-thread serialization, validation-failure cleanup, callback `BaseException`, and clone independence cases; ensure each clone constructs its own primitive/owner state. Record direct-control ownership in the SPR in this same behavior-owning commit.</action>
  <acceptance_criteria>
    - Normal reset and restore do not self-reenter, and all three public methods use exactly one admission.
    - Reentrant direct control fails before nested validation/callback/state change, while independent threads serialize.
    - Clones share topology as before but never share a primitive, owner marker, or active ownership state.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_advanced_functionality.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_advanced_functionality.py</automated>
    <automated>uv run pytest tests/test_ownership_concurrency.py tests/test_advanced_functionality.py -x -q -k "force or reset or restore or clone or ownership"</automated>
    <automated>uv run pytest tests/test_transition_lifecycle.py -x -q</automated>
    <automated>uv run python tools/release_evidence.py slots-policy --json</automated>
  </verify>
  <done>Direct control is serialized and reentry-safe through one private body while all Phase 17 compatibility semantics remain intact.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | trigger/control reentry | high | mitigate | Pre-acquisition checks plus callback-stage and no-mutation assertions. |
| T-18-02 | DoS | sync lock/owner leak | high | mitigate | One finally boundary and next-thread/immediate-reuse proof for BaseException. |
| T-18-06 | Repudiation/Tampering | public delegation | high | mitigate | Private already-owned trigger/control bodies prevent false reentry and bypass. |
| T-18-08 | Information Disclosure | RuntimeError/log text | high | mitigate | Fixed operation/category messages and secret sentinels. |
| T-18-09 | DoS | scheduling expectations | medium | accept | Mutual exclusion only; no fairness, timeout, or queue-order behavior is implemented or tested. |
</threat_model>

<verification>Run deterministic sync ownership, lifecycle, control, clone, slots, Ruff, and mypy gates.</verification>
<success_criteria>Sync trigger and direct-control writes have one auditable ownership entry, no false public delegation reentry, Phase 17 behavior remains green, and no lock/owner leak survives any tested exit.</success_criteria>
<output>Create `.planning/phases/18-safe-ownership-concurrency/18-02-SUMMARY.md` when done.</output>
