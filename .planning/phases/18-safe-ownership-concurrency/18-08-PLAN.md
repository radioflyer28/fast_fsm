---
phase: 18-safe-ownership-concurrency
plan: 08
type: execute
wave: 7
depends_on: [18-07]
files_modified:
  - src/fast_fsm/core.py
  - tests/test_ownership_concurrency.py
  - tests/test_mypyc_guard.py
  - evidence/release-baseline.json
  - .planning/phases/18-safe-ownership-concurrency/18-SECURITY.md
  - .planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [OWN-01, OWN-02, OWN-03, OWN-04, OWN-05, OWN-06, OWN-07]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Per D-14, preparation provenance and active consumer identity are installed at independent seams: marker helpers record only preparation, while every enclosing public sync/async machine-dispatch boundary establishes and restores the consumer machine."
    - "A second machine sharing the exact State objects and source/trigger/target identities cannot consume the first machine's live preparation marker, including when nested public dispatch reaches state policy from a decorator condition before installing its own marker."
    - "Per D-15, the correction preserves the public State, DeclarativeState, and AsyncDeclarativeState subclass hook signatures and adds no hidden caller keyword, mutable process-wide registry, shared machine state, or public symbol."
    - "Per D-16, the collision regression and an AST consumption guard pass from asserted fresh pure and freshly compiled origins, and the complete Phase 18 suite refreshes the release baseline without loading or deleting retained checkout-native shadows."
    - "T-18-03 remains open until one captured 40-character implementation/evidence SHA is the proven clean-worktree execution-branch tip and the explicit argument to the hosted Python 3.10-3.14 helper and read-only independent auditor; the secure-phase orchestrator must mechanically persist that identical SHA and returned verdict in SECURITY.md."
    - "A bounded targeted precheck completes within 30 seconds before baseline generation, the full Phase 18 suite, or hosted waiting begins."
    - "All implementation, evidence, audit persistence, validation, and landing work occurs in a dedicated clean worktree; unrelated staged, unstaged, and untracked source-checkout work is never stashed, reset, staged, committed, or removed."
    - "The Phase 18 Nyquist map includes all three Plan 18-08 tasks and becomes compliant only after their fresh-origin, exact-SHA hosted, and independent-audit gates pass."
  artifacts:
    - {path: "src/fast_fsm/core.py", provides: "Independent preparation-provenance and active-consumer ContextVars wired at marker and public dispatch seams"}
    - {path: "tests/test_ownership_concurrency.py", provides: "Sync and async shared-State cross-machine marker-consumption regressions"}
    - {path: "tests/test_mypyc_guard.py", provides: "AST proof that machine identity participates in marker consumption and public hook signatures remain unchanged"}
    - {path: "evidence/release-baseline.json", provides: "Fresh pure-source baseline generated after the new regressions"}
    - {path: ".planning/phases/18-safe-ownership-concurrency/18-SECURITY.md", provides: "Independent post-CI auditor verdict and exact candidate-SHA audit trail"}
    - {path: ".planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md", provides: "Refreshed Plan 18-08 Nyquist task map and final compliance sign-off"}
  key_links:
    - {from: "StateMachine.can_trigger/trigger/safe_trigger and AsyncStateMachine.can_trigger_async/trigger_async", to: "DeclarativeState.can_transition / AsyncDeclarativeState.can_transition_async", via: "consumer ContextVar installed at the enclosing public dispatch and preparation ContextVar installed only around the state-policy call", pattern: "_declarative_consumer_machine_id|_set_prepared_declarative_guard|_has_prepared_declarative_guard"}
    - {from: "_has_prepared_declarative_guard", to: "_prepared_declarative_guard + _declarative_consumer_machine_id", via: "comparison of independently installed producer and consumer machine identities before source/trigger/target matching", pattern: "marker[0].*_declarative_consumer_machine_id.get"}
    - {from: "Phase 18 candidate commit", to: "hosted ownership_native_probe matrix", via: "pushed exact SHA and phase18_native_probe exact-ref assertion", pattern: "assert-hosted-ci-sha"}
    - {from: "read-only independent security auditor", to: "secure-phase orchestrator then 18-SECURITY.md", via: "structured verdict/candidate return validated for exact SHA equality and mechanically persisted without status reinterpretation", pattern: "SECURED|OPEN_THREATS|ESCALATE|Audited candidate SHA"}
---

<objective>
Close the two blocking Phase 18 security threats by making prepared-marker consumption genuinely machine-qualified, then re-establishing fresh local and exact-SHA hosted evidence before an independent auditor updates the security record.

Purpose: Correct the Plan 18-05 execution miss that stored machine identity without consuming it, and prevent the earlier hosted result from being reused after runtime/test/baseline changes.
Output: Machine-qualified sync/async declarative consumption, structural and behavioral regressions, a bounded fast precheck, a refreshed release baseline, an exact-SHA Python 3.10-3.14 hosted native pass, and the read-only auditor's verdict mechanically persisted by the secure-phase orchestrator in `18-SECURITY.md`, all from a clean execution worktree.
</objective>

## Artifacts this phase produces

- New private production symbol in `src/fast_fsm/core.py`: `_declarative_consumer_machine_id` (one module-level `ContextVar[Optional[int]]` installed and restored by public machine-dispatch boundaries, independently of preparation-marker installation).
- New test symbols in `tests/test_ownership_concurrency.py`: `test_sync_cross_machine_consumer_rejects_outer_preparation_marker` and `test_async_cross_machine_consumer_rejects_outer_preparation_marker`.
- New test symbol in `tests/test_mypyc_guard.py`: `test_prepared_declarative_marker_compares_independent_consumer_identity`.
- Updated evidence artifacts: `evidence/release-baseline.json`, `.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md`, and, only after the independent auditor rerun, `.planning/phases/18-safe-ownership-concurrency/18-SECURITY.md`.
- No new public symbol, public parameter, runtime dependency, global mutable registry, installed-artifact claim, or change to retained checkout-native shadows.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md
@.planning/phases/18-safe-ownership-concurrency/18-RESEARCH.md
@.planning/phases/18-safe-ownership-concurrency/18-PATTERNS.md
@.planning/phases/18-safe-ownership-concurrency/18-SECURITY.md
@.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md
@.planning/phases/18-safe-ownership-concurrency/18-05-SUMMARY.md
@.planning/phases/18-safe-ownership-concurrency/18-07-SUMMARY.md
@src/fast_fsm/core.py
@tests/test_ownership_concurrency.py
@tests/test_mypyc_guard.py
@tools/phase16_isolated_verify.py
@tools/phase18_native_probe.py
@evidence/release-baseline.json

<interfaces>
- Keep `State.can_transition(self, trigger, to_state, *args, **kwargs)`, `DeclarativeState.can_transition(self, trigger, to_state, *args, **kwargs)`, and `AsyncDeclarativeState.can_transition_async(self, trigger, to_state, *args, **kwargs)` unchanged for interpreted subclasses of the mypyc core.
- Reuse `_PreparedDeclarativeGuard`, `_prepared_declarative_guard`, `_set_prepared_declarative_guard()`, `_reset_prepared_declarative_guard()`, and `_has_prepared_declarative_guard()`; the only new production symbol is `_declarative_consumer_machine_id`.
- `_set_prepared_declarative_guard(machine, source_state, trigger, to_state)` and `_reset_prepared_declarative_guard(token)` continue to install and restore only the preparation marker. They must not set, reset, or derive the active consumer identity.
- `StateMachine.can_trigger()`, `StateMachine.trigger()`, `StateMachine.safe_trigger()`, `AsyncStateMachine.can_trigger_async()`, and `AsyncStateMachine.trigger_async()` each install `id(self)` in `_declarative_consumer_machine_id` before transition preparation/state-policy dispatch and reset the exact token in `finally`; inherited sync dispatch on an `AsyncStateMachine` follows the same base boundaries.
- `_has_prepared_declarative_guard(source_state, trigger, to_state)` returns true only when the marker exists, its stored producer machine id equals the independently installed `_declarative_consumer_machine_id.get()`, and its source/trigger/target fields match.
- `tools/phase18_native_probe.py --assert-hosted-ci-sha REF` is the authority for exact-ref hosted proof and requires one successful native ownership job for each Python 3.10 through 3.14.
</interfaces>
</context>

<feasibility_audit>
Current production flow makes the mismatch reachable without private-context manipulation. `StateMachine.trigger()` / `AsyncStateMachine.trigger_async()` evaluate a declarative guard, `_can_transition_after_declarative_guard*()` installs machine A's preparation marker, and the shared state's public policy override runs while that marker is live. That override can invoke machine B's public `can_trigger()` / `can_trigger_async()`. B evaluates its decorator condition before B reaches its own preparation-marker helper; if that condition calls the shared base public state-policy hook, current `_has_prepared_declarative_guard()` sees A's matching source/trigger/target tuple and incorrectly suppresses B's evaluation because it ignores `marker[0]`. Read-only sync and async probes against current `core.py` both completed real dispatch successfully with 2 condition calls; the corrected independently qualified boundary requires exactly 3. This is the Task 1 RED seam.
</feasibility_audit>

<workspace_strategy>
Execute every task in a dedicated clean worktree so the user's existing checkout remains byte-for-byte untouched, including unrelated staged, unstaged, and untracked paths. Before Task 1, record `SOURCE_ROOT="$(git rev-parse --show-toplevel)"`, `SOURCE_HEAD="$(git rev-parse HEAD)"`, and the source checkout's `git status --porcelain=v2 --branch` for the summary; do not stash, reset, clean, checkout, add, or commit anything there. Fetch `origin`, choose a unique `EXEC_BRANCH` under `fix/phase18-security-gap-*`, allocate `WORKTREE_DIR` beneath `/private/tmp`, and create it with `git worktree add -b "$EXEC_BRANCH" "$WORKTREE_DIR" "$SOURCE_HEAD"`. Run Tasks 1-3 only with that worktree as the current directory, propagate the exact `EXEC_BRANCH` and candidate SHA through push, hosted proof, audit input, SECURITY, VALIDATION, and SUMMARY, and never merge/cherry-pick back into the dirty source checkout. After the final ordered landing sequence succeeds and the clean worktree has no uncommitted paths, remove only that exact registered worktree without `--force`; if it is not clean, retain it and report its path rather than risking data loss.
</workspace_strategy>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and fix independently qualified declarative marker consumption</name>
  <reversibility rating="reversible">The correction is confined to private ContextVar plumbing and tests while public subclass signatures and dispatch behavior remain fixed.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_ownership_concurrency.py, tests/test_mypyc_guard.py</files>
  <read_first>src/fast_fsm/core.py:46-137, src/fast_fsm/core.py:1693-1873, src/fast_fsm/core.py:2659-2865, src/fast_fsm/core.py:3394-3569, src/fast_fsm/core.py:3909-4050, tests/test_ownership_concurrency.py:430-566, tests/test_mypyc_guard.py:601-649, .planning/phases/18-safe-ownership-concurrency/18-SECURITY.md</read_first>
  <behavior>
    - Sync RED: two real `StateMachine` instances share the exact `DeclarativeState` source object, target object, and trigger. During machine A's public `trigger()` state-policy call, its shared state hook starts machine B's public `can_trigger()`. Before B installs its own preparation marker, B's decorator condition calls the shared public `DeclarativeState.can_transition()` hook once behind a recursion-depth guard. The live A marker must not suppress this B-owned condition evaluation: current code records 2 guard calls, while correct independent qualification records exactly 3.
    - Async RED: repeat the same production-reachable nesting with two real `AsyncStateMachine` instances, one shared `AsyncDeclarativeState`, one shared target, public `trigger_async()` for A, public `can_trigger_async()` for B, and an awaited condition-to-public-policy call. Current code again records 2 guard calls; correct behavior records exactly 3, and every token restores across await and cancellation boundaries.
    - The tests never set, reset, or monkeypatch `_declarative_consumer_machine_id` or `_prepared_declarative_guard`; all provenance is created by real public machine dispatch. Same-machine dispatch still evaluates each decorator guard exactly once, and the outer consumer/marker pair is restored after nested dispatch.
    - AST proof requires `_set_prepared_declarative_guard` to touch only `_prepared_declarative_guard`; separately requires all five public dispatch entries to set/reset `_declarative_consumer_machine_id`; and requires `_has_prepared_declarative_guard` to compare marker machine provenance with `_declarative_consumer_machine_id.get()`.
    - AST proof also freezes the three public State/declarative hook signatures and rejects a machine parameter, injected caller keyword, mutable dict/set/list registry, or public API addition.
  </behavior>
  <action>First create and enter the dedicated clean execution worktree exactly as specified in `<workspace_strategy>`; abort before editing if it cannot be created from the recorded source `HEAD`. Start with RED and prove the source-level feasibility path above, not a synthetic context setup. Add the two named regressions with local slot-safe declarative subclasses, exact shared source/target identities, one trigger, explicit call-depth flags, and guard-call/event cardinality. The outer shared-state policy hook must launch the second machine through `can_trigger()` or `can_trigger_async()` while the first machine's marker is live; the second machine's decorator condition then invokes the shared base public sync/async state-policy hook before B reaches `_set_prepared_declarative_guard`. Do not subclass either machine class, directly call marker helpers from the new regressions, or mutate/monkeypatch either ContextVar. Run the focused fresh-pure command against current production code and require the behavioral and AST assertions to fail for the ignored producer/consumer distinction; commit only the two test files in that known-RED state. Implement D-14 by adding exactly `_declarative_consumer_machine_id`. Keep the existing marker tuple and marker set/reset helpers scoped only to preparation provenance. Independently wrap the five public machine-dispatch entries named in `<interfaces>` with Python 3.10-compatible ContextVar set/token-reset boundaries so the consumer is active before preparation and restored in `finally`; do not derive or install the consumer inside the preparation helper. Change `_has_prepared_declarative_guard` to require equality between the marker's producer machine id and the active consumer id before matching source/trigger/target. Preserve D-15's public subclass signatures, D-16's deterministic/no-sleep evidence, ownership admission/lifecycle ordering, and exactly-once ordinary guard behavior. Add the AST regression for the distinct setters, public boundary coverage, comparison, signature freeze, and absence of shared mutable registries or hidden parameters. Format the three exact paths, then make the focused fresh-pure and fresh-compiled suites GREEN. Preserve every unrelated path and retained native shadow in the source checkout by never operating there after worktree creation.</action>
  <acceptance_criteria>
    - Both named regressions reach the mismatch through nested real public machine dispatch, share exact State identities, never manipulate ContextVars directly, fail on current production code, and pass after the correction in asserted fresh pure and freshly compiled origins.
    - Preparation provenance and consumer identity come from different setters at different seams; a second machine cannot consume the first machine's live marker even when source, target, trigger, execution context, and nesting match.
    - Same-machine declarative dispatch remains exactly once; nested, exceptional, and cancelled scopes restore both independent ContextVar values.
    - The AST test proves public-boundary consumer installation, preparation-only marker installation, producer/consumer comparison in `_has_prepared_declarative_guard`, and unchanged public subclass hook argument contracts.
  </acceptance_criteria>
  <verify>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_ownership_concurrency.py --include tests/test_mypyc_guard.py --include tools/phase16_isolated_verify.py -- uv run pytest tests/test_ownership_concurrency.py tests/test_mypyc_guard.py -x -q -k "declarative_marker or prepared_declarative_marker or cross_machine_consumer"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_ownership_concurrency.py --include tests/test_mypyc_guard.py --include tools/phase16_isolated_verify.py -- uv run pytest tests/test_ownership_concurrency.py tests/test_mypyc_guard.py -x -q -k "declarative_marker or prepared_declarative_marker or cross_machine_consumer"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_ownership_concurrency.py --include tests/test_mypyc_guard.py --include tools/phase16_isolated_verify.py -- uv run ruff check src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_mypyc_guard.py</automated>
  </verify>
  <commit>Use two exact-path atomic commits: first `test(18-08): expose cross-machine declarative marker consumption` with only the two test files in the known-RED state, then `fix(18-08): qualify declarative marker consumption by machine` with only `src/fast_fsm/core.py` after both fresh origins pass. Never use broad staging.</commit>
  <done>T-18-05 has a current-code RED and fresh-pure/native GREEN through real nested public dispatch: independently sourced consumer identity is compared with preparation provenance, with no public hook or global-registry regression.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh fresh-origin evidence and require the exact-SHA hosted native matrix</name>
  <files>evidence/release-baseline.json</files>
  <read_first>tools/phase16_isolated_verify.py:52-108, tools/phase16_isolated_verify.py:736-873, tools/phase16_isolated_verify.py:972-1048, tools/phase18_native_probe.py:1-48, tools/phase18_native_probe.py:341-477, evidence/release-baseline.json, .planning/phases/18-safe-ownership-concurrency/18-07-SUMMARY.md</read_first>
  <precondition>Task 1's RED and GREEN commits are the clean execution worktree's current `EXEC_BRANCH` tip, `origin/main` and GitHub CI are reachable, and authentication failures use the executor's standard dynamic auth checkpoint.</precondition>
  <action>Remain inside `WORKTREE_DIR`; do not run synchronization or evidence commands in `SOURCE_ROOT`. Before candidate construction, run `git pull --rebase origin main` and then `bd sync` in the clean execution worktree, inspect the resulting commit graph and exact-path diff, and confirm `git status --porcelain` contains only Plan 18-08-owned paths. If synchronization rewrites Task 1 or changes a runtime, test, harness, workflow, or baseline input, rerun Task 1's pure/native focused gates. Before any multi-minute baseline/full/hosted gate, run the dedicated fast precheck command from `<verify>`; it must pass within 30 seconds and cover both cross-machine consumer regressions plus the independent-consumer AST contract. Generate the baseline with this exact command: `uv run python tools/phase16_isolated_verify.py --suite baseline-write --manifest-output evidence/release-baseline.json`. Review `git diff -- evidence/release-baseline.json` in full, run `git diff --check -- evidence/release-baseline.json`, compare it with the pre-write manifest, and reject unexplained decreases in passed/collected tests or coverage; never hand-edit generated observations. Run `uv run python tools/phase16_isolated_verify.py --suite baseline-check` and the authoritative `--suite phase18`, which must execute the new regressions from asserted fresh pure and freshly compiled origins plus release, Ruff, blocking mypy, advisory ty, docs, doctests, slots, and compiled performance gates. Commit only the reviewed baseline. Immediately capture `CANDIDATE_SHA="$(git rev-parse HEAD)"`, validate it against `^[0-9a-f]{40}$`, and retain that literal with `EXEC_BRANCH` for Task 3. Push with `git push -u origin "$EXEC_BRANCH"`, obtain the remote execution-branch tip with `git ls-remote --heads origin "refs/heads/$EXEC_BRANCH"`, and require exact equality with `CANDIDATE_SHA` before accepting hosted evidence. Wait for that exact commit's workflow through GitHub CLI event/status commands without sleep polling, then run `uv run python tools/phase18_native_probe.py --assert-hosted-ci-sha "$CANDIDATE_SHA"`; `HEAD`, a branch name, a later documentation SHA, or any other ref is forbidden for this proof. T-18-03 stays open until the helper confirms one successful Python 3.10-3.14 native matrix for that literal. After capture, changes to candidate-owned runtime/tests/baseline/harness/workflow invalidate the candidate: pull/rebase and sync only in the clean worktree, rerun the fast precheck, regenerate the baseline, rerun all local evidence, recapture a new 40-character SHA, re-prove the remote execution-branch tip, and rerun hosted evidence. The source checkout remains untouched throughout.</action>
  <acceptance_criteria>
    - The regenerated baseline records all newly collected tests with zero failures/errors and does not lower a durable test or coverage floor without the repository's separately reviewed migration contract.
    - `uv run python tools/phase16_isolated_verify.py --suite phase18` exits zero after the baseline refresh and proves both fresh origins plus the complete Phase 18 quality/performance contract.
    - A validated 40-character implementation/evidence SHA is captured only after all implementation and baseline commits; the remote branch tip equals it before hosted proof, and one completed successful CI run for that same literal contains successful native ownership jobs for Python 3.10, 3.11, 3.12, 3.13, and 3.14.
    - No later documentation commit is substituted for the candidate SHA and no stale main-checkout native artifact is loaded, rebuilt in place, removed, or used as evidence.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import subprocess; subprocess.run(['uv', 'run', 'pytest', 'tests/test_ownership_concurrency.py', 'tests/test_mypyc_guard.py', '-x', '-q', '-k', 'cross_machine_consumer or prepared_declarative_marker'], check=True, timeout=30)"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --suite baseline-write --manifest-output evidence/release-baseline.json</automated>
    <automated>git diff --check -- evidence/release-baseline.json</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --suite phase18</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --suite baseline-check</automated>
    <automated>CANDIDATE_SHA="$(git rev-parse HEAD)"; printf '%s' "$CANDIDATE_SHA" | uv run python -c "import re, sys; value = sys.stdin.read(); assert re.fullmatch(r'[0-9a-f]{40}', value)"; EXEC_BRANCH="$(git branch --show-current)"; REMOTE_SHA="$(git ls-remote --heads origin "refs/heads/$EXEC_BRANCH" | awk '{print $1}')"; test "$REMOTE_SHA" = "$CANDIDATE_SHA"; uv run python tools/phase18_native_probe.py --assert-hosted-ci-sha "$CANDIDATE_SHA"</automated>
  </verify>
  <commit>After the full fresh-origin suite passes, stage only `evidence/release-baseline.json` and commit `chore(18-08): refresh marker isolation release baseline`; that commit's exact SHA is the candidate to push and verify. Do not amend it after hosted CI starts.</commit>
  <done>The clean-worktree candidate passed the under-30-second precheck and all full gates, is a captured 40-character SHA equal to the remote `EXEC_BRANCH` tip, and has hosted native evidence under that same explicit value for independent audit.</done>
</task>

<task type="auto">
  <name>Task 3: Reaudit the exact candidate, refresh Nyquist mapping, and land</name>
  <files>.planning/phases/18-safe-ownership-concurrency/18-SECURITY.md, .planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md</files>
  <read_first>.planning/phases/18-safe-ownership-concurrency/18-SECURITY.md, .planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md, .planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md, .planning/phases/18-safe-ownership-concurrency/18-05-SUMMARY.md, src/fast_fsm/core.py:46-137, src/fast_fsm/core.py:1693-1873, src/fast_fsm/core.py:2659-2865, src/fast_fsm/core.py:3394-3569, tests/test_ownership_concurrency.py:430-566, tests/test_mypyc_guard.py:601-649, tools/phase18_native_probe.py:500-613, evidence/release-baseline.json</read_first>
  <precondition>Task 2 left its captured candidate as the current `HEAD`, proved the remote branch tip equals it, and obtained a successful exact-SHA hosted Python 3.10-3.14 native matrix after all refreshed local evidence passed.</precondition>
  <action>Remain inside the clean execution worktree. At task entry recapture current `HEAD` as the 40-character `CANDIDATE_SHA`, recapture `EXEC_BRANCH`, and require the remote execution-branch tip to equal it. Invoke `$gsd-secure-phase 18` so its orchestrating agent dispatches the read-only `gsd-security-auditor` with `candidate_sha=$CANDIDATE_SHA`, `execution_branch=$EXEC_BRANCH`, and the runtime/test/baseline/hosted artifacts. Require the auditor to return structured data containing `verdict` (`SECURED`, `OPEN_THREATS`, or `ESCALATE`), the exact 40-character `candidate_sha`, `threats_open`, explicit T-18-03/T-18-05 statuses, evidence commands/results, and sign-off text. The auditor must trace public consumer installation through preparation provenance and `_has_prepared_declarative_guard`, review the genuine nested public-dispatch sync/async RED/GREEN and AST assertions, independently run the focused fresh-origin checks, and invoke `uv run python tools/phase18_native_probe.py --assert-hosted-ci-sha "$CANDIDATE_SHA"`; it must not write `18-SECURITY.md` or any other file. The secure-phase/orchestrating agent must validate that the returned `candidate_sha` exactly equals its input and that the verdict/status/count fields are internally consistent, then mechanically persist those returned fields and evidence into `18-SECURITY.md`, including the exact line `Audited candidate SHA: $CANDIDATE_SHA` with the literal substituted. It must not independently choose, upgrade, or reinterpret a verdict or threat status. A returned `OPEN_THREATS` or `ESCALATE` is persisted as blocked and stops phase closure; only returned `SECURED` may produce `status: verified`, zero open threats, and closed T-18-03/T-18-05 rows. Commit the mechanically persisted security record without substituting a later documentation SHA. Refresh `18-VALIDATION.md` in a separate exact-path commit: add rows `18-08-01`, `18-08-02`, and `18-08-03` mapping Task 1's fresh public-dispatch checks, Task 2's bounded precheck plus exact baseline/local/hosted chain, and Task 3's read-only audit/persistence/SHA equality checks; record the same candidate literal and `EXEC_BRANCH`; reconcile earlier rows against existing summaries/tests; set `status: validated`, `wave_0_complete: true`, and `nyquist_compliant: true` only for a returned `SECURED` verdict with every mapped command passing. Confirm the source checkout's staged/unstaged/untracked paths were never acted on, create `18-08-SUMMARY.md` on the execution branch with `EXEC_BRANCH`, candidate SHA, source checkout preservation, and worktree location, and commit it by exact path. Perform the definitive mandatory landing sequence inside `WORKTREE_DIR` and in this order: `git pull --rebase`, `bd sync`, candidate-integrity checks, `git push`, then `git status --short --branch`. After pull/sync and before push, parse `CANDIDATE_SHA` from SECURITY, require it to remain an ancestor, and require `git diff --exit-code "$CANDIDATE_SHA" -- src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_mypyc_guard.py tools/phase16_isolated_verify.py tools/phase18_native_probe.py evidence/release-baseline.json` to be empty. If pull/sync rewrites or removes the candidate or changes a candidate-owned path, return to Task 2 in the same clean worktree and repeat bounded precheck, baseline, all full evidence, candidate capture/push, hosted proof, read-only audit, persistence, validation, and summary with the new literal. After push, require local `HEAD` to equal `origin/$EXEC_BRANCH`; the final status output must report that branch up to date. Keep the worktree registered through every `<verify>` command. Only after verification succeeds may the orchestrating agent remove that exact clean `WORKTREE_DIR` without force; if it is not clean or no longer matches the pushed execution branch, retain it and report its exact path.</action>
  <acceptance_criteria>
    - The read-only auditor returns a structured verdict and the exact Task 2 SHA; it writes no files. The secure-phase/orchestrating agent verifies SHA equality and mechanically persists that unchanged verdict/SHA/evidence into `18-SECURITY.md` without choosing statuses.
    - A verified status, zero open threats, closed T-18-03/T-18-05 rows, and completed sign-off appear only when the auditor's verdict is `SECURED`; otherwise the phase remains blocked.
    - `18-VALIDATION.md` contains explicit passing rows for all three Plan 18-08 tasks and declares final Nyquist compliance only after local, hosted, and audit checks pass.
    - The audit trail distinguishes the implementation/evidence candidate SHA from later security/validation commits; the clean-worktree pull/rebase, beads sync, push, and status sequence preserves that candidate and its exact-path content or triggers the complete evidence cycle again.
    - The dirty source checkout retains all unrelated staged, unstaged, and untracked work; only the dedicated clean execution worktree is removed, and only after it is fully committed, pushed, and clean.
  </acceptance_criteria>
  <verify>
    <automated>CANDIDATE_SHA="$(uv run python -c 'import re; from pathlib import Path; text = Path(".planning/phases/18-safe-ownership-concurrency/18-SECURITY.md").read_text(); match = re.search(r"^Audited candidate SHA: ([0-9a-f]{40})$", text, re.MULTILINE); assert match; print(match.group(1))')"; uv run python tools/phase18_native_probe.py --assert-hosted-ci-sha "$CANDIDATE_SHA"; uv run python -c "import re, sys; from pathlib import Path; candidate = sys.argv[1]; text = Path('.planning/phases/18-safe-ownership-concurrency/18-SECURITY.md').read_text(); assert re.fullmatch(r'[0-9a-f]{40}', candidate); assert f'Audited candidate SHA: {candidate}' in text; assert 'status: verified' in text and 'threats_open: 0' in text; assert re.search(r'T-18-03.*closed', text) and re.search(r'T-18-05.*closed', text)" "$CANDIDATE_SHA"</automated>
    <automated>uv run python -c "from pathlib import Path; text = Path('.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md').read_text(); assert all(task in text for task in ('18-08-01', '18-08-02', '18-08-03')); assert 'status: validated' in text; assert 'wave_0_complete: true' in text; assert 'nyquist_compliant: true' in text"</automated>
    <automated>BRANCH_NAME="$(git branch --show-current)"; git fetch origin "$BRANCH_NAME"; test "$(git rev-parse HEAD)" = "$(git rev-parse "origin/$BRANCH_NAME")"; git status --short --branch</automated>
  </verify>
  <commit>After the read-only auditor returns, the secure-phase/orchestrating agent stages exactly its mechanically persisted `18-SECURITY.md` and commits `docs(18-08): record independent ownership security reaudit`; only for a returned `SECURED` verdict, stage exactly `18-VALIDATION.md` and commit `docs(18-08): refresh Nyquist validation map`. Persist non-secured audit results as blocked without manufacturing closure. Never use broad staging.</commit>
  <done>The read-only auditor's exact candidate/verdict is mechanically persisted by the orchestrating agent, SECURITY and Nyquist artifacts name/map the same evidence, the clean execution branch is pushed and up to date after the mandated landing order, and the unrelated source checkout remains untouched.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-05 | Tampering | Declarative prepared-marker consumption | high | mitigate | Install consumer identity independently at every enclosing public machine-dispatch boundary, keep marker helpers preparation-only, compare the two in `_has_prepared_declarative_guard`, and prove the current-code RED with nested cross-machine public dispatch over exact shared State identities in fresh pure/native origins. |
| T-18-03 | Tampering/DoS | Hosted native ownership evidence | high | mitigate | Synchronize first, generate/review the baseline with the exact write command, capture the post-commit 40-character SHA, prove it is the remote tip, use it explicitly for hosted proof and auditor input, and require SECURITY to record the same value. |
| T-18-09 | Denial of Service | Scheduling/offload expectations | medium | accept | This correction changes only private marker qualification and evidence; the documented no-fairness, no-timeout, no-transfer, and no-implicit-offload boundary remains unchanged. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / decision group | Plans | Status | Notes |
|--------|----|--------------------------|-------|--------|-------|
| GOAL | - | Safe-by-default reentrant/concurrent use without global locks or event-loop blocking | 01-08 | COVERED | Plan 08 closes the post-audit marker/evidence blockers. |
| REQ | OWN-01 | Immediate pre-acquisition reentry rejection | 01-08 | COVERED | Revalidated by the complete fresh-origin suite. |
| REQ | OWN-02 | Per-machine independent-thread serialization | 01-08 | COVERED | Revalidated in pure/native suite and hosted matrix. |
| REQ | OWN-03 | Same-loop task serialization without loop blocking | 01-08 | COVERED | Revalidated in pure/native suite and hosted matrix. |
| REQ | OWN-04 | Explicit permanent cross-loop rejection | 01-08 | COVERED | T-18-03 exact-SHA hosted proof is renewed after the fix. |
| REQ | OWN-05 | Trigger/control/topology/history/registrar ownership and declarative isolation | 01-08 | COVERED | Plan 08 makes machine identity participate in marker consumption. |
| REQ | OWN-06 | Release after Exception/BaseException/cancellation | 01-08 | COVERED | ContextVar pair restoration and complete suite retain cleanup boundaries. |
| REQ | OWN-07 | Inline sync callbacks and event-loop-safe async control | 01-08 | COVERED | Public hook and callback contracts remain unchanged. |
| CONTEXT | D-01-D-04 | RuntimeError boundary, precheck, callback classification, redaction | 01-07; revalidated 08 | COVERED | No observable error/result change. |
| CONTEXT | D-05-D-08 | Per-instance sync serialization and full exception-safe envelope | 01-07; revalidated 08 | COVERED | No synchronization primitive or envelope change. |
| CONTEXT | D-09-D-13 | Loop/task/root ownership and inline callbacks | 01-07; revalidated 08 | COVERED | Async marker regression runs inside existing loop contract. |
| CONTEXT | D-14 | Every public write and exact machine-qualified declarative isolation | 01-05,07-08 | COVERED | Task 1 separates enclosing public-dispatch consumer identity from preparation-marker provenance and compares them at consumption. |
| CONTEXT | D-15 | Preserve read/factory/public-hook boundaries | 01-07; guarded 08 | COVERED | AST freezes public subclass signatures and prohibits hidden kwargs. |
| CONTEXT | D-16 | Deterministic tests, fresh origins, supported native matrix, performance floor | 01-08 | COVERED | Tasks 1-3 renew all local/hosted evidence and the Nyquist map after the code/test change. |
| RESEARCH | declarative marker | Context-local machine-qualified preparation and consumption | 05,07-08 | COVERED | Producer and consumer identity are installed at independent seams and compared during consumption. |
| RESEARCH | representation evidence | Fresh pure/native plus exact-SHA Python 3.10-3.14 matrix | 01,07-08 | COVERED | The synchronized candidate is captured after all implementation/baseline commits, proven as remote tip, and passed unchanged through hosted/auditor/security evidence. |
| SECURITY | T-18-05 | Machine-qualified declarative prepared marker | 08 | COVERED | Genuine nested public-dispatch behavior + AST mitigation, independently audited. |
| SECURITY | T-18-03 | Exact-current-SHA native matrix | 08 | COVERED | Remains open until the explicit SHA passes hosted proof, auditor input, and SECURITY equality checks. |
| VALIDATION | Nyquist | Plan 18-08 task map and final compliance | 08 | COVERED | Task 3 adds all three rows, reconciles prior status, and gates compliance on local/hosted/audit success. |

Deferred queued reentry (FUTR-01), cross-loop transfer (FUTR-03), automatic callback offload (FUTR-04), Phase 19 diagnostic snapshots, and Phase 20 installed wheel/sdist parity remain exclusions and are not implemented by this gap plan.
</source_audit>

<verification>From fresh exported trees, prove the production-reachable nested public-dispatch marker mismatch RED/GREEN in pure and compiled modes; run the exact baseline generation command and review/check its diff; run the complete Phase 18 suite; prove one captured 40-character SHA equals the remote branch tip and is passed explicitly to hosted CI, the independent auditor, and SECURITY; refresh the Plan 18-08 Nyquist map; then complete pull/rebase, beads sync, push, and an up-to-date branch assertion without changing candidate-owned content.</verification>
<success_criteria>Preparation provenance and active consumer identity are independently established and compared, exact shared State objects cannot collide across nested machine dispatch in sync or async execution, one mechanically linked implementation/evidence SHA passes all local/hosted/audit gates, SECURITY records that exact value, Nyquist compliance is current, and the repository is pushed and up to date.</success_criteria>
<output>Create and push `.planning/phases/18-safe-ownership-concurrency/18-08-SUMMARY.md` from the clean execution branch before its definitive landing sequence; after all verification succeeds, remove only the clean registered execution worktree without force, or retain and report it if cleanup preconditions fail.</output>
