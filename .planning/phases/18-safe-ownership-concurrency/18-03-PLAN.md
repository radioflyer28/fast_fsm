---
phase: 18-safe-ownership-concurrency
plan: 03
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - src/fast_fsm/core.py
  - .specify/memory/spr-core-api.md
  - tests/test_ownership_concurrency.py
  - tests/test_async.py
  - tests/test_transition_lifecycle.py
autonomous: true
requirements: [OWN-01, OWN-02, OWN-03, OWN-04, OWN-05, OWN-06, OWN-07]
estimate: {tokens: 34000, raw_tokens: 34000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Per D-09 and D-10, the first async control operation permanently binds its machine to one running loop; independent tasks in that loop await one per-instance asyncio lock without blocking the loop, while foreign/closed-loop access fails before acquisition or preparation."
    - "Per D-11, a same-machine child task inheriting the active causal root raises immediately instead of waiting behind its parent; independently created tasks serialize normally."
    - "Per D-12, inherited synchronous mutators are protected before binding and, after binding, are accepted only from the bound loop thread while idle; they never block the event-loop thread."
    - "Per D-13 and D-16, sync callbacks stay inline on the loop thread, async callbacks remain same-slot awaited, and deterministic cancellation tests preserve Phase 17 boundaries while releasing task/root/lock state."
  artifacts:
    - {path: "src/fast_fsm/core.py", provides: "Permanent loop binding, asyncio-native serialization, causal-root ownership, and mixed sync-mutator admission"}
    - {path: "tests/test_ownership_concurrency.py", provides: "Same-loop, cross-loop, causal-child, mixed-mode, and cancellation matrix"}
    - {path: "tests/test_async.py", provides: "Inline callback thread identity and async API regressions"}
  key_links:
    - {from: "AsyncStateMachine.trigger_async", to: "asyncio.Lock", via: "loop/root precheck then async-with owner scope", pattern: "_acquire_async_ownership"}
    - {from: "callback-created task", to: "active machine owner", via: "module ContextVar root identity", pattern: "_ownership_root"}
    - {from: "AsyncStateMachine sync writers", to: "bound-loop policy", via: "overridden sync admission hook", pattern: "_acquire_sync_ownership"}
  prohibitions:
    - "[UNVERIFIED - no SPEC] Do not turn per-machine serialization into a global lock, hidden fair queue, timeout, or scheduler-order promise."
    - "[UNVERIFIED - no SPEC] Do not silently rebind an async machine to another event loop or implicitly offload synchronous callbacks to worker threads."
    - "[UNVERIFIED - no SPEC] Do not disclose trigger arguments, keyword values, callback payloads, stored causes, or arbitrary exception text in ownership errors or logs."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow cancellation, and do not roll back Phase 17 state/history after commit."
---

<objective>
Implement permanent loop ownership, non-blocking same-loop serialization, and causal child-task reentry rejection.

Purpose: Make `AsyncStateMachine` safe under task contention, cross-loop misuse, mixed sync writes, callback-created child tasks, and cancellation without changing Phase 17 callback slots.
Output: Async ownership metadata/helpers, mixed-mode admission policy, and deterministic async/cross-loop/cancellation proof.
</objective>

## Artifacts this phase produces

- New private runtime symbols in `src/fast_fsm/core.py`: module causal/prepared-marker ContextVars; base sync ownership slots/helpers; `AsyncStateMachine._async_ownership_lock`, `_bound_loop`, `_bound_loop_thread_id`, `_async_owner_task`, `_async_owner_root`, async admission/release helpers; private owned trigger/control bodies.
- New files: `tests/test_ownership_concurrency.py`, `18-PERFORMANCE-EVIDENCE.md`, and `ADR-005-safe-ownership-concurrency.md`.
- Extended `phase18` verification suite; no new public symbol, dependency, schema, or external integration.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md
@.planning/phases/18-safe-ownership-concurrency/18-RESEARCH.md
@.planning/phases/18-safe-ownership-concurrency/18-PATTERNS.md
@.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md
@.planning/phases/18-safe-ownership-concurrency/18-02-SUMMARY.md
@.planning/phases/17-atomic-transition-lifecycle/17-VERIFICATION.md
@src/fast_fsm/core.py

<interfaces>
- Add module-level `_ownership_root: ContextVar[Optional[object]]` and use explicit `set()`/`reset(token)` for Python 3.10 through 3.14 compatibility.
- `can_trigger_async()` performs permanent loop bind/check because it is an async control operation, but remains read-only and does not acquire write ownership per D-09 and D-15.
- `trigger_async()` checks loop identity and causal root before awaiting `_async_ownership_lock`; owner task/root are installed only after acquisition and cleared in `finally` before lock exit.
- Async and pre-bind sync admission use one short non-awaiting per-instance metadata gate per the resolved D-12 race design. The gate installs a sync-owner reservation or permanent loop/async reservation atomically, is never held across an await or lifecycle, and is never waited on by the event-loop thread.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind one loop and serialize independent tasks without blocking it</name>
  <reversibility rating="costly">D-09 and D-10 establish permanent loop identity and same-loop waiting semantics visible to all async callers.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_ownership_concurrency.py, tests/test_async.py, .specify/memory/spr-core-api.md</files>
  <read_first>src/fast_fsm/core.py:2721-2802, src/fast_fsm/core.py:3033-3238, tests/test_async.py:621-938, .planning/phases/18-safe-ownership-concurrency/18-02-SUMMARY.md</read_first>
  <behavior>
    - First `can_trigger_async()` or `trigger_async()` binds the exact running loop and its thread; same-loop reuse succeeds, and a different or closed original loop raises before preparation with the binding unchanged.
    - Two independent same-loop tasks serialize one full lifecycle through an asyncio-native per-machine lock while a heartbeat/handshake proves the event loop continues to run.
    - Cancellation while waiting installs no owner; cancellation while owning finalizes/re-raises exactly per Phase 17 and releases owner/root/lock for immediate reuse.
  </behavior>
  <action>Remove the `RED until Plan 18-03` strict-xfail markers from loop binding, same-loop contention, waiting/owning cancellation, and mixed-mode race rows, run them to record RED, then add slotted per-instance async lock, bound-loop object/thread identity, active task, and active root fields to `AsyncStateMachine`, initialized independently in constructor and clone. Implement eager `_bind_or_check_async_loop(operation)` using `asyncio.get_running_loop()` and exact object identity before the async lock is touched. Implement the resolved D-12 race design with one short non-awaiting per-instance admission gate: a sync writer installs its active reservation under the gate and releases it before its lifecycle; the first async operation attempts the gate without blocking the event loop, rejects with a stable busy-category `RuntimeError` when a sync writer is active, or records permanent loop binding plus its async reservation before release. Never hold this gate across an await, callback, lifecycle, or mutation body. After binding, foreign threads/loops reject and bound-loop synchronous writes require idle async ownership. Wrap the existing `trigger_async()` body in `async with` so D-09 and D-10 hold: independent tasks await, owner metadata is installed only after acquisition, cancellation while waiting changes nothing, and an inner `finally` clears metadata for every success/failure/BaseException/cancellation. Make `can_trigger_async()` bind/check without write ownership. Preserve the existing cancellation finalizer and bare re-raise, commit/history boundaries, and callback order. Use only Events, explicit tasks, loop threads, and watchdog timeouts; timeouts detect hangs but do not establish ordering. Update the SPR with the implemented loop/task/cancellation contract.</action>
  <acceptance_criteria>
    - The machine never rebinds, even after its original loop closes, and foreign-loop attempts execute no transition preparation.
    - Same-loop contenders serialize while an independent heartbeat progresses; there is never a thread lock held across await.
    - Waiting and owning cancellation both leave the machine reusable and preserve the exact reached state/history/failure-observer boundary.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_async.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_async.py</automated>
    <automated>uv run pytest tests/test_ownership_concurrency.py tests/test_async.py tests/test_transition_lifecycle.py -x -q -k "same_loop or cross_loop or waiting or cancellation or heartbeat"</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python tools/release_evidence.py slots-policy --json</automated>
  </verify>
  <done>An async machine permanently belongs to one loop, serializes independent tasks without blocking that loop, and releases ownership at every Phase 17 cancellation boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject causal child reentry and enforce bound async-machine sync writes</name>
  <reversibility rating="costly">D-11 through D-13 define child-task, mixed sync/async, and callback-thread semantics across the public API.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_ownership_concurrency.py, tests/test_async.py, tests/test_transition_lifecycle.py, .specify/memory/spr-core-api.md</files>
  <read_first>src/fast_fsm/core.py:510-588, src/fast_fsm/core.py:2721-3238, tests/test_transition_lifecycle.py:742-1019, tests/test_async.py:621-938</read_first>
  <behavior>
    - A same-machine child task created inside an owned callback inherits the causal root and raises before waiting; nested work on a different machine remains allowed.
    - Before loop binding, inherited sync mutators use normal per-instance sync protection; after binding, only the bound loop thread while idle may run them, and foreign threads or active async ownership fail immediately.
    - Synchronous callbacks execute inline on the bound loop thread and async callbacks are awaited at their existing Phase 17 slots; no executor or worker-thread offload occurs.
  </behavior>
  <action>Remove the remaining `RED until Plan 18-03` strict-xfail markers for causal-child and inline/no-offload behavior, run them to record RED, then create the module-level causal-root `ContextVar` and explicit token reset compatible with Python 3.10. A top-level owner creates a private root; nested different-machine operations reuse it; each active machine stores the root it admitted. Before async lock acquisition, compare the inherited root with this machine's active root so callback-created child tasks and direct awaited reentry raise immediately per D-11 instead of deadlocking. Specialize the shared short admission gate for `AsyncStateMachine` so D-12 is exact: unbound machines permit protected configuration through an atomic reservation; bound machines permit sync writes only from the recorded loop thread while no async owner is active; every other context fails without waiting on the loop. Retain inline synchronous callbacks and same-slot awaited async callbacks per D-13; add thread-identity and structural no-offload assertions. Cover nested other-machine success, child-task immediate rejection, bound-loop idle sync writes, foreign thread/loop rejection, and active-operation registration/mutation rejection with explicit handshakes. Update the SPR with causal-root, mixed-mode, and inline-callback invariants.</action>
  <acceptance_criteria>
    - The parent-awaits-child scenario returns a redacted `RuntimeError` without timeout/deadlock, and another machine can be called within the same causal root.
    - Every D-12 unbound/bound-thread/idle/active/foreign case has a deterministic assertion and the loop is never synchronously blocked.
    - Sync callback thread identity equals the bound loop thread and async callback slot/order remains unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_async.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_ownership_concurrency.py tests/test_async.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run pytest tests/test_ownership_concurrency.py tests/test_async.py tests/test_transition_lifecycle.py -x -q -k "causal or child_task or sync_mutator or inline or callback or async_release"</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python -c "from pathlib import Path; text=Path('src/fast_fsm/core.py').read_text(); assert 'ContextVar' in text; assert 'to_thread(' not in text; assert 'run_in_executor(' not in text"</automated>
  </verify>
  <done>Causal same-machine child tasks fail instead of deadlocking, mixed sync writes obey the complete D-12 policy, and async callbacks retain the locked inline/same-slot contract.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | async reentry | high | mitigate | Task/root checks occur before lock and preparation. |
| T-18-02 | DoS | async lock/owner leak | high | mitigate | `async with` plus inner metadata finally and immediate reuse tests. |
| T-18-03 | Tampering/DoS | cross-loop access | high | mitigate | Permanent loop identity and foreign/closed-loop no-preparation tests. |
| T-18-04 | DoS | causal child task | high | mitigate | Inherited ContextVar root rejects before await. |
| T-18-07 | DoS/Tampering | cancellation windows | high | mitigate | Event-handshaken waiting/owning pre/post-commit cancellation matrix. |
| T-18-08 | Information Disclosure | async ownership errors | high | mitigate | Stable categories only; secret-sentinel coverage shares Wave 0 helper. |
| T-18-09 | DoS | fairness/offload expectation | medium | accept | Only non-blocking loop progress and exclusion are promised; no fairness, timeout, or automatic offload. |
</threat_model>

<verification>Run deterministic same-loop, loop-thread, causal-root, cross-loop, cancellation, callback-order, slots, Ruff, and mypy tests.</verification>
<success_criteria>OWN-03, OWN-04, OWN-06, and OWN-07 are executable end to end; the event loop never blocks on a thread lock; cross-loop and causal misuse fail before mutation; Phase 17 cancellation semantics remain exact.</success_criteria>
<output>Create `.planning/phases/18-safe-ownership-concurrency/18-03-SUMMARY.md` when done.</output>
