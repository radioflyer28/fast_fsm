---
phase: 18-safe-ownership-concurrency
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - .specify/memory/spr-core-api.md
  - .github/workflows/ci.yml
  - tests/test_ownership_concurrency.py
  - tests/test_mypyc_guard.py
  - tests/test_performance_benchmarks.py
  - tools/phase18_native_probe.py
  - tools/phase16_isolated_verify.py
  - .planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md
autonomous: true
requirements: [OWN-01, OWN-02, OWN-03, OWN-04, OWN-05, OWN-06, OWN-07]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-01, D-02, D-04, D-05, D-06, D-07, and D-08, one public sync trigger is admitted exactly once, same-thread reentry raises a redacted RuntimeError before preparation, an independent thread serializes for the full lifecycle, and ownership releases after every throwable."
    - "Per D-16, Wave 0 provides deterministic Event/Barrier/async-handshake scenario infrastructure for all OWN-01 through OWN-07 edges, without timing sleeps as correctness evidence."
    - "The tracer preserves Phase 17 state/history/failure-finalizer truth and proves per-instance rather than global serialization."
  artifacts:
    - path: "tests/test_ownership_concurrency.py"
      provides: "Authoritative deterministic OWN-01 through OWN-07 scenario matrix"
    - path: "src/fast_fsm/core.py"
      provides: "Production sync trigger ownership tracer and reusable private sync admission seam"
    - path: "tools/phase18_native_probe.py"
      provides: "Compile-first ownership representation probe for the current interpreter and supported-version CI matrix"
    - path: ".github/workflows/ci.yml"
      provides: "Python 3.10-3.14 native ownership-probe matrix contract established before representation expansion"
    - path: "tools/phase16_isolated_verify.py"
      provides: "Backward-compatible phase18 task/suite selection with asserted origins"
    - path: ".planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md"
      provides: "Environment-labelled before/tracer/final ownership observations"
  key_links:
    - from: "StateMachine.trigger"
      to: "Phase 17 preparation/lifecycle/finalizer"
      via: "one acquire/try/finally/release envelope"
      pattern: "_acquire_sync_ownership|_release_sync_ownership"
    - from: "tests/test_ownership_concurrency.py"
      to: "StateMachine.trigger"
      via: "threading.Event and Barrier-controlled reentry/contention"
      pattern: "reentrant|thread"
    - from: "tools/phase16_isolated_verify.py"
      to: "Phase 18 runtime/tests/evidence"
      via: "explicit overlay inventory and origin assertions"
      pattern: "phase18"
  prohibitions:
    - "[UNVERIFIED - no SPEC] Do not turn per-machine serialization into a global lock, hidden fair queue, timeout, or scheduler-order promise."
    - "[UNVERIFIED - no SPEC] Do not silently rebind an async machine to another event loop or implicitly offload synchronous callbacks to worker threads."
    - "[UNVERIFIED - no SPEC] Do not disclose trigger arguments, keyword values, callback payloads, stored causes, or arbitrary exception text in ownership errors or logs."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow cancellation, and do not roll back Phase 17 state/history after commit."
---

<objective>
Establish the production sync ownership tracer and the complete deterministic, compile-first Wave 0 contract.

Purpose: Prove the hardest architectural constraint—pre-acquisition reentry rejection plus full-operation per-instance serialization—before expanding ownership to async and every write family.
Output: One end-to-end sync trigger path, a locally proven native ownership representation plus supported-version matrix contract, reusable strict-RED ownership inventory, staged structural/performance guards, and fresh-origin harness support.
</objective>

## Artifacts this phase produces

- New private runtime symbols in `src/fast_fsm/core.py`: `_ownership_root`, `_prepared_declarative_guard`, `StateMachine._sync_ownership_lock`, `StateMachine._sync_owner_thread_id`, `_acquire_sync_ownership()`, `_release_sync_ownership()`, `_trigger_owned()`, `_force_state_owned()`, and AsyncStateMachine loop/lock/task/root ownership slots and helpers.
- New test/evidence files: `tests/test_ownership_concurrency.py`, `.planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md`.
- New decision record: `.specify/decisions/ADR-005-safe-ownership-concurrency.md`.
- Extended `phase18` verification suite in `tools/phase16_isolated_verify.py`; no public symbol, schema, dependency, or external API is added.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md
@.planning/phases/18-safe-ownership-concurrency/18-RESEARCH.md
@.planning/phases/18-safe-ownership-concurrency/18-PATTERNS.md
@.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md
@.planning/phases/17-atomic-transition-lifecycle/17-VERIFICATION.md
@src/fast_fsm/core.py
@tools/phase16_isolated_verify.py

<interfaces>
- `StateMachine.trigger(trigger, *args, **kwargs) -> TransitionResult` remains value-returning for ordinary transition failures; ownership misuse raises `RuntimeError` before `_prepare_transition()`.
- `_commit_transition()` remains the only state/history commit seam and `_finalize_failure()` remains the single ordinary failure-observer boundary from Phase 17.
- The chosen private sync contract is `_acquire_sync_ownership(operation: str) -> int` followed by `_release_sync_ownership(owner_thread_id: int) -> None`; public writers call a private already-owned body exactly once inside `try/finally`.
- Ownership error text uses a fixed operation/category vocabulary such as `reentrant`, `cross-loop`, `foreign-thread`, or `busy`; it never formats caller data or arbitrary exception text.
</interfaces>

<flagged_assumptions>
- [ASSUMPTION - no SPEC, probe 1/OWN-01 unclassified] Resolve with direct and callback-originated same-owner calls that assert rejection before preparation and no nested mutation.
- [ASSUMPTION - no SPEC, probe 2/OWN-02 unclassified] Resolve with two coordinated threads on one machine plus two independently progressing machines.
- [ASSUMPTION - no SPEC, probe 3/OWN-03 unclassified] Resolve with same-loop task contention and an independently advancing heartbeat.
- [ASSUMPTION - no SPEC, probe 4/OWN-04 unclassified] Resolve with first-use binding plus foreign and closed-loop attempts that run no preparation.
- [ASSUMPTION - no SPEC, probe 5/OWN-05 unclassified] Resolve with a table enumerating every exact public write family and one-entry/private-body structure.
- [ASSUMPTION - no SPEC, probe 6/OWN-06 boundary] Resolve with success, Exception, KeyboardInterrupt, SystemExit, waiter cancellation, and owner cancellation immediately followed by successful reuse, at Phase 17 pre/post-commit boundaries.
- [CLASSIFICATION MISMATCH - no SPEC, probe 7/OWN-06 precision] Numeric precision is irrelevant to ownership cleanup; retain this flagged row and do not invent a numeric contract.
- [ASSUMPTION - no SPEC, probe 8/OWN-07 unclassified] Resolve with exact loop-thread identity, same-slot await ordering, and absence of implicit executor/offload calls.
- Probe accounting: 8 surfaced = 7 authored acceptance families + 1 retained classification mismatch + 0 silent drops.
</flagged_assumptions>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Tracer-first native representation and sync trigger lifecycle</name>
  <reversibility rating="costly">D-01 and D-06 define public control flow and scheduling semantics that later callers will depend on.</reversibility>
  <files>tools/phase18_native_probe.py, .github/workflows/ci.yml, tests/test_ownership_concurrency.py, src/fast_fsm/core.py, .planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md</files>
  <read_first>src/fast_fsm/core.py:510-588, src/fast_fsm/core.py:2053-2140, src/fast_fsm/core.py:2458-2659, tests/test_transition_lifecycle.py:1-220, .github/workflows/ci.yml:105-180, .specify/memory/spr-core-api.md</read_first>
  <behavior>
    - A callback that calls the same machine's `trigger()` receives `RuntimeError` before nested preparation; when uncaught, the outer Phase 17 result reports the callback's existing stage, and when caught the outer operation continues per D-03.
    - Two independent threads coordinated by Barrier/Event serialize the complete trigger lifecycle on one machine, while a second machine can progress independently per D-05 and D-06.
    - Success, ordinary exception, `KeyboardInterrupt`, and `SystemExit` clear the owner and release the per-instance primitive; immediate reuse succeeds with Phase 17 state/history truth per D-07 and D-08.
    - Before the production slot/type representation is adopted, a minimal standalone ownership representation compiles, imports from a native module on the current interpreter, and the CI contract enumerates Python 3.10 through 3.14.
  </behavior>
  <action>Start RED by adding the deterministic sync-trigger tracer cases and record asserted pure plus freshly compiled before-change trigger observations in the Phase 18 evidence file before changing `core.py`. Create `tools/phase18_native_probe.py` with a minimal mypyc-compilable slotted class that uses the proposed `threading.Lock`, owner identifier, `asyncio.Lock`, loop/task/root, `ContextVar`, and short admission-gate annotations and exercises acquire/release/import behavior. Add the scoped `ownership-native-probe` CI matrix definition for Python 3.10, 3.11, 3.12, 3.13, and 3.14, and require the probe's current-runtime strict compiled build/import/origin check to pass locally; this is the compile-first representation gate required by D-05, D-09, D-11, D-12, and D-16. Only after that local pass, add one per-instance primitive and explicit owner marker to `StateMachine.__slots__` and `__init__`. Implement `_acquire_sync_ownership(operation)` so it checks `threading.get_ident()` against the active owner before acquisition, blocks only independent threads on this machine, installs the owner only after acquisition, and raises a stable redacted `RuntimeError` for same-owner reentry per D-01, D-02, D-04, D-05, and D-06. Implement `_release_sync_ownership()` so owner clearing and primitive release are mechanically paired. Split `trigger()` into one public ownership entry and `_trigger_owned()` containing the unchanged Phase 17 preparation, lifecycle, commit, finalizer, and result construction per D-07 and D-08. The hosted matrix contract is established here, but a pending hosted run is not success; Plan 18-07 owns the exact-SHA completion gate. Do not add a public exception, public lock, runtime dependency, global lock, fairness/timeout semantics, rollback, or a second lifecycle finalizer.</action>
  <acceptance_criteria>
    - Same-thread callback reentry raises before any nested guard/callback/state/history activity and cannot overwrite the outer transition.
    - Independent threads serialize one whole transition each on one machine; a separate machine has independent ownership state.
    - Every tested throwable leaves the machine reusable and at the exact Phase 17 pre- or post-commit boundary.
    - The current interpreter compiles and imports the minimal proposed ownership representation natively before `core.py` adopts it, and the checked CI definition contains all five supported Python versions.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_ownership_concurrency.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_ownership_concurrency.py</automated>
    <automated>uv run pytest tests/test_ownership_concurrency.py tests/test_transition_lifecycle.py -x -q -k "tracer or sync_reentrant or sync_thread or sync_release"</automated>
    <automated>uv run python tools/phase18_native_probe.py --build-mode compiled --assert-native</automated>
    <automated>uv run python tools/phase18_native_probe.py --check-ci .github/workflows/ci.yml</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python tools/release_evidence.py slots-policy --json</automated>
  </verify>
  <done>The proposed ownership representation passes a local native compile/import probe under a Python 3.10-3.14 CI matrix contract, and a production sync trigger admits once, rejects same-owner reentry before preparation, serializes independent threads per machine, and releases cleanly across all synchronous exit classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the staged strict-RED inventory and isolated Phase 18 seam</name>
  <files>tests/test_ownership_concurrency.py, tests/test_mypyc_guard.py, tools/phase16_isolated_verify.py</files>
  <read_first>.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md, tests/test_mypyc_guard.py:1-490, tests/test_performance_benchmarks.py:378-474, tools/phase16_isolated_verify.py:55-81, tools/phase16_isolated_verify.py:181-237, tools/phase16_isolated_verify.py:885-943</read_first>
  <behavior>
    - The scenario table names every OWN-01 through OWN-07 edge and all eight fallback-probe rows, using Event/Barrier/async Event handshakes rather than sleeps per D-16.
    - Wave 0 AST guards require the tracer slots/private-body seam, no process-global lock registry, one core mypyc unit, unchanged public signatures, and a complete declarative inventory of every later writer; full writer-entry enforcement is deliberately activated by Plan 18-04 after those writers exist.
    - `--suite phase18` overlays an explicit inventory, asserts pure `.py` or freshly built native origin before semantics, and retains the compiled 200,000 operations/second floor.
  </behavior>
  <action>Complete Wave 0 before broader runtime changes. Create one authoritative case table covering every OWN-01 through OWN-07 behavior, all exact D-14 writers, all eight fallback-probe rows, redaction, cleanup, safe-trigger, declarative-marker, cross-loop, causal-root, cancellation, pure/native, and no-offload cases. The tracer rows implemented by Task 1 run normally. Every behavior owned by Plans 18-02 through 18-05 lands now as an executable assertion marked `pytest.mark.xfail(strict=True, reason="RED until Plan 18-0N")`, with its owning plan encoded in the case table. Each owning plan must first remove only its rows' strict-xfail markers, run the focused command and observe deterministic assertion failure, then implement until GREEN; an unexpected early pass is a Wave 0 failure. Extend `tests/test_mypyc_guard.py` only with the complete writer-name/owner-plan inventory, tracer slot/private-body checks, no process-global lock registry, standard-library-only dependency boundary, matrix/probe presence, and `core.py` as the sole mypyc unit. Do not require every future public writer to call admission in Wave 0; Plan 18-04 replaces the staged name inventory with full one-entry/private-body enforcement after all writers are implemented. Extend the existing isolation helper backward-compatibly with a `phase18` inventory/suite selector and no duplicate harness; preserve fresh export, explicit overlays, pure shadow rejection, fail-closed native build, and origin assertions.</action>
  <acceptance_criteria>
    - Wave 0 names every requirement, write family, release path, origin mode, threat, and fallback probe with no timing-based correctness assertion.
    - The strict-xfail registry is complete and executable, fails on unexpected early success, and assigns every later row to exactly one plan-removal step; Wave 0 structural guards enforce the tracer and full writer inventory without prematurely enforcing unimplemented writer bodies.
    - The phase18 task mode proves the sync tracer in both asserted source and fresh native origins without deleting checkout shadows.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format tests/test_ownership_concurrency.py tests/test_mypyc_guard.py tests/test_performance_benchmarks.py tools/phase16_isolated_verify.py</automated>
    <automated>uv run ruff check tests/test_ownership_concurrency.py tests/test_mypyc_guard.py tests/test_performance_benchmarks.py tools/phase16_isolated_verify.py</automated>
    <automated>uv run pytest tests/test_ownership_concurrency.py tests/test_mypyc_guard.py -x -q -k "tracer or contract_inventory or staged_writer_inventory or slots"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_ownership_concurrency.py --include tests/test_mypyc_guard.py --include tools/phase16_isolated_verify.py -- uv run pytest tests/test_ownership_concurrency.py tests/test_mypyc_guard.py -x -q -k "tracer or ownership"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_ownership_concurrency.py --include tests/test_mypyc_guard.py --include tests/test_performance_benchmarks.py --include tools/phase16_isolated_verify.py -- uv run pytest tests/test_ownership_concurrency.py tests/test_mypyc_guard.py tests/test_performance_benchmarks.py -x -q -k "tracer or ownership or trigger_min_throughput"</automated>
  </verify>
  <done>Wave 0 is executable and honest: normal tracer rows pass, every later behavior is a strict deterministic RED contract with an explicit owning-plan removal protocol, structural enforcement is staged at the implemented boundary, and all seven requirements plus all eight spec-less probes are inventoried.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record tracer performance and the implemented SPR contract</name>
  <files>tests/test_performance_benchmarks.py, .planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md, .specify/memory/spr-core-api.md</files>
  <read_first>tests/test_performance_benchmarks.py:378-474, .planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md, .specify/memory/spr-core-api.md, .planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md</read_first>
  <behavior>
    - The Phase 18 evidence distinguishes pre-change and sync-tracer observations by module origin, interpreter, platform, command, and policy status.
    - Uncontended sync ownership uses constant-time state and retains the compiled trigger floor of 200,000 operations/second.
    - The SPR records only the implemented sync tracer plus the compile-first/matrix contract; it does not claim later async or complete writer behavior.
  </behavior>
  <action>Add sync-tracer measurements using the existing benchmark warmup, logging-suppression, origin-detection, and fixed-batch shape. Record the measured tracer results beside Task 1's before-change evidence with exact environment labels; exact ownership overhead remains observational while the compiled trigger floor remains policy. Update the SPR with the implemented D-01 through D-08 sync-trigger contract, the locally passed native representation probe, and the supported-version matrix gate. State the remaining strict-RED inventory accurately without claiming that async ownership or complete write-family enforcement has passed.</action>
  <acceptance_criteria>
    - Before/tracer evidence is origin- and environment-labelled and contains no recalled or fabricated rates.
    - Sync ownership structure remains O(1), slots remain explicit, and compiled trigger policy stays at or above 200,000 operations/second.
    - The SPR matches implemented Wave 0 behavior and names later strict-RED coverage without overstating completion.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format tests/test_performance_benchmarks.py</automated>
    <automated>uv run ruff check tests/test_performance_benchmarks.py src/fast_fsm/core.py</automated>
    <automated>uv run pytest tests/test_performance_benchmarks.py tests/test_ownership_concurrency.py -x -q -k "ownership or trigger_min_throughput or tracer"</automated>
    <automated>uv run python tools/release_evidence.py slots-policy --json</automated>
  </verify>
  <done>The sync tracer has measured origin-labelled evidence, retains O(1)/slots/throughput policy, and its implemented contract is recorded without overstating later rows.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | reentry admission | high | mitigate | Pre-acquisition same-owner check and tracer proves no nested preparation/mutation. |
| T-18-02 | Denial of Service | sync primitive/owner | high | mitigate | Per-instance primitive plus one finally cleanup and immediate-reuse tests. |
| T-18-03 | Tampering/DoS | cross-loop state | high | mitigate | Wave 0 specifies explicit permanent loop identity; Plan 03 implements it. |
| T-18-04 | Denial of Service | causal child task | high | mitigate | Wave 0 specifies inherited-root rejection; Plan 03 implements it. |
| T-18-05 | Tampering | declarative prepared marker | high | mitigate | Structural scenario requires context-local machine-qualified state; Plan 05 implements it. |
| T-18-06 | Repudiation/Tampering | `safe_trigger()` | high | mitigate | Inventory requires ownership admission outside the catch; Plan 05 implements it. |
| T-18-07 | Denial of Service/Tampering | cancellation window | high | mitigate | Handshake matrix covers waiting/owning pre/post-commit cancellation; Plan 03 implements it. |
| T-18-08 | Information Disclosure | ownership errors/logs | high | mitigate | Fixed category text and secret-sentinel assertions begin in the tracer. |
| T-18-09 | Denial of Service | fairness/timeout expectations | medium | accept | D-06 promises mutual exclusion only; tests/docs make no queue, fairness, timeout, or offload claim. |
</threat_model>

<verification>Run the focused tracer and structural suite in checkout, asserted pure export, and freshly compiled export; require Ruff, blocking mypy, slots, and the fixed compiled throughput floor.</verification>
<success_criteria>The end-to-end tracer is production code, Wave 0 has complete deterministic coverage architecture, all eight probes are accounted for, and no plan-owned high threat lacks an executable mitigation path.</success_criteria>
<output>Create `.planning/phases/18-safe-ownership-concurrency/18-01-SUMMARY.md` when done.</output>
