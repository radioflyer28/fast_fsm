---
phase: 32-performance-artifact-progressive-guidance-proof
plan: 05
type: execute
wave: 2
depends_on:
  - 32-04
files_modified:
  - README.md
  - docs/QUICK_START.md
  - tests/test_readme_examples.py
autonomous: true
requirements:
  - DOC-02
  - DOC-03
  - DOC-04
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DOC-02/D-09: the first runnable README and Quick Start construction recipe is a small `FSMBuilder` machine; direct construction appears later as supported advanced use and `from_dict()` only as serialized-topology input."
    - "DOC-03/D-10: all four convenience APIs have actionable `FSMBuilder` replacements and correct once-per-call warning/compatibility timing through v0.5.x, with removal no earlier than v0.6.0."
    - "DOC-04/D-11: public paired examples distinguish explicit final versus sink, false guard versus expected rejection versus unexpected failure, and internal versus external self lifecycle."
    - "The README is light near the top and grows denser toward API, diagnostics, artifacts, and performance details."
  artifacts:
    - path: README.md
      provides: "Progressive builder-first landing page, migration mapping, and semantic contrasts"
    - path: docs/QUICK_START.md
      provides: "Short runnable builder-first learning path with exact result assertions"
    - path: tests/test_readme_examples.py
      provides: "Content-order, executable migration, and semantic-example regression checks"
  key_links:
    - from: README.md
      to: docs/QUICK_START.md
      via: "One canonical builder-first recipe and increasing detail"
      pattern: "FSMBuilder"
    - from: README.md
      to: examples/drone_failsafes.py
      via: "Link to the finished deterministic training example after conceptual onboarding"
      pattern: "drone_failsafes"
  flagged_assumptions:
    - id: DOC-02
      status: flagged-unresolved
      reason: "Spec-less edge probe classified the requirement applicable but unclassified; executable region order is explicitly checked."
    - id: DOC-03
      status: flagged-unresolved
      reason: "Spec-less edge probe classified the requirement applicable but unclassified; each named API gets a tested replacement and timing statement."
    - id: DOC-04
      status: flagged-unresolved
      reason: "Spec-less edge probe classified the requirement applicable but unclassified; paired public examples have exact observable result/lifecycle assertions."
  prohibitions: []
---

<objective>
Make the README and Quick Start a coherent builder-first path from a tiny machine to explicit flat-FSM semantics and migration.

Purpose: Users should find one obvious starting interface, understand supported advanced routes, and make no false inference from a graph dead end or unchanged state name.
Output: Reorganized README/Quick Start and executable documentation regressions.
</objective>

## Artifacts this phase produces

- One small first-screen builder recipe, with progressively deeper guards, priority, mode, finality, rejection, diagnostics, async/direct use, and artifact/performance sections.
- Four legacy-convenience-to-builder migration mappings and the exact v0.5.x warning/v0.6.0 earliest removal statement.
- Executable paired semantic contrasts with exact public output/result fields.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-RESEARCH.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-PATTERNS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-04-SUMMARY.md

<interfaces>
`FSMBuilder(State("idle"), name="Worker").add_state(...).add_transition(...).build()` is the primary construction recipe. `StateMachine`/`AsyncStateMachine` direct constructors remain supported advanced APIs; declarative states remain supported; `StateMachine.from_dict(...)` consumes persisted topology. `simple_fsm`, `quick_fsm`, `StateMachine.quick_build`, and `StateMachine.from_states` warn once per public call while remaining available through v0.5.x.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reorder onboarding and provide exact migration replacements</name>
  <reversibility rating="costly">D-09 is the public onboarding hierarchy; reversing it would require coordinated README, Sphinx, and example changes.</reversibility>
  <files>README.md, docs/QUICK_START.md, tests/test_readme_examples.py</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-09/D-10
    - `README.md` current opening, construction, migration, and performance sections
    - `docs/QUICK_START.md` first recipe and construction hierarchy
    - `tests/test_readme_examples.py` `_active_python_regions` and deprecated-construction scanner
    - `src/fast_fsm/core.py` public builder, factory warning messages, and from_dict signatures
  </read_first>
  <behavior>
    - The first runnable construction block in both documents uses `FSMBuilder`; advanced direct construction and `from_dict()` occur only after that path is established.
    - Each of the four deprecated convenience names has a concrete builder replacement and exact compatibility timing.
    - Active runnable teaching regions use the primary API; deprecated names can appear in prose or non-executed before-side notation without becoming recommended examples.
  </behavior>
  <action>Per D-09/D-10, rewrite the README top as a small builder recipe and short value proposition, move dense API/performance details lower, and make Quick Start follow the same ordering. Map `simple_fsm`, `quick_fsm`, `StateMachine.quick_build`, and `StateMachine.from_states` individually to builder operations, with a real before/after explanation and executable builder-side code. State the once-per-call DeprecationWarning compatibility through v0.5.x and earliest v0.6.0 removal; explicitly preserve direct constructors, `from_dict()`, and declarative states as supported distinct uses. Add tests for section/recipe ordering, all four mappings, and executed builder replacement snippets. Respect the existing scanner by keeping legacy examples out of active Python teaching blocks.</action>
  <verify><automated>uv run pytest tests/test_readme_examples.py -k 'builder or migration or deprecat' -x -q</automated></verify>
  <acceptance_criteria>Both guides start with builder, each deprecated name has an actionable replacement and exact timing, and no active teaching code recommends a deprecated helper.</acceptance_criteria>
  <done>DOC-02/DOC-03/D-09/D-10 onboarding and migration are clear, progressive, and executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add paired semantic contrasts with observable results</name>
  <files>README.md, docs/QUICK_START.md, tests/test_readme_examples.py</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-09/D-11
    - `README.md` updated progressive sections and `docs/QUICK_START.md` runnable snippets
    - `tests/test_readme_examples.py` active-region execution helpers
    - `src/fast_fsm/core.py` `is_terminated`, `TransitionResult` rejection fields and internal mode public APIs
  </read_first>
  <behavior>
    - An explicit final state reports terminated, while a non-final sink with no outgoing edge does not.
    - A false guard can fall through to a later priority candidate; expected rejection returns `rejected` and bounded `rejection_code` without commit; unexpected failure has a distinct cause/stage.
    - Internal self commits without state entry/exit; external self re-enters, even though both keep the same state name.
  </behavior>
  <action>Per D-11, add compact paired runnable examples and exact assertions in README and Quick Start for the three distinctions. Demonstrate `is_terminated` alongside topology shape; show a priority group where false eligibility falls through and `TransitionRejected(code)` stops selection; contrast an internal self with default external self using callback counts and `TransitionResult` fields rather than state name alone. Link to the completed Phase 32 drone script for the fuller telemetry/command story. Keep the top quick path small, place the denser contrasts lower, and add focused regression execution of copied snippets or equivalent doctest-style assertions.</action>
  <verify><automated>uv run pytest tests/test_readme_examples.py -x -q</automated></verify>
  <acceptance_criteria>Both public entry documents contain correct paired contrasts and their exact output fields execute under the test suite.</acceptance_criteria>
  <done>DOC-04/D-11 differences are taught through behavior, not ambiguous graph or state-name descriptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public snippets → copied application code | Wrong semantics in examples become real user logic errors. |
| Migration prose → compatibility expectations | Readers may assume a supported API has already been removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-15 | Tampering | Documentation examples | medium | mitigate | Execute builder/mode/finality/rejection snippets through `tests/test_readme_examples.py` and exact result assertions. |
| T-32-16 | Repudiation | Compatibility guidance | medium | mitigate | Test all four replacement mappings and explicit v0.5.x/v0.6.0 timing language. |
| T-32-17 | Information disclosure | Rejection example | low | accept | Fixed illustrative code is payload-free; existing bounded-code API and doc tests prevent arbitrary exception text claims. |
</threat_model>

<verification>
Run `uv run pytest tests/test_readme_examples.py -x -q`, `task docs-check`, and `task docs-test`. Check the README remains shorter and simpler near the top than in the advanced reference sections.
</verification>

<success_criteria>
README and Quick Start teach the builder first, retain the supported advanced hierarchy, map every deprecated convenience, and truthfully contrast the new semantics.
</success_criteria>

<output>
Create `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-05-SUMMARY.md` when done.
</output>
