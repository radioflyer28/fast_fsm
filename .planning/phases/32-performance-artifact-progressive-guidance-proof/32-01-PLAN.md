---
phase: 32-performance-artifact-progressive-guidance-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/artifact_conformance.py
  - tests/test_artifact_conformance.py
  - tests/test_installed_artifacts.py
autonomous: true
requirements:
  - PERF-04
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PERF-04/D-04: one fixed, required-value semantic oracle proves explicit finality differs from a non-final sink in clean source and an exact-origin installed artifact."
    - "PERF-04/D-04: that same oracle distinguishes internal and external self lifecycle, false eligibility fallthrough, and terminal expected rejection with bounded scalar records."
    - "Unknown, missing, reordered, duplicated, or contradicted new scenario facts fail closed even when a caller recomputes the semantic digest."
  artifacts:
    - path: tools/artifact_conformance.py
      provides: "Four deterministic final/mode/rejection scenario definitions and collectors in the shared installed-artifact oracle"
    - path: tests/test_artifact_conformance.py
      provides: "Exact expected rows and independent required-value mutation tests"
    - path: tests/test_installed_artifacts.py
      provides: "First installed-wheel parity test for the expanded oracle"
  key_links:
    - from: tools/artifact_conformance.py
      to: tools/release_evidence.py
      via: "Existing collector-copy and verify_installed_wheel path, with runtime origin checked before comparison"
      pattern: "collect_conformance|verify_installed_wheel"
    - from: _SCENARIO_DEFINITIONS
      to: _scenario_collectors
      via: "Exact one-to-one inventory and required_values"
      pattern: "required_values"
  flagged_assumptions:
    - id: PERF-04
      status: flagged-unresolved
      reason: "The spec-less edge probe classified this requirement as applicable but unclassified; the fixed source/native/wheel/release oracle and exact-origin matrix are explicit planned proofs, not a fabricated probe resolution."
  prohibitions: []
---

<objective>
Make the shared artifact oracle authoritative for the Phase 27–29 semantics before any cross-mode parity claim is accepted.

Purpose: Artifact agreement is insufficient if every artifact agrees on an untested semantic mistake.
Output: Required-value finality, self-mode, and rejection rows with fail-closed tests, including a first installed-wheel end-to-end slice.
</objective>

## Artifacts this phase produces

- `final.explicit-versus-sink`, `mode.internal-self`, `mode.external-self`, and `rejection.false-versus-terminal` scenario records in the existing collector; names may be adjusted only while preserving four independently testable meanings.
- Exact-row and mutation tests for every new required semantic fact.
- One installed-wheel parity assertion using the existing verifier, not a second artifact harness.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-RESEARCH.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-PATTERNS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-VALIDATION.md

<interfaces>
`collect_conformance()` sorts `_scenario_collectors()` records, validates exact `_SCENARIO_DEFINITIONS` fields and `required_values`, then binds suite and semantic SHA-256 digests. `verify_installed_wheel()` copies this collector into a neutral environment, validates wheel build intent and exact package/core loader origin, and compares the collected record. `TransitionRejected(code)` raised by an eligibility guard yields `TransitionResult.rejected`/`rejection_code` without a commit; ordinary false eligibility can proceed to a later candidate.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove explicit-final versus sink truth through an installed artifact</name>
  <files>tools/artifact_conformance.py, tests/test_artifact_conformance.py, tests/test_installed_artifacts.py</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-04 and `.planning/REQUIREMENTS.md` PERF-04
    - `tools/artifact_conformance.py` scenario registry, `_scenario_collectors`, `_validate_scenarios`, and `collect_conformance`
    - `tests/test_artifact_conformance.py` exact-record and `_rehash` mutation patterns; `tests/test_installed_artifacts.py` clean-source and wheel fixtures
    - `tools/release_evidence.py` `verify_installed_wheel` archive/runtime-origin ordering
  </read_first>
  <behavior>
    - A machine in `State("done", final=True)` reports terminated; a no-outgoing non-final sink does not.
    - The final/sink scenario has an exact required scalar record and a rebuilt digest cannot conceal a changed required value.
    - A fresh installed pure wheel yields the same row as clean source only after its exact origin/build intent passes the existing verifier.
  </behavior>
  <action>Per D-04, first add red tests named with `phase32_final` for one final/sink record and the installed pure-wheel comparison. Add one deterministic collector and one registry definition with fixed ordered fields and independent `required_values` for both terminal and sink facts. Use only installed `fast_fsm` plus standard-library imports, keep caller objects and arbitrary exception text out of the row, and leave existing `graph.guard-rejection` unchanged. Wire through the existing `collect_conformance()` and `verify_installed_wheel()` path; assert the latter's origin and build-intent fields rather than accepting printed agreement alone. The fast per-task test checks the collector contract; the actual wheel probe runs at the Wave 1 gate.</action>
  <verify><automated>uv run pytest tests/test_artifact_conformance.py -k 'phase32_final' -x -q</automated></verify>
  <acceptance_criteria>One final/sink scenario is present exactly once, has fixed truthful required values, fails under a rehashed semantic mutation, and matches the exact-origin installed pure wheel.</acceptance_criteria>
  <done>PERF-04/D-04 has a real source-to-wheel semantic and provenance tracer, with tests passing after the initial red run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the oracle to both self modes and rejection selection</name>
  <files>tools/artifact_conformance.py, tests/test_artifact_conformance.py</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-04, D-07, and D-11
    - `tools/artifact_conformance.py` new final/sink tracer row, scenario registry, collector tuple, validation and payload-redaction path
    - `tests/test_artifact_conformance.py` exact-row, missing/extra/duplicate/order, mutation, and payload-sentinel tests
    - `tests/test_transition_modes.py` and `tests/test_expected_rejection.py` canonical behavior and public result fields
  </read_first>
  <behavior>
    - Internal self commits once without state exit/entry; default external self commits with one exit and one entry.
    - False top-ranked guard permits a later eligible candidate; `TransitionRejected("code")` stops the candidate group before a command or commit.
    - Removing or altering any new row, even with recomputed digests, is rejected.
  </behavior>
  <action>Per D-04, add independent `mode.internal-self`, `mode.external-self`, and `rejection.false-versus-terminal` collectors, each using public behavior and bounded scalar outcomes. Use recorded callbacks/counters and result fields to distinguish actual lifecycle, commit, history, priority, rejected status/code, and suppressed lower-candidate work. Add exact expected records and parameterized `required_values` mutation tests for every new field whose wrong value would change the meaning; preserve sorted IDs, fixed field order, suite digest, payload leak verdict, and strict inventory. Do not treat the older topology-oriented `graph.guard-rejection` row as expected-rejection proof.</action>
  <verify><automated>uv run pytest tests/test_artifact_conformance.py -x -q</automated></verify>
  <acceptance_criteria>All four Phase 32 rows have deterministic exact expected records; malformed inventory, changed semantic values, and payload leakage are rejected independently of digest recomputation.</acceptance_criteria>
  <done>PERF-04/D-04 semantic inventory is strict enough to support later pure/native/installed/release comparisons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Installed wheel and child JSON → parent evidence | The archive and child record are not trusted merely because they parse. |
| Caller-provided guard context → semantic JSON | Application payload must not enter portable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-01 | Tampering | Collector scenario record | high | mitigate | Fixed inventory/fields and independent `required_values` mutation tests reject altered facts even after digest recomputation. |
| T-32-02 | Spoofing | Installed core origin | high | mitigate | Reuse `verify_installed_wheel()` exact archive mode, loader, and contained runtime-origin checks before semantic comparison. |
| T-32-03 | Information disclosure | Scenario JSON | medium | mitigate | Allowlisted scalar fields and sentinel-leak tests reject payload-bearing output. |
</threat_model>

<verification>
Run the fast conformance tests after each task. At the Wave 1 gate, run `uv run pytest tests/test_artifact_conformance.py tests/test_installed_artifacts.py -x -q` to exercise the actual installed-wheel tracer. Do not update the tracked release baseline in this plan; Plan 02 owns its deliberate refresh after the complete oracle is reviewed.
</verification>

<success_criteria>
The strict oracle is expanded, tested against semantic mutations, and demonstrably copied into an exact-origin installed wheel without a second harness.
</success_criteria>

<output>
Create `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-01-SUMMARY.md` when done.
</output>
