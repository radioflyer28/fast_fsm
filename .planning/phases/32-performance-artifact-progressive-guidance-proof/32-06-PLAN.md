---
phase: 32-performance-artifact-progressive-guidance-proof
plan: 06
type: execute
wave: 3
depends_on:
  - 32-04
  - 32-05
files_modified:
  - docs/TUTORIAL.md
  - docs/examples/index.md
  - docs/api/core.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DOC-01/D-07/D-08: the Sphinx example gallery points to the runnable controller-owned drone script, describes each progressive semantic beat, and retains its training-only disclaimer."
    - "DOC-02/D-09: the Sphinx tutorial begins with `FSMBuilder` and advances to guards, priority, modes, finality, rejection, diagnostics, then advanced direct/async and artifact topics."
    - "DOC-03/D-10: Sphinx guidance links or states all four migration replacements and the v0.5.x/v0.6.0 compatibility timing without deprecating direct/from_dict/declarative paths."
    - "DOC-04/D-11: executable Sphinx examples and API reference preserve the three public semantic distinctions with exact result fields."
    - "HTML warnings-as-errors and Sphinx doctest pass, and every new code example is runnable or backed by script smoke tests."
  artifacts:
    - path: docs/TUTORIAL.md
      provides: "Progressive builder-first semantic lessons and advanced-path handoff"
    - path: docs/examples/index.md
      provides: "Tiered gallery entry for the complete deterministic drone tutorial"
    - path: docs/api/core.md
      provides: "Precise executable final/mode/rejection reference and compatibility cross-links"
  key_links:
    - from: docs/examples/index.md
      to: examples/drone_failsafes.py
      via: "Sphinx literalinclude plus independent pytest smoke test"
      pattern: "literalinclude.*drone_failsafes"
    - from: docs/TUTORIAL.md
      to: docs/api/core.md
      via: "Progressive lesson to exact public result/API contract"
      pattern: "TransitionRejected|is_terminated|internal"
  flagged_assumptions: []
  prohibitions: []
---

<objective>
Align the Sphinx tutorial, gallery, and API reference with the tested README and drone learning path.

Purpose: Users should encounter one coherent builder-first story while deeper pages preserve exact flat-FSM semantics and supported API boundaries.
Output: Progressive Sphinx lessons, an accurate drone gallery, and executable API contrasts.
</objective>

## Artifacts this phase produces

- A tiered Sphinx tutorial that advances from builder to final/mode/rejection/diagnostics without presenting alternate constructors as equivalent starting points.
- Updated literal-included drone gallery entry with independent smoke-test and training-only context.
- `{testcode}`/`{testoutput}` examples for exact final, mode, and rejection result semantics.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-RESEARCH.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-PATTERNS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-04-SUMMARY.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-05-SUMMARY.md

<interfaces>
Sphinx uses MyST plus `sphinx.ext.doctest`; `{testcode}` and `{testoutput}` execute in `task docs-test`. `docs/examples/index.md` uses `literalinclude` for the drone file but that directive only displays source, so `tests/test_drone_failsafes_example.py` remains its execution proof. README/Quick Start from Plan 05 establish the builder and migration wording to keep synchronized.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teach the progressive workflow and point the gallery to its runnable drone story</name>
  <files>docs/TUTORIAL.md, docs/examples/index.md</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-06 through D-11
    - `docs/TUTORIAL.md` current levels, construction and priority lessons
    - `docs/examples/index.md` tier order, feature-family map, drone literal inclusion
    - `README.md` and `docs/QUICK_START.md` Plan 05 canonical recipe/migration wording
    - `examples/drone_failsafes.py` Plan 04 final runnable simulation and disclaimer
  </read_first>
  <action>Per D-06 through D-10, begin Tutorial with the same small `FSMBuilder` recipe as the public entry pages and progress one lesson at a time through guard/priority, internal versus external self, explicit final states, expected rejection, diagnostics, then supported advanced direct/async and serialized `from_dict()` use. Keep legacy convenience migration cross-linked with all four replacements and compatibility timing. Update the tiered gallery so the drone remains one progressively taught integration example rather than forcing every example to cover every feature; describe the one-tick controller boundary, FSM-owned precedence, both self modes, final landing, rejection contrast, and post-commit aircraft commands. Keep the literalinclude and explicit deterministic training/non-hardware warning; the smoke test, not literalinclude, supplies execution proof.</action>
  <verify><automated>task docs-check</automated></verify>
  <acceptance_criteria>Tutorial and gallery follow the tested README hierarchy, drone claims match its final script/tests, and warnings-as-errors HTML build succeeds.</acceptance_criteria>
  <done>DOC-01/DOC-02/DOC-03 Sphinx learning path is navigable, progressive, and accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Make API semantic distinctions executable in Sphinx</name>
  <files>docs/api/core.md</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-09 through D-11
    - `docs/api/core.md` existing priority and expected-rejection doctest blocks
    - `docs/TUTORIAL.md` Plan 06 progressive wording and `README.md` Plan 05 paired contrasts
    - `src/fast_fsm/core.py` public finality, mode, rejection and result field names
    - `tests/test_final_states.py`, `tests/test_transition_modes.py`, and `tests/test_expected_rejection.py` exact behavioral contract
  </read_first>
  <action>Per D-11, update the core reference with short `{testcode}`/`{testoutput}` pairs for explicit final versus non-final sink, internal self versus default external re-entry (including lifecycle callbacks), and ordinary false-guard fallthrough versus terminal `TransitionRejected(code)` versus unexpected failure. Use exact public `TransitionResult` status/code/commit fields; do not infer finality from no outgoing edge or mode from equal endpoint names. Keep `from_dict()` described as persistence and direct/declarative APIs as supported advanced use, not deprecated; link to the migration section for only the four warned convenience APIs. Run Sphinx doctest and HTML warnings-as-errors after edits.</action>
  <verify><automated>task docs-test</automated></verify>
  <acceptance_criteria>Every new API code block executes with exact expected output, Sphinx doctest and HTML builds pass, and API claims agree with runtime tests and entry guides.</acceptance_criteria>
  <done>DOC-04/D-11 semantic contrasts remain executable and consistent across all public guidance.</done>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plan(s) | Status |
|---|---|---|---|
| GOAL | Install any supported artifact, retain direct-path performance, learn progressive flat FSM | 01–06 | COVERED |
| REQ | PERF-01, PERF-02, PERF-03 | 03 | COVERED |
| REQ | PERF-04 | 01, 02 | COVERED |
| REQ | DOC-01 | 04, 06 | COVERED |
| REQ | DOC-02, DOC-03, DOC-04 | 05, 06 | COVERED |
| RESEARCH | Strict required-value oracle before cross-mode parity; exact-origin source/native/wheel/release-intent evidence | 01, 02 | COVERED |
| RESEARCH | Native singleton gate separate from descriptive final/self/rejection costs; local work independent of unrelated topology | 03 | COVERED |
| RESEARCH | Controller one-tick priority and post-commit commands; final restart contradiction | 04 | COVERED |
| RESEARCH | Executable builder-first docs and Sphinx gallery/API synchronization | 05, 06 | COVERED |
| CONTEXT | D-01, D-02 | 03 | COVERED |
| CONTEXT | D-03, D-04, D-05 | 01, 02 | COVERED |
| CONTEXT | D-06, D-07, D-08 | 04, 06 | COVERED |
| CONTEXT | D-09, D-10, D-11 | 05, 06 | COVERED |

The spec-less edge probe returned eight applicable but unclassified requirements. PERF-01 through PERF-04 and DOC-01 through DOC-04 are recorded exactly once as `flagged-unresolved` assumptions in Plans 01, 03, 04, and 05; the plans add concrete proofs without claiming the probe resolved them. Bespoke safety/transparency prohibitions remain descriptor-less `flagged-unverified` in Plans 02–04. Canonical archive/path/payload security concerns live in the threat models. Context deferred ideas, statecharts/schedulers, and external publication are excluded by explicit scope, not silently dropped.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime semantics → Sphinx claims | Executable guide text must match the public API and tested drone behavior. |
| Literal-included script → reader action | Display of source is not execution or flight-safety certification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-18 | Tampering | API documentation behavior | medium | mitigate | `{testcode}`/`{testoutput}` run in Sphinx doctest, cross-checked with runtime-focused tests. |
| T-32-19 | Repudiation | Drone tutorial claim | medium | mitigate | Gallery states deterministic training-only scope and points to independently executed smoke test. |
| T-32-20 | Information disclosure | Doc output examples | low | accept | Snippets use fixed literal codes and no user payload; bounded rejection display is tested in Plan 04. |
</threat_model>

<verification>
Run `task docs-check`, `task docs-test`, `uv run pytest tests/test_readme_examples.py tests/test_drone_failsafes_example.py -x -q`, and the full sequential `uv run pytest tests/ -x -q` at phase gate. Typecheck with blocking `task typecheck-mypy` and advisory `task typecheck-ty`; run the installed artifact and native-floor tasks before marking Phase 32 verified.
</verification>

<success_criteria>
README, Quick Start, Tutorial, gallery, and API reference form one tested builder-first learning path; all eight phase requirements retain concrete proof tasks and no deferred runtime or publication work was smuggled in.
</success_criteria>

<output>
Create `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-06-SUMMARY.md` when done.
</output>
