---
phase: 16-canonical-graph-dispatch-invariants
plan: 04
type: execute
wave: 3
depends_on:
  - 16-03
files_modified:
  - src/fast_fsm/core.py
  - tests/test_builder.py
  - tests/test_async.py
  - tests/test_advanced_functionality.py
  - tests/test_performance_benchmarks.py
  - .specify/memory/spr-core-api.md
autonomous: true
requirements:
  - GRAF-07
  - GRAF-08
  - LIFE-07
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-15, ordinary sync and async dispatch resolve a declarative handler by canonical source/trigger/target metadata and invoke the matched successful handler exactly once; compatibility helpers delegate without a second call."
    - "Per D-16, tests prove successful exactly-once parity without fixing handler callback-relative position, exception result shape, commit boundary, cancellation, or history-on-failure semantics reserved for Phase 17."
    - "Per D-17, enable_history rejects zero/negative/non-integer capacities before changing the existing configuration and valid re-enable replaces it with an empty bounded buffer."
    - "Per D-18, bounded FIFO eviction is O(1), chronological, copy-on-read, and retains the existing None branch/zero allocation when disabled in sync and async dispatch."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Single declarative resolver/invocation boundary and bounded deque history storage"
      contains: "_resolve_declarative_handler"
    - path: "tests/test_builder.py"
      provides: "Successful declarative from/trigger/to matching and compatibility delegation tests"
    - path: "tests/test_async.py"
      provides: "Async declarative exactly-once parity and async history coverage"
    - path: "tests/test_advanced_functionality.py"
      provides: "History capacity, replacement, FIFO, copy, clone, and sync regression contracts"
    - path: "tests/test_performance_benchmarks.py"
      provides: "History-disabled and bounded-eviction overhead/throughput proof"
    - path: ".specify/memory/spr-core-api.md"
      provides: "Declarative invocation and bounded-history rules updated with their owning implementation commits"
  key_links:
    - from: "ordinary trigger/trigger_async"
      to: "declarative state handler metadata"
      via: "canonical source/trigger/target resolver and one invocation seam"
      pattern: "_resolve_declarative_handler|_invoke_declarative_handler"
    - from: "handle_event/handle_event_async"
      to: "ordinary dispatch seam"
      via: "compatibility delegation without independent second invocation"
      pattern: "handle_event"
    - from: "enable_history/_execute_transition"
      to: "collections.deque"
      via: "validated maxlen and append-only bounded eviction"
      pattern: "deque.*maxlen"
  prohibitions:
    - "[UNVERIFIED — no SPEC] Do not lock declarative handler pre/post-commit position, callback order, failure result, cancellation, or history-on-failure semantics; Phase 17 owns them."
    - "[UNVERIFIED — no SPEC] Do not add Phase 18 ownership/locking around history or dispatch."
    - "[UNVERIFIED — no SPEC] Do not publish a new handler or history API; retain current public symbols and copy-on-read shape."
---

<objective>
Complete ordinary declarative dispatch and bounded history while preserving later lifecycle and ownership decision boundaries.

Purpose: Remove double/disconnected side effects and linear history eviction without prematurely defining Phase 17 semantics.
Output: One declarative invocation seam, exactly-once sync/async tests, validated deque history, and performance coverage.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-CONTEXT.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-RESEARCH.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-PATTERNS.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-VALIDATION.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-03-SUMMARY.md
@src/fast_fsm/core.py
@tests/test_builder.py
@tests/test_async.py
@tests/test_advanced_functionality.py
@tests/test_performance_benchmarks.py

<interfaces>
- Declarative metadata retains method/from_state/to_state/condition/is_async entries and existing None/bool/TransitionResult normalization.
- `trigger`/`trigger_async` remain the ordinary dispatch entry points; `handle_event`/`handle_event_async` remain compatibility entry points.
- New private artifacts are `_resolve_declarative_handler`, `_invoke_declarative_handler`, and `_invoke_declarative_handler_async`.
- Exact test-only declarative fixtures are `DeclarativeInvocationCounter`, `AsyncDeclarativeInvocationCounter`, and `_invoke_and_ignore_phase17_outcome()`; the latter deliberately discards Phase 17 outcome/state/history/order observations and exposes only invocation counts.
- `enable_history(max_entries=1000)`, `disable_history()`, and `history -> List[TransitionRecord]` retain public signatures; storage changes from Optional[List] to Optional[deque].
</interfaces>

<flagged_assumptions>
- [ASSUMPTION — no SPEC] Declarative matching requires trigger plus exact canonical from/to metadata when metadata is present; a nonmatching handler is not invoked even if the trigger name matches.
- [ASSUMPTION — no SPEC] Empty history is a defensive empty list whether disabled or newly enabled. `bool` is not accepted as a positive integer capacity despite being an `int` subclass; non-integer/null inputs raise TypeError before mutation.
- [ASSUMPTION — no SPEC] Existing clone behavior keeps history disabled. Re-enable always discards prior records and starts a new chronological buffer.
</flagged_assumptions>
</context>

<execution_constraints>
- Integrate through Plans 16-01/02 shared canonical resolution/dispatch seams; do not create a parallel declarative trigger implementation.
- Run every import-bearing semantic, slots, and performance command through the Wave 0 isolation helper's task mode with all current task files explicitly overlaid; performance and compiled checks use a separate fail-closed compiled context with native-origin proof before execution. Raw Ruff and static mypy may run in the checkout.
- Test successful handler identity/count/result parity only. Do not encode handler-versus-callback order or handler exception/cancellation semantics.
- Use standard-library `collections.deque(maxlen=...)`; do not hand-roll a ring buffer or add a dependency.
- Preserve disabled-history zero steady-state allocation and the compiled trigger floor.
</execution_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invoke matched declarative handlers exactly once in ordinary dispatch</name>
  <read_first>
    - src/fast_fsm/core.py: transition decorator, DeclarativeState, AsyncDeclarativeState, trigger, trigger_async
    - tests/test_builder.py: declarative classes and handler normalization
    - tests/test_async.py: AsyncDeclarativeState coverage
    - examples/declarative_state_example.py
    - .planning/phases/16-canonical-graph-dispatch-invariants/16-CONTEXT.md: D-15 and D-16
  </read_first>
  <files>src/fast_fsm/core.py, tests/test_builder.py, tests/test_async.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - One ordinary sync trigger invokes one matching sync declarative handler once; one ordinary async trigger invokes/awaits one matching async handler once.
    - Handler matching honors canonical source and target metadata plus trigger; wrong source/target, unknown trigger, or absent handler yields no invocation.
    - Compatibility helpers delegate to the same invocation boundary and cannot double-call when used through ordinary dispatch.
    - None/True/successful TransitionResult normalization remains equivalent across sync and async, without assertions about lifecycle position or failure ordering.
    - False-returning, invalid-return, and raising sync/async handlers are each entered exactly once; those edge tests assert the counter only and make no assertion about returned result, state, callback order, history, commit boundary, exception propagation, or cancellation.
  </behavior>
  <action>
    Add RED integration tests using the exact test-only fixtures `DeclarativeInvocationCounter` and `AsyncDeclarativeInvocationCounter` for sync/async ordinary triggers, exact from/to matching, Unicode names/triggers, missing/nonmatching handlers, compatibility helper delegation, and successful None/True/TransitionResult normalization. Add separate false-returning, invalid-return, and raising handler cases on both sync and async APIs; execute those cases through `_invoke_and_ignore_phase17_outcome()` and assert only that the selected handler counter increments once. Do not inspect or assert result, state, callback order, history, commit boundary, exception propagation, or cancellation for those cases per D-16. Implement `_resolve_declarative_handler` keyed by canonical source/trigger/target and the sync/async invocation helpers. Wire exactly one call into the shared ordinary dispatch seam per D-15; make `handle_event` helpers delegate rather than invoke metadata independently. Keep handler condition context on the Plan 16-02 prepared guard contract. Update `.specify/memory/spr-core-api.md` with ordinary exactly-once dispatch and the explicit Phase 17 outcome fence in this same owning commit. Run `uv run ruff format src/fast_fsm/core.py tests/test_builder.py tests/test_async.py`, `uv run ruff check --fix` on the same files, and the non-mutating Ruff check before paired tests and blocking type/compiled checks.
  </action>
  <threats>T-16-08, T-16-06, T-16-10</threats>
  <acceptance_criteria>
    - Successful ordinary sync/async dispatch increments the matched handler counter exactly once and preserves normalized successful results.
    - Wrong from/to/trigger metadata and absent handlers execute zero declarative side effects.
    - Compatibility helpers share the seam and never produce a second invocation.
    - False-returning, invalid-return, and raising sync/async handlers each have invocation-count-only tests using the named fixtures/helper; their assertions do not encode any Phase 17 result/state/order/history/commit semantics.
    - Tests contain no callback-relative ordering, handler-failure result, cancellation, or history-on-failure assertion.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_builder.py tests/test_async.py</automated>
    <automated>uv run ruff check --fix src/fast_fsm/core.py tests/test_builder.py tests/test_async.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_builder.py tests/test_async.py</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_builder.py --include tests/test_async.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_builder.py tests/test_async.py -x -q -k "declarative and (ordinary or exactly_once or invocation_only or handler or parity)"</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_builder.py --include tests/test_async.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_mypyc_guard.py tests/test_builder.py tests/test_async.py -x -q -k "mypyc or (declarative and (ordinary or exactly_once or invocation_only or handler or parity))"</automated>
  </verify>
  <done>Ordinary sync and async dispatch execute only the canonical matched declarative handler once through one shared boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate capacity and replace linear history trimming with bounded O(1) eviction</name>
  <read_first>
    - src/fast_fsm/core.py: history slots/initialization, enable_history, disable_history, history, sync/async record append, clone
    - tests/test_advanced_functionality.py: TestTransitionHistory
    - tests/test_async.py: history behavior
    - tests/test_performance_benchmarks.py: trigger/history benchmarks
    - .planning/phases/16-canonical-graph-dispatch-invariants/16-CONTEXT.md: D-17 and D-18
  </read_first>
  <files>src/fast_fsm/core.py, tests/test_advanced_functionality.py, tests/test_async.py, tests/test_performance_benchmarks.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - Capacity 0, negative, bool, null, float, and other non-integer values fail immediately without changing prior enabled/disabled buffer, records, or max capacity.
    - Valid positive re-enable replaces any buffer with an empty deque of that maxlen; overflow evicts oldest in O(1) and preserves chronological order.
    - `history` returns a new list each read; mutating it never changes internal records; disable/clone behavior remains compatible.
    - Disabled sync/async transitions allocate no deque and retain the single None hot-path branch; compiled trigger remains >=200,000 ops/sec.
  </behavior>
  <action>
    Write RED tests for all invalid capacity types/values, preservation of an existing buffer on failure, valid reset-on-re-enable, capacity one, FIFO overflow, timestamp/order, defensive list identity/mutation, disabled behavior, clone behavior, and sync/async parity. Replace list storage and front deletion with `collections.deque(maxlen=max_entries)` only after validating a positive non-bool integer per D-17/D-18. Keep `None` as disabled and `list(self._history)` at the public boundary. Extend the existing performance test to exercise steady-state bounded overflow and assert the established compiled trigger floor without fabricating an exact local baseline. Update `.specify/memory/spr-core-api.md` with positive-capacity, reset, deque FIFO, and copy-on-read behavior in this same owning commit. Run `uv run ruff format src/fast_fsm/core.py tests/test_advanced_functionality.py tests/test_async.py tests/test_performance_benchmarks.py`, `uv run ruff check --fix` on the same files, and the non-mutating Ruff check before history tests, mypyc/slots, and environment-labeled pure/compiled performance checks.
  </action>
  <threats>T-16-09, T-16-12</threats>
  <acceptance_criteria>
    - Every LIFE-07 probe edge is explicit: invalid/null/empty-equivalent capacity, capacity one, repeated enable, overflow ordering, copy-on-read, disabled, clone, sync, and async.
    - Invalid capacity leaves the entire previous history configuration unchanged.
    - Source contains no list-front deletion on the history append path; bounded deque handles eviction.
    - Disabled history remains allocation-free after construction and compiled throughput remains at least 200,000 ops/sec.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_advanced_functionality.py tests/test_async.py tests/test_performance_benchmarks.py</automated>
    <automated>uv run ruff check --fix src/fast_fsm/core.py tests/test_advanced_functionality.py tests/test_async.py tests/test_performance_benchmarks.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_advanced_functionality.py tests/test_async.py tests/test_performance_benchmarks.py</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_advanced_functionality.py --include tests/test_async.py --include tests/test_performance_benchmarks.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_advanced_functionality.py tests/test_async.py tests/test_performance_benchmarks.py -x -q -k "history"</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_advanced_functionality.py --include tests/test_async.py --include tests/test_performance_benchmarks.py --include .specify/memory/spr-core-api.md -- uv run python tools/release_evidence.py slots-policy --json</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_advanced_functionality.py --include tests/test_async.py --include tests/test_performance_benchmarks.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_performance_benchmarks.py -x -q -k "trigger_min_throughput or history_enabled"</automated>
  </verify>
  <done>History rejects invalid capacities atomically and uses bounded O(1) FIFO storage with defensive public copies and preserved hot-path performance.</done>
</task>

</tasks>

<threat_model>
## ASVS Target

OWASP ASVS Level 1 applies to in-process dispatch integrity and resource-bounded history configuration.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declarative metadata -> ordinary dispatch | Metadata-selected user code may cause externally visible side effects and must execute at most once. |
| history capacity -> bounded storage | Caller-controlled capacity must not create invalid or linear-eviction state. |
| internal history -> public list | Callers must not gain mutation access to the internal buffer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Test Evidence | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|---------------|---------------|
| T-16-08 | Tampering | declarative side effects | high | mitigate | Resolve canonical metadata and invoke through one ordinary seam; helpers delegate. | Sync/async exact counter, mismatch, and helper tests. | Handler failure/commit ordering remains intentionally unspecified until Phase 17. |
| T-16-09 | Denial of Service/Tampering | history capacity/eviction | medium | mitigate | Validate positive non-bool integer before assignment and use bounded deque. | Invalid-preservation, FIFO overflow, capacity-one, performance tests. | Callers may still choose a very large positive capacity and bear its bounded memory cost. |
| T-16-10 | Tampering | lifecycle ordering | low | accept | Constrain Phase 16 tests to successful exactly-once parity and preserve existing relative placement pending Phase 17. | Review assertion inventory for absence of order/failure contracts. | Existing callback failure behavior remains until Phase 17 hardens it. |
| T-16-12 | Denial of Service | hot-path overhead | high | mitigate | Keep disabled None branch, standard deque append, shared dispatch seam, and compiled floor. | Pure/compiled throughput and history ratio tests. | Hardware observations vary; fixed floor remains authoritative. |
</threat_model>

## Artifacts this phase produces

- New private core records: `_GraphTransition`, `_GraphSnapshot`, and `_PreparedTransition`.
- New private core seams: `_resolve_canonical_state`, `_normalize_transition_request`, `_commit_transition_plan`, `_graph_snapshot`, `_prepare_transition`, `_condition_children`, `_contains_async_requirement`, `_evaluate_condition_sync`, `_evaluate_condition_async`, `_resolve_declarative_handler`, `_invoke_declarative_handler`, and `_invoke_declarative_handler_async`.
- New private builder seams: `FSMBuilder._ensure_mutable` and `FSMBuilder._preflight_async_requirements`.
- New StateMachine field: `_graph_version`; existing `_history` changes to an optional bounded deque while its public property remains a list copy.
- New files: `tests/test_graph_invariants.py`, `tools/phase16_isolated_verify.py`, and `.planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md`.
- Isolation helper interface: `--mode task --build-mode pure|compiled --include PATH ... -- COMMAND` overlays explicit working-tree files; `graph`, `baseline-write`, `baseline-check`, and `phase16` suites provide fixed fail-closed gates.
- Named test fixtures/helpers: `graph_fingerprint()`, `builder_staging_fingerprint()`, `RecordingCondition`, `RecordingAsyncCondition`, `ShortCircuitCondition`, `make_negated_cycle()`, `make_and_cycle()`, `make_or_cycle()`, `make_not_cycle()`, `make_shared_condition_dag()`, `DeclarativeInvocationCounter`, `AsyncDeclarativeInvocationCounter`, and `_invoke_and_ignore_phase17_outcome()`.
- Updated owning SPR: `.specify/memory/spr-core-api.md` changes in the same commits as declarative invocation and bounded-history behavior.
- Modified public signatures without removals: Condition/FuncCondition/CompiledFuncCondition/NegatedCondition/AsyncCondition and built-in condition-template `check*` methods accept and forward `*args, **kwargs`.
- No new public exported symbol, public topology format, dependency, schema, service integration, or installed artifact.

<verification>
  <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_builder.py --include tests/test_async.py --include tests/test_advanced_functionality.py --include tests/test_performance_benchmarks.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_builder.py tests/test_async.py -x -q -k "declarative"</automated>
  <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_builder.py --include tests/test_async.py --include tests/test_advanced_functionality.py --include tests/test_performance_benchmarks.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_advanced_functionality.py tests/test_performance_benchmarks.py -x -q -k "history"</automated>
  <automated>task typecheck-mypy</automated>
</verification>

<success_criteria>
- GRAF-07/GRAF-08/LIFE-07 and D-15 through D-18 are executable sync/async contracts.
- Declarative success side effects occur once without Phase 17 semantics leaking into tests.
- History capacity and eviction are safe, bounded, and performance-verified.
</success_criteria>

<output>
Create `.planning/phases/16-canonical-graph-dispatch-invariants/16-04-SUMMARY.md` when done.
</output>
