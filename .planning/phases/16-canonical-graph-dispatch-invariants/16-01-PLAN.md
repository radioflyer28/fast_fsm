---
phase: 16-canonical-graph-dispatch-invariants
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - tests/test_graph_invariants.py
  - tests/test_mypyc_guard.py
  - tools/phase16_isolated_verify.py
  - .specify/memory/spr-core-api.md
  - .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md
autonomous: true
requirements:
  - GRAF-01
  - GRAF-02
  - GRAF-03
  - GRAF-08
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-01, D-02, D-03, and D-04, only exact registered State identities can become transition endpoints; unknown, foreign same-name, duplicate-source, null, or otherwise invalid requests fail before mutation."
    - "Per D-05 and D-08, the per-machine graph version changes only for successful topology changes and the declared initial state remains independent of current-state movement."
    - "Per D-06 and D-07, library tools can request a fresh, deterministically ordered, structurally immutable internal graph snapshot without changing public snapshot() or to_dict() schemas."
    - "The registry and dispatch authority remains dictionary-backed, add_state()/add_transition() remain O(1) for single endpoints, core.py remains the sole mypyc compilation unit, and no public symbol/signature is removed."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Canonical state/endpoint resolution, validate-then-commit topology mutation, graph version, and private immutable graph snapshot"
      contains: "_GraphSnapshot"
    - path: "tests/test_graph_invariants.py"
      provides: "Wave 0 real-object contract tests and graph fingerprint assertions for GRAF-01/02/03/08"
    - path: "tests/test_mypyc_guard.py"
      provides: "Structural proof that new core records honor the existing mypyc/slots boundary"
    - path: "tools/phase16_isolated_verify.py"
      provides: "Wave 0 working-tree-overlay runner with fail-closed pure/compiled isolation, pre-import origin assertions, task commands, baseline modes, and final-suite execution"
    - path: ".specify/memory/spr-core-api.md"
      provides: "Topology behavior and private snapshot/version contract updated in the same commits as their owning implementation changes"
    - path: ".planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md"
      provides: "Environment-labeled before-change pure and compiled semantic/performance observations"
  key_links:
    - from: "StateMachine.add_state/add_transition"
      to: "StateMachine._states/_transitions/_graph_version"
      via: "canonical resolution followed by one commit section"
      pattern: "_resolve_canonical_state|_commit_transition_plan"
    - from: "StateMachine._graph_snapshot"
      to: "StateMachine._states/_transitions/_initial_state"
      via: "fresh sorted tuple-backed projection"
      pattern: "_GraphSnapshot"
    - from: "tests/test_graph_invariants.py"
      to: "StateMachine topology"
      via: "identity-preserving before/after graph fingerprint"
      pattern: "graph_fingerprint"
  prohibitions:
    - "[UNVERIFIED — no SPEC] Do not introduce a public topology snapshot or serialization compatibility promise; the graph snapshot is private and Phase 19 owns tool migration."
    - "[UNVERIFIED — no SPEC] Do not add locks, ownership fields, or concurrency guarantees; Phase 18 owns concurrent mutation semantics."
    - "[UNVERIFIED — no SPEC] Do not replace dictionary-backed topology or add a runtime graph dependency."
---

<objective>
Prove and implement one canonical topology path from state registration through transition insertion to a fresh immutable tool snapshot.

Purpose: Establish the phase's architectural tracer and Wave 0 contract before dispatch, builder, declarative, or history expansions depend on it.
Output: Canonical topology internals, focused invariant tests, compiled-boundary guards, and before-change performance evidence.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-CONTEXT.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-RESEARCH.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-PATTERNS.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-VALIDATION.md
@.planning/phases/15-release-baseline-evidence-harness/15-VERIFICATION.md
@src/fast_fsm/core.py
@tests/test_mypyc_guard.py
@evidence/release-baseline.json
@.specify/decisions/ADR-003-mypyc-compilation-boundary.md

<interfaces>
- `StateMachine.__slots__` owns all runtime fields; add `_graph_version` there and nowhere else.
- `StateMachine._states: Dict[str, State]` and `_transitions: Dict[str, Dict[str, TransitionEntry]]` remain the O(1) authority.
- `add_state(state: State) -> None` and `add_transition(trigger, from_state, to_state, condition=None, *, unless=None) -> None` retain their public signatures.
- `snapshot() -> Dict[str, Any]` remains the public runtime-state schema and is not the graph snapshot.
- New private artifacts are `_GraphTransition`, `_GraphSnapshot`, `_resolve_canonical_state`, `_normalize_transition_request`, `_commit_transition_plan`, and `_graph_snapshot`.
- Test/evidence artifacts introduced here are `graph_fingerprint()` and the committed `tools/phase16_isolated_verify.py` runner. The runner exposes `--mode task --build-mode pure|compiled --include PATH ... -- COMMAND`, `--suite graph`, `--suite baseline-write`, `--suite baseline-check`, and `--suite phase16`; task mode overlays the named current-working-tree files, including uncommitted changes, onto a temporary committed-HEAD export before setup.
</interfaces>

<flagged_assumptions>
- [ASSUMPTION — no SPEC] Snapshot immutability is structural: records/collections cannot be mutated, while canonical user-extensible State and Condition references remain identity-bearing rather than deep-frozen.
- [ASSUMPTION — no SPEC] Graph version starts at 0 after constructor baseline registration; an exactly identical transition replacement is a no-op; clone copies the source version and later diverges independently; each successful topology-changing compound public call (`add_transitions`, bidirectional, or emergency) advances version exactly once regardless of entry count.
- [ASSUMPTION — no SPEC] State names use exact Python string equality with no Unicode normalization. Empty and Unicode strings follow existing State construction semantics; null/non-State registration input raises TypeError without mutation.
- [ASSUMPTION — no SPEC] `_graph_snapshot()` provides a coherent structural view for a single owner. It caches nothing; an interrupted/failed assembly publishes nothing and mutates nothing. Phase 18 defines concurrent-owner behavior.
</flagged_assumptions>
</context>

<execution_constraints>
- Use `uv run` or Taskfile wrappers for every Python/test/build command. Preserve unrelated dirty/staged files and never use broad staging.
- Establish module origin before every pure or compiled observation. Use a clean git archive/worktree and do not remove developer native shadows.
- Keep `core.py` as the sole mypyc source, preserve the one runtime dependency, all public symbols/signatures, and the compiled trigger floor of 200,000 ops/sec.
- Do not migrate validation, visualization, JSON, or other tools to the snapshot in this plan; Phase 19 owns consumers.
</execution_constraints>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wave 0 tracer — canonical registration to immutable snapshot</name>
  <read_first>
    - src/fast_fsm/core.py: StateMachine.__slots__, __init__, _register_state, add_state, add_transition, snapshot, clone
    - tests/test_basic_functionality.py
    - tests/test_advanced_functionality.py: TestToDict and snapshot/clone coverage
    - tests/test_mypyc_guard.py
    - evidence/release-baseline.json
    - .planning/phases/16-canonical-graph-dispatch-invariants/16-VALIDATION.md
  </read_first>
  <files>tools/phase16_isolated_verify.py, src/fast_fsm/core.py, tests/test_graph_invariants.py, .specify/memory/spr-core-api.md, .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md</files>
  <behavior>
    - RED first: registering one new State then adding one valid transition produces a fresh snapshot whose state/transition rows are sorted tuples with canonical object identities, declared initial state, machine name, and graph version.
    - Same-object registration and current-state movement leave graph version unchanged; a genuine state or transition topology change advances it exactly once.
    - Snapshot field/collection mutation fails, later snapshots remain fresh, and public snapshot()/to_dict() shapes remain unchanged.
    - A one-state machine, including one with no transitions, yields a valid deterministic graph snapshot; null registration fails before mutation.
  </behavior>
  <action>
    Before any Phase 16 semantic or performance import, create `tools/phase16_isolated_verify.py` as the first Wave 0 implementation step. Its `--mode task --build-mode pure|compiled --include PATH ... -- COMMAND` interface must export committed HEAD to a temporary context, validate each explicit repository-relative include, overlay the current working-tree bytes for every named file (including uncommitted edits and new files), reject missing/out-of-repository paths, set the selected build mode before `uv sync --locked --all-groups`, and run the caller command only after an independent `fast_fsm.core` origin assertion. Pure task mode requires a `.py` origin and refuses native artifacts in the temporary source tree. Compiled task mode uses a distinct temporary context, builds native code fail-closed, requires a native-extension origin, and only then runs semantic or performance work. Neither mode may delete, import, or derive evidence from developer-checkout native shadows. Add fixed `graph`, `baseline-write`, `baseline-check`, and `phase16` suites for later tasks; baseline-write may atomically export only the generated manifest to its explicit destination, while baseline-check and phase16 run freshness in isolation, and phase16 also runs the full pure release gate in isolation. First use task mode to capture environment-labeled before-change pure and compiled observations in `16-PERFORMANCE-EVIDENCE.md`: commit/tree identity, asserted module origin, targeted baseline result, compiled trigger throughput, pure trigger throughput, and history-disabled observation. Do not rewrite release evidence yet. Then create `tests/test_graph_invariants.py` using real State/StateMachine objects and the exact test-only helper `graph_fingerprint()`; run the focused tracer through pure task mode and require RED before production changes. Implement tuple-backed `_GraphTransition` and `_GraphSnapshot`, add `_graph_version` to slots/initialization, and implement `_graph_snapshot()` as a fresh sorted projection over the authoritative dictionaries per D-05 through D-08. Implement exact-identity registration semantics per D-01, including TypeError for null/non-State input, same-object no-op, conflicting-object ValueError, and one version increment for a successful new registration. Preserve public schemas and clone the graph version. Update `.specify/memory/spr-core-api.md` with the exact-identity, structural-immutability, and clone-lineage behavior in this owning implementation commit. Run raw Ruff and blocking mypy in the checkout because they are static; run the focused tracer, slots policy, mypyc guard semantics, and compiled smoke only through task-mode isolation with the complete task file list overlaid.
  </action>
  <threats>T-16-01, T-16-03, T-16-11</threats>
  <acceptance_criteria>
    - D-01/D-05/D-06/D-08 are directly asserted by the tracer with identity, version, deterministic ordering, declared-initial, and immutability checks.
    - Empty transition content and a single registered state snapshot successfully; null/non-State registration rejects without changing the graph fingerprint.
    - Unicode and empty names retain exact existing Python-string semantics without normalization or equality-based object substitution.
    - Public `snapshot()` and `to_dict()` output remain byte-for-byte shape compatible, and no new public export appears.
    - Before evidence records both `.py` and native module origins before measuring semantics/throughput and does not delete developer artifacts.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_graph_invariants.py</automated>
    <automated>uv run ruff check --fix src/fast_fsm/core.py tests/test_graph_invariants.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_graph_invariants.py</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include tools/phase16_isolated_verify.py --include src/fast_fsm/core.py --include tests/test_graph_invariants.py --include .specify/memory/spr-core-api.md --include .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md -- uv run pytest tests/test_graph_invariants.py -x -q -k "snapshot or version or registration"</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include tools/phase16_isolated_verify.py --include src/fast_fsm/core.py --include tests/test_graph_invariants.py --include .specify/memory/spr-core-api.md --include .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md -- uv run python tools/release_evidence.py slots-policy --json</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include tools/phase16_isolated_verify.py --include src/fast_fsm/core.py --include tests/test_graph_invariants.py --include .specify/memory/spr-core-api.md --include .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md -- uv run pytest tests/test_mypyc_guard.py tests/test_graph_invariants.py -x -q -k "snapshot or version or registration or mypyc"</automated>
  </verify>
  <done>The thinnest production topology path is test-first, canonical, versioned, immutable to tools, compiled-boundary-safe, and measured before dependent refactors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand endpoint normalization and multi-source atomic rejection</name>
  <read_first>
    - src/fast_fsm/core.py: add_transition, add_transitions, add_bidirectional_transition, add_emergency_transition, convenience constructors
    - tests/test_graph_invariants.py
    - tests/test_boundary_negative.py
    - .planning/phases/16-canonical-graph-dispatch-invariants/16-RESEARCH.md: Concrete Failure-Mode Test Matrix
  </read_first>
  <files>src/fast_fsm/core.py, tests/test_graph_invariants.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - Unknown string or foreign same-name object source/target raises ValueError and preserves registry identities, transition identities/guards, graph version, and current state.
    - A source list is materialized completely; empty source lists, duplicate canonical sources, null endpoints, invalid guard/unless combinations, and a valid-first/invalid-later sequence reject the whole call before any bucket or entry is written.
    - `add_transitions()` validates its complete batch before insertion, so a bad later tuple cannot retain an earlier transition.
    - `add_bidirectional_transition()` preflights both directed legs before either mutation; an invalid second-leg endpoint or guard leaves the complete graph fingerprint unchanged, while a topology-changing successful compound call advances graph version exactly once.
    - `add_emergency_transition()` routes its complete all-source request through the same atomic multi-source normalization/commit path, never inserts only a prefix of sources, and advances graph version exactly once when it changes topology.
    - Ordinary add_transition never registers endpoints; from_states/quick_build/from_dict may register their complete state set first and then use the strict path.
  </behavior>
  <action>
    Extend Wave 0 tests to enumerate invalid string/object/null endpoints, exact identity versus Python equality, duplicate sources, empty/multi-source content, batch `add_transitions()` failure, condition/unless conflicts, Unicode-name identity, and `graph_fingerprint()` equality after rejection. Add direct compound-helper cases: make the second leg of `add_bidirectional_transition()` invalid after a valid first leg and prove no mutation; prove a topology-changing successful bidirectional call advances graph version exactly once; and prove `add_emergency_transition()` delegates one complete all-source request to the same atomic multi-source path and likewise advances once. Implement `_resolve_canonical_state`, `_normalize_transition_request`, and `_commit_transition_plan` so every source/target/guard is materialized and validated before any dictionary write, per D-02 through D-04. Treat duplicate canonical sources in one request as ValueError. Wrap `add_transitions()` and both compound helpers around complete prepared transactions instead of mutable per-leg/per-entry delegation. Preserve single-endpoint dictionary lookup/insertion O(1); multi-source/batch work may be O(n) in request size. Version advances exactly once per successful topology-changing public operation and not for an exact no-op replacement. Update `.specify/memory/spr-core-api.md` with strict ordinary, batch, bidirectional, emergency, and compound-version semantics in this same owning commit. Run `uv run ruff format src/fast_fsm/core.py tests/test_graph_invariants.py`, `uv run ruff check --fix src/fast_fsm/core.py tests/test_graph_invariants.py`, and the non-mutating Ruff check before targeted validation.
  </action>
  <threats>T-16-01, T-16-02, T-16-03</threats>
  <acceptance_criteria>
    - Every GRAF-01 edge family from the spec-less probe is classified and asserted: invalid, foreign same-name, null, empty, duplicate, and later-invalid multi-source/batch input.
    - Rejected operations preserve the complete graph fingerprint, including absence of new transition buckets and stable version/current state.
    - Same-object identity is safe; distinct equality/name matches are rejected; exact Unicode strings are not normalized.
    - Convenience construction still succeeds by registering the declared set before strict transition insertion.
    - Bidirectional second-leg rejection preserves `graph_fingerprint()` exactly; successful bidirectional and emergency topology-changing calls each advance graph version exactly once; emergency addition proves the shared atomic all-source path.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_graph_invariants.py</automated>
    <automated>uv run ruff check --fix src/fast_fsm/core.py tests/test_graph_invariants.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_graph_invariants.py</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_graph_invariants.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_graph_invariants.py -x -q -k "endpoint or atomic or registry or batch or bidirectional or emergency"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_graph_invariants.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_basic_functionality.py tests/test_boundary_negative.py tests/test_advanced_functionality.py -x -q -k "transition or state or quick_build or from_states or from_dict"</automated>
    <automated>task typecheck-mypy</automated>
  </verify>
  <done>All ordinary and batched topology mutations resolve exact canonical identities and either commit completely with one version change or leave no trace.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the compiled record and single-unit topology boundary</name>
  <read_first>
    - src/fast_fsm/core.py: new graph records and StateMachine slots
    - tests/test_mypyc_guard.py
    - .specify/decisions/ADR-003-mypyc-compilation-boundary.md
    - setup.py
    - tools/release_evidence.py: slots-policy and source-origin commands
  </read_first>
  <files>tests/test_mypyc_guard.py, src/fast_fsm/core.py, tools/phase16_isolated_verify.py, .specify/memory/spr-core-api.md</files>
  <action>
    Extend the existing fail-closed AST guards only as needed to prove `_GraphTransition` and `_GraphSnapshot` are tuple-backed/slot-safe, `_graph_version` is declared in StateMachine slots, public exports remain unchanged, and `setup.py` still compiles only `src/fast_fsm/core.py`. Harden the Wave 0 runner without changing its task-mode contract: prove explicit includes overlay current working-tree bytes rather than committed bytes, pure setup rejects any native artifact and asserts `.py` origin, compiled setup uses a separate temporary tree and fails closed on build/origin errors, and trusted child commands start with the temporary repository as their working directory without a containment guarantee. Its graph suite runs the graph/mypyc guards in both modes. Its baseline-check suite overlays the current manifest and runs source preflight plus read-only freshness; its phase16 suite overlays the fixed Phase 16 source/test/docs/evidence inventory, runs identical semantic tests in pure/compiled contexts, runs compiled trigger/history gates, and runs baseline freshness plus the complete pure release gate inside the asserted-pure context. It must never delete, import, or trust native shadows from the developer checkout. Run raw Ruff and blocking mypy in the checkout, then execute the graph proof through task mode in both build modes with all Task 3 files overlaid. If pinned mypyc rejects the selected tuple record shape, replace it behind the same private field contract and update the SPR representation statement in this same commit rather than weakening immutability or changing the public API.
  </action>
  <threats>T-16-03, T-16-11, T-16-12</threats>
  <acceptance_criteria>
    - Pinned mypy/mypyc accepts the new graph records and StateMachine layout.
    - Pure and compiled origins are asserted before behavior, and the same topology suite passes in both modes.
    - `setup.py` retains exactly one mypyc input and conditions/templates remain interpreted.
    - No developer native shadow is removed or used as proof of the clean pure path.
    - The committed helper independently creates pure and compiled archives, selects mode before setup, asserts origin before semantic imports, fails compiled setup closed, and executes the same graph suite in both modes.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_mypyc_guard.py tools/phase16_isolated_verify.py</automated>
    <automated>uv run ruff check --fix src/fast_fsm/core.py tests/test_mypyc_guard.py tools/phase16_isolated_verify.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_mypyc_guard.py tools/phase16_isolated_verify.py</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include tests/test_mypyc_guard.py --include src/fast_fsm/core.py --include tools/phase16_isolated_verify.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_mypyc_guard.py tests/test_graph_invariants.py -x -q</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include tests/test_mypyc_guard.py --include src/fast_fsm/core.py --include tools/phase16_isolated_verify.py --include .specify/memory/spr-core-api.md -- uv run python tools/release_evidence.py slots-policy --json</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include tests/test_mypyc_guard.py --include src/fast_fsm/core.py --include tools/phase16_isolated_verify.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_mypyc_guard.py tests/test_graph_invariants.py -x -q</automated>
  </verify>
  <done>The canonical topology tracer is proven in pure and native modes without widening the runtime dependency or compilation boundary.</done>
</task>

</tasks>

<threat_model>
## ASVS Target

OWASP ASVS Level 1 applies; the relevant control theme is in-process input validation and business-logic integrity. The project blocks on unresolved high-severity threats.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller state/endpoint objects -> canonical registry | Untrusted names and object identities cross into mutable topology. |
| mutation request -> graph commit | A partially validated request must not write authoritative dictionaries or version. |
| mutable graph -> internal tool snapshot | Tools require a coherent immutable structural view without gaining mutation capability. |
| developer checkout -> pure/compiled evidence | Native shadows can spoof which implementation was exercised. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Test Evidence | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|---------------|---------------|
| T-16-01 | Spoofing/Tampering | state and endpoint identity | high | mitigate | Resolve names and objects through the registry and require exact object identity before commit. | Foreign-source/target and equality/Unicode cases compare complete graph fingerprints. | User objects remain mutable by their owners; snapshot immutability is structural. |
| T-16-02 | Tampering | multi-source/batch mutation | high | mitigate | Materialize and validate the whole request/batch before one commit section. | Valid-first/invalid-later, duplicate, empty, null, and guard-conflict tests. | Phase 18 still owns inter-owner races around the commit section. |
| T-16-03 | Repudiation/Tampering | graph version/snapshot | medium | mitigate | Advance only after successful topology change; assemble fresh tuples from canonical dictionaries. | Success/no-op/rejection/current-state version matrix and mutation attempts. | Version is process-local and not a public persisted audit log. |
| T-16-11 | Spoofing | pure/compiled module origin | high | mitigate | Export committed HEAD, overlay only explicit current-working-tree task files, exclude developer native shadows, and assert `.py` or native origin before behavior. | Task-mode pure/compiled targeted suites plus release-evidence origin preflight. | Final installed-wheel identity remains Phase 20. |
| T-16-12 | Denial of Service | dispatch/record layout regression | high | mitigate | Keep sorting off hot paths, preserve dictionaries/slots, compile early, and retain throughput gates. | mypyc build, slots policy, baseline evidence, performance tests. | Hardware-level throughput varies; environment-labeled observations accompany the fixed floor. |
</threat_model>

## Artifacts this phase produces

- New private core records: `_GraphTransition`, `_GraphSnapshot`, and `_PreparedTransition`.
- New private core seams: `_resolve_canonical_state`, `_normalize_transition_request`, `_commit_transition_plan`, `_graph_snapshot`, `_prepare_transition`, `_condition_children`, `_contains_async_requirement`, `_evaluate_condition_sync`, `_evaluate_condition_async`, `_resolve_declarative_handler`, `_invoke_declarative_handler`, and `_invoke_declarative_handler_async`.
- New private builder seams: `FSMBuilder._ensure_mutable` and `FSMBuilder._preflight_async_requirements`.
- New StateMachine field: `_graph_version`; existing `_history` changes to an optional bounded deque while its public property remains a list copy.
- New files: `tests/test_graph_invariants.py` and `.planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md`.
- New test/evidence helpers: `graph_fingerprint()` in `tests/test_graph_invariants.py` and `tools/phase16_isolated_verify.py` with working-tree-overlay task mode plus `graph`, `baseline-write`, `baseline-check`, and `phase16` suites.
- Updated owning SPR: `.specify/memory/spr-core-api.md` changes in the same commits as topology/snapshot behavior.
- Modified public signatures without removals: Condition/FuncCondition/CompiledFuncCondition/NegatedCondition/AsyncCondition and built-in condition-template `check*` methods accept and forward `*args, **kwargs`.
- No new public exported symbol, public topology format, dependency, schema, service integration, or installed artifact.

<verification>
  <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_graph_invariants.py --include tests/test_mypyc_guard.py --include tools/phase16_isolated_verify.py --include .specify/memory/spr-core-api.md --include .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md -- uv run pytest tests/test_graph_invariants.py tests/test_mypyc_guard.py -x -q</automated>
  <automated>task typecheck-mypy</automated>
  <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_graph_invariants.py --include tests/test_mypyc_guard.py --include tools/phase16_isolated_verify.py --include .specify/memory/spr-core-api.md --include .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md -- uv run python tools/release_evidence.py slots-policy --json</automated>
</verification>

<success_criteria>
- GRAF-01/02/03's canonical identity, atomic rejection, version, declared-initial, ordering, immutability, and single-owner snapshot semantics are executable contracts.
- The first vertical topology slice passes in both clean pure and compiled contexts.
- D-01 through D-08 are implemented without any deferred Phase 17–20 behavior.
</success_criteria>

<output>
Create `.planning/phases/16-canonical-graph-dispatch-invariants/16-01-SUMMARY.md` when done.
</output>
