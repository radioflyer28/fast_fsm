---
phase: 16-canonical-graph-dispatch-invariants
plan: 03
type: execute
wave: 2
depends_on:
  - 16-02
files_modified:
  - src/fast_fsm/core.py
  - tests/test_builder.py
  - tests/test_async.py
  - .specify/memory/spr-core-api.md
autonomous: true
requirements:
  - GRAF-02
  - GRAF-04
  - GRAF-05
  - GRAF-08
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-09, every builder mutator, callback registrar, and force-mode method fails immediately after a successful build, while a failed build leaves staging repairable and no machine cached."
    - "Per D-10, successful materialization uses a local candidate, publishes the cache only after all wiring succeeds, and repeated build() calls return the identical machine object."
    - "Per D-11, auto mode detects nested async conditions/declarative handlers/queued callbacks; explicit async remains authoritative and explicit sync rejects incompatible content before allocation."
    - "Empty transition/callback staging still builds a one-state machine, registration/wiring order is deterministic, and repeated build does not replay callbacks or mutate topology/version."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Builder freeze guard, async preflight, and publish-on-success transaction"
      contains: "_ensure_mutable"
    - path: "tests/test_builder.py"
      provides: "Complete mutator freeze, failed-build repair, empty/single content, repeated identity, and deterministic ordering tests"
    - path: "tests/test_async.py"
      provides: "Auto/forced mode nested async materialization and execution proof"
    - path: ".specify/memory/spr-core-api.md"
      provides: "Builder staging/freeze/publication behavior updated with its owning implementation commits"
  key_links:
    - from: "FSMBuilder mutators/registrars/force methods"
      to: "FSMBuilder._ensure_mutable"
      via: "first-statement freeze check"
      pattern: "_ensure_mutable"
    - from: "FSMBuilder.build"
      to: "StateMachine/AsyncStateMachine"
      via: "preflight then local candidate then cache assignment last"
      pattern: "candidate|_machine"
    - from: "FSMBuilder._preflight_async_requirements"
      to: "_condition_children"
      via: "shared cycle-safe built-in wrapper classification from Plan 16-02"
      pattern: "_condition_children|_contains_async_requirement"
  prohibitions:
    - "[UNVERIFIED — no SPEC] Do not warn and discard async behavior in explicit sync mode; fail before candidate creation."
    - "[UNVERIFIED — no SPEC] Do not freeze after a failed build or cache a partially wired candidate."
    - "[UNVERIFIED — no SPEC] Do not create a new public builder/wrapper protocol or change fluent public signatures."
---

<objective>
Make builder materialization transactional, frozen after success, repairable after failure, and consistent with recursive async requirements.

Purpose: Ensure builder users cannot silently mutate ignored staging or receive a partially built/categorized machine.
Output: One builder mutability guard, one complete async preflight, publish-on-success caching, and exhaustive lifecycle tests.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-CONTEXT.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-RESEARCH.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-PATTERNS.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-VALIDATION.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-02-SUMMARY.md
@src/fast_fsm/core.py
@tests/test_builder.py
@tests/test_async.py

<interfaces>
- Existing fluent methods `add_state`, `add_transition`, `on_enter`, `on_exit`, `on_enter_async`, `on_exit_async`, `force_async`, and `force_sync` retain signatures/return `self` before build.
- `_machine: Optional[StateMachine]` remains the successful cache and freeze marker.
- `_detect_async_requirements` converges on Plan 16-02's built-in child classifier; new private builder seams are `_ensure_mutable` and `_preflight_async_requirements`.
- `build() -> Union[StateMachine, AsyncStateMachine]` retains repeated object identity.
- The named test helper `builder_staging_fingerprint()` captures staged state object identities, staged transition/callback identities, mode flags, `_machine` cache identity, and—when published—the eventual machine `graph_fingerprint()`.
</interfaces>

<flagged_assumptions>
- [ASSUMPTION — no SPEC] Empty staging means the required initial state only; it is valid and deterministic. A null/non-State initial or added state is rejected before materialization.
- [ASSUMPTION — no SPEC] Staged dictionaries/lists preserve insertion order for registration and callback wiring; repeated build returns the cache and performs no second traversal.
- [ASSUMPTION — no SPEC] Active wrapper cycles fail with ValueError during preflight; no candidate allocation or cache assignment occurs.
</flagged_assumptions>
</context>

<execution_constraints>
- Use the canonical state/endpoint and guard-wrapper seams from Plans 16-01/02 rather than reimplementing them inside FSMBuilder.
- Run every import-bearing test or semantic check through the Wave 0 isolation helper's pure/compiled task mode with the task's current working-tree files explicitly overlaid; raw Ruff and static mypy may run in the checkout.
- Preserve fluent method names/signatures, public class selection behavior, one core.py mypyc unit, slots, and the single runtime dependency.
- Do not decide declarative handler lifecycle ordering/failure semantics; this plan only detects async metadata and wires the existing behavior.
</execution_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze every builder mutation only after successful publication</name>
  <read_first>
    - src/fast_fsm/core.py: FSMBuilder.__slots__, __init__, all mutators/registrars/force methods, build
    - tests/test_builder.py: basics, gaps, callbacks, repeated-build tests
    - tests/test_basic_functionality.py: real state registration behavior
    - .planning/phases/16-canonical-graph-dispatch-invariants/16-01-SUMMARY.md
  </read_first>
  <files>src/fast_fsm/core.py, tests/test_builder.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - Before build, all fluent methods mutate staging and return self; after success, each immediately raises RuntimeError without changing staging/cache/machine topology.
    - Before build, adding the exact same State object again is an idempotent staging no-op; adding a distinct State object with the same name raises ValueError and preserves `builder_staging_fingerprint()` exactly.
    - Empty staging builds one canonical initial state; one/many staged items preserve deterministic insertion and callback order.
    - A wiring failure leaves `_machine is None`, staged content intact and mutable, and a repaired retry produces a fully wired machine.
    - Repeated successful builds return `is`-identical objects and do not re-register topology, advance graph version, or invoke/register callbacks twice.
  </behavior>
  <action>
    Add the exact test-only `builder_staging_fingerprint()` helper and a parameterized RED matrix covering every builder mutator, callback registrar, and force method before/after successful build; include empty/one/many content, Unicode names, null/invalid state, deterministic ordering, repeated identity, and injected real wiring failure followed by repair. Per D-01/GRAF-02, explicitly add the same State object twice and prove an idempotent staged/cache/topology no-op; then add a distinct same-name State and require ValueError with unchanged staging, cache, and eventual built topology. Implement builder `add_state()` with the same identity-canonical three-way rule as the runtime registry. Implement `_ensure_mutable()` and call it as the first operation in each mutating public method per D-09. Refactor `build()` to create and fully wire a local `candidate`, assign `_machine` only after the final successful callback registration/log preparation, and return the cache unchanged on repeats per D-10. Avoid rollback logic by publishing nothing early. Update `.specify/memory/spr-core-api.md` with builder identity-staging, freeze, and publish-on-success behavior in this same owning commit. Run `uv run ruff format src/fast_fsm/core.py tests/test_builder.py`, `uv run ruff check --fix src/fast_fsm/core.py tests/test_builder.py`, and the non-mutating Ruff check before targeted validation and blocking mypy.
  </action>
  <threats>T-16-07, T-16-02, T-16-03</threats>
  <acceptance_criteria>
    - Every mutator/registrar/force method is covered by the post-success RuntimeError matrix.
    - Same-object staging is idempotent; a distinct same-name object raises ValueError; `builder_staging_fingerprint()` proves staged state, cache, and eventual topology remain unchanged.
    - Failed construction publishes no cache and remains repairable without losing or duplicating staging.
    - Empty and single-content builder probes are explicitly resolved, and repeated build identity/order is stable.
    - No partial machine escapes through `_machine`, return value, callback side effect, or graph version.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_builder.py</automated>
    <automated>uv run ruff check --fix src/fast_fsm/core.py tests/test_builder.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_builder.py</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_builder.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_builder.py -x -q -k "freeze or repair or idempotent or same_name or ordering or empty"</automated>
    <automated>task typecheck-mypy</automated>
  </verify>
  <done>A successful cached builder is fully frozen and idempotent; any failed build remains unpublished, mutable, and repairable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preflight nested async requirements before choosing or allocating a machine</name>
  <read_first>
    - src/fast_fsm/core.py: FSMBuilder._detect_async_requirements, build, declarative metadata, callback queues
    - src/fast_fsm/core.py: Plan 16-02 _condition_children/_contains_async_requirement
    - src/fast_fsm/conditions.py: AsyncCondition and NegatedCondition
    - src/fast_fsm/condition_templates.py: AndCondition, OrCondition, NotCondition
    - tests/test_builder.py: async detection and declarative fixtures
    - tests/test_async.py: nested wrapper behavior
  </read_first>
  <files>src/fast_fsm/core.py, tests/test_builder.py, tests/test_async.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - Auto mode detects async leaves through Negated/And/Or/Not, declarative handler guards/async methods, and queued enter/exit async callbacks, then builds AsyncStateMachine.
    - Explicit async remains async for sync-only or empty content.
    - Explicit sync with any nested async requirement raises before candidate allocation and retains repairable staging; it never warns/drops behavior.
    - Cycles reject deterministically; shared condition DAGs and deeply nested acyclic wrappers terminate and classify correctly.
  </behavior>
  <action>
    Add tests first for each built-in wrapper, nested combinations, declarative async handler/guard metadata, queued async callbacks, explicit modes, active cycles, shared DAGs, failed-build repair, and execution of the selected async machine. Implement `_preflight_async_requirements()` over all staged sources and reuse the exact Plan 16-02 child-edge classifier/cycle semantics per D-11. Run preflight before candidate allocation. Auto mode selects from the preflight result, explicit async stays authoritative, and explicit sync raises RuntimeError or ValueError with the offending component identified instead of logging and discarding it. Ensure failed preflight does not freeze/cache the builder and that nested evaluation itself is proven by awaiting a real transition. Update `.specify/memory/spr-core-api.md` with preflight mode behavior in this same owning commit. Run `uv run ruff format src/fast_fsm/core.py tests/test_builder.py tests/test_async.py`, `uv run ruff check --fix` on the same files, and the non-mutating Ruff check before the async matrix, blocking mypy, and mypyc guard.
  </action>
  <threats>T-16-04, T-16-07, T-16-06</threats>
  <acceptance_criteria>
    - Every GRAF-05 failure-matrix family is tested: direct/nested async, all wrappers, declarative metadata, queued callbacks, cycles, explicit sync, explicit async, and auto mode.
    - Detection and runtime evaluation share the same child classification and cannot disagree about supported wrappers.
    - Explicit sync allocates/caches no candidate and drops no behavior when incompatibility is found.
    - The selected AsyncStateMachine executes and awaits the nested leaf successfully.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_builder.py tests/test_async.py</automated>
    <automated>uv run ruff check --fix src/fast_fsm/core.py tests/test_builder.py tests/test_async.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_builder.py tests/test_async.py</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_builder.py --include tests/test_async.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_builder.py tests/test_async.py -x -q -k "nested or wrapper or cycle or auto_detect or force_sync or async_callback"</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_builder.py --include tests/test_async.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_mypyc_guard.py tests/test_builder.py tests/test_async.py -x -q -k "mypyc or nested or wrapper or cycle or auto_detect or force_sync or async_callback"</automated>
  </verify>
  <done>Builder type selection and publication are a single deterministic preflight transaction across direct, wrapped, declarative, and callback async requirements.</done>
</task>

</tasks>

<threat_model>
## ASVS Target

OWASP ASVS Level 1 applies to construction input validation and business-logic integrity. No external service/authentication boundary exists.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged builder content -> machine candidate | Invalid endpoints, callbacks, or modes must not publish partial runtime state. |
| nested wrapper/declarative metadata -> type selection | Hidden async requirements must not be classified as synchronous. |
| successful cache -> later public mutators | Post-build calls must fail rather than silently changing ignored staging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Test Evidence | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|---------------|---------------|
| T-16-07 | Tampering | builder publication/cache | high | mitigate | Preflight first, local candidate, cache assignment last, complete freeze guard. | Failure/repair, every-mutator freeze, repeated identity, topology fingerprint tests. | User callback bodies may have side effects while being registered only if their API itself executes; existing registrars do not execute them. |
| T-16-04 | Denial of Service | recursive async detection | high | mitigate | Shared identity-aware traversal with cycle rejection. | Wrapper cycle/DAG/depth and explicit-mode tests. | Arbitrary third-party wrappers outside built-ins remain unsupported privately. |
| T-16-06 | Tampering | detected versus executed async semantics | medium | mitigate | Detection and evaluation consume the same child classifier; selected machine runs a real nested transition. | Auto-selection plus awaited execution integration tests. | Phase 17 later defines callback-failure ordering. |
| T-16-02 | Tampering | topology wiring | high | mitigate | Candidate uses canonical validate-then-commit APIs; no early cache. | Later-invalid wiring and graph fingerprint tests. | Phase 18 owns multi-owner races. |
</threat_model>

## Artifacts this phase produces

- New private core records: `_GraphTransition`, `_GraphSnapshot`, and `_PreparedTransition`.
- New private core seams: `_resolve_canonical_state`, `_normalize_transition_request`, `_commit_transition_plan`, `_graph_snapshot`, `_prepare_transition`, `_condition_children`, `_contains_async_requirement`, `_evaluate_condition_sync`, `_evaluate_condition_async`, `_resolve_declarative_handler`, `_invoke_declarative_handler`, and `_invoke_declarative_handler_async`.
- New private builder seams: `FSMBuilder._ensure_mutable` and `FSMBuilder._preflight_async_requirements`.
- New StateMachine field: `_graph_version`; existing `_history` changes to an optional bounded deque while its public property remains a list copy.
- New files: `tests/test_graph_invariants.py`, `tools/phase16_isolated_verify.py`, and `.planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md`.
- Isolation helper interface: `--mode task --build-mode pure|compiled --include PATH ... -- COMMAND` overlays explicit working-tree files; `graph`, `baseline-write`, `baseline-check`, and `phase16` suites provide fixed fail-closed gates.
- Named test fixtures/helpers: `graph_fingerprint()`, `builder_staging_fingerprint()`, `RecordingCondition`, `RecordingAsyncCondition`, `ShortCircuitCondition`, `make_negated_cycle()`, `make_and_cycle()`, `make_or_cycle()`, `make_not_cycle()`, and `make_shared_condition_dag()`.
- Updated owning SPR: `.specify/memory/spr-core-api.md` changes in the same commits as builder staging/freeze/preflight behavior.
- Modified public signatures without removals: Condition/FuncCondition/CompiledFuncCondition/NegatedCondition/AsyncCondition and built-in condition-template `check*` methods accept and forward `*args, **kwargs`.
- No new public exported symbol, public topology format, dependency, schema, service integration, or installed artifact.

<verification>
  <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_builder.py --include tests/test_async.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_builder.py tests/test_async.py -x -q -k "freeze or repair or idempotent or nested or wrapper or cycle or async"</automated>
  <automated>task typecheck-mypy</automated>
  <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_builder.py --include tests/test_async.py --include .specify/memory/spr-core-api.md -- uv run pytest tests/test_mypyc_guard.py tests/test_builder.py tests/test_async.py -x -q</automated>
</verification>

<success_criteria>
- GRAF-04/05/08 and D-09 through D-11 are fully observable in builder tests.
- No successful build can be mutated through builder staging, and no failed build can be mistaken for success.
- Async detection, machine choice, and runtime wrapper evaluation remain aligned.
</success_criteria>

<output>
Create `.planning/phases/16-canonical-graph-dispatch-invariants/16-03-SUMMARY.md` when done.
</output>
