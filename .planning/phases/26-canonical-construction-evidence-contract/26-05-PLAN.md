---
phase: 26-canonical-construction-evidence-contract
plan: 05
type: execute
wave: 4
depends_on:
  - 26-02
  - 26-03
  - 26-04
files_modified:
  - benchmarks/comparison/python_statemachine_2_5.py.lock
  - benchmarks/comparison/python_statemachine_3_2.py.lock
  - benchmarks/benchmark.py
  - Taskfile.yml
  - pyproject.toml
  - uv.lock
  - tests/test_competitor_benchmark_contract.py
autonomous: true
requirements:
  - PERF-05
  - PERF-06
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PERF-05: Adjacent uv script locks reproduce exact isolated `python-statemachine` 2.5.0 and 3.2.1 children whose runtime distribution versions and module origins match their requested lanes."
    - "Required semantic preflight values are validated before warmup/timing; identity or semantic contradictions terminate the run, while declared optional unsupported cells remain explicit and ratio-free."
    - "PERF-06: `task benchmark-compare` is the sole maintained manual observation entry point, writes canonical JSON to stdout by default, and accepts repository persistence only through an explicit output path."
    - "Ordinary project dependency groups, the project uv.lock root requirements, normal CI workflows, release evidence, tests, and quality tasks neither install nor time competitor distributions."
    - "Manual sample/operation/output/subprocess bounds prevent unbounded work, and no competitor rate or ratio is a CI/release threshold."
    - "The single `phase_bead_id` remains claimed through execution and any gap cycle; it is closed only after phase-level verification succeeds."
  artifacts:
    - path: "benchmarks/comparison/python_statemachine_2_5.py.lock"
      provides: "Generated adjacent uv resolution for exact historical child"
    - path: "benchmarks/comparison/python_statemachine_3_2.py.lock"
      provides: "Generated adjacent uv resolution for exact current child"
    - path: "benchmarks/benchmark.py"
      provides: "Compatibility entry point delegating to the isolated comparison parent"
    - path: "Taskfile.yml"
      provides: "Manual stdout-only benchmark-compare task with optional CLI args"
    - path: "pyproject.toml"
      provides: "Project dependency groups without competitor distributions"
    - path: "uv.lock"
      provides: "Regenerated ordinary project lock without competitor root requirements"
    - path: "tests/test_competitor_benchmark_contract.py"
      provides: "Static CI/dependency isolation and live low-count report contract"
  key_links:
    - from: "each PEP 723 child"
      to: "its adjacent `.lock`"
      via: "`uv lock --script` generated resolution consumed by `uv run --locked --script`"
      pattern: "python_statemachine_.*\.py\.lock"
    - from: "Taskfile benchmark-compare"
      to: "benchmarks/comparison/run_comparison.py"
      via: "manual uv project command and explicit CLI arguments"
      pattern: "run_comparison.py"
    - from: "ordinary CI all-groups sync"
      to: "pyproject.toml / uv.lock"
      via: "competitors absent from project dependency roots"
      pattern: "dependency-groups"
    - from: "26-01-SUMMARY.md `phase_bead_id`"
      to: "post-verification `bd close`"
      via: "26-05 records the still-claimed identifier for the phase verifier; implementation never closes it early"
      pattern: "phase_bead_id"
  prohibitions:
    - statement: "Do not invoke competitor children or benchmark-compare from ordinary CI, aggregate quality, release evidence, or release-readiness tasks."
      status: unverified
      verification: judgment
    - statement: "Do not write an implicit repository benchmark result, persist arbitrary environment data, or overwrite an output path the caller did not explicitly request."
      status: unverified
      verification: judgment
    - statement: "Do not publish competitor timing as a durable performance guarantee or make a ratio determine pass/fail."
      status: unverified
      verification: judgment
    - statement: "Do not describe this in-process library and benchmark hardening as application-level OWASP ASVS certification."
      status: unverified
      verification: judgment
---

<objective>
Materialize the human-approved exact isolated environments, remove competitor coupling from the project environment, and ship one manual semantic-first comparison command with executable CI-isolation proof.

Purpose: Complete reproducible comparison evidence while ensuring ordinary development, CI, release evidence, and users remain independent of third-party benchmark packages and noisy ratios.
Output: Two generated adjacent locks, a clean project lock/dependency graph, manual Taskfile/wrapper integration, isolation tests, and one validated low-count observation.
</objective>

## Artifacts this phase produces

- `benchmarks/comparison/python_statemachine_2_5.py.lock`: uv-generated adjacent lock; no hand-authored fields.
- `benchmarks/comparison/python_statemachine_3_2.py.lock`: uv-generated adjacent lock; no hand-authored fields.
- `benchmarks/benchmark.py`: compatibility `main()` delegating to `benchmarks/comparison/run_comparison.py` without top-level competitor imports or implicit files.
- `Taskfile.yml`: `benchmark-compare` invokes the parent through `uv run python` and forwards `CLI_ARGS`; it is independent of test, quality, release, and CI task graphs.
- `pyproject.toml`: shared dependency groups omit `python-statemachine` and `transitions` comparator requirements.
- `uv.lock`: generated project resolution omits both comparator distributions from root dependency metadata and transitive closure when no other project requirement needs them.
- `tests/test_competitor_benchmark_contract.py`: `test_project_groups_exclude_competitors`, `test_ordinary_ci_excludes_comparison_execution`, `test_release_tasks_exclude_comparison_execution`, `test_stdout_default_creates_no_repository_file`, and live low-count observation assertions.
- Comparison report fields added by the parent: `schema_version`, `observation_only`, `generated_at_utc`, `command`, `implementations`, `scenario_comparisons`, and per-comparison `scenario_id`, `status`, `unsupported_reason`, `required_values`, `medians_ns`, `operations_per_second`, and `ratios`.
- `26-05-SUMMARY.md`: carries forward the still-claimed `phase_bead_id` and records that closure is reserved for successful phase verification.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-canonical-construction-evidence-contract/26-CONTEXT.md
@.planning/phases/26-canonical-construction-evidence-contract/26-RESEARCH.md
@.planning/phases/26-canonical-construction-evidence-contract/26-PATTERNS.md
@.planning/phases/26-canonical-construction-evidence-contract/26-VALIDATION.md
@.planning/phases/26-canonical-construction-evidence-contract/26-01-SUMMARY.md
@.planning/phases/26-canonical-construction-evidence-contract/26-02-SUMMARY.md
@.planning/phases/26-canonical-construction-evidence-contract/26-03-SUMMARY.md
@.planning/phases/26-canonical-construction-evidence-contract/26-04-SUMMARY.md

<interfaces>
From Plan 26-02:
- Each child emits the strict record accepted by `common.validate_child_record()`.
- `run_comparison.build_child_commands(args)` returns the repo-root-aware absolute Fast FSM child command and exact absolute `uv run --locked --script` commands for both competitor scripts.
- `run_comparison.build_comparison_report(records)` rejects identity/preflight contradictions and preserves unsupported cells without ratios.
- `run_comparison.main()` prints one canonical comparison envelope and persists only when given an explicit output path.

External approval precondition:
- Plan 26-04 SUMMARY must record human approval of `python-statemachine==2.5.0`, `python-statemachine==3.2.1`, and upstream `fgmacedo/python-statemachine` provenance.

Existing integration points:
- `Taskfile.yml` currently owns `benchmark`, `benchmark-compiled`, and `benchmark-compare`; only the comparison task changes.
- `pyproject.toml` currently lists `python-statemachine>=2.5.0` and `transitions>=0.9.3` in the benchmark group, causing ordinary `uv sync --locked --all-groups` CI to install them.
- `benchmarks/benchmark.py` currently imports competitors in-process and writes `benchmark_results.json`; it becomes a thin compatibility delegate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate exact adjacent locks and remove ordinary project dependency coupling</name>
  <reversibility rating="costly">The committed lock split governs reproducible external evidence and ordinary CI dependency provenance; reversing it requires coordinated script, project-lock, and task changes.</reversibility>
  <files>benchmarks/comparison/python_statemachine_2_5.py.lock, benchmarks/comparison/python_statemachine_3_2.py.lock, pyproject.toml, uv.lock, tests/test_competitor_benchmark_contract.py</files>
  <read_first>
    - `.planning/phases/26-canonical-construction-evidence-contract/26-04-SUMMARY.md` — required exact package/provenance approval; stop if it is absent or records rejection/mismatch.
    - `benchmarks/comparison/python_statemachine_2_5.py` and `python_statemachine_3_2.py` — exact PEP 723 metadata that owns each adjacent lock.
    - `pyproject.toml:11-38` — ordinary project dependency groups containing the two legacy comparator requirements.
    - `uv.lock:470-525,1470-1490,1895-1920` — current root metadata and resolved comparator packages; treat the file as generated.
    - `tests/test_competitor_benchmark_contract.py` — strict contract fixtures and static dependency assertions from Plan 26-02.
  </read_first>
  <behavior>
    - Human-approved exact PEP 723 metadata produces one adjacent lock per child through uv's script-lock command.
    - The two locks resolve only their exact direct version lanes and remain outside project dependency groups/project lock roots.
    - Removing the obsolete shared `python-statemachine` and `transitions` requirements then regenerating `uv.lock` creates no unrelated resolution churn.
    - Ordinary `uv sync --locked --all-groups` has no project-root reason to install either comparator.
    - Tests verify parsed TOML/lock structure rather than fragile comments or raw negative-grep counts.
  </behavior>
  <action>Assert the Plan 26-04 approval precondition before any resolution. Extend contract tests to parse `pyproject.toml`, project `uv.lock`, and both adjacent locks structurally. Remove `python-statemachine` and `transitions` from the shared benchmark dependency group because the new children own exact isolated environments and the maintained comparison no longer imports either from the project. Generate each adjacent lock exclusively with `uv lock --script` against its corresponding human-approved script, then regenerate the ordinary project `uv.lock` with `uv lock`. Do not hand-edit any lock. Review diffs: adjacent locks must carry the matching exact direct version, project root metadata must lose both comparator requirements, and unrelated dependency versions must not churn without a documented resolver necessity. Keep matplotlib/networkx only if maintained project benchmarks still import them. Ensure static tests make ordinary CI dependency isolation executable without importing or installing a competitor.</action>
  <verify>
    <automated>uv lock --check
uv run pytest tests/test_competitor_benchmark_contract.py -x -q -k "project_groups or lock or dependency"
uv run ruff format tests/test_competitor_benchmark_contract.py
uv run ruff check --fix tests/test_competitor_benchmark_contract.py
uv run ruff check tests/test_competitor_benchmark_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - Plan 26-04 human approval exists before either adjacent lock is generated.
    - Both adjacent locks are uv-generated, exact-version-specific, and structurally tied to their matching script metadata.
    - Project dependency groups and project uv.lock root/transitive graph omit both comparator distributions unless a separately identified non-comparison project dependency proves otherwise.
    - Project lock regeneration contains no unexplained unrelated version churn.
    - Ordinary contract tests prove isolation using parsed files and fixtures only.
  </acceptance_criteria>
  <done>Exact competitors are reproducibly pinned beside their isolated scripts while the ordinary Fast FSM project environment no longer contains a comparator dependency lane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the manual observation command and prove ordinary CI isolation</name>
  <files>benchmarks/benchmark.py, Taskfile.yml, tests/test_competitor_benchmark_contract.py</files>
  <read_first>
    - `benchmarks/comparison/run_comparison.py` and `common.py` — validated parent CLI/report and bounds produced by Plan 26-02.
    - `benchmarks/benchmark.py:1-620` — legacy in-process imports and implicit root result write to replace with a compatibility delegate.
    - `Taskfile.yml:200-225` — existing benchmark tasks; preserve Fast FSM-only benchmark and compiled benchmark behavior.
    - `.github/workflows/ci.yml` — ordinary workflow task/command graph that must remain free of comparison child or benchmark-compare invocation.
    - `tools/release_evidence.py` and Taskfile release/readiness task definitions — mandatory evidence graph that must remain competitor-independent.
  </read_first>
  <behavior>
    - `task benchmark-compare` invokes the new parent manually, forwards bounded CLI controls, and creates no repository file by default.
    - An explicit output path writes the same canonical report the command prints; no implicit `benchmark_results.json` is created.
    - A low-count exact run reports distinct 2.5.0 and 3.2.1 resolved versions/origins and passes required flat semantic preflight before timing.
    - The low-count run passes both required scenarios, including exactly one false guard evaluation, unchanged state identity, and zero transition callbacks in every lane.
    - Identity/preflight contradiction is a command failure; optional unsupported cells remain present without ratios.
    - Ordinary CI, test/quality tasks, release evidence, and release readiness contain no path to comparator child execution or timing and no ratio threshold.
  </behavior>
  <action>Replace `benchmarks/benchmark.py` with a thin compatibility delegate to `benchmarks/comparison/run_comparison.py` so direct legacy invocation no longer imports all implementations in-process or writes a root artifact. Point `Taskfile.yml`'s existing `benchmark-compare` task at the new parent through the repository project, describe it as manual environment-labelled observational evidence, and forward `CLI_ARGS`; do not connect it to aggregate test, quality, release, or CI tasks. Extend contract tests to parse ordinary CI YAML/Taskfile/release commands and prove no comparison invocation edge, to run the parent against fixture children for stdout/output-path behavior, and to assert no ratio threshold or implicit result filename. Then run a real bounded smoke observation using both locked children with warmup 10, operations 100, and one sample from a neutral working directory. Verify exact requested/resolved versions, distinct origins, both required preflights, the false-guard invariant of one evaluation/unchanged state/zero callbacks in every lane, `observation_only=true`, and ratio omission for optional unsupported cells. Keep raw stderr/environment data out of the report. Read `phase_bead_id` from `26-01-SUMMARY.md`, verify with `bd show &lt;id&gt; --json` that the same item remains claimed/in progress, and carry the identifier plus this status into `26-05-SUMMARY.md`; do not close it during plan execution. The phase verifier closes it with the Validation Strategy command only after every phase gate succeeds, and leaves it claimed across any gap-closure cycle.</action>
  <verify>
    <automated>uv run pytest tests/test_competitor_benchmark_contract.py -x -q
task benchmark-compare -- --warmup 10 --operations 100 --samples 1
uv run ruff format benchmarks/benchmark.py tests/test_competitor_benchmark_contract.py
uv run ruff check --fix benchmarks/benchmark.py tests/test_competitor_benchmark_contract.py
uv run ruff check benchmarks/benchmark.py tests/test_competitor_benchmark_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - Taskfile and compatibility runner have one maintained comparison path through the strict parent and no implicit output file.
    - The exact low-count run proves both requested/resolved version pairs, distinct module origins, semantic preflight-before-timing, bounded observation labels, and honest unsupported cells.
    - Both required scenarios are semantically identical across all three lanes; the false-guard cell records one guard call, unchanged state identity, and zero transition callbacks and cannot be marked unsupported.
    - Static tests prove ordinary CI, quality, release-evidence, and readiness task graphs cannot invoke comparison children or benchmark-compare.
    - No test imports `statemachine`, executes competitor timing, or asserts a competitor rate/ratio threshold during ordinary CI.
    - The command/report writes only stdout unless an output path is explicit and emits only the allowlisted schema.
    - `26-05-SUMMARY.md` records the same still-claimed `phase_bead_id`; only successful phase verification may close it, and failed verification leaves it open for repair work.
  </acceptance_criteria>
  <done>Maintainers can deliberately run a reproducible exact-version comparison, while ordinary CI and release evidence remain faster, dependency-isolated, and insensitive to competitor timing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Approved PEP 723 metadata → generated lock/artifact environment | Exact external provenance becomes executable resolved code. |
| Manual CLI/subprocesses → repository/stdout evidence | Bounded external work and child-controlled bytes may affect local resources or files. |
| Project dependency/task graph → ordinary CI | Configuration determines whether third-party comparison code enters required gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-05 | Spoofing / Supply Chain | adjacent script locks and runtime child identity | high | mitigate | Require prior human provenance approval, generate with uv only, review exact pins, and validate resolved distribution version plus module origin at runtime. |
| T-26-04 | Tampering / Information Disclosure | comparison output and child process | medium | mitigate | Default to canonical stdout only, require explicit output path, use neutral child working directories, bounded allowlisted records, and no raw environment/exception payloads. |
| T-26-06 | Denial of Service | manual timing / ordinary CI | high | mitigate | Bound counts, timeouts, and output; statically prove comparator commands are absent from ordinary CI/quality/release graphs and keep ratios non-gating. |
</threat_model>

<verification>
- `uv run pytest tests/test_graph_invariants.py tests/test_builder.py tests/test_competitor_benchmark_contract.py -x -q`
- `uv run pytest tests/test_performance_benchmarks.py -x -q -k "singleton or direct or topology"`
- `task benchmark-compare -- --warmup 10 --operations 100 --samples 1`
- `uv run pytest tests/ -x -q`
- `task typecheck-mypy`
- `task typecheck-ty` (record advisory output independently)
- `uv run ruff check src/fast_fsm/core.py benchmarks/comparison benchmarks/benchmark.py tests/test_graph_invariants.py tests/test_builder.py tests/test_competitor_benchmark_contract.py`
- `uv run python tools/release_evidence.py slots-policy --json`
- `task pure-source-check`
</verification>

<success_criteria>
- PERF-05 exact 2.5.0/3.2.1 locks and runtime identity/preflight proof are reproducible and contradiction-intolerant.
- PERF-06 comparison output is labelled observation data, stdout-only by default, and never a normal CI/release gate.
- Ordinary project dependency groups and lock provenance are competitor-free.
- Full construction, benchmark-contract, type, Ruff, slots, pure-source, performance-path, and sequential test gates pass.
- The single phase Beads item remains claimed at execution completion and is closed only by the successful phase-verification procedure.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | One atomic topology-construction boundary and reproducible comparison evidence | 26-01 through 26-05 | COVERED | Construction and evidence are separate dependency branches joined by the phase gate. |
| REQ | BUILD-04 | One private normalization/validation seam | 26-01, 26-03 | COVERED | Direct/batch tracer expands through every retained transition-producing adapter and clone topology reconstruction. |
| REQ | BUILD-05 | Failure leaves machine and reusable builder state unchanged | 26-01, 26-03 | COVERED | Identity-sensitive late-failure, interruption, concurrency, repair, and cache matrices. |
| REQ | PERF-05 | Exact isolated 2.5.0 and 3.2.1 semantic comparisons | 26-02, 26-04, 26-05 | COVERED | Strict offline contract, human provenance gate, generated locks, and bounded live smoke. |
| REQ | PERF-06 | Manual/scheduled observations rather than mandatory CI | 26-02, 26-05 | COVERED | Fixture-only CI contract plus manual Taskfile integration. |
| RESEARCH | — | Frozen raw request carrier feeding `_PreparedTransition` and one publisher | 26-01, 26-03 | COVERED | Adapter parsing remains outside the semantic transaction. |
| RESEARCH | — | Exact PEP 723 scripts with adjacent uv locks | 26-02, 26-04, 26-05 | COVERED | No project-level comparator dependency remains. |
| RESEARCH | — | Strict identity and semantic preflight before timing | 26-02, 26-05 | COVERED | Contradictions fail; explicit optional unsupported cells have no ratios. |
| CONTEXT | discretion | Clone topology traverses the canonical construction transaction | 26-03 | COVERED | Clone preserves State/Condition identity, rebuilds independent entry/slot containers, and has failure/retry parity tests; only Phase 30 metadata parity is deferred. |
| PROJECT RULE | — | One Phase 26 Beads item spans execution through verification | 26-01, 26-05 | COVERED | Preflight creates/claims once, summaries carry the real ID, and only successful phase verification closes it. |
| RESEARCH | — | Stdout-only manual runner and ordinary-CI isolation | 26-02, 26-05 | COVERED | Legacy wrapper delegates; static task/workflow tests prohibit accidental gating. |
| CONTEXT | discretion | Keep `core.py` as the single mypyc unit | 26-01, 26-03 | COVERED | No new runtime module; slots/type gates included. |
| CONTEXT | discretion | Preserve atomic publication and untouched direct singleton dispatch | 26-01, 26-03 | COVERED | Off-table merge plus structural/runtime path checks. |
| CONTEXT | discretion | Competitors stay outside ordinary CI with exact identity/preflight evidence | 26-02, 26-04, 26-05 | COVERED | Trust checkpoint and isolated locks/manual runner. |
| FALLBACK EDGE | — | Equal/touching request behavior | 26-01 | COVERED | Exact duplicate no-op, equal-priority conflict, same-slot local merge, distinct-slot separation. |
| FALLBACK EDGE | — | Empty/single/null input behavior | 26-01, 26-03 | COVERED | Empty no-op/valid factory, singleton canonical path, null/malformed rejection. |
| FALLBACK EDGE | — | Equal-element ordering stability and repeated-use idempotence | 26-01 | COVERED | Identity/order/version assertions. |
| FALLBACK EDGE | — | Interruption and concurrent-use guarantees | 26-01, 26-03 | COVERED | Machine ownership serialized; builder explicitly single-owner and repairable. |
| FALLBACK EDGE | — | Version/origin/preflight contradictions | 26-02, 26-05 | COVERED | Hard evidence failures before timing. |
| FALLBACK EDGE | — | Neutral-cwd Fast FSM child resolution | 26-02, 26-05 | COVERED | Absolute repo project/runner command is fixture-tested and Fast-only smoke-tested before external lanes. |
| FALLBACK EDGE | — | Observation versus CI gating | 26-02, 26-05 | COVERED | No thresholds/dependency/execution edge in ordinary CI. |
| DEFERRED | — | Final states, internal transitions, expected rejection, public construction deprecations | none | EXCLUDED | Explicitly assigned to Phases 27-30. |

<output>
Create `.planning/phases/26-canonical-construction-evidence-contract/26-05-SUMMARY.md` when done.
</output>
