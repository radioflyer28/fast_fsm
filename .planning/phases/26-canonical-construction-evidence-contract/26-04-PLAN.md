---
phase: 26-canonical-construction-evidence-contract
plan: 04
type: execute
wave: 3
depends_on:
  - 26-02
files_modified: []
autonomous: false
requirements:
  - PERF-05
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "PERF-05: A human verifies the exact `python-statemachine` distribution name, 2.5.0 and 3.2.1 release pages, and `fgmacedo/python-statemachine` upstream provenance before adjacent locks are generated or committed."
    - "Approval covers only the two exact PEP 723 comparison dependencies already declared by Plan 26-02 and does not authorize a broader project dependency or another comparator."
  artifacts: []
  key_links:
    - from: "official PyPI release metadata"
      to: "Plan 26-05 adjacent script locks"
      via: "blocking-human provenance approval"
      pattern: "python-statemachine==2.5.0|python-statemachine==3.2.1"
  prohibitions:
    - statement: "Do not approve a package from a similarly named distribution, an unverified mirror, or the requested version string alone."
      status: unverified
      verification: judgment
    - statement: "Do not generate or commit adjacent script locks before the human provenance decision is recorded."
      status: unverified
      verification: judgment
---

<objective>
Establish human trust in the exact external distributions before generating reproducibility locks for the isolated comparison lanes.

Purpose: Close the research package-legitimacy warning without silently auto-approving a supply-chain boundary whose registry telemetry was unavailable.
Output: A blocking approval or rejection signal for Plan 26-05; no repository file changes.
</objective>

## Artifacts this phase produces

- No new repository symbol, file, or field. This plan produces the explicit blocking-human provenance decision consumed by Plan 26-05.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-canonical-construction-evidence-contract/26-CONTEXT.md
@.planning/phases/26-canonical-construction-evidence-contract/26-RESEARCH.md
@.planning/phases/26-canonical-construction-evidence-contract/26-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify exact python-statemachine release provenance</name>
  <read_first>
    - `.planning/phases/26-canonical-construction-evidence-contract/26-RESEARCH.md` §Package Legitimacy Audit — SUS classification caused only by unavailable registry telemetry and requires a human checkpoint.
    - `benchmarks/comparison/python_statemachine_2_5.py` — exact dependency metadata produced by Plan 26-02.
    - `benchmarks/comparison/python_statemachine_3_2.py` — exact dependency metadata produced by Plan 26-02.
    - `https://pypi.org/project/python-statemachine/2.5.0/` — official historical release page.
    - `https://pypi.org/project/python-statemachine/3.2.1/` — official current release page.
    - `https://github.com/fgmacedo/python-statemachine` — upstream source repository named by official package provenance.
  </read_first>
  <action>Present the exact package declarations and official provenance links to the user. Ask them to verify that both PyPI pages name the `python-statemachine` distribution at versions 2.5.0 and 3.2.1 and connect to the `fgmacedo/python-statemachine` upstream repository. Make clear that approval authorizes only generating and committing adjacent uv locks for these two isolated manual benchmark scripts. Stop on rejection or any identity mismatch; do not generate a lock in this task.</action>
  <what-built>Plan 26-02 created offline-testable comparison children declaring only `python-statemachine==2.5.0` and `python-statemachine==3.2.1`; no lock or installation has occurred.</what-built>
  <how-to-verify>
    1. Open both official PyPI version pages and confirm the exact distribution/version pairs.
    2. Confirm the official project provenance points to `fgmacedo/python-statemachine`.
    3. Compare those values with the two PEP 723 declarations in the child scripts.
    4. Approve only if all names, versions, and provenance agree exactly.
  </how-to-verify>
  <verify>
    <human-check>
      1. Open both official PyPI version pages and confirm the exact distribution/version pairs.
      2. Confirm the official project provenance points to `fgmacedo/python-statemachine`.
      3. Compare those values with the two PEP 723 declarations in the child scripts.
      4. Approve only if all names, versions, and provenance agree exactly.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Human approval explicitly names both exact versions and the upstream repository.
    - No adjacent lockfile exists or changes during this checkpoint.
    - A mismatch or rejection blocks Plan 26-05 rather than substituting a different dependency.
  </acceptance_criteria>
  <resume-signal>Reply `approved` to authorize both exact adjacent lockfiles, or describe the mismatch/rejection.</resume-signal>
  <done>The exact external package identities are human-approved for isolated comparison lock generation and no broader dependency authority is inferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External registry/upstream metadata → checked-in dependency lock | Third-party identity and release provenance must be trusted before code is resolved and pinned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-SC | Spoofing / Supply Chain | `python-statemachine` exact releases | high | mitigate | Require a non-bypassable human comparison of official PyPI version pages, declared exact pins, and the official upstream repository before lock generation. |
</threat_model>

<verification>
- Human confirms `python-statemachine==2.5.0`, `python-statemachine==3.2.1`, and `fgmacedo/python-statemachine` from official sources.
- Repository status confirms neither adjacent script lock was created or modified by this checkpoint.
</verification>

<success_criteria>
- PERF-05's external dependency provenance is explicitly approved before resolution.
- The approval scope is limited to the two isolated comparison scripts.
</success_criteria>

<output>
Create `.planning/phases/26-canonical-construction-evidence-contract/26-04-SUMMARY.md` recording the exact approval or rejection signal.
</output>
