---
phase: 30-builder-first-construction-persistence-parity
plan: 05
type: execute
wave: 5
depends_on:
  - 30-04
files_modified:
  - README.md
  - docs/QUICK_START.md
  - docs/TUTORIAL.md
  - docs/api/core.md
  - docs/api/visualization.md
  - docs/api/validation.md
  - docs/dev/architecture.md
  - examples/cross_fsm_demo.py
  - tests/test_readme_examples.py
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-06
  - BUILD-07
  - BUILD-08
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-03: focused guidance presents FSMBuilder as the ordinary programmatic path, direct StateMachine/AsyncStateMachine as advanced control, and from_dict only as serialized-topology reconstruction."
    - "D-09 through D-12: focused migration guidance names all four deprecated helpers, their builder/from_dict replacements, v0.5.x support, and earliest v0.6.0 removal without pulling forward Phase 32's full documentation reorganization."
    - "D-13 through D-17: public docs state true-only internal dictionaries, directional compatibility, clone identity/isolation, and exact state-only snapshot v1 truthfully."
    - "D-06/D-18: architecture guidance describes declarative and explicit requests entering one canonical transaction, with no parallel topology registrar."
    - "Every named active teaching surface uses FSMBuilder for ordinary programmatic construction and executable documentation checks remain green."
  artifacts:
    - path: "README.md"
      provides: "Focused builder-first construction hierarchy and compatibility migration boundary"
    - path: "docs/QUICK_START.md"
      provides: "Builder-first first machine and removal of deprecated helper recommendations"
    - path: "docs/TUTORIAL.md"
      provides: "Builder-authored progressive teaching examples without active deprecated-helper recipes"
    - path: "examples/cross_fsm_demo.py"
      provides: "Runnable builder-first coordinated-machine example"
    - path: "docs/api/core.md"
      provides: "Canonical construction roles and deprecation notices beside API references"
    - path: "docs/api/visualization.md"
      provides: "Builder-authored visualization teaching example"
    - path: "docs/api/validation.md"
      provides: "Builder-authored validation teaching example"
    - path: "docs/dev/architecture.md"
      provides: "One cold-path adapter transaction and dispatch-exclusion architecture"
    - path: "tests/test_readme_examples.py"
      provides: "Executable builder-first snippets and active teaching-surface inventory regression"
  key_links:
    - from: "README/Quick Start/Tutorial/API/example ordinary recipes"
      to: "FSMBuilder"
      via: "executable caller-owned State construction and build() example"
      pattern: "FSMBuilder"
    - from: "deprecated API docs"
      to: "migration replacement"
      via: "fixed compatibility window and programmatic-versus-serialized guidance"
      pattern: "v0.5.x"
  flagged_assumptions:
    - id: "BUILD-01"
      status: "flagged-resolved"
      resolution: "The spec-less probe was unresolved; D-01 through D-03 and D-12 lock the three-role hierarchy and the focused Phase 30 documentation boundary."
      acceptance: "Prominent README/Quick Start/API guidance leads with FSMBuilder, labels direct machines advanced, limits from_dict to serialized topology, and moves helpers to compatibility guidance."
  prohibitions:
    - statement: "Must not create a second topology engine or parallel declarative registrar."
      status: "flagged-unverified"
    - statement: "Must not move construction, deprecation, or persistence policy into dispatch."
      status: "flagged-unverified"
    - statement: "Must not remove simple_fsm, quick_fsm, StateMachine.quick_build, or StateMachine.from_states during v0.5.x."
      status: "flagged-unverified"
    - statement: "Must not claim that pre-v0.5 readers preserve new final/internal semantics."
      status: "flagged-unverified"
---

<objective>
Publish the focused builder-first teaching path and synchronize the public construction/persistence guidance.

Purpose: Make one ordinary construction path discoverable and retain accurate advanced and compatibility roles before normative synchronization and final dual-origin closure.
Output: Focused README/Sphinx/architecture guidance, migrated runnable examples, and executable documentation regressions.
</objective>

## Artifacts this phase produces

- Builder-first focused README, Quick Start, Tutorial, API, and runnable cross-FSM teaching paths.
- A focused regression that prevents active teaching surfaces from recommending deprecated helpers again.
- API/architecture guidance for advanced direct construction, serialized reconstruction, deprecated helpers, declarative import, persistence, clone, and snapshot.
- Executable documentation regressions for the new hierarchy and migration wording.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-PATTERNS.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-04-SUMMARY.md

<interfaces>
Plans 30-01..04 provide the runtime and stub behavior this documentation describes:
- `FSMBuilder` imports topology-complete declarations at build time through one canonical request transaction.
- Four compatibility constructors warn once but remain supported, exported, and typed through v0.5.x.
- `to_dict` emits true-only internal mode and `from_dict` accepts exact absent/false/true semantics atomically.
- Clone preserves semantic/collaborator identity while rebuilding independent containers; snapshot v1 stays state-only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make prominent guides and the cross-FSM example builder-first</name>
  <reversibility rating="costly">D-01 establishes the primary construction story; changing it later would churn public examples and downstream user code.</reversibility>
  <files>README.md, docs/QUICK_START.md, docs/TUTORIAL.md, examples/cross_fsm_demo.py, tests/test_readme_examples.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-01 through D-03 and D-09 through D-12
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md` Builder-First example, State of the Art, and Pattern 3 warning wording
    - `README.md:1-170` current direct-first example, construction table, helper recipes, and builder recipe
    - `docs/QUICK_START.md:1-110,210-345,380-510,675-705` prominent deprecated recipes, persistence, builder, and summary claims
    - `docs/TUTORIAL.md:1-100,330-365` active basic and bulk-construction teaching examples that currently use deprecated helpers
    - `examples/cross_fsm_demo.py` runnable coordinated-machine example currently teaching simple_fsm
    - `tests/test_readme_examples.py` executable README extraction and documentation-surface contract tests
    - `tests/test_examples_smoke.py` parameterized example runner and cross_fsm_demo case identifier
  </read_first>
  <action>
Per D-01/D-02/D-03, inventory every active deprecated-helper teaching occurrence in README, Quick Start, Tutorial, the two API teaching pages owned by Task 2, and `examples/cross_fsm_demo.py`. In this task, replace the prominent README/Quick Start/Tutorial recipes and the runnable cross-FSM example with explicit caller-owned `State` objects assembled fluently through `FSMBuilder`; keep each example compact and executable. Recast the construction-choice guidance as one primary/advanced/adapter hierarchy: builder for ordinary programmatic construction, direct sync/async machines for explicitly labelled advanced identity or incremental-topology control, and `from_dict` only for serialized topology. Move `simple_fsm`, `quick_fsm`, `StateMachine.quick_build`, and `StateMachine.from_states` out of recommended starts into a focused compatibility/migration note with exact v0.5.x support, no-earlier-than-v0.6.0 removal, and builder/from_dict replacement roles. Do not rewrite unrelated tutorial prose, the drone tutorial, or the release walkthrough; those remain Phase 32 work per D-12.

Update executable README tests for the builder-first snippets and add a parameterized `test_active_construction_guidance_uses_builder` with stable ids `readme`, `quick_start`, `tutorial`, and `cross_fsm_demo`. For Markdown, extract only fenced Python/testcode regions; for the runnable script, inspect its import/call syntax. Assert those executable regions neither import nor call any of the four deprecated helpers and do contain the intended FSMBuilder recipe, while allowing explicit compatibility prose outside code. Task 2 extends the parameterization with its two API pages after migrating them, so no committed task leaves a known failing inventory case. Keep `tests/test_examples_smoke.py` unchanged and use its parameterized `cross_fsm_demo.py` case to execute the migrated script. Do not make brittle whole-file prose snapshots; validate runnable examples and the exact public role/window facts.
  </action>
  <acceptance_criteria>
    - "BUILD-01/D-01: ordinary programmatic examples in README, Quick Start, Tutorial, and cross_fsm_demo use FSMBuilder with caller-owned states."
    - "D-02/D-03: direct construction is explicitly advanced and from_dict explicitly reconstructs serialized topology rather than competing as a builder."
    - "BUILD-03/D-09 through D-12: all four helpers are documented as retained warned compatibility surfaces with exact replacement/window guidance."
    - "The Task 1 inventory cases find no deprecated helper taught as runnable construction in README, Quick Start, Tutorial, or cross_fsm_demo; any retained mention is explicit compatibility/migration guidance."
    - "Direct machine construction remains only where its section is explicitly advanced; the changes remain focused and do not pull forward Phase 32's full reorganization or drone/release walkthrough."
    - "README tests, the cross-FSM example smoke test, and strict docs/doctest checks pass."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_readme_examples.py -x -q -k "active_construction_guidance_uses_builder and (readme or quick_start or tutorial or cross_fsm_demo)" &amp;&amp; uv run pytest tests/test_examples_smoke.py -x -q -k "cross_fsm_demo" &amp;&amp; task docs-check &amp;&amp; task docs-test</automated>
  </verify>
  <done>New users encounter one builder-first programmatic path and accurate advanced, persistence, and compatibility roles in the prominent guides.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate API teaching examples and synchronize architecture guidance</name>
  <reversibility rating="costly">These files are public and maintainer-facing contract authorities; drift would misstate compatibility and adapter ownership.</reversibility>
  <files>docs/api/core.md, docs/api/visualization.md, docs/api/validation.md, docs/dev/architecture.md, tests/test_readme_examples.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-01 through D-20 and Deferred Ideas
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md` Patterns 1 through 5, Pitfalls 1/4/6/7, and Deprecated/outdated surfaces
    - `docs/api/core.md` builder, machine, decorator, and helper reference sections
    - `docs/api/visualization.md:68-100` active diagram example using a deprecated quick constructor
    - `docs/api/validation.md:1-55` active validator example using a deprecated quick constructor
    - `tests/test_readme_examples.py` Task 1 construction-guidance parameterization to extend with api_visualization and api_validation
    - `docs/dev/architecture.md` canonical topology, builder, declarative, serialization, clone, snapshot, and helper sections
  </read_first>
  <action>
Convert the active validation and visualization teaching examples from `StateMachine.quick_build` to caller-owned states plus `FSMBuilder`; retain direct machine construction in API material only when the surrounding text labels it advanced and its incremental-control role matters. Extend Task 1's parameterized inventory with stable `api_visualization` and `api_validation` cases after migrating those pages, so the full named active-surface inventory is green at this task boundary. Add a concise construction hierarchy before core API listings and explicit deprecation notices adjacent to retained helper autodoc entries. In architecture guidance, document declaration-to-request derivation on the builder cold path, guard ownership/exactly-once behavior, one complete canonical publication, private non-warning compatibility workers, true-only internal dictionary output, exact atomic input, clone identity/container ownership, and state-only snapshot v1. State directional compatibility precisely: old payloads and new readers are supported, while pre-v0.5 readers may ignore additive final/internal metadata and lose meaning. Do not add a schema version or claim symmetric safety.

Do not update the normative SPR in this task; Plan 30-06 Task 1 synchronizes it after both public and maintainer-facing documentation surfaces agree. Run warning-as-error docs and doctests.
  </action>
  <acceptance_criteria>
    - "All public/maintainer contract files agree on builder primary, direct advanced, from_dict persistence, and the four warned retained helpers."
    - "Validation and visualization API teaching examples use FSMBuilder and the completed executable-region inventory finds no deprecated helper import or call across any named active teaching surface."
    - "D-06/D-18: docs name one declarative/explicit request transaction and no parallel registrar."
    - "D-13 through D-17: true-only output, exact input, directional compatibility, clone ownership, and unchanged snapshot v1 are stated without overclaim."
    - "D-19/D-20: architecture guidance documents all-adapter evidence and cold-path-only construction policy, while Phase 31/32 outputs remain unclaimed."
    - "Strict Sphinx HTML and doctest builds pass."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_readme_examples.py -x -q -k "active_construction_guidance_uses_builder and (api_visualization or api_validation)" &amp;&amp; task docs-check &amp;&amp; task docs-test</automated>
  </verify>
  <done>API teaching examples and contributor architecture describe one accurate builder-first construction and persistence contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public examples → user construction code | Teaching surfaces can cause users to adopt deprecated or semantically weaker construction paths. |
| Runtime/stub behavior → public documentation authority | Contract drift can misstate warning, persistence, clone, or snapshot guarantees. |
| Construction guidance → dispatch architecture | Documentation must not normalize moving cold-path policy into the hot trigger path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-03 | Repudiation | deprecated warning boundaries | medium | mitigate | Document one warning per public boundary and retain exact executable migration checks. |
| T-30-04 | Information Disclosure | warnings/errors/contracts | medium | mitigate | Keep messages bounded and describe indexed structural context without payload repr. |
| T-30-05 | Tampering | from_dict publication | high | mitigate | Document complete validation before one canonical publication. |
| T-30-06 | Tampering | clone ownership | medium | mitigate | State exact collaborator identity and independent mutable-container ownership. |
| T-30-07 | Tampering | adapter-specific semantics | high | mitigate | Align every teaching and contract authority on the same final/internal semantics. |
| T-30-08 | Denial of Service | dispatch path | high | mitigate | State that construction, warning, and persistence work remains off dispatch. |
| T-30-SC | Tampering | package supply chain | low | accept | This plan adds no package install or dependency; uv.lock and the existing toolchain remain unchanged. |
</threat_model>

<verification>
- `uv run pytest tests/test_readme_examples.py -x -q -k "active_construction_guidance_uses_builder"`
- `uv run pytest tests/test_examples_smoke.py -x -q -k "cross_fsm_demo"`
- `task docs-check && task docs-test`
</verification>

<success_criteria>
- BUILD-01..03 and BUILD-06..08 have synchronized focused public and architecture documentation.
- D-01..D-20 are represented without claiming deferred Phase 31/32 deliverables.
- Every named active teaching surface uses FSMBuilder for ordinary programmatic construction.
- The architecture guidance preserves one canonical cold-path transaction and exact persistence/clone/snapshot semantics for Plan 30-06's normative synchronization.
</success_criteria>

<output>
Create `.planning/phases/30-builder-first-construction-persistence-parity/30-05-SUMMARY.md` when done.
</output>
