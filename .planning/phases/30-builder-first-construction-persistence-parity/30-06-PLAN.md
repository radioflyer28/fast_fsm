---
phase: 30-builder-first-construction-persistence-parity
plan: 06
type: execute
wave: 6
depends_on:
  - 30-05
files_modified:
  - .specify/memory/spr-core-api.md
  - tests/test_mypyc_guard.py
  - tests/test_construction_parity.py
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-06
  - BUILD-07
  - BUILD-08
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01 through D-20: the living SPR, runtime, stub, structural tests, pure source, and freshly compiled execution agree on the complete construction/persistence contract."
    - "D-18/D-19/D-20: the same retained-adapter oracle compares observable final/internal semantics and atomic failure across sync/async and pure/native execution."
    - "T-30-08: direct singleton dispatch retains no construction, warning, persistence, reflection, or unrelated-topology work and remains above the compiled 200,000 operations/second floor."
    - "Exact generated native shadows are recoverably relocated on success or failure, and the repository ends on asserted pure src/fast_fsm/core.py origin."
    - "Ruff, mypy, visible ty, slots, strict docs/doctests, and the full sequential suite close Phase 30 without claiming Phase 31/32 outputs."
  artifacts:
    - path: ".specify/memory/spr-core-api.md"
      provides: "Normative Phase 30 construction, persistence, clone, snapshot, compatibility, and runtime-mode contract"
    - path: "tests/test_construction_parity.py"
      provides: "Complete retained-adapter observable-semantics and atomic-failure oracle"
    - path: "tests/test_mypyc_guard.py"
      provides: "Pure/native structural parity, exact authority, and dispatch hot-path absence proof"
  key_links:
    - from: "focused public and architecture documentation"
      to: ".specify/memory/spr-core-api.md"
      via: "normative construction, persistence, clone, and snapshot synchronization before evidence closure"
      pattern: "internal"
    - from: "complete construction parity oracle"
      to: "pure source and fresh native origins"
      via: "identical focused tests under asserted module origins"
      pattern: "FAST_FSM_BUILD_MODE"
    - from: "construction/persistence adapters"
      to: "direct singleton dispatch"
      via: "structural source-region exclusions plus existing compiled throughput test"
      pattern: "test_trigger_min_throughput"
    - from: "fresh native extension"
      to: "restored pure source"
      via: "fail-closed absolute-path inventory and recoverable temporary backup"
      pattern: "verify-source"
  flagged_assumptions: []
  prohibitions:
    - statement: "Must not create a second topology engine or parallel declarative registrar."
      status: "flagged-unverified"
    - statement: "Must not move construction, deprecation, or persistence policy into dispatch."
      status: "flagged-unverified"
    - statement: "Must not remove simple_fsm, quick_fsm, StateMachine.quick_build, or StateMachine.from_states during v0.5.x."
      status: "flagged-unverified"
    - statement: "Must not claim that pre-v0.5 readers preserve new final/internal semantics."
      status: "flagged-unverified"
---

<objective>
Synchronize the normative Phase 30 contract, then close the phase under structural, pure/native, source-restoration, performance, documentation, typing, slots, and full-regression evidence.

Purpose: Prove the complete builder-first and persistence work behaves identically across runtime modes without compromising Fast FSM's speed or source-integrity boundaries.
Output: Final SPR authority, immediate focused structural/parity feedback, dual-origin semantic evidence, restored pure source, and complete phase closure.
</objective>

## Artifacts this phase produces

- A normative SPR synchronized to the implemented and documented Phase 30 contract.
- One structural and semantic parity oracle across retained adapters, sync/async machines, and pure/native runtimes.
- Exact dispatch-exclusion and runtime/stub/carrier authority tests.
- Final source-restoration, slots, typing, documentation, full-suite, and compiled singleton-performance evidence.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-PATTERNS.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-05-SUMMARY.md
@.planning/phases/29-expected-domain-rejection/29-04-PLAN.md

<interfaces>
Plans 30-01..05 provide the complete runtime, typing, persistence, clone/snapshot, compatibility-warning, public-example, API, and architecture behavior.
- `tests/test_construction_parity.py` exercises every retained adapter and is the single observable-semantics oracle for both asserted pure and fresh native origins.
- `tests/test_mypyc_guard.py` owns exact carrier/stub/export/source-region and runtime-origin structural authority.
- `tools/release_evidence.py verify-source --json` is the fail-closed source/shadow inventory authority.
- Plan 30-05 has already made focused public and architecture guidance builder-first; this plan first synchronizes the normative SPR, then proves that contract without widening documentation scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize the normative SPR with the proven Phase 30 contract</name>
  <reversibility rating="costly">The SPR is a living normative authority used by maintainers and later planning; semantic drift would misdirect future changes.</reversibility>
  <files>.specify/memory/spr-core-api.md</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-01 through D-20 and Deferred Ideas
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md` Architectural Responsibility Map, compatibility findings, and resolved questions
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md` behavioral matrices and T-30-01 through T-30-08 controls
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-05-SUMMARY.md` actual focused public/example/architecture migration and final documentation wording
    - `.specify/memory/spr-core-api.md` existing Phase 26-29 normative construction/runtime/persistence bullets and obsolete Phase 30 deferrals
    - `docs/dev/architecture.md` Plan 30-05 canonical construction, persistence, clone, snapshot, and dispatch-exclusion guidance
  </read_first>
  <action>
Update the SPR as the concise normative authority for D-01 through D-20. Record the builder-primary/direct-advanced/from_dict-adapter hierarchy; retained direct construction; immutable declarative metadata; build-time request derivation; state-owned guard execution; one canonical publication; retained helper signatures, single-warning private-worker boundary, and exact v0.5.x/no-earlier-than-v0.6.0 window; true-only dictionary output and exact atomic input; directional compatibility; clone identity and independent containers; exact state-only snapshot v1; and all-adapter pure/native parity with construction policy excluded from dispatch. Remove Phase 30 deferral language made obsolete by the planned implementation. Do not claim Phase 31 diagnostics/visualization work, Phase 32 installed-wheel/progressive tutorial completion, a schema version, executable serialization, topology-bearing snapshots, symmetric reader compatibility, or a second topology registrar. Keep wording aligned with the focused public docs and preserve the four deprecated helpers throughout v0.5.x. Complete this normative synchronization before modifying the structural oracle in Task 2.
  </action>
  <acceptance_criteria>
    - "The SPR captures D-01 through D-20 without contradicting README, API, or architecture guidance."
    - "BUILD-01/BUILD-03: the construction hierarchy and exact four-helper compatibility window are normative and unambiguous."
    - "BUILD-02/BUILD-06: declarative topology uses builder cold-path request derivation, state-owned guards, and one canonical publication."
    - "BUILD-07/BUILD-08: dictionary, clone, directional compatibility, and state-only snapshot-v1 semantics are exact."
    - "The SPR leaves Phase 31/32 deliverables unclaimed and contains no new schema/version/topology engine commitment."
    - "Strict Sphinx and doctest checks remain green after contract synchronization."
  </acceptance_criteria>
  <verify>
    <automated>task docs-check &amp;&amp; task docs-test</automated>
  </verify>
  <done>The living contract is synchronized with the focused public documentation and the complete locked Phase 30 semantics before structural closure begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the final structural and parity oracle</name>
  <reversibility rating="costly">The dual-origin oracle is a cross-tool contract authority; later reversal requires synchronized runtime, typing, documentation, test, and release-evidence changes.</reversibility>
  <files>tests/test_mypyc_guard.py, tests/test_construction_parity.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-01 through D-20 and all deferred ideas
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md` complete matrices and T-30-01 through T-30-08
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-05-SUMMARY.md` actual focused documentation/example migration and final file set
    - `.specify/memory/spr-core-api.md` Task 1 normative contract to prove structurally and semantically
    - `tests/test_mypyc_guard.py` pure/native semantic probes, exact carrier layouts, source-region exclusions, and extension-origin helpers
    - `tests/test_construction_parity.py` complete Phase 30 adapter/semantic oracle
  </read_first>
  <behavior>
    - "Runtime/stub carrier fields and retained exports agree exactly with the final public contract."
    - "Every retained adapter reaches one canonical transaction and no construction/warning/persistence work appears in selectors, lifecycle runners, or triggers."
    - "The complete parity oracle covers observable finality, mode, lifecycle, identity, history, graph version, and atomic failure rather than dictionary equality alone."
  </behavior>
  <action>
Extend `tests/test_mypyc_guard.py` so the Phase 30 oracle contract is asserted under either exact pure-source or extension origin: runtime/stub carrier fields, retained exports, one canonical transaction per adapter, no construction/warning/persistence work in selectors/lifecycle/triggers, and the complete observable-semantic oracle. Keep one oracle implementation in `tests/test_construction_parity.py`; native evidence must invoke the same tests rather than copying scenarios. Immediately after those two final test-file modifications, before any heavyweight phase-wide gate, run the focused sub-60-second command below and repair any failure before continuing. Treat Task 1's synchronized SPR as the normative input and do not reopen documentation scope here.
  </action>
  <acceptance_criteria>
    - "T-30-01/T-30-02/T-30-07/T-30-08 have exact structural and observable-semantic assertions ready for both runtime origins."
    - "The parity oracle remains single-source and covers every retained adapter, sync/async behavior, and atomic failure."
    - "The focused construction-parity/mypyc command passes immediately after the final test-file edits."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_construction_parity.py tests/test_mypyc_guard.py -x -q -k "construction_parity or declarative or adapter or hot_path"</automated>
  </verify>
  <done>The final structural/parity oracle agrees with the synchronized normative contract and passes fast focused feedback before heavyweight closure.</done>
</task>

<task type="auto">
  <name>Task 3: Execute pure/native, restoration, performance, and full-regression closure</name>
  <reversibility rating="reversible">This evidence task changes no public contract; generated native shadows are path-constrained and recoverably relocated to a printed temporary backup.</reversibility>
  <files>tests/test_mypyc_guard.py, tests/test_construction_parity.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md` row 30-06-03, phase-gate sampling contract, and every threat control
    - `.planning/phases/29-expected-domain-rejection/29-04-PLAN.md` Task 2 exact pure/native inventory, EXIT restoration, origin assertions, and benchmark protocol
    - `tools/release_evidence.py` verify-source and slots-policy commands
    - `Taskfile.yml` docs, typing, source, build-check, and benchmark task definitions
    - `tests/test_performance_benchmarks.py` existing compiled singleton throughput regression signal
  </read_first>
  <action>
Run the exact closure sequence below after Tasks 1 and 2: non-mutating Ruff checks over every Phase 30 Python file; blocking mypy and visible advisory ty; slots policy; strict docs/doctests; pure-source check and exact source-origin assertion; the complete Phase 30 oracle in pure mode; a fresh in-place `task build-check`; fail-closed inventory accepting only absolute `$PWD/src/fast_fsm/core*.so` or `core*.pyd`; exact extension-origin assertion; the identical Phase 30 oracle and existing compiled singleton throughput test; recoverably move only verified shadows to a unique printed temporary backup on success or failure; reassert exact pure source origin; then run the full sequential suite. Reuse the Phase 29 EXIT-trap protocol with the phase number and focused test list changed—never broadly delete or trust unmatched paths. Any closure failure remains blocking and is repaired before re-running the affected gate.
  </action>
  <acceptance_criteria>
    - "T-30-01 through T-30-08 each have passing behavioral or structural evidence traceable to the validation register."
    - "The same complete adapter/finality/mode/atomicity oracle passes from exact pure and freshly compiled origins for sync and async machines."
    - "Direct compiled singleton throughput remains at least 200,000 operations/second and source-region tests find no dispatch-time construction policy."
    - "Only exact verified core extension shadows are recoverably moved; final origin is asserted as src/fast_fsm/core.py on success or failure."
    - "Ruff, mypy, visible ty, slots, strict docs/doctests, and the full sequential test suite complete with outputs recorded."
  </acceptance_criteria>
  <verify>
    <automated>
set -eu
uv run ruff format --check src/fast_fsm/core.py src/fast_fsm/core.pyi src/fast_fsm/__init__.py tests/test_construction_parity.py tests/test_builder.py tests/test_advanced_functionality.py tests/test_final_states.py tests/test_transition_modes.py tests/test_graph_invariants.py tests/test_mypyc_guard.py tests/test_readme_examples.py examples/cross_fsm_demo.py
uv run ruff check src/fast_fsm/core.py src/fast_fsm/core.pyi src/fast_fsm/__init__.py tests/test_construction_parity.py tests/test_builder.py tests/test_advanced_functionality.py tests/test_final_states.py tests/test_transition_modes.py tests/test_graph_invariants.py tests/test_mypyc_guard.py tests/test_readme_examples.py examples/cross_fsm_demo.py
task typecheck-mypy
task typecheck-ty || true
uv run python tools/release_evidence.py slots-policy --json
task docs-check
task docs-test
task pure-source-check
uv run python -c 'from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); expected = Path("src/fast_fsm/core.py").resolve(); assert origin == expected, (origin, expected); print(origin)'
FAST_FSM_BUILD_MODE=pure uv run pytest tests/test_construction_parity.py tests/test_builder.py tests/test_advanced_functionality.py tests/test_final_states.py tests/test_transition_modes.py tests/test_graph_invariants.py tests/test_mypyc_guard.py -x -q -k "construction_parity or declarative or deprecat or from_dict or clone or snapshot or adapter or hot_path"
native_backup="$(mktemp -d "${TMPDIR:-/tmp}/fast-fsm-phase30-native-backup.XXXXXX")"
shadow_report="$(mktemp "${TMPDIR:-/tmp}/fast-fsm-phase30-shadow-report.XXXXXX")"
shadow_manifest="${shadow_report}.paths"
shadow_stdout="${shadow_report}.stdout"
inventory_native() { : &gt;"$shadow_manifest"; if uv run python tools/release_evidence.py verify-source --json &gt;"$shadow_stdout" 2&gt;"$shadow_report"; then return 0; fi; shadow_count=0; while IFS= read -r report_line; do case "$report_line" in "  - "*) shadow_path="${report_line#  - }" ;; *) continue ;; esac; case "$shadow_path" in "$PWD"/src/fast_fsm/core*.so|"$PWD"/src/fast_fsm/core*.pyd) ;; *) echo "refusing untrusted shadow path: $shadow_path" &gt;&amp;2; return 1 ;; esac; test -f "$shadow_path" || return 1; printf '%s\n' "$shadow_path" &gt;&gt;"$shadow_manifest"; shadow_count=$((shadow_count + 1)); done &lt;"$shadow_report"; test "$shadow_count" -gt 0; }
restore_native() { if [ ! -s "$shadow_manifest" ]; then inventory_native; fi; while IFS= read -r shadow_path; do test -f "$shadow_path" || continue; mv "$shadow_path" "$native_backup/"; done &lt;"$shadow_manifest"; echo "native shadows retained at $native_backup"; task pure-source-check; uv run python -c 'from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); expected = Path("src/fast_fsm/core.py").resolve(); assert origin == expected, (origin, expected); print(origin)'; }
restore_on_exit() { native_status=$?; trap - EXIT; if ! restore_native; then native_status=1; fi; exit "$native_status"; }
trap restore_on_exit EXIT
task build-check
inventory_native
test -s "$shadow_manifest"
uv run python -c 'from importlib.machinery import EXTENSION_SUFFIXES; from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); assert any(str(origin).endswith(suffix) for suffix in EXTENSION_SUFFIXES), origin; print(origin)'
FAST_FSM_BUILD_MODE=compiled uv run pytest tests/test_construction_parity.py tests/test_builder.py tests/test_advanced_functionality.py tests/test_final_states.py tests/test_transition_modes.py tests/test_graph_invariants.py tests/test_mypyc_guard.py -x -q -k "construction_parity or declarative or deprecat or from_dict or clone or snapshot or adapter or hot_path"
FAST_FSM_BUILD_MODE=compiled uv run pytest tests/test_performance_benchmarks.py::TestAdvancedPerformance::test_trigger_min_throughput -x -q
trap - EXIT
restore_native
FAST_FSM_BUILD_MODE=pure uv run pytest tests/ -x -q
    </automated>
  </verify>
  <done>Phase 30 passes complete dual-origin semantic, structural, source-restoration, throughput, documentation, typing, slots, and regression closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime/stub/docs authority → pure/native oracle | Drift between contract authorities can make one runtime mode silently weaker. |
| Source tree → fresh native extension import | A shadow extension can spoof which implementation tests execute and must be inventoried/restored exactly. |
| Construction adapters → dispatch hot path | Cold policy must not add allocation, reflection, or unrelated scans to direct singleton dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Tampering | declarative metadata | high | mitigate | Complete behavioral/structural matrix under pure/native origins. |
| T-30-02 | Tampering | declarative guard execution | high | mitigate | Exactly-once counters in the shared parity oracle. |
| T-30-03 | Repudiation | deprecated warning boundaries | medium | mitigate | Exact count/category/message/location tests in both runtime modes. |
| T-30-04 | Information Disclosure | warnings/errors/contracts | medium | mitigate | Fixed bounded wording and payload-free error assertions. |
| T-30-05 | Tampering | from_dict publication | high | mitigate | Complete exact parsing plus atomic failure oracle. |
| T-30-06 | Tampering | clone ownership | medium | mitigate | Mutable-container identity/isolation matrix. |
| T-30-07 | Tampering | adapter-specific semantics | high | mitigate | Every retained adapter executes one shared observable-semantic oracle. |
| T-30-08 | Denial of Service | dispatch/native path | high | mitigate | Structural exclusions and compiled singleton throughput floor. |
| T-30-SC | Tampering | package supply chain | low | accept | Phase 30 adds no package install or dependency; uv.lock and the existing toolchain remain unchanged. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | One clear construction path with adapter, clone, and persistence semantic parity | 30-01..06 | COVERED |
| REQ | BUILD-01 | Builder primary, direct advanced, from_dict adapter | 30-05, 30-06 | COVERED |
| REQ | BUILD-02 | Declarative behavior through canonical builder machinery | 30-01, 30-02, 30-06 | COVERED |
| REQ | BUILD-03 | Four warned retained compatibility helpers | 30-03, 30-05, 30-06 | COVERED |
| REQ | BUILD-06 | All retained adapters share final/internal validation | 30-01..04, 30-06 | COVERED |
| REQ | BUILD-07 | Clone/dictionary semantic preservation and legacy defaults | 30-04, 30-05, 30-06 | COVERED |
| REQ | BUILD-08 | Snapshot v1 receiver-owned topology truth | 30-04, 30-05, 30-06 | COVERED |
| CONTEXT | D-01..D-04 | Construction hierarchy and builder identity/cache/repair | 30-01, 30-02, 30-05, 30-06 | COVERED |
| CONTEXT | D-05..D-08 | Declarative metadata/import/identity/direct compatibility | 30-01, 30-02, 30-06 | COVERED |
| CONTEXT | D-09..D-12 | Deprecation boundary/window/wording/docs ownership | 30-03, 30-05, 30-06 | COVERED |
| CONTEXT | D-13..D-17 | Dictionary, compatibility, clone, and snapshot contracts | 30-04, 30-05, 30-06 | COVERED |
| CONTEXT | D-18..D-20 | Canonical all-adapter pure/native parity and hot-path absence | 30-01..04, 30-06 | COVERED |
| RESEARCH | — | Build-time derivation with state-owned guard, private warning workers, exact two-phase persistence, shared semantic oracle | 30-01..06 | COVERED |
| RESEARCH | — | No new dependency, schema version, topology snapshot, executable serialization, or second registrar | 30-01..06 prohibitions/actions | COVERED |

<verification>
- `uv run pytest tests/test_construction_parity.py tests/test_mypyc_guard.py -x -q -k "construction_parity or declarative or adapter or hot_path"`
- `task typecheck-mypy` and visible `task typecheck-ty`
- `uv run python tools/release_evidence.py slots-policy --json`
- `task docs-check && task docs-test`
- Exact pure/native shared Phase 30 oracle with recoverable shadow restoration
- `FAST_FSM_BUILD_MODE=compiled uv run pytest tests/test_performance_benchmarks.py::TestAdvancedPerformance::test_trigger_min_throughput -x -q`
- Final exact pure-source assertion and `FAST_FSM_BUILD_MODE=pure uv run pytest tests/ -x -q`
</verification>

<success_criteria>
- BUILD-01..03 and BUILD-06..08 are synchronized across the normative contract and dual-origin executable evidence.
- D-01..D-20 are proven without any deferred Phase 31/32 feature entering scope.
- All T-30 controls close under pure and freshly compiled runtime evidence.
- The source tree ends on exact pure `src/fast_fsm/core.py` origin after every native closure outcome.
</success_criteria>

<output>
Create `.planning/phases/30-builder-first-construction-persistence-parity/30-06-SUMMARY.md` when done.
</output>
