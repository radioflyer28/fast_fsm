---
phase: 30-builder-first-construction-persistence-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - src/fast_fsm/core.py
  - tests/test_builder.py
  - tests/test_transition_modes.py
  - tests/test_final_states.py
  - tests/test_graph_invariants.py
  - tests/test_construction_parity.py
autonomous: true
requirements:
  - BUILD-02
  - BUILD-06
estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-06/D-07: declaration import handles initial and later staged states, omitted/single/public list source constraints, priority/timing/mode identity, and async auto-detection without manual mirroring."
    - "D-08: destination-less declarations remain direct-handle compatibility behavior and never invent a machine edge."
    - "D-04: invalid destination, final source, non-self internal mode, async incompatibility, and explicit/declarative collisions fail before candidate/cache publication and leave the builder repairable."
    - "BUILD-06/D-18: direct, batch, builder, declarative, and callback-state construction share exact final/internal validation and atomicity."
    - "D-19/D-20: the parity oracle compares observable lifecycle, priority/mode selection, identity, graph version, and failure fingerprints rather than dictionary equality."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Complete declarative derivation, matching, async preflight, and builder repair semantics"
    - path: "tests/test_builder.py"
      provides: "Declarative applicability, conflict, async, cache, and repair matrix"
    - path: "tests/test_transition_modes.py"
      provides: "Sync/async internal/external declarative lifecycle parity"
    - path: "tests/test_final_states.py"
      provides: "Canonical final-source parity through declarative/builder paths"
    - path: "tests/test_construction_parity.py"
      provides: "Direct/batch/builder/declarative/callback observable-semantic comparison"
  key_links:
    - from: "FSMBuilder staged state registry"
      to: "declaration applicability"
      via: "state-owned metadata whose omitted/single/public list source constraint matches the exact staged owner after defensive tuple copying"
      pattern: "from_state"
    - from: "declarative async requirements"
      to: "builder machine type selection"
      via: "existing supported-wrapper preflight over state-owned handlers"
      pattern: "_preflight_async_requirements"
    - from: "invalid derived row"
      to: "repairable builder"
      via: "private candidate failure before callback/cache publication"
      pattern: "self._machine = candidate"
  flagged_assumptions: []
  prohibitions:
    - statement: "Must not create a second topology engine or parallel declarative registrar."
      status: "flagged-unverified"
    - statement: "Must not move construction, deprecation, or persistence policy into dispatch."
      status: "flagged-unverified"
    - statement: "Must not remove simple_fsm, quick_fsm, StateMachine.quick_build, or StateMachine.from_states during v0.5.x."
      status: "flagged-unverified"
    - statement: "Must not claim that pre-v0.5 readers preserve new final/internal semantics."
      status: "flagged-unverified"
---

<objective>
Expand the declarative-builder tracer into the complete state-owned, sync/async, conflict, and atomic-repair contract, then compare it against existing construction adapters.

Purpose: Finish BUILD-02 and the declarative portion of BUILD-06 without changing runtime dispatch ownership or creating machine-wide handler storage.
Output: Complete declarative-builder behavior, exhaustive failure/retry evidence, and an observable-semantic adapter matrix through callback-state construction.
</objective>

## Artifacts this phase produces

- Full topology-complete declaration applicability and conflict rules through the builder.
- Sync/async auto-detection and exactly-once internal/external lifecycle parity.
- Repairable builder failures for unknown/final/mode/conflict/async cases.
- Direct, batch, builder, declarative, and callback-state parity cells in the central oracle.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-PATTERNS.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-01-SUMMARY.md

<interfaces>
Plan 30-01 provides:
- Mode-aware frozen `_DeclarativeHandlerMetadata` and `_DeclarativeHandler` records.
- A builder cold-path derivation helper/path that produces ordinary `_TransitionRequest` rows without copying decorator guards.
- A central `tests/test_construction_parity.py` oracle and structural guards for one canonical apply call.

Existing behavior retained:
- `FSMBuilder._preflight_async_requirements()` is authoritative for supported sync/async condition/handler classification.
- `StateMachine._merge_transition_slot()` owns exact duplicate and equal-priority nonduplicate behavior.
- Direct `DeclarativeState.handle_event()` and `AsyncDeclarativeState.handle_event_async()` resolve state-owned handlers independently of machine topology.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete declarative applicability, async detection, conflicts, and builder repair</name>
  <reversibility rating="costly">D-07 defines how public decorator declarations bind to canonical state identity and collision rules; reversal would affect author code and test matrices.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_builder.py, tests/test_transition_modes.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-04 through D-08
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md` Required Test Matrix items 2/3 and Open Questions (RESOLVED) items 1/2
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-01-SUMMARY.md` actual declaration carrier/derivation identifiers
    - `src/fast_fsm/core.py` declaration discovery, mode-aware resolver, builder async preflight, candidate construction, callback wiring, and cache assignment produced by Plan 30-01
    - `tests/test_builder.py:461-905,1120-1160,1742-1816,2382-2835` current declarative, manual-mirror, async, and failure patterns
    - `tests/test_transition_modes.py:342-470,546-670,799-833` sync/async selected-mode and lifecycle assertions
  </read_first>
  <behavior>
    - "Declarations on initial and later staged state objects import when omitted/single/public list source constraints match that exact owner; the builder copies a public list into an internal tuple, so later caller mutation changes nothing."
    - "Destination-less declarations remain available only to direct handle_event compatibility and create no topology row."
    - "Unknown targets, outgoing final sources, and internal non-self targets fail through canonical bounded errors before cache publication."
    - "Explicit/declarative exact duplicates are version-neutral; equal-priority nonduplicates, including mode differences, use canonical conflict errors rather than overwrite."
    - "Async handler/nested guard requirements auto-select AsyncStateMachine; forced sync rejects before invocation/publication; sync and async guards/handlers execute once."
    - "After every failed build, the complete staging fingerprint and absent cache are unchanged; after supported repair, build succeeds and is cached."
  </behavior>
  <action>
Write the full builder matrix first. Cover initial/later owners, omitted/single/public `list[str]` source constraints, caller-list mutation, topology-incomplete declarations, unknown targets, final sources, invalid internal destinations, explicit/declarative exact duplicates and conflicts, cached success, and supported repaired retry. The public decorator runtime/stub contract remains `str | list[str]`; do not broaden it to tuple. At the boundary, defensively copy the accepted caller list into immutable internal tuple metadata before builder derivation. Replace existing tests that manually mirror decorated topology with builder-only authoring where they are not explicit compatibility/conflict cases. Add sync/async counters for declarative guard, handler, entry guard, state permission, and lifecycle; preserve expected-rejection ownership established in Phase 29.

Adjust only the Plan 30-01 cold-path derivation/matching/preflight seams. A declaration is imported from the exact staged declarative object only when its source constraint applies to that object's canonical name and it has a destination. Bind endpoints for identity convenience but rely on the canonical transaction for final/internal/collision validation. Recompute declarations and async requirements on every failed attempt, never mutate `_transitions` or add a machine-wide handler table, and assign `_machine_type`/`_machine` only after topology and callbacks succeed. Direct compatibility calls may omit a mode filter only when resolution is unambiguous; machine dispatch always includes selected mode.
  </action>
  <acceptance_criteria>
    - "D-06/D-07: applicability, identity, mode, priority, timing, duplicate/conflict, and sync/async behavior are covered without manual topology mirroring; public plural input remains list[str] while internal metadata stores a defensive tuple copy."
    - "D-04: every invalid/collision/async failure leaves states, explicit requests, callbacks, auto-detect state, machine type, and cache fingerprint unchanged; repaired retry succeeds."
    - "D-08: destination-less direct handlers still run through direct APIs and do not create self-edges."
    - "T-30-01/T-30-02: metadata remains immutable and state-owned guard/handler counters remain exactly one."
    - "Targeted builder/mode tests and blocking mypy pass."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_builder.py tests/test_transition_modes.py -x -q -k "declarative and (builder or source or target or conflict or repair or cache or async or internal or exactly_once)" &amp;&amp; task typecheck-mypy</automated>
  </verify>
  <done>Declarative builder authoring is complete across applicability, sync/async mode, conflicts, exactly-once execution, and repairable atomic failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand construction parity through direct, batch, builder, declarative, and callback paths</name>
  <files>tests/test_construction_parity.py, tests/test_final_states.py, tests/test_transition_modes.py, tests/test_graph_invariants.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-18 through D-20
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md` Required Validation Matrices 2/3/7 and T-30-07/T-30-08
    - `tests/test_construction_parity.py` Plan 30-01 oracle contracts
    - `tests/test_final_states.py:79-221` construction surface and final-source atomicity matrix
    - `tests/test_transition_modes.py:90-220,342-470,546-670` mode result/history/lifecycle traces
    - `tests/test_graph_invariants.py:205-354,456-595,707-887` identity, canonical adapter, final/mode atomicity, and graph-version patterns
  </read_first>
  <behavior>
    - "Equivalent direct, batch, builder, declarative, and callback-state topologies select the same priority/mode and produce the same state/finality/lifecycle/history result."
    - "Outgoing-final and internal-nonself failures use the same bounded semantic error and publish no prefix/version change through every covered adapter."
    - "Caller-owned State/CallbackState/DeclarativeState identities remain exact; adapter-specific private container identities remain independent."
    - "The oracle is runtime-mode neutral and contains no implementation mocking or dictionary-only equivalence shortcut."
  </behavior>
  <action>
Expand the central parity oracle rather than duplicating production rules in tests. Parameterize adapter constructors for direct registration, batch registration, FSMBuilder explicit rows, FSMBuilder imported declarations, and CallbackState construction followed by canonical registration. Reuse one small topology containing a final destination plus an internal self-transition and compare observable selected priority/mode, lifecycle trace, state identity/finality, result/history, graph version, and atomic failure fingerprints. Include sync/async variants where the adapter supports them.

Extend final/mode regression files only with adapter-specific boundary assertions that add signal not already in the central oracle. Extend structural adapter coverage so declaration import/callback routes cannot bypass `_TransitionRequest` plus one canonical apply or mutate graph version on failure. Do not include helpers, clone, or deserialization yet; Plans 30-03/04 add those cells after their production contracts exist.
  </action>
  <acceptance_criteria>
    - "BUILD-06/D-18: all adapter cells owned by this plan enforce the same final/internal contract through the canonical seam."
    - "D-19: equivalence is based on dispatch/lifecycle/history/identity/failure behavior, not only serialized topology."
    - "D-20/T-30-07: structural assertions reject bypass, post-publication repair, prefix mutation, and graph-version drift."
    - "The central oracle is ready for helper and persistence expansion without changing its semantic assertions."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_construction_parity.py tests/test_final_states.py tests/test_transition_modes.py tests/test_graph_invariants.py -x -q -k "construction_parity or adapter or declarative or callback or final_source or internal"</automated>
  </verify>
  <done>The central oracle proves observable final/internal parity for direct, batch, builder, declarative, and callback-state construction with atomic failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| State-owned handler metadata → topology derivation | Applicability and destination completeness must be resolved without inventing edges or moving handlers globally. |
| Builder staging → private sync/async candidate | Classification and canonical failures must not leak a partial candidate or corrupt reusable staging. |
| Adapter inputs → canonical transaction | Different syntaxes must not weaken final/internal validation or graph-version atomicity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Tampering | plural declaration sources | high | mitigate | Defensive immutable copy plus post-decoration caller-mutation tests. |
| T-30-02 | Tampering | declarative eligibility | high | mitigate | State-owned exactly-once guard/handler counters across sync/async paths. |
| T-30-07 | Tampering | adapter validation | high | mitigate | One semantic oracle and atomic failure fingerprint across covered adapters. |
| T-30-08 | Denial of Service | runtime dispatch | high | mitigate | Limit all applicability/reflection/classification work to build attempts and retain source-region exclusions. |
</threat_model>

<verification>
- `uv run pytest tests/test_builder.py tests/test_transition_modes.py tests/test_final_states.py tests/test_graph_invariants.py tests/test_construction_parity.py -x -q -k "declarative or construction_parity or adapter or final_source or internal"`
- `uv run ruff format --check` and `uv run ruff check` on Plan 30-02 Python files
- `task typecheck-mypy`
- `task typecheck-ty` (visible advisory result)
</verification>

<success_criteria>
- BUILD-02 is complete for topology-complete declarations without a parallel registrar.
- The builder remains cache-safe and repairable across the complete declarative failure matrix.
- Covered construction adapters share final/internal validation and observable semantics.
- Direct state-owned event handling remains compatible and separate from machine topology authoring.
</success_criteria>

<output>
Create `.planning/phases/30-builder-first-construction-persistence-parity/30-02-SUMMARY.md` when done.
</output>
