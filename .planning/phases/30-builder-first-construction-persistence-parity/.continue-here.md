---
context: phase
phase: 30-builder-first-construction-persistence-parity
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-09-17T21:51:39.386Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Do not discard or blindly replace the four-file partial fix. Inspect `git diff` first; it contains drafted CR-01/WR-01 implementation and tests but has not been verified or committed.
- [ ] CONSTRAINT: Explicit/declarative topology compatibility must yield one logical candidate and one guard evaluation when a legacy explicit row owns nondefault priority, timing, or mode.
- [ ] CONSTRAINT: Pure success is not native proof. Finish with the Plan 30-06 fresh-native oracle, verified shadow relocation, and exact pure-origin assertion.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Restarting over partial work | The code-fixer was interrupted after editing four intended files but before commits/tests. Reinitializing the fix would risk losing or duplicating valid work. | blocking | Inspect and adopt/supersede each existing hunk deliberately; do not revert the four files. |
| Duplicate declaration import | A derived default row beside a legacy explicit row changed priority/mode and evaluated a state-owned guard twice. | blocking | Preserve explicit topology authority for the matching state-owned declaration and prove one evaluation in pure/native tests. |
| Pure-only compatibility confidence | Earlier closure found mypyc-only warning attribution and subclass failures. | blocking | Run the identical focused oracle from asserted pure and fresh native origins, then restore exact pure source. |
| Focused-suite-only completion | Two prior wave regressions escaped focused matrices and appeared in the full suite. | advisory | Run the complete sequential suite before re-review/verifier completion. |

<current_state>
Phase 30 plans and Nyquist/security/UI audits are complete. Deep review `30-REVIEW.md` found CR-01 and WR-01. The active `gsd-code-fixer` was interrupted on user pause. It left uncommitted intended edits in `src/fast_fsm/core.py`, `src/fast_fsm/_construction_compat.py`, `tests/test_builder.py`, and `tests/test_construction_parity.py`. No fix commit or test evidence exists yet. STATE remains verifying, Plan 6 of 6.
</current_state>

<completed_work>

- Restored the prior handoff and explicitly acknowledged both blocking anti-patterns.
- Consumed the old one-shot handoff in commit `4876f47`.
- Dispatched the Phase 30 code-fixer and then explicitly interrupted it for this pause.
- Confirmed the four intended diffs pass `git diff --check` and no compiled `core` shadow exists.
- Drafted code currently appears to:
  - suppress derived declarative topology when an explicit source/trigger/target row resolves to the exact state-owned handler;
  - preserve compatibility callable metadata with `update_wrapper` and runtime annotation globals;
  - add explicit topology/guard-count tests and public provenance/type-hint/pickle tests.
</completed_work>

<remaining_work>

1. Inspect every existing hunk and finish or correct the CR-01/WR-01 implementation without reverting partial work.
2. Run focused pure tests first. Pay particular attention to:
   - whether matching uses the same priority/internal-aware handler resolver correctly for nondefault explicit rows;
   - after/within timing ownership;
   - genuinely distinct candidates;
   - stateful guard one-evaluation behavior;
   - `typing.get_type_hints`, signature/doc/module/qualname, pickle round trips, and subclass factories.
3. Split and commit CR-01 and WR-01 atomically once their focused tests pass.
4. Run Ruff, mypy, package/installed-artifact tests, Phase 29/30 focused matrices, fresh-native oracle, performance/source restoration, and full suite.
5. Update/re-run deep review until clean, then security/regression/verifier gates and `phase.complete 30`.
</remaining_work>

<decisions_made>

- Existing uncommitted changes are authoritative partial work and must be inspected, not thrown away.
- Matching explicit topology should suppress only the corresponding derived declaration; genuinely distinct destinations/candidates remain canonical candidates.
- Wrapper compatibility remains an interpreted cold boundary; no metadata work belongs in compiled dispatch.
- Routine/recommended GSD actions remain pre-approved.
</decisions_made>

<blockers>

- Technical: four intended files are partially changed, unverified, and uncommitted.
- External/nonblocking: push still requires explicit trust approval for `https://github.com/radioflyer28/fast_fsm.git`.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — exact machine-readable partial state.
2. `git diff -- src/fast_fsm/core.py src/fast_fsm/_construction_compat.py tests/test_builder.py tests/test_construction_parity.py` — authoritative partial implementation.
3. `.planning/phases/30-builder-first-construction-persistence-parity/30-REVIEW.md` — CR-01/WR-01 acceptance criteria.
4. `.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md` — required matrices.
5. `.planning/phases/30-builder-first-construction-persistence-parity/30-06-SUMMARY.md` — native closure and restoration protocol.
6. `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` — locked decisions.

## Critical Anti-Patterns (do NOT repeat these)

- Do not reset, checkout, or overwrite the four intended partial files.
- Do not assume the drafted matching logic is correct until focused tests exercise nondefault priority/timing/mode and guard counts.
- Do not mark review clean from pure tests alone.
- Do not stash or stage unrelated `.beads`, `.gsd`, lock, or cache artifacts.

## Infrastructure State

- Branch: `codex/v0.5.0-explicit-flat-fsm-semantics`, ahead of tracked remote.
- Active fix subagent `/root/phase30_review_fix` was explicitly interrupted; no background writer remains.
- No async external jobs or running servers.
- No `src/fast_fsm/core*.so` or `core*.pyd` shadow was found.
- Unrelated dirt remains in `.beads/`, `.gsd/`, `.planning/milestone.lock`, and `.planning/research/.cache/`.

<context>
The next session should continue the interrupted fix, not restart Phase 30. The current diff is plausible but unproven. The most suspicious area is CR-01 matching: it calls `_resolve_declarative_handler` with explicit priority/internal metadata, so verify that this actually identifies the same declaration when the explicit topology intentionally differs from decorator defaults. Tests must drive the compatibility rule, not the current implementation shape.
</context>

<next_action>
Start with: resume a `gsd-code-fixer` on the existing four-file diff, instruct it to inspect/continue rather than revert, run focused pure tests immediately, and only then split atomic CR-01/WR-01 commits followed by native/full closure.
</next_action>
