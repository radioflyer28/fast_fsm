---
context: phase
phase: 30-builder-first-construction-persistence-parity
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-09-17T21:42:28.725Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Explicit/declarative topology compatibility — Phase 30 auto-import currently duplicates a legacy explicit row with the same source/trigger/target, changes priority/mode selection, and may evaluate one state-owned guard twice. The resume path must fix CR-01 and prove one evaluation in pure and native modes before verification.
- [ ] CONSTRAINT: Pure/native compatibility closure — mypyc previously changed warning attribution and rejected interpreted subclasses. Any fix to compatibility boundaries must run the same focused oracle under an asserted fresh native origin, safely relocate only verified extension shadows, and reassert pure source afterward.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Duplicate declaration import | `FSMBuilder.build()` appends a derived row even when legacy code already staged the same source/trigger/target with explicit priority/timing/mode; a probe changed the winner and ran a stateful guard twice. | blocking | Fix CR-01 first; add pure/native tests covering nondefault priority, timing, mode, and one guard evaluation; rerun deep review. |
| Trusting only focused plan tests | Wave 1 and Wave 2 focused suites passed while the full wave gate found Phase 29 rejection and structural-owner regressions. | advisory | Keep the full sequential suite after each fix cycle and before verifier completion. |
| Assuming pure Python proves native compatibility | Phase 30 closure found compiled-only warning attribution and interpreted-subclass construction defects. | blocking | Use the Plan 30-06 fail-closed native protocol and always finish with exact pure-source restoration/origin proof. |
| Stashing unrelated dirty files for rebase | The checkout contains user-owned `.beads`, `.gsd`, lock, and cache artifacts; a safe rebase was correctly skipped rather than stashing them. | advisory | Preserve these files; do not stage, stash, delete, or revert them. Rebase/push only when it can be done without absorbing user dirt. |

<current_state>
Phase 30 plans 30-01 through 30-06 are executed and summarized. Nyquist is validated, security is SECURED with zero open threats, and UI is formally N/A/PASS. The deep code review is committed at `517928a` with status `issues_found`: CR-01 is a blocker and WR-01 is a warning that should also be fixed before verification. STATE remains `verifying`, Plan 6 of 6. Do not call `phase.complete` yet.
</current_state>

<completed_work>

Completed:
- Phase 30 discussion, research, pattern mapping, validation design, six checked plans, and all six execution waves.
- Post-wave full-suite repairs for Phase 29 declarative rejection compatibility and the batch-publication structural invariant.
- Fresh native construction parity, subclass compatibility, warning attribution, throughput, source restoration, docs, typing, slots, and full-suite closure.
- Nyquist gap fix requiring `_construction_compat.py` in synthetic sdist evidence (`b35ac05`, `3577eca`).
- Security payload-disclosure fix and SECURED audit (`7bc4041`, `819c2e3`).
- UI applicability audit (`9f7e84e`).
- Deep code review report (`517928a`).
</completed_work>

<remaining_work>

1. Run `gsd-code-fixer` for both findings in `30-REVIEW.md`:
   - CR-01: preserve explicit topology authority when a matching declarative handler already exists, or reject a truly ambiguous combination; never derive a second candidate that changes semantics or re-runs the guard.
   - WR-01: preserve public module/name/qualname/doc/signature/annotations/wrapped metadata and make runtime type hints plus `simple_fsm`/`quick_fsm` pickling work in pure and native modes.
2. Run focused tests, the complete Phase 30 matrix, a fresh native oracle with safe restoration, and the full sequential suite.
3. Re-run the deep code reviewer until `30-REVIEW.md` is clean.
4. Re-check security if the fixes touch diagnostic or compatibility boundaries; then run the prior-phase regression gate and `gsd-verifier`.
5. Only after verifier passes: run `phase.complete 30`, commit tracking, and continue Phase 31 discuss → plan → execute under the standing goal.
</remaining_work>

<decisions_made>

- Routine/recommended GSD choices remain pre-approved; do not stop for them.
- Worktree isolation is degraded to sequential main-branch execution because HEAD diverges from `origin/HEAD`.
- `FSMBuilder` is the primary programmatic interface; direct construction remains advanced; `from_dict` remains the persistence adapter.
- State-owned declarative guards execute exactly once and are not copied into transition entries.
- Non-blocking audit findings that violate locked contracts are fixed rather than accepted.
</decisions_made>

<blockers>

- Technical: `30-REVIEW.md` CR-01 and WR-01 are not fixed. CR-01 blocks Phase 30 completion.
- External/nonblocking: push to `origin` (`https://github.com/radioflyer28/fast_fsm.git`) was rejected because remote trust was not established in the transcript. Obtain explicit approval for that exact remote before retrying.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable exact pause state.
2. `.planning/phases/30-builder-first-construction-persistence-parity/30-REVIEW.md` — current blocker and warning with probes and fix criteria.
3. `.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md` — validated matrix and native closure contract.
4. `.planning/phases/30-builder-first-construction-persistence-parity/30-SECURITY.md` — closed threats and accepted low supply-chain risk.
5. `.planning/phases/30-builder-first-construction-persistence-parity/30-06-SUMMARY.md` — authoritative pure/native closure protocol and repairs.
6. `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` — D-01 through D-20 locked decisions.
7. `.planning/STATE.md` and `.planning/ROADMAP.md` — current milestone position.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat matching explicit and declarative rows as harmless duplicate candidates; topology metadata changes winner selection and guard evaluation count.
- Do not consider a pure-only fix complete; the public compatibility boundary is intentionally interpreted beside compiled core code and must be tested in both modes.
- Do not skip the full suite because focused matrices pass; this phase already produced two regressions visible only at the full gate.
- Do not stash, stage, or clean unrelated user-owned artifacts to enable rebase/push.

## Infrastructure State

- Branch: `codex/v0.5.0-explicit-flat-fsm-semantics`, more than 100 commits ahead of its remote tracking branch.
- Source origin after native cleanup: `src/fast_fsm/core.py`; no compiled core shadow should remain.
- No running background servers or async external jobs.
- Unrelated dirty artifacts to preserve: `.beads/dolt-server.log`, `.beads/dolt.20260617T201152Z.corrupt.backup/.dolt/`, `.gsd/`, `.planning/milestone.lock`, and `.planning/research/.cache/*.json`.
- `task typecheck-ty` has known advisory relative-import diagnostics; blocking mypy is green.

<context>
The implementation is broadly complete and strongly tested, but the deep review found two real compatibility holes that the committed suite missed. The immediate priority is correctness, not additional feature work: fix explicit/declarative deduplication without creating a second topology engine, then repair wrapper metadata without moving compatibility policy into compiled dispatch code. Preserve the canonical request transaction and cold-path-only design.
</context>

<next_action>
Start with: spawn `gsd-code-fixer` against both findings in `30-REVIEW.md`, require atomic commits and pure/native regressions, then re-run `gsd-code-reviewer` before any verifier or `phase.complete` call.
</next_action>
