---
phase: 28-same-state-transition-modes
plan: 03
type: execute
wave: 3
depends_on:
  - 28-02
files_modified:
  - src/fast_fsm/core.py
  - src/fast_fsm/core.pyi
  - tests/test_transition_modes.py
  - tests/test_async.py
  - tests/test_transition_lifecycle.py
  - tests/test_mypyc_guard.py
  - .specify/memory/spr-core-api.md
autonomous: true
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
  - MODE-04
  - MODE-05
  - MODE-06
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-13 through D-15, async selection/execution produces the same mode, priority, stage, commit, history, timing, failure, and cancellation truth as sync execution."
    - "Cancellation before internal commit is uncommitted; cancellation during retained post-commit work is committed; skipped state lifecycle surfaces cannot originate cancellation."
    - "Every async cancellation releases task ownership and permits a subsequent successful trigger on the same machine."
    - "Per D-01, D-08, D-11, D-16, and D-17, the canonical carrier/stub layouts are slotted, mypyc-safe, callback-payload-neutral, and behaviorally identical in pure and freshly compiled modes."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Async internal/external lifecycle parity and task-local cancellation truth"
      contains: "internal"
    - path: "src/fast_fsm/core.pyi"
      provides: "Runtime/stub field and signature parity"
      contains: "internal: bool"
    - path: "tests/test_transition_modes.py"
      provides: "Async cancellation, failure, timing, and reuse oracle"
    - path: "tests/test_async.py"
      provides: "Machine-type parity regressions"
    - path: "tests/test_transition_lifecycle.py"
      provides: "Event-handshake cancellation and observer evidence"
    - path: "tests/test_mypyc_guard.py"
      provides: "Wave 0 structural, exact-bool, direct-dispatch, and pure/native semantic proof"
    - path: ".specify/memory/spr-core-api.md"
      provides: "Living maintainer contract for the new public mode semantics"
  key_links:
    - from: "async selected candidate / task-local mode"
      to: "outer cancellation finalizer"
      via: "candidate mode retained beside priority, stage, and committed cells"
      pattern: "async_selection.*internal"
    - from: "async internal runner"
      to: "sync internal contract"
      via: "matching retained/suppressed lifecycle slots and one logical commit boundary"
      pattern: "internal"
    - from: "runtime carrier field order"
      to: "core.pyi and native tests"
      via: "exact AST/slot/signature assertions plus a dual-origin semantic probe"
      pattern: "internal"
  prohibitions: []
---

<objective>
Close sync/async parity and prove the transition-mode contract across typing, slots, mypyc, and full regression gates.

Purpose: Make cancellation and ownership truth as explicit as successful execution, then verify that the complete canonical semantic spine behaves identically in pure source and a freshly compiled extension.
Output: Async lifecycle/cancellation/reuse parity, structural and typing guards, pure/native semantic evidence, restored source origin, full-suite proof, and an updated living core contract.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-same-state-transition-modes/28-CONTEXT.md
@.planning/phases/28-same-state-transition-modes/28-RESEARCH.md
@.planning/phases/28-same-state-transition-modes/28-PATTERNS.md
@.planning/phases/28-same-state-transition-modes/28-VALIDATION.md
@.planning/phases/28-same-state-transition-modes/28-02-SUMMARY.md
@src/fast_fsm/core.py
@src/fast_fsm/core.pyi
@tests/test_mypyc_guard.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Match async lifecycle, cancellation, ownership, and reuse truth</name>
  <files>src/fast_fsm/core.py, tests/test_transition_modes.py, tests/test_async.py, tests/test_transition_lifecycle.py</files>
  <read_first>
    - `.planning/phases/28-same-state-transition-modes/28-CONTEXT.md` D-13 through D-15
    - `.planning/phases/28-same-state-transition-modes/28-RESEARCH.md` Pattern 5 and async validation matrix
    - `src/fast_fsm/core.py` async selector, `_execute_transition_async`, `trigger_async`, `_async_selection_priority`, lifecycle truth cells, and ownership release
    - `tests/test_transition_lifecycle.py` event/barrier cancellation matrix and ownership assertions
    - `tests/test_async.py` successful sync/async parity conventions
  </read_first>
  <behavior>
    - "Async internal success retains the same transition-level work and skips the same sync and async exit/entry surfaces as sync internal success."
    - "Guard cancellation before selection/commit reports candidate mode/priority/stage as uncommitted and re-raises cancellation through the established outer boundary."
    - "Cancellation in a retained async declarative/post-commit surface reports committed mode/history truth and stops the suffix."
    - "Every cancellation finalizes failure once, releases owner and lock, and permits a subsequent successful trigger."
    - "Mixed internal/external priority candidates have sync/async selection, failure, cancellation, and reuse parity."
  </behavior>
  <action>
Write the async matrix first using `asyncio.Event`/barrier handshakes and sentinel sync/async state callbacks; never use sleeps. Cover one pre-commit guard cancellation and one retained post-commit declarative cancellation, plus successful reuse after each. Assert selected priority, mode, lifecycle stage, commit flag, state/history, failure-observer cardinality, cancellation re-raise, `_async_owner_task`, and ownership-lock state.

Mirror the Plan 02 branch in `_execute_transition_async` per D-13: internal execution must skip synchronous and asynchronous source-exit/destination-entry hooks, callbacks, and listeners at the same slots while retaining before, logical commit, declarative, trigger, after, result, trace, and failure work. Keep guards, permission, timing, and priority selection mode-neutral per D-15.

Extend the existing task-local selected-candidate bookkeeping so the outer cancellation boundary can report internal mode alongside priority before `_PreparedDispatch` is available, without new per-dispatch mappings or callback payload injection. Preserve the single outer finalizer, bare cancellation re-raise, and `finally` ownership release per D-14. Prove skipped state lifecycle slots cannot be cancellation/failure origins and the same machine is immediately reusable.
  </action>
  <acceptance_criteria>
    - "Sync and async machines agree on mode, priority, stage, committed flag, state, history, timing, and retained/suppressed lifecycle work."
    - "Pre-commit and post-commit cancellation matrices are handshake-driven and preserve the original cancellation outcome."
    - "Failure observation occurs exactly once, ownership is always released, and a subsequent trigger succeeds."
    - "Async internal dispatch introduces no alternate observer family, compensation, shielding, or hidden sync/async bridge."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_transition_modes.py tests/test_async.py tests/test_transition_lifecycle.py tests/test_priority_selection.py -x -q -k "internal or external or cancellation or ownership or reuse or priority"</automated>
  </verify>
  <done>Async mode execution, cancellation, failure finalization, ownership cleanup, and reuse are semantically identical to the synchronous contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock structural, typing, pure/native, and full-suite parity</name>
  <reversibility rating="costly">The public stub and compiled carrier layouts are shared compatibility contracts; undoing their order/default semantics would require coordinated downstream changes.</reversibility>
  <files>src/fast_fsm/core.py, src/fast_fsm/core.pyi, tests/test_mypyc_guard.py, .specify/memory/spr-core-api.md</files>
  <read_first>
    - `.planning/phases/28-same-state-transition-modes/28-CONTEXT.md` D-01, D-08, D-11, D-13, D-16, D-17
    - `.planning/phases/28-same-state-transition-modes/28-RESEARCH.md` native matrix and Pitfall 7
    - `.planning/phases/28-same-state-transition-modes/28-PATTERNS.md` exact runtime/stub field-order and pure/native probe assignments
    - `tests/test_mypyc_guard.py` carrier AST tests, result/record field tests, exact scalar boundaries, selector complexity guards, and dual-mode semantic probe
    - `.github/copilot-instructions.md` mypyc build, slots, quality, and source-origin rules
    - `.specify/memory/spr-core-api.md` living construction/lifecycle/history contract
  </read_first>
  <behavior>
    - "Wave 0 structural RED: every carrier/result/record/runtime/stub field list has the exact trailing internal scalar and retains slots/frozen/default/comparison contracts."
    - "Exact-bool rejection, dual-mode singleton storage, selected-entry-only mode, and dispatch-complexity structure agree in pure and compiled execution."
    - "One semantic probe covers direct/builder authoring, sync/async internal/external lifecycle, result/history/timing, priority, failure/cancellation, clone, and source-origin assertions."
    - "After native proof, no core extension shadows `src/fast_fsm/core.py`, and the full pure-source suite remains green."
  </behavior>
  <action>
Add the missing `tests/test_mypyc_guard.py` Wave 0 assertions before final production adjustments: exact field/slot order for `TransitionEntry`, `_TransitionRequest`, `_PreparedTransition`, `_GraphTransition`, `TransitionResult`, and `TransitionRecord`; matching `core.pyi` signatures/defaults; exact-bool rejection before mutation; selected mode only through `_PreparedDispatch.entry`; and no sorting, unrelated topology scan, reflection, or per-dispatch collection allocation. Extend the narrow semantic probe so it asserts one canonical State can retain distinct external/internal entries and the same sync/async outcome in pure and compiled modes.

Resolve any mypyc-specific field annotations without weakening runtime exact validation or adding a second mode source. Keep the public result/history additions trailing and comparison-neutral, keep hot-path classes slotted, and preserve the direct singleton representation. Update `.specify/memory/spr-core-api.md` in the same commit with the settled per-transition authoring, canonical-self validation, lifecycle, commit/history/timing, failure/cancellation, and Phase 30 adapter boundary; do not pull tutorial, dictionary schema, diagrams, or installed-artifact claims forward from Phases 30 through 32.

Run blocking mypy, advisory ty, Ruff, slots policy, focused pure tests, a freshly compiled focused semantic probe, and then recoverably relocate the generated native shadow before `task pure-source-check`. Finish with the full sequential pure-source suite. If any gate fails, preserve the original failure, restore source origin, repair, and rerun; never leave a generated `core*.so`/`core*.pyd` in `src/fast_fsm`.
  </action>
  <acceptance_criteria>
    - "Runtime and stub fields/signatures have exact trailing defaults, slots/frozen/comparison behavior, and mypyc-compatible ordering."
    - "Structural tests enforce one predictable selected-mode branch and direct O(1)/local O(k) dispatch without unrelated work."
    - "Focused semantics pass under pure source and a fresh native build, then `task pure-source-check` proves the checkout is restored."
    - "Blocking typing, advisory typing, Ruff, slots policy, and the full sequential suite complete with no Phase 28 regression."
    - "The living core SPR records the public/runtime truth without expanding deferred adapter, diagnostic, tutorial, or installed-artifact scope."
  </acceptance_criteria>
  <verify>
    <automated>
task typecheck-mypy
task typecheck-ty
uv run ruff check src/fast_fsm/core.py src/fast_fsm/core.pyi tests/test_transition_modes.py tests/test_mypyc_guard.py tests/test_graph_invariants.py tests/test_hypothesis.py tests/test_builder.py tests/test_transition_lifecycle.py tests/test_transition_timing.py tests/test_priority_selection.py tests/test_async.py
uv run python tools/release_evidence.py slots-policy --json
task pure-source-check
uv run pytest tests/ -x -q
    </automated>
  </verify>
  <done>The transition-mode semantic spine is structurally locked, typed, slots-safe, pure/native equivalent, source-restored, documented in the living contract, and green under the full suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async task -> machine ownership | Cancellation can interrupt before or after commit and must not strand the machine owner/lock. |
| runtime Python -> mypyc extension | Native field coercion/layout must not weaken exact validation or change semantic truth. |
| selected entry -> callback/result/history | Mode must be observable only on library metadata, never injected into application payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | pure/native exact-bool boundary | high | mitigate | Object-typed compiled boundary, exact runtime validation, mutation fingerprints, and dual-origin tests. |
| T-28-02 | Tampering | compiled carrier publication | high | mitigate | Exact slot/field/order guards and regression of prepare-all/publish-once construction under native execution. |
| T-28-03 | Tampering / Information Disclosure | async callbacks/results | medium | mitigate | Keep mode in entry/result/history only, preserve args/kwargs unchanged, and use fixed bounded errors/traces. |
| T-28-04 | Denial of Service | cancellation and async ownership | high | mitigate | Single outer finalizer, bare re-raise, `finally` release, event-handshake cancellation tests, and successful reuse assertions. |
| T-28-05 | Denial of Service | compiled dispatch complexity | high | mitigate | AST/semantic guards require one direct singleton lookup or local candidate loop and reject scans, sorts, reflection, and per-dispatch allocation. |
</threat_model>

<verification>
- Run the focused async matrix after Task 1 and the structural/typing gate before compiling in Task 2.
- Execute the native probe only from a known pure-source starting point, restore the source origin even on failure, and prove restoration with `task pure-source-check`.
- The phase gate is the full sequential pure-source suite plus blocking mypy, advisory ty, Ruff, slots policy, and the focused fresh-native semantic oracle.
</verification>

<success_criteria>
- Synchronous and asynchronous machines produce matching success, failure, cancellation, timing, priority, history, and ownership outcomes for both modes.
- Exact-bool tampering, partial publication, callback-payload collision/exposure, cancellation leakage, and dispatch-complexity risks have active automated mitigations.
- Pure and compiled execution agree, and the repository ends with no native shadow in the source package.
</success_criteria>

## Source Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | User chooses external re-entry or internal logical commit with identical machine truth | 01-03 | COVERED | Tracer, sync expansion, async/native closure. |
| REQ | MODE-01 | Explicit True with omitted/False external default | 01, 03 | COVERED | Public/stub/native evidence. |
| REQ | MODE-02 | Canonical self-only internal registration and atomic rejection | 01, 03 | COVERED | Exact validation, property, native proof. |
| REQ | MODE-03 | Complete external lifecycle and timing reset | 01, 02 | COVERED | Tracer plus full order/timing matrix. |
| REQ | MODE-04 | Suppressed state lifecycle with retained transition behavior and real commit | 01, 02, 03 | COVERED | Sync/async lifecycle and failure matrices. |
| REQ | MODE-05 | Uninterrupted residency across internal events | 02, 03 | COVERED | Fake-clock history-off/on/repeat cases. |
| REQ | MODE-06 | Sync/async priority, failure, cancellation, and reuse parity | 02, 03 | COVERED | Selected-mode and ownership matrices. |
| RESEARCH | R-CARRIER | Immutable slotted scalar through entry/request/prepared/result/record/graph/clone/stub | 01, 03 | COVERED | Structural/native lock. |
| RESEARCH | R-ATOMIC | Exact canonical validation, idempotency, concurrency, and prepare-before-publish | 01 | COVERED | Deterministic atomicity evidence. |
| RESEARCH | R-LIFECYCLE | One post-selection branch with external compatibility and internal specialization | 01, 02, 03 | COVERED | Sync and async runners. |
| RESEARCH | R-TIMING | History event timestamp without entry-epoch reset | 02 | COVERED | Clock-call and residency matrix. |
| RESEARCH | R-SECURITY | Exact bool, topology, payload, ownership, and complexity mitigations | 01-03 | COVERED | Threat models and automated gates. |
| RESEARCH | R-TOOLING | uv-only commands, Beads tracking, no new dependency, source/native proof | 01, 03 | COVERED | Execution protocol and phase gates. |
| CONTEXT | D-01 | Per-entry exact keyword-only boolean, False default | 01, 03 | COVERED | Costly decision recorded. |
| CONTEXT | D-02 | Canonical self-only and final-source invariant | 01 | COVERED | Atomic normalization tests. |
| CONTEXT | D-03 | Mode participates in candidate identity | 01 | COVERED | Duplicate/conflict matrix. |
| CONTEXT | D-04 | Complete external lifecycle order | 01, 02 | COVERED | Tracer and ordered expansion. |
| CONTEXT | D-05 | Retained internal transition behavior | 01, 02, 03 | COVERED | Full sync/async suffix. |
| CONTEXT | D-06 | Skip every state lifecycle surface together | 01-03 | COVERED | Sentinel matrices. |
| CONTEXT | D-07 | Direct controls remain external-only | 02 | COVERED | Negative API/behavior tests. |
| CONTEXT | D-08 | Internal is a real logical commit with result/history mode | 01-03 | COVERED | Result/history/failure proof. |
| CONTEXT | D-09 | Preserve entry epoch; history-only event clock | 02 | COVERED | Clock-call assertions. |
| CONTEXT | D-10 | External resets residency; internal repeats do not | 02 | COVERED | after/within matrix. |
| CONTEXT | D-11 | No callback kwargs injection | 01-03 | COVERED | Payload equivalence assertions. |
| CONTEXT | D-12 | Pre/post-commit internal failure truth | 02 | COVERED | Stage matrix. |
| CONTEXT | D-13 | Sync/async parity | 03 | COVERED | Async behavior matrix. |
| CONTEXT | D-14 | Cancellation stage/commit truth | 03 | COVERED | Event-handshake cancellation. |
| CONTEXT | D-15 | Mode-neutral priority fallthrough | 01-03 | COVERED | Selector tests/guards. |
| CONTEXT | D-16 | Canonical scalar carrier scope | 01, 03 | COVERED | Primary surfaces and graph/clone continuity. |
| CONTEXT | D-17 | Leave adapter fan-out to canonical Phase 30 seam | 01, 03 | COVERED | Explicit boundary checks; no adapter-specific behavior. |

### Prohibition Recall

The adversarial prohibition pass produced no bespoke values, fairness, privacy, transparency, or safety prohibition for this in-process utility. Routine correctness items were converted into the seven automated edge matrices above. Exact-bool tampering, partial publication, callback information exposure, cancellation leakage, and algorithmic denial-of-service are canonical security concerns; they are breadcrumbed to the per-plan threat models and later `$gsd-secure-phase` verification rather than duplicated as `must_haves.prohibitions`. Therefore the descriptor-less flagged-unverified survivor list is empty, as recorded by `must_haves.prohibitions: []`.

<output>
Create `.planning/phases/28-same-state-transition-modes/28-03-SUMMARY.md` when done.
</output>
