---
phase: 28-same-state-transition-modes
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - src/fast_fsm/core.py
  - tests/test_transition_modes.py
  - tests/test_transition_lifecycle.py
  - tests/test_transition_timing.py
  - tests/test_priority_selection.py
autonomous: true
requirements:
  - MODE-03
  - MODE-04
  - MODE-05
  - MODE-06
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-04 and D-10, external self-transitions retain every established state/transition surface and reset the state-entry epoch."
    - "Per D-05, D-06, and D-08, internal transitions skip every state exit/entry surface together but retain transition-level work, one logical commit, results, tracing, optional history, and appropriate observers."
    - "Per D-09, history-disabled internal commits do not read the clock, history-enabled commits timestamp the event without changing the entry epoch, and repeated internal events never restart after/within residency windows."
    - "Per D-12 and D-15, failure/priority truth is stage-accurate and mode-neutral until one candidate is selected."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Complete synchronous external/internal lifecycle, commit, timing, priority, and failure semantics"
      contains: "internal"
    - path: "tests/test_transition_modes.py"
      provides: "Central lifecycle, timing, failure, payload, and observer oracle"
    - path: "tests/test_transition_lifecycle.py"
      provides: "Ordered stage/commit/failure regression evidence"
    - path: "tests/test_transition_timing.py"
      provides: "Deterministic entry-epoch and commit-clock evidence"
    - path: "tests/test_priority_selection.py"
      provides: "Mode-neutral candidate ordering and selected-mode evidence"
  key_links:
    - from: "_PreparedDispatch.entry.internal"
      to: "_execute_transition"
      via: "one branch after selection, never source/target inference"
      pattern: "entry\.internal"
    - from: "internal logical commit"
      to: "TransitionRecord and TransitionResult"
      via: "history-only event timestamp and committed mode metadata"
      pattern: "internal=True"
    - from: "priority selector"
      to: "selected lifecycle"
      via: "existing ascending priority order with no mode-based filtering"
      pattern: "priority"
  prohibitions: []
---

<objective>
Complete the synchronous lifecycle, timing, priority, and failure truth for both same-State transition modes.

Purpose: Expand the Plan 01 tracer into the full MODE-03 through MODE-05 matrices and the synchronous half of MODE-06 without disturbing direct controls or the deterministic selector.
Output: Complete external/re-entry and internal/logical-commit lifecycle order, deterministic residency semantics, retained-versus-suppressed hook edges, selected-mode priority behavior, and staged failure observation.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-same-state-transition-modes/28-CONTEXT.md
@.planning/phases/28-same-state-transition-modes/28-RESEARCH.md
@.planning/phases/28-same-state-transition-modes/28-PATTERNS.md
@.planning/phases/28-same-state-transition-modes/28-VALIDATION.md
@.planning/phases/28-same-state-transition-modes/28-01-SUMMARY.md
@src/fast_fsm/core.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the complete lifecycle and residency matrix</name>
  <files>src/fast_fsm/core.py, tests/test_transition_modes.py, tests/test_transition_lifecycle.py, tests/test_transition_timing.py</files>
  <read_first>
    - `.planning/phases/28-same-state-transition-modes/28-CONTEXT.md` D-04 through D-11
    - `.planning/phases/28-same-state-transition-modes/28-RESEARCH.md` lifecycle, failure, and timing matrices
    - `src/fast_fsm/core.py` `_execute_transition`, `_commit_transition`, timing eligibility, direct-control methods, callback/listener registries, and trace finalization
    - `tests/test_transition_lifecycle.py` exact ordered lifecycle and stage matrix
    - `tests/test_transition_timing.py` `FakeClock` and after/within boundary patterns
  </read_first>
  <behavior>
    - "External self-transition executes resolution, timing, guards, permission, before, all exit surfaces, commit/reset, all entry surfaces, declarative, trigger, and after surfaces in the existing order."
    - "Internal transition executes the retained prefix/suffix exactly once and none of the six synchronous state lifecycle families."
    - "History-off internal success performs no logical-commit clock read; history-on success records event time while preserving `_state_entered_at`."
    - "External self re-entry restarts after/within residency; repeated internal events do not extend either deadline."
    - "force_state, reset, and restore retain their existing external control lifecycle and expose no internal option."
  </behavior>
  <action>
Write the complete ordered lifecycle and deterministic timing cases before adjusting implementation. Use local sentinel callbacks that fail if any suppressed surface executes and a local `FakeClock` with call counts; do not use sleeps or import another test module's fixtures.

Keep the external runner and `_commit_transition()` as the compatibility path per D-04 and D-10. Complete the internal runner so it skips `State.on_exit`, registered sync source-exit callbacks, exit-state listeners, `State.on_enter`, registered sync destination-entry callbacks, and enter-state listeners as one bounded branch per D-06, while retaining all transition-level work per D-05. Ensure success is one real commit with same canonical endpoints and explicit result/history mode per D-08.

Implement the internal commit so history disabled makes no commit-time clock read and never changes `_state_entered_at`; when history is enabled, read once to timestamp the event record without making it a new entry epoch per D-09. Prove external self resets the epoch and repeated internal events leave `after=` and `within=` residency uninterrupted per D-10. Keep callback args/kwargs untouched per D-11. Add negative API/behavior tests proving direct controls do not accept or infer internal mode per D-07.
  </action>
  <acceptance_criteria>
    - "External and internal ordered traces exactly match D-04 through D-06, including every retained and suppressed edge."
    - "Internal result/history state a real commit and selected mode while canonical current-state identity remains unchanged."
    - "Clock call counts distinguish history-off internal, history-on internal, and external self-transition commits."
    - "Repeated internal events do not restart after/within eligibility, and direct controls retain external-only behavior."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_transition_modes.py tests/test_transition_lifecycle.py tests/test_transition_timing.py -x -q</automated>
  </verify>
  <done>The complete synchronous lifecycle and timing contract is observable and deterministic for both transition modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve selected-mode priority and staged failure truth</name>
  <files>src/fast_fsm/core.py, tests/test_transition_modes.py, tests/test_transition_lifecycle.py, tests/test_priority_selection.py</files>
  <read_first>
    - `.planning/phases/28-same-state-transition-modes/28-CONTEXT.md` D-12 and D-15
    - `.planning/phases/28-same-state-transition-modes/28-RESEARCH.md` failure matrix, mode-neutral priority selection, and callback disclosure threat
    - `src/fast_fsm/core.py` `_select_sync_candidate`, `_prepare_transition`, `_build_lifecycle_failure`, `_finalize_failure`, and sync trigger boundary
    - `tests/test_priority_selection.py` fallthrough/terminal candidate ordering tests
    - `tests/test_transition_lifecycle.py` pre/post-commit failure matrix and observer cardinality tests
  </read_first>
  <behavior>
    - "Lower-priority false eligibility falls through without lifecycle effects, and only the selected candidate's mode controls execution."
    - "Mode does not reorder candidates or turn a guard error into fallthrough."
    - "Internal before-listener and commit failures are uncommitted with no history or suffix; retained declarative/trigger/after failures are committed, mode-true, history-retaining, and suffix-stopping."
    - "Skipped state lifecycle surfaces cannot originate an internal failure, and every actual failure is finalized exactly once with bounded public text."
  </behavior>
  <action>
Add failing mixed-mode priority cases first: an ineligible lower-priority internal/external candidate must fall through normally, terminal guard/permission failures must abort, and the mode of the selected immutable entry alone must reach lifecycle execution per D-15. Keep direct singleton lookup O(1), group evaluation local O(k), ascending priority unchanged, and avoid sorting/copying/scanning unrelated topology.

Complete the sync failure matrix per D-12. A before-transition listener or internal logical-commit failure must remain `committed=False`, create no record, stop all later work, and preserve mode/priority/stage truth for the selected candidate. Failures in retained declarative handlers, trigger callbacks, or after-transition listeners must remain committed with one history record and `internal=True`, stop the remaining suffix at the established point, and pass through the single existing failure finalizer exactly once. Use fixed bounded error text and never expose caller objects or inject the scalar into callback kwargs.
  </action>
  <acceptance_criteria>
    - "Priority tests prove selection is mode-neutral and only the selected entry controls lifecycle mode."
    - "Every synchronous internal failure stage reports the correct committed flag, stage, priority, mode, state, history, suffix, and one observer pass."
    - "No skipped exit/entry surface can generate an internal-transition failure."
    - "Structural complexity remains direct O(1) for singleton dispatch and local O(k) for candidate groups."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_transition_modes.py tests/test_priority_selection.py tests/test_transition_lifecycle.py tests/test_graph_invariants.py -x -q -k "internal or external or priority or failure or lifecycle"</automated>
  </verify>
  <done>Synchronous priority selection and every retained failure edge preserve one authoritative selected mode and established commit/finalization truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected candidate -> lifecycle | The selected immutable mode and priority become authoritative execution metadata. |
| caller payload -> callback surfaces | Application args/kwargs cross retained hooks and must not collide with library metadata. |
| timing/history -> observable result | Clock reads, entry epoch, history timestamp, and commit truth must remain internally coherent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | selected mode scalar | high | mitigate | Regress exact-bool construction from Plan 01 and consume only `prepared.entry.internal`; never infer from source/target shape. |
| T-28-02 | Tampering | commit/history publication | high | mitigate | Construct/read before append, set committed only after logical commit, and test failure paths for zero partial history/state publication. |
| T-28-03 | Tampering / Information Disclosure | callbacks and failure results | medium | mitigate | Preserve args/kwargs unchanged, keep mode out of payload, use bounded public errors, and assert exactly-once failure observation. |
| T-28-04 | Denial of Service | lifecycle ownership | high | mitigate | Keep one public ownership/finalization boundary; no nested dispatch ownership or alternate observer family. Async cleanup is proven in Plan 03. |
| T-28-05 | Denial of Service | priority dispatch | high | mitigate | Preserve one singleton lookup or local candidate loop; structural tests reject sorting, unrelated scans, reflection, and per-candidate trace allocation. |
</threat_model>

<verification>
- After each task, run the central oracle plus only the directly affected lifecycle/timing/priority suites.
- At wave close, run `uv run pytest tests/test_transition_modes.py tests/test_transition_lifecycle.py tests/test_transition_timing.py tests/test_priority_selection.py tests/test_graph_invariants.py tests/test_builder.py -x -q`.
- Run `task typecheck-mypy`, record `task typecheck-ty`, run Ruff on changed files, and run the slots-policy audit.
</verification>

<success_criteria>
- External self-transition is a complete re-entry and internal transition is a complete logical commit with exactly the locked retained/suppressed surfaces.
- Residency timing and clock/history behavior are deterministic under disabled/enabled history and repeated events.
- Priority, failure, and observer behavior remain stage-accurate and mode-neutral until selection.
</success_criteria>

<output>
Create `.planning/phases/28-same-state-transition-modes/28-02-SUMMARY.md` when done.
</output>
