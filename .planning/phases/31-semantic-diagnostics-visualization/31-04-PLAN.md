---
phase: 31-semantic-diagnostics-visualization
plan: 04
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/fast_fsm/core.py
  - tests/test_logging_config.py
  - docs/api/core.md
  - .specify/memory/spr-core-api.md
autonomous: true
requirements: [DIAG-01, DIAG-03]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DIAG-01/D-03/D-06: sync and async trace records expose bounded rejection code, selected mode/priority when known, and authoritative current-state finality."
    - "DIAG-03/D-06: disabled tracing does no new semantic work, default records remain metadata-only, and invalid redactor output produces a fixed safe fallback."
    - "DIAG-01/D-03: result/history selected facts and `is_terminated` remain the runtime authority without a copied finality cache."
  artifacts:
    - path: src/fast_fsm/core.py
      provides: matched sync/async trace finalization with bounded semantic metadata
    - path: tests/test_logging_config.py
      provides: trace matrix including confidentiality and disabled-path proof
    - path: docs/api/core.md
      provides: trace field and current-finality interpretation
    - path: .specify/memory/spr-core-api.md
      provides: living runtime/trace contract
  key_links:
    - from: src/fast_fsm/core.py
      to: tests/test_logging_config.py
      via: both public trigger boundaries pass their owned attempt and machine reference to the shared guarded trace helper
    - from: TransitionResult
      to: _emit_fsm_trace
      via: selected priority/internal and validated rejection_code are projected only after TRACE enablement
---

<objective>
Expose semantic facts in the existing fixed TRACE record for both machine types without adding dispatch work or exposing application data.

Purpose: Logs should distinguish expected rejection, selected self-transition mode, and current completion safely.
Output: Matched sync/async trace fields, confidentiality tests, and focused core guidance.
</objective>

## Artifacts this phase produces

- Fixed `trace_rejection_code`, `trace_mode`, and `trace_current_final` fields alongside existing `trace_priority`.
- Sync and async trace tests for selected, unselected, committed, rejected, disabled, redacted, and fail-closed attempts.
- Public and living guidance on field meaning and confidentiality.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-semantic-diagnostics-visualization/31-CONTEXT.md
@.planning/phases/31-semantic-diagnostics-visualization/31-RESEARCH.md
@.planning/phases/31-semantic-diagnostics-visualization/31-PATTERNS.md
@.planning/phases/31-semantic-diagnostics-visualization/31-VALIDATION.md
@.planning/phases/31-semantic-diagnostics-visualization/31-01-SUMMARY.md

<interfaces>
`TransitionResult` already carries comparison-neutral `priority`, `internal`, and `rejection_code`; `TransitionRecord` carries selected `priority`/`internal`; `StateMachine.is_terminated` reads canonical current `State.final`. `_emit_fsm_trace()` returns immediately when TRACE is disabled and currently emits a fixed record with `trace_priority`; both `trigger()` and `trigger_async()` call it once after owned execution. `FSMTraceEvent` is the existing optional redactor input and `_validate_fsm_trace_output()` validates redactor values. Keep callbacks and `can_trigger*()` unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded semantic fields to matched sync and async TRACE finalizers</name>
  <reversibility rating="costly">LogRecord field names and confidentiality are observed by application log consumers.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_logging_config.py</files>
  <read_first>
    - `31-CONTEXT.md` D-03 and D-06; Phase 29 bounded rejection and Phase 28 selected mode contracts
    - `src/fast_fsm/core.py` `_emit_fsm_trace()`, `_validate_fsm_trace_output()`, both public trigger finalizers, `TransitionResult`, and `is_terminated`
    - `tests/test_logging_config.py` default metadata, priority, redactor, sync/async, and disabled-path tests
    - `tests/test_expected_rejection.py` guard/declarative/state-permission rejection cases and code validation
  </read_first>
  <behavior>
    - "For a selected sync or async internal self attempt, record mode `internal` and selected priority; for a committed external self, mode `external_self`; for an unselected attempt, mode and priority are null."
    - "Expected rejection emits only its prevalidated bounded code, selected mode/priority when the selector knows them, and `trace_current_final` from the canonical state at the owned boundary; no destination-final inference is made for an uncommitted attempt."
    - "A committed transition into an explicit final reports current-final true; a non-final sink reports false even without outgoing edges."
  </behavior>
  <action>
Start RED with paired sync and async LogRecord assertions covering the behavior matrix. Per D-03/D-06, let both public trigger sites pass the already materialized `TransitionResult` and `self` machine reference to `_emit_fsm_trace()`; derive new fields only after its existing disabled-TRACE early return so there is no added canonical-state lookup on the disabled path. Add fixed `trace_rejection_code` from `result.rejection_code`, `trace_mode` as `internal` for a selected internal edge, `external_self` only when selected source/destination are both known and equal, `external` for other selected external edges, and `None` if selection is unknown. A selected pre-commit external rejection with unknown destination reports `external`, without inventing a self-target. Read `trace_current_final` from the canonical current state's immutable `.final` after the TRACE guard. Retain `trace_priority`, existing stage/result, and current redactor input contract. Rejection is known only through the validated result code; do not stringify exception messages, trigger text, callback objects, payload, or destination state into default records. Keep sync and async call sites symmetrical and leave direct singleton selection, lifecycle, result/history storage, and `can_trigger*()` untouched.
  </action>
  <verify><automated>uv run pytest tests/test_logging_config.py tests/test_expected_rejection.py -x -q -k "trace or rejection"</automated></verify>
  <acceptance_criteria>
    - "DIAG-01/D-03/D-06: both trigger variants emit selected mode, bounded rejection code, priority, and current finality with clear unknown values."
    - "DIAG-03/D-06: disabled TRACE still returns before new semantic projection."
    - "No result, history record, or public snapshot acquires a cached final flag."
  </acceptance_criteria>
  <done>Both trigger paths emit one safe semantic TRACE record with known/unknown facts represented accurately.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove trace confidentiality and redactor failure behavior, then document fields</name>
  <reversibility rating="costly">Changing the fixed redaction-failure record or default field policy would break log-consumer assumptions.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_logging_config.py, docs/api/core.md, .specify/memory/spr-core-api.md</files>
  <read_first>
    - `31-VALIDATION.md` matrix 6 and T-31-04
    - `src/fast_fsm/core.py` `_trace_configuration_active()`, `_emit_fsm_trace()` fallback, and `_validate_fsm_trace_output()`
    - `tests/test_logging_config.py` hostile payload, redactor, logger hierarchy, and async cancellation tests
    - `docs/api/core.md` results/history/rejection/trace reference
    - `.specify/memory/spr-core-api.md` current trace and termination bullets
  </read_first>
  <behavior>
    - "Default trace and invalid-redactor fallback contain no raw payload, trigger, exception message, callback object, or unvalidated redactor value; the fallback has the fixed new keys with null semantic values."
    - "Guard false, expected rejection, unexpected exception, post-commit failure, and async cancellation are classified without inventing selected or destination facts."
    - "Disabled trace emits no record and does not read the current final bit or invoke the redactor."
  </behavior>
  <action>
Extend exact LogRecord key-set and hostile-secret tests across sync/async paths, including a rejection raised by each approved eligibility seam, ordinary false, unexpected failure, post-commit failure, and redactor success/failure. Keep the existing validated bounded rejection code only and add the new keys with `None` values to the fixed redaction-failure fallback; preserve the existing `FSMTraceEvent` redactor API and output validation (D-06). Assert the disabled path skips redactor invocation and any new finality read through a targeted probe or structural guard. Document each new field's type and when it is null in `docs/api/core.md` and the living SPR; distinguish `trace_current_final` from any destination claim on pre-commit rejection. Retain comparison-neutral result/history facts and O(1) current-state termination (D-03).
  </action>
  <verify><automated>uv run pytest tests/test_logging_config.py tests/test_expected_rejection.py -x -q</automated></verify>
  <acceptance_criteria>
    - "DIAG-03/D-06: default, redacted, fallback, and disabled records obey metadata-only and fail-closed rules."
    - "DIAG-01/D-03: trace finality names the authoritative current state and never stores it in result/history."
    - "Public reference and SPR define field meanings, unknown values, and the redaction-failure shape."
  </acceptance_criteria>
  <done>Trace semantics are confidential, bounded, matched across sync/async, and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime attempt → application logger | A rejected attempt may carry sensitive payload, trigger, or exception objects. |
| Optional application redactor → LogRecord | User-supplied redactor output is untrusted and may fail or return unsafe values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-04 | Information disclosure | default TRACE | high | mitigate | Project only fixed scalar fields after TRACE enablement; test hostile payload and exception absence. |
| T-31-07 | Information disclosure | redactor fallback | medium | mitigate | Preserve output validator and replace failed redaction with fixed null-semantic fallback; exact key-set tests. |
| T-31-08 | Denial of service | disabled TRACE path | low | accept | Existing early return prevents new projection or redactor calls; structural/behavioral tests enforce it. |
</threat_model>

<verification>
Run focused logging and rejection tests after each task. Plan 31-05 performs the single strict docs build and shared pure/native/full-suite evidence.
</verification>

<success_criteria>
Trace records communicate code, mode, priority, and known current finality without leaking application data or adding disabled-path work.
</success_criteria>

<output>
Create `.planning/phases/31-semantic-diagnostics-visualization/31-04-SUMMARY.md` when done.
</output>
