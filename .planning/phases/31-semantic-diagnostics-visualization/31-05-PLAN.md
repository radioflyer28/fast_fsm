---
phase: 31-semantic-diagnostics-visualization
plan: 05
type: execute
wave: 3
depends_on: [31-02, 31-03, 31-04]
files_modified:
  - tests/test_mypyc_guard.py
  - tests/test_final_states.py
  - tests/test_transition_modes.py
  - tests/test_expected_rejection.py
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DIAG-01/D-03: results/history retain their selected mode/rejection facts while `is_terminated` follows the canonical current State across commit, restore, and direct control."
    - "DIAG-01/DIAG-02/DIAG-03: the same final/sink/mode/rejection diagnostic graph is truthful across validation, JSON, diagrams, and sync/async trace."
    - "D-04/D-06/D-09: structural and runtime tests prove one cold capture, a disabled TRACE fast return, escaped bounded output, and no new singleton dispatch work."
    - "The focused semantic oracle passes from asserted pure source and a fresh native extension; native shadows are recoverably relocated and pure source is reasserted before the full sequential suite."
  artifacts:
    - path: tests/test_mypyc_guard.py
      provides: snapshot/stub, trace, hot-path, and dual-origin semantic structural oracle
    - path: tests/test_final_states.py
      provides: authoritative termination and initial-only final assertions
    - path: tests/test_transition_modes.py
      provides: result/history self-mode continuity assertions
    - path: tests/test_expected_rejection.py
      provides: rejection result/trace classification boundary assertions
  key_links:
    - from: tests/test_mypyc_guard.py
      to: src/fast_fsm/core.py
      via: runtime/stub scalar shape and no diagnostic import/scan in singleton dispatch
    - from: focused semantic pytest modules
      to: exact pure and native module origins
      via: identical tests under asserted origin, then pure-source restoration
---

<objective>
Prove Phase 31 semantics and safeguards as one coherent contract in pure and compiled execution, then close the ordinary quality gates.

Purpose: Public observations must agree across runtime, cold diagnostics, both diagrams, and both artifact modes.
Output: Cross-surface oracle, source-origin evidence, strict quality checks, and full sequential regression result.
</objective>

## Artifacts this phase produces

- Structural and behavioral evidence that result/history/current-finality semantics agree with cold diagnostic projections.
- One focused semantic matrix exercised from pure source and freshly compiled `core` origins.
- Recorded Ruff, mypy, advisory ty, slots, strict docs/doctests, native restoration, and full-suite outcomes.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-semantic-diagnostics-visualization/31-CONTEXT.md
@.planning/phases/31-semantic-diagnostics-visualization/31-RESEARCH.md
@.planning/phases/31-semantic-diagnostics-visualization/31-VALIDATION.md
@.planning/phases/31-semantic-diagnostics-visualization/31-01-SUMMARY.md
@.planning/phases/31-semantic-diagnostics-visualization/31-02-SUMMARY.md
@.planning/phases/31-semantic-diagnostics-visualization/31-03-SUMMARY.md
@.planning/phases/31-semantic-diagnostics-visualization/31-04-SUMMARY.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-06-PLAN.md

<interfaces>
`tests/test_mypyc_guard.py` has existing frozen-carrier, trace early-return, hot-path, and native-origin helpers. `tests/test_final_states.py`, `tests/test_transition_modes.py`, and `tests/test_expected_rejection.py` own the settled Phase 27–29 result/history/termination/rejection behavior. Plans 31-01..04 have already built the scalar graph, validator/JSON, both diagrams, and trace. `task pure-source-check` is the non-destructive source-origin preflight; `task build-check` creates a fresh native core shadow. Follow Plan 30-06's exact-source and recoverable-shadow discipline for this phase.
</interfaces>
</context>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|--------|------|----------|--------|
| GOAL | Consistent semantic inspection without confusing completion with graph shape or weakening output safeguards | 31-01..05 | COVERED |
| REQ | DIAG-01 results, history, validation, tracing, JSON | 31-01, 31-02, 31-04, 31-05 | COVERED |
| REQ | DIAG-02 Mermaid and PlantUML final/mode/sink distinctions | 31-03, 31-05 | COVERED |
| REQ | DIAG-03 deterministic, bounded, log-safe diagnostics | 31-01..05 | COVERED |
| RESEARCH | One scalar snapshot/diagnostic graph and unchanged dense adjacency compatibility | 31-01, 31-05 | COVERED |
| RESEARCH | Final-aware validator warnings, scores, and initial-only final | 31-02, 31-05 | COVERED |
| RESEARCH | Both diagrams, hostile text, and exact result/work budgets | 31-03, 31-05 | COVERED |
| RESEARCH | Matched sync/async trace, confidentiality, disabled fast return | 31-04, 31-05 | COVERED |
| RESEARCH | Pure/native origin parity and one final sequential gate | 31-05 | COVERED |
| CONTEXT | D-01 explicit final only; non-final sink distinction | 31-01, 31-02, 31-03 | COVERED |
| CONTEXT | D-02 topology legacy plus separate facts and fair quality | 31-01, 31-02 | COVERED |
| CONTEXT | D-03 current-state termination authority; no result/history final cache | 31-01, 31-04, 31-05 | COVERED |
| CONTEXT | D-04 one captured scalar graph | 31-01..03, 31-05 | COVERED |
| CONTEXT | D-05 additive deterministic JSON mode/finality | 31-01, 31-03 | COVERED |
| CONTEXT | D-06 bounded metadata-only rejection trace | 31-04, 31-05 | COVERED |
| CONTEXT | D-07 explicit-final markers only | 31-03 | COVERED |
| CONTEXT | D-08 self-mode labels, guard/priority, escaping | 31-03 | COVERED |
| CONTEXT | D-09 reserve before output; bounded exception | 31-01..03, 31-05 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock cross-surface result/history and structural invariants</name>
  <files>tests/test_mypyc_guard.py, tests/test_final_states.py, tests/test_transition_modes.py, tests/test_expected_rejection.py</files>
  <read_first>
    - `31-CONTEXT.md` D-01 through D-09 and deferred Phase 32 boundary
    - `31-VALIDATION.md` all seven matrices and T-31-01 through T-31-04
    - `tests/test_mypyc_guard.py` existing frozen snapshot, trace guard, and singleton dispatch AST assertions
    - `tests/test_final_states.py` initial-only final, restore, reset, direct-control tests
    - `tests/test_transition_modes.py` internal/external result/history tests
    - `tests/test_expected_rejection.py` pre-commit rejection, false guard, and unexpected failure cases
    - `src/fast_fsm/core.py` authoritative `is_terminated`, result/history carriers, snapshot capture, trigger finalizers
  </read_first>
  <behavior>
    - "Initial-only final, ordinary final destination, and non-final sink agree across `is_terminated`, result/history, validation, JSON, and both diagrams without a copied result/history final bit."
    - "Internal self and external self results/history retain selected mode; expected rejection carries bounded code and selected facts, creates no history row, and differs from false guard/unexpected failure."
    - "No production singleton selector or trigger gains a diagnostic import, topology traversal, sort, reflection, or per-attempt graph allocation; disabled TRACE returns before new field projection."
  </behavior>
  <action>
Extend the existing Phase 27–29 semantic tests into a shared outcome matrix with initial-only final, reachable final, reachable/unreachable non-final sink, internal and external self, non-self external, selected expected rejection, ordinary false, unexpected failure, and sync/async where applicable. Assert Phase 31 public output from the same constructed machines rather than recreating independent expected state lists (DIAG-01/DIAG-02). Extend the structural oracle to match `_GraphSnapshot.state_finals` with `core.pyi`, `_DiagnosticEdge.internal`, one capture per cold operation, both trigger finalizers, and no new dispatch-path diagnostic work (D-03/D-04/D-06). Keep expected rejection confined to approved pre-commit eligibility seams and preserve result/history selected fields. Run the focused command immediately after edits; any failure is fixed before the heavyweight native gate.
  </action>
  <verify><automated>uv run pytest tests/test_mypyc_guard.py tests/test_final_states.py tests/test_transition_modes.py tests/test_expected_rejection.py -x -q -k "final or mode or rejection or diagnostic or trace or hot_path or snapshot"</automated></verify>
  <acceptance_criteria>
    - "DIAG-01/D-01/D-03: runtime current-state truth and selected result/history facts agree with all applicable diagnostic surfaces."
    - "DIAG-02/D-07/D-08: the common graph yields matching explicit-final and self-mode diagram facts."
    - "DIAG-03/D-04/D-06: structural checks protect one capture, early trace return, and the direct singleton hot path."
  </acceptance_criteria>
  <done>One focused oracle links prior runtime semantics to Phase 31 diagnostics without weakening compiled-path structure.</done>
</task>

<task type="auto">
  <name>Task 2: Close pure/native parity, source restoration, docs, typing, slots, and full suite</name>
  <files>tests/test_mypyc_guard.py</files>
  <read_first>
    - `31-VALIDATION.md` before-verify gate and T-31-01 through T-31-04
    - `30-06-PLAN.md` Task 3 native inventory, recoverable backup, and source-restoration protocol
    - `Taskfile.yml` `pure-source-check`, `build-check`, `docs-check`, `docs-test`, and typecheck tasks
    - `tools/release_evidence.py` verify-source and slots-policy commands
    - `tests/test_mypyc_guard.py` focused structural/origin oracle after Task 1
  </read_first>
  <action>
After Task 1, run non-mutating Ruff checks on all Phase 31 Python files, blocking `task typecheck-mypy`, visible advisory `task typecheck-ty`, slots-policy, strict docs/doctests, and the focused Phase 31 suite under an asserted pure `src/fast_fsm/core.py` origin. Build a fresh native extension, assert its exact extension origin, run the identical focused semantic suite there, then move only exact `src/fast_fsm/core*.so`/`core*.pyd` shadows into a unique printed temporary backup with an EXIT trap covering failures. Reassert pure source after restoration and run the full sequential suite once. Preserve unrelated user files and never clean the broad workspace. Record the exact commands, origins, and outcomes in `31-05-SUMMARY.md`; repair any actual gate failure before claiming closure. This is Phase 31 conformance evidence, not Phase 32 installed-wheel benchmarking, release baseline writing, or tutorial work.
  </action>
  <verify>
    <automated>
set -eu
uv run ruff format --check src/fast_fsm/core.py src/fast_fsm/core.pyi src/fast_fsm/_diagnostics.py src/fast_fsm/validation.py src/fast_fsm/visualization.py tests/test_diagnostic_contracts.py tests/test_validation.py tests/test_visualization.py tests/test_output_safety.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_final_states.py tests/test_transition_modes.py tests/test_expected_rejection.py
uv run ruff check src/fast_fsm/core.py src/fast_fsm/core.pyi src/fast_fsm/_diagnostics.py src/fast_fsm/validation.py src/fast_fsm/visualization.py tests/test_diagnostic_contracts.py tests/test_validation.py tests/test_visualization.py tests/test_output_safety.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_final_states.py tests/test_transition_modes.py tests/test_expected_rejection.py
task typecheck-mypy
task typecheck-ty || true
uv run python tools/release_evidence.py slots-policy --json
task docs-check
task docs-test
task pure-source-check
uv run python -c 'from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); expected = Path("src/fast_fsm/core.py").resolve(); assert origin == expected, (origin, expected); print(origin)'
FAST_FSM_BUILD_MODE=pure uv run pytest tests/test_diagnostic_contracts.py tests/test_validation.py tests/test_visualization.py tests/test_output_safety.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_final_states.py tests/test_transition_modes.py tests/test_expected_rejection.py -x -q
native_backup="$(mktemp -d "${TMPDIR:-/tmp}/fast-fsm-phase31-native-backup.XXXXXX")"
shadow_report="$(mktemp "${TMPDIR:-/tmp}/fast-fsm-phase31-shadow-report.XXXXXX")"
shadow_manifest="${shadow_report}.paths"
shadow_stdout="${shadow_report}.stdout"
inventory_native() { : &gt;"$shadow_manifest"; if uv run python tools/release_evidence.py verify-source --json &gt;"$shadow_stdout" 2&gt;"$shadow_report"; then return 0; fi; shadow_count=0; while IFS= read -r report_line; do case "$report_line" in "  - "*) shadow_path="${report_line#  - }" ;; *) continue ;; esac; case "$shadow_path" in "$PWD"/src/fast_fsm/core*.so|"$PWD"/src/fast_fsm/core*.pyd) ;; *) echo "refusing untrusted shadow path: $shadow_path" &gt;&amp;2; return 1 ;; esac; test -f "$shadow_path" || return 1; printf '%s\n' "$shadow_path" &gt;&gt;"$shadow_manifest"; shadow_count=$((shadow_count + 1)); done &lt;"$shadow_report"; test "$shadow_count" -gt 0; }
restore_native() { if [ ! -s "$shadow_manifest" ]; then inventory_native; fi; while IFS= read -r shadow_path; do test -f "$shadow_path" || continue; mv "$shadow_path" "$native_backup/"; done &lt;"$shadow_manifest"; echo "native shadows retained at $native_backup"; task pure-source-check; uv run python -c 'from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); expected = Path("src/fast_fsm/core.py").resolve(); assert origin == expected, (origin, expected); print(origin)'; }
restore_on_exit() { native_status=$?; trap - EXIT; if ! restore_native; then native_status=1; fi; exit "$native_status"; }
trap restore_on_exit EXIT
task build-check
inventory_native
test -s "$shadow_manifest"
uv run python -c 'from importlib.machinery import EXTENSION_SUFFIXES; from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); expected_dir = Path("src/fast_fsm").resolve(); assert origin.parent == expected_dir and any(str(origin).endswith(s) for s in EXTENSION_SUFFIXES), origin; print(origin)'
FAST_FSM_BUILD_MODE=compiled uv run pytest tests/test_diagnostic_contracts.py tests/test_validation.py tests/test_visualization.py tests/test_output_safety.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_final_states.py tests/test_transition_modes.py tests/test_expected_rejection.py -x -q
trap - EXIT
restore_native
FAST_FSM_BUILD_MODE=pure uv run pytest tests/ -x -q
    </automated>
  </verify>
  <acceptance_criteria>
    - "DIAG-01/DIAG-02/DIAG-03: identical focused semantic tests pass with asserted pure and fresh native core origins."
    - "D-04/D-06/D-09: source structure, slots, escaping, confidentiality, and budget tests pass in both origins."
    - "Only exact native core shadows are moved to a recoverable printed backup; pure `core.py` origin is reasserted on success or failure."
    - "Ruff, blocking mypy, visible advisory ty, strict docs/doctests, slots policy, and one full sequential suite complete with outcomes recorded."
  </acceptance_criteria>
  <done>Phase 31 has focused pure/native semantic proof, restored pure source, and a passing full quality gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime and captured diagnostics → conformance evidence | Tests must compare the same semantic graph and exact module origins, not stale local shadows. |
| Generated native artifacts → source checkout | Native extension shadows can change import behavior until recoverably relocated. |
| Caller text and attempt data → output/logs | The shared matrix must prove escape, budget, and metadata-only controls across artifact modes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | cross-surface oracle | medium | mitigate | Compare final/mode truth from one captured graph against result/history/current-state cases in both origins. |
| T-31-02 | Tampering | diagram evidence | medium | mitigate | Reuse hostile-text and opaque-ID tests in the identical pure/native focused suite. |
| T-31-03 | Denial of service | diagnostic limits | medium | mitigate | Reuse exact/one-less work/result tests for JSON and both renderers under both origins. |
| T-31-04 | Information disclosure | trace evidence | high | mitigate | Reuse hostile payload, bounded-code, and invalid-redactor tests for sync and async native/pure paths. |
| T-31-09 | Tampering | native-origin proof | medium | mitigate | Assert exact source and extension origins and recoverably move only path-constrained core shadows before the final suite. |
</threat_model>

<verification>
Task 1 runs a focused structural/semantic matrix. Task 2 runs the same complete focused modules in pure and fresh native modes, restores source, then runs the full sequential suite and all relevant quality gates.
</verification>

<success_criteria>
Every DIAG requirement and D-01 through D-09 holds across runtime observations and bounded cold output in both pure and compiled execution, with source restored and all Phase 31 quality gates complete.
</success_criteria>

<output>
Create `.planning/phases/31-semantic-diagnostics-visualization/31-05-SUMMARY.md` when done.
</output>
