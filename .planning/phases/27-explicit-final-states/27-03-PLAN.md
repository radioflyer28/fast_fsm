---
phase: 27-explicit-final-states
plan: 03
type: execute
wave: 3
depends_on:
  - 27-02
files_modified:
  - src/fast_fsm/core.py
  - tests/test_final_states.py
  - tests/test_graph_invariants.py
  - tests/test_transition_lifecycle.py
  - tests/test_async.py
  - tests/test_mypyc_guard.py
  - .specify/memory/spr-core-api.md
autonomous: true
requirements:
  - FINAL-03
  - FINAL-05
  - FINAL-06
estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "FINAL-05 / D-08: The current State becomes final at the existing commit boundary; destination entry, listener, callback, observer, exception, or post-commit async cancellation cannot roll back termination truth."
    - "FINAL-06 / D-10: Reset, force_state, and snapshot-v1 restore derive termination solely from the resulting canonical current State, in either direction."
    - "FINAL-06 / D-11: Clone preserves exact canonical State identities and their immutable final markers while rebuilding independent transition topology and resetting to the clone's initial State."
    - "FINAL-06 / D-12: to_dict emits deterministic sorted final_states metadata; legacy omission defaults all states non-final; malformed, duplicate, excessive, or unknown final names fail before a candidate escapes."
    - "FINAL-03 / D-04 / D-05: from_dict constructs marked canonical States before submitting all transition rows once, so an outgoing final-source row fails atomically with its exact row context."
    - "Sync/async, interpreted/native, and pure-source behavior agree, and generated native shadows are removed before final source proof."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Deterministic final_states dictionary persistence and derived lifecycle/control truth"
      contains: "final_states"
    - path: "tests/test_final_states.py"
      provides: "Control, clone, persistence, legacy, lifecycle, and async contract matrix"
    - path: "tests/test_transition_lifecycle.py"
      provides: "Post-commit failure and cancellation termination proof"
    - path: "tests/test_graph_invariants.py"
      provides: "Exact additive schema and atomic dictionary rejection proof"
    - path: ".specify/memory/spr-core-api.md"
      provides: "Complete living Phase 27 contract"
  key_links:
    - from: "StateMachine._commit_transition"
      to: "StateMachine.is_terminated"
      via: "authoritative `_current_state = to_state` assignment before post-commit work"
      pattern: "_current_state = to_state"
    - from: "reset / force_state / restore"
      to: "StateMachine._force_state_owned"
      via: "shared control commit followed by derived current-State truth"
      pattern: "_force_state_owned"
    - from: "StateMachine.to_dict"
      to: "StateMachine.from_dict"
      via: "sorted JSON-native final_states names with omission defaulting to an empty set"
      pattern: "final_states"
    - from: "StateMachine.from_dict"
      to: "StateMachine._apply_transition_requests_owned"
      via: "marked canonical State objects plus one contextual request tuple"
      pattern: "error_contexts"
  flagged_assumptions:
    - id: FINAL-05
      classification: unclassified
      status: unresolved
      statement: "The specless edge probe did not classify post-commit termination; this plan uses locked D-08 and the existing lifecycle commit stage as the complete truth boundary."
    - id: FINAL-06
      classification: unclassified
      status: unresolved
      statement: "The specless edge probe did not classify control/persistence continuity; this plan follows locked D-10 through D-12 and keeps snapshot v1 unchanged."
  prohibitions: []
---

<objective>
Carry explicit finality through lifecycle failure boundaries, direct-control operations, clone reconstruction, and deterministic backward-compatible dictionary persistence, then prove pure/native parity.

Purpose: Make termination dependable after every state-changing operation without adding a second mutable truth source or changing snapshot format.
Output: Additive `final_states` persistence, lifecycle/control/clone parity tests, complete SPR guidance, and full interpreted/native/pure-source evidence.
</objective>

## Artifacts this phase produces

- Public `State.final: bool` getter backed by private slotted field `State._final`.
- Public `StateMachine.is_terminated: bool` inherited unchanged by `AsyncStateMachine`.
- Additive serialized dictionary field `final_states` containing sorted unique final-state names.
- New `tests/test_final_states.py` behavioral oracle.
- Updated `.specify/memory/spr-core-api.md` final-state, construction, lifecycle, control, clone, and persistence contract.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-explicit-final-states/27-CONTEXT.md
@.planning/phases/27-explicit-final-states/27-RESEARCH.md
@.planning/phases/27-explicit-final-states/27-PATTERNS.md
@.planning/phases/27-explicit-final-states/27-VALIDATION.md
@.planning/phases/26-canonical-construction-evidence-contract/26-03-SUMMARY.md
@.planning/phases/27-explicit-final-states/27-01-SUMMARY.md
@.planning/phases/27-explicit-final-states/27-02-SUMMARY.md

<interfaces>
Existing lifecycle/control contracts:
- `_commit_transition(old_state, to_state, trigger, ...)` writes history, `_current_state`, and entry time before destination-side work.
- Sync and async lifecycle runners record `committed=True` after that seam; async cancellation preserves the reached stage and committed destination.
- `reset()` and `restore()` call `_force_state_owned()`; snapshot remains exactly state name plus version 1.
- `_clone_owned()` reuses canonical State objects, reconstructs transition entries through the canonical request transaction, and resets current state to initial.

Persistence contract to extend:
- `to_dict()` returns deterministic JSON-native `name`, `initial`, `states`, and `transitions` collections.
- `from_dict()` parses all rows, discovers the complete state-name set, constructs a private candidate through `cls`, and submits requests once with row-specific `error_contexts`.
- Add `final_states` between `states` and `transitions` in canonical output; omitted input means no final names.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve finality through control, clone, and strict additive dictionary persistence</name>
  <reversibility rating="costly">D-12 defines additive serialized field semantics consumed by Phase 30 and downstream persisted configurations.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_final_states.py, tests/test_graph_invariants.py, tests/test_async.py, .specify/memory/spr-core-api.md</files>
  <read_first>
    - `src/fast_fsm/core.py:1264-1583` — current dictionary parsing, candidate construction, contextual request publication, and deterministic output.
    - `src/fast_fsm/core.py:3307-3444` — reset, snapshot-v1 restore, and clone identity/independent-topology behavior.
    - `src/fast_fsm/core.py:4332-4347` — async clone callback extension over the base clone.
    - `tests/test_graph_invariants.py:203-300,395-422,578-610` — clone identity, exact schema, graph version, and transition serialization patterns.
    - `.planning/phases/27-explicit-final-states/27-CONTEXT.md` decisions D-10 through D-12.
  </read_first>
  <behavior>
    - Resetting to a final initial State reports terminated even after moving away with force_state; reset to a non-final initial State clears termination after force/restore to final.
    - Snapshot output stays byte-shape compatible with version 1; restoring into final and non-final canonical states immediately reflects their markers.
    - A clone reuses each exact State object/final marker, owns independent transition tables/entries, and reports finality of its reset initial State rather than the source machine's current State.
    - `to_dict()` always emits sorted unique `final_states`, including an empty list, without serializing State objects or inferred sinks.
    - Omitted `final_states` in legacy input constructs only non-final States.
    - Input requires an exact list of non-empty strings, rejects duplicates, rejects length greater than the discovered state-name count before item iteration work, and rejects names outside that discovered set.
    - A valid final target round-trips; an outgoing final-source transition fails the whole from_dict candidate with the exact transition row context.
    - `AsyncStateMachine.from_dict()` and clone retain their machine type and identical final semantics.
  </behavior>
  <action>Write control, clone, and dictionary tests before modifying persistence. Per D-10, leave reset/force/restore implementations and snapshot v1 unchanged; assert termination is derived after each control commit. Per D-11, preserve the exact shared State objects and independently reconstructed clone topology, testing both a final initial clone and a source currently final with a non-final initial clone. Per D-12, make `to_dict()` always emit a `final_states` list sorted by state name, based only on explicit State markers. In `from_dict()`, treat omission as an empty list; require the field itself to be a list, reject a length greater than the complete discovered state-name set before scanning items, then in one linear pass require non-empty strings, reject duplicates, and reject unknown names with fixed bounded field-level messages. Do not merge final names into state discovery. Construct canonical State objects with `final=name in final_names`, instantiate the candidate via `cls`, add remaining objects, and retain one contextual `_apply_transition_requests_owned()` call so D-04/D-05 final-source errors identify the exact row without publishing a candidate. Preserve conditions, priorities, timing, async class identity, deterministic key/row order, and all legacy defaults. Update exact schema tests and complete the SPR with unchanged snapshot v1, clone identity, control derivation, and `final_states` semantics.</action>
  <verify>
    <automated>uv run pytest tests/test_final_states.py tests/test_graph_invariants.py tests/test_async.py -x -q -k "reset or restore or force or clone or dict or legacy or final"
uv run ruff format src/fast_fsm/core.py tests/test_final_states.py tests/test_graph_invariants.py tests/test_async.py
uv run ruff check --fix src/fast_fsm/core.py tests/test_final_states.py tests/test_graph_invariants.py tests/test_async.py
uv run ruff check src/fast_fsm/core.py tests/test_final_states.py tests/test_graph_invariants.py tests/test_async.py
task typecheck-mypy
task typecheck-ty</automated>
  </verify>
  <acceptance_criteria>
    - Reset, force_state, and snapshot-v1 restore reflect only the resulting canonical State marker without a stored termination flag.
    - Clone preserves exact State identity/finality, independent topology, reset-to-initial semantics, and async machine type.
    - Canonical dictionaries always contain deterministic `final_states`; legacy omission defaults every reconstructed State to non-final.
    - Wrong field type, excessive count, empty/non-string item, duplicate, and unknown name fail before candidate publication with bounded messages.
    - A dictionary outgoing edge from a declared final State fails atomically with the correct transition index, while an incoming final target round-trips.
    - The living SPR records the complete Phase 27 contract in the same behavior commit.
  </acceptance_criteria>
  <done>Explicit finality survives every required control, clone, and dictionary round trip while malformed or contradictory persisted topology fails closed before publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove post-commit termination and close interpreted/native/pure-source validation</name>
  <files>tests/test_final_states.py, tests/test_transition_lifecycle.py, tests/test_async.py, tests/test_mypyc_guard.py</files>
  <read_first>
    - `src/fast_fsm/core.py:3483-3503,3671-3729` — shared commit and synchronous post-commit lifecycle ordering.
    - `src/fast_fsm/core.py:4447-4516` — asynchronous lifecycle commit and destination-side work.
    - `tests/test_transition_lifecycle.py:422-500,932-997` — tracer and sync failure-boundary matrix with committed-state assertions.
    - `tests/test_transition_lifecycle.py:1146-1256` — event-handshake async cancellation matrix; do not replace it with sleeps.
    - `tests/test_mypyc_guard.py:1790-2010` — Phase 26 native construction and adapter structure proof.
    - `.planning/phases/27-explicit-final-states/27-VALIDATION.md` — required phase gate order and pure/native restoration contract.
  </read_first>
  <behavior>
    - `is_terminated` becomes true at the same point `current_state` changes to the final destination and is observable inside destination entry work.
    - Destination State.on_enter failure, enter callback/listener failure, trigger callback failure, after-listener failure, and failure-observer failure retain final current-state identity and termination.
    - Sync failure results preserve the existing stage, cause, committed flag, history count, and exactly-once observer behavior.
    - Async cancellation before commit stays non-terminated; cancellation at destination-entry or later retains final current State, history, and termination while propagating native CancelledError once.
    - Compiled `core.py` accepts exact final flags, rejects outgoing final sources, round-trips final_states, and reports identical lifecycle/control truth.
    - After native checks, no generated extension shadows source and the full pure-source suite passes again.
  </behavior>
  <action>Extend the existing parameterized lifecycle tests rather than creating a parallel runner. Mark destination States final at every post-commit sync and async boundary, assert the existing result/cancellation fields plus current State identity, history count, and termination, and include an assertion from inside destination entry that the property is already true. Use the established asyncio.Event handshake to cancel deterministically before versus after commit; do not use timing sleeps, shielding, rollback, or compensation. Add native-sensitive behavior assertions to the central final-state oracle and source guards only where needed to prove slots/property shape. Run focused interpreted tests, formatting, Ruff, mypy, advisory ty, slots policy, lock check, then the complete sequential suite once. Run `task build-check`, assert the imported `fast_fsm.core` is a native extension, and run focused final-state, graph, lifecycle, async, builder, and Hypothesis tests against it. After native success, move only the exact generated `src/fast_fsm/core` extension paths reported as source shadows into a uniquely created temporary directory, run `task pure-source-check`, assert the import is `src/fast_fsm/core.py`, and rerun the full pure-source suite. Do not update release baseline or installed-artifact evidence; Phase 32 owns installed artifact proof. Verify the single `phase_bead_id` from Plan 27-01 remains claimed/in progress and carry it into `27-03-SUMMARY.md`; only the phase verifier may close it after any review/gap cycle succeeds.</action>
  <verify>
    <automated>uv run pytest tests/test_final_states.py tests/test_transition_lifecycle.py tests/test_graph_invariants.py tests/test_builder.py tests/test_async.py tests/test_hypothesis.py tests/test_mypyc_guard.py -x -q
uv run ruff check src/fast_fsm/core.py tests/test_final_states.py tests/test_transition_lifecycle.py tests/test_graph_invariants.py tests/test_builder.py tests/test_async.py tests/test_hypothesis.py tests/test_mypyc_guard.py
task typecheck-mypy
task typecheck-ty
uv run python tools/release_evidence.py slots-policy --json
uv lock --check
uv run pytest tests/ -x -q
task build-check
task pure-source-check</automated>
  </verify>
  <acceptance_criteria>
    - Every post-commit sync failure stage remains committed to the final State and reports terminated without changing existing result/observer semantics.
    - Async pre-commit cancellation remains non-final; destination-entry and later cancellation remain committed/final and propagate once.
    - Focused interpreted and compiled tests cover declaration, canonical rejection, persistence, controls, clone, and lifecycle truth.
    - Full sequential tests, Ruff, mypy, advisory ty, slots policy, and lock checks pass.
    - Generated native source shadows are moved only after exact path inspection; `task pure-source-check` and the final full source suite prove restoration.
    - The Phase 27 Beads item remains open through execution and is reserved for successful phase-level verification closure.
  </acceptance_criteria>
  <done>Phase 27 semantics are proven across post-commit exceptions/cancellation, sync/async runtimes, compiled core, and restored pure source with no deferred artifact claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller dictionary → private canonical machine candidate | Untrusted JSON-native names and transition rows become immutable State and topology metadata. |
| Commit boundary → post-commit user callbacks/observers | Authoritative current State crosses into potentially failing or cancelling user code. |
| Native build output → import resolution | Generated extension artifacts can shadow source and alter validation mode. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03 | Tampering | `from_dict` final-state metadata and topology | high | mitigate | Validate exact list/item shape, bound count by discovered states, reject duplicates/unknown names, construct marked canonical States privately, and publish transitions once through the canonical invariant. |
| T-27-10 | Denial of Service | `final_states` parsing | medium | mitigate | Reject excessive count before item scanning, use one set-backed linear pass, and avoid graph scans or repeated name searches. |
| T-27-11 | Information Disclosure | dictionary validation errors | medium | mitigate | Use fixed bounded field errors and established row indices; never echo arbitrary state names or exception payloads. |
| T-27-12 | Tampering | post-commit failure/cancellation | medium | mitigate | Keep current-State assignment authoritative, derive termination from it, and prove callbacks/observers cannot roll state back or replace cancellation semantics. |
| T-27-13 | Tampering | native source shadow | medium | mitigate | Assert compiled origin for native tests, inspect exact generated paths before moving them, then fail closed with pure-source origin verification and a final source suite. |
</threat_model>

<verification>
- `uv run pytest tests/test_final_states.py tests/test_graph_invariants.py tests/test_transition_lifecycle.py tests/test_builder.py tests/test_async.py tests/test_hypothesis.py tests/test_mypyc_guard.py -x -q`
- `uv run ruff check src/fast_fsm/core.py tests/test_final_states.py tests/test_graph_invariants.py tests/test_transition_lifecycle.py tests/test_builder.py tests/test_async.py tests/test_hypothesis.py tests/test_mypyc_guard.py`
- `task typecheck-mypy`
- `task typecheck-ty`
- `uv run python tools/release_evidence.py slots-policy --json`
- `uv lock --check`
- `uv run pytest tests/ -x -q`
- `task build-check`, focused compiled Phase 27 tests, exact shadow cleanup, `task pure-source-check`, and final `uv run pytest tests/ -x -q`
</verification>

<success_criteria>
- FINAL-05 termination remains true after every post-commit sync failure and async cancellation boundary.
- FINAL-06 reset, restore, clone, async, and current/legacy dictionary operations preserve explicit-only termination truth.
- FINAL-03 dictionary input rejects outgoing final-source rows atomically with exact row context.
- Pure source and compiled core share one semantic oracle and no native shadow remains at phase completion.
- The phase item remains open for the verifier and no installed-artifact, diagram, builder-deprecation, transition-mode, or expected-rejection work is claimed.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Represent intentional completion and rely on termination across construction, execution, and control | 27-01 through 27-03 | COVERED | Public tracer, canonical invariant, and continuity/validation slices cover the goal. |
| REQ | FINAL-01 | Immutable `final=True` declaration | 27-01 | COVERED | Exact keyword-only bool on all State construction surfaces. |
| REQ | FINAL-02 | O(1) termination query including initial-final | 27-01 | COVERED | Direct canonical current-State marker read with structural guard. |
| REQ | FINAL-03 | Atomic outgoing-final rejection on every construction path | 27-02, 27-03 | COVERED | Canonical transaction plus complete adapter and dictionary matrices. |
| REQ | FINAL-04 | Non-final sink differs from final completion | 27-01 | COVERED | Topology-identical state pair proves explicit-only semantics. |
| REQ | FINAL-05 | Post-commit failure retains termination | 27-03 | COVERED | Sync/async lifecycle stage and cancellation matrices. |
| REQ | FINAL-06 | Reset, restore, clone, and deserialization parity | 27-03 | COVERED | Control derivation, identity-preserving clone, current/legacy dictionary proof. |
| RESEARCH | — | Private State slot plus getter-only exact boolean | 27-01 | COVERED | Preserves slots, subclassing, and constructor compatibility. |
| RESEARCH | — | One canonical final-source check after registered endpoint resolution | 27-02 | COVERED | No adapter or dispatch duplication. |
| RESEARCH | — | Derived termination through existing commit/control seams | 27-01, 27-03 | COVERED | No machine latch or cache. |
| RESEARCH | — | Deterministic sorted `final_states` metadata with strict legacy input | 27-03 | COVERED | Additive output and omission defaults. |
| RESEARCH | — | Native and pure-source parity for core.py | 27-03 | COVERED | Build, compiled focused tests, exact cleanup, source proof. |
| CONTEXT | D-01 | `State(..., final=True)`, keyword-only exact bool, False default | 27-01 | COVERED | Costly public contract recorded. |
| CONTEXT | D-02 | Explicit intent, never sink inference | 27-01 | COVERED | Dedicated equal-topology test. |
| CONTEXT | D-03 | Read-only property; no subclass or registry | 27-01 | COVERED | Private slot plus getter only. |
| CONTEXT | D-04 | No outgoing edge from final through any path | 27-02, 27-03 | COVERED | Adapter and persistence matrices include all listed paths. |
| CONTEXT | D-05 | Reject before atomic publication and preserve reusable inputs | 27-02, 27-03 | COVERED | Identity fingerprints and builder repair. |
| CONTEXT | D-06 | Final valid as initial and incoming target | 27-01, 27-02 | COVERED | Tracer and invariant target test. |
| CONTEXT | D-07 | O(1) current-State read only | 27-01 | COVERED | Property and AST guard. |
| CONTEXT | D-08 | Commit makes termination visible before later failures | 27-03 | COVERED | Entry-inside observation plus failure/cancellation matrices. |
| CONTEXT | D-09 | Ordinary missing-transition behavior while final | 27-01 | COVERED | No special completion runtime surface. |
| CONTEXT | D-10 | Reset/restore derive from resulting State | 27-03 | COVERED | Both directions with unchanged snapshot v1. |
| CONTEXT | D-11 | Clone exact State identity/finality, independent topology | 27-02, 27-03 | COVERED | Valid identity and invalid private reconstruction tests. |
| CONTEXT | D-12 | Additive bounded deterministic persistence and legacy default | 27-03 | COVERED | Strict field parser and one contextual publication. |
| FALLBACK EDGE | FINAL-01 | unclassified / unresolved | 27-01 | FLAGGED | Explicit assumption retained in frontmatter; locked D-01/D-03 define executable behavior. |
| FALLBACK EDGE | FINAL-02 | unclassified / unresolved | 27-01 | FLAGGED | Explicit assumption retained in frontmatter; locked D-07 defines executable behavior. |
| FALLBACK EDGE | FINAL-03 | unclassified / unresolved | 27-02 | FLAGGED | Explicit assumption retained in frontmatter; locked D-04/D-05 define executable behavior. |
| FALLBACK EDGE | FINAL-04 | unclassified / unresolved | 27-01 | FLAGGED | Explicit assumption retained in frontmatter; locked D-02 defines executable behavior. |
| FALLBACK EDGE | FINAL-05 | unclassified / unresolved | 27-03 | FLAGGED | Explicit assumption retained in frontmatter; locked D-08 defines executable behavior. |
| FALLBACK EDGE | FINAL-06 | unclassified / unresolved | 27-03 | FLAGGED | Explicit assumption retained in frontmatter; locked D-10 through D-12 define executable behavior. |
| PROHIBITION | — | Pure utility recall→precision produced no bespoke values/safety/ethics prohibition | all | COVERED | Routine correctness/security concerns are handled by tests and ASVS L1 threat models, not minted as prohibitions. |
| DEFERRED | — | Transition modes, rejection, deprecations, diagnostics/diagrams, installed artifacts, tutorial | none | EXCLUDED | Explicitly assigned to Phases 28-32. |

<output>
Create `.planning/phases/27-explicit-final-states/27-03-SUMMARY.md` when done.
</output>
