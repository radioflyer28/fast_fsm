---
phase: 27-explicit-final-states
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - src/fast_fsm/core.py
  - tests/test_final_states.py
  - tests/test_graph_invariants.py
  - tests/test_builder.py
  - tests/test_hypothesis.py
  - tests/test_async.py
  - .specify/memory/spr-core-api.md
autonomous: true
requirements:
  - FINAL-03
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "FINAL-03 / D-04: Every retained transition-producing surface rejects any canonical final source, including self, multi-source, priority, bidirectional, emergency, quick factory, builder, declarative, clone, and async paths."
    - "D-05: Final-source rejection occurs only in the Phase 26 canonical normalizer before publication, so a late invalid request rejects the complete operation without changing topology, graph version, current State, or reusable builder staging."
    - "D-06: Incoming transitions to a final target remain valid; the invariant constrains only canonical sources."
    - "Canonical resolution precedes the final-source check, so same-name foreign endpoint metadata cannot override the registered State's marker."
    - "The fixed public failure is bounded and does not echo a caller-controlled State name; dictionary row context remains available when that adapter is completed in Plan 27-03."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "One canonical final-source validation branch in `_normalize_transition_request()`"
      contains: "final state cannot be a transition source"
    - path: "tests/test_final_states.py"
      provides: "Cross-surface final-source rejection matrix"
    - path: "tests/test_graph_invariants.py"
      provides: "Identity-sensitive atomic publication and canonical-resolution proof"
    - path: "tests/test_builder.py"
      provides: "Failed final-source build retryability and staging preservation proof"
    - path: "tests/test_hypothesis.py"
      provides: "Generated final-source rejection and transaction atomicity proof"
  key_links:
    - from: "StateMachine._normalize_transition_request"
      to: "StateMachine._resolve_canonical_state"
      via: "resolve each source before reading its final marker"
      pattern: "_resolve_canonical_state"
    - from: "retained construction adapters"
      to: "StateMachine._apply_transition_requests_owned"
      via: "immutable `_TransitionRequest` tuples established by Phase 26"
      pattern: "_apply_transition_requests_owned"
    - from: "StateMachine._apply_transition_requests_owned"
      to: "StateMachine._commit_transition_plan"
      via: "complete prepared tuple only after every source passes validation"
      pattern: "_commit_transition_plan"
  flagged_assumptions:
    - id: FINAL-03
      classification: unclassified
      status: unresolved
      statement: "The specless edge probe did not classify construction rejection; this plan assumes locked D-04 and D-05 define the exhaustive invariant and atomicity boundary."
  prohibitions: []
---

<objective>
Enforce the final-source invariant once at the canonical Phase 26 transaction seam, then prove every retained construction adapter inherits identical all-or-nothing behavior.

Purpose: Make invalid completion topology impossible without charging runtime dispatch or duplicating checks across public APIs.
Output: Canonical source validation, atomicity and adapter matrices, generated invariant coverage, and updated construction SPR text.
</objective>

## Artifacts this phase produces

- Public `State.final: bool` getter backed by private slotted field `State._final`.
- Public `StateMachine.is_terminated: bool` inherited unchanged by `AsyncStateMachine`.
- Additive serialized dictionary field `final_states` containing sorted unique final-state names.
- New `tests/test_final_states.py` behavioral oracle.
- Updated `.specify/memory/spr-core-api.md` final-state, construction, lifecycle, control, clone, and persistence contract.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-explicit-final-states/27-CONTEXT.md
@.planning/phases/27-explicit-final-states/27-RESEARCH.md
@.planning/phases/27-explicit-final-states/27-PATTERNS.md
@.planning/phases/27-explicit-final-states/27-VALIDATION.md
@.planning/phases/26-canonical-construction-evidence-contract/26-01-SUMMARY.md
@.planning/phases/26-canonical-construction-evidence-contract/26-03-SUMMARY.md
@.planning/phases/27-explicit-final-states/27-01-SUMMARY.md

<interfaces>
Phase 26 contracts consumed here:
- `_apply_transition_requests_owned(tuple[_TransitionRequest, ...], error_contexts=None)` validates every carrier, normalizes the complete collection, then invokes `_commit_transition_plan()` once.
- `_normalize_transition_request()` resolves canonical sources and target and returns one `_PreparedTransition` without publishing.
- `_commit_transition_plan()` builds affected slot replacements off-table and increments `_graph_version` once only when publication changes topology.
- Direct, batch, bidirectional, emergency, quick-build, dictionary, declarative reconstruction, clone, and builder build paths already submit request tuples to the canonical seam.
- `graph_fingerprint(machine)` and `builder_staging_fingerprint(builder)` are the established identity-sensitive rollback assertions.

New invariant:
- A canonical source whose `State.final` is true raises one fixed ValueError from `_normalize_transition_request()` before target, guard, timing, merge, or publication work.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject canonical final sources before any topology publication</name>
  <files>src/fast_fsm/core.py, tests/test_final_states.py, tests/test_graph_invariants.py, .specify/memory/spr-core-api.md</files>
  <read_first>
    - `src/fast_fsm/core.py:1739-1808` — canonical endpoint resolution and transition request normalization.
    - `src/fast_fsm/core.py:1867-1935` — off-table publication and complete request transaction established by Phase 26.
    - `tests/test_graph_invariants.py:22-70,269-300,395-498` — graph fingerprint, clone failure, exact schema, and mixed-batch atomicity patterns.
    - `.planning/phases/27-explicit-final-states/27-CONTEXT.md` decisions D-04 through D-06.
    - `.planning/phases/26-canonical-construction-evidence-contract/26-VERIFICATION.md` truths 2-5 and 12-17 — prerequisite transaction guarantees that must remain intact.
  </read_first>
  <behavior>
    - A direct outgoing edge from a final State, including a self-transition, raises a fixed ValueError and changes no graph/current-state identity or version.
    - An incoming edge to a final target remains valid and its publication behavior is unchanged.
    - A source passed as a same-name foreign State is judged by the registered canonical State's marker, not by the foreign object's marker.
    - A mixed batch with valid rows before and after a final-source row publishes none of them and preserves graph version.
    - Multi-source expansion containing one final canonical source rejects the complete request without a valid-source prefix.
    - The check is construction-only and no finality branch is added to trigger/can_trigger selection.
  </behavior>
  <action>Write the atomicity and canonical-resolution tests first. Per D-04 and D-05, modify only `_normalize_transition_request()`: immediately after resolving each raw source to its registered canonical State, read that object's final marker and raise a fixed bounded ValueError when true. Keep the message independent of arbitrary State names. Do not validate raw source objects before canonicalization, silently filter final elements, or add dispatch-time fallback behavior. Preserve D-06 by allowing final targets and final initial states. Use `graph_fingerprint()` to prove direct, self, mixed-batch, and multi-source failures preserve state registry identities, slot/entry identities, `_graph_version`, current State, and graph snapshot. Add a source-region guard confirming selector and lifecycle functions remain unaware of final-source validation. Update the construction bullet in the living SPR in the same commit.</action>
  <verify>
    <automated>uv run pytest tests/test_final_states.py tests/test_graph_invariants.py -x -q -k "final_source or canonical or atomic or mixed or multi_source"
uv run python tools/release_evidence.py slots-policy --json</automated>
  </verify>
  <acceptance_criteria>
    - Final-source validation occurs exactly once after canonical source resolution and before `_PreparedTransition` creation/publication.
    - Final self-transitions, mixed batches, and multi-source requests fail with a fixed bounded ValueError and no observable mutation.
    - Same-name foreign source objects cannot spoof finality in either direction.
    - Incoming edges to final targets continue to construct successfully.
    - Runtime selector/lifecycle regions remain structurally free of construction invariant checks.
  </acceptance_criteria>
  <done>Invalid outgoing final-state topology is rejected atomically by the one canonical construction transaction, while valid incoming final transitions remain supported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove adapter, builder, clone, generated, and async invariant coverage</name>
  <files>tests/test_final_states.py, tests/test_graph_invariants.py, tests/test_builder.py, tests/test_hypothesis.py, tests/test_async.py</files>
  <read_first>
    - `src/fast_fsm/core.py:1101-1263,1978-2280` — from_states, quick_build, direct/batch, bidirectional, and emergency request adapters.
    - `src/fast_fsm/core.py:3383-3444,4332-4347` — sync/async clone reconstruction paths.
    - `src/fast_fsm/core.py:5650-5735,5880-5975` — builder staging and private candidate publication.
    - `tests/test_builder.py:123-267,1125-1152,2142-2196` — staging fingerprint, retryability, and candidate publication patterns.
    - `tests/test_hypothesis.py:23-183` — bounded generated topology and operation sequence conventions.
    - `tests/test_async.py:946-985,1229-1274` — async callback and independent clone parity patterns.
  </read_first>
  <behavior>
    - Bidirectional registration with a final reverse source publishes neither direction; emergency registration on a graph containing any final State publishes no source edge.
    - Priority candidates and multi-source rows from final sources reject identically without changing existing candidate identity/order.
    - `quick_build` and `quick_fsm` reject supplied final-source State objects through the same candidate transaction; name-only factories remain backward-compatible non-final constructors.
    - A builder containing a final State and an outgoing staged source remains uncached after failed build, preserves staging/machine classification, and can be repaired and built.
    - Declarative reconstruction from a final DeclarativeState fails before topology publication.
    - A deliberately corrupted private source topology cannot be cloned into a valid machine; ordinary valid clones preserve State identity and independent topology.
    - Async machines inherit identical registration failure and graph-version semantics.
    - Generated bounded cases maintain all-or-nothing behavior for arbitrary final markers and request ordering.
  </behavior>
  <action>Expand tests around the single canonical implementation rather than adding adapter code. Cover direct, batch, multi-source, priority, bidirectional, emergency, `StateMachine.quick_build`, `quick_fsm`, FSMBuilder private candidate build, declarative binding, clone reconstruction, and AsyncStateMachine registration. Where an adapter cannot declare final metadata because it accepts names only, assert its existing non-final behavior and the one downstream canonical seam rather than widening that API. Use `builder_staging_fingerprint()` to prove D-05 retryability and cache absence after failure; in the test only, replace the invalid private staged `_TransitionRequest` following the existing priority-conflict repairability test pattern, then prove a later build succeeds. For clone, keep ordinary D-11 identity behavior for valid machines and use a test-only privately corrupted topology solely to prove reconstruction cannot publish outgoing final edges. Add small Hypothesis strategies that mark canonical States final and either filter valid topologies or expect complete construction rejection; assert `is_terminated == current_state.final` throughout valid sequences. Do not add adapter-local checks, public factory parameters, builder deprecations, or new APIs.</action>
  <verify>
    <automated>uv run pytest tests/test_final_states.py tests/test_graph_invariants.py tests/test_builder.py tests/test_hypothesis.py tests/test_async.py -x -q -k "final or canonical or builder or clone or emergency or bidirectional"
uv run ruff format tests/test_final_states.py tests/test_graph_invariants.py tests/test_builder.py tests/test_hypothesis.py tests/test_async.py
uv run ruff check --fix tests/test_final_states.py tests/test_graph_invariants.py tests/test_builder.py tests/test_hypothesis.py tests/test_async.py
uv run ruff check tests/test_final_states.py tests/test_graph_invariants.py tests/test_builder.py tests/test_hypothesis.py tests/test_async.py</automated>
  </verify>
  <acceptance_criteria>
    - Every retained applicable construction adapter has a behavior-level final-source case or an explicit source-structure proof of canonical delegation.
    - Bidirectional and emergency helpers publish no allowed prefix when any generated source is final.
    - Builder failure leaves staging, async classification, and `_machine` unchanged and a repaired build succeeds.
    - Valid sync/async clones retain exact State identity and independent topology; invalid private topology cannot be cloned past canonical validation.
    - Generated tests cover reordered mixed batches and assert no graph/version prefix on rejection.
    - No adapter-local finality logic, extra public construction parameter, or runtime selection check is introduced.
  </acceptance_criteria>
  <done>All current topology-producing paths inherit the same final-source rule and atomicity from the canonical transaction, including sync/async, helper, builder, declarative, and clone shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public adapter endpoints → canonical registered State | Caller-controlled strings and State objects are resolved to authoritative identity. |
| Complete normalized request collection → live topology | Validated transition plans cross into observable immutable slots and graph version. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-02 | Tampering | `_normalize_transition_request` / `_apply_transition_requests_owned` | high | mitigate | Resolve registered identity first, reject final canonical sources during normalization, prepare the entire tuple before one publication, and assert identity-sensitive rollback for late invalid requests. |
| T-27-05 | Denial of Service | multi-source and emergency adapters | medium | mitigate | Reuse the finite supplied/registered source tuple and fail during one linear normalization pass; never scan unrelated topology during dispatch. |
| T-27-06 | Information Disclosure | final-source errors | medium | mitigate | Raise fixed bounded text without echoing caller-controlled names; retain only established adapter/row context. |
| T-27-08 | Tampering | reusable builder and clone candidate | medium | mitigate | Construct privately, publish/cache only after the complete canonical transaction, preserve staging/source topology on failure, and test repair/retry. |
</threat_model>

<verification>
- `uv run pytest tests/test_final_states.py tests/test_graph_invariants.py tests/test_builder.py tests/test_hypothesis.py tests/test_async.py -x -q`
- `uv run ruff check src/fast_fsm/core.py tests/test_final_states.py tests/test_graph_invariants.py tests/test_builder.py tests/test_hypothesis.py tests/test_async.py`
- `task typecheck-mypy`
- `task typecheck-ty`
- `uv run python tools/release_evidence.py slots-policy --json`
</verification>

<success_criteria>
- FINAL-03 rejects all applicable outgoing final-source construction through one canonical branch.
- A failed direct, grouped, helper, factory, builder, declarative, clone, or async operation publishes no prefix and preserves reusable inputs.
- Final destinations remain legal, and non-final factory defaults remain backward compatible.
- Fixed error text and linear construction work satisfy the ASVS L1 input boundary.
</success_criteria>

<output>
Create `.planning/phases/27-explicit-final-states/27-02-SUMMARY.md` when done.
</output>
