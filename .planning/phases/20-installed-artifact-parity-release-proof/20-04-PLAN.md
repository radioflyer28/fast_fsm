---
phase: 20-installed-artifact-parity-release-proof
plan: 04
type: execute
wave: 4
depends_on: [20-03]
files_modified:
  - tools/release_evidence.py
  - tests/test_installed_artifacts.py
  - tests/test_performance_benchmarks.py
  - tests/test_diagnostic_contracts.py
  - evidence/release-baseline.json
autonomous: true
requirements: [TEST-05, TEST-06, TEST-07]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-12, trigger(), can_trigger(), add_state(), and add_transition() retain O(1) operation/data-structure invariants with a coarse multi-size scaling backstop, while diagnostic complexity remains separately enforced by deterministic counters and budgets."
    - "Per D-11, only an installed compiled wheel with asserted native core origin can satisfy the 200000 trigger operations/second floor; warmup plus at least three environment-labelled samples gate on the median."
    - "Per D-13, the complete shared parity suite and final compiled floor rerun against installed artifacts after historical Phase 16-19 evidence validation; source-tree results remain prerequisites/observations, never substitutes."
  artifacts:
    - path: "tests/test_performance_benchmarks.py"
      provides: "Four-operation O(1) invariant/scaling proof and stable sampling contract"
    - path: "tests/test_diagnostic_contracts.py"
      provides: "Separate exact deterministic diagnostic budget boundary proof"
    - path: "tools/release_evidence.py"
      provides: "Native-origin-first installed compiled performance collector and aggregate gate"
    - path: "evidence/release-baseline.json"
      provides: "Separate historical, pure observation, installed compiled, and diagnostic complexity evidence sections"
  key_links:
    - from: "installed compiled origin assertion"
      to: "blocking trigger benchmark"
      via: "ordered verifier stages; benchmark cannot run or emit passing evidence before native classification"
      pattern: "native|benchmark|200000"
    - from: "four core operations"
      to: "O(1) claim"
      via: "operation-count/data-structure invariants plus multi-size ratio backstop"
      pattern: "trigger|can_trigger|add_state|add_transition"
    - from: "historical Phase 16-19 evidence"
      to: "final installed rerun"
      via: "separate schema sections and aggregator ordering"
      pattern: "historical_phase_performance|installed_performance"
  prohibitions:
    - statement: "Do not treat pure-Python throughput, source-tree native throughput, or historical phase measurements as satisfying the installed compiled floor."
      status: unverified
      verification: judgment
    - statement: "Do not put diagnostic traversal, allocation, logging, or evidence collection on the core runtime hot path."
      status: unverified
      verification: judgment
    - statement: "Do not publish exact environment-specific rates as durable README or API guarantees beyond the fixed 200000 floor."
      status: unverified
      verification: judgment
---

<objective>
Prove O(1) core operation structure and enforce the fixed throughput floor on an exact installed native compiled wheel, with diagnostic budgets and historical evidence kept separate.

Purpose: Close TEST-05/TEST-06/TEST-07 with evidence that measures the shipped compiled artifact rather than source shadows or a single unstable timing.
Output: Four-operation complexity tests, deterministic diagnostic-boundary checks, native-origin-first multi-sample benchmark evidence, and aggregate performance rules.
</objective>

## Artifacts this phase produces

- Deterministic O(1) invariants and coarse scaling checks for the four named runtime operations.
- Separate exact-limit/one-less diagnostic budget evidence, outside runtime timing and hot paths.
- Installed compiled benchmark records containing warmup, samples, median, iterations, interpreter/OS/machine, artifact SHA, and asserted native origin.
- Pure/source rates retained only as environment-labelled observations; Phase 16-19 evidence retained only as historical records.
- The final CI plan consumes these gates before release authorization but does not create or publish a release during implementation.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-CONTEXT.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-RESEARCH.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-PATTERNS.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-03-SUMMARY.md
@tests/test_performance_benchmarks.py
@tests/test_diagnostic_contracts.py
@benchmarks/benchmark_fast_fsm.py

<interfaces>
- The blocking installed benchmark result records `artifact_sha256`, `core_origin`, `asserted_mode=compiled`, `warmup_operations`, `iterations`, at least three `samples_ops_per_second`, `statistic=median`, `median_ops_per_second`, Python implementation/version, OS, machine, and environment label.
- `median_ops_per_second >= 200000` is the only durable numeric runtime floor. Pure results use `blocking=false`/observation semantics and cannot occupy the installed compiled matrix field.
- O(1) proof names exactly `trigger`, `can_trigger`, `add_state`, and `add_transition`; deterministic instrumentation/invariants are primary, with broad size scaling only as a regression backstop.
- Diagnostic evidence stays in its own deterministic limit/status/counter section and never contributes elapsed time to the core runtime claim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin O(1) invariants for four core operations and separate diagnostic budgets</name>
  <files>tests/test_performance_benchmarks.py, tests/test_diagnostic_contracts.py</files>
  <read_first>tests/test_performance_benchmarks.py existing trigger floor/construction loops; tests/test_diagnostic_contracts.py exact budget helpers and Phase 19 limits; src/fast_fsm/core.py transition/state dictionaries and ownership wrappers; src/fast_fsm/_diagnostics.py counters; .planning/phases/19-bounded-diagnostics-safe-output/19-PERFORMANCE-EVIDENCE.md; 20-CONTEXT.md D-12; 20-01-PLAN.md A20-13</read_first>
  <behavior>
    - Trigger and can_trigger use constant-count direct transition/guard lookup independent of unrelated states/transitions.
    - Add_state and add_transition use constant-count registry lookup/write/validation work independent of existing topology size.
    - Small/medium/large topology cases retain bounded operation-count invariants and a deliberately loose monotonic ratio backstop without claiming wall-clock timing alone proves asymptotics.
    - Diagnostic exact limits pass and one-less limits fail before work/allocation for work, results, dense cells, and path expansions in a separate suite.
  </behavior>
  <action>Write instrumentation-backed tests around the existing dict-based runtime seams for exactly the four named operations. Count relevant mapping lookups/writes and guard resolution steps on equivalent operations across small, medium, and large unrelated topology sizes, asserting a topology-size-independent upper bound; add a coarse repeated timing ratio only as a backstop tolerant of host noise. Preserve all O(1) production algorithms and avoid adding test hooks or counters to `core.py`. Extend the Phase 19 diagnostic cases only where necessary to demonstrate exact-limit success and one-less reserve-before-work failure for each deterministic dimension, explicitly keeping these assertions out of the runtime benchmark. Use fixed graph shapes/seeds and no parallel or sleep-based measurement.</action>
  <acceptance_criteria>
    - Per A20-13, trigger, can_trigger, add_state, and add_transition each have named invariant tests whose counted lookup/write/guard work remains within the same bound across at least three topology sizes.
    - Per A20-13, coarse multi-size timing is only a broad regression backstop; test names/docs do not claim a single elapsed measurement is an asymptotic proof.
    - Diagnostic work/result/dense/path exact limits pass and one-less limits fail before the forbidden work/allocation with deterministic status/counters.
    - No production runtime file, runtime dependency, hot-path diagnostic import, or public API is added for the tests.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_performance_benchmarks.py tests/test_diagnostic_contracts.py -x -q -k 'constant or scaling or complexity or budget or exact_limit'</automated>
  </verify>
  <done>All four named core operations have deterministic topology-size-independent evidence plus a coarse scaling backstop, while diagnostics retain separate exact counted budgets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate installed native compiled throughput with stable samples</name>
  <reversibility rating="costly">D-11 fixes the release-blocking measurement contract for published compiled artifacts.</reversibility>
  <files>tools/release_evidence.py, tests/test_installed_artifacts.py, tests/test_performance_benchmarks.py, evidence/release-baseline.json</files>
  <read_first>tools/release_evidence.py installed verifier/aggregate/historical sections; tests/test_installed_artifacts.py compiled artifact helpers; tests/test_performance_benchmarks.py existing warmup/gc/perf_counter fixture and Task 1 complexity tests; benchmarks/benchmark_fast_fsm.py; evidence/release-baseline.json Phase 20 schema; 20-CONTEXT.md D-11/D-12/D-13; 20-01-PLAN.md A20-11/A20-12</read_first>
  <behavior>
    - Benchmark execution is unreachable until exact artifact SHA, environment containment, compiled mode, extension loader/origin, metadata version, and native architecture are accepted.
    - Warmup precedes at least three fixed-iteration samples; the median is blocking at 200000 operations/second, while every sample and environment fact is recorded.
    - Pure/source observations are recorded separately and never satisfy the compiled key; historical evidence must be complete before the final installed rerun.
    - A below-floor median, too few/invalid samples, non-native origin, wrong architecture, or detached artifact digest fails aggregation.
  </behavior>
  <action>Refactor the existing real alternating-trigger benchmark into a child-probe callable shared by tests and installed evidence without adding runtime package code. In `tools/release_evidence.py`, invoke it only after Plan 20-01 origin/type/identity acceptance; use a warmup and at least three equal fixed-iteration measurements, compute the median deterministically, and record all exact environment fields listed in Interfaces. Gate only installed compiled records at the exact integer threshold `200000`; serialize pure/source measurements as nonblocking observations. Update aggregate validation and manifest fixtures so the four historical Phase 16-19 records are complete first, then the final installed compiled parity suite and benchmark must rerun and pass independently. Add negative fixtures for below-floor median, one slow outlier with passing median, insufficient/NaN/infinite samples, mismatched architecture/SHA/origin, and an attempted historical/pure substitution.</action>
  <acceptance_criteria>
    - Per A20-12, native origin/identity is asserted before benchmark execution, warmup precedes at least three samples, and median `>= 200000` passes while a lower median fails.
    - Per A20-12, all samples, median, iteration/warmup counts, Python/OS/machine, artifact SHA, build intent, and native core origin are present and deterministic apart from measured numeric values.
    - Per A20-11, complete historical evidence is validated before the final installed run and cannot satisfy either the parity rerun or compiled performance key.
    - Pure observations, source-native observations, too few/nonfinite samples, wrong-origin/architecture records, and detached digests cannot satisfy TEST-06.
    - The local exact compiled wheel fixture passes the floor or reports an honest environment-labelled failure; tests may inject deterministic sample arrays for boundary semantics without claiming hosted matrix execution.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_installed_artifacts.py tests/test_performance_benchmarks.py tests/test_release_evidence.py -x -q -k 'installed and (compiled or performance or benchmark or median or historical)'</automated>
  </verify>
  <done>The release contract enforces a stable, environment-labelled 200000 ops/sec median on the exact installed native compiled artifact after identity/parity checks, with pure and historical evidence kept nonblocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installed artifact identity → benchmark | A fast checkout/native shadow or wrong-architecture extension can falsely satisfy the floor. |
| measured samples → aggregate | Missing, nonfinite, selectively omitted, or relabelled observations can distort the blocking statistic. |
| topology size/diagnostics → complexity claims | Noisy timings or hidden diagnostic work can be misrepresented as O(1) runtime proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-16 | Spoofing | compiled performance origin | high | mitigate | Artifact SHA, environment containment, extension loader/origin, mode, version, and native architecture assertions execute before benchmark entry. |
| T-20-17 | Tampering | benchmark samples/statistic | high | mitigate | At least three recorded finite samples, fixed iterations, deterministic median recomputation, threshold validation, and artifact/evidence digest binding. |
| T-20-18 | Repudiation | performance environment | medium | mitigate | Record exact interpreter/OS/machine/origin/intent/iterations and label all observations; generated summary derives from validated records. |
| T-20-19 | Denial of Service | diagnostic/runtime workload | medium | mitigate | Deterministic diagnostic budgets remain separate; benchmark uses fixed bounded iterations and no graph traversal expansion. |
| T-20-20 | Tampering | historical/pure substitution | high | mitigate | Separate schema types and keys prevent historical or pure records from populating the installed compiled parity/performance requirements. |
</threat_model>

<verification>
- Run targeted complexity, diagnostic, installed artifact, and performance tests sequentially through uv.
- Exercise one local installed compiled artifact and assert the loaded native origin before collecting measurements.
- Confirm the aggregate rejects every pure/source/historical/malformed substitution for the installed compiled floor.
</verification>

<success_criteria>
- Four core operations retain O(1) invariant evidence across topology sizes.
- Diagnostics retain separate deterministic budget-boundary evidence.
- The exact installed native compiled artifact must pass the multi-sample 200000 median gate after parity and identity checks.
</success_criteria>

<output>
Create `.planning/phases/20-installed-artifact-parity-release-proof/20-04-SUMMARY.md` when done.
</output>
