---
phase: 20-installed-artifact-parity-release-proof
plan: 03
type: execute
wave: 3
depends_on: [20-02]
files_modified:
  - tools/release_evidence.py
  - tests/test_release_evidence.py
  - evidence/release-baseline.json
  - pyproject.toml
  - docs/conf.py
  - CHANGELOG.md
  - README.md
  - .github/copilot-instructions.md
  - .specify/memory/spr-core-api.md
autonomous: true
requirements: [REL-01, TEST-04, TEST-05]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-09, release aggregation compares records with one canonical expected matrix and rejects missing, duplicate, unexpected, malformed, or mixed version/commit/tag/suite/artifact identities before generating a deterministic manifest and summary."
    - "Per D-10, package metadata, installed metadata/__version__, changelog, Sphinx identity, public documentation claims, evidence schema, ref name, and checked-out commit are validated as v0.3.0; tag equality is a tag-time check and this plan does not create a tag."
    - "Per D-13, exact allowlisted Phase 16-19 performance evidence is hashed and consolidated as historical context, with an explicit invariant that it cannot satisfy installed compiled parity or throughput gates."
  artifacts:
    - path: "tools/release_evidence.py"
      provides: "Canonical matrix, strict aggregator, v0.3.0 identity gate, historical evidence inventory, and generated summary"
    - path: "evidence/release-baseline.json"
      provides: "Schema-evolved deterministic release manifest contract"
    - path: "pyproject.toml"
      provides: "Authoritative package version 0.3.0 with unchanged dependency/build boundary"
    - path: "docs/conf.py"
      provides: "Sphinx version/release aligned with package identity"
    - path: "CHANGELOG.md"
      provides: "v0.3.0 release section in pre-tag state"
    - path: "README.md"
      provides: "Durable installed-artifact release-proof claims"
  key_links:
    - from: "canonical expected matrix"
      to: "per-artifact evidence records"
      via: "exact key-set and uniqueness reconciliation"
      pattern: "expected.*matrix|missing|unexpected|duplicate"
    - from: "release identity gate"
      to: "pyproject/docs/changelog/README/installed records"
      via: "one adjacent release_identity projection with non-empty consistency checks"
      pattern: "0.3.0|v0.3.0|release_identity"
    - from: "Phase 16-19 performance files"
      to: "release-baseline historical section"
      via: "exact path allowlist plus SHA-256, command, environment, phase and commit fields"
      pattern: "historical_phase_performance|PERFORMANCE-EVIDENCE"
  prohibitions:
    - statement: "Do not create, move, or verify success of a public v0.3.0 tag during ordinary pull-request implementation."
      status: unverified
      verification: judgment
    - statement: "Do not claim SHA-256 proves publisher authenticity; it binds evidence to exact bytes only."
      status: unverified
      verification: judgment
    - statement: "Do not replace installed proof with historical source-tree measurements or exact volatile benchmark numbers in narrative documentation."
      status: unverified
      verification: judgment
---

<objective>
Aggregate the complete artifact matrix, align every static identity surface to v0.3.0, and consolidate Phase 16-19 historical performance evidence without confusing it with installed proof.

Purpose: Turn independent artifact records into one exact, deterministic, fail-closed release identity and parity contract before CI wiring or final performance authorization.
Output: Canonical matrix reconciliation, schema-evolved manifest, generated human summary, v0.3.0 source identity, and hashed historical evidence inventory.
</objective>

## Artifacts this phase produces

- Strict per-artifact JSON records and one deterministic exact-matrix release manifest plus human summary.
- Static and tag-time v0.3.0 identity checks spanning package metadata, installed metadata, changelog, docs, evidence schema, ref, and commit.
- Historical Phase 16-19 performance inventory with exact path/digest/command/environment provenance, kept distinct from final installed compiled proof.
- Later plans add installed compiled multi-sample performance/O(1) proof and a native CI graph whose final release job depends on the accepted aggregate.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-CONTEXT.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-RESEARCH.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-PATTERNS.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-02-SUMMARY.md
@evidence/release-baseline.json
@.planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md
@.planning/phases/17-atomic-transition-lifecycle/17-PERFORMANCE-EVIDENCE.md
@.planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-PERFORMANCE-EVIDENCE.md

<interfaces>
- Define one immutable expected-matrix function/data structure in `tools/release_evidence.py`; workflow contract tests consume its serialized projection instead of independently inventing cells.
- Aggregation key includes artifact filename/SHA-256, CPython minor, OS, machine architecture, asserted mode, and sdist parent where applicable; universal2 requires distinct native-runtime keys per architecture for the same artifact SHA.
- `release_identity` is one adjacent deterministic object. Static mode requires `0.3.0` package/docs/changelog/evidence values without requiring a tag; tag mode additionally requires ref `v0.3.0` and peeled tag commit equal checked-out/aggregated commit.
- Historical evidence is an exact four-path allowlist with SHA-256 and parsed exact command/environment/phase/commit facts; it is never counted as an artifact matrix record.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile an exact release matrix and deterministic aggregate</name>
  <reversibility rating="costly">D-09 fixes the completeness key and evidence schema consumed by release CI and future audits.</reversibility>
  <files>tools/release_evidence.py, tests/test_release_evidence.py, evidence/release-baseline.json</files>
  <read_first>tools/release_evidence.py strict JSON/read-write/check seams plus Plan 20-02 artifact records; tests/test_release_evidence.py manifest fixtures/workflow mutation tests; evidence/release-baseline.json current schema; .github/workflows/release.yml current CPython/platform/architecture selectors; 20-CONTEXT.md D-08/D-09; 20-RESEARCH.md Pattern 3 and strict matrix skeleton</read_first>
  <behavior>
    - One expected matrix enumerates the universal pure wheel coverage, direct compiled matrix, two native executions for universal2, sdist archive record, and explicit pure/compiled sdist-derived children.
    - Aggregate rejects missing, duplicate, unexpected, digest-detached, malformed, mixed-version, mixed-commit, mixed-tag, mixed-suite-digest, wrong-mode, or architecture-incompatible records.
    - Accepted records have equal semantic scenario values except declared origin/artifact/performance envelope fields and render byte-identical sorted JSON plus a concise summary from that object.
  </behavior>
  <action>Write negative fixtures first for each incomplete/substituted matrix condition. Implement the canonical supported CPython/platform/architecture matrix in `tools/release_evidence.py` from the currently declared release contract, including pure-wheel coverage, native compiled cells, universal2 dual-native execution, sdist archive, and explicit derived children. Index accepted records by the full matrix key and independently enforce unique normalized filename/SHA combinations where required. Reject malformed JSON, duplicate keys, missing/extra fields, record reuse, unexpected cells, and any mixed release version, commit, tag, suite digest, artifact digest, or build intent per D-08/D-09. Compare every scenario field with stable scenario/field-level diagnostics while excluding only declared envelope fields. Serialize with strict sorted JSON/newline and generate the human summary solely from the validated aggregate. Evolve `evidence/release-baseline.json` schema/fixtures to express the contract without inventing hosted evidence or accepted timings during implementation; CI later supplies runtime records.</action>
  <acceptance_criteria>
    - Exact expected and actual key sets plus uniqueness constraints pass for a complete fixture and fail independently for missing, duplicate, unexpected, reused, or architecture-incompatible cells.
    - Mixed version, commit, tag, suite digest, semantic field, artifact SHA, mode, build intent, or sdist lineage fails with bounded scenario/field diagnostics.
    - Malformed/non-object/non-standard-number/oversized evidence and duplicate JSON keys are rejected before aggregation.
    - Two differently ordered equivalent inputs serialize to identical aggregate bytes and identical human summary bytes ending in one newline.
    - The manifest schema separates expected matrix, artifact records, release identity, historical evidence, and installed performance; absent hosted records are represented as unaccepted inputs, never fabricated successes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_release_evidence.py -x -q -k 'matrix or aggregate or duplicate or parity or deterministic or malformed'</automated>
  </verify>
  <done>A deterministic aggregator accepts exactly one complete, identity-consistent, semantically equal release matrix and rejects every incomplete, substituted, duplicated, malformed, or mixed set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align v0.3.0 static identity and separate tag-time proof</name>
  <reversibility rating="costly">D-10 aligns public version claims; actual tag creation remains a separate one-way release operation outside this plan.</reversibility>
  <files>pyproject.toml, docs/conf.py, CHANGELOG.md, README.md, tests/test_release_evidence.py</files>
  <read_first>pyproject.toml project/build/dependency declarations; src/fast_fsm/__init__.py metadata-derived `__version__`; docs/conf.py version/release fields; CHANGELOG.md section format; README.md release/performance claims; tools/release_evidence.py Task 1 identity schema; tests/test_release_evidence.py; 20-CONTEXT.md D-10; 20-01-PLAN.md A20-01/A20-02/A20-03</read_first>
  <behavior>
    - Static identity validates package metadata `0.3.0`, installed metadata and `fast_fsm.__version__` `0.3.0`, Sphinx version/release, changelog v0.3.0 section, README durable claims, and evidence schema/version together.
    - Missing, blank, `unknown`, conflicting, stale, or ambiguous duplicate identity values fail with a named field.
    - Ordinary PR checks do not require a pre-existing tag; tag mode requires ref `v0.3.0` and peeled tag commit equal checked-out plus aggregated commit.
  </behavior>
  <action>Add identity fixture tests, then coordinate the version to `0.3.0` across `pyproject.toml`, Sphinx configuration, changelog pre-tag section, and durable README release-proof claims while preserving the metadata-derived `fast_fsm.__version__` implementation. Use one `release_identity` validator/summary projection implemented in Task 1 and assert each source/installed field explicitly. Per D-10 and A20-03, separate `--static` or equivalent pre-build validation from a tag-event-only ref check; the latter reads the supplied Git ref/checked-out commit and peels annotated or lightweight tags without creating, moving, or pushing one. Keep the changelog date explicitly unreleased during ordinary implementation and require a concrete date only at the authorized tag-time gate. Preserve build pins, runtime dependency count, Python support, public symbols, and core.py-only mypyc configuration.</action>
  <acceptance_criteria>
    - Per A20-01, the machine record and generated summary keep all package/docs/changelog/ref/commit identity fields adjacent under one `release_identity` section.
    - Per A20-02, missing, blank, `unknown`, duplicate-ambiguous, or contradictory identity values fail closed with the exact field category.
    - Static mode passes without an existing tag only when every build-time surface and fixture says `0.3.0`; tag mode accepts only `v0.3.0` whose peeled commit equals both checkout and aggregate commit.
    - Changelog uses the repository format and remains explicitly unreleased until authorized tag time; no test or task creates a tag/release.
    - Runtime dependencies, build-system pins, supported Python floor, public exports, and Sphinx/doc builds remain valid.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_release_evidence.py tests/test_readme_examples.py -x -q -k 'identity or version or tag or readme' &amp;&amp; uv run sphinx-build -b html docs docs/_build/html -W --keep-going &amp;&amp; uv run sphinx-build -b doctest docs docs/_build/doctest</automated>
  </verify>
  <done>Every static source and installed identity surface is fail-closed on v0.3.0, while exact tag-to-commit proof is available only as a non-mutating tag-time gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Consolidate historical evidence and reconcile the measured slots registry</name>
  <files>tools/release_evidence.py, tests/test_release_evidence.py, .github/copilot-instructions.md, .specify/memory/spr-core-api.md</files>
  <read_first>the four Phase 16-19 PERFORMANCE-EVIDENCE.md files; tools/release_evidence.py slots registry/measurements and Task 1 aggregate; tests/test_release_evidence.py historical/slots fixtures; src/fast_fsm/_diagnostics.py `DiagnosticBudgetExceeded`; .specify/decisions/ADR-003-mypyc-compilation-boundary.md; .specify/decisions/ADR-006-bounded-diagnostics-safe-output.md; .github/copilot-instructions.md slots policy; .specify/memory/spr-core-api.md; 20-CONTEXT.md D-12/D-13; 20-01-PLAN.md A20-09/A20-10/A20-11</read_first>
  <behavior>
    - The final schema contains exactly one ordered historical entry for each Phase 16, 17, 18, and 19 allowlisted evidence file with file SHA-256, exact command, environment/build mode, measurements/counters, phase, and commit provenance.
    - Missing, duplicate, moved, changed-digest, unparsable, or incomplete historical entries fail; recursive globs cannot silently discover the inventory.
    - The executable slots registry, ADR-accepted Phase 19 diagnostic exception, contributor instructions, and SPR state one measured policy consistently without changing runtime layout to satisfy documentation.
  </behavior>
  <action>Add a four-path constant allowlist for the existing Phase 16-19 performance evidence and parse only explicit documented fields into `historical_phase_performance`, preserving source text SHA-256 for audit. Per D-13 and A20-09 through A20-11, validate all four entries as a set before installed proof and label them historical; ensure no historical measurement can populate an installed artifact record or satisfy the compiled threshold. Resolve the research-noted slots-policy mismatch by treating the accepted Phase 19 `DiagnosticBudgetExceeded` slotted RuntimeError layout as the third measured registered exception alongside `CompiledFuncCondition` and `TransitionError`, because the current executable registry and ADR-006 implementation are shipped facts; update `.github/copilot-instructions.md` and `.specify/memory/spr-core-api.md` to name all three with their distinct boundary rationales in the same task, without altering class layout or the core.py compilation boundary. Add regression tests that documentation, static inventory, runtime layouts, and measurement keys reconcile exactly.</action>
  <acceptance_criteria>
    - Per A20-09, exactly four adjacent ordered historical entries exist under `historical_phase_performance`, one each for Phases 16-19 with path/SHA/command/environment/phase/commit facts.
    - Per A20-10, deleting, duplicating, moving, changing, or making any allowlisted entry incomplete fails aggregation; no recursive discovery fills it.
    - Per A20-11, historical entries are validated before installed performance and cannot satisfy artifact completeness, parity, or the compiled throughput floor.
    - The slots command, tests, contributor instructions, and core SPR consistently name exactly `CompiledFuncCondition`, `TransitionError`, and `DiagnosticBudgetExceeded` with no runtime layout or dependency change.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_release_evidence.py -x -q -k 'historical or performance_evidence or slots' &amp;&amp; uv run python tools/release_evidence.py slots-policy --json</automated>
  </verify>
  <done>The release manifest deterministically inventories all Phase 16-19 performance evidence as historical context and the measured slots policy is internally consistent across executable evidence, ADR-accepted runtime facts, instructions, and SPR memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downloaded record set → aggregate | Records can be malformed, duplicated, substituted, missing, or attached to mixed identities. |
| repository identity files/Git ref → release identity | Stale text, ambiguous values, or a ref targeting another commit can mislabel artifacts. |
| historical evidence files → final manifest | Mutable paths/content can be presented as complete phase proof or substituted for installed evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-11 | Tampering | aggregate matrix | high | mitigate | Canonical expected key set, uniqueness, artifact SHA binding, exact identity equality, and negative fixtures for missing/duplicate/unexpected/substituted records. |
| T-20-12 | Spoofing | release version/ref/commit | high | mitigate | Adjacent non-empty identity projection, static pre-build validation, and non-mutating tag-time peeled-ref equality to checkout and aggregate commit. |
| T-20-13 | Repudiation | deterministic aggregate/summary | medium | mitigate | Strict JSON, canonical ordering, one schema version, source digests, and summary generated only from validated records. |
| T-20-14 | Information Disclosure | parity differences and summary | medium | mitigate | Report scenario/field identifiers and stable envelope facts only; never include raw record payloads, exception strings, or arbitrary paths. |
| T-20-15 | Tampering | historical phase evidence | high | mitigate | Four exact allowlisted paths, SHA-256, required parsed provenance, set completeness, and a schema barrier preventing historical records from satisfying installed gates. |
</threat_model>

<verification>
- Run aggregate, identity, historical, slots, README, Sphinx, and doctest gates sequentially through uv.
- Confirm the manifest/summary are deterministic and no hosted runtime success is fabricated.
- Confirm identity changes do not alter dependencies, public symbols, runtime layout, or selective compilation.
</verification>

<success_criteria>
- Only a complete, unique, semantically equal artifact set can aggregate.
- All static identity surfaces fail closed on v0.3.0 and tag-time equality is available without tag mutation.
- Historical Phase 16-19 evidence and slots-policy facts are complete, exact, and clearly separated from installed proof.
</success_criteria>

<output>
Create `.planning/phases/20-installed-artifact-parity-release-proof/20-03-SUMMARY.md` when done.
</output>
