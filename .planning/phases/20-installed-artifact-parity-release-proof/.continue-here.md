---
context: phase
phase: 20-installed-artifact-parity-release-proof
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-09-05T21:39:53Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Executable evidence over structural proxies — Phase 20 initially had green unit tests while the real `release-evidence-local-check` failed on producer/schema drift. Run the actual evidence command whenever a producer or schema changes; substring/YAML-shape tests alone are insufficient.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Structural-proxy green | Unit and workflow-shape tests passed while the executable local evidence producer emitted obsolete runtime/performance records. | blocking | Run `task --silent release-evidence-local-check` after every producer/schema edit, then the full sequential suite. |
| Baseline fabrication | The tracked baseline is stale, but exact uv 0.12.6 is unavailable locally. Substituting uv 0.12.9 or hand-editing generated evidence would manufacture proof. | advisory | Obtain the exact reviewed toolchain before baseline write/check; preserve the pin and record the gap until then. |
| Hosted-success inference | YAML inspection proves wiring, not runner availability or matrix execution. | advisory | Keep the hosted prerelease check pending until an explicitly authorized exact-SHA evidence-only run exists. |

<current_state>
Phase 20 execution is complete (6/6 plans and summaries). The standard code review found 12 critical and 2 warning findings. Fix iteration 1 and re-review iteration 2 followed. Fix iteration 2 integrated eight atomic commits through `a4d2689`, fixing all iteration-2 findings except stale tracked baseline CR-05. The worktree is `/private/tmp/fast-fsm-phase20-resume` on `worktree-wf_phase19_bounded_diagnostics_20260902`.
</current_state>

<completed_work>

- Plans 20-01 through 20-06: complete and summarized.
- Approved tag-only `contents: write` workflow definition: committed; no workflow dispatched.
- Review fix iteration 1: commits `4082fe0`, `a823c4d`.
- Review fix iteration 2: commits `da51d59`, `3f3dedc`, `9520849`, `7bdcc97`, `de230ee`, `2f3b0f8`, `ac11ab3`, `a4d2689`.
- Verified after iteration 2: Ruff; 204 targeted release/artifact tests; actual `task --silent release-evidence-local-check`; full `uv run pytest tests/ -x -q`.
</completed_work>

<remaining_work>

- Run independent code re-review iteration 3 over the original 24-file scope.
- If findings remain, apply the third/final bounded fixer iteration and re-review for convergence.
- Resolve CR-05 only with exact uv 0.12.6; do not fabricate or weaken the contract.
- Commit converged `20-REVIEW.md` and `20-REVIEW-FIX.md` together.
- Run execute-phase regression gate.
- Run configured Nyquist validation and security audit hooks.
- Run Phase 20 goal verifier; persist/present human UAT items if status is `human_needed`.
- After accepted UAT, complete the phase and milestone audit/transition. Do not push or perform release actions without separate explicit authorization.
</remaining_work>

<decisions_made>

- Exact uv 0.12.6 remains fail-closed; host uv 0.12.9 is not a substitute.
- The local evidence profile is non-authorizing; hosted native evidence remains a separate pre-tag gate.
- User approval covered only adding the checked-in tag-only write-capable workflow definition.
</decisions_made>

<blockers>

- CR-05: `evidence/release-baseline.json` is stale. `which -a uv` finds only `/Users/akriz/.local/share/cargo/bin/uv`, version 0.12.9. Exact uv 0.12.6 is required for truthful regeneration.
- Beads commands currently cannot connect to the local Dolt server at `127.0.0.1:52726`; Git commits still succeed with warnings.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable continuation state.
2. `.planning/phases/20-installed-artifact-parity-release-proof/20-REVIEW.md` — iteration-2 findings and original 24-file scope.
3. `.planning/phases/20-installed-artifact-parity-release-proof/20-REVIEW-FIX.md` — iteration-2 disposition.
4. `.planning/phases/20-installed-artifact-parity-release-proof/20-06-SUMMARY.md` — final execution-plan evidence and external-only gates.
5. `.planning/phases/20-installed-artifact-parity-release-proof/20-CONTEXT.md` — locked decisions D-01 through D-14.

## Critical Anti-Patterns (do NOT repeat these)

- Do not accept workflow/YAML structure tests as runtime producer proof; run the executable evidence path.
- Do not regenerate or hand-edit evidence with the wrong uv version.
- Do not infer hosted success, dispatch workflows, push/fetch, create tags/releases, or publish artifacts.

## Infrastructure State

- Active worktree: `/private/tmp/fast-fsm-phase20-resume`
- Branch: `worktree-wf_phase19_bounded_diagnostics_20260902`
- HEAD: `a4d2689`
- Installed uv: `0.12.9`; required baseline uv: `0.12.6`
- No background agents or external jobs remain.
- Uncommitted by design: updated `20-REVIEW.md`, untracked `20-REVIEW-FIX.md`.

<context>
The code is at a strong checkpoint after the second bounded review-fix pass. Continue with one independent re-review before trusting the fix report. If the code converges, the only known unresolved item should be baseline regeneration under exact uv 0.12.6. Preserve the separation between local proof, hosted proof, and public release authorization.
</context>

<next_action>
Start with: re-review the current 24-file Phase 20 scope at standard depth, writing the result to `20-REVIEW.md`; if issues remain, use the third/final fixer pass.
</next_action>
