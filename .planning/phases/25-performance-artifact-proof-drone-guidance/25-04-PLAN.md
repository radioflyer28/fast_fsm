---
phase: 25-performance-artifact-proof-drone-guidance
plan: "04"
type: execute
wave: 4
depends_on:
  - 25-03
files_modified:
  - evidence/release-baseline.json
autonomous: true
gap_closure: true
requirements:
  - PERF-02
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-02, the durable release baseline is regenerated only by the reviewed uv 0.12.6 toolchain with UV_OFFLINE=1 and no network fallback."
    - "The manifest records 1,805 collected and 1,799 passed tests while errors, failures, and skips remain 0, 0, and 6; every other freshness-defining value remains unchanged, with only generator-owned environment/timing observations allowed to refresh."
    - "The read-only release-baseline freshness check and the relevant quality, source-origin, installed pure/compiled parity, and compiled singleton performance gates pass against the regenerated evidence."
    - "No runtime, test, documentation, task-definition, dependency, lockfile, threshold, or evidence-validator semantics change in this gap closure."
  artifacts:
    - path: evidence/release-baseline.json
      provides: "Fresh generated quality evidence for the current 1,805-test suite"
      contains: '"collected": 1805'
  key_links:
    - from: Taskfile.yml
      to: evidence/release-baseline.json
      via: "release-baseline-write generates the manifest and release-baseline-check validates it without mutation"
      pattern: "release-baseline-(write|check)"
    - from: evidence/release-baseline.json
      to: tests/
      via: "quality_baseline.tests records the exact sequential pytest collection and outcome counts"
      pattern: '"collected": 1805|"passed": 1799'
---

<objective>
Close the sole Phase 25 verification gap by regenerating the stale durable release baseline under the reviewed pinned offline toolchain, accepting only the exact current-suite count change, and re-proving the release quality and installed-artifact contracts per D-02.

Purpose: Phase behavior, documentation, and artifact semantics are already verified; only `evidence/release-baseline.json` still records 1,798 collected / 1,792 passed instead of the observed 1,805 / 1,799.
Output: One reviewed evidence-only manifest update that passes the read-only freshness, quality, and installed-artifact gates without any broader repository change.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-CONTEXT.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-RESEARCH.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-VERIFICATION.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-02-PLAN.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-02-SUMMARY.md
@.github/copilot-instructions.md
@Taskfile.yml
@tools/release_evidence.py
@evidence/release-baseline.json

<interfaces>
`task release-baseline-write` is the sole intentional writer for `evidence/release-baseline.json`; it performs `uv sync --locked --all-groups`, pure-source verification, and `release_evidence.py evidence --write --build-wheel`.

`task release-baseline-check` repeats the locked setup and pure-source proof but invokes `release_evidence.py evidence --check --build-wheel`, so it must not change manifest bytes.

The reviewed executable is `/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin/uv`. Prepending its directory to `PATH` makes every Taskfile `uv` invocation use version 0.12.6; `UV_OFFLINE=1` must be inherited by every sync, build, test, and isolated artifact child.
</interfaces>
</context>

## Artifacts this gap plan produces

| Surface | Contract after execution |
| --- | --- |
| `evidence/release-baseline.json` | Generated by `release-baseline-write` under reviewed uv 0.12.6 with `UV_OFFLINE=1`; `quality_baseline.tests.collected` is 1,805 and `passed` is 1,799, with 0 errors, 0 failures, 6 skips, unchanged stable contract fields, and refreshed environment-labelled timing observations only where the generator owns them. |
| Repository source and policy | Unchanged: no runtime, test, docs, Taskfile, dependency, lockfile, threshold, or validator edit belongs to this closure. |
| Verification evidence | The read-only freshness, full release-quality, installed pure/compiled parity, and installed compiled singleton performance tasks all pass with the pinned offline toolchain. |

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Truthful complexity, equivalent installed artifacts, FSM-owned telemetry routing, and clean release proof | 25-01 through 25-04 | COVERED | The first three plans implement the goal; 25-04 closes only the stale clean-quality evidence. |
| REQ | PERF-01 | O(1) lookup/singleton and local O(k) grouped work | 25-01, 25-03 | COVERED | Already verified; this plan preserves it unchanged. |
| REQ | PERF-02 | Source/pure/compiled parity, compiled singleton floor, and durable proof | 25-02, 25-04 | COVERED | 25-04 refreshes the sole stale manifest field pair and reruns the existing strict gates. |
| REQ | DOC-01 | Controller-owned one-tick drone guidance | 25-03 | COVERED | Already verified; this plan preserves it unchanged. |
| RESEARCH | — | Pinned-toolchain baseline regeneration and diff review (Pitfall 5) | 25-04 | COVERED | Exact uv 0.12.6, locked offline execution, whole-manifest semantic diff gate, then read-only check. |
| RESEARCH | — | Complexity, conformance, and drone implementation patterns | 25-01 through 25-03 | COVERED | Already verified and excluded from evidence-only gap changes. |
| CONTEXT | D-01 | Truthful O(1)/O(k) complexity split | 25-01, 25-03 | COVERED | Preserved; no source or guidance edit. |
| CONTEXT | D-02 | Exact three-origin conformance and compiled 200,000 ops/sec floor | 25-02, 25-04 | COVERED | Existing artifact proof is rerun after the evidence refresh. |
| CONTEXT | D-03 | Controller owns policy, FSM, and adapter | 25-03 | COVERED | Already verified; no example edit. |
| CONTEXT | D-04 | One telemetry tick with fact-only guards and post-commit commands | 25-03 | COVERED | Already verified; no runtime or example edit. |

Deferred dynamic priorities, classifiers, schedulers, parallel guards, and runtime candidate mutation remain excluded and do not appear in this plan.

<tasks>

<task type="tracer">
  <name>Task 1: Regenerate and accept only the exact stale baseline counts</name>
  <precondition>The reviewed executable `/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin/uv` exists and its `--version` output matches the semantic prefix `^uv 0[.]12[.]6([[:space:]]|$)`, and `git diff --quiet -- evidence/release-baseline.json` succeeds before the task writes.</precondition>
  <files>evidence/release-baseline.json</files>
  <read_first>
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-VERIFICATION.md` sole actionable gap and observed 1,805/1,799 counts
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-CONTEXT.md` D-02 and the prohibition on runtime semantic changes
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-RESEARCH.md` Pitfall 5 and reviewed uv path
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-02-PLAN.md` Task 3 evidence-write contract
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-02-SUMMARY.md` completed conformance inventory and prior reviewed baseline proof
    - `.github/copilot-instructions.md` uv-only, pure-source, evidence-write, diff-review, and read-only-check rules
    - `Taskfile.yml` `pure-source-check`, `release-baseline-write`, and `release-baseline-check` definitions
    - `evidence/release-baseline.json` `quality_baseline.tests`, `artifact_evidence`, and `toolchain` objects
  </read_first>
  <action>Save `git status --porcelain=v1` to `/private/tmp/fast-fsm-phase25-gap-status-before.txt` and require the tracked baseline itself to have no pre-existing diff. Verify the reviewed executable with `/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin/uv --version | grep -Eq '^uv 0[.]12[.]6([[:space:]]|$)'`; prepend only its directory to `PATH`, set `UV_OFFLINE=1`, and do not authorize a network fallback. Copy the pre-write manifest to `/private/tmp/fast-fsm-phase25-gap-baseline-before.json`, then run `task release-baseline-write` so the existing locked generator—not a hand edit—updates the durable evidence per D-02. Compare the parsed pre-write and generated records through `tools.release_evidence._stable_manifest()`, the repository's own freshness projection: require the old test object to be exactly 1798 collected, 1792 passed, 0 errors, 0 failures, and 6 skips; require the new object to be exactly 1805 collected, 1799 passed, 0 errors, 0 failures, and 6 skips; replace only collected/passed in the projected pre-write object and require exact equality with the projected generated object. Inspect the raw diff and allow additional changes only inside generator-owned non-freshness observations (`measurement_environment`, `performance_contract.observation`, and `pure_source_performance.observations`); reject toolchain, coverage, conformance, wheel, slot-policy, threshold, or other stable drift. Run `git diff --check`; compare the current porcelain status after filtering the one baseline path with the saved initial status so no unrelated path was introduced; then copy the accepted generated bytes to `/private/tmp/fast-fsm-phase25-gap-baseline-approved.json`. If any tool version, offline resolution, source-origin, exact-count, stable-projection, raw-diff, path-scope, or whitespace check fails, restore only the manifest from the task-local pre-write copy and stop with the failure; do not modify source, tests, docs, tasks, dependencies, lockfiles, thresholds, or evidence validation.</action>
  <verify>
    <automated>test -x /private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin/uv &amp;&amp; /private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin/uv --version | grep -Eq '^uv 0[.]12[.]6([[:space:]]|$)' &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 uv run python -c 'import json, pathlib, sys; sys.path.insert(0,"tools"); import release_evidence as r; p=json.loads(pathlib.Path("/private/tmp/fast-fsm-phase25-gap-baseline-before.json").read_text()); n=json.loads(pathlib.Path("evidence/release-baseline.json").read_text()); pt=p["quality_baseline"]["tests"]; nt=n["quality_baseline"]["tests"]; assert pt == {"collected": 1798, "errors": 0, "failed": 0, "passed": 1792, "skipped": 6}; assert nt == {"collected": 1805, "errors": 0, "failed": 0, "passed": 1799, "skipped": 6}; ps=r._stable_manifest(p); ns=r._stable_manifest(n); ps["quality_baseline"]["tests"]["collected"]=1805; ps["quality_baseline"]["tests"]["passed"]=1799; assert ps == ns' &amp;&amp; git diff --check -- evidence/release-baseline.json &amp;&amp; cp evidence/release-baseline.json /private/tmp/fast-fsm-phase25-gap-baseline-approved.json &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 task release-baseline-check &amp;&amp; cmp evidence/release-baseline.json /private/tmp/fast-fsm-phase25-gap-baseline-approved.json</automated>
  </verify>
  <acceptance_criteria>The reviewed pinned offline writer produces exactly the 1,798→1,805 collected and 1,792→1,799 passed stable-field changes, preserves 0 errors, 0 failures, 6 skips, and every other freshness-defining value, limits any additional raw diff to generator-owned environment/timing observations, leaves every other repository path untouched, and passes the read-only freshness check without changing accepted bytes.</acceptance_criteria>
  <done>The stale durable baseline is replaced by one generated, semantically constrained, read-only-verifiable 1,805/1,799 record.</done>
</task>

<task type="auto">
  <name>Task 2: Re-prove release quality and installed artifact contracts offline</name>
  <files>evidence/release-baseline.json</files>
  <read_first>
    - `Taskfile.yml` `release-gate`, `release-installed-artifacts-check`, and `release-installed-performance-check` definitions
    - `.github/copilot-instructions.md` full sequential suite, mypy authority, documentation, clean-origin, and compiled singleton requirements
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-VERIFICATION.md` previously passing quality and artifact spot-checks
    - `evidence/release-baseline.json` regenerated quality, artifact, and toolchain evidence
  </read_first>
  <action>Keep the reviewed uv directory first on `PATH` and `UV_OFFLINE=1` for every child. Run `task release-gate` to re-prove format, lint, blocking mypy, the full sequential sync/async suite, documentation builds, pure-source origin, and baseline freshness. Then run `task release-installed-artifacts-check` to rebuild and compare clean source with fresh installed pure and compiled wheels, followed by `task release-installed-performance-check` to retain the native loader, three-sample median, and 200,000 ops/sec compiled singleton floor per D-02. Confirm `uv --version` still reports 0.12.6, the manifest bytes are unchanged by all read-only checks, and the repository path delta from the task's initial status remains limited to `evidence/release-baseline.json`. Treat an unavailable offline cache, origin mismatch, explicit compiled failure, threshold failure, or quality failure as a blocking result. Do not enable network access, retry with ambient uv, weaken a check, regenerate again, or patch any implementation/policy surface in this gap plan.</action>
  <verify>
    <automated>/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin/uv --version | grep -Eq '^uv 0[.]12[.]6([[:space:]]|$)' &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 task release-gate &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 task release-installed-artifacts-check &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 task release-installed-performance-check &amp;&amp; cmp evidence/release-baseline.json /private/tmp/fast-fsm-phase25-gap-baseline-approved.json &amp;&amp; git diff --check -- evidence/release-baseline.json</automated>
  </verify>
  <acceptance_criteria>With network disabled and uv 0.12.6 selected, the complete release-quality gate, three-origin installed-artifact parity proof, and fresh installed compiled singleton performance proof pass; all read-only checks preserve the reviewed manifest bytes and no broader path changes are introduced.</acceptance_criteria>
  <done>The sole Phase 25 verification gap is closed by fresh durable evidence backed by the unchanged strict quality and artifact contracts.</done>
</task>

</tasks>

<threat_model>
ASVS Level: 1 (opportunistic controls on the baseline-generation and artifact-verification trust boundaries)

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Ambient host toolchain/cache → release evidence writer | An unreviewed uv binary, unlocked dependency resolution, or network fallback could produce non-reproducible evidence. |
| Generated manifest → tracked release policy | A broad regeneration could disguise unrelated toolchain, conformance, performance, or threshold drift as a test-count refresh. |
| Checkout/native artifacts → claimed clean source and installed wheels | A native shadow or wrong install origin could make passing behavior belong to the wrong artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-25-G01 | Tampering | uv/toolchain selection | high | mitigate | Require the exact reviewed uv 0.12.6 executable, prepend only its directory to PATH, keep `uv sync --locked`, and set `UV_OFFLINE=1` for the writer and every verification child. |
| T-25-G02 | Tampering / Repudiation | `evidence/release-baseline.json` | high | mitigate | Generate through the sole writer, retain a pre-write copy, require exact equality through the repository freshness projection after only the two count replacements, inspect raw observation churn, run `git diff --check`, and follow with the read-only freshness check. |
| T-25-G03 | Tampering | clean-source and installed artifact identity | high | mitigate | Reuse pure-source preflight plus the existing neutral-install archive hash, origin, mode, loader, conformance, and native-performance gates without changing their implementation or thresholds. |
| T-25-G04 | Denial of Service | offline dependency/build cache | medium | mitigate | Fail closed when locked inputs are unavailable offline and report the missing cache input; never broaden authority to network resolution or substitute an ambient toolchain. |
| T-25-SC | Tampering | locally cached package inputs | high | mitigate | Install no new dependency; use only `uv.lock`-pinned existing project groups through reviewed uv 0.12.6 in offline mode, preserving the completed research package audit and lockfile bytes. |

No authentication, session, or access-control surface is introduced. ASVS V5-style fail-closed validation applies to the generated JSON, artifact origins, exact counts, and path scope.
</threat_model>

<verification>
1. Assert exact uv 0.12.6 selection, `UV_OFFLINE=1`, a clean starting manifest path, and the expected stale 1,798/1,792 values.
2. Run the existing baseline writer once, accept only the exact 1,805/1,799 count update, and reject every other semantic or path change.
3. Run the read-only baseline freshness check and confirm it does not mutate the reviewed manifest.
4. Run the full release-quality aggregate plus installed pure/compiled parity and compiled singleton performance proof under the same pinned offline environment.
5. Confirm the final repository delta remains evidence-only and all strict thresholds, origins, schemas, and validators are unchanged.
</verification>

<success_criteria>
- `evidence/release-baseline.json` contains collected 1,805, passed 1,799, errors 0, failed 0, and skipped 6 from the reviewed generator.
- The stable freshness projection differs only at `quality_baseline.tests.collected` and `quality_baseline.tests.passed`; any additional raw diff is confined to generator-owned environment/timing observation fields.
- `task release-baseline-check`, `task release-gate`, `task release-installed-artifacts-check`, and `task release-installed-performance-check` pass with uv 0.12.6 and `UV_OFFLINE=1`.
- No runtime semantics, quality thresholds, validation logic, dependency metadata, or repository path outside `evidence/release-baseline.json` changes.
</success_criteria>

<output>
Create `.planning/phases/25-performance-artifact-proof-drone-guidance/25-04-SUMMARY.md` when done.
</output>
