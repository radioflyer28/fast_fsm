---
phase: 19-bounded-diagnostics-safe-output
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - src/fast_fsm/_diagnostics.py
  - src/fast_fsm/validation.py
  - src/fast_fsm/__init__.py
  - tests/test_diagnostic_contracts.py
autonomous: true
requirements: [DIAG-01, DIAG-04, DIAG-05, DIAG-06, DIAG-07, DIAG-08]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01, D-02, D-05, and D-06, one public validator call captures one immutable graph snapshot, starts reachability at the declared initial state, shares one counted budget, and exposes complete/exhausted status without rereading live topology."
    - "The exact-limit reachability case succeeds and the one-less limit fails before the next counted operation with only deterministic stage, dimension, and counts in the failure boundary."
    - "Every remaining DIAG-04 through DIAG-08 behavior has a real-machine strict-RED row assigned to exactly one later plan before broader production work begins."
    - statement: "[EDGE-PROBE BACKSTOP] The tracer keeps core trigger/can_trigger/add_state/add_transition dispatch free of diagnostic traversal and allocation."
      verification: backstop
  artifacts:
    - path: "src/fast_fsm/_diagnostics.py"
      provides: "Dependency-free scalar graph projection, public limits/status/error contract, and counted reachability tracer"
    - path: "src/fast_fsm/core.py"
      provides: "Ownership-consistent deeply stable _GraphSnapshot scalar fields"
    - path: "src/fast_fsm/validation.py"
      provides: "Snapshot-backed FSMValidator tracer with declared-initial reachability"
    - path: "tests/test_diagnostic_contracts.py"
      provides: "Strict-RED diagnostic contract matrix and exact-budget helper"
  key_links:
    - from: "FSMValidator.__init__"
      to: "StateMachine._graph_snapshot"
      via: "one capture passed to _DiagnosticGraph"
      pattern: "_graph_snapshot"
    - from: "FSMValidator.get_reachable_states"
      to: "_DiagnosticBudget"
      via: "reserve-before-visit and reserve-before-edge accounting"
      pattern: "max_work|work_count"
    - from: "tests/test_diagnostic_contracts.py"
      to: "public validator behavior"
      via: "moved-current real machine and exact/one-less limits"
      pattern: "initial|budget|snapshot"
  prohibitions:
    - "[UNVERIFIED - no SPEC] Do not let a diagnostic, renderer, or logging configuration call mutate machine runtime state, topology, history, callback registries, or application-owned logging objects beyond explicitly reversible library-owned configuration."
    - "[UNVERIFIED - no SPEC] Do not let enabling diagnostics or trace logging turn caller payloads, exception text, or object representations into durable output outside the explicit custom-redactor boundary."
    - "[UNVERIFIED - no SPEC] Do not present exhausted or incomplete diagnostic work as complete evidence and do not hide truncation behind a legacy return shape."
---

<objective>
Prove the production diagnostic tracer and stage every remaining graph contract as strict RED.

Purpose: Resolve the hardest architectural constraint first: one ownership-consistent snapshot and one deterministic budget must survive a real public validator call before SCC, comparison, JSON, or renderer expansion can safely build on it. This implements D-01, D-02, D-05, D-06, and the DIAG-01/DIAG-07/DIAG-08 tracer.
Output: Scalar snapshot fields, the internal diagnostics module and public budget/status/error types, one end-to-end declared-initial reachability path, and the Phase 19 diagnostic strict-RED matrix.
</objective>

## Artifacts this phase produces

- New file `src/fast_fsm/_diagnostics.py` with `DiagnosticLimits`, `DiagnosticStatus`, `DiagnosticBudgetExceeded`, `_DiagnosticEdge`, `_DiagnosticGraph`, `_DiagnosticBudget`, `_graph_from_snapshot()`, `_reachable_indices()`, `_strongly_connected_components()`, `_structural_depth()`, `_sparse_adjacency()`, `_dense_adjacency()`, and `_generate_paths()`.
- `DiagnosticLimits` fields: `max_work`, `max_results`, `max_dense_cells`, `max_path_expansions`; `DiagnosticStatus` fields: `complete`, `exhausted_dimension`, `exhausted_stage`, `work_count`, `result_count`, `dense_cell_count`, `path_expansion_count`; `DiagnosticBudgetExceeded.status` carries the redacted status record.
- Additive `_GraphSnapshot` scalar fields `initial_state_name`, `current_state_name`, and `state_names`; additive `_GraphTransition` scalar fields `from_state_name`, `to_state_name`, and `condition_name`. Existing identity-bearing fields remain available.
- New public logging symbols created later in the phase: `FSMTraceEvent`, `FSMTraceRedactor`, and `FSMLoggingHandle`; new private logging symbols `_FSMStreamHandler`, `_emit_fsm_trace()`, and `_next_fsm_logging_generation()`.
- New visualization helpers created later: `_opaque_state_ids()`, `_escape_mermaid_text()`, `_escape_plantuml_text()`, `_escape_markdown_heading()`, `_escape_markdown_cell()`, `_to_mermaid_from_snapshot()`, `_to_plantuml_from_snapshot()`, `_to_json_from_snapshot()`, `_to_mermaid_fenced_from_snapshot()`, and `_to_mermaid_document_from_snapshot()`.
- New files `tests/test_diagnostic_contracts.py`, `tests/test_output_safety.py`, `.specify/decisions/ADR-006-bounded-diagnostics-safe-output.md`, and `.planning/phases/19-bounded-diagnostics-safe-output/19-PERFORMANCE-EVIDENCE.md`; new `PHASE19_INVENTORY` and `--suite phase19` verifier surfaces.
- Existing `compare_fsms()` and `batch_validate()` gain ordered positional-entry schemas; validator, rendering, JSON, dense-output, path, and logging APIs gain additive keyword-only limit/redactor/propagation controls. No runtime dependency, public topology serialization format, scheduler, or installed-artifact contract is added.

## Edge Probe Resolution and Flagged Assumptions

The spec-less edge probe surfaced exactly 22 questions. Each row below is one
auditable probe, not a grouped summary. `AC-N` means the Nth bullet in the named
task's `<acceptance_criteria>` block; the quoted text is the exact criterion the
executor must satisfy.

| # | Requirement | Category | Atomic probe question | Classification and exact acceptance mapping |
|---|-------------|----------|-----------------------|---------------------------------------------|
| EP-01 | DIAG-01 | reachability origin | After the machine moves, does structural reachability still start at the declared initial state? | RESOLVED — `19-01 Task 1 AC-2`: “A moved-current machine reports reachability from `_GraphSnapshot.initial_state_name` and reports `_GraphSnapshot.current_state_name` separately.” |
| EP-02 | DIAG-02 | duplicate identity | Do comparison and batch preserve the cardinality, input order, and positional identity of same-name machines? | RESOLVED — `19-04 Task 1 AC-1`: “Duplicate-name input cardinality, order, identity, tied ranking, and per-entry status tests pass for comparison and batch.” |
| EP-03 | DIAG-02 | equality semantics | When scores tie, is ranking order determined by ascending input position rather than by name or hash order? | RESOLVED — `19-04 Task 1 AC-1`: “Duplicate-name input cardinality, order, identity, tied ranking, and per-entry status tests pass for comparison and batch.” |
| EP-04 | DIAG-03 | empty comparison | Does zero-input comparison return the exact empty schema without division? | RESOLVED — `19-04 Task 1 AC-2`: “Zero input returns exact D-04 fields: empty entries/rankings, no best, zero count/issues, and `None` average/range.” |
| EP-05 | DIAG-04 | empty/acyclic cycle membership | Do empty and acyclic graphs report no cyclic members? | RESOLVED — `19-03 Task 1 AC-1`: “Self-loop, three-cycle, overlapping/multiple SCC, tail, empty, acyclic, and recursion-depth-exceeding chain tests pass.” |
| EP-06 | DIAG-04 | self-loop membership | Is a one-state self-loop classified as a cyclic SCC member? | RESOLVED — `19-03 Task 1 AC-1`: “Self-loop, three-cycle, overlapping/multiple SCC, tail, empty, acyclic, and recursion-depth-exceeding chain tests pass.” |
| EP-07 | DIAG-04 | multi-SCC membership | Are all and only members of every multi-state cyclic SCC returned once in snapshot order, excluding acyclic tails? | RESOLVED — `19-03 Task 1 AC-2`: “Every cyclic member appears once in snapshot order; no acyclic tail appears.” |
| EP-08 | DIAG-05 | DAG depth | Does a long acyclic graph report exact edge depth without recursion? | RESOLVED — `19-03 Task 1 AC-3`: “Depth result carries exactly one documented interpretation and matches the DAG/condensation expected edge count.” |
| EP-09 | DIAG-05 | cyclic depth | Does a cyclic graph report condensation-DAG depth with the documented interpretation rather than claim an exact simple path? | RESOLVED — `19-03 Task 1 AC-3`: “Depth result carries exactly one documented interpretation and matches the DAG/condensation expected edge count.” |
| EP-10 | DIAG-06 | sparse default | Does a many-state zero-edge graph remain O(V+E) in stored rows without dense allocation? | RESOLVED — `19-03 Task 2 AC-1`: “Sparse default on a many-state zero-edge graph has O(V+E) stored rows and no dense outer allocation.” |
| EP-11 | DIAG-06 | dense compatibility | Do dense transition and adjacency outputs reserve the exact cell product before allocating the outer structure? | RESOLVED — `19-03 Task 2 AC-2`: “Dense exact-cell limits succeed and one-less limits raise before any matrix/list allocation, proven by an allocation sentinel.” |
| EP-12 | DIAG-06 | generated paths | Are path length, result count, and expansion work independently bounded and distinguishable? | RESOLVED — `19-03 Task 2 AC-3`: “Path length, count, and expansion limits have distinct deterministic terminal statuses and never silently truncate a legacy list.” |
| EP-13 | DIAG-07 | exact exhaustion | For every budget dimension, does the exact limit pass and one-less fail before the forbidden operation with no partial legacy value? | RESOLVED — `19-01 Task 2 AC-3`: “Budget cases cover graph work, result count, dense cells, path expansions, and aggregate top-level sharing with exact/pass and one-less/exhaust boundaries.” |
| EP-14 | DIAG-08 | snapshot consistency | Does each top-level call capture exactly once and remain immune to post-capture mutation or live-dictionary rereads? | RESOLVED — `19-01 Task 1 AC-3`: “Snapshot-call instrumentation records exactly one call for the tracer, and mutation after capture cannot change its state/edge/condition strings.” |
| EP-15 | OUT-01 | identifier collisions | Do empty, punctuation-colliding, and Unicode labels receive distinct snapshot-position opaque IDs? | RESOLVED — `19-06 Task 1 AC-1`: “Empty, punctuation-colliding, Unicode, control, directive, comment, URL, quote, and fence corpus cases pass for both grammars.” |
| EP-16 | OUT-02 | Mermaid containment | Can hostile state, trigger, condition, or title text create a Mermaid line, comment, alias, directive, link, or delimiter? | RESOLVED — `19-06 Task 1 AC-3`: “Caller-controlled text cannot change physical line count or introduce active grammar tokens; repeated same-snapshot bytes match.” |
| EP-17 | OUT-02 | PlantUML containment | Can hostile state, trigger, condition, or title text create a PlantUML line, directive, alias, URL/image, or terminator? | RESOLVED — `19-06 Task 1 AC-3`: “Caller-controlled text cannot change physical line count or introduce active grammar tokens; repeated same-snapshot bytes match.” |
| EP-18 | OUT-02 | Markdown containment | Can hostile heading, cell, event, or fence text escape its Markdown data position or create another physical line? | RESOLVED — `19-06 Task 2 AC-3`: “Markdown hostile headings/cells/events/fences remain structurally contained on one encoded line.” |
| EP-19 | OUT-03 | default trace disclosure | Can default sync/async trace expose any trigger, state, argument, keyword value, exception, or representation sentinel through a complete `LogRecord` or handler? | RESOLVED — `19-05 Task 1 AC-1`: “Full-record sentinel tests pass for sync/async success, guard failure, callback failure, committed failure, and cancellation/fixed terminal categories.” |
| EP-20 | OUT-04 | redactor boundary | Does an explicit redactor receive only documented fields and fail closed on raising, invalid, or oversized output without raw fallback? | RESOLVED — `19-05 Task 1 AC-3`: “Invalid/raising/oversized redactor output produces only `redaction_failure` or no event and no sentinel reaches a handler.” |
| EP-21 | OUT-05 | application ownership | Do application handlers, filters, formatters, levels, ordering, and open state survive library configuration and restore? | RESOLVED — `19-05 Task 2 AC-1`: “Application handler/filter/formatter identity, ordering, levels, closed state, and output are byte-for-byte unchanged by configuration and every restore order.” |
| EP-22 | OUT-05 | reversible generations | Do repeated configuration, explicit/default propagation, idempotent restore, and out-of-order handles affect only the current library-owned generation? | RESOLVED — `19-05 Task 2 AC-2`: “Repeated calls leave one current marked handler; replaced owned handlers close; old handles cannot affect current generation.” |

**Arithmetic reconciliation:** `22 surfaced = 22 resolved + 0 flagged + 0 dropped`.

- **[FLAGGED ASSUMPTION A-19-01 — implementation evidence, not a dropped probe]** Exact numeric `DiagnosticLimits` defaults cannot be selected before counted-work fixtures exist. Plan 19-03 must calibrate against ordinary repository examples, commit explicit finite values with headroom, and pin those values in tests and docs; it may not leave dynamic or time-based defaults.
- **[FLAGGED ASSUMPTION A-19-02 — environment capability, not a dropped probe]** Mermaid and PlantUML CLIs are unavailable locally. Plan 19-06 treats structural source containment and deterministic bytes as required evidence; an optional parser smoke test may supplement but never replace that evidence.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-CONTEXT.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-RESEARCH.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-PATTERNS.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-VALIDATION.md
@.planning/phases/18-safe-ownership-concurrency/18-08-SUMMARY.md

<interfaces>
- Existing `_GraphSnapshot(name, initial_state, graph_version, states, transitions)` and `_GraphTransition(from_state, trigger, to_state, condition)` are frozen slot dataclasses in `src/fast_fsm/core.py`; additions must preserve these fields and identity semantics.
- Existing `FSMValidator(fsm, *, name=None)` stores `fsm`, `states`, `transitions`, `events`, `initial_state`, and `_report_name`; constructor/name compatibility remains available.
- `Condition` has slot-backed string fields `name` and `description`; snapshot capture uses those declared strings and never falls back to `str()` or `repr()`.
- Public exports are explicit imports plus `__all__` entries in `src/fast_fsm/__init__.py`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry declared-initial reachability through one snapshot and one budget</name>
  <reversibility rating="costly">D-06 publishes completion and exhaustion semantics that later callers will coordinate around.</reversibility>
  <files>src/fast_fsm/core.py, src/fast_fsm/_diagnostics.py, src/fast_fsm/validation.py, src/fast_fsm/__init__.py, tests/test_diagnostic_contracts.py</files>
  <read_first>src/fast_fsm/core.py (`_GraphSnapshot`, `_graph_snapshot()`, Phase 18 ownership helpers); src/fast_fsm/conditions.py (`Condition.name`/`description`); src/fast_fsm/validation.py (`FSMValidator` constructor and reachability); src/fast_fsm/__init__.py; tests/test_graph_invariants.py; tests/test_validation.py; 19-CONTEXT.md D-01/D-02/D-05/D-06/D-10; 19-VALIDATION.md rows 19-01-01 and deterministic budget matrix</read_first>
  <behavior>
    - Moving a real machine away from its declared initial state does not change unreachable-state analysis; current state is separately reported.
    - One `FSMValidator` construction invokes `_graph_snapshot()` exactly once and later reachability performs no `_states`, `_transitions`, `current_state`, or graph-version read.
    - A high budget reveals the deterministic required work count; that exact limit succeeds and one less raises `DiagnosticBudgetExceeded` before the next operation with a fixed redacted message and structured status.
    - Snapshot scalar labels stay unchanged after post-capture state/condition mutation, and ordinary empty/single-state machines return stable ordered results.
  </behavior>
  <action>Start with unmarked failing tests for the tracer behaviors and run them before production edits. Per D-01 and D-02, extend the existing frozen snapshot records additively with scalar names captured inside an ownership-consistent read boundary; retain all Phase 16 identity fields and ensure callback-local capture cannot deadlock or observe a half-write. Create dependency-free `src/fast_fsm/_diagnostics.py`; define the exact public names and fields listed under Artifacts, keep `_DiagnosticGraph` tuple-backed and snapshot ordered, and make `_DiagnosticBudget` reserve work/results/cells/expansions before the corresponding operation. Per D-05, D-06, and D-10, accept `limits: DiagnosticLimits | None` only as an additive keyword-only constructor option, use a finite explicit default object, expose `DiagnosticStatus`, and make the legacy set-returning reachability boundary raise the fixed redacted `DiagnosticBudgetExceeded` rather than return partial data. Migrate only the `FSMValidator` reachability tracer in this task; preserve every existing public constructor and callable name, export only `DiagnosticLimits`, `DiagnosticStatus`, and `DiagnosticBudgetExceeded`, add no runtime dependency, do not import `_diagnostics` from compiled `core.py`, and add no diagnostic work to trigger/can-trigger/topology-mutator hot paths.</action>
  <acceptance_criteria>
    - `tests/test_diagnostic_contracts.py -k 'initial or snapshot or budget'` first fails on the intended old behavior and then passes.
    - A moved-current machine reports reachability from `_GraphSnapshot.initial_state_name` and reports `_GraphSnapshot.current_state_name` separately.
    - Snapshot-call instrumentation records exactly one call for the tracer, and mutation after capture cannot change its state/edge/condition strings.
    - Exact `max_work` succeeds; `max_work - 1` raises `DiagnosticBudgetExceeded` before the forbidden visit/examination and exposes no caller label or payload in `str(exc)`.
    - `src/fast_fsm/core.py` has no import of `fast_fsm._diagnostics` or `. _diagnostics`, and package metadata/runtime dependencies are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include src/fast_fsm/_diagnostics.py --include src/fast_fsm/validation.py --include src/fast_fsm/__init__.py --include tests/test_diagnostic_contracts.py -- uv run pytest tests/test_diagnostic_contracts.py tests/test_graph_invariants.py tests/test_validation.py -x -q -k 'initial or snapshot or budget'</automated>
  </verify>
  <done>DIAG-01 and the DIAG-07/DIAG-08 tracer work end to end through a single deeply stable snapshot and deterministic reserve-before-work budget, with public types exported and no hot-path or dependency expansion.</done>
</task>

<task type="auto">
  <name>Task 2: Stage the complete graph diagnostic strict-RED matrix</name>
  <files>tests/test_diagnostic_contracts.py</files>
  <read_first>tests/test_diagnostic_contracts.py from Task 1; tests/test_validation.py; tests/test_hypothesis.py; tests/test_graph_invariants.py; 19-VALIDATION.md Wave 0 Requirements, Strict-RED Removal Protocol, and Deterministic Budget Boundary Matrix; 19-CONTEXT.md D-03 through D-10</read_first>
  <action>Add real-machine fixtures for empty, single-state, moved-current, self-loop, three-cycle, multiple cyclic SCCs with tails, a long chain beyond recursion depth, high fan-out DAG, many-state zero-edge sparse graph, duplicate names, a snapshot-call counter, and barrier-controlled post-capture mutation. Add one reusable exact-budget helper that measures required work under a generous limit, proves exact success, then proves one-less exhaustion before a supplied forbidden operation. Keep the implemented tracer rows active. Mark every not-yet-implemented SCC/depth/sparse/dense/path row with `xfail(strict=True, reason="RED until 19-03")`, comparison/report rows with owner `19-04`, and renderer/JSON snapshot rows with owner `19-06`; an XPASS must fail. Run repeated cases under two `PYTHONHASHSEED` subprocess values and assert identical counts and ordering. Do not weaken legacy tests or fabricate production behavior in fixtures.</action>
  <acceptance_criteria>
    - The fixture inventory contains every Wave 0 graph listed in 19-VALIDATION.md.
    - Each unimplemented row has exactly one strict-XFAIL owner among 19-03, 19-04, and 19-06; implemented tracer rows are unmarked and green.
    - Budget cases cover graph work, result count, dense cells, path expansions, and aggregate top-level sharing with exact/pass and one-less/exhaust boundaries.
    - Two hash seeds produce identical counts, SCC/member order, comparison order, sparse rows, and path order.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_diagnostic_contracts.py -x -q</automated>
  </verify>
  <done>The complete DIAG-01 through DIAG-08 adversarial matrix exists, tracer rows pass, all remaining rows are strict RED with one owner, and no edge-probe item is silently omitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutable machine → diagnostic snapshot | Concurrent or callback-local reads must not mix topology versions or invoke caller representations. |
| diagnostic limits → graph traversal | Caller-selected or default budgets control work over potentially adversarial graphs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | `_graph_snapshot()` / `_DiagnosticGraph` | high | mitigate | Capture scalar topology once under the Phase 18 ownership boundary, preserve snapshot order/version, and prohibit nested live reads with call-count and barrier tests. |
| T-19-02 | Denial of Service | `_DiagnosticBudget` / reachability | high | mitigate | Finite default limits, keyword-only overrides, reserve-before-work accounting, and exact/one-less exhaustion tests. |
| T-19-03 | Information Disclosure | `DiagnosticBudgetExceeded` | medium | mitigate | Fixed exception message plus bounded stage/dimension/count status; no labels, payloads, exception causes, `str`, or `repr`. |
| T-19-04 | Repudiation | deterministic counters/order | medium | mitigate | Snapshot indices and two-hash-seed exact count/order assertions make diagnostic evidence reproducible. |
</threat_model>

<verification>
- Run the tracer from an asserted pure temporary tree and confirm the selected legacy graph/validation tests remain green.
- Confirm later rows remain strict XFAIL, never unmarked failures or XPASS.
- Confirm no package file changed and `core.py` retains the single mypyc boundary without importing diagnostics.
</verification>

<success_criteria>
- A real public validator path proves the phase architecture end to end before expansion.
- Declared-initial reachability, one-snapshot capture, immutable scalar labels, and explicit counted exhaustion are executable facts.
- Every later graph/API/output behavior has a strict-RED owner and exact automated command.
</success_criteria>

<output>
Create `.planning/phases/19-bounded-diagnostics-safe-output/19-01-SUMMARY.md` when done.
</output>
