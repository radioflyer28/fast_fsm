---
phase: 19-bounded-diagnostics-safe-output
plan: 07
type: execute
wave: 4
depends_on: [19-04, 19-05, 19-06]
files_modified:
  - README.md
  - docs/api/validation.md
  - docs/api/visualization.md
  - docs/api/core.md
  - docs/dev/architecture.md
  - docs/dev/testing.md
  - .specify/decisions/ADR-006-bounded-diagnostics-safe-output.md
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04, DIAG-05, DIAG-06, DIAG-07, DIAG-08, OUT-01, OUT-02, OUT-03, OUT-04, OUT-05]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "README and public API documentation state D-01 through D-17 exactly: snapshot-per-call, declared initial/current separation, positional duplicate/empty schemas, every limit/default/count, completion/error boundary, SCC/condensation meaning, sparse/dense/path complexity, target encoders, trace/redactor safety, handler ownership, propagation, and restore."
    - "Maintainer architecture and testing docs show diagnostics outside O(1) core operations, one capture/shared graph/shared budget flow, strict-RED ownership, and the asserted pure/fresh-native Phase 19 gate while leaving installed artifacts to Phase 20."
    - "ADR-006 records the costly public schema, exhaustion, encoding, and logging decisions plus rejected alternatives and explicit FUTR-05/Phase 20 deferrals."
    - "The three living SPRs already contain the exact shipped signatures/fields from their owning implementation-task commits; this plan verifies them read-only and does not defer or repeat their content updates."
  artifacts:
    - {path: "README.md", provides: "Consumer-facing Phase 19 diagnostic, positional schema, safe rendering, trace redaction, and reversible logging contract"}
    - {path: "docs/api/validation.md", provides: "Diagnostic limits, schemas, algorithms, status, and complexity contract"}
    - {path: "docs/api/visualization.md", provides: "Opaque IDs, grammar encoding, JSON/Markdown snapshot and dense-output contract"}
    - {path: "docs/api/core.md", provides: "Trace redactor and reversible logging ownership contract"}
    - {path: ".specify/decisions/ADR-006-bounded-diagnostics-safe-output.md", provides: "Accepted durable cross-module Phase 19 decision record"}
  key_links:
    - {from: "README and public docs", to: "shipped signatures/status fields/default constants", via: "consumer summary, autodoc, and exact prose/table contract", pattern: "DiagnosticLimits|DiagnosticStatus|FSMLoggingHandle"}
    - {from: "architecture/testing docs", to: "phase19 verifier", via: "one-capture flow and exact pure/compiled commands", pattern: "phase19|snapshot|budget"}
    - {from: "ADR/SPR records", to: "D-01 through D-17 implementation", via: "accepted decision and living contract summaries", pattern: "SCC|condensation|redactor|propagation"}
---

<objective>
Publish and preserve the complete bounded-diagnostics, safe-output, and logging contract.

Purpose: Make every implemented decision discoverable to users and maintainers, including exact schemas, limits, complexity, security boundaries, reversibility, and Phase 20 exclusions.
Output: README consumer guidance, three API pages, architecture/testing updates, read-only verification of the already-current SPRs, and accepted ADR-006.
</objective>

## Artifacts this phase produces

- New file `.specify/decisions/ADR-006-bounded-diagnostics-safe-output.md`; no other new symbol or file is created by this plan.
- Updates `README.md` with the consumer-facing Phase 19 contract for bounded diagnostics, positional comparison/batch results, safe rendering, metadata-only trace redaction, and reversible logging while preserving its existing tested examples unless Task 1 explicitly updates and validates them.
- Documents every new phase-wide symbol and field from 19-01/19-04/19-05/19-06: `DiagnosticLimits`, `DiagnosticStatus`, `DiagnosticBudgetExceeded`, `FSMTraceEvent`, `FSMTraceRedactor`, `FSMLoggingHandle`, their declared fields, ordered comparison/batch fields, structured validation/JSON fields, keyword-only controls, and private `_diagnostics.py`/renderer/logging seams.
- Records exact calibrated default limit values from 19-03, not placeholders or environment-dependent estimates.
- Verifies `.specify/memory/spr-core-api.md`, `.specify/memory/spr-validation.md`, and `.specify/memory/spr-visualization.md` against shipped source/tests without modifying them; their content changes belong to the exact 19-01/19-03/19-04/19-05/19-06 implementation-task commits that made them current.
- Does not claim a public topology snapshot format, installed wheel/sdist parity, hosted evidence, a logging backend, automatic offload, or TEST-07 completion.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-CONTEXT.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-RESEARCH.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-PATTERNS.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-VALIDATION.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-04-SUMMARY.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-05-SUMMARY.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-06-SUMMARY.md
@.specify/decisions/ADR-005-safe-ownership-concurrency.md
@.specify/memory/spr-core-api.md
@.specify/memory/spr-validation.md
@.specify/memory/spr-visualization.md

<interfaces>
- Public APIs and exact fields are implementation outputs of Plans 19-04 through 19-06; docs must read those source files and tests rather than copying provisional names blindly.
- ADRs are append-only with status/date/context/decision/alternatives/consequences/deferred sections; SPRs are living source files already updated in their owning implementation commits and are read-only inputs to this plan.
- `docs/api/*.md` uses MyST/Sphinx autodoc and Google-style signatures; exact observations belong in evidence, while durable complexity/floor policy belongs in prose.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document public diagnostic, output, and logging APIs</name>
  <files>README.md, docs/api/validation.md, docs/api/visualization.md, docs/api/core.md</files>
  <read_first>src/fast_fsm/_diagnostics.py; src/fast_fsm/validation.py; src/fast_fsm/visualization.py; src/fast_fsm/core.py logging types/helpers; README.md; tests/test_readme_examples.py; docs/api/validation.md; docs/api/visualization.md; docs/api/core.md; 19-CONTEXT.md D-01 through D-17</read_first>
  <action>Own and update `README.md` alongside the detailed API pages. In the README, give users a concise but complete Phase 19 contract: the new diagnostic limits, status, counted-budget, completion, and exhaustion semantics; position-safe comparison/batch entries with duplicate preservation, stable ordering, and zero-machine `None` aggregates; collision-free opaque IDs plus grammar-specific Mermaid, PlantUML, and Markdown rendering safety; metadata-only trace defaults and the custom fail-closed bounded redactor; and reversible logging configuration, application-owned handler preservation, propagation choices, handle generations, and restore behavior. Keep existing README executable examples unchanged unless the documentation needs an example to explain the new contract; if any README example changes, make it executable by the existing `tests/test_readme_examples.py` harness and keep that test in the automated gate. In the API pages, document the shipped signatures and exact calibrated defaults for `DiagnosticLimits`; every `DiagnosticStatus` field/count unit; structured versus legacy exhaustion; declared initial versus current; positional comparison/batch entries, stable ties, and the exact zero schema; SCC membership and DAG/condensation depth interpretation; sparse default plus dense/path opt-in complexity/preflight; and one-snapshot behavior. Document opaque snapshot-order IDs and separate Mermaid/PlantUML/Markdown encoding guarantees for every caller-controlled sink, JSON/document ordering and dense controls, and stale adjacency rejection. Document default metadata-only trace fields, the exact minimum `FSMTraceEvent`, `FSMTraceRedactor` input/output allowlist/bounds/failure behavior, application handler ownership, deliberate/default-preserved propagation, handle generation/restore semantics, and setter delegation. State no new runtime dependency and do not promise parser validation, exact cyclic simple paths, public topology serialization, installed-artifact parity, or hidden partial output.</action>
  <acceptance_criteria>
    - README names `DiagnosticLimits`, `DiagnosticStatus`, and `DiagnosticBudgetExceeded` and explains finite counted budgets, completion, and exhaustion without implying elapsed-time enforcement.
    - README describes positional duplicate-safe comparison/batch entries and explicitly states that zero-machine aggregates are `None`.
    - README describes collision-free opaque IDs and separate Mermaid/PlantUML/Markdown escaping guarantees.
    - README states metadata-only trace defaults, custom redactor fail-closed behavior, application-owned handlers, propagation control, generation-scoped handles, and reversible restore.
    - API pages contain every shipped public signature, field, default, return schema, error/status condition, and complexity class.
    - Empty/duplicate comparison values and all four budget dimensions are stated exactly.
    - Every renderer/logging caller-controlled field and fail-closed boundary is named.
    - Sphinx HTML warnings-as-errors and doctests pass.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'DiagnosticLimits' README.md &amp;&amp; rg -n 'DiagnosticStatus' README.md &amp;&amp; rg -n 'DiagnosticBudgetExceeded' README.md &amp;&amp; rg -n 'budget' README.md &amp;&amp; rg -n 'complet' README.md &amp;&amp; rg -n 'exhaust' README.md &amp;&amp; rg -n 'comparison' README.md &amp;&amp; rg -n 'batch' README.md &amp;&amp; rg -n 'position' README.md &amp;&amp; rg -n 'duplicate' README.md &amp;&amp; rg -n 'zero[- ]machine.{0,120}None|None.{0,120}zero[- ]machine' README.md &amp;&amp; rg -n 'opaque' README.md &amp;&amp; rg -n 'collision' README.md &amp;&amp; rg -n 'Mermaid' README.md &amp;&amp; rg -n 'PlantUML' README.md &amp;&amp; rg -n 'Markdown' README.md &amp;&amp; rg -n 'metadata[- ]only' README.md &amp;&amp; rg -n 'fail[- ]closed' README.md &amp;&amp; rg -n 'redactor' README.md &amp;&amp; rg -n 'handler' README.md &amp;&amp; rg -n 'propagat' README.md &amp;&amp; rg -n 'generation' README.md &amp;&amp; rg -n 'restore' README.md &amp;&amp; uv run pytest tests/test_readme_examples.py -x -q &amp;&amp; uv run sphinx-build -b html docs docs/_build/html -W --keep-going &amp;&amp; uv run sphinx-build -b doctest docs docs/_build/doctest</automated>
  </verify>
  <done>README and API users can implement against every Phase 19 schema, budget, encoding, redaction, ownership, propagation, and restore contract without reading private code, and README examples remain covered by their established test harness.</done>
</task>

<task type="auto">
  <name>Task 2: Record maintainer architecture, validation protocol, and ADR-006</name>
  <files>docs/dev/architecture.md, docs/dev/testing.md, .specify/decisions/ADR-006-bounded-diagnostics-safe-output.md</files>
  <read_first>docs/dev/architecture.md; docs/dev/testing.md; .specify/decisions/ADR-005-safe-ownership-concurrency.md; .specify/memory/spr-core-api.md; .specify/memory/spr-validation.md; .specify/memory/spr-visualization.md; shipped source/tests from Plans 19-01 and 19-03 through 19-06; tools/phase16_isolated_verify.py phase19 suite; .github/copilot-instructions.md SPR anti-poisoning rules; 19-VALIDATION.md; 19-CONTEXT.md; 19-RESEARCH.md Architectural Responsibility Map</read_first>
  <action>Extend the import/ownership diagram with interpreted `_diagnostics.py` and the exact top-level capture to scalar graph/shared budget to validation/JSON/renderer flow; state that compiled core only supplies capture and guarded trace/logging seams and all diagnostics remain off O(1) trigger/can-trigger/add-state/add-transition paths. Document strict-RED owner removal, exact counted-work boundary methodology, two-hash-seed determinism, hostile output/logging matrix, asserted pure/fresh-native origins, slots/type/docs/performance gates, and local `--suite phase19`; explicitly assign installed artifact matrix/publication to Phase 20. Create accepted ADR-006 covering D-01 through D-17, the costly positional/empty/status/encoding/logging contracts, iterative SCC/condensation, sparse-first dense preflight, metadata-only logging, fail-closed redaction, handler generations, rejected recursive/enumerating/dense-default/generic-sanitizer/handler-clearing/raw-log alternatives, and FUTR-05/Phase 20 deferrals. Before committing Task 2, verify the already-current core, validation, and visualization SPRs against the exact shipped identifiers/defaults and regression tests, and require `git diff --exit-code` for those three paths so this documentation/ADR task neither edits them nor postpones an implementation-owned memory update.</action>
  <acceptance_criteria>
    - Architecture diagram has no reverse import from `core.py` to diagnostics/validation/visualization and identifies one mypyc unit.
    - Testing guide names every focused/final gate and distinguishes exact counters from environment-labelled timing.
    - ADR-006 status is `Accepted`, cites D-01 through D-17 semantics, and lists alternatives/consequences/deferred scope.
    - No hosted or installed-artifact evidence is claimed for Phase 19.
    - All three SPRs contain their exact shipped Phase 19 identifiers/defaults and have no Task 2 diff; any mismatch fails this task and must be corrected in the implementation task/commit that owns the stale contract.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_mypyc_guard.py tests/test_validation.py tests/test_visualization.py tests/test_logging_config.py -x -q &amp;&amp; rg -n 'DiagnosticLimits|FSMTraceEvent|FSMLoggingHandle' .specify/memory/spr-core-api.md &amp;&amp; rg -n 'DiagnosticStatus|condensation_dag_depth|position|max_dense_cells' .specify/memory/spr-validation.md &amp;&amp; rg -n '_opaque_state_ids|_escape_mermaid_text|_escape_plantuml_text|diagnostic_status' .specify/memory/spr-visualization.md &amp;&amp; git diff --exit-code -- .specify/memory/spr-core-api.md .specify/memory/spr-validation.md .specify/memory/spr-visualization.md &amp;&amp; uv run sphinx-build -b html docs docs/_build/html -W --keep-going &amp;&amp; uv run sphinx-build -b doctest docs docs/_build/doctest &amp;&amp; git diff --check</automated>
  </verify>
  <done>Maintainers have an auditable architecture/test protocol and accepted ADR for every lasting Phase 19 choice and boundary, and the implementation-owned SPRs are proven current without a Plan 19-07 content change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation → public/maintainer contract | Missing or inaccurate prose can cause consumers to bypass safety limits or misunderstand partial/cyclic semantics. |
| current source → living/append-only memory | Stale SPRs or rewritten ADR history can poison future changes and audits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-30 | Spoofing | documented completion/complexity | high | mitigate | Exact shipped fields/defaults/counters and commands; never infer completeness from elapsed time or omit exhaustion semantics. |
| T-19-31 | Information Disclosure | logging/redactor documentation | high | mitigate | Enumerate default-safe and explicit-raw boundaries, allowlist, failure path, and handler ownership exactly. |
| T-19-32 | Tampering | ADR/SPR history | medium | mitigate | New accepted ADR-006 plus same-task implementation/SPR commits in Plans 19-01/03/04/05/06; Plan 19-07 checks the three SPR paths remain unchanged. |
| T-19-33 | Denial of Service | undocumented dense/path behavior | medium | mitigate | Publish sparse default, exact preflight/count units, and finite controls alongside examples. |
</threat_model>

<verification>
- Assert the README contains each required bounded-diagnostic, positional zero-machine, renderer, redactor, and reversible-logging concept, then run its established example test.
- Build HTML with warnings as errors and run doctests after public and maintainer documentation changes.
- Compare documented signatures/defaults and the read-only current SPRs to source and structured schemas to tests.
- Run `git diff --check`; no production or evidence file changes belong to this plan.
</verification>

<success_criteria>
- Every locked decision is documented in README and/or the user or maintainer reference altitude where it is needed.
- ADR-006 and the already-current implementation-owned SPRs preserve durable design intent without expanding scope or creating a follow-up memory commit.
- Phase 20 can consume explicit complexity/performance prerequisites without Phase 19 claiming installed-artifact proof.
</success_criteria>

<output>
Create `.planning/phases/19-bounded-diagnostics-safe-output/19-07-SUMMARY.md` when done.
</output>
