---
phase: 19-bounded-diagnostics-safe-output
plan: 03
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - src/fast_fsm/_diagnostics.py
  - src/fast_fsm/validation.py
  - tests/test_diagnostic_contracts.py
  - tests/test_validation.py
autonomous: true
requirements: [DIAG-04, DIAG-05, DIAG-06, DIAG-07, DIAG-08]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Per D-07, self-loops and every member of every multi-state cyclic SCC are returned exactly once in snapshot order; empty and acyclic graphs return no cyclic members."
    - "Per D-08, DAG depth is memoized and cyclic depth is computed on the condensation DAG with an explicit `dag_longest_path` or `condensation_dag_depth` interpretation."
    - "Per D-09, sparse ordered edges are the default, dense V²/V×events outputs preflight the exact cell budget before outer allocation, and path generation independently honors length, result, and expansion limits."
    - "Per D-05, D-06, and D-10, all graph algorithms share deterministic reserve-before-work counts; exact limits pass and one-less limits expose the correct stage/dimension without partial legacy results."
  artifacts:
    - {path: "src/fast_fsm/_diagnostics.py", provides: "Iterative SCC, condensation depth, sparse/dense adapters, and bounded path algorithms"}
    - {path: "src/fast_fsm/validation.py", provides: "Public validator adapters exposing corrected cycle/depth/sparse/dense/path behavior"}
    - {path: "tests/test_diagnostic_contracts.py", provides: "Exact graph semantics and complexity-counter boundaries"}
  key_links:
    - {from: "FSMValidator.find_cycles", to: "_strongly_connected_components", via: "one captured _DiagnosticGraph and shared budget", pattern: "strongly_connected|cyclic"}
    - {from: "EnhancedFSMValidator._find_longest_path", to: "_structural_depth", via: "condensation DAG memoization", pattern: "condensation_dag_depth|dag_longest_path"}
    - {from: "dense/path public adapters", to: "_DiagnosticBudget", via: "preflight/reserve before allocation or append", pattern: "max_dense_cells|max_path_expansions|max_results"}
---

<objective>
Expand the tracer into correct linear graph semantics and bounded sparse/dense/path diagnostics.

Purpose: Implement D-05 through D-10 so all later validation, JSON, and rendering consumers share deterministic SCC/depth/storage/path behavior rather than duplicating traversal.
Output: Iterative SCC membership, condensation-DAG depth, sparse-first graph data, preflighted dense compatibility, bounded paths, and public validator adapters.
</objective>

## Artifacts this phase produces

- Uses and completes the phase-wide symbols declared in 19-01: `_strongly_connected_components()`, `_structural_depth()`, `_sparse_adjacency()`, `_dense_adjacency()`, and `_generate_paths()` plus their `DiagnosticStatus` stages/counters.
- Adds no second graph representation, no recursive DFS API, no runtime dependency, and no core hot-path symbol.
- Public validator methods retain their callable names; additive keyword-only `limits`/`include_dense`/`max_expansions` controls and structured status fields are the only API expansion in this plan.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-CONTEXT.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-RESEARCH.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-PATTERNS.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-VALIDATION.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-01-SUMMARY.md

<interfaces>
- Plan 19-01 provides `_DiagnosticGraph`, `_DiagnosticBudget`, `DiagnosticLimits`, `DiagnosticStatus`, `DiagnosticBudgetExceeded`, and the snapshot-backed `FSMValidator` tracer.
- Existing compatibility methods are `FSMValidator.find_cycles() -> list[list[str]]`, `FSMValidator.generate_test_paths(max_length=10, max_paths=50)`, `FSMValidator.get_transition_matrix()`, `FSMValidator.get_adjacency_matrix()`, and `EnhancedFSMValidator._find_longest_path() -> int`.
- Existing matrix schema keys are `states`, `events`, `transitions`, and `matrix`; transition rows carry `idx`, source/target indices/names, and event index/name.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Report complete cyclic membership and condensation structural depth</name>
  <files>src/fast_fsm/_diagnostics.py, src/fast_fsm/validation.py, tests/test_diagnostic_contracts.py, tests/test_validation.py</files>
  <read_first>src/fast_fsm/_diagnostics.py and src/fast_fsm/validation.py after 19-01; tests/test_diagnostic_contracts.py rows owned by 19-03; tests/test_validation.py cycle/complexity tests; 19-CONTEXT.md D-07/D-08; 19-RESEARCH.md iterative Kosaraju and condensation guidance</read_first>
  <behavior>
    - Empty and acyclic graphs have no cyclic members; a self-loop has one member; three-cycle and multiple SCC cases include all and only cyclic members once.
    - SCCs and members are ordered by lowest snapshot index and then snapshot position, independent of hash seed.
    - A long DAG computes exact edge depth without recursion; a cyclic graph reports condensation depth with the documented interpretation.
    - Exact work/result counts pass and one-less limits fail before the next vertex/edge/result action.
  </behavior>
  <action>Remove only Task 19-03 SCC/depth strict-XFAIL markers, run the focused selection to observe intended failures, then implement iterative Kosaraju using ordered forward/reverse adjacency from `_DiagnosticGraph`. Count forward/reverse vertex visits and edge examinations before work; sort each component by snapshot index and order components by their lowest member; classify size greater than one or a self-loop as cyclic. Build deduplicated cross-component edges, topologically process the condensation DAG, and memoize longest edge depth without recursion. Per D-07 and D-08, expose `dag_longest_path` for acyclic graphs and `condensation_dag_depth` for cyclic graphs, while legacy list/scalar methods return the corrected value only on complete analysis and otherwise raise `DiagnosticBudgetExceeded`. Preserve `find_cycles()` callable compatibility by returning deterministic representative closed paths only if legacy tests require them, but source canonical membership and all report/issue logic from SCC member indices.</action>
  <acceptance_criteria>
    - Self-loop, three-cycle, overlapping/multiple SCC, tail, empty, acyclic, and recursion-depth-exceeding chain tests pass.
    - Every cyclic member appears once in snapshot order; no acyclic tail appears.
    - Depth result carries exactly one documented interpretation and matches the DAG/condensation expected edge count.
    - Work/result exact-limit and one-less tests pass under two hash seeds with identical counts/order.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_diagnostic_contracts.py tests/test_validation.py -x -q -k 'cycle or scc or depth or longest or budget'</automated>
  </verify>
  <done>DIAG-04 and DIAG-05 are complete with deterministic SCC membership, non-recursive linear structural depth, explicit cyclic interpretation, and fail-closed counted budgets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make sparse output default and bound dense allocation and path expansion</name>
  <reversibility rating="costly">D-06 and D-09 establish default output shape and exhaustion behavior consumed by validation and JSON callers.</reversibility>
  <files>src/fast_fsm/_diagnostics.py, src/fast_fsm/validation.py, tests/test_diagnostic_contracts.py, tests/test_validation.py</files>
  <read_first>src/fast_fsm/_diagnostics.py and validation adapters after Task 1; tests/test_diagnostic_contracts.py sparse/dense/path rows owned by 19-03; tests/test_validation.py adjacency/path compatibility; 19-CONTEXT.md D-05/D-06/D-09/D-10; 19-VALIDATION.md deterministic boundary matrix</read_first>
  <behavior>
    - Many-state zero-edge analysis returns ordered sparse rows without creating an N×N outer list.
    - Dense transition and adjacency compatibility preflight exact V×events or V² cells; exact succeeds and one less raises before outer allocation.
    - Path generation uses iterative frames and independently distinguishes `max_length`, `max_paths`, and `max_path_expansions`.
    - Shared top-level budgets aggregate nested work and never reset between graph helpers.
  </behavior>
  <action>Remove only Task 19-03 sparse/dense/path markers and observe RED. Implement snapshot-ordered sparse state/event/edge rows as the default report representation. Keep existing `get_transition_matrix()` and `get_adjacency_matrix()` shapes as explicit dense compatibility adapters with additive keyword-only `limits`; compute required cell count with overflow-safe integer arithmetic, reserve the full `V*events` or `V*V` amount, and reject before constructing the outer dictionary/list when insufficient. Replace recursive path enumeration with ordered iterative frames; reserve expansion before following an edge and result before copying/appending a path; preserve positional `max_length=10` and `max_paths=50`, and add keyword-only `max_expansions`/`limits`. Calibrate explicit finite default limit values from the maximum counted ordinary repository fixture plus documented headroom, pin those exact constants in tests, and ensure the generated adversarial fixtures deterministically exhaust. Per D-06, structured analysis records may carry incomplete status but legacy dict/list/scalar helpers return no partial payload and raise the redacted exception.</action>
  <acceptance_criteria>
    - Sparse default on a many-state zero-edge graph has O(V+E) stored rows and no dense outer allocation.
    - Dense exact-cell limits succeed and one-less limits raise before any matrix/list allocation, proven by an allocation sentinel.
    - Path length, count, and expansion limits have distinct deterministic terminal statuses and never silently truncate a legacy list.
    - Explicit defaults are finite constants, complete all legacy docs/tests, and exhaust the adversarial calibration graphs without wall-clock checks.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_diagnostic_contracts.py tests/test_validation.py -x -q -k 'sparse or dense or matrix or path or budget'</automated>
  </verify>
  <done>DIAG-06 and the remaining graph portion of DIAG-07 are complete: sparse-first analysis, preflighted dense compatibility, separately bounded paths, and calibrated deterministic defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted/generated graph → diagnostic algorithms | Graph size, fan-out, cycles, and depth can exhaust CPU, recursion, or memory. |
| public limits → legacy/structured outputs | Exhaustion must not be hidden as a plausible partial result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-10 | Denial of Service | SCC/depth traversal | high | mitigate | Iterative O(V+E) passes, shared finite counted budget, reserve-before-work, and long-chain/high-fan-out adversarial tests. |
| T-19-11 | Denial of Service | dense matrix adapters | high | mitigate | Explicit opt-in plus exact V²/V×events preflight before outer allocation. |
| T-19-12 | Denial of Service | path generation | high | mitigate | Independent length/result/expansion limits and iterative frames. |
| T-19-13 | Tampering | incomplete legacy results | high | mitigate | Structured completion metadata or fixed redacted exception; legacy shapes never return partial values. |
| T-19-14 | Repudiation | hash/set traversal order | medium | mitigate | Snapshot-index ordering and repeated two-hash-seed tests for results and counts. |
</threat_model>

<verification>
- Run each task's exact focused selection before and after implementation; later-plan strict-XFAIL rows remain intact.
- Run `uv run ruff check` on the four changed Python files and `task typecheck-mypy` after the plan.
- Confirm `core.py` is unchanged by this plan and the compiled trigger path has no diagnostics import.
</verification>

<success_criteria>
- All cycle members and structural depths are correct and deterministic.
- Sparse/generated graphs cannot force unconditional dense allocation or unbounded recursion/expansion.
- Counts, status, defaults, and exception boundaries are explicit and test pinned.
</success_criteria>

<output>
Create `.planning/phases/19-bounded-diagnostics-safe-output/19-03-SUMMARY.md` when done.
</output>
