---
phase: 29-expected-domain-rejection
plan: 03
type: execute
wave: 3
depends_on:
  - 29-02
files_modified:
  - tests/test_expected_rejection.py
  - tests/test_transition_lifecycle.py
  - tests/test_logging_config.py
  - docs/api/core.md
  - docs/api/conditions.md
autonomous: true
requirements:
  - REJECT-02
  - REJECT-03
  - REJECT-07
  - REJECT-09
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "REJECT-09 / D-11: TransitionRejected raised from timing, lifecycle, declarative action, trigger callback, after listener, observer, tracing, or another non-eligibility surface remains an ordinary staged failure/cause or propagating observer/control exception according to the existing boundary."
    - "D-15: eligibility rejection is destination-free and history-free; post-selection ordinary failures retain exact pre/post-commit destination, history, stage, priority, internal, and hidden cause truth."
    - "D-14: expected rejection logging is debug-only validated scalar metadata; no warning, traceback, exception representation, or caller payload is exposed."
    - "REJECT-07 / D-12: a lifecycle failure is finalized once through the existing observer family, while an observer-raised signal remains isolated observer failure rather than relabeling the trigger result."
    - "Public API docs distinguish false eligibility, expected rejection, unexpected defects, and cancellation without pulling Phase 32 tutorials forward."
  artifacts:
    - path: "tests/test_expected_rejection.py"
      provides: "Complete eligibility-versus-lifecycle classification matrix"
    - path: "tests/test_transition_lifecycle.py"
      provides: "Established pre/post-commit failure and observer regression evidence"
    - path: "tests/test_logging_config.py"
      provides: "Metadata-only expected-rejection and payload-confidentiality evidence"
    - path: "docs/api/core.md"
      provides: "Public signal/result/query/boundary API reference"
    - path: "docs/api/conditions.md"
      provides: "Composition propagation and direct-evaluation reference"
  key_links:
    - from: "approved selector catches"
      to: "ordinary lifecycle catches"
      via: "only selector eligibility calls convert; all later surfaces retain existing failure truth"
      pattern: "TransitionRejected"
    - from: "TransitionResult.rejection_code"
      to: "docs/api/core.md"
      via: "exact public result and escalation contract"
      pattern: "rejection_code"
    - from: "condition combinators"
      to: "docs/api/conditions.md"
      via: "boolean short-circuit plus unchanged rejection/cancellation propagation"
      pattern: "TransitionRejected"
  flagged_assumptions:
    - id: REJECT-09
      classification: unclassified
      status: mapped-to-locked-contract
      statement: "The specless probe did not classify non-eligibility use; D-11 requires existing ordinary staged-failure/cancellation/observer truth everywhere outside the three approved calls."
  prohibitions:
    - "Do not catch TransitionRejected in timing, lifecycle, handlers, callbacks, observers, tracing, `_trigger_owned`, or `_trigger_async_owned`."
    - "Do not expose arbitrary exception/caller payloads or add lifecycle-specific rejection outcomes."
    - "Do not add tutorials, serialization projections, diagrams, or installed-artifact claims owned by later phases."
---

<objective>
Prove that identical signals outside eligibility remain ordinary failures, extend logging confidentiality evidence, and publish the focused public API distinction.

Purpose: Close the semantic boundary before structural/native release validation.
Output: Outside-boundary lifecycle matrix, logging safety regression tests, and warnings-clean core/conditions API reference.
</objective>

## Artifacts this phase produces

- Public `TransitionRejected(code)` and additive rejection result metadata from Plan 29-01.
- Sync/async query and composition parity from Plan 29-02.
- Focused lifecycle/logging classification and API reference from this plan.
- Structural/native/release/performance closure in dependent Plan 29-04.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-expected-domain-rejection/29-CONTEXT.md
@.planning/phases/29-expected-domain-rejection/29-RESEARCH.md
@.planning/phases/29-expected-domain-rejection/29-PATTERNS.md
@.planning/phases/29-expected-domain-rejection/29-VALIDATION.md
@.planning/phases/29-expected-domain-rejection/29-02-SUMMARY.md

<interfaces>
Plans 29-01/02 provide the complete public signal/result contract and all sync/async eligibility, query, and composition behavior.
- Lifecycle helpers preserve ordinary `Exception` objects as hidden causes with stage-specific commit/history truth.
- `_finalize_failure()` snapshots existing observers and isolates observer `BaseException` values.
- Trace logging is metadata-only by default and hostile-repr tests are the confidentiality authority.
- `docs/api/core.md` owns public result/exception/query reference; `docs/api/conditions.md` owns composition semantics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove outside-boundary classification, logging safety, and API truth</name>
  <files>tests/test_expected_rejection.py, tests/test_transition_lifecycle.py, tests/test_logging_config.py, docs/api/core.md, docs/api/conditions.md</files>
  <read_first>
    - `.planning/phases/29-expected-domain-rejection/29-CONTEXT.md` D-03, D-05, D-11, D-12, D-14, and D-15
    - `.planning/phases/29-expected-domain-rejection/29-VALIDATION.md` row 29-03-01 and lifecycle classification matrix
    - `src/fast_fsm/core.py` timing selection, sync/async lifecycle runners, declarative handler, trigger callback, after listener, trace, observer, and cancellation catches
    - `tests/test_transition_lifecycle.py` precommit, destination-enter, full stage matrix, declarative failure, cancellation, and observer isolation tests
    - `tests/test_logging_config.py` metadata-only records, warning suppression, hostile repr, and cancellation-control tests
    - `docs/api/core.md` priority candidates, data classes, exceptions, and safe trace logging
    - `docs/api/conditions.md` recommended composition and direct condition semantics
  </read_first>
  <behavior>
    - "The same signal raised before commit outside eligibility yields an ordinary result with rejected=False, rejection_code=None, cause identity, and accurate stage/priority/internal truth."
    - "The same signal raised after commit preserves destination/current state/history and cause identity while remaining an ordinary staged failure."
    - "Observer-raised signal remains isolated observer failure; trace-redactor process-control/cancellation behavior and timing-error classification remain unchanged."
    - "Expected rejection exposes only validated debug code metadata; warning, traceback, exception repr, and caller payload remain absent."
    - "API docs expose exact validation, result/escalation, query, composition, and selector-only conversion semantics."
  </behavior>
  <action>
Add the remaining failing classification cases to the central oracle and adjacent owning suites. Raise the same `TransitionRejected` type from timing, before-transition, source exit/callback/listener, destination enter/callback/listener, declarative handler, trigger callback, after-transition, failure observer, and relevant async cancellation-adjacent surfaces. Assert ordinary behavior: `rejected is False`, `rejection_code is None`, hidden cause identity where stored, exact stage, pre/post-commit destination/current state/history, selected priority/internal mode, one trigger finalization, and no lower candidate. Do not modify production catches; Plans 29-01/02 must already limit conversion to the three approved calls per REJECT-09/D-11/T-29-07.

Extend hostile-repr/captured-log evidence so eligibility rejection exposes only validated debug code metadata, while outside-boundary failures retain existing redaction and cancellation behavior per D-14/T-29-04/T-29-08. Observer-raised signals stay isolated like other observer BaseException values rather than relabeling the trigger result.

Update `docs/api/core.md` with TransitionRejected beside TransitionError, exact input grammar, result fields/property, fixed error/cause/escalation behavior, group-abort/query semantics, and the three approved conversion sites. Update `docs/api/conditions.md` to state that composition short-circuits booleans but propagates rejection/cancellation unchanged and direct evaluation raises the signal. Keep this reference-focused; Phase 32 owns tutorials and progressive examples.
  </action>
  <acceptance_criteria>
    - "Every non-eligibility signal follows existing ordinary stage/cause/commit/history/cancellation truth and never receives rejection metadata."
    - "Logging evidence proves bounded expected metadata and unchanged payload-safe ordinary failure handling."
    - "Existing failure observers finalize real trigger failure once and isolate observer-raised signals."
    - "Core and conditions API pages accurately expose the public contract with no later-phase claims."
    - "T-29-03, T-29-04, T-29-06, T-29-07, and T-29-08 controls block completion on ambiguity, disclosure, duplicate observation, relabeling, or cancellation swallowing."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_expected_rejection.py tests/test_transition_lifecycle.py tests/test_logging_config.py -x -q -k "outside_boundary or lifecycle or payload or observer or cancellation or rejection"</automated>
  </verify>
  <done>REJECT-09 and the public API reference distinguish expected eligibility rejection from ordinary lifecycle failure, observer failure, and cancellation without ambiguity or disclosure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected dispatch -> lifecycle | The same exception type must not be relabeled after leaving approved eligibility calls. |
| runtime metadata -> logs/docs | A bounded identifier may be documented/logged, but arbitrary exception/caller data must stay hidden. |
| trigger result -> failure observer | One existing observer pass must not become a new rejection listener or recursive finalization path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-03 | Repudiation | lifecycle classification | high | mitigate | Parameterized outside-boundary stage/commit/cause matrix; failure blocks completion. |
| T-29-04 | Information Disclosure | logs, results, docs | high | mitigate | Validated scalar only plus hostile-repr/captured-record ASVS error-handling checks. |
| T-29-06 | Denial of Service / Repudiation | observer finalization | medium | mitigate | Existing single finalizer, snapshot/isolation tests, and no new listener family. |
| T-29-07 | Tampering / Repudiation | post-selection lifecycle | high | mitigate | No production catch changes plus exact pre/post-commit cause/history/destination matrix. |
| T-29-08 | Denial of Service | async cancellation | high | mitigate | Existing cancellation identity/re-raise/ownership evidence remains green adjacent to rejection. |
</threat_model>

<verification>
- Run the focused lifecycle/logging matrix after the red-green cycle.
- Run `uv run sphinx-build -b html docs docs/_build/html -W --keep-going`; warnings block plan completion.
- Run the Phase 29 adjacent regression command from `29-VALIDATION.md` before producing the summary.
</verification>

<success_criteria>
- Only approved eligibility calls classify expected rejection.
- Every later signal preserves ordinary lifecycle/observer/cancellation truth and logging confidentiality.
- Public API pages build warning-free and describe the exact bounded contract.
</success_criteria>

## Source Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Do not hide defects while exposing domain rejection | 29-03 | COVERED | Outside-boundary proof. |
| REQ | REJECT-02 | Payload-safe identifiers | 29-03 | COVERED | Logging/docs disclosure evidence. |
| REQ | REJECT-03 | Eligibility-only classification | 29-03 | COVERED | Lifecycle matrix. |
| REQ | REJECT-07 | Existing observer exactly once | 29-03 | COVERED | Trigger versus observer-raised signal. |
| REQ | REJECT-09 | Ordinary outside-boundary semantics | 29-03 | COVERED | Primary completion. |
| CONTEXT | D-11 | Direct/FSM boundary | 29-03 | COVERED | Non-eligibility matrix. |
| CONTEXT | D-12 | Existing observer family | 29-03 | COVERED | No new listener. |
| CONTEXT | D-14 | Metadata-only debug | 29-03 | COVERED | Hostile-repr logging tests. |
| CONTEXT | D-15 | Accurate stage/priority/internal/no-history truth | 29-03 | COVERED | Pre/post-commit assertions. |
| RESEARCH | R-DOCS | Focused API exposure before Phase 32 guidance | 29-03 | COVERED | Core/conditions pages. |
| RESEARCH | R-SECURITY | Classification/disclosure/observer/cancellation controls | 29-03 | COVERED | T-29-03/04/06/07/08. |

<output>
Create `.planning/phases/29-expected-domain-rejection/29-03-SUMMARY.md` when done.
</output>
