---
phase: 29-expected-domain-rejection
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - src/fast_fsm/core.py
  - tests/test_expected_rejection.py
  - tests/test_priority_selection.py
  - tests/test_async.py
  - tests/test_transition_lifecycle.py
  - tests/test_condition_interface.py
autonomous: true
requirements:
  - REJECT-03
  - REJECT-04
  - REJECT-06
  - REJECT-07
  - REJECT-08
  - REJECT-09
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "REJECT-06 / D-13: can_trigger() and can_trigger_async() stop at expected rejection and return False without state/history mutation, lifecycle, observer notification, trace finalization, or lower-candidate evaluation."
    - "REJECT-03 / REJECT-04 / D-10 / D-15: asynchronous transition guards, declarative guards, and state permission checks produce the same terminal rejection metadata and ordered selection behavior as synchronous dispatch."
    - "REJECT-07 / D-12: asynchronous trigger dispatch performs one unchanged failure-observer pass; query dispatch performs none."
    - "REJECT-08 / D-08 / D-09 / D-10 / D-11: direct, AND, OR, NOT, legacy NegatedCondition, nested, and deferred sync/async evaluation propagate the identical signal and never evaluate a later child."
    - "D-10: asyncio cancellation remains cancellation, is finalized through existing stage/priority/mode truth, re-raises identically, and releases ownership."
    - "Ordinary query exception compatibility remains unchanged; only expected rejection gains uniform False projection."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Async three-seam rejection conversion and unchanged query projection"
      contains: "_select_async_candidate"
    - path: "tests/test_expected_rejection.py"
      provides: "Sync/async query, composition, priority, and cancellation matrix"
    - path: "tests/test_condition_interface.py"
      provides: "Direct/deferred/nested composition propagation evidence"
    - path: "tests/test_priority_selection.py"
      provides: "No-fallback and query ordering regression evidence"
  key_links:
    - from: "can_trigger / can_trigger_async"
      to: "sync/async selectors"
      via: "existing terminal TransitionResult-to-False projection without finalization"
      pattern: "not isinstance(selected, TransitionResult)"
    - from: "_select_async_candidate"
      to: "TransitionResult.rejection_code"
      via: "specific catch at each awaited approved eligibility call"
      pattern: "TransitionRejected"
    - from: "condition combinators"
      to: "FSM selector boundary"
      via: "unchanged exception propagation through one await ownership chain"
      pattern: "_continue_compound_check"
  flagged_assumptions:
    - id: REJECT-06
      classification: unclassified
      status: mapped-to-locked-contract
      statement: "The specless probe did not classify query projection; D-13 defines uniform False/no-side-effect behavior while preserving existing ordinary exception compatibility."
    - id: REJECT-08
      classification: unclassified
      status: mapped-to-locked-contract
      statement: "The specless probe did not classify composition; D-08 through D-11 define terminal identity propagation across every supported wrapper and await shape."
  prohibitions:
    - "Do not add rejection-aware catch/return logic to condition combinators; propagation is proven by non-interference."
    - "Do not introduce async fan-out, speculative candidate tasks, shielding, sync/async bridging, a rejection ContextVar, or a second selector."
    - "Do not unify or otherwise refactor legacy ordinary query exception behavior as part of the rejection-specific contract."
---

<objective>
Extend expected rejection through observer-free queries, every supported condition-composition shape, and sequential asynchronous selection without changing cancellation or ordinary exception compatibility.

Purpose: Prove the same finite priority and three-valued eligibility semantics across both machine types and composed conditions.
Output: Sync/async query behavior, three async conversion seams, composition propagation evidence, cancellation/ownership regression proof, and parity tests.
</objective>

## Artifacts this phase produces

- Public `TransitionRejected(code)` control signal with exact bounded ASCII validation.
- Additive `TransitionResult.rejection_code` and derived read-only `rejected` status.
- Sync/async conversion only at transition guard, declarative guard, and state permission seams.
- New `tests/test_expected_rejection.py` requirement-labelled semantic oracle.
- Synchronized runtime/stub/package exports, slots-policy authorities, API docs, and living SPR.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-expected-domain-rejection/29-CONTEXT.md
@.planning/phases/29-expected-domain-rejection/29-RESEARCH.md
@.planning/phases/29-expected-domain-rejection/29-PATTERNS.md
@.planning/phases/29-expected-domain-rejection/29-VALIDATION.md
@.planning/phases/29-expected-domain-rejection/29-01-SUMMARY.md

<interfaces>
Plan 29-01 provides `TransitionRejected`, `TransitionResult.rejection_code`, `TransitionResult.rejected`, and all three synchronous selector conversions.
Existing async/query contracts to preserve:
- `can_trigger*()` calls the ordinary selector with `for_query=True` and returns whether selection is not a terminal TransitionResult; it does not finalize failures.
- `_select_async_candidate(...)` awaits one candidate stage at a time and returns `_PreparedDispatch`, terminal `TransitionResult`, or `None` for ordinary grouped fallthrough.
- `_trigger_async_owned()` retains selection stage, priority, and internal mode for cancellation, finalizes cancellation once, and re-raises.
- `AndCondition`, `OrCondition`, `NotCondition`, and `NegatedCondition` contain no exception-to-false conversion; deferred wrappers own and await each child once.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Match query, async selection, observer, and cancellation behavior</name>
  <files>src/fast_fsm/core.py, tests/test_expected_rejection.py, tests/test_priority_selection.py, tests/test_async.py, tests/test_transition_lifecycle.py</files>
  <read_first>
    - `.planning/phases/29-expected-domain-rejection/29-CONTEXT.md` D-10, D-12, D-13, and D-15
    - `.planning/phases/29-expected-domain-rejection/29-VALIDATION.md` row 29-02-01 and query/observer/cancellation matrices
    - `src/fast_fsm/core.py` `can_trigger`, `can_trigger_async`, `_select_sync_candidate`, `_select_async_candidate`, and `_trigger_async_owned`
    - `tests/test_priority_selection.py` sync/async query, terminal-exception, no-speculation, and cancellation tests
    - `tests/test_async.py` async guard/query/ownership exception behavior
    - `tests/test_transition_lifecycle.py` event-handshake cancellation and observer registration/isolation tests
  </read_first>
  <behavior>
    - "Sync and async queries return False for rejection at guard, declarative guard, and permission seams without observer, history, trace, lifecycle, lower-candidate, or state effects."
    - "Real async triggers return the same code/error/cause/stage/priority/internal/commit/destination truth as sync triggers and notify existing failure observers exactly once."
    - "Async candidate evaluation remains strictly sequential, including sync leaves, AsyncCondition, callable-backed awaitables, declarative guards, and async state permission."
    - "Cancellation adjacent to rejection retains CancelledError identity, existing stage/priority/mode metadata, one observer finalization, ownership release, and immediate machine reuse."
    - "Ordinary sync/async query exceptions keep their established propagate-versus-terminal-False compatibility behavior."
  </behavior>
  <action>
Add failing tests for sync/async `can_trigger*()` across all three approved seams, singleton/grouped topology, internal/external candidates, and ordinary-exception controls. Assert False plus zero mutation, history, observer, trace event, lifecycle, and lower-candidate work per REJECT-06/D-13; real trigger calls on the same shapes must preserve the structured code and one observer pass per D-12.

Add the specific `TransitionRejected` catch before each general async exception branch at the awaited transition guard, declarative guard, and state-permission calls. Reuse the same cold-path validator/result helper as sync, set selected priority/internal/stage before returning, and return terminal `TransitionResult` in query and trigger modes. Preserve one-candidate-at-a-time awaits, direct singleton lookup, ordinary false `None` fallthrough, and legacy ordinary query exception behavior per D-10/D-15 and T-29-02/T-29-03.

Extend the event-handshake cancellation matrix without sleeps. Cancellation before/around every async eligibility seam must remain cancellation, never rejection; `_trigger_async_owned()` must finalize once, bare re-raise, release owner/lock/context tokens, and allow immediate reuse. Do not catch `BaseException`, add a rejection ContextVar, or fan out tasks. High-severity T-29-08 cancellation identity/ownership failure blocks task completion.
  </action>
  <acceptance_criteria>
    - "Both query APIs stop at rejection and return False with no side effects or finalization."
    - "Both trigger APIs return equivalent rejection metadata and exactly one observer pass."
    - "Async priority scanning remains sequential and never inspects a lower candidate after rejection."
    - "Cancellation remains observer-visible cancellation, is re-raised unchanged, and cannot strand ownership or become rejection."
    - "T-29-02, T-29-03, T-29-06, and T-29-08 controls block completion on fallback, ambiguity, duplicate observation, or cancellation swallowing."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_expected_rejection.py tests/test_priority_selection.py tests/test_async.py tests/test_transition_lifecycle.py -x -q -k "query or async or cancellation or priority or rejection"</automated>
  </verify>
  <done>REJECT-06 and sync/async portions of REJECT-03, REJECT-04, and REJECT-07 are equivalent, observer-correct, cancellation-safe, and ownership-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove direct, nested, deferred, and negated composition propagation</name>
  <files>tests/test_expected_rejection.py, tests/test_condition_interface.py</files>
  <read_first>
    - `.planning/phases/29-expected-domain-rejection/29-CONTEXT.md` D-08 through D-11
    - `.planning/phases/29-expected-domain-rejection/29-RESEARCH.md` Pattern 5, Pitfall 3, and composition validation matrix
    - `.planning/phases/29-expected-domain-rejection/29-PATTERNS.md` conditions.py verify-only assignment and composition test analogs
    - `src/fast_fsm/conditions.py` `_continue_compound_check`, `_check_compound_conditions`, `_negate_guard_result`, AndCondition, OrCondition, NotCondition, and NegatedCondition
    - `src/fast_fsm/core.py` iterative condition evaluation and sync/async selector conversion boundaries
    - `tests/test_condition_interface.py` canonical wrapper short-circuit and deferred single-await ownership tests
  </read_first>
  <behavior>
    - "Direct Condition/FuncCondition/AsyncCondition evaluation outside an FSM raises the identical TransitionRejected object."
    - "AndCondition and OrCondition propagate rejection immediately regardless of operator position and never evaluate a later child."
    - "NotCondition and legacy NegatedCondition propagate rejection unchanged rather than negating it into a Boolean."
    - "Nested and deferred sync/async wrappers retain signal identity, left-to-right ordering, cycle cleanup, and exactly-one await ownership."
    - "CancelledError raised by a composed async child remains cancellation and no later child runs."
  </behavior>
  <action>
Write the complete propagation matrix against the existing condition implementation: direct leaves, canonical `&`, `|`, and `~`, explicit AndCondition/OrCondition/NotCondition, legacy NegatedCondition, nested wrappers, a deferred awaitable in first/middle positions, and async cancellation. Use an identity assertion on the exact raised signal plus later-child counters; verify the deferred result remains single-use and traversal state is cleaned after the unwind.

Per D-08 through D-11, do not add catch/re-raise or exception-to-false code to `conditions.py`; its existing absence of exception handling is the desired production behavior. Confirm the same composite raised inside an FSM is converted only at the approved selector boundary, while direct evaluation propagates normally. Preserve ordinary boolean short-circuit behavior as the control and leave interpreted `conditions.py` outside mypyc compilation.
  </action>
  <acceptance_criteria>
    - "Every direct and composed sync/async shape propagates the identical signal and suppresses all later children."
    - "Negation applies only to boolean results; rejection and cancellation identities are unchanged."
    - "Direct evaluation never constructs a TransitionResult; conversion occurs only after the signal reaches an FSM eligibility call."
    - "No production composition catch, alternate evaluator, await fan-out, or compilation-boundary change is introduced."
    - "T-29-02 and T-29-08 controls block completion if composition permits fallback or swallows cancellation."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_expected_rejection.py tests/test_condition_interface.py -x -q -k "compos or deferred or nested or negated or cancellation or rejection"</automated>
  </verify>
  <done>REJECT-08 is proven recursively across direct, nested, deferred, negated, synchronous, and asynchronous condition evaluation without production composition changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query caller -> shared selector | Query must consume structured rejection as False without triggering mutation or observation. |
| awaited application hook -> async selector | Rejection, ordinary defect, and cancellation must retain distinct identities and group behavior. |
| condition wrapper -> child awaitable | Composition owns ordering and await lifetime but must not reinterpret exception control signals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-02 | Tampering / Elevation of Privilege | async priority group and composition | high | mitigate | Terminal result plus later-candidate/child sentinels enforce no fallback after rejection. |
| T-29-03 | Repudiation | query and async classification | high | mitigate | Query-specific rejection catch maps only validated rejection to False; ordinary defects retain compatibility paths. |
| T-29-06 | Denial of Service / Repudiation | async finalization | medium | mitigate | Queries never finalize; real triggers reuse the single public finalizer and observer snapshot. |
| T-29-08 | Denial of Service | CancelledError and async ownership | high | mitigate | Catch Exception only, preserve dedicated cancellation finalizer/bare re-raise/finally release, and use deterministic handshakes. |
</threat_model>

<verification>
- Run Task 1 query/async/cancellation matrix after its red-green cycle.
- Run Task 2 direct/composed/deferred propagation matrix without modifying production combinators.
- Close the wave with the full adjacent regression command from `29-VALIDATION.md`, Ruff on changed Python files, blocking mypy, visible advisory ty, and slots policy.
</verification>

<success_criteria>
- Sync and async query callers receive False without losing trigger-time structured metadata.
- All condition wrappers propagate rejection/cancellation and preserve ordinary boolean short circuiting.
- Async selection remains sequential, priority-terminal, observer-correct, and ownership-safe.
</success_criteria>

## Source Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Expected rejection cannot select a lower behavior | 29-02 | COVERED | Async and composition sentinels. |
| REQ | REJECT-03 | Approved conversion only | 29-02, 29-03 | COVERED | Async catches plus outside-boundary proof. |
| REQ | REJECT-04 | False fallthrough versus terminal rejection | 29-02 | COVERED | Query/async group matrix. |
| REQ | REJECT-06 | Query False and trigger code | 29-02 | COVERED | Task 1. |
| REQ | REJECT-07 | Existing observer once | 29-02 | COVERED | Real async trigger versus query. |
| REQ | REJECT-08 | Composition propagation | 29-02 | COVERED | Task 2. |
| REQ | REJECT-09 | Cancellation and outside-boundary classification | 29-02, 29-03, 29-04 | COVERED | Cancellation here; lifecycle signals and closure follow. |
| CONTEXT | D-08..D-11 | Operator-independent recursive propagation and direct evaluation | 29-02 | COVERED | Task 2 cites all four. |
| CONTEXT | D-12..D-13 | Observer and query behavior | 29-02 | COVERED | Task 1. |
| CONTEXT | D-15 | Selected precommit metadata | 29-02 | COVERED | Async parity. |
| RESEARCH | R-COMPOSITION | Prove existing catch-free combinators rather than rewrite | 29-02 | COVERED | Verify-only production posture. |
| RESEARCH | R-CANCELLATION | Preserve cancellation identity and ownership | 29-02 | COVERED | Event-handshake matrix. |

<output>
Create `.planning/phases/29-expected-domain-rejection/29-02-SUMMARY.md` when done.
</output>
