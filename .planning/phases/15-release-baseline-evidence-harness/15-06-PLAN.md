---
phase: 15-release-baseline-evidence-harness
plan: 06
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - src/fast_fsm/visualization.py
  - tests/test_advanced_functionality.py
autonomous: true
requirements:
  - REL-05
estimate:
  tokens: 5000
  raw_tokens: 5000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The two reproduced Ruff blockers are repaired without changing Fast FSM behavior, so the clean baseline can be collected from committed source (REL-05)."
    - "The repair remains restricted to the two known files and does not become an unrelated repository-wide formatting sweep."
  artifacts:
    - path: "src/fast_fsm/visualization.py"
      provides: "Ruff-formatted visualization source"
    - path: "tests/test_advanced_functionality.py"
      provides: "Advanced test with the reproduced unused assignment resolved without weakening assertions"
  key_links:
    - from: "src/fast_fsm/visualization.py"
      to: "Plan 03 clean-checkout evidence"
      via: "scoped Ruff repair is committed before baseline collection"
      pattern: "ruff format"
    - from: "tests/test_advanced_functionality.py"
      to: "Plan 03 clean-checkout evidence"
      via: "lint-clean test source remains behaviorally verified"
      pattern: "test_advanced"
---

<objective>
Clear only the two reproduced Ruff blockers before authoritative evidence collection.

Purpose: Keep the mechanical quality repair isolated from the release-history/evidence plan so each executor has a small, reviewable file set.
Output: Two behavior-preserving Ruff-clean source/test files.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-release-baseline-evidence-harness/15-CONTEXT.md
@.planning/phases/15-release-baseline-evidence-harness/15-RESEARCH.md
@.planning/phases/15-release-baseline-evidence-harness/15-PATTERNS.md
@.planning/phases/15-release-baseline-evidence-harness/15-VALIDATION.md
@.planning/phases/15-release-baseline-evidence-harness/15-02-SUMMARY.md
@src/fast_fsm/visualization.py
@tests/test_advanced_functionality.py

<flagged_assumptions>
- REL-05 assumption: the two reproduced Ruff failures could tempt a broad formatting sweep. Resolve them in their own two-file plan and make Plan 03 depend on this exact commit.
</flagged_assumptions>
</context>

<execution_constraints>
- Use `uv run` for Python tests and Ruff.
- Preserve unrelated worktree edits. Stage/commit only the two listed files; never use `git add .` or `git add -A`.
- The phase bead `fast_fsm-lw2` is already claimed and `in_progress`; inspect it with `bd show fast_fsm-lw2 --json`. Create any newly discovered work with `bd create ... --deps discovered-from:fast_fsm-lw2 --json`. Do not close the phase bead: the root orchestrator closes it only after the final Phase 15 verifier passes.
- Do not change runtime behavior, public APIs, assertions, or visualization semantics.
</execution_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Repair and prove the two known Ruff failures end to end</name>
  <read_first>
    - src/fast_fsm/visualization.py
    - tests/test_advanced_functionality.py
    - .planning/codebase/TESTING.md
  </read_first>
  <files>src/fast_fsm/visualization.py, tests/test_advanced_functionality.py</files>
  <action>
    Apply Ruff formatting only to `src/fast_fsm/visualization.py`. Remove or use the reproduced unused assignment at `tests/test_advanced_functionality.py:1488` while preserving the surrounding assertions and test intent. Do not apply automatic fixes to other paths. Run focused Ruff checks on exactly these two files and the focused visualization/advanced tests before committing only these paths; Plan 03 consumes this commit when it generates the clean baseline.
  </action>
  <acceptance_criteria>
    - Ruff format and lint checks pass for both modified files.
    - The F841 defect is gone without deleting or weakening the surrounding behavioral assertions.
    - Focused visualization and advanced tests retain their observable behavior.
    - No file outside the two-path ownership list is modified or committed by this plan.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format --check src/fast_fsm/visualization.py tests/test_advanced_functionality.py</automated>
    <automated>uv run ruff check src/fast_fsm/visualization.py tests/test_advanced_functionality.py</automated>
    <automated>uv run pytest tests/test_visualization.py tests/test_advanced_functionality.py -x -q</automated>
  </verify>
  <done>The two reproduced quality blockers are cleared in a scoped commit that Plan 03 can include in clean-checkout evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mechanical formatter -> runtime source | Automated formatting touches production visualization code. |
| lint repair -> test intent | Removing an unused assignment must not weaken the behavioral test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|--------------|---------------|
| T-15-24 | Tampering | visualization formatting | medium | mitigate | Restrict Ruff formatting to the single reproduced source path and prohibit semantic edits. | Focused Ruff checks and visualization tests. | Formatter output can still shift line layout; code review confirms no semantic delta. |
| T-15-25 | Tampering | advanced test repair | medium | mitigate | Preserve assertions and resolve only the unused binding. | Focused advanced tests plus diff review. | Tests cannot prove every accidental semantic edit; two-file ownership limits review surface. |
</threat_model>

## Artifacts this phase produces

- Updated files: `src/fast_fsm/visualization.py`, `tests/test_advanced_functionality.py`.
- No new public symbol, dependency, CLI, environment variable, schema field, release artifact, or Phase 20 publication behavior.

<verification>
<automated>uv run ruff format --check src/ tests/</automated>
<automated>uv run ruff check src/ tests/</automated>
<automated>uv run pytest tests/test_visualization.py tests/test_advanced_functionality.py -x -q</automated>
</verification>

<success_criteria>
- The two known Ruff blockers are absent.
- Focused behavior remains green.
- Plan 03 can collect clean-checkout evidence from the committed repair.
</success_criteria>

<output>
Create `.planning/phases/15-release-baseline-evidence-harness/15-06-SUMMARY.md` when done.
</output>
