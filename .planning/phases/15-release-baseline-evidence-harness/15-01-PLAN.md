---
phase: 15-release-baseline-evidence-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/__init__.py
  - tools/build_modes.py
  - tools/release_evidence.py
  - MANIFEST.in
  - setup.py
  - tests/test_build_modes.py
  - tests/test_release_evidence.py
autonomous: true
requirements:
  - REL-04
  - REL-06
  - REL-08
  - TEST-02
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A maintainer can select auto, pure, or compiled build intent, and the FAST_FSM_PURE_PYTHON=1 alias still selects pure mode (D-13)."
    - "A pure-source check fails before import when any native core shadow exists, names every path, and leaves every developer file untouched (D-15, TEST-02)."
    - "A maintainer can classify a repeatable collection containing one universal pure wheel and multiple compiled platform wheels by independent filename, tag, metadata, native-member, and classified-mode evidence without adding a public runtime API (D-14, D-16, REL-04)."
    - "The slots-policy inventory recursively audits every relevant production class and measures the registered CompiledFuncCondition and TransitionError exceptions instead of silently overstating the policy (REL-08)."
  artifacts:
    - path: "tools/build_modes.py"
      provides: "Shared build-mode parsing contract"
      exports: ["BuildMode", "resolve_build_mode"]
    - path: "MANIFEST.in"
      provides: "sdist inclusion contract for setup-time build-mode imports"
      contains: "tools/build_modes.py"
    - path: "tools/release_evidence.py"
      provides: "Maintainer-only source, wheel, and slots verification CLI"
      contains: "verify-source"
    - path: "tests/test_build_modes.py"
      provides: "Selector precedence and conflict matrix"
    - path: "tests/test_release_evidence.py"
      provides: "Native-shadow, wheel-identity, and slots-policy regression coverage"
  key_links:
    - from: "setup.py"
      to: "tools/build_modes.py"
      via: "resolve_build_mode(os.environ) chooses the existing mypyc branch"
      pattern: "resolve_build_mode"
    - from: "tools/release_evidence.py"
      to: "src/fast_fsm/core.py"
      via: "native-shadow preflight precedes import and origin classification"
      pattern: "EXTENSION_SUFFIXES"
    - from: "tools/release_evidence.py"
      to: "wheel archive metadata"
      via: "ZipFile inspection without extraction classifies tags, metadata, and native members"
      pattern: "ZipFile"
---

<objective>
Establish the production-quality build-mode and artifact-evidence primitives that every later Phase 15 gate uses.

Purpose: Prove the hardest constraint first: a requested pure run must be identifiable and cannot silently execute a stale native core module. This implements D-13 through D-16 without taking on Phase 20's strict compiled-publication enforcement.
Output: Shared build selector, maintainer verification CLI, and fail-first unit/integration tests.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-release-baseline-evidence-harness/15-CONTEXT.md
@.planning/phases/15-release-baseline-evidence-harness/15-RESEARCH.md
@.planning/phases/15-release-baseline-evidence-harness/15-PATTERNS.md
@.planning/phases/15-release-baseline-evidence-harness/15-VALIDATION.md
@setup.py
@src/fast_fsm/core.py
@tests/test_mypyc_guard.py

<interfaces>
Create a Python-3.10-compatible `class BuildMode(str, Enum)` with values `auto`, `pure`, and `compiled`, plus `resolve_build_mode(environ: Mapping[str, str]) -> BuildMode`. The canonical maintainer CLI is `uv run python tools/release_evidence.py` with JSON-capable `verify-source`, repeatable `verify-wheel --wheel PATH [--wheel PATH ...]`, and `slots-policy` subcommands. Wheel inspection returns a deterministic collection sorted by normalized wheel basename and tags, with independent evidence per artifact. Keep these interfaces under `tools/`; do not export them from `fast_fsm`.
</interfaces>

<flagged_assumptions>
- REL-04 assumption: selector conflict behavior was unclassified. Resolve it by accepting an unset alias or `FAST_FSM_PURE_PYTHON=0`, mapping `=1` to pure, accepting matching new/legacy pure intent, and rejecting invalid or contradictory values with actionable messages.
- REL-08 assumption: slots inventory drift was unclassified. Resolve it with an explicit measured exception registry containing both `fast_fsm.core.CompiledFuncCondition` and `fast_fsm.core.TransitionError`, plus a recursive AST-backed failure when any future production class is omitted from the audit or introduces an unregistered instance-`__dict__` exception.
- TEST-02 assumption: native suffix behavior varies by platform. Resolve it with `importlib.machinery.EXTENSION_SUFFIXES` plus explicit `.so`/`.pyd` coverage in temporary fixtures.
</flagged_assumptions>
</context>

<execution_constraints>
- Execute every Phase 15 plan and commit on the already-created `feat/release-baseline-evidence` branch. Before Task 1, `git branch --show-current` must print exactly `feat/release-baseline-evidence`; never implement this phase directly on `main`.
- Use `uv run` for every Python invocation. Do not use bare `python`, `pip`, or `python -m`.
- The phase bead `fast_fsm-lw2` is already claimed and `in_progress`; inspect it with `bd show fast_fsm-lw2 --json`. Create any newly discovered work with `bd create ... --deps discovered-from:fast_fsm-lw2 --json`. Do not close the phase bead: the root orchestrator closes it only after the final Phase 15 verifier passes.
- The worktree contains unrelated user edits. Stage and commit only the paths in the active task's `<files>` element; never use `git add .` or `git add -A`.
- Preserve ADR-003: only `src/fast_fsm/core.py` may be compiled; condition modules remain interpreted.
- A `compiled` selector records explicit intent in Phase 15, but strict build failure and installed compiled/pure parity stay in Phase 20 per D-16. Do not implement REL-03 or TEST-03/TEST-04 here.
</execution_constraints>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire one build-mode selection path from environment to setup behavior</name>
  <read_first>
    - setup.py
    - pyproject.toml
    - .specify/decisions/ADR-003-mypyc-compilation-boundary.md
    - tests/test_mypyc_guard.py
  </read_first>
  <files>tools/__init__.py, tools/build_modes.py, MANIFEST.in, setup.py, tests/test_build_modes.py</files>
  <behavior>
    - Test: no selector yields `BuildMode.AUTO`.
    - Test: the selector imports and resolves all three modes under Python 3.10, proving it does not depend on `enum.StrEnum`.
    - Test: `FAST_FSM_BUILD_MODE=pure` and legacy `FAST_FSM_PURE_PYTHON=1` each suppress mypyc extension generation.
    - Test: auto and compiled modes retain the existing `core.py`-only mypyc boundary.
    - Test: invalid new/legacy values and conflicting selectors fail with the names and values of the conflicting variables.
    - Test: an intentional pure sdist contains `tools/build_modes.py`, and building a wheel from the unpacked sdist succeeds without importing files outside the archive.
  </behavior>
  <action>
    Start with failing selector-matrix tests. Implement `BuildMode` as `class BuildMode(str, Enum)` and the pure `resolve_build_mode(environ)` function in `tools/build_modes.py`, using `FAST_FSM_BUILD_MODE` as the new selector per D-13 and remaining importable on Python 3.10. Make `setup.py` call that parser before importing mypyc. Add `MANIFEST.in` with explicit inclusion of `tools/__init__.py` and `tools/build_modes.py`, because setup-time imports must work from an unpacked sdist rather than only a repository checkout. Pure mode must produce no extension configuration; auto retains the current optional fallback; compiled records explicit intent and attempts the same `core.py` compilation seam without adding Phase 20's strict artifact failure. Keep invalid configuration errors outside the compiler fallback so they cannot be swallowed. The tracer is complete only when real subprocess tests demonstrate the selector reaching setup's extension decision and build an intentional pure sdist followed by a wheel from its unpacked contents in temporary directories. Commit only the five listed paths.
  </action>
  <acceptance_criteria>
    - `FAST_FSM_BUILD_MODE` accepts exactly `auto`, `pure`, and `compiled` case-normalized values.
    - `FAST_FSM_PURE_PYTHON=1` remains a working compatibility alias; contradictory intent fails closed.
    - `setup.py` still passes only `src/fast_fsm/core.py` to `mypycify`.
    - `MANIFEST.in` makes the setup-time selector package present in the sdist, and the unpacked sdist builds a wheel successfully with `FAST_FSM_BUILD_MODE=pure`.
    - Python 3.10 imports `BuildMode` and resolves auto/pure/compiled without `StrEnum`.
    - No public symbol is added to `src/fast_fsm/__init__.py`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_build_modes.py -x -q</automated>
    <automated>uv run --python 3.10 --no-project python -c "from tools.build_modes import BuildMode, resolve_build_mode; assert resolve_build_mode({'FAST_FSM_BUILD_MODE':'pure'}) is BuildMode.PURE"</automated>
  </verify>
  <done>The environment-to-build-selection happy path works end to end, conflict behavior is deterministic, and the selective compilation boundary is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove pure-source and wheel identity non-destructively</name>
  <read_first>
    - src/fast_fsm/__init__.py
    - src/fast_fsm/core.py
    - tests/test_mypyc_guard.py
    - .planning/codebase/TESTING.md
  </read_first>
  <files>tools/release_evidence.py, tests/test_release_evidence.py</files>
  <behavior>
    - Test: a temporary native-suffix sibling is reported by exact path before import, the command exits nonzero, and the file remains present.
    - Test: a clean copied package imports `fast_fsm.core` from a normalized `.py` origin and records installed distribution metadata.
    - Test: a `py3-none-any` wheel with no native members classifies as pure.
    - Test: a purported universal pure wheel containing a native member or inconsistent WHEEL metadata fails with an actionable mismatch.
    - Test: `verify-wheel --wheel pure.whl --wheel linux.whl --wheel windows.whl --json` accepts one universal pure wheel and multiple distinct compiled platform wheels, preserves independent filename/tags/METADATA/native-member evidence for each, and emits the same normalized order regardless of argument order.
  </behavior>
  <action>
    Write failing subprocess and archive tests first. Implement standard-library-only path/origin and wheel inspection in `tools/release_evidence.py`. `verify-source` must scan before import using platform extension suffixes, report all discovered shadow paths, and never unlink, move, or rewrite them per D-15. After a clean preflight it imports `fast_fsm.core`, requires a `.py` origin, normalizes repository-relative paths, and records `importlib.metadata.version("fast-fsm")`. `verify-wheel --wheel PATH [--wheel PATH ...] --json` must accept a repeatable wheel input, read each WHEEL and METADATA plus archive member names with `zipfile.ZipFile` without extracting, and report independent normalized basename, filename/archive tags, metadata version, classified mode, and native members. Sort the artifact collection deterministically by normalized basename and tags, reject duplicate normalized artifact identities, and prove with temporary fixtures that one universal pure wheel coexists with multiple distinct compiled platform wheels. This implements D-14 and explicitly models D-16's Phase 20 artifact set without building, uploading, or publishing that set in Phase 15. Commit only the two listed paths.
  </action>
  <acceptance_criteria>
    - Native shadows cause a pre-import failure that includes every exact path and an explicit cleanup instruction.
    - The verifier leaves all inspected paths byte-for-byte intact.
    - Pure wheel identity requires universal tags and zero native members; inconsistent evidence fails closed.
    - JSON output contains artifact mode, normalized core origin, installed metadata version, and a deterministic artifact collection with independent basename, wheel tags, metadata, and native-member evidence for every repeated `--wheel` input.
    - One universal pure wheel and multiple distinct compiled platform-wheel fixtures coexist in the collection; Phase 15 does not publish them.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_release_evidence.py -x -q -k "shadow or source_origin or wheel"</automated>
  </verify>
  <done>A maintainer can distinguish clean pure source and pure wheels from stale/native artifacts using one canonical, non-destructive verifier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Recursively inventory and measure the registered slots-policy exceptions</name>
  <read_first>
    - src/fast_fsm/core.py
    - src/fast_fsm/conditions.py
    - src/fast_fsm/condition_templates.py
    - tests/test_mypyc_guard.py
    - .specify/decisions/ADR-003-mypyc-compilation-boundary.md
  </read_first>
  <files>tools/release_evidence.py, tests/test_release_evidence.py</files>
  <behavior>
    - Test: recursive discovery covers every relevant class declaration under `src/fast_fsm/**/*.py`, including exception classes, and every discovered class is classified as slot-protected or present in the explicit exception registry.
    - Test: `CompiledFuncCondition` and `TransitionError` are the two registered deliberate exceptions; each real instance measurement records `__dict__` presence and `sys.getsizeof` with its distinct `@mypyc_attr(native_class=False)` rationale.
    - Test: a synthetic class added in a nested `src/fast_fsm` module, an unregistered instance-`__dict__` class, a silently omitted exception class, or a stale registry entry makes inventory validation fail with qualified class/path/line details.
  </behavior>
  <action>
    Add the fail-first slots inventory tests, then implement `slots-policy --json`. Recursively AST-scan every Python module below `src/fast_fsm`, including exception classes, and require every top-level importable production class to appear in the emitted audit as slot-protected/inherited-slot-protected or as a registered deliberate exception; declarations nested only under `if __name__ == "__main__"` demo code are not production inventory entries. Register both `fast_fsm.core.CompiledFuncCondition` and `fast_fsm.core.TransitionError`: both use `@mypyc_attr(native_class=False)` and can have an instance `__dict__`; the former preserves the interpreted `Condition` boundary and the latter preserves normal Python exception behavior. Instantiate and measure both registered exceptions plus representative ordinary slotted classes. Fail on an unregistered exception, a registry entry no longer found in source, or any discovered class omitted from the report, with qualified class/path/line diagnostics. Do not redesign either class. Emit deterministic class names, policy classification, exception reason, `has_instance_dict`, and environment-labeled instance byte measurements for later manifest collection. Commit only the two listed paths.
  </action>
  <acceptance_criteria>
    - Unexpected missing slots and stale allowlist entries both fail with actionable source locations.
    - `CompiledFuncCondition` and `TransitionError` are independently measured, not merely asserted in prose.
    - The recursive audit accounts for every relevant class under `src/fast_fsm` and cannot silently omit a future exception.
    - The result is deterministic except for explicitly labeled environment-specific byte measurements.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_release_evidence.py -x -q -k slots</automated>
  </verify>
  <done>The repository has a tested recursive slots policy whose two registered deliberate exceptions are explicit, independently measured, and protected against future silent omissions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment -> build parser | Developer/CI-controlled strings select packaging behavior. |
| filesystem/archive -> verifier | Local paths and wheel member names are untrusted verifier inputs. |
| verifier -> evidence consumer | Incorrect origin or artifact classification could certify the wrong implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|--------------|---------------|
| T-15-01 | Tampering | build-mode environment | high | mitigate | Reject invalid/conflicting selectors before setup fallback; preserve exact enum values. | Selector conflict/invalid subprocess tests. | A caller with control of the build environment can intentionally choose a valid different mode; recorded intent makes that auditable. |
| T-15-02 | Tampering | pure-source import resolution | high | mitigate | Scan native suffixes before import, require `.py` origin, and fail without deleting files. | Stale-shadow test asserts exit code, path text, import ordering, and fixture survival. | Exotic import hooks outside the inspected package path remain possible; clean-checkout verification in Plan 03 narrows that risk. |
| T-15-03 | Tampering | wheel artifact-set identity | high | mitigate | Inspect every repeated wheel input's normalized filename, tags, WHEEL/METADATA, and native members without extraction; reject contradictions, duplicates, or collapsed records. | Pure/native mismatch plus one-pure/multiple-compiled order-independence tests. | Full installed compiled behavioral parity and publication are intentionally enforced in Phase 20. |
| T-15-04 | Information Disclosure | verifier output | medium | mitigate | Emit normalized repository-relative paths and selected tool facts only; never serialize environment variables. | JSON schema/output tests. | Exact local shadow paths appear in human error output because D-15 requires actionable diagnostics. |
| T-15-05 | Denial of Service | crafted archive | low | accept | Read central-directory/member names only and never extract; CI inputs are repository-built wheels. | Archive fixture tests complete under focused-test budget. | A very large malicious archive can consume inspection time; this maintainer CLI is not a network service. |
| T-15-28 | Tampering | slots-policy inventory | high | mitigate | Recursively enumerate every top-level importable class under `src/fast_fsm`, require an emitted classification for each, register exactly both source-proven exceptions, and fail on new/unregistered/omitted/stale entries. | Nested-module, unregistered-`__dict__`, omitted-exception, and stale-registry tests with path/line diagnostics. | A future policy change requires an explicit reviewed registry and documentation update. |
</threat_model>

## Artifacts this phase produces

- New files: `tools/__init__.py`, `tools/build_modes.py`, `tools/release_evidence.py`, `MANIFEST.in`, `tests/test_build_modes.py`, `tests/test_release_evidence.py`.
- New symbols: `BuildMode`, `resolve_build_mode`, native-shadow scanner, source-origin classifier, wheel inspector, slots-policy inventory/measurement functions.
- New environment variable: `FAST_FSM_BUILD_MODE=auto|pure|compiled`.
- Preserved environment alias: `FAST_FSM_PURE_PYTHON=1` maps to pure mode.
- New CLI commands/flags: `verify-source --json`, `verify-wheel --wheel PATH [--wheel PATH ...] --json`, and `slots-policy --json`, all invoked through `uv run python tools/release_evidence.py`.
- No public package API, database/schema field, or generated manifest is created by this plan.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Trustworthy version, gates, toolchain, and pure-source evidence | 01-06 | COVERED | End-to-end across all plans and remote proof. |
| REQ | REL-02 | Additive v0.2.3 mismatch correction | 03,05 | COVERED | Local history plus external notice. |
| REQ | REL-04 | Intentional identifiable pure wheel | 01,02 | COVERED | Selector, wheel verifier, manifest. |
| REQ | REL-05 | Clean complete gate and documented baseline | 02-06 | COVERED | Independent CI, scoped Ruff repair, clean manifest, docs, and exact-SHA Actions proof. |
| REQ | REL-06 | Pinned release tools and stable typing | 01,02,05 | COVERED | Python-3.10-compatible selector, exact uv/build pins, mypy/ty authority, supported-version matrix, and remote completion evidence. |
| REQ | REL-08 | Measured slots exceptions | 01,02,04 | COVERED | Recursive inventory, two-exception registry, manifest, durable policy. |
| REQ | TEST-02 | No stale native shadow and meaningful source coverage | 01-03 | COVERED | Preflight, clean coverage, regression gate. |
| RESEARCH | R-01 | Shared verification CLI and deterministic manifest | 01,02 | COVERED | Standard-library primitives then collection. |
| RESEARCH | R-02 | Independent PR/release gate categories | 02,05 | COVERED | Separate jobs, reusable release gate, and exact-SHA all-job record. |
| RESEARCH | R-03 | Clean-checkout evidence and non-regression coverage | 02,03,05,06 | COVERED | Write/check modes, scoped Ruff repair, isolated regeneration, and remote proof. |
| RESEARCH | R-04 | Dated correction and external audit | 03,05 | COVERED | Canonical correction file and checkpoint. |
| RESEARCH | R-05 | Durable rounded prose, exact generated evidence | 03,04 | COVERED | Manifest owns exact observations. |
| CONTEXT | D-01 | v0.2.3 shipped with defective metadata | 03 | COVERED | Historical correction text. |
| CONTEXT | D-02 | Preserve tag and artifacts | 03,05 | COVERED | Live history check and additive release edit. |
| CONTEXT | D-03 | Dated v0.2.2/v0.2.3 changelog sections | 03 | COVERED | Changelog task. |
| CONTEXT | D-04 | Same GitHub release correction; no README warning | 03,05 | COVERED | Canonical source and external checkpoint. |
| CONTEXT | D-05 | Mypy blocking; ty advisory | 02 | COVERED | Taskfile and CI jobs. |
| CONTEXT | D-06 | Measured pure coverage baseline | 02,03 | COVERED | Manifest write/check and clean regeneration. |
| CONTEXT | D-07 | Complete gate on PR/release | 02,05 | COVERED | Reusable independent jobs plus exact-commit remote run. |
| CONTEXT | D-08 | Independent blocking checks | 02,05 | COVERED | Failure categories remain visible and are recorded individually. |
| CONTEXT | D-09 | Narrative says 700+ | 04 | COVERED | Docs task. |
| CONTEXT | D-10 | Tracked machine-readable manifest and CI summary | 02 | COVERED | Deterministic JSON and summary rendering. |
| CONTEXT | D-11 | Explicit write/check; CI never writes | 02 | COVERED | Separate commands and freshness gate. |
| CONTEXT | D-12 | Stable performance contract; exact observation in evidence | 02,04 | COVERED | Schema and docs. |
| CONTEXT | D-13 | Auto/pure/compiled selector and alias | 01 | COVERED | Tracer task. |
| CONTEXT | D-14 | Canonical artifact/origin verifier; no runtime API | 01,02 | COVERED | Maintainer CLI. |
| CONTEXT | D-15 | Non-destructive stale-shadow failure | 01 | COVERED | Fail-first integration test. |
| CONTEXT | D-16 | Evidence supports compiled wheels plus universal pure fallback | 01,02 | COVERED | Mode/schema contract; publication stays Phase 20. |

Excluded, not missing: REL-03, REL-01, REL-07, TEST-01, and TEST-03 through TEST-07 are explicitly deferred to Phase 20; no task implements strict compiled publication or installed compiled/pure conformance.

<verification>
<automated>uv run pytest tests/test_build_modes.py tests/test_release_evidence.py -x -q</automated>
<automated>uv run mypy tools/build_modes.py tools/release_evidence.py</automated>
</verification>

<success_criteria>
- Build intent is deterministic and backward compatible.
- Pure-source and wheel identity checks fail closed on contradictions without destructive cleanup.
- Slots-policy evidence recursively accounts for all relevant production classes and explicitly measures `CompiledFuncCondition` and `TransitionError`.
- All focused tests and mypy checks pass using `uv`.
</success_criteria>

<output>
Create `.planning/phases/15-release-baseline-evidence-harness/15-01-SUMMARY.md` when done.
</output>
