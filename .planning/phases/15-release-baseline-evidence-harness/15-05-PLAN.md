---
phase: 15-release-baseline-evidence-harness
plan: 05
type: execute
wave: 5
depends_on:
  - 15-03
  - 15-04
files_modified: []
autonomous: false
requirements:
  - REL-02
  - REL-05
  - REL-06
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The GitHub v0.2.3 release notice contains the same concise correction facts as the tracked canonical record (D-04)."
    - "The existing GitHub release/tag and any artifacts remain in place; the correction is additive (D-01, D-02)."
    - "The repository does not claim the external correction was published until an authenticated maintainer verifies the live notice."
    - "The exact pushed Phase 15 commit has a completed GitHub Actions run whose independently named jobs prove the complete gate and locked sync/build on Python 3.10-3.14 before REL-05 or REL-06 can close."
  artifacts:
    - path: "GitHub release v0.2.3"
      provides: "Authenticated external correction notice"
    - path: "docs/release-corrections/v0.2.3.md"
      provides: "Canonical local wording source"
    - path: "GitHub Actions run for the pushed Phase 15 commit"
      provides: "Remote all-job proof including supported-Python locked sync/build results"
  key_links:
    - from: "docs/release-corrections/v0.2.3.md"
      to: "GitHub release v0.2.3 body"
      via: "authenticated additive edit followed by live `gh release view` comparison"
      pattern: "v0.2.3"
---

<objective>
Prove the pushed Phase 15 gates remotely, then publish and independently verify the authenticated external half of the v0.2.3 correction.

Purpose: Close REL-02 without pretending repository tests can mutate or prove GitHub state. This explicit checkpoint supplies the authority boundary required by D-04.
Output: Recorded all-job Actions evidence for the exact pushed commit plus a live GitHub v0.2.3 release notice containing the canonical additive correction while preserving URL, tag target, and artifacts.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-release-baseline-evidence-harness/15-CONTEXT.md
@.planning/phases/15-release-baseline-evidence-harness/15-03-SUMMARY.md
@docs/release-corrections/v0.2.3.md
@docs/dev/releasing.md
@CHANGELOG.md

<flagged_assumptions>
- REL-02 assumption: GitHub authentication, repository write authority, and current release-body content are external state. Resolve it by stopping at this blocking checkpoint, preserving the existing body, making an additive authenticated edit, and checking the live result before resuming.
</flagged_assumptions>
</context>

<execution_constraints>
- This plan pushes already-scoped Phase 15 commits, observes/dispatches GitHub Actions, and edits only the existing release body; it must not modify repository file content, move/delete Git refs or tags, or upload/delete artifacts.
- The phase bead `fast_fsm-lw2` is already claimed and `in_progress`; inspect it with `bd show fast_fsm-lw2 --json`. Create any newly discovered work with `bd create ... --deps discovered-from:fast_fsm-lw2 --json`. Do not close the phase bead in this plan: the root orchestrator closes it only after the final Phase 15 verifier passes, not after Plan 05.
- Preserve the existing release body and append/update only the canonical correction paragraph. Never replace the body with the paragraph alone.
- Do not expose GitHub tokens or authentication output in summaries/evidence.
- If `gh auth status` fails or the account lacks release-write authority, stop without claiming REL-02 complete.
</execution_constraints>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wait for and record every remote gate on the exact pushed commit</name>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
    - evidence/release-baseline.json
    - docs/dev/releasing.md
  </read_first>
  <precondition>All Phase 15 repository plans are committed on `feat/release-baseline-evidence`, `git branch --show-current` prints exactly that branch, `gh auth status` can read Actions for `radioflyer28/fast_fsm`, and the branch can be pushed without including unrelated uncommitted files.</precondition>
  <reversibility rating="reversible">Pushing the scoped Phase 15 commits and dispatching a read-only CI run can be followed by corrective commits/runs; this task does not publish package artifacts.</reversibility>
  <files>External GitHub Actions run and `15-05-SUMMARY.md` evidence only; no source modification</files>
  <action>
    Push only `feat/release-baseline-evidence` with the project landing workflow, record the exact `git rev-parse HEAD` SHA, and verify `refs/remotes/origin/feat/release-baseline-evidence` resolves to that SHA. Find the CI run for that exact head SHA or dispatch `.github/workflows/ci.yml` at `feat/release-baseline-evidence` when no matching run exists. Wait with `gh run watch RUN_ID --exit-status`; do not treat workflow YAML presence, queueing, or a partial matrix as proof. Fetch `gh run view RUN_ID --json url,headSha,status,conclusion,jobs` and record every job name/conclusion in the plan summary. Require independently visible successful format, lint, mypy, OS/Python tests, HTML docs, doctest, pure coverage/freshness, mypyc smoke, performance, and supported-Python locked sync/build results for Python 3.10, 3.11, 3.12, 3.13, and 3.14; advisory ty must be present and recorded but may be nonblocking per D-05. Verify the remote run reports uv `0.12.6` through the manifest/setup contract. Stop on a missing, cancelled, skipped blocking, or failed required job and leave REL-05/REL-06 open.
  </action>
  <what-built>The reusable independent CI gates, exact uv setup, supported-Python build matrix, clean manifest, and durable documentation from Plans 01-04 and 06.</what-built>
  <how-to-verify>
    1. Verify `git branch --show-current` prints `feat/release-baseline-evidence`, push that branch, record `HEAD_SHA=$(git rev-parse HEAD)`, and verify `git rev-parse refs/remotes/origin/feat/release-baseline-evidence` equals the same SHA.
    2. Run `gh run list --repo radioflyer28/fast_fsm --workflow ci.yml --commit "$HEAD_SHA" --json databaseId,headSha,status,conclusion,url,event --limit 20`; if none exists, dispatch `gh workflow run ci.yml --repo radioflyer28/fast_fsm --ref feat/release-baseline-evidence` and locate its exact-SHA run.
    3. Run `gh run watch <run-id> --repo radioflyer28/fast_fsm --exit-status` to a terminal conclusion.
    4. Run `gh run view <run-id> --repo radioflyer28/fast_fsm --json url,headSha,status,conclusion,jobs` and check every required independent job plus all Python 3.10-3.14 locked-sync/build entries.
    5. Record the run URL, exact head SHA, and the complete redacted job-name/conclusion table in `15-05-SUMMARY.md`; do not record tokens or authentication output.
  </how-to-verify>
  <acceptance_criteria>
    - The watched Actions run is terminal and identifies the exact pushed Phase 15 commit SHA.
    - The pushed and watched ref is exactly `feat/release-baseline-evidence`; Phase 15 implementation was not committed directly to `main`.
    - Every required blocking job is present and successful; the advisory ty outcome is visible and separately labeled.
    - Python 3.10, 3.11, 3.12, 3.13, and 3.14 each complete locked sync plus pure sdist/unpacked-wheel proof using uv 0.12.6.
    - The summary records the run URL, SHA, and all job conclusions without credentials; REL-05/REL-06 remain open on any missing or incomplete evidence.
  </acceptance_criteria>
  <verify>
    <automated>gh run list --repo radioflyer28/fast_fsm --workflow ci.yml --commit "$(git rev-parse HEAD)" --status completed --json headSha,status,conclusion,url --limit 1</automated>
  </verify>
  <resume-signal>Type `approved` only after the exact-SHA run finishes and the complete required job table, including all five supported-Python build jobs, is recorded; otherwise provide the failed/missing job names.</resume-signal>
  <done>The exact pushed Phase 15 commit has complete remote all-job proof, including locked sync/build on every supported Python, so REL-05 and REL-06 can proceed to final phase verification.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Authorize, publish, and verify the v0.2.3 GitHub correction</name>
  <read_first>
    - docs/release-corrections/v0.2.3.md
    - docs/dev/releasing.md
    - CHANGELOG.md
  </read_first>
  <precondition>`gh auth status` identifies an account with release-write access to `radioflyer28/fast_fsm`, and `gh release view v0.2.3` returns the existing release without exposing credentials.</precondition>
  <reversibility rating="reversible">A release-body edit can be corrected through another authenticated edit without moving the tag or replacing artifacts.</reversibility>
  <files>External GitHub release v0.2.3 notice only; no repository file modification</files>
  <action>
    Pause for explicit maintainer authorization because this changes external public state. Following `docs/dev/releasing.md`, capture the pre-edit `gh release view` JSON containing URL, tag name, assets, and body in a private temporary directory, and separately capture the remote tag object plus peeled target SHA with `git ls-remote origin refs/tags/v0.2.3 'refs/tags/v0.2.3^{}'`. Preserve the current body, add the exact whitespace-normalized canonical correction paragraph from `docs/release-corrections/v0.2.3.md` without discarding existing notes, and apply the edit with authenticated `gh release edit`. Capture the same live JSON and tag-target evidence after the edit. Compare URL and tag name exactly; compare a normalized asset identity list (name, size, digest/URL where available) exactly; compare the tag object/peeled target output exactly; and verify the normalized canonical paragraph is contained in the live body. Do not move the tag, upload/delete artifacts, change package-index state, or edit README. Record only the release URL, tag name, target SHA, asset identity/count, and redacted boolean comparison results in the summary—not credentials, auth output, temporary paths, or the full body.
  </action>
  <what-built>The repository-side canonical correction, changelog record, history audit, and operator runbook from Plan 03.</what-built>
  <how-to-verify>
    1. Run `gh auth status` and confirm the expected maintainer account/repository access.
    2. Run `gh release view v0.2.3 --repo radioflyer28/fast_fsm --json url,body,tagName,assets` and preserve the JSON in a private temporary file; capture `git ls-remote origin refs/tags/v0.2.3 'refs/tags/v0.2.3^{}'` separately.
    3. Follow `docs/dev/releasing.md` to append or reconcile the canonical correction, then run `gh release edit v0.2.3 --repo radioflyer28/fast_fsm --notes-file <preserved-and-corrected-body>`.
    4. Re-run both captures, normalize the canonical file by excluding Markdown headings and collapsing whitespace, and require that exact normalized paragraph within the normalized live body.
    5. Confirm URL, tag name, normalized asset identities, tag object, and peeled target SHA are byte-identical before/after; record only redacted comparison booleans plus public URL/tag/SHA/asset identities in the summary.
  </how-to-verify>
  <instructions>
    Use the five numbered steps in `<how-to-verify>`. Preserve the existing release body in a temporary file, reconcile only the canonical correction paragraph, and stop immediately if authentication, repository ownership, or pre/post tag/asset comparison is uncertain.
  </instructions>
  <acceptance_criteria>
    - An authenticated maintainer explicitly authorizes the external edit.
    - Live GitHub body contains the exact whitespace-normalized canonical correction paragraph while retaining pre-existing release notes.
    - The release URL, tag name, remote tag object/peeled target SHA, and normalized artifact identity list are unchanged.
    - The public URL/tag/SHA/asset identities and redacted successful comparison booleans are recorded in the plan summary without secrets or full body text.
  </acceptance_criteria>
  <verify>
    <automated>gh release view v0.2.3 --repo radioflyer28/fast_fsm --json url,body,tagName,assets | uv run python -c "import json,sys; from pathlib import Path; d=json.load(sys.stdin); canonical=' '.join(line.strip() for line in Path('docs/release-corrections/v0.2.3.md').read_text().splitlines() if line.strip() and not line.lstrip().startswith('#')); live=' '.join(d['body'].split()); assert d['tagName']=='v0.2.3' and canonical in live; print(d['url'])"</automated>
  </verify>
  <verification>
    Run the canonical live-body assertion, compare pre/post URL, tag name, normalized assets, tag object, and peeled target SHA, and record public identity facts plus redacted comparison booleans in the summary.
  </verification>
  <resume-signal>Type `approved` after the authenticated edit and live verification succeed, or provide the authorization/error output with secrets removed.</resume-signal>
  <done>The live GitHub v0.2.3 release notice carries the canonical additive correction and the historical tag/artifacts remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository -> authenticated GitHub | Canonical text crosses into public external state. |
| pushed commit -> GitHub Actions | Remote jobs must prove the exact commit rather than a nearby branch state. |
| maintainer credentials -> gh CLI | Privileged credentials authorize release mutation. |
| live release body -> phase evidence | Remote state must be re-read before the phase claims completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|--------------|---------------|
| T-15-20 | Elevation of Privilege | GitHub release edit | high | mitigate | Blocking explicit authorization, `gh auth status`, repository-scoped command, and no token capture. | Precondition plus live authenticated view. | A compromised maintainer account remains GitHub/account-security risk. |
| T-15-21 | Tampering | existing release body/tag/assets | high | mitigate | Preserve body, edit notes only, and compare tag/assets before and after; prohibit retag/upload/delete. | Pre/post `gh release view` and asset comparison. | GitHub UI may normalize Markdown rendering without changing semantic content. |
| T-15-22 | Repudiation | publication claim | medium | mitigate | Record release URL and post-edit live check in summary; never infer success from local files. | Automated body assertion and checkpoint approval. | GitHub retains the authoritative edit audit history. |
| T-15-23 | Information Disclosure | credentials/output | high | mitigate | Never print tokens, redact auth details, and store only URL/outcome. | Summary review; command uses gh credential store. | GitHub account identity is inherently visible on the public edit history. |
| T-15-26 | Spoofing | Actions run selection | high | mitigate | Match the watched run's `headSha` to the exact pushed commit and record every job conclusion. | `gh run list`/`watch`/`view` exact-SHA procedure. | GitHub-hosted Actions remains third-party platform evidence. |
| T-15-27 | Repudiation | supported-Python proof | high | mitigate | Require named Python 3.10-3.14 locked-sync/build jobs to reach terminal success; YAML presence and partial matrices do not count. | Complete job table in summary and blocking checkpoint approval. | A later runner image can differ; the run URL preserves the observed execution. |
</threat_model>

## Artifacts this phase produces

- External generated artifact: updated body text on the existing GitHub `v0.2.3` release.
- External evidence artifact: terminal GitHub Actions run for the exact pushed Phase 15 SHA, including all independent jobs and Python 3.10-3.14 locked-sync/build entries.
- Evidence recorded in `15-05-SUMMARY.md`: Actions URL/SHA/all-job conclusions; live release URL/tag/peeled target SHA/normalized assets; exact normalized correction comparison; and redacted unchanged-identity booleans.
- No new/modified repository file, package symbol, CLI, flag, command implementation, environment variable, schema field, Git tag, or release artifact.

<verification>
<automated>gh run list --repo radioflyer28/fast_fsm --workflow ci.yml --commit "$(git rev-parse HEAD)" --status completed --json headSha,status,conclusion,url --limit 1</automated>
<automated>gh release view v0.2.3 --repo radioflyer28/fast_fsm --json url,body,tagName</automated>
<human-check>Authenticated maintainer confirms the exact-SHA Actions job table is complete, the live correction matches `docs/release-corrections/v0.2.3.md`, and URL/tag target/artifacts remain unchanged.</human-check>
</verification>

<success_criteria>
- The authenticated live notice carries the correction.
- The pre-existing release body, tag target, and artifacts are preserved.
- The exact pushed commit has terminal remote proof for every required gate and every supported-Python locked-sync/build entry.
- No local-only evidence is misrepresented as external publication proof.
</success_criteria>

<output>
Create `.planning/phases/15-release-baseline-evidence-harness/15-05-SUMMARY.md` when done.
</output>
