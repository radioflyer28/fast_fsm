---
phase: 15-release-baseline-evidence-harness
plan: 03
type: execute
wave: 4
depends_on:
  - 15-02
  - 15-06
files_modified:
  - tools/release_evidence.py
  - tests/test_release_evidence.py
  - Taskfile.yml
  - CHANGELOG.md
  - docs/release-corrections/v0.2.3.md
  - docs/dev/releasing.md
  - docs/index.rst
  - evidence/release-baseline.json
autonomous: true
requirements:
  - REL-02
  - REL-05
  - REL-08
  - TEST-02
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A maintainer can audit that v0.2.3 shipped with 0.2.2 metadata while the existing tag and artifacts remain unchanged (D-01, D-02)."
    - "The changelog has dated v0.2.2 and v0.2.3 sections containing the shipped entries and the v0.2.3 metadata correction (D-03)."
    - "One canonical correction paragraph is available for the GitHub v0.2.3 notice without adding a README warning (D-04)."
    - "A clean committed checkout establishes pure mode before locked sync, passes the immediate native-shadow/source-origin preflight, and regenerates evidence showing at least 722 passing tests, pure `.py` core origin, meaningful nonzero total/core source coverage, and both registered slots exceptions (REL-05, REL-08, TEST-02)."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Dated additive v0.2.2/v0.2.3 release history"
      contains: "0.2.3"
    - path: "docs/release-corrections/v0.2.3.md"
      provides: "Canonical correction paragraph used for the external GitHub release notice"
    - path: "docs/dev/releasing.md"
      provides: "Auditable release-history and evidence operator procedure"
    - path: "evidence/release-baseline.json"
      provides: "Clean-checkout generated exact Phase 15 baseline"
  key_links:
    - from: "docs/release-corrections/v0.2.3.md"
      to: "CHANGELOG.md"
      via: "same facts: shipped release, defective 0.2.2 metadata, immutable tag/artifacts, correction path"
      pattern: "0.2.2"
    - from: "Taskfile.yml"
      to: "tools/release_evidence.py"
      via: "release-history-check compares live tag metadata, changelog, and canonical correction"
      pattern: "verify-history"
    - from: "evidence/release-baseline.json"
      to: "clean committed checkout"
      via: "explicit write then read-only check from isolated worktree"
      pattern: "quality_baseline"
---

<objective>
Repair the historical release record additively and produce the authoritative Phase 15 baseline from a clean committed checkout.

Purpose: Convert the new harness and the scoped Ruff repair from Plan 06 into auditable proof: immutable v0.2.3 history is documented rather than rewritten, and exact evidence comes from uncontaminated pure source.
Output: Canonical historical correction, release runbook, and refreshed tracked evidence after the dedicated gate repair.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-release-baseline-evidence-harness/15-CONTEXT.md
@.planning/phases/15-release-baseline-evidence-harness/15-RESEARCH.md
@.planning/phases/15-release-baseline-evidence-harness/15-PATTERNS.md
@.planning/phases/15-release-baseline-evidence-harness/15-VALIDATION.md
@.planning/phases/15-release-baseline-evidence-harness/15-02-SUMMARY.md
@CHANGELOG.md
@pyproject.toml
@evidence/release-baseline.json

<flagged_assumptions>
- REL-02 assumption: the current external GitHub notice state is unknown and source alone cannot prove publication. Resolve local auditability here with immutable-tag verification and one canonical correction file; Plan 05 performs the authenticated external audit/update.
- REL-05 assumption: the live worktree is not clean. Resolve provenance by committing each scoped task, generating in an isolated worktree at the committed HEAD, and checking the copied manifest without relying on ignored native artifacts.
- TEST-02 assumption: a developer checkout may contain platform-native shadows. Resolve it by treating the verifier's non-destructive failure as a hard stop and using an isolated clean worktree rather than cleaning user files.
</flagged_assumptions>
</context>

<execution_constraints>
- Use `uv` for every Python, build, test, and docs operation.
- Require local uv `0.12.6`, matching the exact setup-uv and manifest contract established in Plan 02.
- The phase bead `fast_fsm-lw2` is already claimed and `in_progress`; inspect it with `bd show fast_fsm-lw2 --json`. Create any newly discovered work with `bd create ... --deps discovered-from:fast_fsm-lw2 --json`. Do not close the phase bead: the root orchestrator closes it only after the final Phase 15 verifier passes.
- Preserve unrelated dirty changes and ignored binaries. Stage/commit only the files listed by the active task; never use `git add .` or `git add -A`.
- Never move the v0.2.3 tag, replace published artifacts, or change `pyproject.toml` to pretend the shipped artifact had correct metadata.
- Do not add the v0.2.3 historical warning to README. Plan 04 updates only durable claims there.
- Do not delete a stale native module. The pure verifier reports it, and clean evidence runs in an isolated checkout.
</execution_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the additive v0.2.3 correction and executable history audit</name>
  <read_first>
    - CHANGELOG.md
    - pyproject.toml
    - tools/release_evidence.py
    - tests/test_release_evidence.py
    - Taskfile.yml
    - .planning/phases/15-release-baseline-evidence-harness/15-CONTEXT.md
  </read_first>
  <files>tools/release_evidence.py, tests/test_release_evidence.py, Taskfile.yml, CHANGELOG.md, docs/release-corrections/v0.2.3.md</files>
  <behavior>
    - Test: fixture history with a v0.2.3 tag whose pyproject version is 0.2.2 passes only when the correction record names the mismatch and preserves the tag/artifact policy.
    - Test: missing dated sections, altered correction facts, moved-tag expectations, or a correction file that diverges from the changelog fail with an actionable diff.
  </behavior>
  <action>
    Add fixture tests, then implement `verify-history --tag v0.2.3 --correction docs/release-corrections/v0.2.3.md` using subprocess argument arrays. Reorganize `CHANGELOG.md` into dated v0.2.2 and v0.2.3 sections and move the currently misplaced shipped entries out of `Unreleased` per D-03. State exactly that v0.2.3 shipped with defective 0.2.2 package metadata, remains a shipped release, and keeps the existing tag/artifacts per D-01 and D-02. Add one concise canonical correction paragraph in `docs/release-corrections/v0.2.3.md` for the GitHub notice per D-04. Add `task release-history-check`; Plan 02's full-history checkout makes the tag available in CI. Do not edit README. Commit only the five listed paths.
  </action>
  <acceptance_criteria>
    - Dated v0.2.2 and v0.2.3 changelog sections contain the correct shipped changes.
    - The canonical correction and changelog describe the same immutable-history facts and direct users to the correction/v0.3.0 path without describing v0.2.3 as withdrawn.
    - The history verifier proves `v0.2.3:pyproject.toml` declared 0.2.2 and does not mutate Git refs or artifacts.
    - No persistent historical warning appears in README.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_release_evidence.py -x -q -k release_history</automated>
    <automated>task release-history-check</automated>
  </verify>
  <done>The repository contains an additive, source-tested correction record and a live immutable-tag audit command ready for the external notice checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and check the authoritative baseline from a clean committed checkout</name>
  <read_first>
    - docs/release-corrections/v0.2.3.md
    - CHANGELOG.md
    - Taskfile.yml
    - evidence/release-baseline.json
    - docs/index.rst
    - docs/dev/contributing.md
  </read_first>
  <files>docs/dev/releasing.md, docs/index.rst, evidence/release-baseline.json</files>
  <action>
    Add a concise release runbook describing exact uv `0.12.6`, pure mode before/during locked sync and every install, the mandatory immediate source-origin preflight, independent gate commands, explicit `release-baseline-write` followed by review and `release-baseline-check`, multi-wheel/source identity, non-destructive native-shadow remediation, immutable v0.2.3 history, the supported-Python Actions proof, and the authenticated external notice procedure. Link it from the developer-guide toctree. After Plan 06 and Task 1 are committed, create an isolated temporary git worktree at current HEAD and export `FAST_FSM_BUILD_MODE=pure` before environment creation. Run exactly `FAST_FSM_BUILD_MODE=pure uv sync --locked`, then immediately `FAST_FSM_BUILD_MODE=pure uv run python tools/release_evidence.py verify-source --json`; stop without running tests, coverage, builds, installs, or artifact collection if either command fails. Keep `FAST_FSM_BUILD_MODE=pure` on every later `uv sync --locked`, install/build, Taskfile, and `uv run` command in this clean path. Only after the preflight succeeds, run the complete pure release gate and `FAST_FSM_BUILD_MODE=pure task release-baseline-write`, then copy only `evidence/release-baseline.json` back to the working tree. Do not remove any native artifact from the developer checkout. Require at least 722 passing tests, `.py` core origin, nonzero meaningful total plus core coverage, exact uv `0.12.6`, a complete recursively discovered slots inventory, and independent registered/measurement records for both `CompiledFuncCondition` and `TransitionError`; then repeat the same pure locked-sync -> immediate preflight sequence in an equivalent clean worktree before `FAST_FSM_BUILD_MODE=pure task release-baseline-check`. Commit only the three listed paths.
  </action>
  <acceptance_criteria>
    - The runbook gives copyable `uv`/Taskfile commands and distinguishes local source proof from the external GitHub action.
    - Clean evidence records at least 722 passed with zero failures/errors, pure `.py` core origin, exact tool versions, universal pure-wheel identity, nonzero total/core source coverage, and independent registered/measurement records for `CompiledFuncCondition` and `TransitionError`.
    - The committed manifest passes read-only check mode without mutation from an equivalent clean checkout.
    - Every blocking quality/documentation command succeeds independently before baseline commit.
    - Every clean regeneration/check path sets `FAST_FSM_BUILD_MODE=pure` before and during the initial `uv sync --locked` and every later install/build, then runs `verify-source --json` immediately after sync and before tests, coverage, or artifact collection.
  </acceptance_criteria>
  <verify>
    <automated>FAST_FSM_BUILD_MODE=pure uv sync --locked</automated>
    <automated>FAST_FSM_BUILD_MODE=pure uv run python tools/release_evidence.py verify-source --json</automated>
    <automated>FAST_FSM_BUILD_MODE=pure uv run python -c "import json; d=json.load(open('evidence/release-baseline.json')); names={x['qualified_name'] for x in d['slots_policy']['registered_exceptions']}; assert d['toolchain']['uv']=='0.12.6' and d['quality_baseline']['tests']['passed'] in range(722,1000000) and d['artifact_evidence']['source']['core_origin'].endswith('.py') and d['slots_policy']['inventory'] and names=={'fast_fsm.core.CompiledFuncCondition','fast_fsm.core.TransitionError'}"</automated>
    <automated>FAST_FSM_BUILD_MODE=pure task release-baseline-check</automated>
  </verify>
  <done>A clean committed checkout produces and re-checks the authoritative pure-source evidence, and maintainers have an auditable release procedure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| immutable git history -> correction record | Historical tag contents are evidence, while current prose is mutable. |
| dirty developer checkout -> clean baseline | Ignored native files and unrelated changes must not contaminate proof. |
| canonical correction -> external operator | Local text will later be copied into GitHub release state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|--------------|---------------|
| T-15-11 | Tampering | v0.2.3 history | high | mitigate | Read tag contents, assert metadata mismatch, and document additively; never write refs or artifacts. | `task release-history-check` plus git status/ref comparison during execution. | A remote host could differ from local fetched refs; Plan 05 audits the GitHub release directly. |
| T-15-12 | Spoofing | clean-checkout provenance | high | mitigate | Generate from a temporary worktree at committed HEAD, set pure mode before/during every locked install, and run native-shadow/source-origin verification immediately after sync before any collection. | Concrete pure sync -> immediate preflight -> complete gate -> manifest write/check sequence runs inside the isolated checkout. | Hosted CI environment can differ from the reference measurement environment; stable/observational fields are separated. |
| T-15-13 | Tampering | developer native artifacts | high | mitigate | Never clean automatically; fail with exact paths and use an isolated worktree for proof. | Shadow fixture survival test and clean origin recorded in manifest. | Developer must perform explicit cleanup before local pure runs outside isolation. |
| T-15-14 | Repudiation | correction wording | medium | mitigate | Store one canonical correction file and test its required facts against changelog/history. | Release-history fixture tests and canonical-file diff. | GitHub publication remains unproven until Plan 05. |
| T-15-15 | Information Disclosure | baseline environment | low | accept | Record selected Python/platform/tool facts only; normalize paths and exclude secrets/environment dumps. | Manifest schema tests. | Public evidence intentionally discloses coarse platform/tool versions. |
</threat_model>

## Artifacts this phase produces

- New files: `docs/release-corrections/v0.2.3.md` and `docs/dev/releasing.md`.
- Updated historical artifact: dated v0.2.2/v0.2.3 sections in `CHANGELOG.md`; the Git tag and published artifacts are unchanged.
- New CLI command/flags: `verify-history --tag v0.2.3 --correction PATH`.
- New Taskfile command: `release-history-check`.
- Updated generated artifact: `evidence/release-baseline.json` regenerated from a clean committed checkout with exact test/coverage/tool/origin/wheel/performance evidence plus a recursive slots inventory and independent `CompiledFuncCondition`/`TransitionError` exception measurements.
- New documentation navigation entry: release runbook in `docs/index.rst`.
- No new environment variable, public package symbol, database/schema field, tag, or published artifact.

<verification>
<automated>uv run ruff format --check src/ tests/</automated>
<automated>uv run ruff check src/ tests/</automated>
<automated>task release-history-check</automated>
<automated>FAST_FSM_BUILD_MODE=pure uv sync --locked</automated>
<automated>FAST_FSM_BUILD_MODE=pure uv run python tools/release_evidence.py verify-source --json</automated>
<automated>FAST_FSM_BUILD_MODE=pure task release-baseline-check</automated>
<automated>FAST_FSM_BUILD_MODE=pure uv run pytest tests/ -x -q</automated>
</verification>

<success_criteria>
- The v0.2.3 mismatch is explicitly auditable without retagging or artifact replacement.
- The repository quality defects are cleared.
- A clean pure-source run proves `.py` origin before any collection, at least 722 passing tests, meaningful coverage, both registered slots exceptions, and manifest freshness.
- The canonical correction is ready for authenticated publication in Plan 05 and is absent from README.
</success_criteria>

<output>
Create `.planning/phases/15-release-baseline-evidence-harness/15-03-SUMMARY.md` when done.
</output>
