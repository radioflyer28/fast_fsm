---
phase: 15-release-baseline-evidence-harness
plan: 08
type: execute
wave: 5
depends_on:
  - 15-03
files_modified:
  - tests/test_release_evidence.py
  - .github/workflows/ci.yml
autonomous: true
gap_closure: true
requirements:
  - REL-05
  - REL-06
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Every `.github/workflows/ci.yml` job that executes a Taskfile command provisions the official cross-platform Task runner before its first `task` invocation, using `arduino/setup-task@v3.0.0` with exact Task version `3.53.1` (REL-05, REL-06, D-07, D-08)."
    - "Workflow contract tests enumerate the complete Taskfile-consuming job set and fail when a current or newly added Taskfile-consuming job omits, mispins, or misorders its Task setup."
    - "The independent CI jobs, Python 3.10-3.14 and OS test matrices, uv 0.12.6 setup, pure-source sync/preflight ordering, compiled build smoke test, and benchmark gate retain their existing behavior."
    - "Plan 15-08 proves the corrected workflow locally; Plan 15-05 remains responsible for pushing the corrective SHA and recording a successful exact-SHA GitHub Actions rerun before Phase 15 closes."
  artifacts:
    - path: "tests/test_release_evidence.py"
      provides: "Fail-first CI contract proving complete, pinned, ordered Task runner provisioning"
    - path: ".github/workflows/ci.yml"
      provides: "Official pinned Task runner setup in every Taskfile-consuming CI job"
    - path: ".planning/phases/15-release-baseline-evidence-harness/15-08-SUMMARY.md"
      provides: "RED/GREEN command evidence, scoped commit SHAs, and open fast_fsm-6yg handoff to Plan 15-05"
  key_links:
    - from: "tests/test_release_evidence.py"
      to: ".github/workflows/ci.yml"
      via: "the contract enumerates each job containing a `run: task ...` step and asserts an earlier exact setup action/version step"
      pattern: "arduino/setup-task@v3.0.0|version: \"3.53.1\""
    - from: ".github/workflows/ci.yml"
      to: "Taskfile.yml"
      via: "the pinned Task binary resolves every existing named Taskfile command on Ubuntu, Windows, and macOS runners"
      pattern: "task (format-check|lint|typecheck-mypy|typecheck-ty|test|supported-python-build-check|release-baseline-check|docs-check|docs-test)"
    - from: "15-08-SUMMARY.md"
      to: "15-05-PLAN.md"
      via: "the local workflow repair leaves fast_fsm-6yg open for the subsequent pushed exact-SHA rerun"
      pattern: "fast_fsm-6yg"
---

<objective>
Restore executable hosted quality gates after exact-SHA GitHub Actions run 33274685952 failed every Taskfile-based job with exit 127 because `task` was unavailable.

Purpose: Make the independently named Phase 15 quality jobs actually invoke their canonical Taskfile commands on every supported GitHub-hosted OS while preserving all existing release-evidence contracts.
Output: Fail-first workflow contract coverage, pinned official Task provisioning in `.github/workflows/ci.yml`, and a verified handoff to Plan 15-05 for the corrective exact-SHA rerun.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-release-baseline-evidence-harness/15-CONTEXT.md
@.planning/phases/15-release-baseline-evidence-harness/15-RESEARCH.md
@.planning/phases/15-release-baseline-evidence-harness/15-PATTERNS.md
@.planning/phases/15-release-baseline-evidence-harness/15-VALIDATION.md
@.planning/phases/15-release-baseline-evidence-harness/15-02-SUMMARY.md
@.planning/phases/15-release-baseline-evidence-harness/15-03-SUMMARY.md
@.planning/phases/15-release-baseline-evidence-harness/15-05-PLAN.md
@.github/workflows/ci.yml
@tests/test_release_evidence.py
@Taskfile.yml
@.github/copilot-instructions.md

<gap_evidence>
- GitHub Actions run: `33274685952` for an exact pushed Phase 15 SHA.
- Observable failure: every job whose final command used the Taskfile exited 127 with `task: command not found`.
- Root cause boundary: `.github/workflows/ci.yml` sets up pinned uv and Python but never installs the cross-platform Task runner before Taskfile commands.
- Linked issue: `fast_fsm-6yg`; it stays open through this local repair and closes only after Plan 15-05 records a successful corrective exact-SHA Actions run.
</gap_evidence>

<flagged_assumptions>
- The GitHub-hosted runners are current `ubuntu-latest`, `windows-latest`, and `macos-latest` images, so the official Node 24 based `arduino/setup-task@v3.0.0` action is compatible. Preserve these hosted-runner labels and do not add a self-hosted-runner compatibility branch.
- Task `3.53.1` is the exact repository-consistent pin: `task --version` reports `3.53.1` in the planning environment, and the official setup action accepts exact versions through its `version` input.
- The existing failing run is diagnostic evidence, not completion evidence. Only Plan 15-05 may close the remote-proof gap after a successful run whose `headSha` exactly matches the pushed corrective commit.
</flagged_assumptions>
</context>

<execution_constraints>
- Execute on the existing `feat/release-baseline-evidence` branch. Before editing, `git branch --show-current` must print exactly `feat/release-baseline-evidence`; do not create a replacement branch and do not implement directly on `main`.
- Inspect `fast_fsm-6yg` with `bd show fast_fsm-6yg --json` and claim it with `bd update fast_fsm-6yg --claim --json` only if it is open and unclaimed. Keep it open after every local check because Plan 15-05 owns the pushed corrective exact-SHA run and closes the bead only after that run succeeds.
- Preserve unrelated dirty and staged work. Commit the RED contract change with only `tests/test_release_evidence.py`, then commit the GREEN workflow change with only `.github/workflows/ci.yml`; allow GSD to add only `15-08-SUMMARY.md` and scoped tracking metadata separately. Never use `git add .` or `git add -A`.
- Use `uv run` for every Python, pytest, and YAML-parse command. Use the existing Task binary only to run repository Taskfile gates; do not add a Python package, npm package, runtime dependency, lockfile entry, shell installer, OS-specific package-manager branch, or generated binary to the repository.
- Use exactly `arduino/setup-task@v3.0.0` with `version: "3.53.1"` in each applicable job. Put it after the existing `astral-sh/setup-uv@v5` step and before dependency synchronization and the first Taskfile command, producing an identical explicit setup contract on Ubuntu, Windows, and macOS.
- Modify only the nine CI jobs that invoke Taskfile commands: `format`, `lint`, `typecheck_mypy`, `typecheck_ty`, `test`, `supported_python_build`, `evidence`, `docs_html`, and `docs_doctest`. Do not merge jobs, alter `continue-on-error`, reduce matrices, change uv/Python pins, change pure-mode environment or preflight ordering, or modify the non-Taskfile `build_check` and `benchmark` job behavior.
- Do not dispatch or judge the corrective hosted run in this plan. After local verification and commits, record the exact handoff in `15-08-SUMMARY.md`; Plan 15-05 pushes, dispatches/watches, validates every job for the exact SHA, and closes `fast_fsm-6yg` only on success.
</execution_constraints>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Provision and prove pinned Task execution across every Taskfile-based CI job</name>
  <read_first>
    - .github/workflows/ci.yml
    - tests/test_release_evidence.py
    - Taskfile.yml
    - .planning/phases/15-release-baseline-evidence-harness/15-02-SUMMARY.md
    - .planning/phases/15-release-baseline-evidence-harness/15-03-SUMMARY.md
    - .planning/phases/15-release-baseline-evidence-harness/15-05-PLAN.md
    - .github/copilot-instructions.md
  </read_first>
  <files>tests/test_release_evidence.py, .github/workflows/ci.yml</files>
  <behavior>
    - RED contract: discover the top-level CI job bodies that contain `run: task ...`, assert the discovered job IDs are exactly `format`, `lint`, `typecheck_mypy`, `typecheck_ty`, `test`, `supported_python_build`, `evidence`, `docs_html`, and `docs_doctest`, and observe failure before workflow edits because no job provisions Task.
    - Pin contract: each discovered job contains `uses: arduino/setup-task@v3.0.0` with exact input `version: "3.53.1"`; a missing setup, a floating version, a different version, or a new unaccounted Taskfile-consuming job fails the focused test.
    - Ordering contract: the end of the pinned setup step occurs before that job's first `run: task ...` command; the test cannot pass from a setup step in a sibling job or a later unreachable position.
    - Preservation contract: existing assertions continue to prove independent job IDs, advisory ty, `fail-fast: false`, Python 3.10-3.14, exact uv 0.12.6, and locked-sync followed immediately by pure-source preflight; `build_check` and `benchmark` retain their existing non-Taskfile commands.
  </behavior>
  <action>
    RED: extend `tests/test_release_evidence.py` using the file's existing `_workflow_text`, job-block regex, and explicit contract-assertion style. Add a helper that returns complete top-level job bodies without conflating matrix children or steps, then add a focused test named for pinned Task runner provisioning. Make the test prove non-vacuous complete job discovery, exact action/input pins, and within-job setup-before-invocation ordering. Run only the new focused test and require it to fail for the missing setup; record the failing assertion and commit only the test file with a `test(15-08)` commit.

    GREEN: add an `Install pinned Task runner` step to each of the nine Taskfile-consuming jobs in `.github/workflows/ci.yml`, immediately after its existing uv setup block. Use the official cross-platform action `arduino/setup-task@v3.0.0` and exact `version: "3.53.1"`. Do not extract a composite action or alter any surrounding job, matrix, environment, sync, preflight, task command, compiled-build, or benchmark content. Run the focused Task/workflow contracts until green, parse the workflow as YAML, run the complete `tests/test_release_evidence.py`, and execute the local Phase 15 release gate. Commit only `.github/workflows/ci.yml` with a `fix(15-08)` commit. Record both scoped commit SHAs, the RED failure, GREEN results, exact setup choice, untouched job contracts, run 33274685952 diagnosis, and the still-open `fast_fsm-6yg` handoff in `15-08-SUMMARY.md`.
  </action>
  <acceptance_criteria>
    - The new focused contract demonstrably fails against the pre-fix workflow and passes after the workflow change.
    - The contract discovers exactly all nine current Taskfile-consuming job IDs and proves every one has the exact official action tag, exact Task version input, and setup-before-first-invocation ordering.
    - `.github/workflows/ci.yml` parses successfully and contains no changes outside the nine explicit setup steps.
    - Existing workflow contracts still prove independent jobs, separately advisory ty, Python 3.10-3.14, the full OS matrix, exact uv 0.12.6, locked sync followed immediately by pure-source verification, compiled smoke behavior, and benchmark behavior.
    - The complete release-evidence test file and local release gate pass after the repair.
    - The RED commit contains only `tests/test_release_evidence.py`; the GREEN commit contains only `.github/workflows/ci.yml`; unrelated dirty/staged files are untouched.
    - `fast_fsm-6yg` remains open, and `15-08-SUMMARY.md` assigns its closure plus the corrective exact-SHA rerun to Plan 15-05.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_release_evidence.py -x -q -k "task_runner or workflow_contract or setup_uv or supported_python"</automated>
    <automated>uv run python -c "from pathlib import Path; import yaml; data=yaml.safe_load(Path('.github/workflows/ci.yml').read_text(encoding='utf-8')); assert isinstance(data, dict) and isinstance(data.get('jobs'), dict); print('ci.yml jobs:', len(data['jobs']))"</automated>
    <automated>uv run pytest tests/test_release_evidence.py -x -q</automated>
    <automated>FAST_FSM_BUILD_MODE=pure uv run python tools/release_evidence.py verify-source --json</automated>
    <automated>FAST_FSM_BUILD_MODE=pure task release-gate</automated>
  </verify>
  <done>The CI workflow has a tested exact Task bootstrap before every Taskfile command, all local release contracts remain green, the two scoped commits are recorded, and the open remote-proof bead is handed to Plan 15-05.</done>
</task>

</tasks>

<threat_model>
## ASVS Target

OWASP ASVS Level 1 applies. The change crosses a third-party CI/action supply-chain boundary but adds no application authentication, request-data, archive-extraction, package-runtime, or public API surface.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository workflow -> third-party setup action | Hosted jobs execute Arduino's action code to acquire a Task binary. |
| setup action -> Go Task release asset | An exact Task version must be selected consistently on Linux, Windows, and macOS. |
| CI job steps -> Taskfile commands | The installed binary must be available before the first Taskfile invocation in the same job. |
| local workflow proof -> hosted Actions | Local text/YAML contracts cannot substitute for the corrective exact-SHA hosted run owned by Plan 15-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|--------------|---------------|
| T-15-32 | Tampering | Task runner supply chain | high | mitigate | Use Arduino's dedicated official action at immutable release tag `v3.0.0`, request exact Task `3.53.1`, and test both literals in every consuming job. | Focused non-vacuous pin contract plus scoped workflow diff. | A compromise of the upstream GitHub action/release remains third-party platform risk; Phase 15 does not vendor executables. |
| T-15-33 | Denial of Service | Taskfile-consuming CI jobs | high | mitigate | Provision Task within every consuming job before its first invocation and enumerate the complete job set so omitted/new jobs fail locally. | Ordering contract and corrective exact-SHA rerun in Plan 15-05. | GitHub or upstream release availability can still interrupt hosted jobs. |
| T-15-34 | Tampering | existing CI evidence contracts | high | mitigate | Limit changes to setup steps and retain exact uv, pure-mode, sync/preflight, matrices, independent jobs, compiled smoke, and benchmark commands. | Existing workflow contracts, YAML parse, full release-evidence tests, and local release gate. | Hosted runner image changes can affect later executions; exact run evidence records the observed environment. |
| T-15-35 | Repudiation | CI repair completion claim | medium | mitigate | Record RED/GREEN outputs and scoped SHAs locally, keep fast_fsm-6yg open, and require Plan 15-05 to bind terminal hosted results to the exact corrective SHA. | `15-08-SUMMARY.md` plus Plan 15-05 complete job table. | GitHub Actions remains the authoritative external execution record. |
</threat_model>

## Artifacts this phase produces

- Updated test contract: `tests/test_release_evidence.py` enumerates and validates every Taskfile-consuming CI job, exact official action/version pin, and within-job setup order.
- Updated workflow: `.github/workflows/ci.yml` installs Task `3.53.1` through `arduino/setup-task@v3.0.0` in the nine Taskfile-consuming jobs.
- Evidence artifact: `15-08-SUMMARY.md` records run 33274685952's root cause, RED/GREEN results, scoped commit SHAs, local gate results, exact setup choice, and the still-open `fast_fsm-6yg` handoff.
- No changed Taskfile command, Python source, runtime dependency, lockfile, public API, package build selector, pure-source preflight, Python/OS matrix, compiled smoke command, benchmark command, release artifact, Git tag, or GitHub release body.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Maintainers can trust the repository quality-gate and toolchain evidence before runtime semantics change. | Task 1 makes the canonical Taskfile gates executable and proves the bootstrap contract locally; Plan 15-05 proves the resulting exact SHA remotely. |
| REQ | REL-05 clean-checkout quality gates. | Task 1 restores execution of formatting, lint, mypy, tests, Sphinx HTML, doctest, and evidence freshness jobs without merging categories. |
| REQ | REL-06 reproducibly pinned release-producing tools. | Task 1 pins the official setup action to v3.0.0 and the installed Task runner to exact version 3.53.1. |
| RESEARCH | Independent CI categories must call canonical Taskfile/tool commands rather than reimplement checks in YAML. | Task 1 preserves all independent jobs and their existing Taskfile commands, adding only the missing runner bootstrap. |
| RESEARCH | Exact uv 0.12.6, Python 3.10-3.14 validation, pure-source sync/preflight, and existing artifact/benchmark boundaries. | Existing contracts remain required and the action change is isolated from those steps. |
| CONTEXT | D-05 keeps mypy blocking and ty separately advisory. | The repair provisions both jobs identically while retaining `continue-on-error: true` only for ty. |
| CONTEXT | D-07 requires the complete quality gate on pull requests and releases. | Every Taskfile category becomes executable; Plan 15-05 supplies terminal exact-SHA proof. |
| CONTEXT | D-08 requires independently visible blocking checks. | The nine jobs remain independent and the contract continues to enumerate them separately. |
| CONTEXT | D-11 keeps CI evidence checking read-only. | The `evidence` job still invokes only `release-baseline-check`; no manifest write path is introduced. |
| GAP | Exact-SHA run 33274685952 returned exit 127 for every Taskfile-based job. | Task 1 installs pinned Task before every applicable invocation and reserves hosted rerun proof for Plan 15-05. |

Deferred Phase 20 compiled-wheel publication and installed-artifact parity remain excluded. Context decisions D-01-D-04, D-06, D-09-D-10, and D-12-D-16 remain fully owned by the already executed Phase 15 plans and are not modified by this focused CI-bootstrap gap closure. No relevant source item is omitted.

<verification>
<automated>uv run pytest tests/test_release_evidence.py -x -q -k "task_runner or workflow_contract or setup_uv or supported_python"</automated>
<automated>uv run python -c "from pathlib import Path; import yaml; data=yaml.safe_load(Path('.github/workflows/ci.yml').read_text(encoding='utf-8')); assert isinstance(data, dict) and isinstance(data.get('jobs'), dict)"</automated>
<automated>uv run pytest tests/test_release_evidence.py -x -q</automated>
<automated>FAST_FSM_BUILD_MODE=pure uv run python tools/release_evidence.py verify-source --json</automated>
<automated>FAST_FSM_BUILD_MODE=pure task release-gate</automated>
<human-check>Review the two scoped commits and confirm the workflow diff contains only nine identical pinned setup steps; do not accept local checks as remote proof or close fast_fsm-6yg before Plan 15-05's corrective exact-SHA run succeeds.</human-check>
</verification>

<success_criteria>
- A fail-first test guards the complete Taskfile-consuming job set, exact setup action/version pins, and within-job ordering.
- All nine Taskfile-consuming jobs provision Task 3.53.1 through `arduino/setup-task@v3.0.0` before invoking Taskfile commands on their existing runners.
- Independent jobs, advisory ty, Python 3.10-3.14 and OS matrices, uv 0.12.6, pure preflight, compiled build, and benchmark behavior are unchanged and covered by local contracts.
- YAML parsing, focused contracts, the full release-evidence test file, pure source verification, and the local release gate pass.
- The implementation is committed in scoped RED/GREEN commits without unrelated files.
- `fast_fsm-6yg` remains open and Plan 15-05 owns the pushed exact-SHA rerun, terminal job-table proof, and issue closure.
</success_criteria>

<output>
Create `.planning/phases/15-release-baseline-evidence-harness/15-08-SUMMARY.md` when done.
</output>
