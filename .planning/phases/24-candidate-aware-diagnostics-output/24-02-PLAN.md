---
phase: 24-candidate-aware-diagnostics-output
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/fast_fsm/_diagnostics.py
  - src/fast_fsm/validation.py
  - src/fast_fsm/visualization.py
  - tests/test_diagnostic_contracts.py
  - tests/test_validation.py
  - tests/test_visualization.py
autonomous: true
requirements:
  - DIAG-02
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-03: JSON, Mermaid, PlantUML, validation Markdown, visualization Markdown, sparse/dense adjacency, transition matrices, and generated paths emit every candidate with explicit numeric priority in canonical order."
    - "D-03: same-target candidates remain distinct in all candidate-bearing output, and `show_conditions=False` never hides priority."
    - "D-04: graph traversal, candidate rows, rendered edges, transition findings, and path steps charge the existing budget per candidate occurrence before work/result exposure."
    - "D-04: exact limits succeed and one-less limits raise the fixed redacted exhaustion exception with incomplete status; no partial output appears complete."
    - "D-01: every top-level output captures once and uses existing grammar-specific escaping plus opaque diagram state identifiers."
  artifacts:
    - path: "src/fast_fsm/_diagnostics.py"
      provides: "Priority-bearing sparse/dense adjacency, transition-matrix candidate records, and four-scalar generated path steps"
    - path: "src/fast_fsm/validation.py"
      provides: "Candidate counts plus priority-complete JSON and Markdown validation reports"
    - path: "src/fast_fsm/visualization.py"
      provides: "Priority-complete Mermaid, PlantUML, JSON, adjacency validation, and Markdown document sinks"
    - path: "tests/test_diagnostic_contracts.py"
      provides: "Same-target multiplicity, exact candidate-sized budget, path, and one-capture coverage"
    - path: "tests/test_validation.py"
      provides: "Transition-matrix shape, candidate metrics, report JSON/Markdown, and budget regressions"
    - path: "tests/test_visualization.py"
      provides: "Cross-format priority, escaping, stale-adjacency, and deterministic ordering goldens"
  key_links:
    - from: "_DiagnosticGraph.edges"
      to: "_sparse_adjacency/_dense_adjacency/_generate_paths"
      via: "one indexed candidate record and reservation per edge occurrence"
      pattern: "priority"
    - from: "_DiagnosticGraph.edges"
      to: "Mermaid/PlantUML/JSON renderers"
      via: "one shared transition label and numeric priority per rendered edge"
      pattern: "[priority"
    - from: "dense adjacency transitions"
      to: "Markdown documents and stale-matrix validation"
      via: "candidate-complete positional records including priority"
      pattern: "adjacency matrix does not match captured snapshot"
---

<objective>
Carry candidate multiplicity, numeric priority, and candidate-sized accounting through every existing diagnostic and output surface (D-01, D-03, D-04, DIAG-02).

Purpose: Make structured and human-readable diagnostics auditable representations of the finite ordered topology while preserving Phase 19 escaping, one-snapshot, and fail-explicit budget contracts.
Output: Priority-complete adjacency/path data, JSON, Mermaid, PlantUML, validation reports, Markdown documents, and exact budget-boundary tests.
</objective>

## Artifacts this phase produces

- Sparse and dense adjacency rows containing numeric `priority`, with transition-matrix cells evolved to ordered candidate records rather than target-name strings.
- Generated path steps shaped as `(from_state, event, to_state, priority)` so same-target candidate paths remain distinguishable.
- Mermaid and PlantUML labels ending in generated `[priority N]` text even when conditions are hidden.
- JSON and both Markdown report families with one candidate row per edge, explicit priority, and unchanged format-specific text escaping.
- Exact-limit/one-less tests proving every candidate inspection and emitted record remains under the existing shared diagnostic ledger.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-candidate-aware-diagnostics-output/24-CONTEXT.md
@.planning/phases/24-candidate-aware-diagnostics-output/24-RESEARCH.md
@.planning/phases/24-candidate-aware-diagnostics-output/24-PATTERNS.md
@.planning/phases/24-candidate-aware-diagnostics-output/24-VALIDATION.md
@.planning/phases/24-candidate-aware-diagnostics-output/24-01-SUMMARY.md
@.planning/phases/23-construction-declarative-serialization-parity/23-VERIFICATION.md
@.specify/decisions/ADR-006-bounded-diagnostics-and-safe-output.md
@.specify/decisions/ADR-007-priority-topology.md

<interfaces>
- Plan 24-01 supplies one `_DiagnosticEdge` per snapshot candidate with `priority`, scalar guard presence, and conservative static evidence in canonical source/trigger/priority order.
- `_DiagnosticBudget` owns `max_work`, `max_results`, `max_dense_cells`, and `max_path_expansions`; every reservation happens before the represented work or result is exposed.
- `_sparse_adjacency()` and `_dense_adjacency()` are the shared adapters consumed by validators and visualization; do not duplicate their candidate projection downstream.
- `_capture_diagnostic_graph()` returns one snapshot, one graph, and one ledger for each top-level renderer.
- Mermaid, PlantUML, and Markdown use distinct existing escaping functions; diagram states use opaque positional IDs.
- Fixed-shape public outputs raise `DiagnosticBudgetExceeded("diagnostic budget exhausted")` with scalar `DiagnosticStatus` rather than returning partial values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve candidate identity in adjacency and generated paths with exact accounting</name>
  <reversibility rating="costly">D-03 and D-04 deliberately evolve candidate-bearing data shapes and budget cardinality; downstream consumers must see one stable record per candidate.</reversibility>
  <files>src/fast_fsm/_diagnostics.py, src/fast_fsm/validation.py, tests/test_diagnostic_contracts.py, tests/test_validation.py</files>
  <read_first>
    - `src/fast_fsm/_diagnostics.py` — inspect `_sparse_adjacency`, both `_dense_adjacency` representations, `_generate_paths`, graph traversals, and every reserve-before-work/result stage.
    - `src/fast_fsm/validation.py` — inspect `get_sparse_adjacency`, `get_transition_matrix`, `get_adjacency_matrix`, `validate_completeness`, `generate_test_paths`, `_analyze_structure`, and output annotations/docstrings.
    - `tests/test_diagnostic_contracts.py` — extend exact/generous/one-less work/result/dense/path fixtures and one-capture mutation tests; keep this as the sole budget contract suite.
    - `tests/test_validation.py` — update transition-matrix, metrics, completeness, generated-path, and report compatibility assertions.
    - `24-RESEARCH.md` Patterns 3–4 and Pitfalls 2/5 — preserve same-target candidate indices and reserve each candidate occurrence.
  </read_first>
  <behavior>
    - Sparse edges and dense adjacency transition records include `priority` and retain identical-target candidates as separate ordered indices.
    - Dense transition-matrix cells contain ordered records with `to_state` and `priority`, not target-name-only strings; ordinary singleton rows still produce one record with default priority zero.
    - `validate_completeness()["total_transitions"]` and enhanced `actual_transitions` use candidate edge count while intentional event/state coverage sets remain set-based.
    - Generated paths contain four-scalar steps `(from_state, event, to_state, priority)` and preserve duplicate-target candidate paths in stored order.
    - Reachability, SCC, depth, adjacency, and paths inspect each candidate edge before structural deduplication; event catalogs and condensation arcs may remain deduplicated only after per-edge work is reserved.
    - Generous and exact budgets complete, while a one-less work/result/dense/path budget raises before the next candidate operation and exposes the existing incomplete scalar status.
  </behavior>
  <action>Write failing same-target candidate and budget-boundary cases first. Per D-03, add exact numeric `priority` to sparse edge rows and dense adjacency transition records. Evolve the `representation="transition"` matrix from target-name lists to ordered candidate dictionaries with exactly `to_state` and `priority`, then update validator annotations, docstrings, and tests for that deliberate data-shape change. Change generated path steps and public annotations/docstrings to the four-scalar candidate identity. Derive total transition counts and enhanced actual-transition metrics from `len(_diagnostic_graph.edges)` so compatibility target sets cannot collapse candidates. Per D-04, audit each traversal/output loop and reserve work, result, dense-cell, or path-expansion counts before each candidate occurrence; preserve per-edge reservation even when an event catalog, reachable state, SCC arc, or condensation arc is structurally deduplicated. Reuse the one existing ledger and fixed exception/status; do not create a second budget type, a new test module, or partial-return mode. Keep snapshot order and do not sort candidate edges.</action>
  <verify>
    <automated>task pure-source-check
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/test_diagnostic_contracts.py tests/test_validation.py -k "priority or candidate or adjacency or path or budget or transition_matrix" -x -q
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff format src/fast_fsm/_diagnostics.py src/fast_fsm/validation.py tests/test_diagnostic_contracts.py tests/test_validation.py
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check --fix src/fast_fsm/_diagnostics.py src/fast_fsm/validation.py tests/test_diagnostic_contracts.py tests/test_validation.py
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check src/fast_fsm/_diagnostics.py src/fast_fsm/validation.py tests/test_diagnostic_contracts.py tests/test_validation.py</automated>
  </verify>
  <acceptance_criteria>
    - Sparse adjacency, dense adjacency, transition matrices, counts, and paths retain every candidate and numeric priority (D-03, DIAG-02).
    - Same-target candidates remain separate ordered rows/indices/path steps rather than collapsing through compatibility sets.
    - Every candidate edge inspected or emitted consumes the correct existing budget dimension before work/result exposure (D-04).
    - Exact limits succeed and one-less limits fail with the fixed message and `complete=False`; no partial data is returned as complete.
    - No runtime dispatch/storage, new dependency, or Phase 25 benchmark/documentation behavior is added.
  </acceptance_criteria>
  <done>Adjacency, transition matrices, counts, and paths are candidate-complete, priority-explicit, and bounded per candidate occurrence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render every candidate safely across JSON, diagrams, and Markdown</name>
  <reversibility rating="costly">D-03 fixes the cross-format candidate identity contract and D-04 keeps each sink inside one shared fail-explicit budget.</reversibility>
  <files>src/fast_fsm/visualization.py, src/fast_fsm/validation.py, tests/test_visualization.py, tests/test_validation.py, tests/test_diagnostic_contracts.py</files>
  <read_first>
    - `src/fast_fsm/visualization.py` — inspect `_capture_diagnostic_graph`, `_transition_label`, Mermaid/PlantUML sinks, `_to_json_from_snapshot`, `_validate_adjacency_matrix`, and `_to_mermaid_document_from_snapshot`.
    - `src/fast_fsm/validation.py` — inspect `_export_json`, `_export_markdown`, issue output, transition tables, and adjacency cells after Task 1.
    - `tests/test_visualization.py` — update diagram docstring snapshots, escaping fixtures, JSON topology, adjacency mismatch, and Markdown table assertions.
    - `tests/test_validation.py` — extend validation JSON/Markdown candidate output, escaping, metric, and stable-order cases.
    - `tests/test_diagnostic_contracts.py` — extend top-level one-capture and aggregate-budget assertions rather than duplicating renderer fixtures.
    - `24-RESEARCH.md` Pattern 3 and Pitfalls 4–5 — use one numeric spelling while retaining each sink's existing encoder.
  </read_first>
  <behavior>
    - Mermaid and PlantUML emit one edge per candidate with a trailing generated `[priority N]`; `show_conditions=False` suppresses only the escaped condition label.
    - `to_json()["topology"]["transitions"]`, sparse adjacency, optional dense adjacency, and enhanced validation JSON each contain one ordered numeric-priority record per candidate.
    - Visualization and validation Markdown adjacency cells show every escaped event with its numeric priority, and both numbered transition tables contain a `Priority` column.
    - A supplied adjacency matrix is accepted only when state order, event order, every candidate record including priority, and every cell index match the one captured graph; mismatch uses the existing fixed non-leaking error.
    - Caller-controlled state, trigger, condition, and title strings remain inert under the existing format-specific encoders; generated integer priority is not passed through text sanitizers.
    - Every top-level renderer captures once, shares one ledger across nested analysis/output, reserves before every candidate line/record, and raises rather than exposing an apparently complete partial value.
  </behavior>
  <action>Build one hostile-label same-target candidate fixture and assert it across every named surface before implementation. Per D-03, extend `_transition_label()` with numeric priority and append generated `[priority N]` after the optional escaped condition. Pass priority from every Mermaid and PlantUML edge; update public docstring snapshots. Add numeric priority to JSON topology transitions and source `has_guard` from the scalar edge field rather than condition display text, relying on Task 1 shared adjacency records for sparse/dense output. Extend `_validate_adjacency_matrix()` expected candidate records with priority so a stale or tampered priority fails through the existing fixed mismatch. Render adjacency cells as escaped event text plus unambiguous generated numeric priority, and add a `Priority` column to both visualization and validation transition tables. Preserve opaque state IDs and the existing Mermaid, PlantUML, Markdown heading/cell, trigger, condition, and title encoders; do not introduce a generic sanitizer or pass numeric priority through caller-text escaping. Per D-01/D-04, retain one `_capture_diagnostic_graph()` result and one shared ledger for nested output, reserve every candidate record/line before append, and cover exact/one-less aggregate limits without printing or returning partial output.</action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/test_visualization.py tests/test_validation.py tests/test_diagnostic_contracts.py -k "priority or candidate or json or mermaid or plantuml or markdown or adjacency or escape or snapshot or budget" -x -q
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff format src/fast_fsm/visualization.py src/fast_fsm/validation.py tests/test_visualization.py tests/test_validation.py tests/test_diagnostic_contracts.py
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check --fix src/fast_fsm/visualization.py src/fast_fsm/validation.py tests/test_visualization.py tests/test_validation.py tests/test_diagnostic_contracts.py
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check src/fast_fsm/visualization.py src/fast_fsm/validation.py tests/test_visualization.py tests/test_validation.py tests/test_diagnostic_contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - JSON, Mermaid, PlantUML, validation Markdown, visualization Markdown, and adjacency render every candidate exactly once with numeric priority (D-03, DIAG-02).
    - Priority remains visible when conditions are hidden, and same-target candidates remain distinct in stable source/trigger/priority order.
    - Existing grammar-specific escaping and opaque state IDs keep all caller text inert; generated priority never becomes an encoding bypass.
    - Caller-supplied adjacency priority tampering fails with the fixed mismatch message and no caller text leakage.
    - One-capture and exact/one-less aggregate budget tests prove nested output remains snapshot-consistent and explicitly incomplete on exhaustion (D-01, D-04).
  </acceptance_criteria>
  <done>Every structured and human-readable diagnostic sink is candidate-complete, priority-explicit, safely escaped, and bounded by one shared ledger.</done>
</task>

</tasks>

<threat_model>
## ASVS Level

OWASP ASVS L1 applies. Caller-controlled labels and candidate-sized diagnostic workloads cross output and resource-consumption boundaries.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Scalar diagnostic graph → Mermaid/PlantUML/Markdown/JSON | Caller-controlled labels become syntax-bearing output while generated priority remains a validated scalar. |
| Candidate graph → bounded adjacency/path/report operations | A finite but large candidate group can amplify traversal and result cardinality. |
| Caller-provided adjacency → captured Markdown document | External positional records must match the exact captured graph before rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-05 | Tampering / Information Disclosure | diagram and Markdown renderers | high | mitigate | Preserve format-specific final-sink encoders, opaque state IDs, and hostile-label goldens across every priority-bearing surface. |
| T-24-06 | Denial of Service | adjacency, traversal, paths, and output rows | high | mitigate | Reserve existing work/result/dense/path dimensions per candidate before action and prove exact/one-less failure boundaries. |
| T-24-07 | Tampering | caller-supplied adjacency matrix | high | mitigate | Compare all ordered states/events/candidate fields including priority and cell indices, then return only the fixed mismatch error. |
| T-24-08 | Information Disclosure | budget and output failures | medium | mitigate | Retain scalar-only status plus fixed exception/error text and reject partial output exposure under exhaustion. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every candidate represented exactly once in deterministic priority order under existing safety budgets | 24-01..03 | COVERED | Plan 02 owns all named output and budget surfaces. |
| REQ | DIAG-01 | Strict ordered determinism and conservative shadows | 24-01, 24-03 | COVERED | Plan 01 supplies findings consumed by reports. |
| REQ | DIAG-02 | JSON, Mermaid, PlantUML, Markdown, adjacency, and paths retain multiplicity/priority | 24-02, 24-03 | COVERED | Both tasks plus final native proof. |
| CONTEXT | D-01 | One immutable diagnostic projection | 24-01..03 | COVERED | Task 2 preserves one capture through nested output. |
| CONTEXT | D-02 | Strict ordering and conservative shadow analysis | 24-01, 24-03 | COVERED | Reported by Plan 01; unchanged here. |
| CONTEXT | D-03 | One numeric-priority record per candidate in all outputs | 24-02, 24-03 | COVERED | Tasks 1–2. |
| CONTEXT | D-04 | Candidate-sized accounting and explicit incompleteness | 24-01..03 | COVERED | Tasks 1–2 plus final full gate. |
| RESEARCH | R-01 | Extend shared scalar edge, not renderer-specific flatteners | 24-01 | COVERED | Consumed from Plan 01. |
| RESEARCH | R-02 | Candidate-complete adjacency/path shapes | 24-02 | COVERED | Task 1. |
| RESEARCH | R-03 | Existing format-specific encoders and one numeric spelling | 24-02 | COVERED | Task 2. |
| RESEARCH | R-04 | Reserve once per candidate occurrence | 24-02 | COVERED | Tasks 1–2. |
| RESEARCH | R-05 | Fresh pure/native compiled-core parity | 24-03 | COVERED | Dedicated final wave. |

<verification>
- Focused pure tests cover adjacency, transition matrices, paths, JSON, Mermaid, PlantUML, both Markdown families, escaping, one-capture behavior, and every budget dimension.
- Ruff formatting/fix/validation passes on diagnostics, validation, visualization, and their tests.
- Same-target candidates appear as separate ordered priority-bearing records across every named surface.
- No Phase 25 benchmarks, installed artifacts, README/Sphinx guidance, or drone example is implemented.
</verification>

<success_criteria>
- DIAG-02 is implemented by one shared scalar graph and candidate-complete output adapters.
- D-01, D-03, and D-04 remain observable across structured and human-readable results.
- All high-severity output/resource threats have automated mitigation.
- Plan 03 can prove the complete snapshot-to-output contract in pure and compiled core without changing schemas again.
</success_criteria>

<output>
Create `.planning/phases/24-candidate-aware-diagnostics-output/24-02-SUMMARY.md` when done.
</output>
