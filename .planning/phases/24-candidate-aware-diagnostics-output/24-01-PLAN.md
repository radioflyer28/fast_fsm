---
phase: 24-candidate-aware-diagnostics-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - src/fast_fsm/_diagnostics.py
  - src/fast_fsm/validation.py
  - tests/test_graph_invariants.py
  - tests/test_validation.py
autonomous: true
requirements:
  - DIAG-01
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: validation derives every candidate result from one immutable snapshot and never reads private singleton/group storage or recaptures topology."
    - "D-02: exact strictly increasing candidate priorities are deterministic, while equal, descending, Boolean, and non-integer priorities are explicit validation errors."
    - "D-02: lower candidates are reported as provably shadowed only from a snapshot-captured exact-base-State unconditional fact; other unguarded state types yield possible-shadow observations without executing user code."
    - "Every validation record retains source, trigger, target, candidate priority, and shadowing priority in canonical source/trigger/priority order."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Frozen candidate snapshot rows with priority and narrow static-unconditional evidence"
      contains: "statically_unconditional"
    - path: "src/fast_fsm/_diagnostics.py"
      provides: "Scalar candidate edges preserving priority, guard presence, and static evidence from one snapshot"
      contains: "class _DiagnosticEdge"
    - path: "src/fast_fsm/validation.py"
      provides: "Strict-priority determinism and conservative candidate-shadow analysis"
      contains: "provably_shadowed_candidates"
    - path: "tests/test_graph_invariants.py"
      provides: "Immutable candidate snapshot ordering and static-proof coverage"
    - path: "tests/test_validation.py"
      provides: "Ordered, malformed, proved-shadow, possible-shadow, and no-user-code validation coverage"
  key_links:
    - from: "StateMachine._graph_snapshot_owned()"
      to: "_graph_from_snapshot()"
      via: "one frozen row and one scalar edge per candidate"
      pattern: "statically_unconditional"
    - from: "_DiagnosticGraph.edges"
      to: "FSMValidator.check_determinism()"
      via: "one contiguous source/trigger group scan in stored priority order"
      pattern: "priority_errors"
    - from: "FSMValidator.check_determinism()"
      to: "EnhancedFSMValidator._analyze_determinism()"
      via: "additive malformed/proved/possible candidate records mapped to error/warning/info issues"
      pattern: "possibly_shadowed_candidates"
---

<objective>
Make candidate-aware validation truthful from snapshot capture through strict priority and conservative shadow classification (D-01, D-02, DIAG-01).

Purpose: Replace pre-priority target-cardinality assumptions with one immutable, side-effect-free candidate analysis seam that accepts valid ordered groups and distinguishes structural errors from conservative observations.
Output: Frozen static candidate facts, scalar priority-bearing diagnostic edges, deterministic validation records, and enhanced validator issues with evidence-appropriate severity.
</objective>

## Artifacts this phase produces

- A trailing frozen `statically_unconditional` snapshot fact computed only from an absent transition guard and exact base `State` source type.
- A priority-bearing `_DiagnosticEdge` that preserves guard presence and one candidate per Phase 23 snapshot row without sorting or inspecting runtime storage.
- Additive `priority_errors`, `provably_shadowed_candidates`, and `possibly_shadowed_candidates` records on the existing determinism result.
- Error, warning, and informational enhanced-validation issues that preserve candidate identity and never execute guards or permissions.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-candidate-aware-diagnostics-output/24-CONTEXT.md
@.planning/phases/24-candidate-aware-diagnostics-output/24-RESEARCH.md
@.planning/phases/24-candidate-aware-diagnostics-output/24-PATTERNS.md
@.planning/phases/24-candidate-aware-diagnostics-output/24-VALIDATION.md
@.planning/phases/23-construction-declarative-serialization-parity/23-03-SUMMARY.md
@.planning/phases/23-construction-declarative-serialization-parity/23-VERIFICATION.md
@.planning/phases/22-ordered-runtime-selection-lifecycle-integration/22-VERIFICATION.md
@.specify/decisions/ADR-006-bounded-diagnostics-and-safe-output.md
@.specify/decisions/ADR-007-priority-topology.md

<interfaces>
- `_GraphSnapshot.transitions` is already candidate-complete and ordered by source, trigger, then stored ascending priority.
- `_GraphTransition` is a frozen/slotted private core record carrying identity, scalar names, numeric priority, and optional `condition_ref`; Phase 24 may append diagnostic-only scalar evidence but may not add traversal to core.
- `_graph_from_snapshot()` is the sole snapshot-to-scalar graph projection and must preserve row order without sorting.
- `FSMValidator.transitions` is a compatibility target-set adapter; candidate counts, determinism, and shadow analysis must consume `_diagnostic_graph.edges` instead.
- `_DiagnosticBudget` is the only diagnostics ledger and uses reserve-before-work/result semantics with a fixed redacted exhaustion exception.
- Phase 22 eligibility order remains transition guard, declarative guard, then source-state permission; Phase 24 must not call any of those stages.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one ordered candidate group through the immutable validation projection</name>
  <reversibility rating="costly">D-01 and D-02 establish the shared diagnostic projection and conservative proof boundary; changing either later would invalidate every downstream report.</reversibility>
  <files>src/fast_fsm/core.py, src/fast_fsm/_diagnostics.py, src/fast_fsm/validation.py, tests/test_graph_invariants.py, tests/test_validation.py</files>
  <read_first>
    - `src/fast_fsm/core.py` — inspect `_GraphTransition`, `_GraphSnapshot`, `_graph_snapshot_owned`, `_transition_entries`, exact base `State.can_transition`, and both Phase 22 candidate selectors.
    - `src/fast_fsm/_diagnostics.py` — inspect `_DiagnosticEdge`, `_DiagnosticGraph`, `_DiagnosticBudget`, and `_graph_from_snapshot`.
    - `src/fast_fsm/validation.py` — inspect `_initialize_from_snapshot`, `_extract_fsm_structure`, `_operation_budget`, and `check_determinism`.
    - `tests/test_graph_invariants.py` — extend the candidate-complete snapshot ordering and frozen/slotted identity assertions.
    - `tests/test_validation.py` — follow existing determinism and enhanced-validator result conventions.
    - `24-RESEARCH.md` Patterns 1–2 and Pitfalls 1–3 — use the prescribed exact-base-state static proof and contiguous group scan.
  </read_first>
  <behavior>
    - A two-candidate same-source/same-trigger group reaches `FSMValidator.check_determinism()` as two scalar edges in ascending stored priority order from exactly one snapshot capture.
    - `_GraphTransition` and `_DiagnosticEdge` retain exact numeric priority; the diagnostic edge retains scalar guard presence, and the core row additionally retains a frozen Boolean static-unconditional fact.
    - The static fact is true only when `entry.condition is None` and the canonical source has exact type `State`; it is false for guarded entries, `State` subclasses, and declarative source types.
    - A valid strictly increasing group is deterministic even when its targets differ or are identical.
    - Snapshot and diagnostic projection do not call a transition guard, declarative guard, or state permission and do not read `fsm._transitions` outside core snapshot capture.
  </behavior>
  <action>Write the end-to-end failing assertions first. Per D-01, append a defaulted Boolean `statically_unconditional` field to the frozen/slotted `_GraphTransition` record and compute it inside `_graph_snapshot_owned()` from the already captured canonical source and entry using only `entry.condition is None and type(source_state) is State`. Do not inspect condition names, invoke a condition, call `can_transition`, or infer anything from subclass shape. Append exact `priority`, scalar `has_guard`, and the static Boolean to frozen/slotted `_DiagnosticEdge`; derive guard presence from `transition.condition is not None` and copy all scalar fields in `_graph_from_snapshot()` in existing snapshot order without sorting or consulting `_TransitionSlot`. Replace the target-cardinality definition in `check_determinism()` with an initial contiguous `(from_index, trigger)` group scan that accepts a valid strictly increasing group and keeps the compatibility keys `is_deterministic` and `non_deterministic_transitions`. Reserve one work unit before each candidate inspection through the existing ledger. Keep singleton behavior compatible and leave selection, lifecycle, callbacks, public topology APIs, and Phase 25 work unchanged.</action>
  <verify>
    <automated>task pure-source-check
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/test_graph_invariants.py tests/test_validation.py -k "snapshot or candidate or priority or determinism" -x -q
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff format src/fast_fsm/core.py src/fast_fsm/_diagnostics.py src/fast_fsm/validation.py tests/test_graph_invariants.py tests/test_validation.py
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check --fix src/fast_fsm/core.py src/fast_fsm/_diagnostics.py src/fast_fsm/validation.py tests/test_graph_invariants.py tests/test_validation.py
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check src/fast_fsm/core.py src/fast_fsm/_diagnostics.py src/fast_fsm/validation.py tests/test_graph_invariants.py tests/test_validation.py</automated>
  </verify>
  <acceptance_criteria>
    - One immutable capture feeds one candidate-complete scalar graph and one valid-group determinism result (D-01, DIAG-01).
    - Snapshot and diagnostic records remain frozen/slotted and preserve candidate source, trigger, target, condition label, and numeric priority.
    - Static unconditional evidence uses only the exact base-State/no-transition-guard rule and never evaluates caller code (D-02).
    - Different-target and same-target strictly increasing groups are deterministic; registration order is irrelevant because no downstream sort occurs.
    - Core runtime storage and Phase 22 sync/async selectors are unchanged beyond the cold snapshot record projection.
  </acceptance_criteria>
  <done>A real ordered group remains candidate-complete and deterministic from one immutable core snapshot through the scalar validator seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify malformed priorities and proved-versus-possible shadowing conservatively</name>
  <reversibility rating="costly">D-02 fixes the safety meaning of error, proved shadow, and possible shadow categories so validation never overstates eligibility facts.</reversibility>
  <files>src/fast_fsm/validation.py, tests/test_validation.py</files>
  <read_first>
    - `src/fast_fsm/validation.py` — inspect Task 1 group scan, `ValidationIssue`, `_analyze_determinism`, metrics initialization, report issue serialization, and budget helpers.
    - `tests/test_validation.py` — reuse fake/captured snapshot fixtures, issue severity assertions, deterministic ordering checks, and exact-budget patterns.
    - `24-RESEARCH.md` Pattern 2 and Pitfall 3 — preserve malformed topology as an error while treating unproved eligibility only as an observation.
  </read_first>
  <behavior>
    - Equal, descending, Boolean, and non-integer candidate priorities create deterministic `priority_errors` records and make the containing source/trigger group non-deterministic.
    - `priority_errors` identify `from_state`, `event`, `to_state`, `priority`, and a fixed scalar `reason`; `non_deterministic_transitions` contains each malformed source/event pair once.
    - A lower candidate after an exact-base-State statically unconditional candidate appears in `provably_shadowed_candidates` with its own identity and `shadowing_priority`.
    - A lower candidate after an unguarded subclass or declarative source appears only in `possibly_shadowed_candidates`; a guarded higher candidate alone proves neither category.
    - Enhanced validation maps malformed priority topology to errors, proved shadows to warnings, and possible shadows to information in canonical candidate order.
    - Exact and one-less budgets count each inspected candidate and each emitted error/shadow record, with exhaustion represented by the existing fixed exception and incomplete status.
  </behavior>
  <action>Construct malformed immutable snapshot inputs in tests because public registration correctly prevents invalid topology; keep those fixtures scalar and deterministic. Per D-02, finish the one-pass group analysis with additive list fields named `priority_errors`, `provably_shadowed_candidates`, and `possibly_shadowed_candidates`. Use records containing candidate source/event/target/priority plus `shadowing_priority` where applicable; use fixed non-caller-controlled reason categories for malformed values. Defensively require `type(priority) is int` and strict increase without coercion, normalization, or reordering. Once an earlier edge is statically unconditional, classify subsequent candidates as proved shadowed; when an earlier edge is merely unguarded on a non-exact source type, classify later candidates as possible unless stronger proof applies. Reserve work before candidate inspection and results before appending each diagnostic record. Update `_analyze_determinism()` to create error, warning, and info `ValidationIssue` entries with priority-specific recommendations, replacing the obsolete advice that valid multi-target groups need conditions. Assert no guard/permission calls, no raw repr leakage, stable ordering, same-target identity, and complete failure on a one-less budget. Do not modify runtime selection or introduce a public topology API.</action>
  <verify>
    <automated>UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/test_validation.py -k "priority or candidate or determinism or shadow or budget" -x -q
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff format src/fast_fsm/validation.py tests/test_validation.py
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check --fix src/fast_fsm/validation.py tests/test_validation.py
UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check src/fast_fsm/validation.py tests/test_validation.py</automated>
  </verify>
  <acceptance_criteria>
    - Malformed priorities are candidate-specific validation errors; valid strictly ordered candidate groups remain deterministic (D-02, DIAG-01).
    - Proved and possible shadow records are distinct, ordered, and carry complete numeric candidate identity.
    - No validation path calls transition conditions, declarative guards, or state permissions.
    - Enhanced issues use error/warning/info severity according to evidence strength and no longer recommend guards for a valid ordered group.
    - Candidate inspection and emitted findings consume the existing ledger and preserve explicit incomplete-result behavior.
  </acceptance_criteria>
  <done>Validation rejects only malformed priority topology and reports shadowing with evidence-appropriate certainty without executing user logic.</done>
</task>

</tasks>

<threat_model>
## ASVS Level

OWASP ASVS L1 applies. Snapshot capture and static classification are the relevant in-process trust boundaries.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Live canonical topology → immutable snapshot | Mutable caller-owned state and condition objects become frozen identity/scalar facts under the ownership read boundary. |
| Immutable snapshot → validator findings | Candidate facts become correctness errors or advisory observations without executing user policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-01 | Tampering | snapshot-backed validation | high | mitigate | Capture once, project only from the frozen snapshot, and test mutation-after-capture plus absence of private storage reads. |
| T-24-02 | Tampering / Denial of Service | shadow classification | high | mitigate | Capture only the exact-base-State/no-guard Boolean and prove diagnostics never invoke guards, declarative handlers, or permissions. |
| T-24-03 | Tampering | malformed priority analysis | high | mitigate | Defensively require exact integers in strict increasing order, emit fixed scalar reasons, and never normalize or reorder input. |
| T-24-04 | Information Disclosure | validation errors/issues | medium | mitigate | Emit canonical scalar candidate fields and fixed reason/recommendation text; tests reject callable, exception, and private identity representations. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every candidate represented exactly once in deterministic priority order under existing safety budgets | 24-01..03 | COVERED | Plan 01 establishes the immutable candidate analysis seam. |
| REQ | DIAG-01 | Strict groups deterministic; malformed ties rejected; proved shadows conservative | 24-01, 24-03 | COVERED | Both tasks implement semantics; Plan 03 proves native parity. |
| REQ | DIAG-02 | Candidate-complete outputs, adjacency, and paths | 24-02, 24-03 | COVERED | Deliberately deferred to dependent Plans 02/03. |
| CONTEXT | D-01 | One immutable diagnostic projection | 24-01..03 | COVERED | Task 1 creates the seam; all later plans consume it. |
| CONTEXT | D-02 | Strict ordering and conservative proved-versus-possible shadows | 24-01, 24-03 | COVERED | Tasks 1–2 plus native proof. |
| CONTEXT | D-03 | Candidate-complete numeric output | 24-02, 24-03 | COVERED | Dependent output plan and integration proof. |
| CONTEXT | D-04 | Candidate-sized budgets and explicit incompleteness | 24-01..03 | COVERED | Task 2 begins candidate findings accounting; Plan 02 covers every output/path. |
| RESEARCH | R-01 | Extend `_GraphTransition` and `_DiagnosticEdge` once | 24-01 | COVERED | Task 1. |
| RESEARCH | R-02 | Group by source/trigger identity and order, not target cardinality | 24-01 | COVERED | Tasks 1–2. |
| RESEARCH | R-03 | Reuse existing ledger/encoders and keep runtime selectors untouched | 24-01..03 | COVERED | Explicit task exclusions and dependent proof. |
| RESEARCH | R-04 | Fresh pure/native compiled-core parity | 24-03 | COVERED | Dedicated final wave. |

<verification>
- `task pure-source-check` passes before any pure import.
- Focused snapshot and validation tests accept ordered groups, reject malformed groups, classify proved/possible shadows, and prove no user-code evaluation.
- Ruff formatting/fix/validation passes on every changed Python file.
- Plan 01 introduces no renderer, benchmark, installed-artifact, public documentation, or drone-example change.
</verification>

<success_criteria>
- DIAG-01 is executable through candidate-complete pure-source tests.
- D-01 and D-02 are visibly implemented in one frozen projection and one conservative validator.
- Every high-severity threat has a concrete automated mitigation.
- Plan 02 can consume one stable priority-bearing scalar graph without knowing runtime singleton/group storage.
</success_criteria>

<output>
Create `.planning/phases/24-candidate-aware-diagnostics-output/24-01-SUMMARY.md` when done.
</output>
