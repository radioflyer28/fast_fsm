---
phase: 17-atomic-transition-lifecycle
plan: 04
type: execute
wave: 3
depends_on: [17-03]
files_modified:
  - src/fast_fsm/core.py
  - .specify/memory/spr-core-api.md
  - tests/test_transition_lifecycle.py
  - tests/test_async.py
  - tests/test_builder.py
autonomous: true
requirements: [LIFE-01, LIFE-03, LIFE-04, LIFE-05, LIFE-06]
estimate: {tokens: 38000, raw_tokens: 38000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Per D-03, async exit/enter work is awaited at the same lifecycle slot as its sync counterpart, never as a detached tail."
    - "Per D-13/D-14, cancellation finalizes observers once at the reached stage, preserves the reached commit boundary, stops later callbacks, and re-raises the identical CancelledError without shielding or rollback."
    - "The same scenario table proves sync/async parity for order, result, state, history, guard context, failure, and declarative behavior."
    - "Synchronous callbacks on an async machine remain inline; no worker-thread offload or Phase 18 ownership mechanism is introduced."
  artifacts:
    - {path: "src/fast_fsm/core.py", provides: "Same-slot async lifecycle runner and cancellation boundary"}
    - {path: "tests/test_transition_lifecycle.py", provides: "Shared sync/async parity and event-synchronized cancellation matrix"}
    - {path: "tests/test_async.py", provides: "Legacy async callback/declarative/history compatibility regressions"}
    - {path: "tests/test_builder.py", provides: "Async builder/declarative exactly-once regressions"}
  key_links:
    - {from: "AsyncStateMachine.trigger_async", to: "async exit/enter callbacks", via: "await at source/destination registration slots", pattern: "await"}
    - {from: "CancelledError boundary", to: "failure observers", via: "finalize once then bare re-raise", pattern: "CancelledError"}
  prohibitions:
    - "[UNVERIFIED - no SPEC] After the first lifecycle callback failure or cancellation, do not continue later lifecycle callbacks and do not roll back state/history already committed."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow asyncio.CancelledError; observation is followed by re-raising the original cancellation."
    - "[UNVERIFIED - no SPEC] Do not disclose callback payloads or cause text through TransitionResult repr, TransitionError text, logs, or injected observer kwargs."
---

<objective>
Complete async same-slot lifecycle execution and cancellation-safe parity with the synchronous transaction.

Purpose: Deliver LIFE-06 while preserving Python cancellation semantics and coherent state/history.
Output: Explicit async runner, deterministic cancellation tests, and updated async compatibility coverage.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-atomic-transition-lifecycle/17-CONTEXT.md
@.planning/phases/17-atomic-transition-lifecycle/17-RESEARCH.md
@.planning/phases/17-atomic-transition-lifecycle/17-PATTERNS.md
@.planning/phases/17-atomic-transition-lifecycle/17-VALIDATION.md
@.planning/phases/17-atomic-transition-lifecycle/17-03-SUMMARY.md
@src/fast_fsm/core.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run the async lifecycle at the matching semantic slots</name>
  <files>src/fast_fsm/core.py, tests/test_transition_lifecycle.py, tests/test_async.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - Async success has the identical D-02 order, inserting awaited async source callbacks immediately after sync source callbacks and awaited async destination callbacks immediately after sync destination callbacks.
    - Every ordinary exception produces the same stage/commit/state/history/cause/suffix/observer outcome as the matching sync injection.
    - Guard context positional identity and sanitized kwargs remain equivalent; declarative handlers execute exactly once and control the post-commit result.
  </behavior>
  <action>Implement the explicit asynchronous half of the shared stage model per D-01/D-02/D-03. Do not call the complete sync executor and append async work. Reuse result, commit, and finalizer helpers while spelling every stage in the same order; run sync callbacks inline and await async callbacks only at their registered slot. Convert ordinary callback exceptions into failed values per D-04, but keep control-flow cancellation separate. Rewrite old async-tail and callback-success tests into exact order/failure assertions, preserving builder auto-async selection, guard context, clone registries, and direct `handle_event_async()` compatibility. In this same task and commit, replace the obsolete async-tail statement in `.specify/memory/spr-core-api.md` with the implemented `trigger_async` same-slot order, inline-sync-callback, parity, and declarative exactly-once invariants.</action>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_transition_lifecycle.py tests/test_async.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_transition_lifecycle.py tests/test_async.py</automated>
    <automated>uv run pytest tests/test_transition_lifecycle.py tests/test_async.py -x -q -k "order or parity or callback or declarative or history or kwargs"</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python -c "from pathlib import Path; text = Path('.specify/memory/spr-core-api.md').read_text(); assert all(token in text for token in ('trigger_async', 'same-slot', 'inline', 'declarative'))"</automated>
  </verify>
  <done>Async dispatch uses the same stage transaction and outcome semantics as sync dispatch with same-slot awaiting, and spr-core-api.md records that implemented parity contract in the same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve cancellation identity at every awaited boundary</name>
  <files>src/fast_fsm/core.py, tests/test_transition_lifecycle.py, tests/test_async.py, tests/test_builder.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - Event-synchronized cancellation at async guard and source-exit callback leaves source/no history; cancellation at destination-enter callback and declarative handler leaves destination/one history record.
    - Each cancellation invokes all failure observers once at the reached stage, stops later lifecycle work, and re-raises the exact original CancelledError.
    - Observer-raised cancellation during an ordinary failure remains isolated and cannot replace or recursively finalize the original transition cause.
  </behavior>
  <action>Per D-13, catch `asyncio.CancelledError` only at the public async lifecycle boundary, create the observer-facing staged failure using the explicit local committed state, finalize once, then use a bare re-raise. Per D-14, do not shield, compensate, roll back, or resume the suffix. Build tests with `asyncio.Event` handshakes at guard, source-exit async callback, destination-enter async callback, and async declarative handler; prohibit timing sleeps. Assert cancellation object identity, callback suffix absence, exact observer order/count, and commit/history coherence. Update core async docstrings to state same-slot order, inline sync callbacks, and native cancellation propagation. In this same task and commit, complete `.specify/memory/spr-core-api.md` with native `CancelledError` propagation, exactly-once failure observation, reached-stage commit/history semantics, suffix suppression, and the no-shield/no-rollback rule.</action>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_builder.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_builder.py</automated>
    <automated>uv run pytest tests/test_transition_lifecycle.py tests/test_async.py tests/test_builder.py -x -q -k "cancel or lifecycle or declarative"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_transition_lifecycle.py --include tests/test_async.py --include tests/test_builder.py -- uv run pytest tests/test_transition_lifecycle.py tests/test_async.py tests/test_builder.py -x -q -k "cancel or parity or lifecycle"</automated>
    <automated>uv run python -c "from pathlib import Path; text = Path('.specify/memory/spr-core-api.md').read_text(); assert all(token in text for token in ('CancelledError', 'failure observer', 'history', 'shield', 'rollback'))"</automated>
  </verify>
  <done>Cancellation is observed once, never converted or swallowed, and leaves state/history at the exact reached commit boundary, with spr-core-api.md updated to that final runtime contract in the same commit.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02 | Tampering | async state/history | high | mitigate | Shared non-awaiting commit plus pre/post cancellation matrix. |
| T-17-04 | Information Disclosure | cancellation/callback output | high | mitigate | Stage/type metadata only; secret-bearing parity tests retained. |
| T-17-05 | Denial of Service/Tampering | cancellation | high | mitigate | Event synchronization, one finalization, suffix absence, bare re-raise. |
| T-17-06 | Tampering | sync/async drift | high | mitigate | One stage catalog and one parameterized parity table. |
| T-17-09 | Tampering | reentrancy/concurrency | high | transfer | Phase 18; no locks, ownership flags, or cross-loop policy here. |
| T-17-10 | Spoofing/Tampering | installed artifacts | high | transfer | Phase 20; fresh source/native origin is not installed-wheel proof. |
</threat_model>

<verification>Run async/parity/cancellation selections in checkout and fresh native origin, plus Ruff and blocking mypy.</verification>
<success_criteria>LIFE-01 and LIFE-03 through LIFE-06 hold for async execution, including identical cancellation propagation.</success_criteria>
<output>Create `.planning/phases/17-atomic-transition-lifecycle/17-04-SUMMARY.md` when done.</output>
