---
phase: 17-atomic-transition-lifecycle
plan: 02
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - src/fast_fsm/core.py
  - .specify/memory/spr-core-api.md
  - tests/test_transition_lifecycle.py
  - tests/test_boundary_negative.py
  - tests/test_mypyc_guard.py
autonomous: true
requirements: [LIFE-02, LIFE-04]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Per D-05 through D-11, every resolution, guard, and state-permission failure produces one truthful result and one ordered failure-observer pass without changing the value-returning trigger API."
    - "`raise_if_failed()` remains opt-in, returns self on success, and raises `TransitionError` directly chained from the stored original cause on failure."
    - "Zero, one, and multiple failure observers preserve the same original result/cause; observer failures neither recurse nor stop later observers."
  artifacts:
    - {path: "src/fast_fsm/core.py", provides: "Shared result builder and finalizer for all pre-lifecycle failure families"}
    - {path: "tests/test_transition_lifecycle.py", provides: "Resolution/guard/permission and observer cardinality matrix"}
    - {path: "tests/test_boundary_negative.py", provides: "Public result/error compatibility and disclosure regressions"}
    - {path: "tests/test_mypyc_guard.py", provides: "Slotted result and sole-compilation-unit guards"}
  key_links:
    - {from: "StateMachine.trigger / AsyncStateMachine.trigger_async", to: "failure finalizer", via: "one public failure boundary", pattern: "_finalize_failure"}
    - {from: "TransitionResult.raise_if_failed", to: "TransitionError", via: "raise ... from cause", pattern: "raise.*from"}
  prohibitions:
    - "[UNVERIFIED - no SPEC] After the first lifecycle callback failure or cancellation, do not continue later lifecycle callbacks and do not roll back state/history already committed."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow asyncio.CancelledError; observation is followed by re-raising the original cancellation."
    - "[UNVERIFIED - no SPEC] Do not disclose callback payloads or cause text through TransitionResult repr, TransitionError text, logs, or injected observer kwargs."
---

<objective>
Complete the additive truthful result contract and route all pre-lifecycle failures through one finalization seam.

Purpose: Make LIFE-02 and LIFE-04 uniform before sync and async callback runners expand.
Output: Stable staged failures, original-cause chaining, and exact-once observer semantics.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-atomic-transition-lifecycle/17-CONTEXT.md
@.planning/phases/17-atomic-transition-lifecycle/17-RESEARCH.md
@.planning/phases/17-atomic-transition-lifecycle/17-PATTERNS.md
@.planning/phases/17-atomic-transition-lifecycle/17-VALIDATION.md
@.planning/phases/17-atomic-transition-lifecycle/17-01-SUMMARY.md
@.specify/decisions/ADR-002-trigger-result-not-exception.md
@src/fast_fsm/core.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize resolution, guard, and state-permission failures once</name>
  <files>src/fast_fsm/core.py, tests/test_transition_lifecycle.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - Missing transition is stage `resolution`; false/raising guards are stage `guard`; false/raising state permission is stage `state-permission`; every case is pre-commit with source current and no history.
    - Raised exceptions retain identity in `cause`; false outcomes are cause-less; all cases invoke each observer once in registration order with original kwargs.
    - No observer produces the same result; one observer runs once; multiple observers keep order even when early observers raise RuntimeError, CancelledError, KeyboardInterrupt, or SystemExit.
  </behavior>
  <action>Per D-05/D-06/D-07, use the tracer's result builder to classify every canonical preparation, guard, and state-permission failure without changing ordinary return behavior. Per D-09, ensure lower helpers only construct outcomes and exactly one public boundary finalizes them. Per D-10/D-11, keep observer isolation local, non-recursive, ordered, signature-compatible, and free of reserved payload keys. Extend the lifecycle matrix with both sync and async preparation/permission cases while preserving Phase 16 sanitized guard context and declarative exactly-once selection. In this same task and commit, incrementally update `.specify/memory/spr-core-api.md` to state that `resolution`, `guard`, and `state-permission` failures are pre-commit and pass exactly once through the unchanged `on_failed` observer contract; preserve the tracer and Phase 16 invariants already recorded. Do not add locks, ownership fields, or alternative dispatch routes.</action>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run pytest tests/test_transition_lifecycle.py tests/test_safety_kwargs.py -x -q -k "resolution or guard or permission or observer"</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python -c "from pathlib import Path; text = Path('.specify/memory/spr-core-api.md').read_text(); assert all(token in text for token in ('resolution', 'guard', 'state-permission', 'on_failed', 'pre-commit'))"</automated>
  </verify>
  <done>Every failure before lifecycle callbacks has the correct stage/commit/cause and exactly one non-recursive observer pass, with those implemented failure families reflected in spr-core-api.md in the same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze result compatibility, chaining, slots, and redaction</name>
  <files>src/fast_fsm/core.py, tests/test_boundary_negative.py, tests/test_mypyc_guard.py, tests/test_transition_lifecycle.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - Existing one-to-five positional TransitionResult construction remains valid with appended defaults; success reports committed only when produced by a completed trigger.
    - `raise_if_failed()` returns the same success object and raises a `TransitionError` whose `.result` is identical and whose `__cause__` is the original cause.
    - A secret-bearing exception is absent from result repr/equality display, error text, caplog, and observer kwargs while remaining directly accessible as `result.cause`.
  </behavior>
  <action>Per D-05 and compatibility policy, retain the exact first-five field order and append defaults only. Per D-08, implement concise stage-aware `TransitionError` text and explicit exception chaining without formatting the cause. Extend boundary tests for positional/default/equality behavior and direct cause identity; extend mypyc guards for slots, public signatures/exports, and the unchanged `TransitionError` exception boundary. In this same task and commit, refine `.specify/memory/spr-core-api.md` with the final additive `TransitionResult` field/default contract and stage-aware `raise_if_failed()` cause-chaining/redaction invariant. Keep `core.py` the sole compiled unit and run the focused matrix in asserted pure and freshly compiled task contexts.</action>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_boundary_negative.py tests/test_mypyc_guard.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_boundary_negative.py tests/test_mypyc_guard.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run pytest tests/test_boundary_negative.py tests/test_mypyc_guard.py tests/test_transition_lifecycle.py -x -q -k "TransitionResult or raise_if_failed or redaction or observer"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_boundary_negative.py --include tests/test_mypyc_guard.py --include tests/test_transition_lifecycle.py -- uv run pytest tests/test_boundary_negative.py tests/test_mypyc_guard.py tests/test_transition_lifecycle.py -x -q -k "TransitionResult or lifecycle or mypyc"</automated>
    <automated>uv run python -c "from pathlib import Path; text = Path('.specify/memory/spr-core-api.md').read_text(); assert all(token in text for token in ('TransitionResult', 'committed', 'stage', 'cause', 'raise_if_failed'))"</automated>
  </verify>
  <done>The additive result surface is source/native compatible, stage-aware, cause-chaining, and non-disclosing, and spr-core-api.md records that exact public contract in the same commit.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering/Repudiation | pre-lifecycle outcomes | high | mitigate | Table-test each failure stage, source state, and no-history outcome. |
| T-17-03 | Denial of Service/Tampering | observers | high | mitigate | One non-recursive finalizer; zero/one/many and raising-observer tests. |
| T-17-04 | Information Disclosure | cause/output | high | mitigate | Hidden cause, safe error/log metadata, and secret-sentinel assertions. |
| T-17-09 | Tampering | reentrancy/concurrency | high | transfer | Phase 18; no ownership mechanism in this plan. |
| T-17-10 | Spoofing/Tampering | installed artifacts | high | transfer | Phase 20; source/native tree checks are not wheel parity. |
</threat_model>

<verification>Focused result/observer tests pass in checkout and fresh compiled origin with Ruff and blocking mypy.</verification>
<success_criteria>LIFE-02 and the result/finalizer portion of LIFE-04 are executable and backward compatible.</success_criteria>
<output>Create `.planning/phases/17-atomic-transition-lifecycle/17-02-SUMMARY.md` when done.</output>
