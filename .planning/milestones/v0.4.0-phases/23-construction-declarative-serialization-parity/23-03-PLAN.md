---
phase: 23-construction-declarative-serialization-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified:
  - .specify/memory/spr-core-api.md
  - src/fast_fsm/core.py
  - tests/test_builder.py
  - tests/test_mypyc_guard.py
autonomous: true
requirements:
  - PAR-01
  - PAR-02
  - PAR-03
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: `quick_build`, `quick_fsm`, builders, factories, helpers, declarative construction, and deserialization produce the same ordered candidate fingerprint as direct registration through one atomic registrar."
    - "D-02: malformed/tied/async-incompatible late input leaves no published machine, cache, frozen staging, partial slot, or graph-version change and remains repairable."
    - "D-05/D-06/D-07: scalar round-trip, candidate-complete projection, query, clone, and declarative identity compile and behave identically in pure and native core."
    - "D-08: all public callback and condition signatures retain `*args, **kwargs`; priority/reference data never enters caller payloads."
    - "The sole `core.py` mypyc boundary remains explicit/slotted, selectors keep the Phase 22 result union and direct singleton path, and no Phase 24/25 work enters scope."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Priority-bearing quick/factory row parity across the canonical registrar"
    - path: "tests/test_builder.py"
      provides: "Cross-constructor fingerprint, atomicity, retryability, and helper compatibility proof"
    - path: "tests/test_mypyc_guard.py"
      provides: "Exact promoted record/sequence layout, selector preservation, slots, and native parity proof"
    - path: ".specify/memory/spr-core-api.md"
      provides: "Living construction, declarative, serialization, ambiguity, callback, and complexity contract"
  key_links:
    - from: "quick_build()/quick_fsm() 3/4/5-field rows"
      to: "StateMachine.add_transitions()"
      via: "one complete normalized batch"
      pattern: "add_transitions"
    - from: "all construction surfaces"
      to: "canonical topology fingerprint"
      via: "same immutable candidate ordering, guard/reference identity, and graph version"
      pattern: "priority"
    - from: "tests/test_mypyc_guard.py"
      to: "src/fast_fsm/core.py"
      via: "AST, mypy, slots policy, compiled import, and focused native behavior"
      pattern: "_TransitionGroup"
---

<objective>
Finish constructor parity by routing quick/factory rows through the canonical registrar, then lock the complete promoted topology, declarative, serialization, clone, query, and callback contracts across pure and compiled core (D-01, D-02, D-05 through D-08).

Purpose: Prove every supported entry point creates one equivalent finite candidate topology without weakening mypyc layout or Phase 22 dispatch.
Output: Quick/factory 3/4/5-row support, cross-constructor fingerprints, atomic retryability, living SPR updates, and pure/native/type/slots evidence.
</objective>

## Artifacts this phase produces

- Priority-bearing quick/factory row parsing that delegates one batch to the existing registrar.
- Cross-constructor canonical fingerprint tests covering direct, batch, builder, quick factory, declarative, and dictionary construction.
- Exact AST/slots/type/native guards for candidate references, plural declarations, snapshot rows, and unchanged selectors.
- Updated SPR language for construction parity, `condition_ref`, ambiguity, callback payloads, and the hot/cold representation split.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-construction-declarative-serialization-parity/23-CONTEXT.md
@.planning/phases/23-construction-declarative-serialization-parity/23-RESEARCH.md
@.planning/phases/23-construction-declarative-serialization-parity/23-PATTERNS.md
@.planning/phases/23-construction-declarative-serialization-parity/23-VALIDATION.md
@.planning/phases/23-construction-declarative-serialization-parity/23-01-SUMMARY.md
@.planning/phases/23-construction-declarative-serialization-parity/23-02-SUMMARY.md
@.planning/phases/22-ordered-runtime-selection-lifecycle-integration/22-03-SUMMARY.md
@.specify/decisions/ADR-007-priority-topology.md
@.specify/memory/spr-core-api.md

<interfaces>
- `StateMachine.add_transitions()` already accepts 3/4/5-field rows and owns exact priority validation plus atomic merge/publication.
- `FSMBuilder` already stages five-field rows and publishes one local candidate through `add_transitions()` before caching.
- `StateMachine.quick_build()` and `quick_fsm()` currently expose only three-field row annotations and loop `add_transition()`.
- Plan 23-01 creates the cold immutable candidate sequence and `condition_ref` record path; Plan 23-02 creates plural declarative records and exact selected handler resolution.
- `core.py` remains the sole compiled module; Phase 22 selector signatures and `Union[_PreparedDispatch, TransitionResult]` are fixed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make quick and factory construction canonically atomic and equivalent</name>
  <files>src/fast_fsm/core.py, tests/test_builder.py</files>
  <read_first>
    - `src/fast_fsm/core.py` — inspect `quick_build`, `quick_fsm`, `add_transitions`, `_normalize_transition_request`, `_commit_transition_plan`, `FSMBuilder.build`, `from_states`, and `simple_fsm` after Plans 01/02.
    - `tests/test_builder.py` — extend quick-build identity, batch atomicity, builder retryability, ownership independence, and declarative parity fixtures.
    - `tests/test_graph_invariants.py` — reuse its candidate-complete topology fingerprint semantics without duplicating private merge logic in tests.
    - `23-RESEARCH.md` §Pattern 4 — follow canonical row parsing and one registrar batch.
  </read_first>
  <behavior>
    - `quick_build()` and `quick_fsm()` accept the same 3/4/5-field rows as `add_transitions`; three-field behavior stays unchanged and omitted priority is `0`.
    - Direct, batch, builder, quick, factory, declarative, and from-dict construction produce identical ordered candidate fingerprints for out-of-order, same-target, and multi-source declarations.
    - Quick/factory parsing preserves supplied `State` object identity and list-source expansion while transporting condition and exact priority.
    - Every quick/factory topology is submitted in one registrar batch; a late malformed priority or tie returns no machine and publishes no prefix.
    - Builder failure remains retryable and does not cache/freeze staging; exact duplicate rows remain idempotent through canonical registration.
    - `from_states()` and `simple_fsm()` remain unchanged because they declare no transitions.
  </behavior>
  <action>Write cross-constructor and late-failure tests first. Per D-01, widen `quick_build` and `quick_fsm` annotations/parsing to the established 3/4/5 transition-row union: `(trigger, source, target)`, optional condition, then optional priority. Parse endpoint identities from every form, collect all state objects/names before machine allocation, preserve exact supplied `State` identity, and submit the complete original-semantic rows to one `add_transitions()` call. Do not loop public `add_transition`, sort candidates, or merge groups in the adapter. Per D-02, keep the machine local until all rows validate and the one registrar publication completes; test late tie, bool/non-int priority, invalid endpoint, and async incompatibility without partial topology or cached builder state. Compare candidate-complete fingerprints across direct, batch, `FSMBuilder`, `quick_build`, `quick_fsm`, declarative construction, and `from_dict`, including list-source and same-target groups. Preserve ordinary three-field calls and leave transition-free helpers unchanged.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/test_builder.py tests/test_graph_invariants.py tests/test_advanced_functionality.py -k "quick or factory or builder or priority or candidate or atomic or retry" -x -q
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff format src/fast_fsm/core.py tests/test_builder.py
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check --fix src/fast_fsm/core.py tests/test_builder.py
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check src/fast_fsm/core.py tests/test_builder.py</automated>
  </verify>
  <acceptance_criteria>
    - All supported constructors yield the same complete ordered candidate identity and winner semantics as direct registration (D-01, PAR-01).
    - Quick/factory helpers accept 3/4/5 rows, preserve state identity and default priority, and delegate one batch to the registrar.
    - Late malformed/conflicting/async-incompatible input publishes no prefix or machine and keeps builder repairable (D-02).
    - No adapter introduces its own candidate sorting, merging, tie, duplicate, or version policy.
    - Transition-free helpers and ordinary three-field construction remain compatible.
  </acceptance_criteria>
  <done>Every supported construction adapter transports complete candidate rows through one atomic registrar and produces the canonical topology.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the promoted topology and callback contract in pure and compiled core</name>
  <files>.specify/memory/spr-core-api.md, src/fast_fsm/core.py, tests/test_mypyc_guard.py, tests/test_builder.py</files>
  <read_first>
    - `src/fast_fsm/core.py` — inspect final candidate, prepared, snapshot, declarative, adapter, clone, query, and selector declarations after Task 1.
    - `tests/test_mypyc_guard.py` — extend exact record fields, union narrowing, sole compiled module, selector no-copy/no-sort, native-origin, and package export assertions.
    - `tests/test_builder.py` — reuse the final cross-constructor fingerprint and callback-payload probes in pure/native runs.
    - `.specify/memory/spr-core-api.md` — update the living core contract only; README/Sphinx/tutorial/drone material remains Phase 25.
  </read_first>
  <behavior>
    - Candidate, prepared, graph, and declarative records have exact typed trailing identity fields and remain slotted/frozen where required by their role.
    - All cold parity surfaces use one immutable candidate sequence; selectors retain direct singleton narrowing, local stored-group iteration, and the exact Phase 22 return union.
    - Pure and freshly compiled runs agree on constructor fingerprints, explicit and ambiguous condition references, selected declarative handler, queries, snapshots, clones, results/history/tracing, and callback payloads.
    - `core.py` remains the sole mypyc unit; `conditions.py` stays interpreted; no new runtime dependency or package-root private export appears.
    - Full pure suite, Ruff, mypy, advisory ty, slots policy, compiled build/import, and focused native Phase 23 behavior pass.
  </behavior>
  <action>Update AST/slots tests first to declare the intended representation promotion: exact fields for `TransitionEntry`, `_PreparedTransition`, `_GraphTransition`, and the plural declarative record; frozen/slotted immutable sequence owners; and no singleton-requirement call in serialization, declarative, clone, snapshot, or query regions. Retain explicit `_TransitionSlot` union narrowing and assert both Phase 22 selectors keep their exact names, keyword-only query flag, `Union[_PreparedDispatch, TransitionResult]`, direct singleton branch, and no cold-helper call, sort, copy, topology scan, or async fan-out. Add a focused pure/native semantic probe covering all three requirements and a caller `priority` kwarg to prove D-08. Update the SPR with the representation promotion/parity invariant, 3/4/5 construction rows, plural declarative source/trigger/target/priority identity, `condition_ref` and legacy ambiguity rules, clone/query semantics, public snapshot stability, and callable-safe errors. Before any pure import or suite, run `task pure-source-check`; if it reports the currently reviewed generated native shadows `src/fast_fsm/core.cpython-312-darwin.so` and `src/fast_fsm/core__mypyc.cpython-312-darwin.so`, confirm those exact paths are generated/untracked, remove only those reported artifacts, and rerun the preflight until it succeeds. Apply Ruff formatting and autofixes before collecting final-source validation, type, slots, and full pure-suite evidence. Then perform a fresh compiled build, assert native `fast_fsm.core` origin, and run focused native parity. After native verification, remove only the newly generated native artifacts at those same reviewed paths, report their cleanup, and rerun `task pure-source-check` successfully so the worktree returns to an unshadowed source state. Do not create wheel/sdist/install evidence, benchmark claims, diagnostics/renderers, README/Sphinx content, or the drone example.</action>
  <verify>
    <automated>task pure-source-check
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff format src/fast_fsm/core.py tests/test_mypyc_guard.py tests/test_builder.py
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check --fix src/fast_fsm/core.py tests/test_mypyc_guard.py tests/test_builder.py
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check src/fast_fsm/core.py tests/test_mypyc_guard.py tests/test_builder.py
task typecheck-mypy
task typecheck-ty
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline python tools/release_evidence.py slots-policy --json
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/ -x -q
FAST_FSM_BUILD_MODE=compiled task build-check
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache FAST_FSM_BUILD_MODE=compiled uv run --offline pytest tests/test_mypyc_guard.py tests/test_builder.py tests/test_advanced_functionality.py tests/test_graph_invariants.py tests/test_state_machine_utils.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_priority_selection.py -k "priority or candidate or declarative or condition_ref or quick or clone or snapshot or reachable or transition_exists" -x -q -s -p no:cov
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline python -c "from pathlib import Path; [path.unlink() for path in (Path('src/fast_fsm/core.cpython-312-darwin.so'), Path('src/fast_fsm/core__mypyc.cpython-312-darwin.so')) if path.is_file()]"
task pure-source-check</automated>
  </verify>
  <acceptance_criteria>
    - AST, mypy, and slots gates lock the promoted immutable candidate record/sequence and candidate-qualified declarative/reference fields.
    - Pure and native core agree on PAR-01 through PAR-03 and D-01 through D-08 behavior.
    - Callback signatures and payloads are unchanged; priority/reference data remains candidate-derived metadata only (D-08).
    - Phase 22 selector shape and direct O(1) singleton path are structurally unchanged; grouped work remains local without runtime sorting/copying.
    - No Phase 24 diagnostics/output or Phase 25 documentation, benchmark, drone, release, or installed-artifact work is introduced.
  </acceptance_criteria>
  <done>Phase 23 construction and identity parity is documented, slotted, type-clean, and equivalent across pure and compiled core without hot-path regression.</done>
</task>

</tasks>

<threat_model>
## ASVS Level

OWASP ASVS L1 applies. Construction adapters and the Python-to-mypyc boundary are the primary trust boundaries.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Public quick/factory rows → canonical registrar | Heterogeneous caller rows must validate completely before one topology appears. |
| Typed Python topology → compiled `core.py` | Native layout and selector behavior must match pure execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-23-09 | Tampering | quick/factory row adapters | high | mitigate | Delegate complete 3/4/5-row batches to the canonical registrar and compare fingerprints across constructors. |
| T-23-10 | Tampering | failed construction publication | high | mitigate | Keep machines/caches local until full validation and one commit; test late failures and retryability. |
| T-23-11 | Information Disclosure | native/error parity | medium | mitigate | Use scalar/indexed errors and pure/native tests that reject callable, condition, payload, and exception representations. |
| T-23-12 | Denial of Service | selector regression through shared helper | high | mitigate | AST-lock direct singleton/group selector structure and prohibit cold projection calls, sorting, copying, topology scans, and async fan-out. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | All construction, declarative, clone, query, serialization paths preserve identity | 23-01..03 | COVERED | Final plan integrates and proves all surfaces. |
| REQ | PAR-01 | Constructors/declarative preserve complete candidates | 23-02, 23-03 | COVERED | Declarative and adapter tasks. |
| REQ | PAR-02 | Runtime metadata/query/clone/snapshot coherence | 23-01..03 | COVERED | Projection, declarative runtime, native integration. |
| REQ | PAR-03 | Callable-safe exact round-trip | 23-01, 23-03 | COVERED | Scalar schema plus native proof. |
| CONTEXT | D-01 | Existing APIs and canonical registrar | 23-02, 23-03 | COVERED | Plan 03 Task 1. |
| CONTEXT | D-02 | Unpublished retryable failures | 23-02, 23-03 | COVERED | Preflight and adapter atomicity. |
| CONTEXT | D-03 | Exact plural handler identity | 23-02, 23-03 | COVERED | Plan 02 Task 1 and native proof. |
| CONTEXT | D-04 | Sync/async declarative parity | 23-02, 23-03 | COVERED | Plan 02 Task 2 and native proof. |
| CONTEXT | D-05 | Deterministic candidate serialization | 23-01, 23-03 | COVERED | Plan 01 Task 1 and native proof. |
| CONTEXT | D-06 | Explicit references, legacy ambiguity error | 23-01, 23-03 | COVERED | Plan 01 Task 1 and native proof. |
| CONTEXT | D-07 | Clone/snapshot/query complete identity | 23-01, 23-03 | COVERED | Plan 01 Task 2 and native proof. |
| CONTEXT | D-08 | Callback signatures/payloads unchanged | 23-01..03 | COVERED | Cross-cutting tests. |
| RESEARCH | R-01 | Promote cold topology noun | 23-01, 23-03 | COVERED | Explicit decision, invariant, AST guard. |
| RESEARCH | R-02 | One atomic publication | 23-01, 23-03 | COVERED | Serializer and adapters. |
| RESEARCH | R-03 | Preserve one-file mypyc and runtime fast path | 23-03 | COVERED | Task 2. |
| RESEARCH | R-04 | Candidate-qualified declarative storage | 23-02, 23-03 | COVERED | Plan 02 and native proof. |
| RESEARCH | R-05 | No Phase 24/25 scope creep | 23-01..03 | COVERED | Explicit exclusions in all actions/criteria. |

<verification>
- Full pure suite and focused native Phase 23 suite pass.
- Ruff, blocking mypy, advisory ty, slots policy, and compiled build/import pass.
- Cross-constructor fingerprints, guard references, declarative identity, query/snapshot/clone parity, and unchanged callbacks are equivalent in pure/native execution.
- No new runtime dependency, compiled module, public snapshot version, diagnostic/output behavior, installed artifact, benchmark, documentation tutorial, or drone example is added.
</verification>

<success_criteria>
- PAR-01 through PAR-03 are covered by executable pure/native tests and canonical construction/projection artifacts.
- D-01 through D-08 are visibly implemented and verified.
- The representation promotion and parity invariant are structurally guarded without altering Phase 22's selector shape or singleton fast path.
- All high-severity threats are mitigated, and Phase 24 receives one complete immutable topology snapshot seam.
</success_criteria>

<output>
Create `.planning/phases/23-construction-declarative-serialization-parity/23-03-SUMMARY.md` when done.
</output>
