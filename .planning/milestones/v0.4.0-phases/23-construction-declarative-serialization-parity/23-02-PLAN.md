---
phase: 23-construction-declarative-serialization-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/fast_fsm/core.py
  - tests/test_builder.py
  - tests/test_transition_lifecycle.py
  - tests/test_async.py
autonomous: true
requirements:
  - PAR-01
  - PAR-02
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-03/D-04: every same-trigger declarative candidate survives discovery and only the handler matching the selected source, target, and exact priority enters one sync or async lifecycle."
    - "D-03: direct declarative helpers preserve legacy behavior for one match and fail without invocation when candidate identity is ambiguous."
    - "D-02: plural declarative async detection and preflight inspect every declaration before any machine is cached or builder staging is frozen."
    - "D-01: declarative rows replay through the canonical atomic registrar with default priority 0 rather than creating an adapter-specific topology."
    - "D-08: the selected handler receives the original application args/kwargs exactly once and receives no injected priority or condition-reference keyword."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Immutable plural declarative metadata and exact candidate-qualified sync/async handler resolution"
    - path: "tests/test_builder.py"
      provides: "Discovery, stacked decorator, direct ambiguity, builder preflight, and retryability coverage"
    - path: "tests/test_transition_lifecycle.py"
      provides: "One selected declarative handler within one lifecycle and unchanged payload proof"
    - path: "tests/test_async.py"
      provides: "Sequential async candidate-handler identity and plural preflight parity"
  key_links:
    - from: "@transition metadata"
      to: "DeclarativeState._handlers"
      via: "immutable plural declarations carrying source, target, guard, async classification, and priority"
      pattern: "priority"
    - from: "Phase 22 selected TransitionEntry"
      to: "_PreparedDispatch.declarative_handler"
      via: "canonical source/trigger/target/priority resolver"
      pattern: "_resolve_declarative_handler"
    - from: "DeclarativeState._handlers"
      to: "FSMBuilder async preflight"
      via: "every declaration is flattened and classified before candidate publication"
      pattern: "_preflight_async_requirements"
---

<objective>
Make declarative metadata candidate-complete and bind exactly one source/target/priority handler after Phase 22 selection, with matching synchronous and asynchronous lifecycle and builder-preflight semantics (D-01 through D-04, D-08).

Purpose: Remove trigger-key overwrite and discovery-order precedence without introducing a second dispatcher or changing callbacks.
Output: Plural immutable handler declarations, priority-aware decorator metadata, exact resolver wiring, direct-call ambiguity behavior, and full async preflight.
</objective>

## Artifacts this phase produces

- A frozen/slotted declarative candidate record and immutable per-trigger declaration sequence.
- Additive keyword-only `priority=0` on `@transition`, including stacked-decorator retention.
- Exact selected-entry handler lookup by source, trigger, target, and priority.
- Sync/async direct-helper ambiguity and builder preflight/retryability proof.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-construction-declarative-serialization-parity/23-CONTEXT.md
@.planning/phases/23-construction-declarative-serialization-parity/23-RESEARCH.md
@.planning/phases/23-construction-declarative-serialization-parity/23-PATTERNS.md
@.planning/phases/23-construction-declarative-serialization-parity/23-VALIDATION.md
@.planning/phases/23-construction-declarative-serialization-parity/23-01-SUMMARY.md
@.planning/phases/22-ordered-runtime-selection-lifecycle-integration/22-VERIFICATION.md
@.specify/decisions/ADR-007-priority-topology.md

<interfaces>
- `_select_transition_sync` and `_select_transition_async` already own guard → declarative guard → permission ordering and create one `_PreparedDispatch` before lifecycle.
- `_PreparedDispatch.declarative_handler` is the only declarative callback passed into `_execute_transition*`; priority remains on `entry.priority`.
- `transition()` currently stores scalar method attributes; `DeclarativeState._discover_handlers()` currently overwrites `_handlers[trigger]`.
- `FSMBuilder._detect_async_requirements()` and `_preflight_async_requirements()` currently assume each `_handlers` value is one mapping.
- `FSMBuilder.build()` keeps its candidate private and assigns `_machine` only after topology/callback wiring succeeds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve plural declarations and resolve the exact sync handler</name>
  <reversibility rating="costly">D-03 establishes observable safety identity for declarative candidate handlers.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_builder.py, tests/test_transition_lifecycle.py</files>
  <read_first>
    - `src/fast_fsm/core.py` — inspect `transition`, `_resolve_declarative_handler`, sync selector preparation, `DeclarativeState._discover_handlers`, `can_transition`, and `handle_event`.
    - `tests/test_builder.py` — follow current ordinary declarative dispatch, handler discovery, builder state, and exact-once assertions.
    - `tests/test_transition_lifecycle.py` — reuse event-ledger, callback payload, selected priority, history, and post-commit handler tests.
    - `23-RESEARCH.md` §Pattern 3 — follow immutable plural storage and exact selected-candidate resolution.
  </read_first>
  <behavior>
    - Multiple and stacked same-trigger decorated methods all survive discovery independent of attribute/decorator order.
    - `@transition(..., priority=N)` exact-validates a non-boolean integer and defaults ordinary declarations to `0`.
    - Runtime selection resolves the declarative guard and handler only for the candidate matching canonical source, trigger, target, and priority, including two candidates with the same target.
    - One selected handler runs at the existing lifecycle slot; rejected/lower handlers remain untouched and original args/kwargs arrive unchanged.
    - Direct `handle_event()` and `can_transition()` preserve their one-declaration behavior but return a failed result/false and invoke nothing when multiple declarations remain after source/target filtering.
    - Malformed duplicate declarative identity fails before transition publication rather than using discovery order.
  </behavior>
  <action>Write focused RED cases first for stacked declarations, same-trigger different-target candidates, same-target different-priority candidates, reversed attribute order, malformed duplicate identity, direct unique behavior, direct ambiguity, and a caller keyword named `priority`. Per D-03 and D-04, replace the trigger-to-mapping `_handlers` model with a trigger-to-immutable-tuple of typed frozen/slotted declaration records containing method, canonical source metadata, target metadata, condition, async classification, and exact priority. Extend `transition()` with keyword-only `priority: object = 0`, normalize it through `_normalize_priority`, preserve legacy scalar metadata for a single unstacked declaration, and accumulate immutable declaration metadata so stacked decorators cannot overwrite. Discover into a complete local mapping and publish once. Change `_resolve_declarative_handler` to accept selected priority and require exact canonical source/trigger/target/priority identity; pass `entry.priority` from both selectors before `_PreparedDispatch` is built. Reject multiple exact matches during construction where possible. Keep one-handler lifecycle invocation and callbacks unchanged per D-08. For direct helpers lacking selected-candidate context, accept one unambiguous filtered declaration and return a redacted failed `TransitionResult`/false with no invocation for multiple matches; never choose by discovery order or lowest priority. Do not move guard selection outside the FSM or alter Phase 22 selection order.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/test_builder.py tests/test_transition_lifecycle.py tests/test_priority_selection.py -k "declarative and (priority or candidate or handler or lifecycle or ambiguous)" -x -q
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff format src/fast_fsm/core.py tests/test_builder.py tests/test_transition_lifecycle.py
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check --fix src/fast_fsm/core.py tests/test_builder.py tests/test_transition_lifecycle.py
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check src/fast_fsm/core.py tests/test_builder.py tests/test_transition_lifecycle.py</automated>
  </verify>
  <acceptance_criteria>
    - Every declarative candidate survives discovery and numeric priority—not decorator or attribute order—determines selection (D-03, D-04).
    - The exact source/trigger/target/priority declaration supplies one guard and one handler, including same-target candidates.
    - Direct helper ambiguity returns failure/false and invokes no declaration; unique legacy direct calls remain compatible.
    - Original callback args/kwargs are unchanged and internal priority never collides with a caller keyword (D-08).
    - Declarative metadata is immutable, typed, slotted, and promoted to the same candidate-sequence concept as Plan 01.
  </acceptance_criteria>
  <done>Sync declarative discovery and lifecycle bind exactly one selected candidate handler without overwrite, implicit precedence, or callback changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror declarative identity through async dispatch and full builder preflight</name>
  <reversibility rating="costly">D-04 requires sync/async declarative identity and precedence to remain identical.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_builder.py, tests/test_async.py, tests/test_transition_lifecycle.py</files>
  <read_first>
    - `src/fast_fsm/core.py` — inspect async selector/lifecycle, `handle_event_async`, `can_transition_async`, `_detect_async_requirements`, `_preflight_async_requirements`, and `build` after Task 1.
    - `tests/test_async.py` — follow async declarative exact-once, cancellation, clone, and callback-independence patterns.
    - `tests/test_builder.py` — follow auto/forced mode detection, recursive condition preflight, failed build retry, and `_machine is None` assertions.
    - `tests/test_transition_lifecycle.py` — preserve the Phase 22 sequential-selection and one-lifecycle ledger.
  </read_first>
  <behavior>
    - Async selection awaits only the exact candidate declaration's guard and invokes only its handler after one selected lifecycle.
    - Rejection, exception, and cancellation do not invoke another declaration or lower candidate; cancellation bare re-raises under the existing boundary.
    - Direct async helper ambiguity returns failure/false without awaiting or invoking any declaration.
    - Auto-detection and explicit-sync preflight inspect every declaration in every tuple, including a hidden later async handler or nested async guard.
    - Failed preflight leaves `_machine is None`, staged declarations/transitions intact, mode retryable, and no partial candidate topology visible.
    - Async callback payloads and Phase 22 selector return shapes remain unchanged.
  </behavior>
  <action>Add async RED cases before implementation: a lower-priority async declaration discovered first; same-target distinct priorities; false/exception/cancellation termination; direct ambiguity; and an async requirement in the second or later plural declaration. Flatten every declarative tuple in `_detect_async_requirements` and `_preflight_async_requirements`, applying existing nested-condition classification to each declaration. Preserve the builder's validate-before-mutate and local-candidate-before-cache envelopes per D-02: explicit sync rejects any async declaration before creating/publishing topology, auto mode chooses `AsyncStateMachine`, and failed attempts keep `_machine` unset and staging repairable. Update `handle_event_async` and `can_transition_async` with the same unique-versus-ambiguous rule as sync. Keep async candidate evaluation sequential, selector return type `Union[_PreparedDispatch, TransitionResult]`, cancellation bookkeeping, ownership, and one lifecycle unchanged. Ensure no handler or condition reference appears in public error text.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/test_builder.py tests/test_async.py tests/test_transition_lifecycle.py tests/test_priority_selection.py -k "declarative or preflight or priority or candidate or cancellation" -x -q
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff format src/fast_fsm/core.py tests/test_builder.py tests/test_async.py tests/test_transition_lifecycle.py
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check --fix src/fast_fsm/core.py tests/test_builder.py tests/test_async.py tests/test_transition_lifecycle.py
UV_CACHE_DIR=/tmp/fast-fsm-phase23-uv-cache uv run --offline ruff check src/fast_fsm/core.py tests/test_builder.py tests/test_async.py tests/test_transition_lifecycle.py</automated>
  </verify>
  <acceptance_criteria>
    - Sync and async declarative paths bind guards/handlers by the same exact source/trigger/target/priority identity and preserve one-handler lifecycle behavior (D-04).
    - Async false, exception, and cancellation preserve Phase 22 sequential terminal semantics without lower-handler invocation.
    - Detection and preflight classify every plural declaration, including later async requirements.
    - Failed preflight/build leaves builder staging, cache, and topology unpublished and retryable (D-02).
    - Callback signatures, application kwargs, ownership, and selector shape remain unchanged (D-08).
  </acceptance_criteria>
  <done>Plural declarative identity is equivalent in sync and async machines, and builder preflight remains complete, atomic, and retryable.</done>
</task>

</tasks>

<threat_model>
## ASVS Level

OWASP ASVS L1 applies. Decorator metadata and user-supplied guards/callbacks cross into selected lifecycle execution.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Decorated methods → declarative candidate table | Discovery order and metadata must not alter candidate identity or precedence. |
| Selected candidate → declarative lifecycle handler | Exactly one matching handler receives caller payload after selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-23-05 | Tampering | declarative discovery/resolution | high | mitigate | Store every immutable declaration and exact-match source/trigger/target/priority; test reversed discovery and same-target candidates. |
| T-23-06 | Tampering | builder async preflight | high | mitigate | Inspect every plural declaration before candidate/cache publication and prove retryability after rejection. |
| T-23-07 | Information Disclosure | ambiguity/preflight errors | medium | mitigate | Use fixed/indexed metadata-only errors and assert no callable, condition, or payload representations. |
| T-23-08 | Denial of Service | async declaration evaluation | medium | mitigate | Retain Phase 22 sequential short-circuit and cancellation semantics; no fan-out or speculative awaits. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Candidate identity through declarative and construction paths | 23-02 | COVERED | Plural metadata and builder preflight. |
| REQ | PAR-01 | Declarative handlers preserve identity/priority | 23-02 | COVERED | Both tasks. |
| REQ | PAR-02 | `can_trigger*`, lifecycle metadata, callback compatibility | 23-02, 23-03 | COVERED | Runtime regressions and final integration proof. |
| CONTEXT | D-01 | Existing declarative/builder surface uses canonical registrar | 23-02, 23-03 | COVERED | Builder construction remains canonical. |
| CONTEXT | D-02 | Failure unpublished and retryable | 23-02, 23-03 | COVERED | Task 2 plus adapter proof. |
| CONTEXT | D-03 | Plural exact candidate handlers | 23-02 | COVERED | Task 1. |
| CONTEXT | D-04 | Sync/async parity and numeric precedence | 23-02 | COVERED | Both tasks. |
| CONTEXT | D-08 | Callback signatures unchanged | 23-02 | COVERED | Both tasks. |
| RESEARCH | R-04 | Immutable plural declarative metadata | 23-02 | COVERED | Task 1. |
| RESEARCH | R-05 | Flatten all declarations in preflight | 23-02 | COVERED | Task 2. |

<verification>
- Focused sync/async declarative, priority-selection, lifecycle, and builder-preflight tests pass.
- Ruff validates core and touched tests.
- Ambiguity and preflight errors disclose no user callable/payload representation.
- Plan 23-03 supplies whole-phase type/native/full-suite evidence.
</verification>

<success_criteria>
- PAR-01 declarative storage and dispatch preserve all candidates and exact priority identity.
- PAR-02 sync/async query/lifecycle behavior and callback payload compatibility remain coherent.
- D-01 through D-04 and D-08 are covered by active tests and every high-severity threat is mitigated.
</success_criteria>

<output>
Create `.planning/phases/23-construction-declarative-serialization-parity/23-02-SUMMARY.md` when done.
</output>
