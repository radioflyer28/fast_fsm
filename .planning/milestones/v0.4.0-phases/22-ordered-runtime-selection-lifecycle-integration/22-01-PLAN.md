---
phase: 22-ordered-runtime-selection-lifecycle-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - tests/test_priority_selection.py
  - tests/test_graph_invariants.py
  - tests/test_transition_lifecycle.py
autonomous: true
requirements:
  - SEL-01
  - SEL-03
  - SEL-04
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: synchronous grouped dispatch evaluates transition guard, target-specific declarative guard, and captured-source permission in ascending stored priority order, then stops at the first fully eligible candidate."
    - "D-02: ordinary rejection advances only within a group; an eligibility exception terminates immediately and never reaches a lower candidate."
    - "D-03: selection completes before one existing lifecycle begins, so rejected candidates cannot run callbacks, mutate state, append history, or notify success observers."
    - "D-04: group exhaustion is one uncommitted selection-stage failure finalized once, while missing-trigger resolution and singleton rejection keep their established meanings."
    - "D-05: can_trigger applies the same ordered eligibility and short-circuit rules without lifecycle work, mutation, history, or failure observation."
    - "D-06: singleton lookup stays direct O(1), and grouped dispatch scans only the stored local tuple without copying, sorting, or unrelated topology work."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Direct-singleton and ordered-group synchronous selection feeding one prepared lifecycle handoff"
      contains: "_TransitionGroup"
    - path: "tests/test_priority_selection.py"
      provides: "Focused synchronous ordered-selection, query, exception, and exhaustion contract"
    - path: "tests/test_graph_invariants.py"
      provides: "Runtime-selection versus still-fail-closed projection boundary"
    - path: "tests/test_transition_lifecycle.py"
      provides: "Selection stage and one-lifecycle ordering/finalization evidence"
  key_links:
    - from: "StateMachine.trigger"
      to: "_TransitionGroup.entries"
      via: "one direct source/trigger lookup followed by ascending local iteration only for a real group"
      pattern: "_TransitionGroup"
    - from: "candidate eligibility"
      to: "_PreparedDispatch"
      via: "the exact winning entry, captured source, sanitized guard context, and target-specific declarative handler are retained once"
      pattern: "_PreparedDispatch"
    - from: "prepared winner"
      to: "StateMachine._execute_transition"
      via: "one internal positional handoff begins lifecycle only after all eligibility stages pass"
      pattern: "_execute_transition"
---

<objective>
Make direct-registered synchronous candidate groups select one fully eligible transition deterministically, then enter the existing lifecycle exactly once (D-01 through D-05).

Purpose: Prove the phase's end-to-end runtime architecture on the synchronous path before mirroring it across asynchronous awaits and public runtime metadata.
Output: A production synchronous selector, a focused selection contract suite, and lifecycle/projection boundary regressions.
</objective>

## Artifacts this phase produces

- A direct singleton branch and an ascending tuple scan only for `_TransitionGroup` slots.
- One prepared-winner handoff that captures source identity, guard context, and the exact target-specific declarative handler before lifecycle work.
- Focused sync tests for three-stage ordering, fallthrough, exceptions, exhaustion, query parity, and one-lifecycle behavior.
- A retained fail-closed projection boundary: runtime selection works while `to_dict()` and other Phase 23 projections still reject grouped topology.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-ordered-runtime-selection-lifecycle-integration/22-CONTEXT.md
@.planning/phases/22-ordered-runtime-selection-lifecycle-integration/22-RESEARCH.md
@.planning/phases/22-ordered-runtime-selection-lifecycle-integration/22-PATTERNS.md
@.planning/phases/22-ordered-runtime-selection-lifecycle-integration/22-VALIDATION.md
@.planning/phases/21-priority-contract-atomic-registration/21-VERIFICATION.md
@.planning/phases/21-priority-contract-atomic-registration/21-02-SUMMARY.md
@.specify/decisions/ADR-007-priority-topology.md
@.specify/memory/constitution.md
@.specify/memory/spr-core-api.md

<interfaces>
Current Phase 21 topology consumed by this plan:
- `TransitionEntry(to_state, condition=None, priority=0)` is the direct singleton candidate.
- `_TransitionGroup.entries: Tuple[TransitionEntry, ...]` is frozen, slotted, and already sorted by ascending priority.
- `_TransitionSlot = Union[TransitionEntry, _TransitionGroup]` is the exact value at `_transitions[source][trigger]`.
- `_require_singleton_entry()` is the deliberate Phase 23 projection guard; runtime selection must not weaken it.

Current lifecycle seams preserved by this plan:
- `_select_transition_sync(..., *, for_query: bool) -> Union[_PreparedDispatch, TransitionResult]` is the exact private selector contract; no additional outcome type is introduced.
- `_PreparedDispatch(entry, source_state, current_name, trigger, args, condition_kwargs, declarative_handler)` is the exact frozen/slotted field order; candidate priority remains available only as `entry.priority`.
- `_PRIORITY_GROUP_EXHAUSTED_ERROR` is exactly `"No eligible transition candidate"`.
- `_evaluate_condition_sync()`, `_evaluate_declarative_condition_sync()`, and `_can_transition_after_declarative_guard()` are the three eligibility stages.
- `_build_failure_result()` describes failures and `_finalize_failure()` is called once only by the owned public trigger path.
- `_execute_transition()` and `_commit_transition()` own the established callback, listener, history, and state-mutation lifecycle.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one ordered synchronous winner through one lifecycle</name>
  <reversibility rating="costly">D-01 and D-03 define observable priority, eligibility-stage, and lifecycle boundaries consumed by every later constructor and diagnostic surface.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_priority_selection.py, tests/test_transition_lifecycle.py</files>
  <read_first>
    - `src/fast_fsm/core.py` — read `_LIFECYCLE_STAGES`, `_TransitionGroup`, `_PreparedDispatch`, `can_trigger`, `_prepare_transition`, the declarative/permission evaluators, `trigger`, `_trigger_owned`, `_execute_transition`, and `_commit_transition` from the current source; do not rely on historical line numbers.
    - `tests/test_graph_invariants.py` — use the current priority registration helpers and real singleton/group assertions without mocking the selector.
    - `tests/test_transition_lifecycle.py` — reuse `LifecycleRecorder`, real state subclasses, event-ledger assertions, and one-history-record checks.
    - `.planning/phases/22-ordered-runtime-selection-lifecycle-integration/22-RESEARCH.md` §Architecture Patterns 1-4 and §Required Test Matrix — follow the verified direct-branch, prepared-winner, and failure-boundary guidance.
  </read_first>
  <behavior>
    - Register candidates out of order so the highest numeric priority rejects at its transition guard, the next candidate passes transition and declarative guards plus target permission, and a later candidate has a side-effect counter; `trigger()` chooses by ascending stored numeric priority and never touches the later counter.
    - Candidate event order is transition guard, target-specific declarative guard, then captured-source target permission; every eligibility event precedes before-transition, state callbacks, listeners, commit/history, declarative handler, trigger callbacks, and after-transition.
    - The selected target is committed once, history contains one record, the declarative handler belongs to that exact target, and success observers/listeners run once.
    - Application positional and keyword payload reaches guard and lifecycle callbacks under the existing sanitized-versus-original mapping contract, including a caller keyword named `priority` without collision with internal metadata.
    - A direct singleton retains its existing guard, permission, callback, and result behavior without tuple allocation or iteration.
  </behavior>
  <action>Write the focused tracer tests first in `tests/test_priority_selection.py` and the end-to-end lifecycle ledger in `tests/test_transition_lifecycle.py`. In `core.py`, remove only the grouped-runtime fail-closed branch and implement `_select_transition_sync(..., *, for_query: bool) -> Union[_PreparedDispatch, TransitionResult]`; do not introduce another outcome class or widen the union to `Any`. Perform the outer and inner dictionary lookups once; branch directly for `TransitionEntry`, and iterate `slot.entries` without copying or sorting only for `_TransitionGroup`, per D-01 and D-06. For each candidate, prepare a frozen/slotted `_PreparedDispatch` with the exact ordered fields `entry`, `source_state`, `current_name`, `trigger`, `args`, `condition_kwargs`, and `declarative_handler`; keep priority solely on `entry.priority`. Evaluate transition guard, declarative guard, then captured-source permission in that exact order. Ordinary false is internal group scan control; a singleton false remains its established terminal guard or state-permission outcome. Stop immediately on the first fully eligible candidate and pass that prepared value into the lifecycle as an internal positional object so caller kwargs—including `priority`—remain untouched. Do not re-resolve the declarative handler, re-evaluate any guard, or reread `_current_state` between selection and lifecycle. Use the existing `_execute_transition` and `_commit_transition` callback/history/state seams once; do not place lifecycle execution inside the candidate loop. Do not alter `_require_singleton_entry`, serialization, graph snapshots, decorators, builders, factories, or diagnostic consumers.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/test_priority_selection.py tests/test_transition_lifecycle.py -k "sync and (priority or candidate or selection or lifecycle)" -x -q
UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache uv run --offline ruff format src/fast_fsm/core.py tests/test_priority_selection.py tests/test_transition_lifecycle.py
UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache uv run --offline ruff check --fix src/fast_fsm/core.py tests/test_priority_selection.py tests/test_transition_lifecycle.py
UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache uv run --offline ruff check src/fast_fsm/core.py tests/test_priority_selection.py tests/test_transition_lifecycle.py</automated>
  </verify>
  <acceptance_criteria>
    - A real grouped `trigger()` chooses the first fully eligible candidate in ascending priority order despite registration order, and no later candidate is inspected (D-01, SEL-01).
    - The observed candidate stages are guard → declarative guard → captured-source permission, followed by one complete existing lifecycle (D-03, SEL-03).
    - The winning candidate's target-specific declarative handler runs once and no rejected candidate produces callback, history, state, success-observer, or failure-observer effects.
    - Direct singleton dispatch behavior and original application args/kwargs are unchanged; internal priority transport cannot collide with a caller `priority` keyword.
    - Runtime selection does not weaken the Phase 23 projection guard or add any public topology-inspection surface.
  </acceptance_criteria>
  <done>A synchronous direct-registered candidate group resolves one winner before one lifecycle, while the singleton path and later-phase projection boundary remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close synchronous rejection, exception, exhaustion, and query boundaries</name>
  <reversibility rating="costly">D-02, D-04, and D-05 establish terminal failure and observer-free query semantics that downstream APIs must preserve.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_priority_selection.py, tests/test_graph_invariants.py, tests/test_transition_lifecycle.py</files>
  <read_first>
    - `src/fast_fsm/core.py` — read current `_build_failure_result`, `_finalize_failure`, `_evaluate_declarative_condition_sync`, `_can_transition_after_declarative_guard`, `can_trigger`, and `_trigger_owned` implementations after Task 1.
    - `tests/test_graph_invariants.py` — split `test_grouped_consumers_fail_closed_without_selecting_or_projecting`: runtime now resolves candidates, but the projection error remains asserted.
    - `tests/test_transition_lifecycle.py` — reuse the pre-commit failure matrix and lifecycle-stage catalog; the actual file is `test_transition_lifecycle.py`, not the stale `test_lifecycle_results.py` reference in context.
    - `tests/test_priority_selection.py` — extend the tracer fixture with per-stage outcomes and side-effect counters rather than mocking selector internals.
  </read_first>
  <behavior>
    - A group candidate rejected by transition guard, declarative guard, or permission falls through; all candidates rejected returns one failure with `stage="selection"`, `committed=False`, `to_state=None`, `cause=None`, and one failure-observer call.
    - An ordinary exception at any of the three eligibility stages stops immediately, preserves the evaluated candidate's cause and truthful existing guard or state-permission stage, leaves state/history unchanged, and finalizes failure once.
    - A lifecycle failure after selection never evaluates another candidate; its established stage, commit flag, state/history truth, cause identity, and single failure observation remain unchanged.
    - A missing trigger remains a resolution failure, and a rejected singleton retains its existing guard or state-permission stage rather than becoming selection exhaustion.
    - `can_trigger()` applies the same candidate order and stops on a winner or exception but never enters lifecycle, mutates state/history, or invokes success/failure observers; preserve its established outward exception behavior while preventing any exception from advancing to a lower candidate.
  </behavior>
  <action>Add `_LIFECYCLE_STAGE_SELECTION` to the stable lifecycle catalog immediately after resolution and define `_PRIORITY_GROUP_EXHAUSTED_ERROR = "No eligible transition candidate"`. Extend `_select_transition_sync(..., *, for_query: bool)` within its exact `Union[_PreparedDispatch, TransitionResult]` return contract; ordinary group rejection continues internally, a winner returns `_PreparedDispatch`, and terminal trigger-path failure returns `TransitionResult`. For grouped dispatch, continue only after a normal false at an eligibility stage. Convert ordinary trigger-path exceptions through `_build_failure_result` with their existing guard/state-permission stage and cause, then let `_trigger_owned` call `_finalize_failure` once. When the tuple is exhausted, return one selection-stage failure whose error equals `_PRIORITY_GROUP_EXHAUSTED_ERROR` exactly, with no selected target, priority, or cause, and finalize once. Preserve missing-trigger resolution and direct singleton failure wording/stages. Make `can_trigger()` invoke the selector with `for_query=True`, convert a returned `TransitionResult` to `False` without finalization or lifecycle work, preserve outward transition-condition/state-permission exceptions and terminal declarative-query `False`, and ensure no exception activates a lower candidate. Assert the exact fixed exhaustion string and absence of candidate count, priority values, trigger/state text, condition/exception representations, and payload data. Update the Phase 21 grouped-consumer regression so direct runtime selection is enabled but `_require_singleton_entry` projections still raise their fixed Phase 23 error. Add a post-selection callback-failure regression that proves no lower candidate evaluation and exactly one truthful lifecycle failure.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/test_priority_selection.py tests/test_graph_invariants.py tests/test_transition_lifecycle.py -k "priority or candidate or selection or exhaust or exception or singleton or projection" -x -q
UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache uv run --offline ruff format src/fast_fsm/core.py tests/test_priority_selection.py tests/test_graph_invariants.py tests/test_transition_lifecycle.py
UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache uv run --offline ruff check --fix src/fast_fsm/core.py tests/test_priority_selection.py tests/test_graph_invariants.py tests/test_transition_lifecycle.py
UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache uv run --offline ruff check src/fast_fsm/core.py tests/test_priority_selection.py tests/test_graph_invariants.py tests/test_transition_lifecycle.py</automated>
  </verify>
  <acceptance_criteria>
    - False at each of the three eligibility stages advances to the next group candidate, while an exception at any stage terminates without inspecting a lower candidate (D-02, SEL-01).
    - Exhaustion yields exactly one uncommitted `selection` failure and failure-observer sequence; missing trigger remains `resolution` and singleton rejection retains its prior stage (D-04, SEL-04).
    - Group exhaustion uses the exact error `"No eligible transition candidate"`; the message contains no candidate, topology, condition, exception, or payload detail.
    - A callback/listener/declarative-handler failure after selection never retries selection and preserves the existing one-lifecycle commit/history truth (D-03, SEL-03).
    - `can_trigger()` is ordered and short-circuited identically but remains observer-free and mutation-free, with its established outward exception compatibility preserved (D-05).
    - Grouped runtime support does not unlock serialization, graph snapshots, construction adapters, diagnostics, output, or public candidate inspection.
  </acceptance_criteria>
  <done>Synchronous selection distinguishes rejection, terminal exception, exhaustion, missing resolution, and lifecycle failure without duplicate work or observation.</done>
</task>

</tasks>

<threat_model>
## ASVS Level

OWASP ASVS L1 applies. All high-severity threats below are blocking and must be mitigated by this plan's implementation and automated tests.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-controlled guards/state hooks → selector | User code and trigger payload influence candidate eligibility inside the compiled runtime. |
| Selected candidate → lifecycle | A prepared winner crosses from speculative eligibility into callbacks, history, observers, and state mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | synchronous candidate scan | high | mitigate | Evaluate the stored immutable tuple in ascending order, enforce guard/declarative/permission ordering, stop on winner or exception, and test untouched lower-candidate counters. |
| T-22-02 | Tampering | prepared winner → lifecycle | high | mitigate | Carry one frozen/slotted prepared winner into the existing lifecycle exactly once; test that rejected candidates have no callbacks, history, observer, or mutation effects. |
| T-22-03 | Repudiation | exhaustion and failure finalization | medium | mitigate | Build one truthful staged result and call the established failure finalizer once; preserve cause identity separately from fixed redacted error text. |
| T-22-04 | Information Disclosure | guard payload and error reporting | high | mitigate | Reuse bounded sanitized condition kwargs, retain original callback kwargs without internal-key injection, and keep exhaustion/exception messages free of payload, condition, and priority-group details. |
</threat_model>

<verification>
- `UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache FAST_FSM_BUILD_MODE=pure uv run --offline pytest tests/test_priority_selection.py tests/test_graph_invariants.py tests/test_transition_lifecycle.py -x -q`
- `UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache uv run --offline ruff format src/fast_fsm/core.py tests/test_priority_selection.py tests/test_graph_invariants.py tests/test_transition_lifecycle.py`
- `UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache uv run --offline ruff check --fix src/fast_fsm/core.py tests/test_priority_selection.py tests/test_graph_invariants.py tests/test_transition_lifecycle.py`
- `UV_CACHE_DIR=/tmp/fast-fsm-phase22-uv-cache uv run --offline ruff check src/fast_fsm/core.py tests/test_priority_selection.py tests/test_graph_invariants.py tests/test_transition_lifecycle.py`
- Confirm `_require_singleton_entry` and the fixed grouped-projection error remain active for Phase 23 consumers.
</verification>

<success_criteria>
- SEL-01: synchronous grouped dispatch selects the first candidate passing guard, target-specific declarative guard, and permission in ascending priority order.
- SEL-03: all selection work completes before exactly one lifecycle, history append, state commit, and success sequence.
- SEL-04: group exhaustion is one selection-stage failure/observer event, distinct from missing resolution and singleton rejection.
- D-05 query parity is proven without observers or mutation, and every high-severity threat is mitigated by active tests.
</success_criteria>

<output>
Create `.planning/phases/22-ordered-runtime-selection-lifecycle-integration/22-01-SUMMARY.md` when done.
</output>
