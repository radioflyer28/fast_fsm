---
phase: 25-performance-artifact-proof-drone-guidance
plan: "05"
type: execute
wave: 5
depends_on:
  - 25-04
files_modified:
  - Taskfile.yml
  - tools/release_evidence.py
  - tests/test_release_evidence.py
  - evidence/release-baseline.json
autonomous: true
gap_closure: true
requirements:
  - PERF-02
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-02, every tracked-baseline write/check and Phase 25 release-proof aggregate reaches a single ordered preflight before any proof child: it requires UV_OFFLINE exactly 1, verifies the resolved executable reports uv 0.12.6, then runs pure-source-check; each release entry point depends only on that preflight."
    - "The tracked release-baseline writer snapshots existing bytes and rejects a candidate before writing unless raw and stable projections differ only at quality_baseline.tests.collected, quality_baseline.tests.passed, measurement_environment, performance_contract.observation, and pure_source_performance.observations."
    - "A rejected offline/toolchain preflight or prohibited manifest drift leaves evidence/release-baseline.json byte-for-byte unchanged; relative and absolute spellings of that resolved protected path use the guarded writer, while a resolved non-baseline temporary path retains generic write_or_check_manifest behavior."
    - "The durable baseline is regenerated by the guarded writer after the adversarial tests are added; its final collected/passed values come from that collection rather than a pre-change constant, errors and failures remain zero, skips remain six, and all read-only release proofs pass under the reviewed offline toolchain."
  artifacts:
    - path: Taskfile.yml
      provides: "One release-proof environment preflight inherited by baseline and Phase 25 artifact-proof tasks"
      contains: "release-proof-environment-check"
    - path: tools/release_evidence.py
      provides: "Fail-closed release environment validation and transactional tracked-baseline refresh guard"
      contains: "_write_release_baseline_guarded"
    - path: tests/test_release_evidence.py
      provides: "Adversarial environment, Taskfile-wiring, allowlist, and byte-preservation coverage"
    - path: evidence/release-baseline.json
      provides: "Fresh evidence produced only through the guarded reviewed-toolchain path"
  key_links:
    - from: Taskfile.yml
      to: tools/release_evidence.py
      via: "release-proof-environment-check performs environment/version checks and then pure-source-check in command order; each relevant release task has this gate as its sole direct dependency before uv sync/build/run, while evidence collection independently revalidates the environment"
      pattern: "release-proof-environment-check|_require_release_proof_environment"
    - from: tools/release_evidence.py
      to: evidence/release-baseline.json
      via: "the evidence --write path resolves/canonicalizes the requested manifest before comparing it with the resolved repository baseline and selects _write_release_baseline_guarded for matching relative or absolute spellings"
      pattern: "resolve|_write_release_baseline_guarded"
    - from: tests/test_release_evidence.py
      to: Taskfile.yml
      via: "parsed task definitions prove every Phase 25 proof entry point depends on the environment gate"
      pattern: "release-proof-environment-check"
---

<objective>
Close the Phase 25 security audit gaps by making the existing release-evidence workflow enforce its reviewed offline execution context and constrain tracked baseline regeneration before any bytes can change, per D-02.

Purpose: Plan 25-04 used the correct uv 0.12.6 and UV_OFFLINE=1 environment and manually reviewed the diff, but the repository does not yet make those controls mandatory for the next maintainer. T-25-G01, T-25-G02, and T-25-SC remain open until the workflow itself rejects an ambient toolchain, network-capable execution, or unrelated durable evidence drift.
Output: One guarded release-proof environment task, private release-evidence validation/write helpers, adversarial regression tests, and a freshly regenerated/read-only-verified baseline. No runtime FSM, dependency, lockfile, public library API, or quality-threshold change.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-CONTEXT.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-RESEARCH.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-PATTERNS.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-VERIFICATION.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-UAT.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-SECURITY.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-04-PLAN.md
@.planning/phases/25-performance-artifact-proof-drone-guidance/25-04-SUMMARY.md
@.github/copilot-instructions.md
@Taskfile.yml
@tools/release_evidence.py
@tests/test_release_evidence.py
@evidence/release-baseline.json

<interfaces>
`REQUIRED_UV_VERSION` is the repository authority for the exact reviewed `0.12.6` executable. `_command_environment()` carries caller environment into evidence children while forcing pure build mode; the new environment guard must run before `_source_preflight()` or any build/test/coverage child.

`write_or_check_manifest(manifest, manifest_path, write)` is the generic tested serializer/checker and remains unchanged for custom paths. Before choosing a writer, the existing `evidence --write` command anchors a relative `manifest_path` to the repository root, preserves an absolute input, and then canonicalizes/resolves it for comparison with the resolved repository-owned `evidence/release-baseline.json`; a match, including the relative Taskfile value, receives the private guarded seam, while a resolved non-baseline path remains generic. No additional evidence CLI mode or public package API is introduced.

The only refreshable tracked-baseline paths are `quality_baseline.tests.collected`, `quality_baseline.tests.passed`, `measurement_environment`, `performance_contract.observation`, and `pure_source_performance.observations`. Stable comparison still applies Python/layout normalization, but the raw projection permits no other byte-semantic field drift.
</interfaces>
</context>

## Artifacts this phase produces

| Surface | Contract after execution |
| --- | --- |
| `Taskfile.yml` task `release-proof-environment-check` | Requires caller-provided `UV_OFFLINE=1`, verifies exact `uv 0.12.6`, and only then invokes `pure-source-check`; each of `release-baseline-write`, `release-baseline-check`, `release-gate`, `release-installed-artifacts-check`, `release-installed-performance-check`, and `release-readiness-check` uses this preflight as its sole direct dependency before collection/build work. |
| `tools.release_evidence._require_release_proof_environment()` | Revalidates exact offline and uv identity inside evidence collection before the first source/test/build child. |
| `tools.release_evidence._validate_release_baseline_refresh()` | Compares pre-write and candidate raw/stable projections after removing exactly the five approved refresh paths; rejects every other field difference with actionable paths. |
| `tools.release_evidence._write_release_baseline_guarded()` | Receives the canonical repository baseline path after relative/absolute path resolution, reads and retains original tracked bytes, validates both static baseline envelopes and the constrained refresh, then writes only an accepted candidate. |
| `tests/test_release_evidence.py` | Covers missing/invalid offline state, wrong uv, structural Taskfile dependency/command ordering, relative protected-path routing, non-baseline generic routing, permitted observation/count refresh, prohibited durable drift, and rejected-write byte preservation. |
| `evidence/release-baseline.json` | Regenerated once through the guarded path and preserved unchanged by all subsequent read-only proof commands. |

No new public `fast_fsm` symbol, command-line mode, dependency, lockfile entry, release threshold, or FSM behavior is produced.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Truthful complexity, equivalent installed artifacts, and FSM-owned drone routing | 25-01 through 25-05 | COVERED | Plans 01-04 implement and verify the goal; 25-05 makes the release proof itself fail closed. |
| REQ | PERF-01 | O(1) lookup/singleton and local O(k) group work | 25-01, 25-03 | COVERED | Already verified; this plan changes no runtime or complexity claim. |
| REQ | PERF-02 | Equivalent clean source/pure/compiled behavior and compiled singleton floor | 25-02, 25-04, 25-05 | COVERED | This plan closes the three open security controls on the durable proof path. |
| REQ | DOC-01 | Controller-owned one-tick drone guidance | 25-03 | COVERED | Already verified and unchanged. |
| RESEARCH | — | Pitfall 5: baseline regeneration must use reviewed uv 0.12.6 and accept only intended evidence drift | 25-05 | COVERED | Environment and diff review become executable fail-closed controls rather than operator convention. |
| RESEARCH | — | No new packages; reuse existing release tooling and Taskfile seams | 25-05 | COVERED | The plan modifies only repository code/tests/tasks/evidence and leaves uv.lock unchanged. |
| CONTEXT | D-01 | Truthful O(1)/local O(k) contract | 25-01, 25-03 | COVERED | Preserved without runtime changes. |
| CONTEXT | D-02 | Three-origin conformance and reviewed compiled performance proof | 25-02, 25-04, 25-05 | COVERED | The same proof now requires exact offline toolchain identity and guarded baseline regeneration. |
| CONTEXT | D-03 | Controller owns telemetry, FSM, and aircraft adapter | 25-03 | COVERED | Preserved without example changes. |
| CONTEXT | D-04 | One telemetry tick; fact-only guards; post-commit commands | 25-03 | COVERED | Preserved without example or dispatch changes. |

Deferred dynamic priorities, classifiers, schedulers, parallel guards, and runtime candidate mutation remain excluded.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce reviewed offline execution and guarded tracked-baseline refresh</name>
  <files>Taskfile.yml, tools/release_evidence.py, tests/test_release_evidence.py</files>
  <read_first>
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-SECURITY.md` open T-25-G01, T-25-G02, and T-25-SC controls
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-04-PLAN.md` manual pinned-environment, pre-write copy, stable-projection, and raw-diff contract that must become enforceable
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-04-SUMMARY.md` successful reviewed offline proof and the exact commands that currently depend on operator discipline
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-CONTEXT.md` D-02 artifact and performance proof boundary
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-RESEARCH.md` Pitfall 5, reviewed uv path, and no-new-package constraint
    - `.github/copilot-instructions.md` uv-only, locked, non-destructive, explicit-write/read-only-check rules
    - `Taskfile.yml` baseline, release-gate, installed-artifact, installed-performance, and readiness entry points
    - `tools/release_evidence.py` `REQUIRED_UV_VERSION`, `_command_environment`, `_resolved_uv_version`, `_stable_manifest`, `collect_manifest`, `_read_manifest`, `_write_manifest`, `write_or_check_manifest`, parser, and `main`
    - `tests/test_release_evidence.py` manifest fixtures/comparison/write tests and parsed Taskfile contract helpers
    - `evidence/release-baseline.json` current static envelope and five refreshable field paths
  </read_first>
  <behavior>
    - "Missing UV_OFFLINE, empty UV_OFFLINE, and values such as 0 or true fail before any uv/source/test/build subprocess; only the exact string 1 reaches uv identity validation."
    - "An offline environment using any uv version other than 0.12.6 fails before evidence collection; the valid environment performs the existing source-first collection flow."
    - "The tracked baseline accepts simultaneous changes to collected/passed plus the three generator-owned observation paths, but rejects toolchain, coverage, failure/skip count, conformance, threshold, schema, slot-policy, or any other durable/raw drift."
    - "Every rejection occurs before the tracked path is written and preserves its exact original bytes."
    - "The relative protected path evidence/release-baseline.json and its equivalent absolute spelling are resolved before writer selection and both route through _write_release_baseline_guarded; a resolved non-baseline temporary path retains the generic write_or_check_manifest path."
    - "Every relevant Taskfile entry point has release-proof-environment-check as its sole direct dependency; that gate checks UV_OFFLINE, checks the resolved uv version, and only then invokes pure-source-check, so no sibling dependency can race a uv command ahead of validation."
  </behavior>
  <action>Start with failing tests in `tests/test_release_evidence.py`. Add parametrized environment tests that pass missing, empty, `0`, and `true` values to the private guard, record `_run_checked` calls, and prove no subprocess is reached; add a valid-`1` wrong-version case and a valid exact-version case. Extend the collection-order test so `_require_release_proof_environment` is the first operation before `_source_preflight`. Add a structural parsed-Taskfile validator covering exactly `release-baseline-write`, `release-baseline-check`, `release-gate`, `release-installed-artifacts-check`, `release-installed-performance-check`, and `release-readiness-check`. For each entry point, assert its dependency list contains exactly `release-proof-environment-check` and not `pure-source-check` or a sibling dependency. Assert the gate itself has no dependency that can execute first and that its ordered commands test exact `UV_OFFLINE=1`, then call `uv --version` and require semantic `0.12.6`, then invoke `pure-source-check`; reject mutations that swap this command order, move `pure-source-check` into gate dependencies, restore `pure-source-check` as a release-task dependency, add a racing sibling dependency, remove the gate, accept a non-`1` offline value, or weaken the version match. Add routing tests that invoke the existing evidence write selection with the relative Taskfile value `evidence/release-baseline.json` and an equivalent absolute path, spy on `_write_release_baseline_guarded`, and prove both resolved spellings select it; invoke a resolved non-baseline temporary manifest and prove it continues through generic `write_or_check_manifest`. Add guarded-write tests at the canonical protected path: one candidate may change only the two test counts and three generator-owned observation paths; parametrized candidates changing `toolchain.uv`, coverage, failures/skips, conformance, thresholds, schema/static envelope, slots data, or another raw field must raise `EvidenceError` with the differing path and leave the original bytes unchanged.

In `tools/release_evidence.py`, retain `REQUIRED_UV_VERSION = "0.12.6"` as the single version authority and add `_require_release_proof_environment(*, environment)` that accepts only `environment.get("UV_OFFLINE") == "1"` before calling `_resolved_uv_version`. Call it in `collect_manifest()` immediately after `_command_environment()` and before `_source_preflight()`, so the `evidence --write` and `evidence --check` flows revalidate their inherited environment before any collection child. Define one private repository-root-derived tracked baseline path and an exact five-path refresh allowlist. Before selecting the evidence writer, expand the supplied manifest path, anchor it to the repository root when it is relative, preserve it when already absolute, and call `resolve()` before comparing it with the resolved repository baseline; do not compare raw strings or unresolved `Path` objects. Route a resolved match—including the Taskfile's relative `evidence/release-baseline.json` value—through `_write_release_baseline_guarded()`, pass the canonical protected path to it, and leave resolved non-baseline paths on generic `write_or_check_manifest()`. Implement `_validate_release_baseline_refresh(snapshot, candidate)` by validating both static envelopes, comparing `_stable_manifest()` projections after excluding only collected/passed, and comparing full raw projections after excluding only collected/passed plus the three complete observation paths; raise field-level `EvidenceError` output for either stable or raw drift. Implement `_write_release_baseline_guarded()` so it reads and retains the existing bytes, parses the snapshot, validates performance and the constrained candidate before writing, and never touches the path on a validation error. Keep `--check` read-only and do not alter generic custom-path serializer/checker semantics.

In `Taskfile.yml`, add `release-proof-environment-check` as an ordered internal preflight with no `deps`: its first command requires caller-provided `UV_OFFLINE` to equal `1`, its next check captures `uv --version` and accepts only semantic version `0.12.6`, and only its subsequent command invokes `pure-source-check`. Replace `release-installed-artifacts-check`'s current `pure-source-check` dependency and give each of the other five exact release entry points the same sole direct dependency on `release-proof-environment-check`. Do not list both tasks as dependencies: Task dependency scheduling may be concurrent, so pure-source verification must be serialized inside the gate after environment/version validation. Do not set UV_OFFLINE inside the Taskfile, because missing or invalid caller state must be observable as a failure, and do not change the existing locked sync/build/conformance/performance commands or thresholds. Run Ruff format/check on the two changed Python files, then the focused release-evidence test file. This closes T-25-G01, T-25-G02, and T-25-SC without adding an evidence CLI mode or public library API.</action>
  <verify>
    <automated>PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run ruff format --check tools/release_evidence.py tests/test_release_evidence.py &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run ruff check tools/release_evidence.py tests/test_release_evidence.py &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache uv run pytest tests/test_release_evidence.py -x -q</automated>
  </verify>
  <acceptance_criteria>`tests/test_release_evidence.py` proves invalid offline values fail before subprocess work, wrong uv fails before source collection, and all six release-proof tasks have the environment gate as their sole dependency. Structural tests prove the gate has no dependencies and orders the offline check, version check, and `pure-source-check` invocation without a concurrency race. Routing tests prove relative `evidence/release-baseline.json` and its absolute equivalent resolve to `_write_release_baseline_guarded`, while a resolved non-baseline temporary path keeps generic writer behavior. Only the five approved paths can differ during a protected refresh, and every prohibited drift preserves the original bytes. `Taskfile.yml`, `tools/release_evidence.py`, and the focused tests pass Ruff and pytest under reviewed uv 0.12.6 with `UV_OFFLINE=1`.</acceptance_criteria>
  <done>The repository—not operator convention—now enforces the exact reviewed offline execution context and validates a constrained tracked-baseline candidate before changing evidence bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and re-prove release evidence through the guarded path</name>
  <precondition>The reviewed executable `/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin/uv` exists, reports `uv 0.12.6`, and the existing cache directory `/private/tmp/fast-fsm-phase23-uv-cache` is readable; `git diff --quiet -- evidence/release-baseline.json` succeeds before regeneration.</precondition>
  <files>evidence/release-baseline.json</files>
  <read_first>
    - `Taskfile.yml` completed environment gate and the six guarded release-proof tasks
    - `tools/release_evidence.py` completed environment, projection, and tracked-write helpers
    - `tests/test_release_evidence.py` adversarial contracts from Task 1
    - `.planning/phases/25-performance-artifact-proof-drone-guidance/25-SECURITY.md` three threats this proof must close
    - `.github/copilot-instructions.md` release evidence regeneration and read-only verification rules
    - `evidence/release-baseline.json` pre-write snapshot, whose 1,805 collected / 1,799 passed values describe only the inventory before Task 1 adds tests, plus the durable artifact/toolchain contract
  </read_first>
  <action>Verify the reviewed binary directly, prepend only its directory to `PATH`, and export `UV_OFFLINE=1` plus `UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache` for the entire task. Save the tracked baseline bytes to `/private/tmp/fast-fsm-phase25-security-baseline-before.json` and record that snapshot's 1,805 collected / 1,799 passed values strictly as the pre-Task-1 inventory, then run `task release-baseline-write` exactly once. Confirm from verbose Task output that its sole dependency executed the offline check, version check, and `pure-source-check` in that order before the task's locked sync. The Taskfile passes the relative manifest value `evidence/release-baseline.json`; require path canonicalization to recognize it as the protected repository baseline and require the private guarded writer to accept the candidate only after the raw/stable allowlist comparison. Do not hand-edit JSON, prescribe post-write collected/passed constants, or bypass the helper. Run `git diff --check` and inspect the actual before/after manifest diff. Require the final `quality_baseline.tests.collected` and `quality_baseline.tests.passed` values to come solely from guarded collection and to be validated by the subsequent full release gate; require errors to remain 0, failed to remain 0, skips to remain 6, `toolchain.uv` to remain 0.12.6, and all semantic changes to be confined to `quality_baseline.tests.collected`, `quality_baseline.tests.passed`, `measurement_environment`, `performance_contract.observation`, and `pure_source_performance.observations`, with every other stable field equal. Copy the accepted bytes to `/private/tmp/fast-fsm-phase25-security-baseline-approved.json`. Then run `task release-baseline-check`, `task release-gate`, `task release-installed-artifacts-check`, and `task release-installed-performance-check` under the same environment; require every command to pass and compare the tracked manifest to the approved copy after each read-only proof. After all blocking release-evidence, artifact, performance, byte-preservation, and diff checks pass, run `task typecheck-ty` under the same reviewed offline environment and record its independently visible advisory result; a ty diagnostic does not weaken, replace, or short-circuit any blocking proof. Treat missing offline cache inputs, a toolchain mismatch, source/native shadow, explicit compiled build failure, artifact/conformance mismatch, quality failure, or performance below 200,000 ops/sec as blocking. Do not retry with network access or ambient uv, weaken validation, modify uv.lock/pyproject/setup metadata, change runtime FSM code, or alter release thresholds.</action>
  <verify>
    <automated>test -x /private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin/uv &amp;&amp; /private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin/uv --version | grep -Eq '^uv 0[.]12[.]6([[:space:]]|$)' &amp;&amp; test -r /private/tmp/fast-fsm-phase23-uv-cache &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache task release-baseline-check &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache task release-gate &amp;&amp; cmp evidence/release-baseline.json /private/tmp/fast-fsm-phase25-security-baseline-approved.json &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache task release-installed-artifacts-check &amp;&amp; cmp evidence/release-baseline.json /private/tmp/fast-fsm-phase25-security-baseline-approved.json &amp;&amp; PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache task release-installed-performance-check &amp;&amp; cmp evidence/release-baseline.json /private/tmp/fast-fsm-phase25-security-baseline-approved.json &amp;&amp; git diff --check -- Taskfile.yml tools/release_evidence.py tests/test_release_evidence.py evidence/release-baseline.json &amp;&amp; { PATH=/private/tmp/fast-fsm-uv-0.12.6/uv-aarch64-apple-darwin:$PATH UV_OFFLINE=1 UV_CACHE_DIR=/private/tmp/fast-fsm-phase23-uv-cache task typecheck-ty || printf '%s\n' 'ADVISORY: task typecheck-ty reported diagnostics'; }</automated>
  </verify>
  <acceptance_criteria>The guarded writer runs once under caller-provided `UV_OFFLINE=1` and reviewed uv 0.12.6 after the serialized preflight checks environment, version, and pure source in that order. The Taskfile's relative manifest path resolves to the protected writer. Final collected/passed values are the guarded collection's truthful post-test inventory—not the pre-write 1,805/1,799 snapshot—and the full release gate validates them with 0 errors, 0 failed, and 6 skipped. The before/after diff changes only `quality_baseline.tests.collected`, `quality_baseline.tests.passed`, and the three generator-owned observation paths; the durable toolchain/artifact/threshold contract and every other stable field remain equal. The read-only baseline check, clean-source/pure/compiled conformance proof, and installed native three-sample median gate all pass without changing approved manifest bytes. After those blocking checks pass, `task typecheck-ty` runs separately and its advisory result is visible in the execution record.</acceptance_criteria>
  <done>Phase 25 ends with regenerated durable evidence whose creation and every relevant local proof are mechanically bound to the reviewed offline toolchain and constrained manifest delta.</done>
</task>

</tasks>

<threat_model>
ASVS Level: 1 (opportunistic controls on release-evidence generation and artifact-proof trust boundaries)

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Caller environment → release Taskfile | Ambient PATH, uv identity, and offline intent cross into tasks capable of resolving, building, testing, and writing evidence. |
| Generated candidate → tracked release baseline | Fresh measurements and test counts cross into a durable manifest whose unrelated fields must remain authoritative. |
| Checkout/cache → source and installed artifact proof | Local source, cached lockfile inputs, and freshly built wheel origins must not be substituted or network-refreshed during proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-25-G01 | Tampering | baseline writer and verification children | high | mitigate | Task 1 makes a no-dependency preflight the sole dependency of all six proof entry points and serializes exact UV_OFFLINE, uv 0.12.6, then pure-source checks in its command list; structural tests reject racing siblings and reordered checks. The evidence tool independently revalidates before collection. |
| T-25-G02 | Tampering / Repudiation | tracked baseline regeneration | high | mitigate | Task 1 resolves the requested path before protected-baseline selection, routes relative/absolute protected spellings to the guarded writer, snapshots original bytes, validates static envelopes, compares raw and stable projections with an exact five-path allowlist, and writes only after acceptance; routing and adversarial tests prove generic temp behavior and rejected-drift non-mutation. |
| T-25-SC | Tampering | uv.lock-pinned cached inputs | high | mitigate | Tasks 1-2 require the reviewed executable and offline-only existing cache, install no package, and leave uv.lock/dependency metadata unchanged. |
| T-25-G03 | Tampering | source/pure/compiled artifact provenance | high | mitigate | Task 2 reruns the existing archive, origin, loader, conformance, and installed native performance checks without weakening their thresholds. |
| T-25-G04 | Denial of Service | offline cache availability | medium | accept | An absent reviewed cache input blocks proof; this availability cost is preferable to silently granting network resolution authority. |
| T-25-06 | Denial of Service | benchmark variance | low | accept | Environment-labelled grouped timings remain observations; only the installed compiled singleton median is a release threshold. |
</threat_model>

<verification>
1. Run focused tests that fail for missing/non-`1` offline state, wrong uv, any Taskfile dependency/command ordering that can race proof work ahead of the gate, unresolved protected-path routing, prohibited stable/raw drift, and any rejected write that changes original bytes.
2. Prove valid exact offline/toolchain state reaches the existing source-first evidence collection while custom-path manifest behavior remains unchanged.
3. Preserve the 1,805/1,799 manifest as the pre-change snapshot, regenerate the tracked baseline once through guarded `release-baseline-write`, and inspect the actual diff: final collected/passed values must come from collection, errors/failed remain zero, skips remain six, and only the two count paths plus the three generator-owned observation paths may change.
4. Run the read-only baseline freshness check, full release quality gate, source/pure/compiled installed parity proof, and compiled singleton performance proof under the same reviewed offline environment.
5. Confirm read-only commands preserve approved manifest bytes and no dependency, lockfile, public API, runtime FSM, or threshold change exists.
</verification>

<success_criteria>
- T-25-G01, T-25-G02, and T-25-SC have executable mitigations and adversarial tests suitable for a follow-up security audit.
- Missing/non-`1` UV_OFFLINE and non-0.12.6 uv fail before `pure-source-check` or any release evidence Taskfile proof child; structural tests prove the environment gate is the sole dependency and its command order is serialized.
- Relative and absolute spellings of the resolved repository baseline route through the guarded writer, a resolved non-baseline temporary path retains generic behavior, and the protected writer permits changes at exactly five paths while leaving original bytes unchanged for every other difference.
- The pre-change 1,805/1,799 counts are never asserted as final values; regenerated collected/passed counts are accepted only from the guarded writer and validated by the full release gate with 0 errors, 0 failed, and 6 skipped.
- `task release-baseline-check`, `task release-gate`, `task release-installed-artifacts-check`, and `task release-installed-performance-check` pass with reviewed uv 0.12.6, `UV_OFFLINE=1`, and no network fallback.
- After all blocking release-proof checks pass, `task typecheck-ty` runs under the same reviewed offline environment and its advisory result remains visible without changing blocking gate semantics.
- `write_or_check_manifest` custom-path behavior remains unchanged; no second user-facing API, package dependency, lockfile change, runtime FSM change, or threshold change is introduced.
</success_criteria>

<output>
Create `.planning/phases/25-performance-artifact-proof-drone-guidance/25-05-SUMMARY.md` when done.
</output>
