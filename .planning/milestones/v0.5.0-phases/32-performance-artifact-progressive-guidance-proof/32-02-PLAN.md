---
phase: 32-performance-artifact-progressive-guidance-proof
plan: 02
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-03
  - 32-05
files_modified:
  - tools/release_evidence.py
  - tests/test_installed_artifacts.py
  - tests/test_release_evidence.py
  - evidence/release-baseline.json
autonomous: true
requirements:
  - PERF-04
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PERF-04/D-04: the Plan 01 fixed oracle matches clean pure source, a freshly built native core, installed pure and compiled wheels, and locally built release-intent artifacts only after exact origin/build-mode proof."
    - "D-03: baseline writes are deliberate and diff-reviewed; `uv.lock` and normal locked sync govern dependencies without patch, offline, or cache requirements."
    - "D-05: competitor 2.5.0/3.2.1 lanes remain exact, manual/scheduled, semantically preflighted, and non-gating for ordinary CI."
    - "A local candidate result is labelled non-authorizing and cannot be mistaken for a published release."
  artifacts:
    - path: tests/test_installed_artifacts.py
      provides: "Exact-origin five-mode fixed-oracle parity, including a fresh isolated native build"
    - path: tests/test_release_evidence.py
      provides: "Origin/shadow and local-release-scope regression checks"
    - path: evidence/release-baseline.json
      provides: "Reviewed refreshed source/semantic/test inventory evidence"
  key_links:
    - from: tools/artifact_conformance.py
      to: tests/test_installed_artifacts.py
      via: "One Plan 01 semantic digest and required-value inventory compared in all modes"
      pattern: "compare_conformance"
    - from: Taskfile.yml
      to: evidence/release-baseline.json
      via: "Explicit write, reviewed diff, then read-only check"
      pattern: "release-baseline-write|release-baseline-check"
  flagged_assumptions: []
  prohibitions:
    - statement: "Must not present a local unreleased candidate or an older published artifact as proof that v0.5.0 was published."
      status: flagged-unverified
    - statement: "Must not turn environment-specific competitor observations into an unqualified product speed claim."
      status: flagged-unverified
---

<objective>
Close exact-origin artifact parity and reviewed evidence freshness after the strict semantic oracle is complete.

Purpose: Users need the same final/mode/rejection behavior from each supported runtime form, without mistaking local candidates for an authorized release.
Output: Fresh source/native/wheel/release-intent mode assertions and a deliberate, reviewed release-baseline update.
</objective>

## Artifacts this phase produces

- A single-test-path pure source, fresh native, installed pure wheel, installed compiled wheel, and local release-candidate matrix using the existing collector and verifier.
- Fail-closed origin/build-mode and recoverable native-shadow regression evidence.
- A reviewed `evidence/release-baseline.json` that CI checks read-only.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-RESEARCH.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-PATTERNS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-01-SUMMARY.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-03-SUMMARY.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-05-SUMMARY.md

<interfaces>
`release_evidence.verify_source()` fails before importing a shadowed pure core; `verify_installed_wheel(artifact, expected_mode, build_intent)` validates archive, installed origin/loader, conformance, and the compiled-only floor. `task release-evidence-local-check` builds local candidate wheels/sdist and returns `scope=local-non-authorizing`, `authorizes_release=false`; `task release-baseline-write` is an explicit write and `task release-baseline-check` is read-only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assert the same strict oracle in all fresh local runtime modes</name>
  <files>tests/test_installed_artifacts.py, tests/test_release_evidence.py</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-03 through D-05
    - `tests/test_installed_artifacts.py` existing clean-source, pure/compiled wheel fixtures and exact-origin assertions
    - `tests/test_release_evidence.py` source-shadow and local matrix tests
    - `tools/release_evidence.py` `verify_source`, `verify_installed_wheel`, local matrix aggregation, native floor checks
    - `tools/artifact_conformance.py` Plan 01 scenario inventory and `compare_conformance`
  </read_first>
  <behavior>
    - Pure source, a fresh native extension, installed pure wheel, installed compiled wheel, and local release-intent candidates all validate the same fixed four new rows.
    - Every accepted mode has the expected exact core loader/origin and build intent; contradiction stops before comparing semantics.
    - A generated native shadow in an isolated source copy is moved only between validated paths in a temporary backup and restored in `finally`; the working checkout is never silently cleaned.
    - The local matrix reports non-authorizing scope, and a stale or altered archive cannot pass as a current artifact.
  </behavior>
  <action>Per D-04, extend the existing integration fixture to build the native extension from a verified clean source copy in an isolated temporary directory, run the installed collector in a neutral child with exact expected source/native origins, and compare all mode records to the same Plan 01 source oracle. Reuse existing wheel fixture/verifier and local release-intent matrix instead of a new install harness. For any in-place native shadow in the temporary copy, validate its resolved path is strictly within that copy, move it to a recoverable temporary backup for a pure rerun, and restore it in `finally`; do not alter user checkout shadows. Add fast synthetic tests in `tests/test_release_evidence.py` named `test_phase32_origin_*`, `test_phase32_shadow_*`, and `test_phase32_local_scope_*` for contradictory mode/origin and local-scope facts; the actual multi-mode build runs at the Wave 3 gate. Keep the two exact `python-statemachine` versions in existing observational lanes per D-05; do not install them in required CI.</action>
  <verify><automated>uv run pytest tests/test_release_evidence.py -k 'phase32_origin or phase32_shadow or phase32_local_scope' -x -q</automated></verify>
  <acceptance_criteria>All five runtime forms have matching new rows after exact origin/mode checks; intentional origin or archive contradictions fail; no working-checkout shadow is deleted or moved.</acceptance_criteria>
  <done>PERF-04/D-04 parity is asserted against the strict oracle rather than version labels or printed output.</done>
</task>

<task type="auto">
  <name>Task 2: Review and refresh the release evidence without changing policy</name>
  <reversibility rating="costly">D-03 governs the shared manifest/CI evidence contract; an accidental automatic rewrite would change how maintainers authorize releases.</reversibility>
  <files>evidence/release-baseline.json</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-01, D-03, D-04, D-05
    - `evidence/release-baseline.json` current schema, source origin and semantic inventory
    - `Taskfile.yml` `pure-source-check`, `release-baseline-write`, `release-baseline-check`, `release-evidence-local-check`, and `release-installed-performance-check`
    - `.github/copilot-instructions.md` release-evidence write/check and uv.lock policy
  </read_first>
  <action>Per D-03, run normal `uv sync --locked --all-groups`, the non-destructive pure-source preflight, and the explicit baseline write only after the expanded oracle tests pass. Review `git diff -- evidence/release-baseline.json` before retaining the write: allowed changes are current test/coverage/tool/origin observations and the expanded semantic inventory/digest, not an unexplained policy or prior-version artifact substitution. Run the read-only baseline check and local release-intent matrix; assert `local-non-authorizing` and `authorizes_release=false`. Per D-01, retain the separate fresh installed compiled ≥200,000/s gate unchanged; per D-05, run the existing comparator-contract tests but leave manual/scheduled competitor timing outside ordinary CI. Do not create a tag, Release, or package publication.</action>
  <verify><automated>uv run pytest tests/test_release_evidence.py -k 'manifest_freshness or guarded_release_baseline' -x -q</automated></verify>
  <acceptance_criteria>Manifest diff is explicitly reviewed and retained, its read-only check passes with normal locked uv, local release evidence is labelled non-authorizing, and installed compiled performance still uses the existing floor.</acceptance_criteria>
  <done>PERF-04/D-03/D-05 release evidence is fresh and truthful without changing ordinary CI or publication status.</done>
</task>

</tasks>

## Execution deviation

The tracked baseline's guarded writer predates the Phase 32 semantic-oracle and
slots-inventory additions. Task 2 may therefore make a narrow, fail-closed
update to `tools/release_evidence.py`: it may accept only a regenerated current
conformance contract, regenerated current slots-policy inventory fields, and a
non-decreasing `quality_baseline.coverage.core_percent`. The coverage exception
may only strengthen the stored floor; total coverage and every unrelated
manifest path remain immutable. The writer must reject a regression, arbitrary
values, or unrelated manifest changes before writing.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local source/native/wheel artifacts → evidence | Origin and build intent can be spoofed by stale shadows or swapped archives. |
| Local candidate record → release claim | A non-authorizing run must not be described as a hosted published release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-04 | Spoofing | Native source or installed loader | high | mitigate | Verify exact module path, extension suffix, package containment, and requested build mode before semantic comparison. |
| T-32-05 | Tampering | Wheel/sdist archive and evidence manifest | high | mitigate | Reuse archive snapshot/hash checks; review baseline diff; run read-only freshness check. |
| T-32-06 | Repudiation | Local release-candidate record | medium | mitigate | Assert `local-non-authorizing` scope and preserve origin/build/tool labels; do not report publication. |
| T-32-07 | Denial of service | Build/probe subprocess | medium | mitigate | Reuse existing bounded neutral child timeouts and output caps. |
</threat_model>

<verification>
After each task, run its fast focused unit checks. At the Wave 3/phase gate, run the full `uv run pytest tests/test_installed_artifacts.py -x -q` matrix, `task release-installed-artifacts-check`, `task release-installed-performance-check`, `task release-evidence-local-check`, `task release-baseline-check`, and `uv run pytest tests/test_competitor_benchmark_contract.py -x -q`. Record exact modes/origins and the manifest diff in the summary. The hosted release matrix remains a later publication gate, not a local-pass claim.
</verification>

<success_criteria>
No mode is accepted on semantic agreement alone; the complete local artifact matrix proves the same strict oracle with exact provenance, and the tracked baseline is deliberately refreshed and read-only checked.
</success_criteria>

<output>
Create `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-02-SUMMARY.md` when done.
</output>
