---
phase: 32-performance-artifact-progressive-guidance-proof
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - benchmarks/performance_demo.py
  - tests/test_performance_benchmarks.py
  - Taskfile.yml
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PERF-01/D-01: fresh installed compiled unfeatured singleton `trigger()` remains direct O(1) and has a median of at least 200,000 transitions/second; no source-tree timing replaces the gate."
    - "PERF-02/D-01: unused final/mode/rejection semantics cause no unrelated topology scan, reflection, or added per-dispatch diagnostic allocation."
    - "PERF-03/D-02: separate final-entry, internal-self, external-self, and expected-rejection observations identify exact runtime/build origin, Python/uv/library versions, platform, explicit environment label, method, samples, iterations, and local work shape."
    - "Timing rows are descriptive observations; deterministic lookup and guard-count tests establish topology independence."
  artifacts:
    - path: benchmarks/performance_demo.py
      provides: "Four separately named, reachable semantic-cost reporters beside the existing priority observations"
    - path: tests/test_performance_benchmarks.py
      provides: "Direct-path, topology-independence, reporter schema, and feature-local work regressions"
    - path: src/fast_fsm/core.py
      provides: "Existing direct selector preserved, with only a measured regression repair if tests expose one"
    - path: Taskfile.yml
      provides: "Existing benchmark entry points pass explicit accurate environment labels to the reporter"
  key_links:
    - from: src/fast_fsm/core.py
      to: tests/test_performance_benchmarks.py
      via: "Two direct source/trigger lookups and local candidate work verified independently of elapsed time"
      pattern: "_transitions.get|entries.get"
    - from: benchmarks/performance_demo.py
      to: tests/test_performance_benchmarks.py
      via: "Scenario inventory, median and environment/origin labels asserted without a fabricated speed ratio"
      pattern: "collect_.*observations"
    - from: Taskfile.yml
      to: benchmarks/performance_demo.py
      via: "Both benchmark tasks supply a named environment; compiled task also asserts native core mode"
      pattern: "environment-label|expected-core-mode"
  flagged_assumptions:
    - id: PERF-01
      status: flagged-unresolved
      reason: "Spec-less edge probe classified the requirement applicable but unclassified; the existing fresh installed floor and deterministic direct-lookup tests are the planned proof."
    - id: PERF-02
      status: flagged-unresolved
      reason: "Spec-less edge probe classified the requirement applicable but unclassified; structural and counted-work tests must explicitly verify the no-feature path."
    - id: PERF-03
      status: flagged-unresolved
      reason: "Spec-less edge probe classified the requirement applicable but unclassified; each separately labelled scenario and metadata field receives a concrete test."
  prohibitions:
    - statement: "Must not present local, environment-specific feature timings as a universal speedup or hardware-independent performance guarantee."
      status: flagged-unverified
---

<objective>
Protect the unfeatured direct singleton dispatch and expose separately labelled costs for the new optional semantics.

Purpose: Fast FSM's reason to exist is a measured installed native floor and local work, not a broad unqualified benchmark claim.
Output: Direct-path regression proof and an environment-labelled descriptive scenario reporter.
</objective>

## Artifacts this phase produces

- `collect_semantic_observations(...)` in the existing benchmark module, with final-entry, internal-self, external-self, and expected-rejection scenario names and median samples; `main()` accepts `--environment-label` and emits all four `SEMANTIC_OBSERVATION` rows.
- Existing `task benchmark` and `task benchmark-compiled` invocations supply explicit labels; compiled mode fails if its asserted native core origin is not loaded.
- Structural and counted-work tests proving direct singleton and local candidate work independent of unrelated topology.
- A retained `200_000` fresh installed compiled median gate, with any needed measured direct-path repair confined to the existing selector/TRACE seam.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-RESEARCH.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-PATTERNS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-VALIDATION.md

<interfaces>
`StateMachine` currently resolves `_transitions.get(current_name)` then `entries.get(trigger)`; only `_TransitionGroup` enters ranked iteration. `collect_priority_group_observations()` and `_runtime_labels()` already provide descriptive median rows. `tools/release_evidence.py` separately validates a fresh installed compiled three-sample median against `_COMPILED_TRIGGER_OPS_PER_SECOND_MIN = 200_000`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the unfeatured singleton work shape and native floor</name>
  <reversibility rating="costly">D-01 is the library's public speed identity; weakening the native floor or adding hot-path work would require a coordinated product-contract correction.</reversibility>
  <files>tests/test_performance_benchmarks.py, src/fast_fsm/core.py</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-01 and `.planning/REQUIREMENTS.md` PERF-01/PERF-02
    - `tests/test_performance_benchmarks.py` direct singleton, AST, topology-scaling and guard-rank tests
    - `src/fast_fsm/core.py` synchronous and async selector, trigger entry, disabled TRACE fast path
    - `tools/release_evidence.py` native installed median validator and fixed floor
  </read_first>
  <behavior>
    - Direct singleton dispatch performs the same two source/trigger mapping lookups for small and large unrelated topology.
    - Disabled diagnostics do not construct trace metadata, inspect result semantics, or invoke a trace collector on the no-feature path; a targeted AST assertion proves semantic trace preparation is dominated by the TRACE-enabled branch.
    - Registered candidate work stops at the selected local rank; no unrelated group is scanned.
  </behavior>
  <action>Per D-01, extend structural and counted-work tests in `tests/test_performance_benchmarks.py` for the no-feature singleton, disabled TRACE, and unrelated-topology independence in pure and fresh native modes. Add a targeted AST regression asserting both `trigger` and `trigger_async` prepare semantic TRACE arguments and call the trace collector only under the `logger.isEnabledFor(TRACE)` branch, plus a disabled-TRACE sentinel that fails if that collector executes. This targets incremental diagnostic allocation without asserting zero total allocation: `TransitionResult` is still legitimately created. Check singleton selection for reflection helpers and unrelated registry iteration. Preserve the existing `200_000` constant and installed-native three-sample validator. Change `src/fast_fsm/core.py` only if these tests expose an actual regression, confining a repair to the direct selector or disabled tracing boundary; keep `__slots__` and O(1) lookup intact.</action>
  <verify><automated>uv run pytest tests/test_performance_benchmarks.py -k 'singleton or constant_lookup or priority_group_work or trace' -x -q</automated></verify>
  <acceptance_criteria>Direct lookup count, targeted AST dominance and disabled-TRACE sentinel, reflection absence, and local group work assertions pass independent of unrelated topology; the installed native gate remains 200,000/s and is run at the plan/phase gate.</acceptance_criteria>
  <done>PERF-01/PERF-02/D-01 are protected by deterministic regressions without altering release threshold policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish separate descriptive feature-cost observations</name>
  <files>benchmarks/performance_demo.py, tests/test_performance_benchmarks.py, Taskfile.yml</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-02 and D-05
    - `benchmarks/performance_demo.py` existing `_runtime_labels`, reachable priority fixtures, `measure_time`, and CLI output
    - `Taskfile.yml` existing `benchmark` and `benchmark-compiled` commands and build ordering
    - `tests/test_performance_benchmarks.py` priority reporter schema and local-work test patterns
    - `tests/test_final_states.py`, `tests/test_transition_modes.py`, and `tests/test_expected_rejection.py` public semantic expectations
  </read_first>
  <behavior>
    - Each of four semantic costs has its own scenario row, finite positive median, and explicit environment/sample-method metadata.
    - The command line requires an explicit `--environment-label`, and `main()` emits exactly one `SEMANTIC_OBSERVATION` JSON line for each of the four semantic scenarios in addition to existing priority output.
    - `task benchmark` labels the active runtime without pretending it is pure or native; `task benchmark-compiled` labels the fresh compiled run and fails if the observed core loader is not native.
    - Final entry times only transition dispatch on a prebuilt batch of machines; per-sample construction occurs outside the timed region.
    - Internal/external self fixtures remain repeatedly reachable; rejected attempts remain uncommitted and repeatedly reachable.
    - Unrelated topology growth does not alter deterministic guard-evaluation counts or direct registry work.
  </behavior>
  <action>Per D-02, extend `benchmarks/performance_demo.py` with `collect_semantic_observations(environment_label, sample_count, iterations, ...)` and four separately named scenarios. Add argparse flags `--environment-label` (required, nonempty), `--sample-count`, `--iterations`, and optional `--expected-core-mode`; pass sample controls to both the existing priority reporter and the new semantic reporter. When `--expected-core-mode` is set, compare it to `_runtime_labels()["core_mode"]` and fail before timing on mismatch. Make `main()` emit one `SEMANTIC_OBSERVATION ` JSON line per semantic scenario, alongside its existing priority rows. Update `Taskfile.yml`: `benchmark` passes `--environment-label task-active-runtime` (neutral because it can load either origin); `benchmark-compiled`, after its existing clean/build sequence, passes `--environment-label task-compiled-build --expected-core-mode compiled-native` so its label cannot conceal a pure-source run. Report exact `core.__file__`/loader mode, distribution version, Python version/implementation, uv version when invoked, platform, method (`perf_counter_ns` and median), environment label, sample count, iterations, operation count, and ops/sec. Build final-entry batches outside the clock, keep self/rejection operations reachable without private state mutation, and record scenario-specific guard work. Compare at least two unrelated topology sizes in deterministic tests; reject nonfinite/nonpositive observations and changed expected outcomes. Add a fast CLI output test invoking one sample and iteration with a named environment, parsing all four marker-prefixed JSON rows and checking scenario inventory and label; add a Taskfile contract test for both named invocations and a CLI mismatch test for the expected mode. Keep feature timings non-gating, with no universal ratio claim and no instrumentation inserted into `trigger()`; D-05 competitor observations remain in their separate manual/scheduled lane.</action>
  <verify><automated>uv run pytest tests/test_performance_benchmarks.py -k 'semantic_observation' -x -q</automated></verify>
  <acceptance_criteria>`main()` accepts a required named environment and emits all four marker-prefixed semantic JSON rows; both existing Taskfile benchmark commands remain callable with honest labels, and the compiled command refuses a non-native loader. Fast CLI/Taskfile tests validate inventory, labels, provenance/method fields, mismatch failure, and finite positive medians; topology growth changes no counted local-work invariant.</acceptance_criteria>
  <done>PERF-03/D-02 yields reproducible environment-labelled observations without turning new-feature costs into the unfeatured native floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Benchmark output → published performance claim | Timing is environment-dependent and must not be promoted to a universal product promise. |
| Public dispatch → benchmark instrumentation | Evidence code must not add work to the production no-feature path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-08 | Repudiation | Descriptive timing row | medium | mitigate | Persist exact origin/build, tool versions, platform, explicit environment label, method and sample counts in every row. |
| T-32-09 | Tampering | Native floor policy | high | mitigate | Keep the existing fixed threshold/validator untouched and run fresh installed compiled proof separately from scenario reporter. |
| T-32-10 | Denial of service | Benchmark fixture topology | low | accept | Existing bounded sample/iteration and finite fixture sizes are maintainer-controlled; reporter tests reject nonpositive inputs. |
</threat_model>

<verification>
Run `uv run pytest tests/test_performance_benchmarks.py -x -q`, `task benchmark`, `task benchmark-compiled`, `task release-slots-check`, and `task release-installed-performance-check` at the plan/phase gate. The direct CLI smoke is `uv run python benchmarks/performance_demo.py --environment-label phase32-local --sample-count 1 --iterations 10`. Record actual loader mode/origin and installed native median; do not infer an asymptotic guarantee from elapsed time alone.
</verification>

<success_criteria>
The installed compiled singleton remains above the durable floor and structurally direct; each optional semantic has a separate labelled, non-gating cost record.
</success_criteria>

<output>
Create `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-03-SUMMARY.md` when done.
</output>
