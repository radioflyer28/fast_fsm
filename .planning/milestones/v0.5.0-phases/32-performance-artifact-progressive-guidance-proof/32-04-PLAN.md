---
phase: 32-performance-artifact-progressive-guidance-proof
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/drone_failsafes.py
  - tests/test_drone_failsafes_example.py
autonomous: true
requirements:
  - DOC-01
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DOC-01/D-06: one normalized telemetry sample causes exactly one `telemetry_tick`; the owned FSM, not controller branches or TelemetryPolicy, owns state-dependent guards and priority."
    - "DOC-01/D-07: internal mission updates commit without re-entry command; external mission self-reentry invokes its entry behavior once after commit."
    - "DOC-01/D-07: normal and emergency landing end in explicit final states, with no outgoing restart edge; a second flight constructs a new controller."
    - "DOC-01/D-08: ordinary false eligibility and terminal `TransitionRejected(code)` have distinct results, and neither issues an uncommitted aircraft command."
    - "The runnable simulation remains deterministic training software, never a real-hardware or flight-safety claim."
  artifacts:
    - path: examples/drone_failsafes.py
      provides: "Progressive controller-owned drone tutorial with final, self-mode, rejection and committed-command beats"
    - path: tests/test_drone_failsafes_example.py
      provides: "One-event, priority, lifecycle, finality, rejection and command-order smoke proof"
  key_links:
    - from: DroneController.update_from_telemetry
      to: StateMachine.trigger
      via: "One `telemetry_tick` carrying TelemetryPolicy facts"
      pattern: 'trigger\("telemetry_tick"'
    - from: FSMBuilder.on_enter
      to: AircraftCommands
      via: "Bound callbacks only on committed destination entry"
      pattern: "issue_aircraft_command"
  flagged_assumptions:
    - id: DOC-01
      status: flagged-unresolved
      reason: "Spec-less edge probe classified the requirement applicable but unclassified; tests explicitly cover one-tick ownership, both self modes, final landings, rejection, and post-commit commands."
  prohibitions:
    - statement: "Must not portray the deterministic simulation as certified flight-control, real-time safety software, or a direct real-aircraft integration."
      status: flagged-unverified
---

<objective>
Complete one progressive, deterministic controller-owned drone simulation that teaches the new flat-FSM semantics through observable aircraft commands.

Purpose: The FSM should own transition rules and precedence while the controller owns composition and the replaceable adapter owns effects.
Output: Updated runnable script and smoke tests covering the entire learning sequence.
</objective>

## Artifacts this phase produces

- Explicit `Landed` and `EmergencyLanded` final states and a new-controller-per-flight simulation sequence.
- Mission `telemetry_tick` internal and external self candidates, distinct from existing critical/link/battery failsafes.
- One bounded-code expected-rejection guard, false-guard contrast, and command/lifecycle assertions.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-RESEARCH.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-PATTERNS.md
@.planning/phases/32-performance-artifact-progressive-guidance-proof/32-VALIDATION.md

<interfaces>
`DroneController` owns `_fsm`, `_telemetry_policy`, `_aircraft`; `update_from_telemetry(sample)` calls policy `observe()` and one `trigger("telemetry_tick", telemetry_policy=...)`. `FSMBuilder.on_enter(state_name, callback)` already binds `issue_aircraft_command(...)` after commit. Lower exact integer priority wins. `State("name", final=True)` forbids outgoing construction. `internal=True` is valid only for an exact self-target; default self mode exits and re-enters.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teach final landings and both mission self-transition modes</name>
  <files>examples/drone_failsafes.py, tests/test_drone_failsafes_example.py</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-06 through D-08
    - `examples/drone_failsafes.py` `TelemetrySample`, `create_drone_fsm`, entry callbacks, `DroneController`, `simulated_telemetry`, and `main`
    - `tests/test_drone_failsafes_example.py` command-order, callback-count, one-tick, and priority smoke tests
    - `tests/test_final_states.py` and `tests/test_transition_modes.py` public final/mode contracts
  </read_first>
  <behavior>
    - Existing critical fault, lost link, and low battery keep priorities 0, 10, and 20 on the single telemetry event.
    - Healthy Mission telemetry can make an internal self commit; a distinct explicit re-entry fact chooses a higher-ranked external self candidate.
    - Internal self leaves mission entry-command count and residency timing unchanged; external self re-enters and issues the mission command once after commit.
    - Normal and emergency touchdowns reach final states; post-final trigger attempts do not restart flight or issue commands.
  </behavior>
  <action>Per D-06/D-07, add a fact-only telemetry flag such as `reenter_mission` and guarded Mission self candidates on `telemetry_tick` after the existing failsafes (for example priorities 50 external, 60 internal). The external candidate uses the default lifecycle; the internal one uses `internal=True` and must not be implemented as a no-op. Add explicit normal and emergency landing finals, remove the `Landed` restart edge, and make a second simulated flight create a fresh controller while retaining the replaceable `AircraftCommands` port. Keep priority and eligibility exclusively in `FSMBuilder` transition guards; no controller-side failsafe branch or timer/scheduler. Add fake-clock, callback-count, history/result, and command-at-committed-state tests first, then implement. Preserve the visible training-only disclaimer.</action>
  <verify><automated>uv run pytest tests/test_drone_failsafes_example.py -k 'mode or final or critical or telemetry_tick' -x -q</automated></verify>
  <acceptance_criteria>Both self modes have distinguishable lifecycle/command effects, both landing paths are truly final, the critical/link/battery precedence and one-tick input boundary remain intact.</acceptance_criteria>
  <done>DOC-01/D-06/D-07 has a progressive final/mode flight story with only committed aircraft effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contrast false guard fallthrough with expected rejection</name>
  <files>examples/drone_failsafes.py, tests/test_drone_failsafes_example.py</files>
  <read_first>
    - `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-CONTEXT.md` D-07/D-08
    - `examples/drone_failsafes.py` updated Mission candidate order and `_report`
    - `tests/test_drone_failsafes_example.py` command-order and ineligible-tick tests
    - `tests/test_expected_rejection.py` `TransitionRejected(code)` and result status/code contract
  </read_first>
  <behavior>
    - A benign false guard permits a later eligible Mission self candidate; an invalid navigation fact raises a stable `TransitionRejected("navigation-conflict")` and suppresses later candidates.
    - Critical/link/battery safety candidates outrank the demonstration rejection, so an active failsafe is not hidden by the tutorial policy case.
    - Rejection returns an uncommitted result with code, unchanged state/history/command log; an ordinary ineligible ReturnHome tick has no rejection code.
  </behavior>
  <action>Per D-07/D-08, add one normalized `navigation_conflict` fact and a Mission `telemetry_tick` guard placed after safety failsafes but before routine self candidates. It returns false when the fact is absent and raises public `TransitionRejected("navigation-conflict")` when present; its policy does not select a new event or destination. Update `_report` to print the bounded rejection status/code distinctly from ordinary no-transition and unexpected failure, without exposing arbitrary payload. Add deterministic tests for false fallthrough, terminal rejection without lower-candidate work, simultaneous safety-signal precedence, and absence of uncommitted adapter effects. Run the full script to ensure both flights and all teaching beats are visible without real-time assumptions.</action>
  <verify><automated>uv run pytest tests/test_drone_failsafes_example.py -x -q</automated></verify>
  <acceptance_criteria>One tick can visibly select a failsafe, external self, internal self, ordinary ineligibility, or terminal expected rejection according to FSM-owned rules, with exact committed command order.</acceptance_criteria>
  <done>DOC-01/D-08 example contrasts the relevant outcomes and remains deterministic, builder-first, and adapter-replaceable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Normalized telemetry facts → transition eligibility | Even a simulation should make precedence and rejected outcomes explicit. |
| Committed FSM entry → aircraft command port | Side effects must not occur for false/rejected/pre-commit paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-11 | Tampering | Telemetry policy/guard boundary | high | mitigate | One-event FSM-owned priority tests ensure critical/link/battery signals outrank tutorial rejection and routine self updates. |
| T-32-12 | Elevation of privilege | Aircraft command callback | high | mitigate | Bind commands only to destination entry and test state-at-command after commit; reject/false/internal paths issue no entry command. |
| T-32-13 | Information disclosure | Rejection report | medium | mitigate | Display only fixed bounded rejection code/status; assert no sample or arbitrary exception representation reaches output. |
| T-32-14 | Repudiation | Training simulation claim | medium | mitigate | Keep explicit non-hardware, non-certified disclaimer in script output and public guide. |
</threat_model>

<verification>
Run `uv run pytest tests/test_drone_failsafes_example.py -x -q` and `uv run python examples/drone_failsafes.py`; inspect the deterministic transcript for distinct internal, external, final, false, and rejected beats.
</verification>

<success_criteria>
The drone controller feeds one telemetry event per sample, the FSM owns all competing transitions, and only a committed entry invokes the simulated aircraft adapter.
</success_criteria>

<output>
Create `.planning/phases/32-performance-artifact-progressive-guidance-proof/32-04-SUMMARY.md` when done.
</output>
