---
phase: 29-expected-domain-rejection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - src/fast_fsm/core.pyi
  - src/fast_fsm/__init__.py
  - tests/test_expected_rejection.py
  - tests/test_mypyc_guard.py
  - tests/test_release_evidence.py
  - tools/release_evidence.py
  - .github/copilot-instructions.md
  - .specify/memory/spr-core-api.md
autonomous: true
requirements:
  - REJECT-01
  - REJECT-02
  - REJECT-03
  - REJECT-04
  - REJECT-05
  - REJECT-07
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "REJECT-01 / D-01 / D-02 / D-03: users can construct public TransitionRejected(code) only from an exact built-in str matching [a-z][a-z0-9_.-]* at length 1..64, with eager TypeError/ValueError rejection and no normalization."
    - "REJECT-05 / D-04 / D-05 / D-06 / D-07: an expected rejection produces an uncommitted TransitionResult with cause=None, a comparison-neutral repr-visible rejection_code, derived read-only rejected status, fixed bounded error text, and unchanged TransitionError escalation."
    - "REJECT-03 / REJECT-04 / D-15: only transition guards, declarative guards, and state permission checks convert the signal; conversion preserves source, trigger, stage, selected priority, and internal mode while stopping the complete candidate group before lifecycle."
    - "D-12: trigger dispatch finalizes a rejected result through the existing failure-observer signature exactly once, without a rejection listener family."
    - "D-14: rejection diagnostics are debug-only scalar metadata and never expose an exception representation, traceback, warning, caller payload, or arbitrary object."
    - "Ordinary false eligibility continues deterministic local fallthrough; rejection is terminal and never shares the None scan-control channel."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Validated TransitionRejected control signal, additive TransitionResult metadata, and synchronous three-seam conversion"
      contains: "class TransitionRejected"
    - path: "src/fast_fsm/core.pyi"
      provides: "Exact public exception and result field/property contract"
      contains: "class TransitionRejected"
    - path: "src/fast_fsm/__init__.py"
      provides: "Package-root TransitionRejected export"
      contains: "TransitionRejected"
    - path: "tests/test_expected_rejection.py"
      provides: "Wave 0 central REJECT-01..09 semantic oracle"
    - path: "tools/release_evidence.py"
      provides: "Immediate registration of the public non-native exception so every wave remains slots-policy clean"
  key_links:
    - from: "TransitionRejected.__init__"
      to: "StateMachine._select_sync_candidate"
      via: "validated read-only code rechecked only on the cold rejection conversion path"
      pattern: "TransitionRejected"
    - from: "StateMachine._select_sync_candidate"
      to: "TransitionResult"
      via: "terminal result at each of the three approved eligibility calls"
      pattern: "rejection_code"
    - from: "StateMachine._trigger_owned"
      to: "StateMachine._finalize_failure"
      via: "existing single failed-result finalization boundary"
      pattern: "_finalize_failure"
  flagged_assumptions:
    - id: REJECT-01
      classification: unclassified
      status: mapped-to-locked-contract
      statement: "The specless probe did not classify signal construction; this plan uses D-01 through D-03 as the complete public input contract without widening accepted types."
    - id: REJECT-02
      classification: unclassified
      status: mapped-to-locked-contract
      statement: "The specless probe did not classify payload safety; this plan maps it to D-02, D-03, D-05, and D-14 plus T-29-01/T-29-04 controls."
    - id: REJECT-03
      classification: unclassified
      status: mapped-to-locked-contract
      statement: "The specless probe did not classify conversion boundaries; this plan treats the three eligibility calls named by the phase boundary as exhaustive."
    - id: REJECT-04-ADJACENCY
      classification: adjacency
      status: mapped-to-locked-contract
      statement: "Adjacent false and rejection candidates preserve the existing false-versus-terminal distinction: false may advance once, rejection stops all later work."
    - id: REJECT-04-EMPTY-SINGLE-NULL
      classification: empty-single-null
      status: mapped-to-existing-semantics
      statement: "No-slot resolution, singleton selection, and group exhaustion keep their existing results; null rejection codes are impossible under D-02 and do not create a fourth selector outcome."
    - id: REJECT-04-EQUAL-ORDERING
      classification: ordering-equal-elements
      status: mapped-to-existing-semantics
      statement: "Existing equal-priority duplicate/conflict registration rules and immutable registration order remain unchanged; Phase 29 only distinguishes ordinary false from terminal rejection during selection."
    - id: REJECT-05
      classification: unclassified
      status: mapped-to-locked-contract
      statement: "The specless probe did not classify result shape; D-04 through D-07 define the complete additive carrier and escalation contract."
    - id: REJECT-07
      classification: unclassified
      status: mapped-to-locked-contract
      statement: "The specless probe did not classify observation; D-12 fixes exactly one existing failure-observer pass and forbids a new listener family."
  prohibitions:
    - "Do not add an alternate rejection API, status enum, stored rejected boolean, rich payload, validator family, or rejection listener family."
    - "Do not represent expected rejection as False or None, evaluate a lower candidate after rejection, or catch it at the public trigger boundary."
    - "Do not add success-path validation, regex work, formatting, reflection, sorting, candidate copying, or allocation."
---

<objective>
Deliver the thinnest production expected-rejection path: construct a bounded public signal, raise it from any approved synchronous eligibility seam, and receive one structured terminal result without lifecycle or priority fallthrough.

Purpose: Establish the public contract and selector outcome algebra before extending it to queries, composition, and asynchronous execution.
Output: Public signal/result API, package and stub exposure, synchronous selector conversion, observer/logging behavior, and the Wave 0 semantic oracle.
</objective>

## Artifacts this phase produces

- Public `TransitionRejected(code)` control signal with exact bounded ASCII validation.
- Additive `TransitionResult.rejection_code` and derived read-only `rejected` status.
- Sync/async conversion only at transition guard, declarative guard, and state permission seams.
- New `tests/test_expected_rejection.py` requirement-labelled semantic oracle.
- Synchronized runtime/stub/package exports, slots-policy authorities, API docs, and living SPR.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-expected-domain-rejection/29-CONTEXT.md
@.planning/phases/29-expected-domain-rejection/29-RESEARCH.md
@.planning/phases/29-expected-domain-rejection/29-PATTERNS.md
@.planning/phases/29-expected-domain-rejection/29-VALIDATION.md
@.planning/phases/28-same-state-transition-modes/28-03-SUMMARY.md
@.planning/phases/28-same-state-transition-modes/28-VERIFICATION.md

<interfaces>
Existing contracts to deepen:
- `TransitionError(RuntimeError)` is `@mypyc_attr(native_class=False)`, stores the originating `TransitionResult`, and is the only exception raised by `TransitionResult.raise_if_failed()`.
- `TransitionResult` is a slotted dataclass whose legacy first five fields remain positional; its current comparison-neutral tail is committed, stage, hidden cause, priority, and internal.
- `_select_sync_candidate(...)` returns `_PreparedDispatch`, terminal `TransitionResult`, or `None`; only `None` advances a local candidate group.
- `_build_failure_result(...)` constructs outcomes without observation; `_trigger_owned()` and `_finalize_failure()` own exactly one failed-trigger observer pass.
- `core.pyi` mirrors public field order/defaults, and `fast_fsm.__all__` is the package-root export authority.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the bounded public signal into one structured synchronous result</name>
  <reversibility rating="costly">D-01 and D-04 define released public exception and result shapes; undoing exact input or carrier semantics would require coordinated compatibility changes.</reversibility>
  <files>src/fast_fsm/core.py, src/fast_fsm/core.pyi, src/fast_fsm/__init__.py, tests/test_expected_rejection.py, tests/test_mypyc_guard.py, tests/test_release_evidence.py, tools/release_evidence.py, .github/copilot-instructions.md, .specify/memory/spr-core-api.md</files>
  <read_first>
    - `.planning/phases/29-expected-domain-rejection/29-CONTEXT.md` D-01 through D-07 and D-15
    - `.planning/phases/29-expected-domain-rejection/29-RESEARCH.md` Patterns 1 through 3, public control-signal example, validation matrix, and T-29-01/T-29-03/T-29-04 controls
    - `.planning/phases/29-expected-domain-rejection/29-PATTERNS.md` core.py, core.pyi, package export, and central semantic-oracle assignments
    - `src/fast_fsm/core.py` TransitionError, TransitionResult, `_build_failure_result`, `_select_transition_sync`, and `_select_sync_candidate`
    - `src/fast_fsm/core.pyi` TransitionError and TransitionResult declarations
    - `src/fast_fsm/__init__.py` core imports and `__all__`
    - `tests/test_mypyc_guard.py` additive result layout and TransitionError boundary assertions
    - `tools/release_evidence.py` REGISTERED_SLOTS_EXCEPTIONS and runtime instance inventory
    - `tests/test_release_evidence.py` exact registered-exception set and authority synchronization assertions
    - `.github/copilot-instructions.md` and `.specify/memory/spr-core-api.md` exact measured exception policy
  </read_first>
  <behavior>
    - "RED first: exact built-in strings at lengths 1 and 64 with valid lowercase ASCII grammar construct; non-string, str subclass, empty, 65-character, uppercase, whitespace, slash, and Unicode inputs fail eagerly with the locked exception types."
    - "The signal exposes a read-only validated code; no normalization, arbitrary source object, mutable alias, or hostile representation enters the result."
    - "TransitionResult appends rejection_code after internal with default None, compare=False, repr-visible behavior, and rejected derived solely from code presence."
    - "A rejected result is unsuccessful, uncommitted, destination-free, cause-free, stage-aware, priority/mode-aware, and has exactly `Transition rejected: <code>` as bounded public error text."
    - "raise_if_failed returns the same success result and raises the existing TransitionError carrying a rejected result, never TransitionRejected."
  </behavior>
  <action>
Create `tests/test_expected_rejection.py` first and demonstrate the focused public-contract/result tests fail. Implement `TransitionRejected` beside `TransitionError` as an explicit non-native public `Exception` boundary. Centralize exact `type(code) is str`, length-before-pattern, and full ASCII grammar validation; store only the validated scalar behind a getter-only property and revalidate it on conversion so subclass-crafted or mutated objects cannot reach public metadata per D-01 through D-03 and T-29-01/T-29-05.

Append `rejection_code: Optional[str] = field(default=None, compare=False)` to `TransitionResult` after `internal`, add getter-only `rejected`, and preserve the legacy positional prefix, equality, repr, cause hiding, and `raise_if_failed()` behavior per D-04 through D-07. Extend the failure-result construction seam with the scalar or a narrow observer-free helper; rejection uses `committed=False`, `to_state=None`, `cause=None`, selected stage/priority/internal, and fixed error text per D-05/D-15. Add matching runtime/stub declarations and package-root import/`__all__` exposure without an alternate exception module or status model. In the same atomic task, update existing result/export structural expectations and register `fast_fsm.core.TransitionRejected` as the fourth measured instance-dictionary exception across `tools/release_evidence.py`, its exact-set tests, project instructions, and the core SPR. This immediate authority synchronization keeps the Plan 29-01 slots-policy wave gate green; Plan 29-04 expands dual-origin/catch-placement proof rather than deferring registry correctness.

Complete the tracer by converting one synchronous transition-guard signal at its immediate eligibility call and returning the existing terminal `TransitionResult` channel. Keep ordinary false as `None` only for grouped fallthrough and leave public trigger/lifecycle catches untouched. Then make the complete Task 1 public and boundary matrix green.
  </action>
  <acceptance_criteria>
    - "TransitionRejected accepts only the exact D-01 through D-03 language and exposes no writable or unvalidated public payload."
    - "Runtime, stub, and package root expose one consistent signal and one trailing result scalar/property contract."
    - "Structural and release authorities agree immediately on the exact four measured instance-dictionary exceptions, so slots policy passes after this wave."
    - "Result equality remains compatible, repr includes rejection_code, cause remains hidden and None, and raise_if_failed still raises TransitionError."
    - "One transition-guard rejection is terminal before lifecycle and carries truthful source/trigger/stage/priority/internal metadata."
    - "T-29-01, T-29-03, and T-29-04 controls block completion if exact validation, outcome classification, or bounded payload behavior fails."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_expected_rejection.py tests/test_mypyc_guard.py tests/test_release_evidence.py -x -q -k "public or construct or validation or payload or result or rejection or slots_policy" &amp;&amp; uv run python tools/release_evidence.py slots-policy --json</automated>
  </verify>
  <done>REJECT-01, REJECT-02, and REJECT-05 have a public, typed, bounded synchronous end-to-end path with stable result and escalation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete synchronous three-seam terminality, observation, and safe logging</name>
  <files>src/fast_fsm/core.py, tests/test_expected_rejection.py</files>
  <read_first>
    - `.planning/phases/29-expected-domain-rejection/29-CONTEXT.md` D-12, D-14, D-15 and the three approved conversion boundaries
    - `.planning/phases/29-expected-domain-rejection/29-VALIDATION.md` row 29-01-02 and required eligibility/observer matrices
    - `src/fast_fsm/core.py` `_select_sync_candidate`, `_evaluate_declarative_condition_sync`, `_can_transition_after_declarative_guard`, `_build_failure_result`, `_trigger_owned`, and `_finalize_failure`
    - `tests/test_priority_selection.py` false fallthrough, guard exception, declarative/permission exception, and mixed-mode priority analogs to reproduce in the central Phase 29 oracle
    - `tests/test_transition_lifecycle.py` precommit truth and failure-observer isolation/reentry analogs to reproduce in the central Phase 29 oracle
    - `tests/test_logging_config.py` metadata-only trace, warning suppression, hostile repr, and redactor safety analogs to reproduce in the central Phase 29 oracle
  </read_first>
  <behavior>
    - "Transition guard, declarative guard, and state permission rejection all stop singleton/group selection and enter no before/exit/commit/entry/declarative/trigger/after surface."
    - "A false candidate immediately adjacent to rejection may fall through to the rejecting candidate, but rejection never advances to a later candidate; existing equal-priority registration behavior is unchanged."
    - "Selected priority and internal mode survive rejection; state identity, residency, history, destination, and commit truth remain unchanged."
    - "One real trigger invokes the unchanged failure-observer callback family exactly once even with multiple, failing, or reentrant observers."
    - "Expected rejection emits only debug metadata containing the validated code and emits no warning, traceback, exception representation, or raw caller value."
  </behavior>
  <action>
Write failing parameterized tests in `tests/test_expected_rejection.py` for all three synchronous eligibility seams in singleton and grouped slots, including adjacent ordinary-false/rejection candidates, selected negative/positive priorities, internal/external candidates, and lower-candidate sentinels. Preserve existing no-slot, singleton, group-exhaustion, equal-priority conflict/idempotency, and ordinary exception outcomes rather than creating a null or tie-breaking rejection path. Keep adjacent suite files read-only in this plan; Plans 29-02 and 29-03 own their focused query/async and lifecycle/logging extensions.

Add the specific `TransitionRejected` conversion before each existing general `Exception` branch in `_select_sync_candidate`. Defensively validate the cold-path scalar; a corrupt signal remains an ordinary unexpected failure rather than emitting invalid rejection metadata. Return a terminal result for both direct and group paths, never `None`; do not catch timing, prepared-dispatch creation, lifecycle, handlers, callbacks, tracing, observers, or the outer trigger per REJECT-03/04, D-15, and T-29-02/T-29-03/T-29-07.

Route real triggers through the unchanged `_trigger_owned()` and `_finalize_failure()` path per D-12. Add observer snapshot/isolation/reentry assertions and metadata-only debug checks to the central oracle per D-14, using hostile repr/caller payload sentinels. Logging may include only the validated code as debug metadata; it must not call `str(signal)`, `repr(signal)`, or emit warning/traceback content. Keep the callback signature unchanged and add no listener registry.
  </action>
  <acceptance_criteria>
    - "All three synchronous approved seams return the same structured terminal classification and no lower candidate or lifecycle surface executes."
    - "Ordinary false still falls through in deterministic priority order, while ordinary exceptions retain hidden-cause defect behavior."
    - "Rejected triggers preserve stage, priority, internal, source, trigger, no destination, no commit, and no history."
    - "Existing failure observers run exactly once through their unchanged signature and remain isolated from observer failure/reentry."
    - "T-29-02, T-29-03, T-29-04, and T-29-06 controls block completion if selection, classification, disclosure, or observer cardinality regresses."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_expected_rejection.py -x -q -k "approved_boundary or priority or fallthrough or observer or logging or rejection"</automated>
  </verify>
  <done>REJECT-03, REJECT-04, and REJECT-07 are complete for synchronous trigger dispatch with bounded diagnostics and exactly-once observation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application hook -> public control signal | Untrusted application values attempt to become stable result/log metadata. |
| eligibility callback -> candidate selector | False, expected rejection, and unexpected defect must remain distinct control outcomes. |
| selector -> public trigger finalizer | A terminal result must stop the group and be observed exactly once without lifecycle work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Spoofing | TransitionRejected code input | high | mitigate | Exact built-in string, 1..64 length-before-pattern validation, positive ASCII grammar, no normalization, and conversion-time revalidation; ASVS input-validation gate blocks completion. |
| T-29-02 | Tampering / Elevation of Privilege | priority-group selection | high | mitigate | Rejection returns terminal TransitionResult, never None; lower-candidate sentinels and false-versus-terminal adjacency tests block completion. |
| T-29-03 | Repudiation | outcome classification | high | mitigate | Explicit rejection_code with cause=None and exact stage metadata remains distinct from ordinary false and hidden-cause defects. |
| T-29-04 | Information Disclosure | result and debug logging | high | mitigate | Fixed bounded text from validated scalar only; hostile-repr/caplog tests forbid payloads, warnings, tracebacks, and exception representations under ASVS error-handling policy. |
| T-29-06 | Denial of Service / Repudiation | failure observers | medium | mitigate | Selector builds only; public trigger finalizes once; observer snapshot/isolation/reentry tests enforce one pass. |
</threat_model>

<verification>
- Demonstrate Task 1 public/result tests red before implementation, then run its focused structural matrix.
- Demonstrate Task 2 three-seam/priority/observer/logging matrix red before completing conversion.
- After the wave, run `uv run pytest tests/test_expected_rejection.py tests/test_priority_selection.py tests/test_condition_interface.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_release_evidence.py -x -q` and the slots-policy audit.
</verification>

<success_criteria>
- Applications can raise one bounded public signal at every synchronous approved eligibility seam and receive one structured terminal result.
- False fallthrough, expected rejection, unexpected defects, and observer ownership remain semantically distinct.
- Public/stub/export shape and ASVS input/error controls are active before async expansion begins.
</success_criteria>

## Source Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Expected domain outcome without defect masking or lower-priority selection | 29-01..04 | COVERED | Tracer, parity expansion, boundary closure, and structural/native closure. |
| REQ | REJECT-01 | Public bounded signal | 29-01, 29-04 | COVERED | Task 1 public tracer plus closure. |
| REQ | REJECT-02 | Stable payload-safe identifier | 29-01, 29-03, 29-04 | COVERED | Validation, disclosure, and native closure. |
| REQ | REJECT-03 | Three approved eligibility seams only | 29-01, 29-03, 29-04 | COVERED | Immediate sync catches, outside-boundary proof, structural closure. |
| REQ | REJECT-04 | Rejection aborts group; false falls through | 29-01, 29-02, 29-04 | COVERED | Sync/async candidate matrices and native oracle. |
| REQ | REJECT-05 | Structured uncommitted result | 29-01, 29-04 | COVERED | Additive carrier tracer and layout proof. |
| REQ | REJECT-06 | Query false with trigger code retained | 29-02, 29-04 | COVERED | Sync/async query matrix and closure. |
| REQ | REJECT-07 | Existing failure observer exactly once | 29-01, 29-02, 29-04 | COVERED | Sync/async finalizer evidence. |
| REQ | REJECT-08 | Composition propagates rejection | 29-02, 29-04 | COVERED | Direct/nested/deferred condition proof. |
| REQ | REJECT-09 | Outside eligibility remains ordinary failure | 29-03, 29-04 | COVERED | Lifecycle classification and closure matrix. |
| CONTEXT | D-01..D-07 | Exact signal/result/exception contract | 29-01 | COVERED | Task 1 cites every decision. |
| CONTEXT | D-08..D-11 | Composition and direct-evaluation propagation | 29-02 | COVERED | Deferred/nested sync/async proof. |
| CONTEXT | D-12..D-15 | Observer, query, logging, and metadata behavior | 29-01..04 | COVERED | Trigger/query/lifecycle behavior plus structural/native closure. |
| RESEARCH | R-SELECTOR | Existing None / prepared / terminal-result protocol | 29-01, 29-02 | COVERED | No new selector carrier. |
| RESEARCH | R-NATIVE | Fourth measured exception and pure/native parity | 29-01, 29-04 | COVERED | Atomic authority synchronization then native oracle. |
| RESEARCH | R-SECURITY | Validation, disclosure, DoS, observer, lifecycle, cancellation controls | 29-01..04 | COVERED | T-29-01 through T-29-08. |
| RESEARCH | R-PERF | No success-path work or unrelated topology scan | 29-01, 29-04 | COVERED | AST and singleton benchmark closure. |

<output>
Create `.planning/phases/29-expected-domain-rejection/29-01-SUMMARY.md` when done.
</output>
