---
phase: 29-expected-domain-rejection
plan: 04
type: execute
wave: 4
depends_on:
  - 29-03
files_modified:
  - tests/test_mypyc_guard.py
  - tests/test_release_evidence.py
  - tools/release_evidence.py
  - .github/copilot-instructions.md
  - .specify/memory/spr-core-api.md
autonomous: true
requirements:
  - REJECT-01
  - REJECT-02
  - REJECT-03
  - REJECT-04
  - REJECT-05
  - REJECT-06
  - REJECT-07
  - REJECT-08
  - REJECT-09
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "REJECT-01..09: runtime, stub, package export, AST, slots registry, release tests, policy text, SPR, pure source, and fresh native execution agree on one bounded contract."
    - "The four measured instance-dictionary exceptions are synchronized exactly; core.py remains the only mypyc unit and conditions.py stays interpreted."
    - "T-29-05: direct singleton dispatch adds no rejection validation/formatting/allocation, sorting, copying, reflection, task fan-out, or unrelated topology scan."
    - "Pure and compiled origins pass the same rejection/query/composition/lifecycle/cancellation semantic oracle and existing singleton floor."
    - "Exact generated native shadows are recoverably relocated on success or failure, and the repository ends on asserted pure `src/fast_fsm/core.py` origin."
  artifacts:
    - path: "tests/test_mypyc_guard.py"
      provides: "Exact result/exception/export/catch-site/fast-path and dual-origin oracle"
    - path: "tests/test_release_evidence.py"
      provides: "Exact four-exception authority and slots inventory regression"
    - path: "tools/release_evidence.py"
      provides: "Measured TransitionRejected instance-dictionary registry entry"
      contains: "fast_fsm.core.TransitionRejected"
    - path: ".github/copilot-instructions.md"
      provides: "Current slots and quality-gate policy"
    - path: ".specify/memory/spr-core-api.md"
      provides: "Living three-valued selector/result/observer/native contract"
  key_links:
    - from: "runtime TransitionRejected / TransitionResult"
      to: "mypyc structural and semantic oracle"
      via: "exact field/property/export/catch-site assertions"
      pattern: "rejection_code"
    - from: "REGISTERED_SLOTS_EXCEPTIONS"
      to: "release tests, instructions, and SPR"
      via: "same exact four measured names"
      pattern: "TransitionRejected"
    - from: "pure source oracle"
      to: "fresh mypyc oracle"
      via: "same tests under asserted origins and recoverable shadow relocation"
      pattern: "FAST_FSM_BUILD_MODE"
  flagged_assumptions: []
  prohibitions:
    - "Do not weaken slots policy, compile conditions.py, add a dependency, or register a fifth unmeasured exception."
    - "Do not mutate files outside this plan during task-level Ruff operations."
    - "Do not leave, delete broadly, or trust an unverified native shadow in src/fast_fsm."
    - "Do not claim installed-artifact or feature-local performance evidence owned by Phase 32."
---

<objective>
Synchronize structural/native/release authorities and close Phase 29 under pure/native semantics, source restoration, full regression, and the existing singleton performance signal.

Purpose: Make the expected-rejection contract trustworthy across runtime modes without widening the hot path or release boundary.
Output: Exact structural and slots-policy proof, living policy/SPR, dual-origin semantic evidence, restored pure source, and mandatory phase-closure gates.
</objective>

## Artifacts this phase produces

- Public bounded rejection API and synchronous tracer from Plan 29-01.
- Query/composition/async parity from Plan 29-02.
- Lifecycle/logging/API-docs closure from Plan 29-03.
- Exact structural/native/release/performance closure and source restoration from this plan.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-expected-domain-rejection/29-CONTEXT.md
@.planning/phases/29-expected-domain-rejection/29-RESEARCH.md
@.planning/phases/29-expected-domain-rejection/29-PATTERNS.md
@.planning/phases/29-expected-domain-rejection/29-VALIDATION.md
@.planning/phases/29-expected-domain-rejection/29-03-SUMMARY.md

<interfaces>
Plans 29-01..03 provide complete production behavior, central/adjacent tests, and API docs.
- `tests/test_mypyc_guard.py` owns runtime/stub field order, selector AST complexity, package export, and pure/native semantic probes.
- Plan 29-01 synchronizes `REGISTERED_SLOTS_EXCEPTIONS`, exact-set tests, project instructions, and SPR to four named boundaries; this plan verifies and deepens that authority.
- `task pure-source-check` is non-destructive; only verified absolute `src/fast_fsm/core*.so` or `core*.pyd` paths may be recoverably relocated.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock structural, slots-policy, and dual-origin rejection contracts</name>
  <reversibility rating="costly">The public compiled carrier and exact measured exception registry are cross-tool compatibility authorities; changing them later requires synchronized runtime, tests, policy, and release evidence.</reversibility>
  <files>tests/test_mypyc_guard.py, tests/test_release_evidence.py, tools/release_evidence.py, .github/copilot-instructions.md, .specify/memory/spr-core-api.md</files>
  <read_first>
    - `.planning/phases/29-expected-domain-rejection/29-CONTEXT.md` D-01 through D-15
    - `.planning/phases/29-expected-domain-rejection/29-RESEARCH.md` native-layout consequence, T-29-01/T-29-04/T-29-05 controls, and closure gates
    - `.planning/phases/29-expected-domain-rejection/29-PATTERNS.md` mypyc guard, release evidence, policy, SPR, and hot-path assignments
    - `src/fast_fsm/core.py` TransitionRejected, TransitionResult, sync/async selectors, trigger/lifecycle/finalizer code, and combinator bridge
    - `src/fast_fsm/core.pyi` exception/result declarations
    - `src/fast_fsm/__init__.py` package export authority
    - `tests/test_mypyc_guard.py` result layout, selector AST, Phase 28 carrier, export, and pure/native probe
    - `tools/release_evidence.py` REGISTERED_SLOTS_EXCEPTIONS and runtime instance inventory
    - `tests/test_release_evidence.py` exact registered set, authority synchronization, and measured inventory
    - `.github/copilot-instructions.md` slots/selective-compilation/source-origin/performance rules
    - `.specify/memory/spr-core-api.md` result/selector/condition/observer/cancellation/native contract
  </read_first>
  <behavior>
    - "Runtime/stub field order ends with internal then rejection_code; rejected is getter-only; TransitionRejected is package-exported, explicit non-native, and one of exactly four measured instance-dictionary exceptions."
    - "AST evidence finds only the approved three sync and three async conversion sites and no broad trigger/lifecycle/composition catch."
    - "Direct singleton and local group selectors retain one topology lookup with no sort/copy/reflection/task fan-out or success-path rejection work."
    - "One semantic probe covers public validation, result equality/repr/escalation, false-versus-terminal selection, queries, async parity, composition, lifecycle classification, cancellation, and observer cardinality."
    - "Policy and SPR describe the exact shipped contract without Phase 31/32 claims."
  </behavior>
  <action>
Extend structural tests first: assert exact runtime/stub result tail and property, public exception decorator/property/export, only six selector conversion sites, unchanged catch-free combinators, and no rejection conversion in lifecycle/trigger/finalizer code. Extend the existing semantic probe to cover the complete public and boundary contract in either pure or compiled origin.

Confirm and deepen Plan 29-01's four-exception authority. Keep `tools/release_evidence.py`, exact-set/runtime-construction tests, project instructions, and SPR synchronized; fail if any still names three or invents a fifth. Preserve core.py-only mypyc compilation and interpreted conditions.py. Record the three-valued selector protocol, bounded scalar/error contract, observer/query ownership, cancellation, and selector-only conversion without later-phase claims.

For fast task feedback, run only the focused structural/release selection below. Apply mutating Ruff format/fixes only to this task's declared Python files (`tests/test_mypyc_guard.py`, `tests/test_release_evidence.py`, and `tools/release_evidence.py`). Task 2 owns typing, docs, slots, native build/oracle, benchmark, restoration, non-mutating whole-phase Ruff checks, and full suite.
  </action>
  <acceptance_criteria>
    - "Runtime, stub, export, AST, slots registry, release tests, instructions, and SPR agree on the exact public/layout contract and four measured exceptions."
    - "Selectors contain only approved conversion sites and retain direct O(1) singleton/local O(k) group structure."
    - "Task-owned Python files are formatted/fixed without mutating prior-plan files."
    - "The focused structural/release command completes under the sub-60-second feedback target."
    - "T-29-01 through T-29-08 remain represented in structural or phase-closure verification."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_mypyc_guard.py tests/test_release_evidence.py -x -q -k "transition_result or rejection or slots_policy"</automated>
  </verify>
  <done>Structural, slots-policy, release-policy, and dual-origin oracle definitions are synchronized and ready for the mandatory phase-closure gate.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the mandatory quality, native, restoration, and regression closure</name>
  <reversibility rating="reversible">This task changes no public contract; its only transient native artifacts are path-constrained and recoverably relocated to a printed backup directory.</reversibility>
  <files>tests/test_mypyc_guard.py, tests/test_release_evidence.py, tools/release_evidence.py, .github/copilot-instructions.md, .specify/memory/spr-core-api.md</files>
  <read_first>
    - `.planning/phases/29-expected-domain-rejection/29-CONTEXT.md` D-01 through D-15 and all explicit non-goals
    - `.planning/phases/29-expected-domain-rejection/29-VALIDATION.md` row 29-04-02 and mandatory validation matrices
    - `.planning/phases/28-same-state-transition-modes/28-03-PLAN.md` Task 2 native inventory, EXIT trap, restoration, and origin assertions
    - `.planning/phases/28-same-state-transition-modes/28-03-SUMMARY.md` actual native closure outcome and source-restoration evidence
    - `tools/release_evidence.py` verify-source and slots-policy commands
    - `Taskfile.yml` type, source, and build task definitions
    - `tests/test_performance_benchmarks.py` existing singleton throughput regression signal
  </read_first>
  <action>
Run the exact closure sequence below after Task 1. The first two commands may mutate only this plan's three declared Python files; the whole-phase Ruff commands are non-mutating. Blocking mypy, visible advisory ty, slots, warning-as-error API docs, asserted pure/native semantic parity, the existing singleton benchmark, recoverable native-shadow relocation on every exit, final pure origin, and the full sequential suite are all mandatory. Preserve the Phase 28 fail-closed inventory pattern exactly: only absolute paths reported by `verify-source --json` that match `$PWD/src/fast_fsm/core*.so` or `$PWD/src/fast_fsm/core*.pyd` may be moved, never removed, and the printed unique backup remains recoverable.

set -eu
uv run ruff format tests/test_mypyc_guard.py tests/test_release_evidence.py tools/release_evidence.py
uv run ruff check --fix tests/test_mypyc_guard.py tests/test_release_evidence.py tools/release_evidence.py
uv run ruff format --check src/fast_fsm/core.py src/fast_fsm/core.pyi src/fast_fsm/__init__.py tests/test_expected_rejection.py tests/test_priority_selection.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_condition_interface.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_release_evidence.py tools/release_evidence.py
uv run ruff check src/fast_fsm/core.py src/fast_fsm/core.pyi src/fast_fsm/__init__.py tests/test_expected_rejection.py tests/test_priority_selection.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_condition_interface.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_release_evidence.py tools/release_evidence.py
task typecheck-mypy
task typecheck-ty || true
uv run python tools/release_evidence.py slots-policy --json
uv run sphinx-build -b html docs docs/_build/html -W --keep-going
task pure-source-check
uv run python -c 'from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); expected = Path("src/fast_fsm/core.py").resolve(); assert origin == expected, (origin, expected); print(origin)'
FAST_FSM_BUILD_MODE=pure uv run pytest tests/test_expected_rejection.py tests/test_priority_selection.py tests/test_condition_interface.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_release_evidence.py -x -q -k "rejection or query or async or cancellation or compos or lifecycle or logging or transition_result or slots_policy"
native_backup="$(mktemp -d "${TMPDIR:-/tmp}/fast-fsm-phase29-native-backup.XXXXXX")"
shadow_report="$(mktemp "${TMPDIR:-/tmp}/fast-fsm-phase29-shadow-report.XXXXXX")"
shadow_manifest="${shadow_report}.paths"
shadow_stdout="${shadow_report}.stdout"
inventory_native() { : &gt;"$shadow_manifest"; if uv run python tools/release_evidence.py verify-source --json &gt;"$shadow_stdout" 2&gt;"$shadow_report"; then return 0; fi; shadow_count=0; while IFS= read -r report_line; do case "$report_line" in "  - "*) shadow_path="${report_line#  - }" ;; *) continue ;; esac; case "$shadow_path" in "$PWD"/src/fast_fsm/core*.so|"$PWD"/src/fast_fsm/core*.pyd) ;; *) echo "refusing untrusted shadow path: $shadow_path" &gt;&amp;2; return 1 ;; esac; test -f "$shadow_path" || return 1; printf '%s\n' "$shadow_path" &gt;&gt;"$shadow_manifest"; shadow_count=$((shadow_count + 1)); done &lt;"$shadow_report"; test "$shadow_count" -gt 0; }
restore_native() { if [ ! -s "$shadow_manifest" ]; then inventory_native; fi; while IFS= read -r shadow_path; do test -f "$shadow_path" || continue; mv "$shadow_path" "$native_backup/"; done &lt;"$shadow_manifest"; echo "native shadows retained at $native_backup"; task pure-source-check; uv run python -c 'from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); expected = Path("src/fast_fsm/core.py").resolve(); assert origin == expected, (origin, expected); print(origin)'; }
restore_on_exit() { native_status=$?; trap - EXIT; if ! restore_native; then native_status=1; fi; exit "$native_status"; }
trap restore_on_exit EXIT
task build-check
inventory_native
test -s "$shadow_manifest"
uv run python -c 'from importlib.machinery import EXTENSION_SUFFIXES; from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); assert any(str(origin).endswith(suffix) for suffix in EXTENSION_SUFFIXES), origin; print(origin)'
FAST_FSM_BUILD_MODE=compiled uv run pytest tests/test_expected_rejection.py tests/test_priority_selection.py tests/test_condition_interface.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_release_evidence.py -x -q -k "rejection or query or async or cancellation or compos or lifecycle or logging or transition_result or slots_policy"
FAST_FSM_BUILD_MODE=compiled uv run pytest tests/test_performance_benchmarks.py::TestAdvancedPerformance::test_trigger_min_throughput -x -q
trap - EXIT
restore_native
FAST_FSM_BUILD_MODE=pure uv run pytest tests/ -x -q
  </action>
  <acceptance_criteria>
    - "Mutating Ruff operations touch only Plan 29-04-owned Python files; non-mutating Ruff checks cover every Phase 29 Python target."
    - "Blocking mypy, visible advisory ty, slots policy, and warning-as-error API documentation complete with their outputs visible."
    - "The same focused semantic oracle passes from exact pure and freshly built extension origins, and the existing singleton throughput test remains green."
    - "On success or failure, only verified absolute core extension paths are moved to the printed backup, and final source checks assert exact `src/fast_fsm/core.py` origin."
    - "The complete sequential test suite passes after native-shadow restoration."
  </acceptance_criteria>
  <verify>
    <automated>
set -eu
uv run ruff format tests/test_mypyc_guard.py tests/test_release_evidence.py tools/release_evidence.py
uv run ruff check --fix tests/test_mypyc_guard.py tests/test_release_evidence.py tools/release_evidence.py
uv run ruff format --check src/fast_fsm/core.py src/fast_fsm/core.pyi src/fast_fsm/__init__.py tests/test_expected_rejection.py tests/test_priority_selection.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_condition_interface.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_release_evidence.py tools/release_evidence.py
uv run ruff check src/fast_fsm/core.py src/fast_fsm/core.pyi src/fast_fsm/__init__.py tests/test_expected_rejection.py tests/test_priority_selection.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_condition_interface.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_release_evidence.py tools/release_evidence.py
task typecheck-mypy
task typecheck-ty || true
uv run python tools/release_evidence.py slots-policy --json
uv run sphinx-build -b html docs docs/_build/html -W --keep-going
task pure-source-check
uv run python -c 'from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); expected = Path("src/fast_fsm/core.py").resolve(); assert origin == expected, (origin, expected); print(origin)'
FAST_FSM_BUILD_MODE=pure uv run pytest tests/test_expected_rejection.py tests/test_priority_selection.py tests/test_condition_interface.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_release_evidence.py -x -q -k "rejection or query or async or cancellation or compos or lifecycle or logging or transition_result or slots_policy"
native_backup="$(mktemp -d "${TMPDIR:-/tmp}/fast-fsm-phase29-native-backup.XXXXXX")"
shadow_report="$(mktemp "${TMPDIR:-/tmp}/fast-fsm-phase29-shadow-report.XXXXXX")"
shadow_manifest="${shadow_report}.paths"
shadow_stdout="${shadow_report}.stdout"
inventory_native() { : &gt;"$shadow_manifest"; if uv run python tools/release_evidence.py verify-source --json &gt;"$shadow_stdout" 2&gt;"$shadow_report"; then return 0; fi; shadow_count=0; while IFS= read -r report_line; do case "$report_line" in "  - "*) shadow_path="${report_line#  - }" ;; *) continue ;; esac; case "$shadow_path" in "$PWD"/src/fast_fsm/core*.so|"$PWD"/src/fast_fsm/core*.pyd) ;; *) echo "refusing untrusted shadow path: $shadow_path" &gt;&amp;2; return 1 ;; esac; test -f "$shadow_path" || return 1; printf '%s\n' "$shadow_path" &gt;&gt;"$shadow_manifest"; shadow_count=$((shadow_count + 1)); done &lt;"$shadow_report"; test "$shadow_count" -gt 0; }
restore_native() { if [ ! -s "$shadow_manifest" ]; then inventory_native; fi; while IFS= read -r shadow_path; do test -f "$shadow_path" || continue; mv "$shadow_path" "$native_backup/"; done &lt;"$shadow_manifest"; echo "native shadows retained at $native_backup"; task pure-source-check; uv run python -c 'from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); expected = Path("src/fast_fsm/core.py").resolve(); assert origin == expected, (origin, expected); print(origin)'; }
restore_on_exit() { native_status=$?; trap - EXIT; if ! restore_native; then native_status=1; fi; exit "$native_status"; }
trap restore_on_exit EXIT
task build-check
inventory_native
test -s "$shadow_manifest"
uv run python -c 'from importlib.machinery import EXTENSION_SUFFIXES; from pathlib import Path; import fast_fsm.core as core; origin = Path(core.__file__).resolve(); assert any(str(origin).endswith(suffix) for suffix in EXTENSION_SUFFIXES), origin; print(origin)'
FAST_FSM_BUILD_MODE=compiled uv run pytest tests/test_expected_rejection.py tests/test_priority_selection.py tests/test_condition_interface.py tests/test_transition_lifecycle.py tests/test_async.py tests/test_logging_config.py tests/test_mypyc_guard.py tests/test_release_evidence.py -x -q -k "rejection or query or async or cancellation or compos or lifecycle or logging or transition_result or slots_policy"
FAST_FSM_BUILD_MODE=compiled uv run pytest tests/test_performance_benchmarks.py::TestAdvancedPerformance::test_trigger_min_throughput -x -q
trap - EXIT
restore_native
FAST_FSM_BUILD_MODE=pure uv run pytest tests/ -x -q
    </automated>
  </verify>
  <done>All mandatory Phase 29 quality gates pass; pure and compiled semantics agree; the singleton benchmark remains green; exact native shadows are recoverably retained outside the source tree; final origin is asserted pure; and the full sequential suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python source -> mypyc extension | Public fields, exception behavior, catches, and cancellation truth must remain identical. |
| source tree -> native artifacts | Generated shadows can alter import origin and require fail-closed recoverable handling. |
| runtime contract -> release/policy authorities | Registry, tests, instructions, and SPR must name the same exact bounded contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Spoofing | pure/native validation | high | mitigate | Dual-origin exact-type/grammar tests plus synchronized stub/export/registry. |
| T-29-02 | Tampering / Elevation of Privilege | selector structure | high | mitigate | AST terminal-result/no-fallback proof and full semantic oracle. |
| T-29-03 | Repudiation | catch placement | high | mitigate | Exact six approved sites and no broad lifecycle/trigger conversion. |
| T-29-04 | Information Disclosure | result/log/policy | high | mitigate | Bounded scalar/error assertions and full logging regression. |
| T-29-05 | Denial of Service | validation and hot selectors | high | mitigate | Length-before-pattern, no-sort/no-copy/no-reflection/no-fan-out AST checks, singleton floor. |
| T-29-06 | Denial of Service / Repudiation | observer finalization | medium | mitigate | Single-finalizer structural and behavioral evidence. |
| T-29-07 | Tampering / Repudiation | lifecycle boundary | high | mitigate | No broad catch and full outside-boundary oracle. |
| T-29-08 | Denial of Service | compiled cancellation | high | mitigate | Dual-origin cancellation identity/re-raise/ownership evidence. |
</threat_model>

<verification>
- Task 1 verification is the focused structural/release command and must stay under 60 seconds.
- Task 2 is the explicit heavyweight exception and the single authoritative executable phase-closure route; it owns all broad, build, performance, restoration, documentation, typing, slots, and full-suite evidence.
- Prior-plan Python files are checked non-mutating; only this plan's declared Python files receive Ruff mutations.
</verification>

<success_criteria>
- Exact structural, slots-policy, policy, and release authorities agree on the complete expected-rejection contract.
- Pure and compiled semantics match, existing singleton performance remains green, and the repository returns to asserted pure source.
- All broad Phase 29 quality, docs, security, and regression gates pass without mutating files outside Plan 29-04 ownership.
</success_criteria>

## Source Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Structured domain rejection without hidden defects or priority fallback | 29-01..04 | COVERED | Four sequential plans close behavior and artifacts. |
| REQ | REJECT-01..09 | Complete Phase 29 requirement set | 29-01..04 | COVERED | All requirements reach dual-origin closure. |
| RESEARCH | R-API | One public signal and additive result tail | 29-01, 29-04 | COVERED | Structural/native proof. |
| RESEARCH | R-SELECTOR | Terminal result at exactly three seams | 29-01..04 | COVERED | Behavior plus AST count. |
| RESEARCH | R-COMPOSITION | Existing propagation by non-interference | 29-02, 29-04 | COVERED | Tests plus no-catch proof. |
| RESEARCH | R-OBSERVERS | Finalize only at public trigger | 29-01..04 | COVERED | Cardinality and structural evidence. |
| RESEARCH | R-NATIVE | Four exceptions and core-only compilation | 29-01, 29-04 | COVERED | Atomic registry then native closure. |
| RESEARCH | R-PERF | Cold rejection work; singleton path unchanged | 29-04 | COVERED | AST and existing floor. |
| CONTEXT | D-01..D-07 | Signal/result/escalation contract | 29-01, 29-04 | COVERED | Public and native proof. |
| CONTEXT | D-08..D-11 | Composition/direct/boundary behavior | 29-02..04 | COVERED | Composition and lifecycle closure. |
| CONTEXT | D-12..D-15 | Observer/query/logging/metadata behavior | 29-01..04 | COVERED | Complete behavioral and structural closure. |

### Specless Edge-Probe Coverage

All 11 surfaced rows remain explicit: unclassified REJECT-01/02/03/05/06/07/08/09 map to locked decisions and acceptance criteria; REJECT-04 adjacency, empty/single/null, and equal-ordering rows remain recorded in Plan 29-01 flagged assumptions with existing deterministic registration and false-versus-terminal semantics. No row is dropped by the four-plan split.

### Prohibition Recall

The recall/precision pass found no new bespoke values, fairness, privacy, transparency, safety, or ethics prohibition. Phase-specific prohibitions come from CONTEXT/PATTERNS; input validation, disclosure, algorithmic DoS, observer, lifecycle, and cancellation remain T-29-01 through T-29-08 security controls.

<output>
Create `.planning/phases/29-expected-domain-rejection/29-04-SUMMARY.md` when done.
</output>
