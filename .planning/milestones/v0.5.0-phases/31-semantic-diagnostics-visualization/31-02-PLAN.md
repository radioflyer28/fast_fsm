---
phase: 31-semantic-diagnostics-visualization
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/fast_fsm/validation.py
  - tests/test_validation.py
  - docs/api/validation.md
  - .specify/memory/spr-validation.md
autonomous: true
requirements: [DIAG-01, DIAG-03]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DIAG-01/D-01/D-02: validation names explicit finals and non-final sinks separately while legacy dead-state methods remain topology-based."
    - "DIAG-01/D-02: intentional finals receive no dead-end, missing-transition, or cannot-return penalty, including initial-only final and dense blended scores."
    - "DIAG-03/D-04/D-09: reports derive from the Plan 31-01 scalar graph with deterministic ordering and the existing per-operation ledger."
  artifacts:
    - path: src/fast_fsm/validation.py
      provides: truthful report fields and final-aware enhanced issues/scoring
    - path: tests/test_validation.py
      provides: final/sink and score regression matrix
    - path: docs/api/validation.md
      provides: public distinction between topology dead states and explicit completion
    - path: .specify/memory/spr-validation.md
      provides: living validator contract
  key_links:
    - from: src/fast_fsm/_diagnostics.py
      to: src/fast_fsm/validation.py
      via: copied state_finals and forward edge indices drive report and issue classification
    - from: src/fast_fsm/validation.py
      to: src/fast_fsm/visualization.py
      via: JSON quality consumes the same snapshot-backed validator without a fresh machine capture
---

<objective>
Make validation distinguish intentional completion from graph dead ends and score those cases truthfully.

Purpose: Users should be able to inspect a flat FSM without treating an explicit final as a design defect.
Output: Additive report fields, corrected enhanced warnings and score, tests, and focused public/maintainer guidance.
</objective>

## Artifacts this phase produces

- Topology-compatible dead-state reports with separate `final_states` and `non_final_sinks` facts.
- Final-aware enhanced warnings, reachability notes, and scores for ordinary and initial-only completion.
- Focused validator contract tests and documentation.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-semantic-diagnostics-visualization/31-CONTEXT.md
@.planning/phases/31-semantic-diagnostics-visualization/31-RESEARCH.md
@.planning/phases/31-semantic-diagnostics-visualization/31-VALIDATION.md
@.planning/phases/31-semantic-diagnostics-visualization/31-01-SUMMARY.md

<interfaces>
Plan 31-01 adds `_DiagnosticGraph.state_finals` parallel to sorted `state_names`; `graph.forward[index]` is the captured outgoing-edge tuple. `FSMValidator.find_dead_states()` and `validate_completeness()["dead_states"]` currently mean no outgoing edge. `EnhancedFSMValidator._analyze_structure()`, `_analyze_reachability()`, `_analyze_completeness()`, and `get_validation_score()` own the warning/score paths. `visualization._quality_from_snapshot()` constructs a validator from the supplied captured snapshot and shared budget; do not open a second graph epoch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Report explicit finals and non-final sinks without changing dead-state compatibility</name>
  <reversibility rating="costly">Public report keys and dead-state meanings are consumed by callers and serialized evidence.</reversibility>
  <files>src/fast_fsm/validation.py, tests/test_validation.py</files>
  <read_first>
    - `31-CONTEXT.md` D-01, D-02, D-04 and D-09
    - `src/fast_fsm/validation.py` `_dead_state_names()`, `find_dead_states()`, `validate_completeness()`, report/export paths
    - `src/fast_fsm/_diagnostics.py` Plan 31-01 scalar graph and budget
    - `tests/test_validation.py` dead-state, completeness, dense and export tests
  </read_first>
  <behavior>
    - "A final with no outgoing edge appears in the old dead-state set and the new final set, but not the new non-final-sink set."
    - "A reachable or unreachable non-final sink appears in both old dead-state and new non-final-sink sets, and never in the final set."
    - "An initial-only final produces final={initial}, dead={initial}, non-final-sinks=empty; report/export ordering is stable."
  </behavior>
  <action>
Add tests first for the three cases above. Per D-01/D-02, preserve `find_dead_states()`, `dead_states`, `has_dead_states`, and legacy topology meanings. Add additive `final_states` and `non_final_sinks` to the completeness report and appropriate public report/export views, deriving both from Plan 31-01 `graph.state_finals` and `graph.forward` in stable `state_names` order (D-04). Continue using the caller's existing `_operation_budget()` or shared report ledger; reserve each scan and emitted finding before appending (D-09). Do not infer finality from missing edges or read live `_states` after capture. Preserve old report types and ordering for existing keys. If a report is a set by contract, retain that type while use of its new named lists/serialized form is deterministic.
  </action>
  <verify><automated>uv run pytest tests/test_validation.py -x -q -k "dead or complete or report or export"</automated></verify>
  <acceptance_criteria>
    - "DIAG-01/D-01/D-02: old topology fields and new final/non-final-sink fields express different correct facts."
    - "DIAG-03/D-04/D-09: all new findings come from one captured scalar graph and consume the operation budget."
  </acceptance_criteria>
  <done>Callers can read both legacy graph dead ends and explicit domain completion from validation reports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct enhanced findings and scores, then document the contract</name>
  <reversibility rating="costly">Quality scores and warning categories form an observed public diagnostic contract.</reversibility>
  <files>src/fast_fsm/validation.py, tests/test_validation.py, docs/api/validation.md, .specify/memory/spr-validation.md</files>
  <read_first>
    - `31-CONTEXT.md` D-01/D-02 and discretion on report names
    - `src/fast_fsm/validation.py` `_analyze_structure()`, `_analyze_reachability()`, `_analyze_completeness()`, `get_validation_score()`
    - `tests/test_validation.py` enhanced validator, sparse/dense scoring, and report assertions
    - `docs/api/validation.md` bounded diagnostics and score reference
    - `.specify/memory/spr-validation.md` existing living diagnostic contract
  </read_first>
  <behavior>
    - "No-outgoing explicit final causes no dead-end warning, no all-events-missing error, and no cannot-return note; a non-final sink retains the relevant findings."
    - "An initial-only final is accepted as intentional completion, not a no-event structural defect."
    - "Structural, completeness, and dense blended scores do not lose points solely because an explicit final has no outgoing edges; non-final defects still count."
  </behavior>
  <action>
Write focused score and issue tests before changing classification. Use the captured final bit to exempt explicit finals from no-event/no-outgoing, all-events-missing, and no-return findings, including the single-state initial-final path (D-01/D-02). Keep reachability warnings for genuinely unreachable states, preserve sparse/dense scoring formula and grade thresholds, and ensure final-only exemptions flow through the existing issue lists rather than post-hoc score manipulation. Test both reachable and unreachable non-final sinks, adjacent final/non-final states, and dense blend with nonzero completeness weight. Document the new report keys and topology-versus-completion distinction in `docs/api/validation.md` and the living SPR; explain that intentional final states do not incur missing-exit penalties. Keep validation cold and independent of `trigger*()`.
  </action>
  <verify><automated>uv run pytest tests/test_validation.py tests/test_diagnostic_contracts.py -x -q</automated></verify>
  <acceptance_criteria>
    - "DIAG-01/D-02: final-only topology does not receive unintended warnings or score loss; ordinary non-final sinks remain reportable."
    - "DIAG-03/D-04: enhanced findings and JSON quality remain tied to the captured graph rather than current mutable tables."
    - "Public reference and SPR describe every new field and the retained dead-state meaning."
  </acceptance_criteria>
  <done>Validation reports, enhanced findings, scores, and documentation agree on explicit completion versus graph shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Captured graph → validation report | Public reports and scores must reflect one epoch and retain compatibility semantics. |
| Large caller topology → analysis | Graph traversal and findings can consume unbounded resources unless every operation is counted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | validator classification | medium | mitigate | Classify with captured final flags and outgoing indices; test initial-only, reachable, and unreachable sink matrices. |
| T-31-03 | Denial of service | validation reports | medium | mitigate | Preserve one operation ledger and reserve scans/findings before appending. |
| T-31-06 | Repudiation | legacy diagnostic keys | low | accept | Keep existing dead-state keys and document their topology meaning; new final/sink keys make intent explicit without silent reinterpretation. |
</threat_model>

<verification>
Run focused validation and diagnostic contract tests after each task. Plan 31-05 performs the single strict docs build and shared closure.
</verification>

<success_criteria>
Users can identify explicit finals separately from topology sinks; enhanced validation never penalizes intentional completion merely because it has no outgoing edge.
</success_criteria>

<output>
Create `.planning/phases/31-semantic-diagnostics-visualization/31-02-SUMMARY.md` when done.
</output>
