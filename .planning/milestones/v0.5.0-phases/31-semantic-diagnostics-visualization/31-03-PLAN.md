---
phase: 31-semantic-diagnostics-visualization
plan: 03
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/fast_fsm/visualization.py
  - tests/test_visualization.py
  - tests/test_output_safety.py
  - docs/api/visualization.md
  - .specify/memory/spr-visualization.md
autonomous: true
requirements: [DIAG-02, DIAG-03]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DIAG-02/D-07: Mermaid and PlantUML mark only explicitly final states; no non-final sink receives a completion arrow."
    - "DIAG-02/D-08: internal and external self edges carry distinct fixed labels while trigger, guard name, and priority remain visible."
    - "DIAG-03/D-09: caller text is escaped for each output language, marker/edge lines reserve budget before append, and over-limit calls return no partial text."
  artifacts:
    - path: src/fast_fsm/visualization.py
      provides: final-aware Mermaid and PlantUML renderers
    - path: tests/test_visualization.py
      provides: exact marker and mode label matrix
    - path: tests/test_output_safety.py
      provides: hostile text, deterministic order, and exact budget probes
    - path: docs/api/visualization.md
      provides: public diagram and JSON semantic schema guidance
    - path: .specify/memory/spr-visualization.md
      provides: living bounded renderer contract
  key_links:
    - from: src/fast_fsm/_diagnostics.py
      to: src/fast_fsm/visualization.py
      via: scalar state_finals and edge.internal feed both renderers from the one captured graph
    - from: src/fast_fsm/visualization.py
      to: tests/test_output_safety.py
      via: opaque IDs, language-specific escaping, and reserve-before-line checks
---

<objective>
Render the settled flat-FSM semantics accurately in Mermaid and PlantUML under the existing output safeguards.

Purpose: Diagram readers must see declared completion and deliberate self-transition modes without confusing them with graph shape.
Output: Both diagram renderers, exact tests, hostile-text/budget evidence, and focused guidance.
</objective>

## Artifacts this phase produces

- Explicit-final completion markers in Mermaid and PlantUML, with no marker for ordinary sinks.
- Distinct internal and external self-edge labels retaining existing trigger, guard, and priority information.
- Stable escaped output and exact result-limit proof, including composed Mermaid output.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-semantic-diagnostics-visualization/31-CONTEXT.md
@.planning/phases/31-semantic-diagnostics-visualization/31-RESEARCH.md
@.planning/phases/31-semantic-diagnostics-visualization/31-PATTERNS.md
@.planning/phases/31-semantic-diagnostics-visualization/31-VALIDATION.md
@.planning/phases/31-semantic-diagnostics-visualization/31-01-SUMMARY.md

<interfaces>
`_to_mermaid_from_snapshot()` and `_to_plantuml_from_snapshot()` take one `_DiagnosticGraph` and `_DiagnosticBudget`; `_append_rendered_line()` reserves each physical line. Both use `_opaque_state_ids()` and `_transition_label()`. Mermaid currently has no final marker; PlantUML currently derives a marker from no outgoing edges. Plan 31-01 supplies `graph.state_finals` and `edge.internal`. `to_mermaid_fenced()` and `generate_visualization_document()` compose private renderers and must share the operation ledger.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mark explicit finals and distinguish the two self-transition modes</name>
  <files>src/fast_fsm/visualization.py, tests/test_visualization.py</files>
  <read_first>
    - `31-CONTEXT.md` D-01, D-04, D-07, D-08, D-09
    - `src/fast_fsm/visualization.py` both private renderers, `_transition_label()`, `_opaque_state_ids()`, and escapers
    - `tests/test_visualization.py` current PlantUML sink-arrow and Mermaid edge expectations
    - `src/fast_fsm/_diagnostics.py` Plan 31-01 scalar graph fields
  </read_first>
  <behavior>
    - "Both languages draw exactly one end arrow for each explicit final, including initial-only final; neither draws an end arrow for a reachable or unreachable non-final sink."
    - "Internal self is labeled `internal`; external self is labeled `external self`; non-self external edges retain established label notation."
    - "Each edge still shows its escaped trigger, optional escaped guard name, and exact priority in deterministic snapshot order."
  </behavior>
  <action>
Start RED by replacing old PlantUML sink-marker expectations and adding Mermaid marker assertions for explicit final, non-final sink, unreachable sink, and initial-only final. Per D-07, iterate captured `graph.state_finals` in state order and append a completion arrow only for true entries in both renderers; remove PlantUML's has-outgoing completion inference. Per D-08, specialize only self-edge labels: append fixed wording `internal` when `edge.internal` is true and `external self` when a self edge is external; leave ordinary non-self external label unchanged. Preserve the existing trigger/guard/priority formatter and opaque `sN` IDs. Reserve work and line results before each new marker or label-bearing edge; no live machine read or second snapshot is allowed (D-04/D-09).
  </action>
  <verify><automated>uv run pytest tests/test_visualization.py -x -q -k "mermaid or plantuml or final or terminal"</automated></verify>
  <acceptance_criteria>
    - "DIAG-02/D-07: final markers are emitted only for explicit finals in both languages, including the initial-only case."
    - "DIAG-02/D-08: both self modes, ordinary edges, priority, and optional guard names are unambiguous."
    - "DIAG-03/D-09: each new physical line is reserved before append and follows captured order."
  </acceptance_criteria>
  <done>Both text diagrams display explicit completion and self-edge intent without changing ordinary edge notation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove escaping and exact budgets, then describe the output contract</name>
  <files>src/fast_fsm/visualization.py, tests/test_output_safety.py, tests/test_visualization.py, docs/api/visualization.md, .specify/memory/spr-visualization.md</files>
  <read_first>
    - `31-VALIDATION.md` matrices 4 and 5; T-31-02/T-31-03
    - `tests/test_output_safety.py` hostile text, one-snapshot, result-budget, and composed-output tests
    - `src/fast_fsm/visualization.py` language-specific escapers, fenced renderer, Markdown document, and row reservation helper
    - `docs/api/visualization.md` current renderer/JSON contract
    - `.specify/memory/spr-visualization.md` existing output safeguards
  </read_first>
  <behavior>
    - "Hostile state names, trigger strings, guard names, and titles remain inert one-line Mermaid/PlantUML/Markdown text; state identifiers remain opaque positions."
    - "Repeated renders of an unchanged graph produce identical text, including marker and edge ordering."
    - "For each renderer and composed Mermaid document, exact max_results succeeds and one less raises `DiagnosticBudgetExceeded` without a partial returned string."
  </behavior>
  <action>
Use the existing hostile-text corpus and add final-marker/self-label cases with delimiter, newline, Unicode, and apparent diagram syntax. Assert both language-specific escapes and opaque IDs, including title and optional guard-name paths (D-08). Measure actual successful line counts, then test exact and one-less result limits for Mermaid, PlantUML, fenced Mermaid, and the composed document; test work-limit behavior for added marker visits. Correct any budget bypass by placing reservations before append and retaining one shared ledger for composed output (D-09). Update `docs/api/visualization.md` and the living SPR with completion-marker semantics, self-mode wording, JSON additions from Plan 31-01, deterministic ordering, escaping, and budget failure behavior; keep Phase 32's tutorial out of this reference work.
  </action>
  <verify><automated>uv run pytest tests/test_visualization.py tests/test_output_safety.py -x -q</automated></verify>
  <acceptance_criteria>
    - "DIAG-02/D-07/D-08: hostile caller text cannot create a marker or alter the intended edge, and both diagram forms preserve all relevant metadata."
    - "DIAG-03/D-09: exact and one-less work/result limits cover new marker rows and composed output; no partial diagram is returned."
    - "Public reference and SPR state the final/sink distinction, labels, JSON schema, and output safeguards."
  </acceptance_criteria>
  <done>Diagram and composed output semantics are stable, escaped, exactly bounded, and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-controlled labels → diagram/Markdown text | Untrusted names, triggers, guards, and titles can be interpreted as syntax by renderers. |
| Large graph → rendered output | Physical lines and traversal work must fit one finite diagnostic budget. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-02 | Tampering | Mermaid/PlantUML/Markdown | medium | mitigate | Preserve opaque IDs and sink-specific escaping; assert hostile corpus cannot add diagram syntax. |
| T-31-03 | Denial of service | diagram and composed document | medium | mitigate | Reserve work and each physical line in the shared ledger; exact/one-less tests assert bounded exception. |
| T-31-01 | Tampering | completion markers | medium | mitigate | Draw end arrows from captured final flags only; test both non-final sink reachability cases. |
</threat_model>

<verification>
Targeted renderer and output-safety modules pass. Plan 31-05 performs the single strict docs build and repeats the semantic matrix under pure and fresh native origins.
</verification>

<success_criteria>
Mermaid and PlantUML reveal deliberate finality and mode while preserving safe, deterministic, budgeted output.
</success_criteria>

<output>
Create `.planning/phases/31-semantic-diagnostics-visualization/31-03-SUMMARY.md` when done.
</output>
