---
phase: 31-semantic-diagnostics-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - src/fast_fsm/core.pyi
  - src/fast_fsm/_diagnostics.py
  - src/fast_fsm/visualization.py
  - tests/test_diagnostic_contracts.py
autonomous: true
requirements: [DIAG-01, DIAG-03]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DIAG-01/D-01/D-04: one captured graph records immutable explicit final flags and selected edge mode as scalars."
    - "DIAG-01/D-02/D-05: JSON adds explicit final states, non-final sinks, and intelligible transition modes while legacy terminal and topology keys retain their meanings."
    - "DIAG-03/D-09: JSON ordering is deterministic and every new diagnostic entry consumes the same operation budget before output."
  artifacts:
    - path: src/fast_fsm/core.py
      provides: frozen snapshot final flags captured alongside state names
    - path: src/fast_fsm/_diagnostics.py
      provides: scalar final and internal projection shared by cold diagnostics
    - path: src/fast_fsm/visualization.py
      provides: additive JSON semantic fields from one graph
    - path: tests/test_diagnostic_contracts.py
      provides: end-to-end and exact-limit JSON semantic oracle
  key_links:
    - from: src/fast_fsm/core.py
      to: src/fast_fsm/_diagnostics.py
      via: _graph_from_snapshot copies state_finals and edge.internal from one _GraphSnapshot
    - from: src/fast_fsm/_diagnostics.py
      to: src/fast_fsm/visualization.py
      via: _to_json_from_snapshot consumes scalar graph and one _DiagnosticBudget
---

<objective>
Make one canonical final/internal graph visible end to end in diagnostic JSON while preserving topology compatibility and bounded output.

Purpose: The scalar graph is the shared truth boundary for subsequent validation and diagram plans.
Output: Snapshot final flags, diagnostic edge mode, additive JSON semantics, and focused capture/budget evidence.
</objective>

## Artifacts this phase produces

- A frozen snapshot and interpreted graph containing explicit finality and transition mode from one owned capture.
- JSON with `topology.final_states`, per-transition `mode`, and `analysis.reachability.non_final_sinks`, alongside unchanged legacy keys.
- Deterministic, exact-budget tests for this first complete capture-to-public-output path.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-semantic-diagnostics-visualization/31-CONTEXT.md
@.planning/phases/31-semantic-diagnostics-visualization/31-RESEARCH.md
@.planning/phases/31-semantic-diagnostics-visualization/31-PATTERNS.md
@.planning/phases/31-semantic-diagnostics-visualization/31-VALIDATION.md

<interfaces>
`_GraphSnapshot` is frozen/slotted and currently contains `state_names: Tuple[str, ...]`; `_GraphTransition.internal: bool` already exists. `_graph_snapshot_owned()` sorts states and source/trigger groups under machine ownership. `_DiagnosticGraph` has `state_names`, `edges`, `forward`, and `reverse`; `_DiagnosticEdge` currently omits mode. `to_json()` calls `_capture_diagnostic_graph()` exactly once, then `_to_json_from_snapshot()` with one `_DiagnosticBudget`. Preserve `topology.states`, `topology.transitions`, sparse/dense adjacency, and `analysis.reachability.terminal` as existing compatibility surfaces.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace explicit finality and internal mode from capture to JSON</name>
  <reversibility rating="costly">The additive JSON schema and retained topology key meanings are public diagnostic contracts.</reversibility>
  <files>src/fast_fsm/core.py, src/fast_fsm/core.pyi, src/fast_fsm/_diagnostics.py, src/fast_fsm/visualization.py, tests/test_diagnostic_contracts.py</files>
  <read_first>
    - `31-CONTEXT.md` D-01, D-02, D-04, D-05, D-09 and deferred boundary
    - `src/fast_fsm/core.py` `_GraphSnapshot`, `_GraphTransition`, `_graph_snapshot_owned()`
    - `src/fast_fsm/core.pyi` matching private snapshot record
    - `src/fast_fsm/_diagnostics.py` `_DiagnosticGraph`, `_DiagnosticEdge`, `_graph_from_snapshot()`, `_DiagnosticBudget`
    - `src/fast_fsm/visualization.py` `_capture_diagnostic_graph()`, `_to_json_from_snapshot()`, `to_json()`
    - `tests/test_diagnostic_contracts.py` existing snapshot and JSON capture tests
  </read_first>
  <behavior>
    - "A machine with a reachable explicit final, a non-final sink, an internal self edge, and an external self edge yields the original topology keys plus `topology.final_states`, `analysis.reachability.non_final_sinks`, and transition modes `internal`, `external_self`, or `external`."
    - "Legacy `analysis.reachability.terminal` still lists every no-outgoing state, including an explicit final; `non_final_sinks` excludes explicit finals even when unreachable."
    - "An internal self edge and an external self edge differ even when source and target names match."
  </behavior>
  <action>
Start with a failing public `to_json()` test for the graph in the behavior block. Per D-01/D-04, add `state_finals: Tuple[bool, ...]` parallel to `state_names` to `_GraphSnapshot` and its stub, populated from canonical States inside `_graph_snapshot_owned()`; copy it into `_DiagnosticGraph` and copy existing `_GraphTransition.internal` into `_DiagnosticEdge`. Do not add mutable finality to `TransitionResult`, `TransitionRecord`, or public `snapshot()` (D-03), and keep `core.py` free of interpreted-diagnostic imports. In `_to_json_from_snapshot()` add ordered `topology.final_states: list[str]`, ordered `analysis.reachability.non_final_sinks: list[str]`, and a `mode` string on each canonical `topology.transitions` row: `internal` for internal self, `external_self` for external self, `external` for other edges (D-02/D-05). Preserve existing `terminal`, sparse/dense adjacency row shapes, graph order, and one capture/ledger. Reserve work and result counts before every newly emitted list entry or transition row (D-09). The tracer is complete when the one public JSON call returns the correct full graph, not merely a private tuple.
  </action>
  <verify><automated>uv run pytest tests/test_diagnostic_contracts.py -x -q -k "snapshot or json"</automated></verify>
  <acceptance_criteria>
    - "DIAG-01/D-01/D-04: captured final bits and edge modes survive through the public JSON call without rereading live topology."
    - "DIAG-01/D-02/D-05: explicit final, non-final sink, internal self, external self, and ordinary edge are distinguishable; legacy JSON keys retain compatible values."
    - "DIAG-03/D-09: each added list item or transition row is reserved before publication."
  </acceptance_criteria>
  <done>The first real graph renders its semantic truth through `to_json()` with deterministic fields and existing budget behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock capture isolation, JSON ordering, and exact budget thresholds</name>
  <reversibility rating="costly">Exact JSON field and ordering behavior becomes a stable serialized evidence contract.</reversibility>
  <files>tests/test_diagnostic_contracts.py, src/fast_fsm/visualization.py</files>
  <read_first>
    - `31-VALIDATION.md` matrices 1, 2, and 5; T-31-01 and T-31-03
    - `tests/test_diagnostic_contracts.py` late-mutation, hash-seed, JSON, and budget tests
    - `src/fast_fsm/visualization.py` JSON row/collection reservation sites after Task 1
    - `src/fast_fsm/_diagnostics.py` budget status and exact `current + amount > limit` rule
  </read_first>
  <behavior>
    - "Late mutation after capture cannot change final flags, transition modes, state ordering, or the returned JSON; repeated calls on an unchanged graph serialize identically."
    - "An initial-only explicit final yields `terminal` and `final_states` with that state, an empty `non_final_sinks` list, and no invented outgoing edge."
    - "For each added JSON result entry, the measured exact max_results succeeds and one less raises `DiagnosticBudgetExceeded` with scalar status and no returned partial object; exact and one-less max_work behave likewise."
  </behavior>
  <action>
Extend the existing snapshot/late-mutation tests to assert copied final flags and internal mode, including initial-only final and unreachable non-final sink cases (D-01/D-04). Test stable `json.dumps(to_json(...))` order for states, transitions, and named lists under repeated capture; retain dense adjacency compatibility and its existing exact-row validation (D-05). Measure the updated one-operation work/result counters from a successful call and test the exact value and one lower rather than embedding guessed totals. If any new JSON collection bypasses the ledger, correct the JSON renderer and rerun the focused test. Assert a budget failure raises the existing bounded exception rather than returning a partial dictionary (D-09). Keep this as cold tooling; no selector, lifecycle, or `trigger*()` change belongs here.
  </action>
  <verify><automated>uv run pytest tests/test_diagnostic_contracts.py -x -q</automated></verify>
  <acceptance_criteria>
    - "DIAG-03/D-04: the captured final/mode scalars remain stable under a later live graph mutation and JSON order is repeatable."
    - "DIAG-01/D-01/D-02: initial-only final and non-final sink cases retain distinct completion/topology meaning."
    - "DIAG-03/D-05/D-09: exact and one-less work/result limits exercise all added output and fail without a partial return."
  </acceptance_criteria>
  <done>JSON semantics are single-capture, stable, serializable, and exactly budgeted across boundary cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical machine → cold diagnostics | A mutable machine and caller-named topology become one immutable scalar graph; mixed epochs could misstate completion. |
| Caller topology → JSON consumer | Large or hostile graph data crosses a bounded serialization surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | snapshot/diagnostic graph | medium | mitigate | Copy final flags and mode under one owned capture; assert post-capture mutation does not alter output. |
| T-31-03 | Denial of service | JSON builder | medium | mitigate | Reserve graph work and every added result entry in one ledger; assert exact/one-less limits raise bounded status. |
| T-31-05 | Information disclosure | JSON labels | low | accept | JSON is an explicit caller-requested serialization of that caller's own named machine; it adds no exception or payload values. |
</threat_model>

<verification>
Focused JSON tests pass after each task; Plan 31-05 owns the pure/native and full-suite closure.
</verification>

<success_criteria>
One public `to_json()` call conveys explicit finality and transition mode from a single capture; legacy topology keys remain truthful and budgets reject excess work/output.
</success_criteria>

<output>
Create `.planning/phases/31-semantic-diagnostics-visualization/31-01-SUMMARY.md` when done.
</output>
