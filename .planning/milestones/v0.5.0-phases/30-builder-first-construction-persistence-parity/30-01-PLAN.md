---
phase: 30-builder-first-construction-persistence-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - src/fast_fsm/core.pyi
  - tests/test_construction_parity.py
  - tests/test_builder.py
  - tests/test_graph_invariants.py
  - tests/test_mypyc_guard.py
autonomous: true
requirements:
  - BUILD-02
  - BUILD-06
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BUILD-02/D-05/D-06: one topology-complete @transition declaration added to FSMBuilder becomes an ordinary canonical transition without a mirrored add_transition call."
    - "D-07: the generated request carries priority, timing, endpoints, and exact internal mode, while the decorator guard remains state-owned and executes exactly once."
    - "D-04/D-18: explicit and derived rows are submitted together through one prepare-all/publish-once transaction; failed builds do not mutate builder staging or cache a candidate."
    - "T-30-01/T-30-02: decorator metadata is deeply immutable and no guard is copied into both entry and handler seams."
    - "T-30-08: declaration import is construction-only; selectors, lifecycle runners, and trigger regions remain free of builder/import work."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Immutable mode-aware declarative metadata and build-time canonical request derivation"
    - path: "src/fast_fsm/core.pyi"
      provides: "Public keyword-only transition(..., internal: bool = False) contract"
    - path: "tests/test_construction_parity.py"
      provides: "Central Phase 30 observable-semantics oracle with a first declarative-builder slice"
    - path: "tests/test_mypyc_guard.py"
      provides: "Exact frozen/slotted declarative carrier and stub/runtime layout checks"
    - path: "tests/test_graph_invariants.py"
      provides: "Canonical-transaction and cold-path structural proof"
  key_links:
    - from: "@transition metadata"
      to: "FSMBuilder.build()"
      via: "fresh derivation from staged state-owned immutable handler tuples"
      pattern: "_DeclarativeHandlerMetadata|_DeclarativeHandler"
    - from: "FSMBuilder.build()"
      to: "StateMachine._apply_transition_requests_owned()"
      via: "one tuple containing both explicit and declarative _TransitionRequest rows"
      pattern: "_apply_transition_requests_owned"
    - from: "selected TransitionEntry"
      to: "declarative handler resolution"
      via: "source/trigger/target/priority/internal identity match without a copied guard"
      pattern: "entry.internal"
  flagged_assumptions:
    - id: "BUILD-02"
      status: "flagged-resolved"
      resolution: "The spec-less probe was unresolved; D-05 through D-08 lock builder import, state-owned guards, and direct-handle compatibility."
      acceptance: "A decorated state builds and dispatches through FSMBuilder with no mirrored registration, no second registrar, and one guard/handler invocation."
    - id: "BUILD-06"
      status: "flagged-resolved"
      resolution: "The spec-less probe was unresolved; D-07, D-18, D-19, and D-20 require canonical validation and parity evidence."
      acceptance: "The declarative adapter produces the same final/internal validation and atomic-failure fingerprint as explicit builder registration."
  prohibitions:
    - statement: "Must not create a second topology engine or parallel declarative registrar."
      status: "flagged-unverified"
    - statement: "Must not move construction, deprecation, or persistence policy into dispatch."
      status: "flagged-unverified"
    - statement: "Must not remove simple_fsm, quick_fsm, StateMachine.quick_build, or StateMachine.from_states during v0.5.x."
      status: "flagged-unverified"
    - statement: "Must not claim that pre-v0.5 readers preserve new final/internal semantics."
      status: "flagged-unverified"
---

<objective>
Prove the Phase 30 architecture with one production-quality declarative-builder path, then lock its canonical, immutable, exactly-once structure.

Purpose: Resolve the hardest phase constraint first: decorator-authored topology must enter the existing builder transaction without creating a second registrar or evaluating policy twice.
Output: A central parity oracle, mode-aware immutable declaration records, one build-time request derivation path, synchronized typing, and structural cold-path evidence.
</objective>

## Artifacts this phase produces

- A reusable all-adapter observable-semantics oracle, initially populated with the declarative-builder tracer.
- Immutable declarative metadata/handler records carrying canonical transition mode.
- Builder cold-path derivation of topology-complete declarations into `_TransitionRequest` rows.
- Runtime/stub/AST proof that the canonical transaction is the only publication seam and dispatch remains unchanged.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-PATTERNS.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md
@.planning/phases/26-canonical-construction-evidence-contract/26-VERIFICATION.md
@.planning/phases/28-same-state-transition-modes/28-VERIFICATION.md

<interfaces>
Existing contracts to consume directly:
- `_TransitionRequest(trigger, sources, to_state, condition, unless, priority, condition_ref, after, within, internal)` retains raw values until exact canonical validation.
- `StateMachine._apply_transition_requests_owned(tuple[...])` prepares every row, merges replacement slots off-table, and publishes once.
- `FSMBuilder.build()` creates a private candidate, binds staged explicit requests, applies one complete tuple, wires callbacks, then caches the candidate.
- `_DeclarativeHandlerMetadata` and `_DeclarativeHandler` are frozen/slotted declaration carriers; state-owned handlers remain the execution seam.
- Machine-owned declarative resolution already matches canonical source, trigger, target, and priority and must add selected-entry mode.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one internal declarative transition from decorator through builder to dispatch</name>
  <reversibility rating="costly">Per D-05/D-06, decorator metadata and builder import become public authoring behavior whose later reversal would require coordinated runtime, typing, examples, and user-code changes.</reversibility>
  <files>src/fast_fsm/core.py, src/fast_fsm/core.pyi, tests/test_construction_parity.py, tests/test_builder.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-04 through D-08 and D-18 through D-20
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md` Patterns 1/2/5, Pitfalls 1/2/3, and Required Test Matrix items 1 through 3
    - `src/fast_fsm/core.py:823-889` private request and declarative carrier fields
    - `src/fast_fsm/core.py:5591-5701` transition decorator and mode-blind resolver
    - `src/fast_fsm/core.py:5892-6148` declarative discovery/dispatch ownership
    - `src/fast_fsm/core.py:6158-6646` builder staging, async preflight, candidate transaction, and cache publication
    - `src/fast_fsm/core.pyi:371-433` decorator, declarative states, and builder public typing
    - `tests/test_builder.py:80-170,461-905,2382-2405` fingerprints, current declarative behavior, and manually mirrored topology analogs
    - `tests/test_transition_modes.py:90-220,546-670` observable internal/external lifecycle traces
  </read_first>
  <behavior>
    - "A topology-complete declaration on the initial DeclarativeState builds through FSMBuilder without any explicit add_transition mirror."
    - "@transition accepts keyword-only internal with an exact built-in bool contract; omitted/False remains external and True is carried to the selected entry."
    - "An internal self-transition returns internal=True, runs transition/declarative behavior once, and skips state exit/entry lifecycle."
    - "A stateful declarative guard and its handler each execute exactly once per committed attempt; the generated request condition remains None."
    - "The successful builder remains cached; its caller-owned state identity is retained."
  </behavior>
  <action>
Start RED by adding `tests/test_construction_parity.py` with one small oracle scenario: a `DeclarativeState` owns an internal self-transition with counters for guard, handler, transition callback, and lifecycle; construct only through `FSMBuilder(initial).build()`, dispatch once, and assert selected mode, state identity, counters, history/result truth, and cached repeat build. Add focused decorator exact-bool/default tests in `tests/test_builder.py`.

Implement per D-05/D-06/D-07. Add raw `internal` to both frozen/slotted declarative record layouts, deep-freeze plural `from_state` collections at the public decoration boundary, and synchronize the keyword-only stub. During `FSMBuilder.build()`, derive topology-complete declaration rows afresh from each exact staged declarative state, bind destination names through the staged state registry, concatenate them with the endpoint-bound explicit requests, and call `_apply_transition_requests_owned()` once. Do not append derived rows to builder staging. Carry priority, `after`, `within`, and `internal`; leave the declaration guard on `_DeclarativeHandler.condition` so the transition entry is unguarded by that same callable. Add mode to machine-owned handler identity/resolution and pass selected `entry.internal`; preserve unfiltered direct `handle_event*()` compatibility for D-08 when unambiguous. GREEN requires the tracer and targeted builder tests to pass; then refactor only within these seams while retaining frozen/slotted carriers and the private candidate/cache order.
  </action>
  <acceptance_criteria>
    - "D-05/D-06: the public decorator/stub and both immutable carrier records include exact keyword-only internal mode, and caller-owned plural sources cannot mutate stored metadata."
    - "D-07/T-30-02: the tracer proves one builder-authored edge, one guard invocation, one handler invocation, and selected internal lifecycle truth without copied guard execution."
    - "D-04/D-18: explicit plus derived requests enter one canonical apply call before callback/cache publication; derived rows never persist in builder staging."
    - "D-08: direct state event handling remains callable and no destination is invented for a destination-less declaration."
    - "No declarative registrar, second builder, or dispatch-time import branch is introduced."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_construction_parity.py tests/test_builder.py -x -q -k "declarative and (builder or internal or exactly_once or immutable or direct_handle)"</automated>
  </verify>
  <done>The thinnest real decorated-state path works end-to-end through the builder and canonical machine transaction with exact mode and exactly-once execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock declaration layout, canonical publication, and hot-path absence</name>
  <files>src/fast_fsm/core.py, src/fast_fsm/core.pyi, tests/test_graph_invariants.py, tests/test_mypyc_guard.py, tests/test_construction_parity.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md` rows 30-W0-01/02, Required Validation Matrices 1/2/3/7, and T-30-01/T-30-02/T-30-07/T-30-08
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-PATTERNS.md` graph-invariant and mypyc-guard assignments
    - `tests/test_graph_invariants.py:310-354,763-887,1028-1050` adapter transaction, atomicity, and hot-path source-slicing patterns
    - `tests/test_mypyc_guard.py:490-590` exact frozen/slotted record field assertions
    - `src/fast_fsm/core.py:1890-2116` canonical normalization, apply, merge, and publication authority
    - `src/fast_fsm/core.py` sync/async selectors, lifecycle runners, and trigger regions named by existing structural tests
  </read_first>
  <behavior>
    - "Runtime and stub declaration layouts agree and both private declaration records remain frozen and slotted."
    - "Builder declaration derivation contains one canonical apply and no direct normalize, commit, published-slot repair, or alternate registry."
    - "The public exact-bool boundary rejects bool-like values before staging and behaves identically in interpreted tests."
    - "Selectors, lifecycle runners, trigger methods, and hot-path carrier layouts contain no reflection, declaration import, warning, or persistence work."
  </behavior>
  <action>
Write structural assertions before any follow-up implementation. Extend the exact AST/dataclass field sets in `tests/test_mypyc_guard.py` for `internal` and assert the decorator runtime signature, stub signature, frozen/slots options, and raw-object-before-normalization boundary. Extend the retained-adapter source-region test in `tests/test_graph_invariants.py` to cover declarative derivation inside builder build: exactly one `_apply_transition_requests_owned` call for the complete operation, no direct `_normalize_transition_request`/`_commit_transition_plan`, no post-publication repair, and no second topology table. Add selector/lifecycle/trigger absence checks for decorator discovery/import symbols and warnings.

Strengthen the tracer oracle with atomic invalid-internal and final-source cases that compare builder staging/cache fingerprints before and after failure. Keep errors bounded and do not format arbitrary state/guard payloads. Run focused structural tests, Ruff only on the declared Python files, and blocking mypy after the tests pass.
  </action>
  <acceptance_criteria>
    - "T-30-01: frozen/slotted exact field tests include mode and prove defensive tuple copying for plural source metadata."
    - "T-30-07: declarative rows reach the same exact final/internal normalizer and a late invalid row changes no graph version, topology, builder staging, or cache."
    - "T-30-08: construction-only symbols are absent from selector, lifecycle, and trigger regions."
    - "Runtime, PEP 561 stub, and mypyc structural authorities agree without widening core.py's hot-path objects."
    - "Targeted tests, Ruff validation, and blocking mypy pass."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_construction_parity.py tests/test_graph_invariants.py tests/test_mypyc_guard.py -x -q -k "declarative or construction or adapter or hot_path" &amp;&amp; uv run ruff format --check src/fast_fsm/core.py src/fast_fsm/core.pyi tests/test_construction_parity.py tests/test_builder.py tests/test_graph_invariants.py tests/test_mypyc_guard.py &amp;&amp; uv run ruff check src/fast_fsm/core.py src/fast_fsm/core.pyi tests/test_construction_parity.py tests/test_builder.py tests/test_graph_invariants.py tests/test_mypyc_guard.py &amp;&amp; task typecheck-mypy</automated>
  </verify>
  <done>The declarative tracer is backed by exact layout, transaction, atomicity, and hot-path structural evidence suitable for later adapter expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decorator arguments/caller collections → immutable metadata | Public exact scalars and mutable source collections must be normalized without retaining attacker-controlled mutation. |
| State-owned declarations → builder private candidate | Derived topology must use canonical validation once without duplicating executable guard policy. |
| Construction cold path → runtime dispatch | Reflection/import work must not cross into the throughput-sensitive selector/lifecycle/trigger boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Tampering | declarative metadata | high | mitigate | Exact bool validation, defensive tuple copies, frozen/slotted layouts, and caller-list mutation tests. |
| T-30-02 | Tampering | declarative guard ownership | high | mitigate | Keep guards on handlers, generate topology scalars only, and assert one guard/handler invocation. |
| T-30-07 | Tampering | builder declarative adapter | high | mitigate | Submit derived and explicit rows through one canonical transaction and compare failure fingerprints. |
| T-30-08 | Denial of Service | dispatch hot path | high | mitigate | Structural source-region exclusions for import/reflection/warning/persistence work. |
</threat_model>

<verification>
- `uv run pytest tests/test_construction_parity.py tests/test_builder.py tests/test_graph_invariants.py tests/test_mypyc_guard.py -x -q -k "declarative or construction or adapter or hot_path"`
- `uv run ruff format --check` and `uv run ruff check` on Plan 30-01 Python files
- `task typecheck-mypy`
- `task typecheck-ty` (visible advisory result; does not replace mypy)
</verification>

<success_criteria>
- A topology-complete decorator author can construct through `FSMBuilder` without mirrored registration.
- The canonical transaction remains the sole publication authority and the decorator guard executes once.
- Declaration mode/identity is synchronized across runtime, stub, AST, sync execution, and structural tests.
- Builder failure remains inspectable/repairable and successful build remains cached.
</success_criteria>

<output>
Create `.planning/phases/30-builder-first-construction-persistence-parity/30-01-SUMMARY.md` when done.
</output>
