---
phase: 30-builder-first-construction-persistence-parity
plan: 04
type: execute
wave: 4
depends_on:
  - 30-03
files_modified:
  - src/fast_fsm/core.py
  - tests/test_advanced_functionality.py
  - tests/test_final_states.py
  - tests/test_transition_modes.py
  - tests/test_graph_invariants.py
  - tests/test_construction_parity.py
autonomous: true
requirements:
  - BUILD-06
  - BUILD-07
  - BUILD-08
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-13/D-14: topology dictionaries retain string states/final_states, emit internal only when true, accept omitted/exact false as external, reject non-bool values exactly, and publish no malformed prefix."
    - "D-15: compatibility evidence and later guidance state only old-payload→new-reader and new-payload→new-reader safety; no schema version or symmetric old-reader claim is introduced."
    - "D-16/T-30-06: clone preserves concrete sync/async type, State/subclass/callable identity, final/internal semantics, and graph version while rebuilding independent mutable containers and resetting current/history."
    - "D-17: snapshot remains exactly state plus version 1; restore derives termination and transition mode from the receiving machine's existing topology."
    - "D-18/D-19/D-20: from_dict and clone join the same canonical observable-semantic/atomic-failure parity oracle as all earlier adapters."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Exact internal dictionary parsing/output plus retained clone/snapshot semantics"
    - path: "tests/test_advanced_functionality.py"
      provides: "Dictionary round-trip, exact-type, clone ownership, and snapshot schema matrix"
    - path: "tests/test_final_states.py"
      provides: "Finality through dictionary/clone/receiver-owned restore"
    - path: "tests/test_transition_modes.py"
      provides: "Internal mode through dictionary/clone and post-restore dispatch"
    - path: "tests/test_construction_parity.py"
      provides: "Deserialization and clone adapter parity cells"
  key_links:
    - from: "from_dict transition rows"
      to: "_TransitionRequest.internal"
      via: "complete exact parsing with indexed bounded context before one canonical apply"
      pattern: "from_dict: transition["
    - from: "TransitionEntry.internal"
      to: "to_dict transition row"
      via: "true-only JSON-native emission"
      pattern: "internal"
    - from: "source machine topology"
      to: "clone topology"
      via: "canonical request replay into independent entries/groups/tables/registries"
      pattern: "_clone_owned"
    - from: "snapshot state name"
      to: "receiving machine finality/mode"
      via: "restore changes only current canonical state; topology remains receiver-owned"
      pattern: "version"
  flagged_assumptions:
    - id: "BUILD-07"
      status: "flagged-resolved"
      resolution: "The spec-less probe was unresolved; D-13 through D-16 lock additive directional dictionary compatibility and clone ownership."
      acceptance: "JSON round trips and clones preserve final/internal semantics; legacy omissions default safely; clone containers are independent."
    - id: "BUILD-08"
      status: "flagged-resolved"
      resolution: "The spec-less probe was unresolved; D-17 locks exact state-only snapshot format v1 and receiver-owned topology truth."
      acceptance: "Snapshot remains exactly two keys and restore finality/mode changes with the receiving topology, not the snapshot payload."
  prohibitions:
    - statement: "Must not create a second topology engine or parallel declarative registrar."
      status: "flagged-unverified"
    - statement: "Must not move construction, deprecation, or persistence policy into dispatch."
      status: "flagged-unverified"
    - statement: "Must not remove simple_fsm, quick_fsm, StateMachine.quick_build, or StateMachine.from_states during v0.5.x."
      status: "flagged-unverified"
    - statement: "Must not claim that pre-v0.5 readers preserve new final/internal semantics."
      status: "flagged-unverified"
---

<objective>
Carry final-state and transition-mode meaning through exact dictionary reconstruction, cloning, and unchanged snapshot v1, then complete the retained-adapter parity matrix.

Purpose: Make persisted and reconstructed machines behaviorally equivalent without weakening exact validation, aliasing mutable clone state, or changing snapshot ownership.
Output: True-only internal serialization, exact atomic deserialization, clone isolation evidence, receiver-owned snapshot semantics, and all-adapter parity.
</objective>

## Artifacts this phase produces

- Directionally compatible topology dictionaries with exact true-only `internal` metadata.
- Complete pre-publication dictionary validation and indexed bounded errors.
- Clone semantic/identity/isolation proof for sync and async machines.
- Exact unchanged snapshot-v1 and receiver-owned restore proof.
- Deserialization and clone cells completing the retained construction parity oracle.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-PATTERNS.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-03-SUMMARY.md
@.planning/phases/27-explicit-final-states/27-VERIFICATION.md
@.planning/phases/28-same-state-transition-modes/28-VERIFICATION.md

<interfaces>
Existing contracts to preserve:
- `to_dict()` emits deterministic JSON-native topology with sorted top-level `final_states` and one row per candidate.
- `from_dict()` parses a private candidate, resolves only opaque condition references, builds one request tuple, and attaches indexed context to canonical errors.
- `_clone_owned()` reuses exact canonical State/callable identities but rebuilds transition and callback/listener containers through canonical requests.
- `snapshot()` is exactly `{state, version: 1}`; `restore()` changes current state only and validates version/state.
- The central parity oracle from Plans 30-01..03 already covers direct, batch, builder, declarative, callback, factory, and helper paths.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exact true-only dictionary mode round trips and atomic validation</name>
  <reversibility rating="one-way">D-13 makes emitted dictionary fields a persisted interoperability contract; reversibility gates are disabled because the locked context already authorizes this additive shape.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_advanced_functionality.py, tests/test_final_states.py, tests/test_transition_modes.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-03 and D-13 through D-15
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md` Pattern 4, Pitfalls 5/6, Required Test Matrix item 5, and T-30-04/T-30-05/T-30-07
    - `src/fast_fsm/core.py:1362-1709` complete dictionary parser, condition-reference resolution, canonical apply, and deterministic output
    - `tests/test_advanced_functionality.py:1348-1595,1912-2005,2227-2410` from_dict/to_dict/condition/priority serialization patterns
    - `tests/test_final_states.py:352-400` final dictionary validation and async reconstruction
    - `tests/test_transition_modes.py` internal result/history/lifecycle cases reusable after reconstruction
  </read_first>
  <behavior>
    - "External transition output remains byte-shape compatible with no internal field; internal entries emit internal: true only."
    - "Missing or exact false input reconstructs external behavior; exact true reconstructs internal behavior; 0, 1, strings, subclasses, and truthy objects fail before candidate publication."
    - "Exact built-in config/list/string/bool checks produce stable transition[index]/field context without payload repr."
    - "Final_states, priority groups, timing fields, condition_ref identity, and internal mode survive JSON dumps/loads plus from_dict/to_dict round trips."
    - "Unknown names, outgoing final sources, internal non-self targets, and a malformed final row return no partial machine or graph-version prefix in sync and async classmethods."
  </behavior>
  <action>
Write the dictionary matrix first. Preserve existing external-row expectations exactly, add true-only internal output and JSON round-trip behavior, accept only omitted/exact False/exact True, and parameterize bool-like/type/container negatives with indexed bounded messages. Include final/internal/priority/timing/reference combinations plus inherited `AsyncStateMachine.from_dict()` behavior. Test semantic lifecycle after reconstruction, not only dictionary equality.

In `from_dict`, parse and exact-validate the complete document into local rows before candidate publication. Keep untrusted scalar implementation values as object/Any until `type(value) is bool`; map omission or exact false to external and exact true to internal. Continue ignoring unknown extra keys unless an existing rule rejects them. Resolve executable guards only from the caller's opaque condition registry, construct one complete `_TransitionRequest` tuple with indexed contexts, and call the canonical apply once. In `to_dict`, retain existing keys/order and append `internal: True` only for an internal entry. Add no schema-version field and no executable serialization.
  </action>
  <acceptance_criteria>
    - "D-13: deterministic old external rows stay unchanged; new internal rows emit only JSON Boolean true; legacy omissions and explicit false reconstruct external mode."
    - "D-14/T-30-05: the complete exact-type/semantic document validates before one publication; invalid late rows expose no machine/prefix/callback detail."
    - "D-15: no schema version or old-reader semantic guarantee is encoded or asserted."
    - "T-30-04/T-30-07: indexed errors are bounded/payload-free and canonical final/internal rules are not duplicated or weakened."
    - "Round-trip tests dispatch and observe correct lifecycle/finality/mode in sync and async machines."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_advanced_functionality.py tests/test_final_states.py tests/test_transition_modes.py -x -q -k "from_dict or to_dict or roundtrip or serialization or legacy or internal or final" &amp;&amp; task typecheck-mypy</automated>
  </verify>
  <done>Topology dictionaries preserve final/internal meaning with exact directional compatibility and atomic bounded reconstruction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove clone isolation, snapshot-v1 ownership, and complete adapter parity</name>
  <reversibility rating="costly">Clone identity/container ownership and snapshot format are public behavioral contracts relied on by later diagnostics and installed-artifact proof.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_advanced_functionality.py, tests/test_final_states.py, tests/test_graph_invariants.py, tests/test_construction_parity.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-16 through D-20
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md` Clone/Snapshot pattern, Pitfall 7, Required Test Matrix items 6/7, and T-30-06/T-30-07/T-30-08
    - `src/fast_fsm/core.py:3578-3711` snapshot/restore/clone ownership and canonical replay
    - `src/fast_fsm/core.py:4532-4760` async clone layering and ownership registries
    - `tests/test_advanced_functionality.py:857-1110,1316-1345,1762-1778,2172-2220` snapshot, clone, callback, history, and ownership tests
    - `tests/test_graph_invariants.py:205-307,402-430,748-775` clone container/identity, snapshot schema, version, and internal-mode proof
    - `tests/test_construction_parity.py` current adapter oracle and semantic fingerprint
  </read_first>
  <behavior>
    - "Clone keeps concrete StateMachine/AsyncStateMachine type, exact State/CallbackState/DeclarativeState objects, final marker, callable collaborators, entry semantics, and graph version."
    - "Clone owns distinct transition tables, slots, entries/groups, callback/listener lists, locks/ownership primitives, current state, and history; it resets to initial with history disabled."
    - "Snapshot remains exactly two keys with version 1 and no final/internal/topology fields."
    - "Restoring the same state snapshot into different receiving topologies derives different finality/mode behavior from those machines."
    - "The completed oracle covers direct, batch, builder, factory, declarative, helper, callback, clone, and deserialization paths in sync/async variants and compares atomic failures."
  </behavior>
  <action>
Add clone assertions beyond topology equality: same state/subclass/callable identities, distinct mutable container identities at every table/slot/entry/group/callback/listener layer, concrete async type and fresh async ownership, source current/history not copied, initial reset, final/internal/priority/timing/reference semantics preserved, and source fingerprint unchanged after clone mutation or injected late reconstruction failure. Adjust `_clone_owned()` only where tests expose a gap; keep canonical request replay and set externally visible graph version only after complete success.

Lock snapshot output to the exact two-key v1 value. Construct receiving machines with the same named states but different final/internal topology, restore the same snapshot, and prove `is_terminated` plus subsequent mode/lifecycle come from the receiver, not serialized metadata. Add from_dict and clone cells to `tests/test_construction_parity.py`; include every retained adapter named by D-18 (including deprecated helpers captured with their expected warning) and compare behavior/failure fingerprints, not dictionary equality. Extend structural tests for one canonical apply and no dispatch policy. Do not redesign snapshot or serialize callbacks.
  </action>
  <acceptance_criteria>
    - "D-16/T-30-06: clone identity/semantic preservation and all mutable-container independence are executable for sync and async machines."
    - "D-17/BUILD-08: snapshot remains exact v1 state-only and receiver-owned finality/mode is proven with divergent topologies."
    - "D-18/D-19/BUILD-06: all retained adapter cells share canonical final/internal behavior and atomic failures."
    - "D-20/T-30-07/T-30-08: structural tests reject canonical bypass, graph-version mutation, post-publication repair, and dispatch-time construction work."
    - "No topology snapshot, executable callback serialization, or schema version is introduced."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_advanced_functionality.py tests/test_final_states.py tests/test_graph_invariants.py tests/test_construction_parity.py -x -q -k "clone or snapshot or restore or construction_parity or adapter or internal or final"</automated>
  </verify>
  <done>Clone, dictionary, and snapshot contracts preserve semantic truth and ownership while the all-adapter parity oracle is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON-native dictionary → private candidate | Untrusted containers/scalars/references must validate completely before topology publication. |
| Source machine → clone | Immutable collaborator identity may be shared, but every mutable topology/callback/ownership container must be independent. |
| Snapshot payload → receiving machine | The payload may select only a known state; topology/finality/mode remain receiver-owned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04 | Information Disclosure | dictionary errors | medium | mitigate | Stable indexed structural context without repr of payloads/callables/exceptions. |
| T-30-05 | Tampering | from_dict publication | high | mitigate | Exact complete parse and one canonical apply before candidate escape. |
| T-30-06 | Tampering | clone containers | medium | mitigate | Identity/isolation matrix for tables, entries, registries, and ownership primitives. |
| T-30-07 | Tampering | persistence/clone adapters | high | mitigate | Complete adapter parity oracle and canonical transaction structural assertions. |
| T-30-08 | Denial of Service | dispatch boundary | high | mitigate | Keep parsing/cloning/snapshot validation cold-path-only and verify source-region absence. |
</threat_model>

<verification>
- `uv run pytest tests/test_advanced_functionality.py tests/test_final_states.py tests/test_transition_modes.py tests/test_graph_invariants.py tests/test_construction_parity.py -x -q -k "from_dict or to_dict or roundtrip or clone or snapshot or restore or internal or final or construction_parity"`
- `uv run ruff format --check` and `uv run ruff check` on Plan 30-04 Python files
- `task typecheck-mypy`
- `task typecheck-ty` (visible advisory result)
</verification>

<success_criteria>
- BUILD-07 dictionary/clone semantics and directional legacy defaults are complete.
- BUILD-08 exact snapshot-v1 receiving-topology semantics are complete.
- BUILD-06 covers every retained adapter with observable semantics and atomic failure.
- Persistence and clone work remain cold-path-only and payload-safe.
</success_criteria>

<output>
Create `.planning/phases/30-builder-first-construction-persistence-parity/30-04-SUMMARY.md` when done.
</output>
