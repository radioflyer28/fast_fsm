---
phase: 30-builder-first-construction-persistence-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 30-02
files_modified:
  - src/fast_fsm/core.py
  - src/fast_fsm/core.pyi
  - src/fast_fsm/__init__.py
  - tests/test_builder.py
  - tests/test_final_states.py
  - tests/test_construction_parity.py
autonomous: true
requirements:
  - BUILD-03
  - BUILD-06
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09/D-11: each of the four deprecated public construction surfaces emits exactly one fixed bounded DeprecationWarning attributed to the user's call site, including validation failures."
    - "D-10: all four symbols remain exported, typed, callable, behaviorally correct, and supported throughout v0.5.x; earliest removal remains v0.6.0."
    - "Nested helper implementation uses private non-warning workers so public-to-public delegation cannot multiply warnings."
    - "BUILD-06/D-18/D-20: deprecated helpers remain in the adapter parity oracle and retain final/internal validation, caller identity, subclass result type, and atomic failure."
    - "T-30-04: warning and validation messages contain fixed structural guidance and never caller payload representations."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Four public warning boundaries and private non-warning compatibility workers"
    - path: "src/fast_fsm/core.pyi"
      provides: "Retained v0.5.x compatibility signatures"
    - path: "src/fast_fsm/__init__.py"
      provides: "Retained package exports"
    - path: "tests/test_builder.py"
      provides: "Exact warning category/count/text/location and behavior tests"
    - path: "tests/test_construction_parity.py"
      provides: "Deprecated helper final/internal/atomicity parity cells"
  key_links:
    - from: "simple_fsm / quick_fsm / quick_build / from_states"
      to: "private compatibility workers"
      via: "one warning at the public boundary, then non-warning delegation"
      pattern: "DeprecationWarning"
    - from: "private compatibility workers"
      to: "canonical construction path"
      via: "existing State identities and canonical request transaction where transitions exist"
      pattern: "_TransitionRequest"
    - from: "runtime symbols"
      to: "stub and package exports"
      via: "unchanged names/signatures during the compatibility cycle"
      pattern: "simple_fsm|quick_fsm|quick_build|from_states"
  flagged_assumptions:
    - id: "BUILD-03"
      status: "flagged-resolved"
      resolution: "The spec-less probe was unresolved; D-09 through D-12 lock the exact warning boundary, compatibility window, and documentation ownership."
      acceptance: "Every deprecated entry warns once at stacklevel=2 with fixed replacement text and returns the same correct machine through v0.5.x."
  prohibitions:
    - statement: "Must not create a second topology engine or parallel declarative registrar."
      status: "flagged-unverified"
    - statement: "Must not move construction, deprecation, or persistence policy into dispatch."
      status: "flagged-unverified"
    - statement: "Must not remove simple_fsm, quick_fsm, StateMachine.quick_build, or StateMachine.from_states during v0.5.x."
      status: "flagged-unverified"
    - statement: "Must not claim that pre-v0.5 readers preserve new final/internal semantics."
      status: "flagged-unverified"
---

<objective>
Introduce the complete warned compatibility cycle at the four legacy construction boundaries without changing their supported behavior or multiplying topology implementations.

Purpose: Give users actionable migration guidance while preserving v0.5.x compatibility, exact construction semantics, and package/type surfaces.
Output: Private non-warning workers, one user-attributed warning per public invocation, retained symbols/signatures, and helper parity evidence.
</objective>

## Artifacts this phase produces

- Fixed bounded deprecation messages for all four compatibility constructors.
- Private non-warning workers that preserve classmethod subclass behavior and avoid nested warnings.
- Exact call-site/category/count/error-path warning tests.
- Deprecated helper cells in the central final/internal construction parity oracle.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-PATTERNS.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md
@.planning/phases/30-builder-first-construction-persistence-parity/30-02-SUMMARY.md

<interfaces>
Existing public boundaries:
- `StateMachine.from_states(...)` and `StateMachine.quick_build(...)` are classmethods and must continue constructing through `cls`.
- `simple_fsm(...)` currently delegates to `StateMachine.from_states(...)`; `quick_fsm(...)` delegates to `StateMachine.quick_build(...)`.
- `quick_build`/`quick_fsm` transition rows already reach the canonical request transaction; `from_states`/`simple_fsm` are transition-free state constructors.
- `src/fast_fsm/core.pyi` and `src/fast_fsm/__init__.py` retain all four public names for the complete v0.5.x cycle.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exactly-once user-attributed warning boundaries</name>
  <reversibility rating="one-way">D-10 records the public compatibility window; once a released version removes these names the contract cannot be silently recreated, but this pre-authorized phase retains them and adds warnings without a checkpoint.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_builder.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-09 through D-12 and D-20
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-RESEARCH.md` Pattern 3, Pitfall 4, Required Test Matrix item 4, and T-30-03/T-30-04
    - `src/fast_fsm/core.py:1198-1359` classmethod construction implementations and current public-to-public nesting targets
    - `src/fast_fsm/core.py:6953-7012` module helper boundaries
    - `tests/test_builder.py` existing factory behavior, subclass/classmethod result, identity, and atomicity tests
  </read_first>
  <behavior>
    - "Each public invocation records exactly one DeprecationWarning under simplefilter(always), even when argument validation later raises."
    - "The warning filename/line resolves to the test caller, not core.py; message text is exact, fixed, bounded, and payload-free."
    - "Messages name FSMBuilder for programmatic construction, StateMachine.from_dict for serialized topology, v0.5.x support, and no-earlier-than-v0.6.0 removal."
    - "Calling simple_fsm or quick_fsm does not also surface from_states or quick_build warnings."
    - "Direct constructors, from_dict, State.create, FSMBuilder, and declarative state APIs remain silent."
  </behavior>
  <action>
Write exact warning tests first using `warnings.catch_warnings(record=True)` plus `simplefilter("always")`; assert category, count, exact text, test filename and line attribution, behavior on valid calls, and one warning before invalid-argument failure for each of `simple_fsm`, `quick_fsm`, `StateMachine.quick_build`, and `StateMachine.from_states`. Add negative controls for all nondeprecated surfaces named by D-09.

Refactor the current implementations into private non-warning workers (exact private names are discretionary) that preserve `cls` construction and current validation/identity/atomicity behavior. Each public boundary must perform one direct `warnings.warn(FIXED_CONSTANT, DeprecationWarning, stacklevel=2)` as its first operation and then call a non-warning worker. Module helpers must not call warning-emitting public classmethods. Keep the message constants bounded and free of caller values. Do not place warning checks in builder, normalization, selection, lifecycle, or trigger code.
  </action>
  <acceptance_criteria>
    - "D-09/D-11/T-30-03: every public entry emits exactly one fixed warning at the caller, including invalid invocations, with no nested warning."
    - "T-30-04: message and error tests prove no caller payload repr or callable detail is echoed."
    - "Nondeprecated construction and runtime surfaces emit no warning."
    - "Private workers preserve classmethod/subclass result construction and existing validation order."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_builder.py -x -q -k "deprecat or warning or quick_build or from_states or simple_fsm or quick_fsm"</automated>
  </verify>
  <done>The four public compatibility constructors warn once with actionable caller-attributed guidance and delegate only to non-warning workers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve helper API, typing, identity, and semantic parity through v0.5.x</name>
  <reversibility rating="costly">Retained exports and signatures are published compatibility surfaces and must change in lockstep with documentation and the earliest-removal policy.</reversibility>
  <files>src/fast_fsm/core.py, src/fast_fsm/core.pyi, src/fast_fsm/__init__.py, tests/test_final_states.py, tests/test_construction_parity.py</files>
  <read_first>
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-CONTEXT.md` D-10, D-18, and D-20
    - `.planning/phases/30-builder-first-construction-persistence-parity/30-VALIDATION.md` warning contract and adapter parity matrices
    - `src/fast_fsm/core.pyi:234-259,467-479` retained classmethod/module helper signatures
    - `src/fast_fsm/__init__.py:7-33,75-111` explicit imports and `__all__`
    - `tests/test_final_states.py:79-221` helper/factory final-state matrix
    - `tests/test_construction_parity.py` adapter oracle after Plan 30-02
  </read_first>
  <behavior>
    - "All four names remain importable and typed with their existing call signatures throughout this phase."
    - "StateMachine subclasses invoking classmethod helpers receive their subclass result type."
    - "Supplied State/final identity, internal transition mode, priority, and atomic late-failure behavior remain identical to the canonical adapters."
    - "The helper parity cells deliberately capture one warning while comparing construction/dispatch semantics."
  </behavior>
  <action>
Extend the central oracle with each deprecated surface, capturing exactly one expected warning so warnings neither obscure nor excuse semantic regressions. For state-only helpers, add transitions afterward through canonical registration and compare state/final identity. For quick helpers, include final destination, internal self-transition, priority selection, and a late invalid row proving no machine escapes. Add a classmethod subclass case to prove the private worker constructs through `cls`. Keep helper-specific tests focused on outward compatibility rather than duplicating warning assertions from Task 1.

Assert runtime definitions, PEP 561 declarations, package imports, and `__all__` retain the four exact names/signatures. Change stub/export files only if synchronization is required; do not annotate removal or delete public declarations. Run blocking mypy and targeted parity tests.
  </action>
  <acceptance_criteria>
    - "D-10: simple_fsm, quick_fsm, StateMachine.quick_build, and StateMachine.from_states remain callable, importable, typed, and behaviorally supported."
    - "D-18/D-20: helper adapter cells share canonical final/internal validation, exact identity, graph-version atomicity, and no post-publication repair."
    - "No nondeprecated constructor warning or new public construction abstraction is introduced."
    - "Targeted behavior tests and blocking mypy pass."
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_builder.py tests/test_final_states.py tests/test_construction_parity.py -x -q -k "deprecat or quick or from_states or simple_fsm or construction_parity" &amp;&amp; task typecheck-mypy</automated>
  </verify>
  <done>The warned compatibility surfaces remain fully exported, typed, subclass-safe, atomic, and semantically equivalent for the entire v0.5.x cycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User call site → deprecated public boundary | Warning attribution and bounded wording must identify the caller without exposing arguments. |
| Public boundary → private worker | Nested delegation must preserve behavior while emitting no second warning. |
| Compatibility helper input → canonical topology | Deprecated syntax must not receive weaker final/internal validation during the support cycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-03 | Repudiation | nested deprecated wrappers | medium | mitigate | Direct boundary warnings, private non-warning workers, exact count/location tests. |
| T-30-04 | Information Disclosure | warning/validation text | medium | mitigate | Fixed constants and payload-free exact text assertions. |
| T-30-07 | Tampering | deprecated adapter semantics | high | mitigate | Keep helpers in the canonical parity oracle through the complete compatibility cycle. |
| T-30-08 | Denial of Service | dispatch hot path | high | mitigate | Warning calls exist only in cold public construction wrappers; structural closure verifies absence from dispatch. |
</threat_model>

<verification>
- `uv run pytest tests/test_builder.py tests/test_final_states.py tests/test_construction_parity.py -x -q -k "deprecat or warning or quick or from_states or simple_fsm or construction_parity"`
- `uv run ruff format --check` and `uv run ruff check` on Plan 30-03 Python files
- `task typecheck-mypy`
- `task typecheck-ty` (visible advisory result)
</verification>

<success_criteria>
- BUILD-03 runtime guidance is actionable, exact, once-per-invocation, and user-attributed.
- All compatibility surfaces remain correct and supported for v0.5.x.
- Deprecated helpers share the final/internal construction transaction and observable parity oracle.
- Warning policy remains entirely outside dispatch and does not expose caller payloads.
</success_criteria>

<output>
Create `.planning/phases/30-builder-first-construction-persistence-parity/30-03-SUMMARY.md` when done.
</output>
