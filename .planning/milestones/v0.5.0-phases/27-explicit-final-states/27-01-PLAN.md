---
phase: 27-explicit-final-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - tests/test_final_states.py
  - tests/test_mypyc_guard.py
  - .specify/memory/spr-core-api.md
autonomous: true
requirements:
  - FINAL-01
  - FINAL-02
  - FINAL-04
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "FINAL-01 / D-01 / D-03: State, CallbackState, State.create, DeclarativeState, and inherited AsyncDeclarativeState construction expose one exact keyword-only immutable final boolean whose default is False."
    - "FINAL-02 / D-07: StateMachine and AsyncStateMachine report termination through one O(1) read of the canonical current State's explicit final marker, including at initialization."
    - "FINAL-04 / D-02: Two topology-identical sink states can differ in termination solely because one was explicitly declared final."
    - "D-06: A final State is valid as an initial state and as the destination of a normal incoming transition."
    - "D-09: Attempting a trigger while final retains ordinary missing-transition result behavior and introduces no completion event, callback family, or special exception."
    - "One repository-level Phase 27 Beads item is discovered or created, claimed before implementation, and retained through execution and any gap cycle."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Immutable State.final metadata and derived StateMachine.is_terminated query"
      contains: "def is_terminated"
    - path: "tests/test_final_states.py"
      provides: "Central behavioral oracle for explicit final-state semantics"
    - path: "tests/test_mypyc_guard.py"
      provides: "Slots, inheritance, and direct-read structural proof"
    - path: ".specify/memory/spr-core-api.md"
      provides: "Living public final-state and termination contract"
  key_links:
    - from: "State.__init__"
      to: "State.final"
      via: "private slotted _final value with getter-only public property"
      pattern: "_final"
    - from: "StateMachine.is_terminated"
      to: "StateMachine._current_state.final"
      via: "single inherited current-State attribute read"
      pattern: "_current_state.final"
    - from: "State.create / CallbackState / DeclarativeState"
      to: "State.__init__"
      via: "keyword forwarding of the exact final boolean"
      pattern: "final=final"
  flagged_assumptions:
    - id: FINAL-01
      classification: unclassified
      status: unresolved
      statement: "The specless edge probe did not classify immutable final declaration; this plan operationalizes it from locked D-01 and D-03 without inferring additional semantics."
    - id: FINAL-02
      classification: unclassified
      status: unresolved
      statement: "The specless edge probe did not classify termination lookup; this plan treats locked D-07's direct current-State read as the complete query contract."
    - id: FINAL-04
      classification: unclassified
      status: unresolved
      statement: "The specless edge probe did not classify sink/final distinction; this plan follows locked D-02's explicit-only model."
  prohibitions: []
---

<objective>
Deliver the thinnest production final-state path: declare an immutable final State, initialize or transition a machine into it, and query termination without graph inference.

Purpose: Establish explicit completion identity and an O(1) runtime truth before enforcing topology or persistence rules.
Output: Public State.final and StateMachine.is_terminated behavior, constructor propagation, central tests, structural guards, and the updated living SPR.
</objective>

## Artifacts this phase produces

- Public `State.final: bool` getter backed by private slotted field `State._final`.
- Public `StateMachine.is_terminated: bool` inherited unchanged by `AsyncStateMachine`.
- Additive serialized dictionary field `final_states` containing sorted unique final-state names.
- New `tests/test_final_states.py` behavioral oracle.
- Updated `.specify/memory/spr-core-api.md` final-state, construction, lifecycle, control, clone, and persistence contract.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-explicit-final-states/27-CONTEXT.md
@.planning/phases/27-explicit-final-states/27-RESEARCH.md
@.planning/phases/27-explicit-final-states/27-PATTERNS.md
@.planning/phases/27-explicit-final-states/27-VALIDATION.md
@.planning/phases/26-canonical-construction-evidence-contract/26-01-SUMMARY.md

<interfaces>
Existing contracts to deepen:
- `State.__slots__ = ("name",)` and `State.__init__(name)` own canonical state identity metadata.
- `State.create(name, on_enter=None, on_exit=None)` constructs `CallbackState` and preserves callback positional compatibility.
- `CallbackState.__init__` and `DeclarativeState.__init__` explicitly forward to `State`; `AsyncDeclarativeState` inherits declarative construction.
- `StateMachine.current_state` returns the canonical `_current_state`; `AsyncStateMachine` inherits the base property surface.
- `_commit_transition()` assigns `_current_state` before destination entry and observer work.

New public contracts fixed by D-01, D-03, and D-07:
- `State(name, *, final=False)` accepts only an exact built-in bool and exposes getter-only `state.final`.
- Existing state subclasses/factories preserve their current positional parameters and add only keyword-only `final` forwarding.
- `machine.is_terminated` returns `machine.current_state.final` without a topology scan, cache, or machine-side termination field.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace explicit final declaration through initialization, transition, and termination query</name>
  <precondition>`bd ready --json` returns valid JSON from this repository's configured Beads store; if the store is unavailable, stop before editing and surface the prerequisite.</precondition>
  <reversibility rating="costly">D-01 establishes a public constructor/property contract consumed by every later construction and persistence phase.</reversibility>
  <files>src/fast_fsm/core.py, tests/test_final_states.py, .specify/memory/spr-core-api.md</files>
  <read_first>
    - `src/fast_fsm/core.py:839-938` — current State and CallbackState slots, constructors, factory, representation, and callback signatures.
    - `src/fast_fsm/core.py:2278-2310` — current-State property region where the derived query belongs.
    - `src/fast_fsm/core.py:3483-3503` — existing state commit boundary that must remain the only runtime truth source.
    - `.planning/phases/27-explicit-final-states/27-CONTEXT.md` decisions D-01 through D-03 and D-06 through D-09.
    - `.specify/memory/spr-core-api.md` — living state/runtime contract to update in the same public-behavior commit.
  </read_first>
  <behavior>
    - `State("done", final=True)` exposes `final is True`; omission exposes `False`; positional use of the final flag is rejected.
    - Non-bool values including integers, strings, and truthy objects raise a fixed TypeError without invoking coercion hooks.
    - Assignment to `state.final` fails and the slotted State gains no dynamic instance dictionary.
    - A machine initialized in a final State reports terminated immediately; an incoming transition commits the same final State and then reports terminated.
    - A non-final sink with identical lack of outgoing edges reports non-terminated.
    - A trigger attempted in the final State produces the existing ordinary missing-transition result shape.
  </behavior>
  <action>Run `bd ready --json`, reuse an existing Phase 27 item when present, otherwise create exactly one `Phase 27: Explicit Final States` task with `bd create ... --json`, then claim it with `bd update &lt;id&gt; --claim --json`; record the real identifier as `phase_bead_id` in the plan summary and leave it open until successful phase verification. Write the new behavioral tests first. Per D-01 and D-03, add private `_final` to `State.__slots__`, add keyword-only `final: bool = False`, reject every value whose exact type is not built-in bool with fixed bounded wording, assign the private field, and expose a getter-only `final` property. Document the new public State constructor parameter and `State.final` property with Google-style docstrings, keeping types in signatures only. Do not change `name` mutability, subclassability decoration, callback signatures, or repr diagnostics. Per D-07, add an inherited getter-only `StateMachine.is_terminated` property whose body directly returns the current canonical State's marker; give the new public property a Google-style docstring and do not add a machine field, scan transitions, or modify `_commit_transition()`. Cover D-02, D-06, and D-09 with initial-final, incoming-final, non-final-sink, and final-state trigger tests. Update the living SPR in the same commit with explicit-only finality and direct derived termination.</action>
  <verify>
    <automated>bd ready --json
uv run pytest tests/test_final_states.py -x -q -k "state or initial or sink or transition or missing"
uv run python tools/release_evidence.py slots-policy --json</automated>
  </verify>
  <acceptance_criteria>
    - One real Phase 27 Beads item is claimed and recorded; no per-plan duplicate is created or prematurely closed.
    - `State.final` has a False default, accepts exact True/False only as a keyword, is publicly read-only, and does not add `__dict__`.
    - `StateMachine.is_terminated` is a direct current-State marker read and is true for an initially final machine and after an incoming final transition.
    - The new public constructor parameter and both public properties have Google-style docstrings with signature-owned type information.
    - A topology-identical non-final sink remains non-terminated, proving no graph-shape inference.
    - Triggering from final uses the existing missing-transition result and no new completion signal surface exists.
    - The SPR records the public contract in the same commit.
  </acceptance_criteria>
  <done>A user can explicitly mark completion and observe exact termination truth through one constant-time read without changing ordinary sink or missing-transition behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate final metadata through every State construction surface and structural boundary</name>
  <files>src/fast_fsm/core.py, tests/test_final_states.py, tests/test_mypyc_guard.py, .specify/memory/spr-core-api.md</files>
  <read_first>
    - `src/fast_fsm/core.py:839-938` — explicit State.create and CallbackState signatures whose existing positional callback compatibility must remain.
    - `src/fast_fsm/core.py:5252-5285` — DeclarativeState constructor; AsyncDeclarativeState inherits this path.
    - `tests/test_mypyc_guard.py:241-259,390-445,990-1010` — decorator, slots, and source-shape guard patterns.
    - `.planning/phases/27-explicit-final-states/27-PATTERNS.md` §State model and subclass construction.
  </read_first>
  <behavior>
    - `State.create`, `CallbackState`, and `DeclarativeState` accept and preserve only the keyword-only exact final boolean while retaining all current callback/logger positional calls.
    - `AsyncDeclarativeState` receives the same marker through inheritance without a duplicate async implementation.
    - Existing interpreted subclasses remain constructible and can call the base initializer with the new keyword.
    - Structural guards prove `_final` is a State slot, no `_terminated`-style machine slot exists, and the query body does not inspect transition topology.
    - Existing state callback `*args, **kwargs`, repr, and logger behavior remain unchanged.
  </behavior>
  <action>Extend the tests before implementation. Thread the D-01 keyword-only `final` parameter through `State.create`, `CallbackState.__init__`, and `DeclarativeState.__init__`, forwarding it by keyword to the base State while preserving every existing positional parameter and `*args, **kwargs` callback contract. Update the affected public constructor/factory docstrings in Google style without duplicating annotated types. Rely on inheritance for AsyncDeclarativeState rather than copying final-state logic. Add AST/slots tests that assert the new State storage and getter shape, the absence of any second termination truth on StateMachine, continued `allow_interpreted_subclasses`, and a one-attribute-chain query implementation. Keep State repr and diagnostics unchanged because semantic diagnostic projection belongs to Phase 31. Amend the SPR only if Task 1's text does not yet enumerate these construction surfaces. Build the Sphinx API documentation with warnings treated as errors after the public docstring changes; broader tutorial and guidance work remains deferred to Phase 32.</action>
  <verify>
    <automated>uv run pytest tests/test_final_states.py tests/test_mypyc_guard.py -x -q
uv run ruff format src/fast_fsm/core.py tests/test_final_states.py tests/test_mypyc_guard.py
uv run ruff check --fix src/fast_fsm/core.py tests/test_final_states.py tests/test_mypyc_guard.py
uv run ruff check src/fast_fsm/core.py tests/test_final_states.py tests/test_mypyc_guard.py
task typecheck-mypy
task typecheck-ty
uv run sphinx-build -b html docs docs/_build/html -W --keep-going</automated>
  </verify>
  <acceptance_criteria>
    - All explicit State construction surfaces preserve final metadata and reject non-bool values consistently.
    - Existing callback and logger positional compatibility is unchanged; `final` remains keyword-only everywhere.
    - Async declarative construction uses the inherited implementation and both machine kinds expose the same termination query.
    - Source/AST guards fail if finality becomes dynamically stored, machine-cached, topology-derived, or async-duplicated.
    - Public constructor/factory/property docstrings are Google-style and the Sphinx warnings-as-errors API build succeeds.
    - Ruff, blocking mypy, advisory ty, focused tests, and slots policy pass.
  </acceptance_criteria>
  <done>The explicit marker and O(1) query are uniform across supported state classes and remain compatible with slots, mypyc, and interpreted subclassing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public State constructor → compiled slotted State | Caller-controlled final values become immutable state identity metadata. |
| Canonical current State → public termination query | Runtime state identity becomes externally visible completion truth. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | `State.__init__` / `State.final` | high | mitigate | Require exact built-in bool before assignment, store in a private slot, expose no setter, and prove the behavior in pure Python plus native Phase 27 validation. |
| T-27-04 | Denial of Service | `StateMachine.is_terminated` | medium | mitigate | Implement one current-State attribute chain with an AST guard; prohibit graph scans, allocations, caches, or dispatch work in the query. |
| T-27-07 | Spoofing | canonical State identity | low | accept | The query reads the already-canonical current object; no authentication boundary exists, and identity substitution is governed by existing machine registration/control rules. |
| T-27-09 | Tampering | Phase Beads identity | medium | mitigate | Accept only a real JSON identifier returned by bd, claim one phase-level item, and carry it through summaries until verification closes it. |
</threat_model>

<verification>
- `uv run pytest tests/test_final_states.py tests/test_mypyc_guard.py -x -q`
- `uv run ruff check src/fast_fsm/core.py tests/test_final_states.py tests/test_mypyc_guard.py`
- `task typecheck-mypy`
- `task typecheck-ty`
- `uv run sphinx-build -b html docs docs/_build/html -W --keep-going`
- `uv run python tools/release_evidence.py slots-policy --json`
</verification>

<success_criteria>
- FINAL-01 exact immutable final metadata is available through all State construction surfaces.
- FINAL-02 termination is an inherited O(1) direct read, including initial-final machines.
- FINAL-04 explicit finality and non-final dead-end topology remain distinct.
- D-06 and D-09 preserve initial/destination validity and ordinary missing-transition behavior.
- No deferred transition-mode, rejection, diagnostics, deprecation, installed-artifact, or tutorial work is introduced.
</success_criteria>

<output>
Create `.planning/phases/27-explicit-final-states/27-01-SUMMARY.md` when done.
</output>
