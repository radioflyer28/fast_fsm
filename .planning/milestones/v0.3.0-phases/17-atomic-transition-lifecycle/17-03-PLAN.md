---
phase: 17-atomic-transition-lifecycle
plan: 03
type: execute
wave: 2
depends_on: [17-02]
files_modified:
  - src/fast_fsm/core.py
  - .specify/memory/spr-core-api.md
  - tests/test_transition_lifecycle.py
  - tests/test_advanced_functionality.py
  - tests/test_listeners.py
  - tests/test_builder.py
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04, LIFE-05]
estimate: {tokens: 40000, raw_tokens: 40000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Per D-01/D-02/D-04, sync callbacks run in the exact locked order, preserve registration order, and stop at the first failure."
    - "Per D-06/D-12, the sole commit section updates destination and history together; pre-commit failure records nothing, post-commit failure records once and reports failed-plus-committed."
    - "Per D-09/D-10, lifecycle and declarative failures use the same one-pass finalizer without duplicate handler or observer invocation."
    - "force_state, reset, restore, clone callbacks, builder wiring, and direct handle_event compatibility remain explicitly tested."
  artifacts:
    - {path: "src/fast_fsm/core.py", provides: "Complete synchronous pre-commit/commit/post-commit executor"}
    - {path: "tests/test_transition_lifecycle.py", provides: "Every synchronous lifecycle injection point and ordered success matrix"}
    - {path: "tests/test_advanced_functionality.py", provides: "History/result/control-mutator regressions"}
    - {path: "tests/test_listeners.py", provides: "Fail-fast listener and stable registration-order regressions"}
    - {path: "tests/test_builder.py", provides: "Declarative outcome and exactly-once builder compatibility"}
  key_links:
    - {from: "sync lifecycle runner", to: "commit helper", via: "after exit-state listeners and before destination entry", pattern: "_commit_transition"}
    - {from: "declarative handler outcome", to: "trigger result/finalizer", via: "normalized lifecycle stage", pattern: "declarative-handler"}
  prohibitions:
    - "[UNVERIFIED - no SPEC] After the first lifecycle callback failure or cancellation, do not continue later lifecycle callbacks and do not roll back state/history already committed."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow asyncio.CancelledError; observation is followed by re-raising the original cancellation."
    - "[UNVERIFIED - no SPEC] Do not disclose callback payloads or cause text through TransitionResult repr, TransitionError text, logs, or injected observer kwargs."
---

<objective>
Implement the complete synchronous lifecycle transaction and reconcile legacy success-after-error expectations.

Purpose: Deliver exact callback order, fail-fast state atomicity, committed-only history, and declarative integration for sync users.
Output: One authoritative sync runner plus exhaustive real-object regressions.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-atomic-transition-lifecycle/17-CONTEXT.md
@.planning/phases/17-atomic-transition-lifecycle/17-RESEARCH.md
@.planning/phases/17-atomic-transition-lifecycle/17-PATTERNS.md
@.planning/phases/17-atomic-transition-lifecycle/17-VALIDATION.md
@.planning/phases/17-atomic-transition-lifecycle/17-02-SUMMARY.md
@src/fast_fsm/core.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute the exact synchronous lifecycle around one atomic commit</name>
  <files>src/fast_fsm/core.py, tests/test_transition_lifecycle.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - Success order is before listeners, source on_exit, registered source callbacks, exit listeners, commit, destination on_enter, registered destination callbacks, enter listeners, declarative handler, trigger callbacks, after listeners; two registrations in each collection retain order.
    - Every pre-commit injection leaves source/no history/committed false; every post-commit injection leaves destination/one record/committed true; all are failed results with exact stage/cause and no later calls.
    - Declarative None/True/success result continues; False/failed result/invalid return/exception fails once at `declarative-handler`; ordinary invocation remains exactly once.
  </behavior>
  <action>Replace the scattered sync callback chain with the paired-runner architecture's synchronous half per D-01/D-02. Call every surface in the locked order and preserve registration order/caller arguments. Per D-04, catch ordinary lifecycle exceptions at the active stage, stop immediately, and finalize once without compensation or rollback. Per D-12, call the tracer's no-user-code commit seam exactly once between exit and entry stages so current state/history cannot diverge. Normalize ordinary declarative outcomes at their post-commit stage while preserving the Phase 16 resolver and exactly-once invocation. In this same task and commit, update `.specify/memory/spr-core-api.md` with the complete synchronous pre-commit/commit/post-commit order, fail-fast suffix suppression, declarative-handler outcome rules, and commit-owned history invariant; retain previously recorded result and observer details. Avoid generic descriptor allocation on the no-listener path and keep all lookup/mutation complexity O(1).</action>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run pytest tests/test_transition_lifecycle.py -x -q -k "sync and (order or precommit or postcommit or declarative or history)"</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python -c "from pathlib import Path; text = Path('.specify/memory/spr-core-api.md').read_text(); assert all(token in text for token in ('pre-commit', 'commit', 'post-commit', 'declarative-handler', 'history'))"</automated>
  </verify>
  <done>The synchronous public trigger exposes the full documented transaction and truthful result at every failure point, and the same commit makes spr-core-api.md accurately describe that runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile listeners, history, builder, and control compatibility</name>
  <files>src/fast_fsm/core.py, tests/test_advanced_functionality.py, tests/test_listeners.py, tests/test_builder.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - Legacy callback/listener tests now assert first-failure stop, correct stage, state, history, and one observer pass instead of catch-and-continue success.
    - Enabled history appends once at commit on success or post-commit failure and never on pre-commit failure; bounded deque/copy behavior remains unchanged.
    - force_state/reset/restore, clone callback lists, builder registration/freeze, and direct handle_event normalization retain existing signatures and compatibility behavior.
  </behavior>
  <action>Update contradictory sync tests identified by research to the D-04 safe-default contract; do not merely delete their assertions. Extend history cases for pre/post failures per D-12 and prove callback order no longer controls record authority. Preserve `force_state`, `reset`, `restore`, synthetic `__force__`, clone registries, builder publication/freeze, and direct `handle_event()` behavior unless a test demonstrates an intended ordinary-trigger integration change. Confirm the declarative handler is invoked once and its failed/invalid/raising outcome controls only ordinary transition completion. In this same task and commit, refine `.specify/memory/spr-core-api.md` so its lifecycle bullets distinguish ordinary-trigger declarative failure semantics from the preserved `force_state`/`reset`/`restore` and direct-handler compatibility contracts.</action>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_advanced_functionality.py tests/test_listeners.py tests/test_builder.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_advanced_functionality.py tests/test_listeners.py tests/test_builder.py</automated>
    <automated>uv run pytest tests/test_transition_lifecycle.py tests/test_advanced_functionality.py tests/test_listeners.py tests/test_builder.py -x -q</automated>
    <automated>uv run pytest tests/test_state_machine_utils.py tests/test_basic_functionality.py -x -q -k "force or reset or restore or clone or trigger"</automated>
    <automated>uv run python -c "from pathlib import Path; text = Path('.specify/memory/spr-core-api.md').read_text(); assert all(token in text for token in ('force_state', 'reset', 'restore', 'declarative'))"</automated>
  </verify>
  <done>Sync lifecycle semantics replace unsafe legacy expectations without collateral changes to control or builder contracts, and spr-core-api.md records both the new lifecycle behavior and preserved compatibility boundaries in the same commit.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering/Repudiation | callback failure | high | mitigate | Exhaustive stage injection asserts failure and suffix suppression. |
| T-17-02 | Tampering | state/history commit | high | mitigate | One non-callback commit seam plus pre/post cardinality tests. |
| T-17-03 | Denial of Service/Tampering | failure observation | high | mitigate | Every sync failure reaches one finalizer exactly once. |
| T-17-04 | Information Disclosure | callback exceptions | high | mitigate | Preserve hidden cause and stage-only diagnostics during all new catches. |
| T-17-09 | Tampering | reentrancy/concurrency | high | transfer | Phase 18; sync runner adds no lock/reentry behavior. |
| T-17-10 | Spoofing/Tampering | installed artifacts | high | transfer | Phase 20; source/native tree proof only. |
</threat_model>

<verification>Run the full sync lifecycle/listener/history/builder selection and blocking type checks.</verification>
<success_criteria>LIFE-01 through LIFE-05 are complete for synchronous execution with no compatibility drift outside ordinary trigger lifecycle.</success_criteria>
<output>Create `.planning/phases/17-atomic-transition-lifecycle/17-03-SUMMARY.md` when done.</output>
