---
phase: 17-atomic-transition-lifecycle
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/fast_fsm/core.py
  - .specify/memory/spr-core-api.md
  - tests/test_transition_lifecycle.py
  - tests/test_boundary_negative.py
  - tests/test_mypyc_guard.py
  - tests/test_performance_benchmarks.py
  - tools/phase16_isolated_verify.py
  - .planning/phases/17-atomic-transition-lifecycle/17-PERFORMANCE-EVIDENCE.md
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04, LIFE-05, LIFE-06]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01, D-05, D-06, D-07, D-09, and D-12, one real public transition traverses the shared stage catalog, the indivisible state/history commit, a post-commit callback failure, the truthful result, and one failure-observer pass."
    - "The authoritative lifecycle test module exists in Wave 0 and records callback order, stage, commit state, current state, history, cause identity, later-call suppression, observer cardinality/order, cancellation, and sync/async parity as executable expectations."
    - "Before-change and tracer measurements identify pure and freshly compiled origins; checkout native shadows are neither removed nor accepted as evidence."
  artifacts:
    - path: "tests/test_transition_lifecycle.py"
      provides: "Real-object scenario recorder and parameter tables for LIFE-01 through LIFE-06"
    - path: "src/fast_fsm/core.py"
      provides: "Production tracer through stage/result, commit, callback failure, and finalization seams"
    - path: "tools/phase16_isolated_verify.py"
      provides: "Backward-compatible phase17 task/suite selection with explicit overlays and origin assertions"
    - path: ".planning/phases/17-atomic-transition-lifecycle/17-PERFORMANCE-EVIDENCE.md"
      provides: "Environment-labelled before/tracer/final lifecycle measurements"
  key_links:
    - from: "StateMachine.trigger"
      to: "TransitionResult and failure observers"
      via: "stage-aware runner and one finalizer"
      pattern: "_finalize_failure"
    - from: "commit helper"
      to: "StateMachine._current_state and _history"
      via: "one non-callback, non-awaiting section"
      pattern: "_commit_transition"
    - from: "tools/phase16_isolated_verify.py"
      to: "tests/test_transition_lifecycle.py"
      via: "phase17 explicit overlay and asserted-origin command"
      pattern: "phase17"
  prohibitions:
    - "[UNVERIFIED - no SPEC] After the first lifecycle callback failure or cancellation, do not continue later lifecycle callbacks and do not roll back state/history already committed."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow asyncio.CancelledError; observation is followed by re-raising the original cancellation."
    - "[UNVERIFIED - no SPEC] Do not disclose callback payloads or cause text through TransitionResult repr, TransitionError text, logs, or injected observer kwargs."
---

<objective>
Establish the Phase 17 vertical tracer and executable Wave 0 lifecycle contract.

Purpose: Prove one public callback-failure transaction through every architectural seam before broadening stage coverage.
Output: A production post-commit failure tracer, authoritative lifecycle test infrastructure, and origin-labelled baseline evidence.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-atomic-transition-lifecycle/17-CONTEXT.md
@.planning/phases/17-atomic-transition-lifecycle/17-RESEARCH.md
@.planning/phases/17-atomic-transition-lifecycle/17-PATTERNS.md
@.planning/phases/17-atomic-transition-lifecycle/17-VALIDATION.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-VERIFICATION.md
@src/fast_fsm/core.py
@tools/phase16_isolated_verify.py
@evidence/release-baseline.json

<interfaces>
- Preserve the first five positional `TransitionResult` fields: `success`, `from_state`, `to_state`, `trigger`, `error`; append `committed=False`, `stage=None`, and hidden-from-repr `cause=None`.
- Stable public stage strings use the research catalog: `resolution`, `guard`, `state-permission`, `before-transition`, `source-exit`, `source-exit-callback`, `exit-state-listener`, `commit`, `destination-enter`, `destination-enter-callback`, `enter-state-listener`, `declarative-handler`, `trigger-callback`, `after-transition`.
- Preserve `on_failed(trigger, from_state, error, **kwargs)`, all caller `*args/**kwargs`, ordinary value-returning triggers, and `TransitionError.result`.
</interfaces>

<flagged_assumptions>
- [ASSUMPTION - no SPEC, probe 1/LIFE-01] Resolve by exact successful sync/async order assertions over every documented collection and two equal-position registrations.
- [ASSUMPTION - no SPEC, probe 2/LIFE-02] Resolve by concrete pre-commit injection criteria for source preservation, stage, cause identity, zero history, and suffix suppression.
- [ASSUMPTION - no SPEC, probe 3/LIFE-03] Resolve by concrete post-commit injection criteria for destination preservation, failed-plus-committed result, one history record, and suffix suppression.
- [ASSUMPTION - no SPEC, probe 4/LIFE-04 adjacency] Generic equality/touch taxonomy maps to exact-once observer cardinality when lifecycle and observer failures meet; assert one call per observer and no recursive merge.
- [ASSUMPTION - no SPEC, probe 5/LIFE-04 empty] Generic empty/single/null taxonomy maps to zero-observer behavior; assert identical original result/cause with no observer configured and exactly one call with one observer.
- [ASSUMPTION - no SPEC, probe 6/LIFE-04 ordering] Generic equal-order taxonomy maps to stable observer registration order; assert later observers run once even when earlier observers raise.
- [ASSUMPTION - no SPEC, probe 7/LIFE-05] Resolve by a success/pre-failure/post-failure/pre-cancel/post-cancel history cardinality and state matrix.
- [ASSUMPTION - no SPEC, probe 8/LIFE-06] Resolve by executing the same semantic cases against sync and async machines in asserted pure and freshly compiled origins.
- Probe accounting: 8 surfaced = 8 authored/resolved acceptance families + 0 dropped.
</flagged_assumptions>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one post-commit callback failure through the complete transaction</name>
  <files>.planning/phases/17-atomic-transition-lifecycle/17-PERFORMANCE-EVIDENCE.md, tests/test_transition_lifecycle.py, src/fast_fsm/core.py, .specify/memory/spr-core-api.md</files>
  <behavior>
    - First record asserted pure and freshly compiled before-change origin and trigger observations, then write a failing real-object test for a destination `on_enter` exception after history is enabled.
    - The tracer commits destination plus exactly one history record, stops every later callback, returns `success=False`, `committed=True`, `stage="destination-enter"`, and retains the exception by identity without displaying its message.
    - Each failure observer is called once in registration order with the unchanged signature; an observer exception does not recurse, replace the result/cause, or prevent later observers.
  </behavior>
  <action>Use the existing isolated task interface to capture before-change pure and compiled observations before editing production code. Create the lifecycle recorder with real State/CallbackState objects and require the tracer RED. In `core.py`, per D-01/D-05/D-06/D-07 define the private stage catalog and additive slotted result fields; per D-12 introduce a no-callback/no-await commit helper that updates current state and appends history together; wire the single destination-enter failure slice through a non-recursive finalizer per D-09/D-10/D-11. Ordinary trigger remains value-returning and the first five positional fields remain unchanged. The finalizer passes original caller kwargs without reserved additions, catches observer `BaseException` only inside the observer loop, continues registration order, logs safe stage/index/type metadata only, and returns the original failed result. In this same task and commit, update `.specify/memory/spr-core-api.md` in its terse invariant style to record the implemented `destination-enter` post-commit failure, `committed`/`cause` result semantics, commit-owned history, and exactly-once failure-observer behavior; preserve all unrelated Phase 16 invariants. Keep this tracer production-quality so later plans expand the same runner/finalizer rather than replacing it.</action>
  <verify>
    <automated>uv run ruff format src/fast_fsm/core.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run ruff check src/fast_fsm/core.py tests/test_transition_lifecycle.py</automated>
    <automated>uv run pytest tests/test_transition_lifecycle.py -x -q -k "destination_enter and tracer"</automated>
    <automated>task typecheck-mypy</automated>
    <automated>uv run python -c "from pathlib import Path; text = Path('.specify/memory/spr-core-api.md').read_text(); assert all(token in text for token in ('destination-enter', 'committed', 'cause', 'failure observer', 'history'))"</automated>
  </verify>
  <done>The first production slice demonstrates truthful post-commit failure, coherent state/history, hidden cause retention, and one ordered observer pass end to end, and the same commit records those implemented invariants in spr-core-api.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Establish Wave 0 compatibility, probe, and isolated-evidence guards</name>
  <files>tests/test_transition_lifecycle.py, tests/test_boundary_negative.py, tests/test_mypyc_guard.py, tests/test_performance_benchmarks.py, tools/phase16_isolated_verify.py</files>
  <behavior>
    - Legacy five-field positional construction and equality remain valid; appended fields have defaults; cause never appears in repr/equality/error/log text.
    - The lifecycle scenario table encodes all eight spec-less probe families and the exact stage/state/history/cause/order/cardinality expectations that Plans 02-04 expand.
    - A phase17 isolation selection overlays an explicit inventory, asserts `.py` or freshly built native origin before tests, never consumes checkout shadows, and includes the compiled 200,000 ops/sec lifecycle-success selection.
  </behavior>
  <action>Extend boundary and mypyc guards for the additive slotted result layout, original five-field positional calls, unchanged public exports/signatures, `core.py`-only compilation, and payload-sentinel redaction. Organize `tests/test_transition_lifecycle.py` as reusable sync/async scenario records; make every currently owned tracer/result/probe assertion pass and leave later scenario rows to be authored by their owning Plans 02-04 rather than disabling tests. Extend `tools/phase16_isolated_verify.py` backward-compatibly with a `phase17` suite and explicit lifecycle source/test/doc/evidence inventory, preserving byte overlays, fresh exported trees, fail-closed compiled build, pure native-shadow rejection, and pre-command origin assertions. Add a lifecycle-success performance selection using the existing benchmark pattern and fixed compiled floor; record the tracer observation in the performance evidence without inventing an exact rate when the test reports only pass/fail.</action>
  <verify>
    <automated>uv run ruff format tests/test_transition_lifecycle.py tests/test_boundary_negative.py tests/test_mypyc_guard.py tests/test_performance_benchmarks.py tools/phase16_isolated_verify.py</automated>
    <automated>uv run ruff check tests/test_transition_lifecycle.py tests/test_boundary_negative.py tests/test_mypyc_guard.py tests/test_performance_benchmarks.py tools/phase16_isolated_verify.py</automated>
    <automated>uv run pytest tests/test_transition_lifecycle.py tests/test_boundary_negative.py tests/test_mypyc_guard.py -x -q -k "TransitionResult or observer or tracer or lifecycle"</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include src/fast_fsm/core.py --include tests/test_transition_lifecycle.py --include tests/test_boundary_negative.py --include tests/test_mypyc_guard.py --include tests/test_performance_benchmarks.py --include tools/phase16_isolated_verify.py -- uv run pytest tests/test_transition_lifecycle.py tests/test_boundary_negative.py tests/test_mypyc_guard.py -x -q</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode compiled --include src/fast_fsm/core.py --include tests/test_transition_lifecycle.py --include tests/test_boundary_negative.py --include tests/test_mypyc_guard.py --include tests/test_performance_benchmarks.py --include tools/phase16_isolated_verify.py -- uv run pytest tests/test_transition_lifecycle.py tests/test_performance_benchmarks.py -x -q -k "lifecycle or trigger_min_throughput"</automated>
  </verify>
  <done>Wave 0 supplies executable contract infrastructure, compatibility guards, fresh-origin proof, and explicit 8-of-8 probe accounting without silent drops.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering/Repudiation | callback outcome | high | mitigate | Tracer asserts failed-plus-committed result and stops the suffix. |
| T-17-03 | Denial of Service/Tampering | failure observers | high | mitigate | One non-recursive ordered finalizer with adjacency/empty/order tests. |
| T-17-04 | Information Disclosure | result/error/log/kwargs | high | mitigate | Hidden cause plus secret-sentinel checks across every output channel. |
| T-17-07 | Spoofing | source/native origin | high | mitigate | Fresh export, explicit overlay, native-shadow rejection, and origin assertion. |
| T-17-08 | Denial of Service | hot path | high | mitigate | Slotted shapes and before/tracer compiled throughput observations. |
| T-17-09 | Tampering | reentrancy/concurrency | high | transfer | Phase 18 owns rejection/serialization; add no ownership logic here. |
| T-17-10 | Spoofing/Tampering | installed artifacts | high | transfer | Phase 20 owns installed-wheel parity; this plan proves source-tree modes only. |
</threat_model>

<verification>
Run the focused Wave 0 suite in checkout, asserted pure export, and freshly compiled export; require Ruff, blocking mypy, slots/API guards, and the compiled throughput floor.
</verification>

<success_criteria>
The tracer is production code, Wave 0 tests are executable, all eight fallback probes are explicitly accounted for, and no high-severity plan-owned threat remains without a concrete automated mitigation.
</success_criteria>

<output>
Create `.planning/phases/17-atomic-transition-lifecycle/17-01-SUMMARY.md` when done.
</output>
