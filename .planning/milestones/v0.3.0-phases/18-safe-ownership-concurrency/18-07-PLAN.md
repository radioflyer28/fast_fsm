---
phase: 18-safe-ownership-concurrency
plan: 07
type: execute
wave: 6
depends_on: [18-06]
files_modified:
  - .github/workflows/ci.yml
  - tools/phase18_native_probe.py
  - tools/phase16_isolated_verify.py
  - tests/test_mypyc_guard.py
  - tests/test_performance_benchmarks.py
  - evidence/release-baseline.json
  - .planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md
autonomous: true
requirements: [OWN-01, OWN-02, OWN-03, OWN-04, OWN-05, OWN-06, OWN-07]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Fresh asserted pure and freshly compiled source exports pass identical OWN-01 through OWN-07 semantics with the complete writer gate, safe-trigger/declarative contracts, cancellation, causal-root, redaction, and no-offload proof."
    - "The supported Python 3.10-3.14 CI matrix compiles `core.py`, asserts native origin, and executes the ownership selection; Phase 18 cannot close until every matrix job succeeds for the exact implementation SHA."
    - "Uncontended ownership observations are environment-labelled, all core operations remain O(1), slots remain closed, and compiled trigger throughput stays at least 200,000 operations/second."
    - "The complete sequential suite, Ruff, blocking mypy, advisory ty, Sphinx warnings-as-errors, doctests, slots, and read-only baseline freshness pass without claiming Phase 20 installed-artifact parity."
  artifacts:
    - {path: ".github/workflows/ci.yml", provides: "Supported Python 3.10-3.14 native source ownership compile/semantic matrix"}
    - {path: "tools/phase18_native_probe.py", provides: "Local representation probe, CI contract validator, and exact-SHA hosted result assertion"}
    - {path: "tools/phase16_isolated_verify.py", provides: "Authoritative fresh pure/native Phase 18 suite"}
    - {path: ".planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md", provides: "Final origin-labelled semantics and performance evidence"}
    - {path: "evidence/release-baseline.json", provides: "Reviewed current release-baseline manifest"}
  key_links:
    - {from: "CI native matrix", to: "ownership semantic suite", via: "compiled source build plus asserted native origin on Python 3.10-3.14", pattern: "3.10|3.11|3.12|3.13|3.14|ownership"}
    - {from: "phase18 isolated suite", to: "runtime/tests/docs/evidence", via: "explicit overlays and pre-command origin assertions", pattern: "phase18"}
    - {from: "hosted gate", to: "phase closure candidate", via: "GitHub run headSha equality plus successful matrix conclusions", pattern: "headSha|success"}
  prohibitions:
    - "[UNVERIFIED - no SPEC] Do not turn per-machine serialization into a global lock, hidden fair queue, timeout, or scheduler-order promise."
    - "[UNVERIFIED - no SPEC] Do not silently rebind an async machine to another event loop or implicitly offload synchronous callbacks to worker threads."
    - "[UNVERIFIED - no SPEC] Do not disclose trigger arguments, keyword values, callback payloads, stored causes, or arbitrary exception text in ownership errors or logs."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow cancellation, and do not roll back Phase 17 state/history after commit."
---

<objective>
Close Phase 18 with fresh local release evidence and a successful exact-SHA hosted native matrix.

Purpose: Prove the complete ownership contract across source/native modes and every supported interpreter before phase closure, without conflating this source-tree gate with Phase 20 wheel/sdist parity.
Output: Finalized Phase 18 harness, CI matrix, performance/baseline evidence, and a blocking exact-SHA hosted success assertion.
</objective>

## Artifacts this phase produces

- New private runtime symbols in `src/fast_fsm/core.py`: `_ownership_root`, `_prepared_declarative_guard`, base/async ownership slots/helpers, private trigger/control bodies, and short D-12 admission state.
- New files: `tests/test_ownership_concurrency.py`, `tools/phase18_native_probe.py`, `18-PERFORMANCE-EVIDENCE.md`, and `.specify/decisions/ADR-005-safe-ownership-concurrency.md`.
- New CI capability: supported Python 3.10-3.14 native source compilation plus ownership semantics in `.github/workflows/ci.yml`.
- Extended `phase18` verification suite; no new public symbol, runtime dependency, schema, external API, or installed-wheel parity claim.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md
@.planning/phases/18-safe-ownership-concurrency/18-RESEARCH.md
@.planning/phases/18-safe-ownership-concurrency/18-PATTERNS.md
@.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md
@.planning/phases/18-safe-ownership-concurrency/18-06-SUMMARY.md
@.planning/phases/17-atomic-transition-lifecycle/17-VERIFICATION.md
@.github/workflows/ci.yml
@tools/phase18_native_probe.py
@tools/phase16_isolated_verify.py
@evidence/release-baseline.json

<interfaces>
- Wave 0 already established the local compile-first representation probe and complete Python-version matrix contract; this plan strengthens that matrix to compile/import actual `core.py` and execute the final ownership selection.
- `tools/phase18_native_probe.py --assert-hosted-ci-sha HEAD` resolves the exact candidate SHA, finds only the matching CI run, and exits nonzero unless every required Python 3.10-3.14 native ownership job has conclusion `success`.
- Hosted source/native proof does not imply installed wheel/sdist parity; Phase 20 remains responsible for built-artifact verification.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize supported-Python native ownership proof and the Phase 18 harness</name>
  <files>.github/workflows/ci.yml, tools/phase18_native_probe.py, tools/phase16_isolated_verify.py, tests/test_mypyc_guard.py, .planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md</files>
  <read_first>.github/workflows/ci.yml, tools/phase18_native_probe.py, tools/phase16_isolated_verify.py:55-81, tools/phase16_isolated_verify.py:885-943, tests/test_mypyc_guard.py:306-490, .planning/phases/18-safe-ownership-concurrency/18-06-SUMMARY.md</read_first>
  <behavior>
    - CI compiles actual `core.py` in strict compiled source mode and runs ownership/lifecycle semantics with asserted native origin on Python 3.10, 3.11, 3.12, 3.13, and 3.14.
    - The local `phase18` suite overlays every Phase 18 runtime/test/doc/ADR/SPR/workflow/evidence file and runs identical ownership semantics in asserted pure and freshly compiled exports.
    - Structural guards require every version, strict compiled build intent, actual native module origin, all writer families, and no installed-wheel claim.
  </behavior>
  <action>Strengthen the Wave 0 `ownership-native-probe` CI matrix so each supported interpreter installs the locked dependencies with `uv`, builds actual `src/fast_fsm/core.py` fail-closed, asserts the imported `fast_fsm.core` is native, and runs the ownership, lifecycle, async, and mypyc-guard selection. Keep the standalone representation probe as the fast local/current-runtime check and add `--assert-hosted-ci-sha REF`: resolve REF to one exact commit, query the configured CI workflow through `gh`, reject absent/queued/running/cancelled/skipped/stale-SHA results, and require one successful ownership job for each Python 3.10 through 3.14. Do not alter the release workflow or inspect installed wheels. Finalize the `phase18` inventory in the existing isolation harness with every changed runtime/test/doc/ADR/SPR/CI/evidence path, identical semantics in pure and compiled source exports, full writer AST enforcement, compiled floors, slots, type/docs, and pure release checks. Strengthen YAML/AST tests so removal of a version, native-origin assertion, actual ownership command, exact-SHA comparison, or sole-mypyc-unit boundary fails locally.</action>
  <acceptance_criteria>
    - Local structural tests prove all five supported versions compile actual core and execute ownership semantics from native source mode in CI.
    - The phase18 suite fails closed on wrong origin, missing inventory, native build failure, semantic divergence, writer bypass, slots/type/docs failure, or throughput below policy.
    - The hosted result helper cannot accept a different SHA or any non-success matrix state.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format tools/phase18_native_probe.py tools/phase16_isolated_verify.py tests/test_mypyc_guard.py</automated>
    <automated>uv run ruff check tools/phase18_native_probe.py tools/phase16_isolated_verify.py tests/test_mypyc_guard.py src/fast_fsm/core.py tests/test_ownership_concurrency.py</automated>
    <automated>uv run pytest tests/test_mypyc_guard.py tests/test_ownership_concurrency.py -x -q -k "supported_python or native_probe or write_family or ownership"</automated>
    <automated>uv run python tools/phase18_native_probe.py --build-mode compiled --assert-native</automated>
    <automated>uv run python tools/phase18_native_probe.py --check-ci .github/workflows/ci.yml</automated>
  </verify>
  <done>The final harness and CI definition prove actual native core ownership semantics across the supported-version contract, and the hosted helper accepts only a successful exact-SHA matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh performance, baseline, and complete local release evidence</name>
  <files>tools/phase16_isolated_verify.py, tests/test_performance_benchmarks.py, evidence/release-baseline.json, .planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md</files>
  <read_first>.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md, tools/phase16_isolated_verify.py, tests/test_performance_benchmarks.py:378-474, evidence/release-baseline.json, .planning/phases/18-safe-ownership-concurrency/18-PERFORMANCE-EVIDENCE.md</read_first>
  <behavior>
    - Environment-labelled pure/native uncontended ownership observations retain O(1) operation structure and the compiled trigger floor of 200,000 operations/second.
    - The authoritative suite checks all OWN behavior, sequential regressions, Ruff, mypy, advisory ty visibility, docs, doctests, slots, pure/native origin, performance, and baseline freshness.
    - Baseline regeneration is explicit, diff-reviewed, and independently checked read-only from asserted pure origin.
  </behavior>
  <action>Finish performance tests for uncontended sync/async ownership overhead and O(1) operation scaling using existing warmup, logging suppression, and origin patterns; exact rates are environment-labelled observations, while only compiled trigger has the durable 200,000 operations/second floor. Run the authoritative `phase18` suite after all runtime, tests, documentation, workflow, and evidence artifacts exist. Require the complete sequential tests, Ruff, blocking mypy, advisory ty visibility, Sphinx warnings-as-errors, doctests, slots, pure/native ownership parity, and compiled performance floor. Regenerate the manifest only through `uv run python tools/phase16_isolated_verify.py --suite baseline-write --manifest-output evidence/release-baseline.json`, inspect the exact diff before retaining it, then rerun read-only freshness from the asserted pure export. Record only measured counts, rates, origins, and commands in the evidence file. Preserve checkout native shadows and unrelated dirty/staged files.</action>
  <acceptance_criteria>
    - The final phase18 suite exits zero with asserted pure/native origins and all semantic, docs, type, slots, and performance gates.
    - The baseline manifest is intentionally regenerated, reviewed, and independently proven fresh.
    - Evidence distinguishes environment observations and source/native results from Phase 20 installed-artifact parity.
  </acceptance_criteria>
  <verify>
    <automated>uv run python tools/phase16_isolated_verify.py --suite phase18</automated>
    <automated>task typecheck-mypy</automated>
    <automated>task typecheck-ty</automated>
    <automated>uv run sphinx-build -b html docs docs/_build/html -W --keep-going</automated>
    <automated>uv run sphinx-build -b doctest docs docs/_build/doctest</automated>
    <automated>task release-baseline-check</automated>
  </verify>
  <done>All local Phase 18 semantic, quality, performance, documentation, slots, and baseline evidence is current and source-origin asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Require the exact-SHA hosted Python 3.10-3.14 matrix</name>
  <files>None — hosted verification only; no plan-owned repository artifact changes after candidate selection</files>
  <precondition>The GitHub remote and CI workflow are reachable; an authentication failure becomes the standard dynamic auth checkpoint.</precondition>
  <action>After Task 2's implementation/evidence commit exists, capture that exact candidate SHA and push it to the configured remote. Wait for the CI workflow associated with that SHA, then use `tools/phase18_native_probe.py --assert-hosted-ci-sha` to require successful Python 3.10, 3.11, 3.12, 3.13, and 3.14 native ownership jobs. Do not accept a run for another SHA, or a queued, running, cancelled, skipped, neutral, timed-out, action-required, or failed conclusion. Do not change any plan-owned runtime, test, CI, harness, baseline, or evidence file after selecting the candidate; if any such change occurs, push the new SHA and repeat the entire hosted gate. Planning SUMMARY/STATE metadata may be committed afterward without changing the verified implementation candidate.</action>
  <acceptance_criteria>
    - The exact Task 2 candidate SHA exists on the remote and has a completed successful ownership-native-probe job for all five supported Python versions.
    - No non-success or stale-SHA state is interpreted as success.
    - No plan-owned implementation/evidence artifact changes after the verified candidate SHA.
  </acceptance_criteria>
  <verify>
    <automated>uv run python tools/phase18_native_probe.py --assert-hosted-ci-sha HEAD</automated>
  </verify>
  <done>The exact Phase 18 implementation candidate has successful hosted Python 3.10-3.14 native source ownership evidence; phase closure is blocked otherwise.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | reentry bypass | high | mitigate | Full writer matrix and fresh pure/native tests reject before preparation. |
| T-18-02 | DoS | lock/owner leak | high | mitigate | Exception/BaseException/cancellation reuse matrix in both origins. |
| T-18-03 | Tampering/DoS | cross-loop corruption | high | mitigate | Permanent identity tests plus exact-SHA cross-version native execution. |
| T-18-04 | DoS | causal child deadlock | high | mitigate | Context-root immediate rejection in asserted source/native modes. |
| T-18-05 | Tampering | declarative marker collision | high | mitigate | Concurrent independent-machine marker tests and no-global-registry AST guard. |
| T-18-06 | Repudiation/Tampering | safe-trigger downgrade | high | mitigate | Pure/native boundary tests require ownership errors to escape. |
| T-18-07 | DoS/Tampering | cancellation windows | high | mitigate | Deterministic waiting/owning pre/post-commit matrix plus immediate reuse. |
| T-18-08 | Information Disclosure | errors/logs | high | mitigate | Secret sentinels and payload-free ownership messages across both origins. |
| T-18-09 | DoS | fairness/timeout/offload expectations | medium | accept | Tests promise only exclusion/loop progress and documents name every non-promise. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / decision group | Plans | Status |
|--------|----|--------------------------|-------|--------|
| GOAL | - | Safe-by-default reentrant/concurrent use without global locks or loop blocking | 01-07 | COVERED |
| REQ | OWN-01 | Immediate pre-acquisition reentry rejection | 01-05,07 | COVERED |
| REQ | OWN-02 | Per-machine independent-thread serialization | 01-02,04,07 | COVERED |
| REQ | OWN-03 | Same-loop task serialization without loop blocking | 01,03,07 | COVERED |
| REQ | OWN-04 | Explicit permanent cross-loop rejection | 01,03,05,07 | COVERED |
| REQ | OWN-05 | Trigger/control/topology/history/all registrar write coverage | 01-05,07 | COVERED |
| REQ | OWN-06 | Release after Exception/BaseException/cancellation at coherent boundaries | 01-05,07 | COVERED |
| REQ | OWN-07 | Inline sync callbacks and event-loop-safe async control | 01,03,06-07 | COVERED |
| CONTEXT | D-01-D-04 | RuntimeError contract, precheck, callback classification, redaction | 01-02,05-07 | COVERED |
| CONTEXT | D-05-D-08 | Per-instance sync serialization and exception-safe full envelope | 01-02,04,06-07 | COVERED |
| CONTEXT | D-09-D-13 | Permanent loop, async lock, causal root, D-12 admission race, inline callbacks | 01,03,05-07 | COVERED |
| CONTEXT | D-14-D-16 | Every write, read/factory boundaries, deterministic pure/native/performance proof | 01,04-07 | COVERED |
| RESEARCH | resolved safe-trigger | Ownership admission outside the compatibility catch | 01,05-07 | COVERED |
| RESEARCH | resolved mixed-mode race | Short non-awaiting gate atomically arbitrates initial binding/configuration | 01,03,06-07 | COVERED |
| RESEARCH | declarative marker | Context-local machine-qualified marker | 01,05,07 | COVERED |
| RESEARCH | representation evidence | Local compile-first probe plus exact-SHA hosted Python 3.10-3.14 matrix | 01,07 | COVERED |
| RESEARCH | T-18-01-T-18-09 | Executable mitigations and explicit accepted non-promise | 01-07 | COVERED |
| PROBE | OWN-01/02/03/04/05/07 unclassified | Reentry/thread/task/loop/write/callback acceptance families | 01-07 | COVERED |
| PROBE | OWN-06 boundary | Throwable/cancellation pre/post-commit release matrix | 01-03,07 | COVERED |
| PROBE | OWN-06 precision | Numeric precision mismatch retained without fabricated contract | 01 | COVERED (FLAGGED MISMATCH) |
| PROHIBITION | recall-to-precision | Four descriptor-less safety/value prohibitions retain explicit dispositions | 01-07 | COVERED (UNVERIFIED) |
| ARTIFACT | runtime | Ownership slots/helpers/private bodies and ContextVars in `core.py` | 01-05 | COVERED |
| ARTIFACT | tests/harness | Ownership matrix, mypyc/performance guards, native probe, isolated suite | 01-05,07 | COVERED |
| ARTIFACT | records | README, Quick Start, architecture/testing, SPR, ADR-005, performance evidence | 01-07 | COVERED |
| ARTIFACT | CI/baseline | Python 3.10-3.14 exact-SHA native matrix and fresh baseline | 01,07 | COVERED |

Deferred queued reentry (FUTR-01), cross-loop transfer (FUTR-03), callback offload (FUTR-04), Phase 19 diagnostic snapshots, and Phase 20 installed artifact parity are exclusions, not gaps. Spec-less accounting remains 8 surfaced = 7 acceptance families + 1 classification mismatch + 0 silent drops.
</source_audit>

<verification>The authoritative phase18 suite, complete writer gate, repository quality/documentation/evidence gates, fixed compiled floor, current baseline, and exact-SHA hosted Python 3.10-3.14 native matrix all pass.</verification>
<success_criteria>All seven OWN requirements, sixteen locked decisions, resolved research questions, every write family, safe-trigger/declarative seams, nine threats, eight probe rows, four prohibitions, artifact families, pure/compiled evidence, docs, and performance constraints are covered with zero silent omissions; no pending hosted run is accepted.</success_criteria>
<output>Create `.planning/phases/18-safe-ownership-concurrency/18-07-SUMMARY.md` when done.</output>
