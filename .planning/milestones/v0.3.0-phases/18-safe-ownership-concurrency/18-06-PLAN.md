---
phase: 18-safe-ownership-concurrency
plan: 06
type: execute
wave: 5
depends_on: [18-05]
files_modified:
  - README.md
  - docs/QUICK_START.md
  - docs/dev/architecture.md
  - docs/dev/testing.md
  - .specify/decisions/ADR-005-safe-ownership-concurrency.md
autonomous: true
requirements: [OWN-01, OWN-02, OWN-03, OWN-04, OWN-05, OWN-06, OWN-07]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Public docs state D-01 through D-16 exactly: pre-acquisition RuntimeError, per-instance sync/async serialization, permanent loop binding, causal reentry, complete write coverage, cleanup boundaries, inline sync callbacks, and explicit non-promises."
    - "Maintainer docs and ADR-005 explain the short non-awaiting D-12 admission gate, private already-owned bodies, deterministic strict-RED progression, fresh-origin evidence, and the Phase 19/20 boundaries."
    - "The published `safe_trigger()` wording matches the resolved research decision: ownership admission is outside its catch while ordinary post-admission Exceptions remain value-returning."
  artifacts:
    - {path: "README.md", provides: "Concise public ownership and safe-trigger contract"}
    - {path: "docs/QUICK_START.md", provides: "Actionable sync/async ownership guidance and non-promises"}
    - {path: "docs/dev/architecture.md", provides: "Maintainer ownership/admission/lifecycle model"}
    - {path: "docs/dev/testing.md", provides: "Deterministic strict-RED, pure/native, and exact-SHA gate guidance"}
    - {path: ".specify/decisions/ADR-005-safe-ownership-concurrency.md", provides: "Append-only ownership/reentry/loop/callback decision record"}
  key_links:
    - {from: "README/Quick Start", to: "runtime behavior", via: "exact operation, exception, loop, callback, and non-promise vocabulary", pattern: "ownership|RuntimeError|event loop|inline"}
    - {from: "architecture/testing/ADR", to: "runtime/tests/CI", via: "admission flow, strict RED ownership, fresh origins, and hosted SHA evidence", pattern: "admission|xfail|native|SHA"}
  prohibitions:
    - "[UNVERIFIED - no SPEC] Do not turn per-machine serialization into a global lock, hidden fair queue, timeout, or scheduler-order promise."
    - "[UNVERIFIED - no SPEC] Do not silently rebind an async machine to another event loop or implicitly offload synchronous callbacks to worker threads."
    - "[UNVERIFIED - no SPEC] Do not disclose trigger arguments, keyword values, callback payloads, stored causes, or arbitrary exception text in ownership errors or logs."
    - "[UNVERIFIED - no SPEC] Do not convert, shield, or swallow cancellation, and do not roll back Phase 17 state/history after commit."
---

<objective>
Publish the exact ownership contract in user guidance, maintainer documentation, and ADR-005.

Purpose: Make the runtime semantics from Plans 18-01 through 18-05 auditable and usable without mixing documentation work into the final CI/performance/baseline closure gate.
Output: Updated README, Quick Start, architecture/testing guides, and accepted ADR-005.
</objective>

## Artifacts this phase produces

- New private runtime symbols in `src/fast_fsm/core.py`: `_ownership_root`, `_prepared_declarative_guard`, base/async ownership slots/helpers, private trigger/control bodies, and the short D-12 admission gate.
- New files: `tests/test_ownership_concurrency.py`, `tools/phase18_native_probe.py`, `18-PERFORMANCE-EVIDENCE.md`, and `.specify/decisions/ADR-005-safe-ownership-concurrency.md`.
- Extended `phase18` verification suite and supported-Python native probe matrix; no new public symbol, runtime dependency, schema, external API, or installed-wheel parity claim.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md
@.planning/phases/18-safe-ownership-concurrency/18-RESEARCH.md
@.planning/phases/18-safe-ownership-concurrency/18-PATTERNS.md
@.planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md
@.planning/phases/18-safe-ownership-concurrency/18-05-SUMMARY.md
@.planning/phases/17-atomic-transition-lifecycle/17-VERIFICATION.md
@.specify/decisions/ADR-004-atomic-transition-lifecycle.md
@.specify/memory/spr-core-api.md

<interfaces>
- `safe_trigger()` admits ownership outside its ordinary Exception conversion catch per the resolved D-01/D-14 decision.
- D-12 initial binding/configuration uses a short non-awaiting per-instance gate that installs reservations atomically and is never held across an await, callback, lifecycle, or mutation body.
- The user contract promises mutual exclusion and loop responsiveness, not fairness, timeout, queue ordering, transfer, offload, or cross-field read snapshots.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the exact user-facing ownership contract</name>
  <reversibility rating="costly">The D-01, D-06, D-09, and D-13 exception, scheduling, loop, and callback contracts become public v0.3.0 behavior.</reversibility>
  <files>README.md, docs/QUICK_START.md</files>
  <read_first>.planning/phases/18-safe-ownership-concurrency/18-CONTEXT.md, .planning/phases/18-safe-ownership-concurrency/18-05-SUMMARY.md, README.md:300-380, docs/QUICK_START.md:480-530, .specify/memory/spr-core-api.md</read_first>
  <action>Document D-01 through D-16 as one concise public contract. Explain per-instance thread serialization, same-owner `RuntimeError`, permanent first-loop binding, same-loop task waiting without event-loop blocking, causal child-task rejection, the complete write families, cleanup/state/history boundaries, and inline synchronous callbacks with same-slot awaited async callbacks. Use the resolved safe-trigger wording exactly: ownership, loop, causal-root, foreign-thread, and busy preconditions occur before the catch and raise, while ordinary post-admission Exceptions retain value conversion. State that there is no fairness, timeout, queue-order, loop-transfer, automatic offload, cross-field snapshot, or installed-artifact promise. Keep Phase 19 diagnostic snapshots and Phase 20 installed-artifact parity explicit exclusions. Do not publish private helper names as public API.</action>
  <acceptance_criteria>
    - Public docs distinguish ownership preconditions from ordinary `TransitionResult` failure and never imply `safe_trigger()` catches ownership misuse.
    - Sync/async serialization, permanent loop identity, causal reentry, all write families, cleanup, and inline callbacks are explicit.
    - Every deferred scheduler, snapshot, transfer, offload, and installed-artifact behavior remains a named non-promise.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_readme_examples.py tests/test_ownership_concurrency.py -x -q -k "docs or ownership or callback"</automated>
    <automated>uv run sphinx-build -b html docs docs/_build/html -W --keep-going</automated>
    <automated>uv run sphinx-build -b doctest docs docs/_build/doctest</automated>
  </verify>
  <done>Users have one accurate ownership/reentry/loop/callback contract whose safe-trigger boundary and non-promises match the locked decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Record the maintainer architecture, validation protocol, and ADR</name>
  <files>docs/dev/architecture.md, docs/dev/testing.md, .specify/decisions/ADR-005-safe-ownership-concurrency.md</files>
  <read_first>.planning/phases/18-safe-ownership-concurrency/18-RESEARCH.md, .planning/phases/18-safe-ownership-concurrency/18-VALIDATION.md, docs/dev/architecture.md:161-258, docs/dev/testing.md:171-219, .specify/decisions/ADR-004-atomic-transition-lifecycle.md, .specify/memory/spr-core-api.md</read_first>
  <action>Document admission before preparation, private already-owned bodies, sync/async cleanup shapes, the exact D-12 short-gate reservation race, permanent loop identity, ContextVar roots/markers, complete writer enforcement, and inline/no-offload behavior. Explain Wave 0's strict-xfail ownership table, per-plan marker removal/RED/GREEN protocol, the Plan 18-04 structural tightening point, fresh pure/native origins, the current-runtime compile-first representation probe, and the exact-SHA hosted 3.10–3.14 closure gate. Create accepted append-only ADR-005 following ADR-004's structure, recording D-01 through D-16 and rejection of RLock, global/shared locks, blocking a thread lock across await, task identity alone, rebinding, automatic offload, and public-to-public ownership delegation. Read the SPR as consistency authority without modifying it and do not edit ADR-004.</action>
  <acceptance_criteria>
    - Architecture and ADR describe the same D-12 arbitration and safe-trigger boundary as research/runtime.
    - Testing guidance makes staged strict RED, full writer enforcement, fresh origins, and exact-SHA hosted success independently reproducible.
    - ADR-005 is accepted and append-only, with Phase 19 and Phase 20 scope fences intact.
  </acceptance_criteria>
  <verify>
    <automated>uv run sphinx-build -b html docs docs/_build/html -W --keep-going</automated>
    <automated>uv run sphinx-build -b doctest docs docs/_build/doctest</automated>
    <automated>uv run python -c "from pathlib import Path; paths=['README.md','docs/QUICK_START.md','docs/dev/architecture.md','docs/dev/testing.md','.specify/memory/spr-core-api.md','.specify/decisions/ADR-005-safe-ownership-concurrency.md']; text='\n'.join(Path(p).read_text() for p in paths); assert all(token in text for token in ('RuntimeError','safe_trigger','event loop','inline','200,000','exact SHA'))"</automated>
  </verify>
  <done>Maintainers have an exact architecture and validation contract, ADR-005 records the lasting choices, and no later-phase or hidden scheduler promise is made.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 / block-on-high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | public ownership contract | high | mitigate | Document admission-before-preparation and complete writer coverage with matching behavior tests. |
| T-18-03 | Tampering/DoS | loop ownership wording | high | mitigate | Permanent loop identity and D-12 race are explicit in public and maintainer docs. |
| T-18-06 | Repudiation/Tampering | `safe_trigger()` wording | high | mitigate | Exact resolved precondition-outside-catch wording appears in docs and ADR. |
| T-18-07 | DoS/Tampering | cancellation contract | high | mitigate | Docs preserve bare propagation, coherent Phase 17 boundaries, and cleanup. |
| T-18-08 | Information Disclosure | examples/errors | high | mitigate | Examples use stable categories only and disclose no caller payload/cause. |
| T-18-09 | DoS | fairness/timeout/offload expectations | medium | accept | Every non-promise is explicit in user docs and ADR. |
</threat_model>

<verification>Build documentation with warnings as errors, run doctests and README ownership examples, and assert the required contract vocabulary across public/maintainer/SPR/ADR records.</verification>
<success_criteria>All sixteen locked decisions and all seven requirements are documented without contradictions, the two resolved research decisions are explicit, and Phase 19/20 boundaries remain intact.</success_criteria>
<output>Create `.planning/phases/18-safe-ownership-concurrency/18-06-SUMMARY.md` when done.</output>
