---
phase: 20-installed-artifact-parity-release-proof
plan: 06
type: execute
wave: 6
depends_on: [20-05]
files_modified:
  - .github/workflows/release-evidence.yml
  - .github/workflows/release.yml
  - .github/workflows/ci.yml
  - tests/test_release_evidence.py
  - Taskfile.yml
  - docs/dev/releasing.md
  - docs/dev/testing.md
autonomous: true
requirements: [REL-01, REL-07, TEST-01, TEST-03, TEST-04, TEST-06]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-03, every produced compiled wheel is consumed by a matching native verification cell running the complete shared oracle and uploading one machine-readable record; cross-build success alone never counts."
    - "Per D-01/D-04, release CI builds the intentional universal pure wheel, sdist with explicit derivation proof, and the declared CPython 3.10-3.14 compiled platform matrix without accepting AUTO as compiled evidence."
    - "Per D-14, the write-capable GitHub release job has one successful dependency path through quality, build, native verification, identity, conformance, installed performance, and exact aggregation, with no partial/advisory bypass."
    - "Per D-10, ordinary CI validates static v0.3.0 identity and tag-triggered CI additionally proves v0.3.0 resolves to the exact aggregated checkout commit before release creation."
    - "Per D-03/D-14, a manual/reusable evidence-only workflow resolves an input ref once, checks out that exact SHA in every job, executes the complete hosted `release` matrix, exposes head SHA/terminal aggregate/matrix/evidence artifacts, and has no structural path or permission capable of creating a GitHub release."
    - "Per D-03/D-14, hosted native-runner availability is never inferred from YAML: an explicitly authorized exact-SHA evidence run and read-only terminal-evidence inspection must pass before tagging, and any unavailable runner or missing matrix cell blocks release."
    - "The local readiness command validates only the deterministic `local` projection from Plan 20-03; it proves locally available direct/derived artifacts, parity, origin, and installed compiled performance but cannot emit release authorization."
  artifacts:
    - path: ".github/workflows/release-evidence.yml"
      provides: "Manual/reusable exact-SHA evidence-only full native matrix with no release job or write permission"
    - path: ".github/workflows/release.yml"
      provides: "Tag-only caller → accepted reusable release evidence → tag identity → release dependency graph"
    - path: ".github/workflows/ci.yml"
      provides: "Static identity and local contract gates without source-only release claims"
    - path: "tests/test_release_evidence.py"
      provides: "YAML-semantic action pin, matrix routing, permissions, and release-bypass regression tests"
    - path: "Taskfile.yml"
      provides: "uv-based local artifact verify, aggregate, identity, and readiness commands"
    - path: "docs/dev/releasing.md"
      provides: "No-publication implementation runbook and later authorized tag/native evidence checklist"
    - path: "docs/dev/testing.md"
      provides: "Source versus installed evidence boundaries and sequential validation commands"
  key_links:
    - from: "compiled build matrix"
      to: "matching native verification matrix"
      via: "artifact identity/matrix key routing and one uploaded evidence JSON per artifact/runtime cell"
      pattern: "build_wheels|verify_native|upload-artifact"
    - from: "native verification/performance records"
      to: "aggregate job"
      via: "downloaded exact evidence set validated by release_evidence aggregator"
      pattern: "aggregate|needs"
    - from: "aggregate and tag identity jobs"
      to: "github_release"
      via: "ordinary successful `needs` edges with contents:write scoped only to final job"
      pattern: "github_release|contents: write"
    - from: "explicitly authorized hosted run"
      to: "v0.3.0 tag-time operation"
      via: "read-only inspection of the evidence-only workflow's exact head SHA, terminal aggregate job, canonical matrix digest, and downloaded evidence artifacts"
      pattern: "release-hosted-prerelease-check|FAST_FSM_HOSTED_RUN_ID|FAST_FSM_EXPECTED_SHA"
    - from: "task release-readiness-check"
      to: "canonical expected matrix"
      via: "`profile=local` projection only, with a non-authorizing result rejected by release consumers"
      pattern: "profile.*local|release_authorized"
  prohibitions:
    - statement: "Do not dispatch hosted workflows, create or move a tag, create a GitHub release, publish to PyPI, or push a branch while executing this plan."
      status: unverified
      verification: judgment
    - statement: "Do not claim hosted matrix success from YAML inspection or local workflow contract tests; those prove wiring only."
      status: unverified
      verification: judgment
    - statement: "Do not let an advisory job, artifact glob/count, cross-build skip, or `always()` condition authorize release creation."
      status: unverified
      verification: judgment
---

<objective>
Wire the complete installed-artifact evidence system into native CI and the release dependency graph, then publish exact local and authorized tag-time procedures without performing any external release action.

Purpose: Make D-03/D-10/D-14 mechanically true in workflow contracts so a release can only be created after every required artifact, semantic, identity, and performance proof succeeds.
Output: Exact-SHA evidence-only native workflow, tag-only release caller, immutable action pins, release-bypass regression tests, local projection commands, read-only hosted inspection, and maintainer runbooks.
</objective>

## Artifacts this phase produces

- A manual/reusable evidence-only CI contract for one pure wheel, one sdist, explicit sdist-derived pure/compiled wheels, and the full supported compiled CPython/platform matrix at one resolved SHA.
- One checkout-independent conformance/evidence verifier invoked identically for every native artifact execution.
- Per-artifact evidence uploads, exact expected-matrix aggregation, static and tag-time identity checks, and installed compiled performance gating.
- A tag-only write-capable release job reachable only through the reusable workflow's accepted `release` aggregate and exact tag-to-commit proof; manual/reusable evidence invocations cannot reach it.
- Local uv Taskfile commands and documentation that distinguish local/workflow-contract proof from later hosted/native and public release evidence.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Installed pure and compiled artifacts prove equal hardened behavior, identity, and performance before v0.3.0 publication | 20-01..20-06 | COVERED | Tracer → artifact set → matrix profiles/identity → history → performance → release gate. |
| REQ | REL-01 | All metadata/docs/tag identity is v0.3.0 | 20-03, 20-06 | COVERED | Static and tag-time gates are separate. |
| REQ | REL-03 | Explicit compiled failure and native inclusion | 20-02 | COVERED | Compiler and post-build/post-install checks. |
| REQ | REL-07 | Installed pure/compiled CI proof | 20-01, 20-02, 20-06 | COVERED | Exact path, neutral cwd, matching native cells. |
| REQ | TEST-01 | One parameterized hardened conformance suite | 20-01, 20-06 | COVERED | Shared digest-bound oracle in all modes. |
| REQ | TEST-03 | Substantive native compiled behavior | 20-01, 20-06 | COVERED | Complete oracle, never smoke import. |
| REQ | TEST-04 | Origin/version/architecture/parity/type | 20-01, 20-02, 20-03, 20-06 | COVERED | Per-record validation plus profile-exact aggregate. |
| REQ | TEST-05 | Phase 16-19 performance evidence | 20-04, 20-05 | COVERED | Truthful categorical history plus exact new installed rerun. |
| REQ | TEST-06 | Installed compiled 200000 ops/sec floor | 20-05, 20-06 | COVERED | Native-origin-first median gate. |
| REQ | TEST-07 | O(1) runtime and separate diagnostic budgets | 20-05 | COVERED | Invariants/scaling plus deterministic counters. |
| CONTEXT | D-01 | Pure wheel, sdist, declared compiled matrix | 20-02, 20-06 | COVERED | Explicit artifact and CI matrix contracts. |
| CONTEXT | D-02 | Fresh exact-path isolation and spoof rejection | 20-01, 20-02 | COVERED | Installed verifier and sdist children. |
| CONTEXT | D-03 | Matching native verifier/evidence per artifact | 20-06 | COVERED | Exact-SHA evidence-only workflow routes cross-build outputs to native cells. |
| CONTEXT | D-04 | Sdist archive plus explicit pure/compiled children | 20-02, 20-06 | COVERED | Bounded inspection and lineage proof. |
| CONTEXT | D-05 | One oracle across source/artifact/API styles | 20-01 | COVERED | One collector and suite digest. |
| CONTEXT | D-06 | All Phase 16-19 hardened contracts | 20-01 | COVERED | Required scenario families. |
| CONTEXT | D-07 | Deterministic payload-safe parity records | 20-01, 20-03 | COVERED | Allowlists and scenario/field diffs. |
| CONTEXT | D-08 | Complete installed record identity | 20-01, 20-03 | COVERED | Parent/child checks and schema. |
| CONTEXT | D-09 | Exact complete aggregate and summary | 20-03 | COVERED | Set reconciliation and deterministic rendering. |
| CONTEXT | D-10 | v0.3.0 surface and exact tag commit | 20-03, 20-06 | COVERED | No tag creation during implementation. |
| CONTEXT | D-11 | Native installed compiled floor | 20-05, 20-06 | COVERED | Origin precedes benchmark. |
| CONTEXT | D-12 | O(1) runtime / separate diagnostics | 20-05 | COVERED | Different evidence sections and tests. |
| CONTEXT | D-13 | Historical evidence plus installed rerun | 20-04, 20-05 | COVERED | Original unavailable precision is explicit; new installed proof is exact. |
| CONTEXT | D-14 | All gates precede release creation | 20-06 | COVERED | Evidence-only invocations cannot publish; tag release uses ordinary needs and scoped write permission. |
| RESEARCH | R-01 | Parent verifier plus checkout-independent child | 20-01 | COVERED | Exact byte/runtime binding. |
| RESEARCH | R-02 | Explicit sdist derivation graph | 20-02 | COVERED | Parent archive and two children. |
| RESEARCH | R-03 | Canonical matrix and universal2 dual-native proof | 20-03, 20-06 | COVERED | Runtime keys distinguish native slices and local/release profiles. |
| RESEARCH | R-04 | Deterministic semantic oracle | 20-01 | COVERED | Stable IDs/schema/digest/redaction. |
| RESEARCH | R-05 | Two-stage release identity | 20-03, 20-06 | COVERED | Static then tag-time. |
| RESEARCH | R-06 | Native performance and complexity evidence | 20-05 | COVERED | Multi-sample median plus invariants. |
| RESEARCH | R-07 | Bounded archive/evidence parsing | 20-01, 20-02, 20-03 | COVERED | Size/count/schema bounds. |
| RESEARCH | R-08 | Immutable current cibuildwheel action pin | 20-06 | COVERED | Upgrade is isolated and regression-tested. |
| RESEARCH | R-09 | Slots registry conflict reconciliation | 20-04 | COVERED | Accepted Phase 19 runtime fact aligned across authorities. |

Excluded without gap: CONTEXT Deferred Ideas (PyPI/trusted publisher, new platforms, musllinux, PyPy, build-info API, topology snapshot v2). RESEARCH runner entitlement is covered by the fail-closed, explicitly authorized, exact-SHA pre-release checkpoint documented and validated by Task 3; availability remains external state and is not claimed by implementation.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-CONTEXT.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-RESEARCH.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-PATTERNS.md
@.planning/phases/20-installed-artifact-parity-release-proof/20-05-SUMMARY.md
@.github/workflows/release-evidence.yml
@.github/workflows/release.yml
@.github/workflows/ci.yml
@Taskfile.yml

<interfaces>
- Use the Plan 20-03 canonical expected matrix as the workflow contract; hosted YAML selectors/routing serialize only the `release` profile, while Taskfile readiness consumes only `local`.
- `.github/workflows/release-evidence.yml` owns `workflow_dispatch` and `workflow_call`, resolves its required `ref` input to one full SHA, and exposes that SHA plus terminal aggregate/matrix/evidence outputs. It contains no release job and no `contents: write` permission.
- Every build job uploads artifact bytes separately from evidence. Every matching native verify job downloads one concrete artifact, runs the same `release_evidence.py` verifier, and uploads one uniquely keyed evidence JSON.
- `aggregate_release_evidence` downloads all required evidence and invokes strict `profile=release` aggregation. Tag-triggered `.github/workflows/release.yml` calls the reusable workflow, then `github_release` uses ordinary successful dependencies on its accepted outputs plus tag identity and has the only `contents: write` permission.
- All third-party Actions use full 40-character immutable commit SHAs with an adjacent version provenance comment; cibuildwheel is upgraded to the researched official v4.2.0 SHA `1828c10ab37f080699c7b81cea34097c684a7074` and its output tags/native contents are regression-tested.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build an exact-SHA evidence-only native workflow</name>
  <reversibility rating="costly">D-01/D-03 define the published platform matrix and native proof routing; changing it later alters release artifacts and runner requirements.</reversibility>
  <files>.github/workflows/release-evidence.yml, .github/workflows/release.yml, tests/test_release_evidence.py</files>
  <read_first>.github/workflows/release.yml current build/sdist/test-skip/artifact jobs; .github/workflows/ci.yml quality/native patterns; tools/release_evidence.py canonical `release` profile and CLI from Plans 20-01..20-05; tests/test_release_evidence.py workflow YAML semantic helpers/action pin tests; 20-CONTEXT.md D-01/D-03/D-04/D-05/D-14; 20-RESEARCH.md Pattern 3 and native runner caveat; 20-01-PLAN.md A20-07</read_first>
  <behavior>
    - Manual and reusable invocations require a `ref`, resolve it once to a full 40-character SHA, expose that `head_sha`, and make every quality/build/verify/aggregate job check out the resolved SHA rather than a moving ref.
    - Builders produce the declared CPython 3.10-3.14 pure/sdist/compiled artifact set, but only matching native verifier jobs create accepted runtime evidence under `profile=release`.
    - Each native verifier installs one concrete artifact outside the checkout, runs the complete suite-digest-bound oracle, asserts identity/origin/architecture/mode, and uploads a unique JSON record.
    - Linux aarch64 and macOS x86_64 cross-built outputs have explicit matching native consumers; universal2 is verified on both macOS architectures.
    - The terminal aggregate job exposes the resolved head SHA, terminal job identity/conclusion, canonical release-matrix digest, manifest/summary artifact name, and per-cell evidence artifact names for later read-only inspection.
    - The evidence-only workflow has read-only permissions, no `github_release` job, no release API/action, and no output usable as authorization unless the tag-only caller separately passes aggregate and tag equality.
    - All action refs are immutable full SHAs; cibuildwheel v4.2.0 changes are covered by tag/native-content contract tests.
  </behavior>
  <action>Start with YAML-semantic tests generated from the Plan 20-03 canonical `release` matrix. Create `.github/workflows/release-evidence.yml` with both `workflow_dispatch` and `workflow_call` entry points and one required `ref` input. Add an initial resolver job that checks out the input, resolves `HEAD` to a full SHA, emits it as `head_sha`, and require every downstream checkout, artifact name, evidence record, and aggregate input to use that immutable output. Move/refactor quality, artifact builders, matching native verification, and aggregate jobs into this evidence-only workflow per D-03. Preserve CPython 3.10-3.14, Linux x86_64/aarch64, Windows AMD64, and macOS x86_64/arm64/universal2 while excluding musllinux. Build the intentional pure wheel and sdist/children under explicit intent; route every compiled artifact, including cross-built Linux aarch64/macOS x86_64 and both universal2 slices, to a matching native runner executing the full installed oracle. Upload uniquely SHA-keyed machine records and have the terminal `aggregate_release_evidence` job run only `profile=release`, expose its job/conclusion, matrix digest, manifest/summary artifact name, and evidence artifact inventory. Give the entire evidence-only workflow read-only permissions and structurally forbid any release action/API/job. Leave `release.yml` as the future tag-only caller shell for Task 2. Upgrade cibuildwheel to the exact full v4.2.0 SHA in Interfaces, keep all other actions full-SHA pinned, and update pin/tag/native tests. Do not dispatch either entry point.</action>
  <acceptance_criteria>
    - Workflow selectors and routing keys equal the canonical expected matrix exactly for Python 3.10-3.14 and all declared native architecture cells.
    - Both manual and reusable entry points resolve the supplied ref once; mutation tests fail if any downstream job checks out the original ref, default branch, or event SHA instead of the emitted full `head_sha`.
    - Per A20-07, every accepted native cell invokes the complete shared oracle/verifier and uploads a record; no smoke import, builder success, QEMU/cross-build skip, or file count is accepted as behavior proof.
    - Linux aarch64 and macOS x86_64 outputs have matching native consumers; universal2 has separate arm64 and x86_64 runtime records bound to one wheel SHA.
    - Pure and sdist jobs use explicit intended modes/derivations; compiled jobs cannot use AUTO.
    - Every third-party action ref is a full SHA with adjacent version comment, and cibuildwheel v4.2.0 tag/native archive expectations pass regression tests.
    - The terminal job exposes exact head SHA, terminal status/identity, canonical matrix digest, manifest/summary artifact, and per-cell evidence inventory; the workflow has no write permission or release-capable node.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_release_evidence.py -x -q -k 'workflow and (evidence_only or exact_sha or matrix or native or action or cibuildwheel or artifact)'</automated>
  </verify>
  <done>A manual/reusable exact-SHA workflow produces inspectable full hosted release-matrix evidence while remaining structurally incapable of publishing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make exact aggregation and tag identity the only release path</name>
  <reversibility rating="costly">This task implements the reversible workflow gate around D-10's separately recorded one-way public tag/release operation; changing the gate later requires coordinated workflow and evidence-schema updates.</reversibility>
  <files>.github/workflows/release-evidence.yml, .github/workflows/release.yml, .github/workflows/ci.yml, tests/test_release_evidence.py</files>
  <read_first>.github/workflows/release-evidence.yml after Task 1; .github/workflows/release.yml tag/release shell; .github/workflows/ci.yml quality/source gates; tools/release_evidence.py aggregate/static/tag identity/performance commands; tests/test_release_evidence.py workflow mutation helpers; 20-CONTEXT.md D-09/D-10/D-11/D-14; 20-01-PLAN.md A20-03</read_first>
  <behavior>
    - Static identity and quality gates precede all builders in the reusable evidence workflow; downloaded records feed strict `release` aggregation after every native verifier.
    - Installed compiled performance is blocking within accepted native evidence; pure/historical/advisory results cannot satisfy aggregate success.
    - Tag event additionally proves `v0.3.0`, checkout SHA, aggregate commit, and peeled tag commit equality.
    - Tag-triggered `release.yml` is the only workflow containing a write-capable release job; it calls the reusable evidence workflow with the tag ref and has ordinary successful dependencies on its aggregate outputs plus tag identity.
    - Direct `workflow_dispatch`/`workflow_call` of the evidence workflow cannot reach `github_release`, even with crafted inputs or local-profile artifacts.
  </behavior>
  <action>Complete the reusable workflow's `aggregate_release_evidence` job so it depends on all quality/build/native verification branches, downloads exact SHA-keyed records, invokes `profile=release`, validates semantic parity/history/identity/installed compiled performance, and emits deterministic manifest/summary outputs. Add static identity to ordinary CI. Make `.github/workflows/release.yml` tag-triggered only: call `release-evidence.yml` with the triggering tag ref, then run a non-mutating identity job that peels exact `v0.3.0` and compares tag, checkout, reusable `head_sha`, and aggregate commit. Make `github_release` depend normally on the successful reusable evidence call and tag identity, consume only aggregate-approved artifact SHA paths, and retain `contents: write` only in that final tag-only job. Add graph/mutation tests proving no `workflow_dispatch` or direct `workflow_call` event can reach the release job because it lives outside the evidence-only workflow, and proving local-profile output is rejected as authorization. Remove any permissive condition, advisory edge, glob/count completeness, or alternate write-capable path. Do not create a tag/release or dispatch hosted CI.</action>
  <acceptance_criteria>
    - Tag-only caller → reusable static identity/quality → build → matching-native verify → exact `release` aggregate/performance → tag identity → github_release is the only topological release path.
    - Per A20-03, ordinary PR/static checks do not require a tag; the tag job requires exact `v0.3.0` and peeled ref, checkout, and aggregate commit equality after aggregate success.
    - `github_release` cannot run if any required quality/build/native/parity/identity/performance/aggregate job fails, skips, is absent, or is advisory.
    - Only the final release job has `contents: write`; no `always()`, `continue-on-error`, artifact count/glob completeness, or alternate write-capable path bypasses the aggregate.
    - Workflow tests mutate each required edge/permission/condition and fail, proving the checks are structural rather than text-count assertions.
    - Evidence-only `workflow_dispatch`/`workflow_call` graphs contain no release-capable node or write permission, and a `local` aggregate cannot satisfy the tag caller's authorization contract.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_release_evidence.py -x -q -k 'workflow and (aggregate or identity or release or permission or bypass)'</automated>
  </verify>
  <done>GitHub release creation is structurally unreachable unless the complete native installed evidence aggregate, fixed performance floor, and exact v0.3.0 tag-to-commit identity all succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish local projection and read-only hosted evidence checks</name>
  <files>Taskfile.yml, tests/test_release_evidence.py, docs/dev/releasing.md, docs/dev/testing.md</files>
  <read_first>Taskfile.yml release evidence commands; docs/dev/releasing.md source checks/release steps; docs/dev/testing.md Phase 20 handoff; .github/copilot-instructions.md uv/testing/typecheck/docs rules; tools/release_evidence.py final CLI; release.yml after Tasks 1-2; 20-CONTEXT.md D-11/D-13/D-14 and Deferred Ideas</read_first>
  <behavior>
    - `release-identity-check`, `release-installed-artifacts-check`, `release-sdist-check`, `release-evidence-local-check`, `release-installed-performance-check`, and `release-slots-check` are exact read-only Taskfile entry points; temporary build/install outputs are allowed, but none rewrites tracked evidence, tags, refs, or remote state.
    - `release-evidence-local-check` builds/validates the canonical `local` projection for the available interpreter/platform, including direct pure/compiled wheels, sdist-derived pure/compiled children, parity, origin, and installed compiled performance, and always emits non-authorizing scope.
    - `release-readiness-check` invokes, in deterministic sequential order, `format-check`, `lint`, `typecheck-mypy`, `test`, `docs-check`, `docs-test`, `pure-source-check`, `release-baseline-check`, and all six local Phase 20 checks; it never invokes `profile=release` or a hosted inspection and cannot produce release authorization. It runs `typecheck-ty` as separately labelled advisory feedback whose exact exit/output is recorded without authorizing release.
    - Taskfile semantic tests fail if any required transitive command is missing, reordered across an evidence prerequisite, replaced by a write/publication task, or permitted to pass through an advisory/ignored blocking gate.
    - `release-hosted-prerelease-check FAST_FSM_HOSTED_RUN_ID=<authorized-run-id> FAST_FSM_EXPECTED_SHA=<40-char-sha>` is read-only: it inspects the evidence-only workflow's run metadata and terminal job, downloads evidence artifacts to a temporary directory, and validates exact head SHA, `release` matrix digest/completeness, evidence artifact SHA bindings, and successful terminal aggregation before tagging.
  </behavior>
  <action>Write Taskfile-structure tests first, then add the exact uv-backed commands `release-identity-check`, `release-installed-artifacts-check`, `release-sdist-check`, `release-evidence-local-check`, `release-installed-performance-check`, `release-slots-check`, `release-readiness-check`, and `release-hosted-prerelease-check`. Wire the first six to the read-only CLI checks delivered by Plans 20-01 through 20-05 with explicit artifact paths/build intent and isolated temporary output. Make `release-evidence-local-check` request only `profile=local`, build the executing interpreter/platform's direct pure/compiled wheels and sdist-derived pure/compiled children, validate parity/origin/installed compiled performance, and assert the result has non-authorizing scope. Make `release-readiness-check` sequentially run Ruff format checking, Ruff lint, blocking mypy, the full sequential pytest suite, Sphinx warnings-as-errors, Sphinx doctests, source-origin preflight, baseline freshness, the six local evidence checks, and separately labelled advisory ty; add structural tests that reject any `profile=release`, release-authorization output, hosted command, manifest write, or publication edge in this local task. Implement `release-hosted-prerelease-check` as a separate explicitly invoked read-only inspection path: validate both inputs, use `gh run view` to capture the authorized `release-evidence.yml` run's head SHA/event/status/conclusion/jobs, require its terminal `aggregate_release_evidence` job to succeed, use `gh run download` into a fresh temporary directory for the SHA-keyed manifest/summary/per-cell evidence artifacts, then invoke `tools/release_evidence.py` to validate `profile=release`, canonical matrix digest/completeness, and every artifact/evidence SHA against `FAST_FSM_EXPECTED_SHA`. Unavailable, queued, skipped, cancelled, missing, mismatched, non-evidence workflow, or non-terminal results fail. Add fixture-driven tests for the inspection parser without contacting GitHub. Update releasing/testing runbooks with the manual/reusable exact-SHA workflow, how to obtain the run ID, exact inspection command, local-versus-hosted profile boundary, historical-versus-final performance distinction, and tag-time equality. Execute only `task release-readiness-check`; do not execute the hosted command or any remote mutation. Record the advisory ty result verbatim in the summary.</action>
  <acceptance_criteria>
    - The eight exact Taskfile commands named in `<behavior>` exist, invoke uv-based tooling with explicit artifact paths/build intent, and keep local checks read-only with respect to tracked evidence and external release state; no direct pip/python or publication command is introduced.
    - `task release-readiness-check` transitively and sequentially invokes format-check, lint, blocking mypy, full tests, docs HTML/doctests, source-origin preflight, baseline freshness, locally built direct pure/compiled artifacts, sdist-derived pure/compiled children, parity, origin, installed compiled performance, and slots policy through `profile=local`.
    - A passing local readiness result is marked non-authorizing, cannot be consumed by tag/release jobs, and fails tests if it invokes `profile=release`, hosted inspection, or any release-authorization constructor.
    - Releasing/testing docs distinguish source, installed pure, installed compiled, sdist lineage, historical performance, local workflow contracts, hosted native evidence, and public tag/release operations.
    - Documentation states the fixed 200000 compiled floor and O(1)/diagnostic contracts without embedding volatile exact rates or claiming hosted matrix success.
    - Per D-03/D-14, the documented hosted-native checkpoint occurs before tagging and inspects an explicitly authorized evidence-only run's head SHA, successful terminal aggregate job, full canonical release matrix digest, and downloaded evidence artifact bindings; this plan neither runs it nor records it as passed.
    - The single local readiness command passes all blocking local gates and reports ty separately with its exact status/output.
    - No task dispatches a hosted workflow, pushes a ref, creates/moves a tag, creates a release, or publishes an artifact externally.
  </acceptance_criteria>
  <verify>
    <automated>task release-readiness-check</automated>
    <human-check>Confirm the readiness output and plan summary record the `task typecheck-ty` advisory exit status/output verbatim without treating it as a blocking-gate or hosted-release result.</human-check>
  </verify>
  <done>Maintainers have a passing non-authorizing local projection and a separate exact-SHA read-only path for inspecting terminal hosted release evidence before any tag operation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build artifacts → matching native jobs | Cross-runner artifacts can be swapped, routed to the wrong architecture, or counted without execution. |
| downloaded evidence → aggregate | Artifact names/globs can hide duplicates, missing cells, malformed records, or detached digests. |
| manual/reusable evidence workflow → tag-only release workflow | Evidence runs must remain non-publishing until a separate exact tag identity and authorization path succeeds. |
| aggregate/tag identity → write-capable release job | Dependency/condition/permission mistakes can bypass failed gates or release the wrong commit. |
| third-party action ref → workflow execution | Mutable tags can change build/upload/release behavior without repository review. |
| local projection → release authorization | A locally complete subset can be mislabeled as the full hosted matrix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-23 | Tampering | artifact routing/native evidence | high | mitigate | Canonical release-profile keys, artifact SHA binding, matching native runner assertions, unique evidence names, and universal2 dual-runtime records. |
| T-20-24 | Spoofing | source/editable import in CI | high | mitigate | Every native job invokes the Plan 20-01 neutral-cwd fresh-environment verifier and rejects checkout/direct-url origins. |
| T-20-25 | Tampering | third-party Actions | high | mitigate | Full 40-character SHAs, adjacent version comments, exact expected-pin tests, and isolated cibuildwheel tag/native regression coverage. |
| T-20-26 | Elevation of Privilege | release-gate bypass | critical | mitigate | Evidence workflow is read-only and release-free; write exists only in the tag caller's final job with ordinary successful needs on release aggregate/tag identity and mutation tests rejecting alternate paths. |
| T-20-27 | Tampering | malformed/duplicate downloaded evidence | high | mitigate | Strict bounded parser, exact expected key set, uniqueness, mixed-identity rejection, and no file-count/glob completeness acceptance. |
| T-20-28 | Repudiation | hosted execution claims and runner availability | medium | mitigate | A separately authorized evidence-only run must pass read-only head-SHA, terminal-job, matrix-digest, and downloaded-record inspection before tagging; unavailable or missing state fails closed. |
| T-20-29 | Elevation of Privilege | local projection authorization | high | mitigate | Local output carries non-authorizing scope, release consumers accept only `profile=release`, and structural tests prohibit release-profile or hosted calls from local readiness. |
</threat_model>

<verification>
- Parse workflow YAML semantically and mutation-test matrix routing, immutable pins, dependency graph, conditions, and permissions.
- Run all locally executable artifact, conformance, aggregate, identity, performance, quality, typing, and documentation gates sequentially.
- Confirm no tag/release/publish/remote-push action occurred and no hosted matrix success is claimed.
- Confirm the runbook places the externally authorized evidence-only run plus `release-hosted-prerelease-check` before tagging and that local execution invokes neither.
</verification>

<success_criteria>
- Every promised artifact has a matching substantive native verifier and evidence record in the workflow contract.
- Only the complete aggregate plus exact tag-to-commit identity can reach the write-capable release job.
- Local commands/docs clearly separate implementation proof from later hosted/public operations.
- The complete local projection passes without requiring unavailable hosted records and cannot authorize release.
- All four source types and all 13 edge-probe assumptions are covered with no deferred item accidentally planned.
</success_criteria>

<output>
Create `.planning/phases/20-installed-artifact-parity-release-proof/20-06-SUMMARY.md` when done.
</output>
