---
phase: 15-release-baseline-evidence-harness
plan: 04
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - README.md
  - docs/dev/testing.md
  - docs/dev/contributing.md
  - .github/copilot-instructions.md
  - .specify/memory/spr-core-api.md
autonomous: true
requirements:
  - REL-05
  - REL-08
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Users see durable claims such as 700+ tests and the stable compiled trigger floor, while exact changing observations live only in the evidence manifest (D-09, D-12)."
    - "Contributors know mypy is blocking, ty is advisory, and how to write/check the clean evidence baseline (D-05, D-11)."
    - "Every project policy document names and measures both CompiledFuncCondition and TransitionError as registered slots-policy exceptions, and describes the recursive all-production-class audit instead of claiming no exceptions (REL-08)."
    - "The README does not carry a persistent v0.2.3 historical warning (D-04)."
  artifacts:
    - path: "README.md"
      provides: "Durable user-facing verification/performance claims"
    - path: "docs/dev/testing.md"
      provides: "Pure-source evidence and exact-manifest workflow"
    - path: "docs/dev/contributing.md"
      provides: "Build-mode, mypy/ty, and measured slots-policy guidance"
    - path: ".github/copilot-instructions.md"
      provides: "Updated executable project quality contract"
    - path: ".specify/memory/spr-core-api.md"
      provides: "Current sparse memory of both registered slots-policy exceptions"
  key_links:
    - from: "README.md"
      to: "evidence/release-baseline.json"
      via: "durable prose links exact evidence instead of duplicating volatile values"
      pattern: "release-baseline.json"
    - from: "docs/dev/testing.md"
      to: "Taskfile.yml"
      via: "documented write/check and pure-source task commands"
      pattern: "release-baseline-check"
    - from: ".github/copilot-instructions.md"
      to: "tools/release_evidence.py"
      via: "operational rules point to canonical verifier and measured exception"
      pattern: "TransitionError"
---

<objective>
Align every durable narrative and contributor policy with the generated evidence and measured slots boundary.

Purpose: Prevent exact observations from drifting in prose and ensure future agents/contributors use the new gates correctly without overstating memory or type-check guarantees.
Output: Updated README, developer guides, agent instructions, and sparse project memory.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-release-baseline-evidence-harness/15-CONTEXT.md
@.planning/phases/15-release-baseline-evidence-harness/15-RESEARCH.md
@.planning/phases/15-release-baseline-evidence-harness/15-02-SUMMARY.md
@README.md
@docs/dev/testing.md
@docs/dev/contributing.md
@.github/copilot-instructions.md
@.specify/memory/spr-core-api.md
@evidence/release-baseline.json

<flagged_assumptions>
- REL-05 assumption: exact test/tool/coverage numbers in prose become stale at a different cadence than code. Resolve it by limiting narrative claims to “700+ tests” and stable contracts, linking the tracked manifest for exact observations.
- REL-08 assumption: policy prose can drift from the measured exception registry. Resolve it by naming both `CompiledFuncCondition` and `TransitionError`, their distinct `native_class=False` rationales, the recursive all-relevant-class audit, and the canonical slots-policy command in all authoritative contributor/agent memory.
</flagged_assumptions>
</context>

<execution_constraints>
- Use `uv` for all Python, test, and Sphinx commands.
- The phase bead `fast_fsm-lw2` is already claimed and `in_progress`; inspect it with `bd show fast_fsm-lw2 --json`. Create any newly discovered work with `bd create ... --deps discovered-from:fast_fsm-lw2 --json`. Do not close the phase bead: the root orchestrator closes it only after the final Phase 15 verifier passes.
- Preserve unrelated edits. Stage/commit only files named by the active task; never use `git add .` or `git add -A`.
- Do not copy exact test counts, coverage percentages, tool versions, host-specific timings, or benchmark observations from the manifest into narrative prose.
- Preserve the stable ≥200,000 compiled `trigger()` operations/sec contract.
- Do not add the v0.2.3 correction to README; it remains in dated release records per D-04.
</execution_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Replace volatile user/developer claims with durable evidence links and commands</name>
  <read_first>
    - README.md
    - docs/dev/testing.md
    - docs/dev/contributing.md
    - evidence/release-baseline.json
    - Taskfile.yml
  </read_first>
  <files>README.md, docs/dev/testing.md, docs/dev/contributing.md</files>
  <action>
    Replace stale exact narrative test baselines with “700+ tests” per D-09 and point exact counts, coverage, tools, origin, artifact mode, and benchmark context to `evidence/release-baseline.json`. Keep the stable ≥200,000 compiled trigger operations/sec floor per D-12. Document `FAST_FSM_BUILD_MODE=auto|pure|compiled`, the legacy pure alias, the non-destructive shadow failure/explicit cleanup procedure, and the exact Taskfile write/check commands. State that contributors review generated diffs and CI checks freshness without writing. Update contributing/testing guidance so mypy is the blocking compatibility authority and ty is separate advisory feedback. Qualify the slots rule with both measured registry entries: `CompiledFuncCondition` uses `@mypyc_attr(native_class=False)` to preserve the interpreted `Condition` boundary, while `TransitionError` uses it to preserve normal Python exception behavior; both can have instance `__dict__`. Document that the canonical command recursively audits every relevant class under `src/fast_fsm` and fails on a future unregistered or omitted exception. Do not place historical v0.2.3 correction prose in README. Commit only the three listed documentation files.
  </action>
  <acceptance_criteria>
    - Narrative docs use “700+ tests” and the stable performance floor, not stale exact baselines or volatile benchmark headlines.
    - Exact evidence is discoverable through the tracked manifest and documented write/check commands.
    - Build-mode selection, legacy compatibility, pure-source preflight, mypy/ty authority, both slots exceptions, and the recursive inventory are all accurately described.
    - README contains no persistent v0.2.3 defect warning.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_readme_examples.py -x -q</automated>
  </verify>
  <done>Users and contributors see durable claims and executable evidence procedures without duplicating volatile manifest values.</done>
</task>

<task type="auto">
  <name>Task 2: Update the agent contract and sparse memory to the measured policy</name>
  <read_first>
    - .github/copilot-instructions.md
    - .specify/memory/spr-core-api.md
    - docs/dev/contributing.md
    - tools/release_evidence.py
  </read_first>
  <files>.github/copilot-instructions.md, .specify/memory/spr-core-api.md</files>
  <action>
    Update the operational instructions to require uv-only evidence commands, blocking mypy, advisory ty, independently visible gates, clean pure-source origin verification, manifest freshness, and scoped commits in the dirty worktree. Replace the stale 290-test rule with “700+ tests” plus the manifest path. Qualify the all-slots rule by naming both `CompiledFuncCondition` and `TransitionError` as deliberate measured registry entries tied to `@mypyc_attr(native_class=False)` and ADR-003, and require recursive audit coverage for every relevant `src/fast_fsm` class so a future exception cannot be silently omitted. Update `spr-core-api.md` in the same commit so its slot/mypyc statements match source and the canonical measurement command. Do not change unrelated workflow or runtime policies. Commit only the two listed files.
  </action>
  <acceptance_criteria>
    - Project agent instructions and sparse memory agree with the source, contributor docs, and evidence registry.
    - No stale 290/653/exact test claim remains in the changed policy files.
    - Both registered exceptions are independently measured and justified; the general hot-path slots requirement remains intact and recursive discovery prevents silent omissions.
    - Mypy/ty and pure-evidence authority match CI behavior from Plan 02.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_mypyc_guard.py tests/test_release_evidence.py -x -q -k "slots or mypyc"</automated>
  </verify>
  <done>The machine-readable and human operational memories now enforce the same durable evidence and measured slots policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated evidence -> narrative docs | Exact machine observations are summarized for humans. |
| project instructions -> future agents | Operational prose controls later automated repository changes. |
| measured slots registry -> performance claim | An exception can invalidate an unqualified memory claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|--------------|---------------|
| T-15-16 | Spoofing | narrative evidence claims | medium | mitigate | Use rounded durable claims and link exact tracked evidence; prohibit copied volatile values. | Sphinx builds plus repository search/review of changed files. | Readers may not open the manifest; the stable contracts remain accurate independently. |
| T-15-17 | Tampering | agent workflow instructions | medium | mitigate | Match exact Taskfile/CLI names and source policy; update sparse memory in the same commit. | Execute documented baseline and focused slots commands. | Future tooling renames still require synchronized docs updates. |
| T-15-18 | Repudiation | slots exception policy | medium | mitigate | Name both registered classes and distinct rationales, require recursive source inventory, cite the ADR, and publish the measurement command in every authoritative policy surface. | Recursive inventory/omission tests and manifest freshness. | Instance byte size varies by platform and is explicitly labeled observational. |
| T-15-19 | Information Disclosure | documentation | low | accept | Publish tool/platform versions from the reviewed manifest only; never document secrets or full environments. | Documentation review and manifest schema tests. | Coarse public build environment facts are intentional evidence. |
</threat_model>

## Artifacts this phase produces

- Updated files: `README.md`, `docs/dev/testing.md`, `docs/dev/contributing.md`, `.github/copilot-instructions.md`, `.specify/memory/spr-core-api.md`.
- Durable narrative claims: “700+ tests” and ≥200,000 compiled `trigger()` operations/sec.
- Documented commands/interfaces: `FAST_FSM_BUILD_MODE`, `FAST_FSM_PURE_PYTHON=1`, `task pure-source-check`, `task release-baseline-write`, `task release-baseline-check`, blocking `task typecheck-mypy`, advisory `task typecheck-ty`, and slots-policy CLI usage.
- Documented measured exceptions: `CompiledFuncCondition` and `TransitionError`, each with `native_class=False`, independent instance `__dict__`/environment-labeled size evidence, and a recursive inventory contract for all relevant production classes.
- No new file, package symbol, CLI implementation, manifest schema field, generated artifact, environment variable, or public historical README warning.

<verification>
<automated>uv run sphinx-build -b html docs docs/_build/html -W --keep-going</automated>
<automated>uv run sphinx-build -b doctest docs docs/_build/doctest</automated>
<automated>task release-baseline-check</automated>
</verification>

<success_criteria>
- All changed prose uses durable claims and links exact generated evidence.
- Contributor and agent guidance accurately reflects build modes, clean-source verification, mypy/ty authority, and CI freshness behavior.
- Slots policy is both strict and honest about the measured `CompiledFuncCondition` and `TransitionError` exceptions, with recursive coverage preventing future silent omissions.
- README remains free of the historical correction warning.
</success_criteria>

<output>
Create `.planning/phases/15-release-baseline-evidence-harness/15-04-SUMMARY.md` when done.
</output>
