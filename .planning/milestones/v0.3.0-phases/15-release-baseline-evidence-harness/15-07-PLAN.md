---
phase: 15-release-baseline-evidence-harness
plan: 07
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - src/fast_fsm/core.py
autonomous: true
requirements:
  - REL-05
estimate:
  tokens: 5000
  raw_tokens: 5000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Sphinx 9.1 renders the public docstrings for State.create, StateMachine.quick_build, StateMachine.add_listener, quick_fsm, and condition_builder with warnings treated as errors (REL-05, D-07)."
    - "The repair changes docstring markup and indentation only; executable code, signatures, behavior, and the ADR-003 core.py compilation boundary remain byte-for-byte or AST-equivalent outside those docstring literals."
    - "The documentation, doctest, focused core behavior, and completed Wave 3 release gate all pass before clean baseline collection begins."
  artifacts:
    - path: "src/fast_fsm/core.py"
      provides: "Sphinx-9.1-compatible Google/reStructuredText docstrings for the five affected public APIs"
    - path: ".planning/phases/15-release-baseline-evidence-harness/15-07-SUMMARY.md"
      provides: "Exact docstring-only diff, verification, scoped-commit, and fast_fsm-041 closure evidence"
  key_links:
    - from: "src/fast_fsm/core.py"
      to: "docs/api/core.md"
      via: "Sphinx autodoc plus Napoleon renders the corrected docstrings under warnings-as-errors"
      pattern: "State.create|StateMachine.quick_build|StateMachine.add_listener|quick_fsm|condition_builder"
    - from: "15-07 docstring-only source guard"
      to: "ADR-003 mypyc boundary"
      via: "the normalized AST outside docstrings is unchanged and no non-docstring source line appears in the scoped diff"
      pattern: "core.py"
    - from: "15-07-SUMMARY.md"
      to: "Plan 15-03 clean baseline"
      via: "15-03 depends on 15-07 and consumes only a verified documentation-clean commit"
      pattern: "docs-check"
---

<objective>
Restore the blocking Sphinx warnings-as-errors gate by correcting only the five malformed public docstrings discovered during Plan 15-02.

Purpose: Complete REL-05's documentation category before the authoritative clean baseline is generated, without changing Fast FSM runtime semantics or its compiled boundary.
Output: One scoped `src/fast_fsm/core.py` documentation commit plus exact verification and bead-lifecycle evidence in `15-07-SUMMARY.md`.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-release-baseline-evidence-harness/15-CONTEXT.md
@.planning/phases/15-release-baseline-evidence-harness/15-RESEARCH.md
@.planning/phases/15-release-baseline-evidence-harness/15-PATTERNS.md
@.planning/phases/15-release-baseline-evidence-harness/15-VALIDATION.md
@.planning/phases/15-release-baseline-evidence-harness/15-02-SUMMARY.md
@.specify/decisions/ADR-003-mypyc-compilation-boundary.md
@docs/api/core.md
@docs/conf.py
@Taskfile.yml
@src/fast_fsm/core.py

<flagged_assumptions>
- REL-05 assumption: the Sphinx 9.1 failure is confined to reStructuredText markup and indentation inside five existing docstrings. Resolve it with a docstring-only source guard and the exact warnings-as-errors build rather than changing executable code or Sphinx configuration.
- D-07 assumption: a focused HTML build is necessary but not the final repository verdict. Resolve it with independent HTML, doctest, and focused core tests here, followed by `task release-gate` after all Wave 3 plans complete.
</flagged_assumptions>
</context>

<execution_constraints>
- Execute on the existing `feat/release-baseline-evidence` branch. Before editing, `git branch --show-current` must print exactly `feat/release-baseline-evidence`; do not create a replacement branch and do not implement directly on `main`.
- Inspect `fast_fsm-041` with `bd show fast_fsm-041 --json`, claim it with `bd update fast_fsm-041 --claim --json` if it is still open and unclaimed, and keep it in progress throughout the edit and every task verification. Close it with `bd close fast_fsm-041 --reason "Verified Sphinx 9.1 core docstring repair" --json` only after every automated task check and the scoped source guard pass. The parent phase bead `fast_fsm-lw2` remains open until the root orchestrator's final Phase 15 verifier passes.
- Use `uv run` for every Python, Sphinx, and pytest command. Do not use bare `python`, `pip`, or `python -m`.
- Preserve unrelated worktree edits. Stage and commit only `src/fast_fsm/core.py` for the implementation task, and allow GSD to add only `15-07-SUMMARY.md`/tracking metadata separately; never use `git add .` or `git add -A`.
- Do not run Ruff auto-fix or a repository-wide formatter. Do not change executable statements, decorators, imports, annotations, overloads, signatures, defaults, runtime strings, comments, or whitespace outside the five affected docstring literals.
- Preserve ADR-003: `src/fast_fsm/core.py` remains the sole compiled module, and this task does not alter setup/build configuration or the interpreted condition boundary.
</execution_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Repair and prove the five Sphinx-failing core docstrings end to end</name>
  <read_first>
    - .planning/phases/15-release-baseline-evidence-harness/15-02-SUMMARY.md
    - src/fast_fsm/core.py
    - docs/api/core.md
    - docs/conf.py
    - Taskfile.yml
    - .github/copilot-instructions.md
    - .specify/decisions/ADR-003-mypyc-compilation-boundary.md
  </read_first>
  <files>src/fast_fsm/core.py</files>
  <action>
    Record the pre-edit commit SHA and reproduce `uv run sphinx-build -b html docs docs/_build/html -W --keep-going`, confirming the nine reported warnings/errors are confined to `State.create`, `StateMachine.quick_build`, `StateMachine.add_listener`, `quick_fsm`, and `condition_builder`. Edit only those five docstring literal bodies. For `State.create`, render wildcard callback syntax as inline literals and indent its example as a valid reStructuredText literal/code block. For `StateMachine.quick_build` and `quick_fsm`, convert each unindented `Example` body into a correctly indented reStructuredText literal/code block. For `StateMachine.add_listener`, replace Markdown strong-marker prose with a reStructuredText-compatible rubric/plain heading and render `source`, `target`, `trigger`, and `**kwargs` as balanced inline literals. For `condition_builder`, retain the Google-style Args/Returns meaning while fixing the example-block indentation and inline literals. Preserve all documented facts and examples; do not add, remove, or alter executable code, overloads, signatures, callbacks, behavior, or compiled-boundary annotations. Before committing, run the exact docstring-change-set AST guard from `<verify>`, `git diff --check -- src/fast_fsm/core.py`, and `git diff --function-context -- src/fast_fsm/core.py`; the diff must show changes only inside the five named docstring literals. Run the fresh Sphinx HTML build, confirm all five API names occur in `docs/_build/html/api/core.html`, run doctest and the focused behavior tests, then commit only `src/fast_fsm/core.py`. Close `fast_fsm-041` only after all checks pass and record the pre-edit SHA, scoped commit SHA, commands, results, and bead closure in `15-07-SUMMARY.md`; leave `fast_fsm-lw2` open. The root orchestrator runs the full `task release-gate` after Plans 15-04, 15-06, and 15-07 all finish, satisfying D-07 without making this parallel task modify sibling-owned files.
  </action>
  <acceptance_criteria>
    - `uv run sphinx-build -b html docs docs/_build/html -W --keep-going` completes with zero warnings/errors under Sphinx 9.1, and the generated core API page contains all five affected API names.
    - `uv run sphinx-build -b doctest docs docs/_build/doctest` completes successfully.
    - The automated source guard reports exactly `State.create`, `StateMachine.quick_build`, `StateMachine.add_listener`, `quick_fsm`, and `condition_builder` as changed docstrings and proves the normalized AST is otherwise identical.
    - `git diff --function-context -- src/fast_fsm/core.py` contains no changed line outside those five docstring literals; `git diff --check` is clean.
    - Focused tests for State.create, quick_build, add_listener, quick_fsm, and condition_builder pass without behavior changes.
    - Only `src/fast_fsm/core.py` is in the implementation commit; no setup/build/config/test file or compiled-boundary behavior changes.
    - `fast_fsm-041` is closed only after verification, `fast_fsm-lw2` remains open, and both states are recorded in `15-07-SUMMARY.md`.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c 'exec("import ast, pathlib, subprocess\np=\"src/fast_fsm/core.py\"\nbefore=subprocess.check_output([\"git\",\"show\",\"HEAD:\"+p], text=True)\nafter=pathlib.Path(p).read_text(encoding=\"utf-8\")\ndef normalized(source):\n    tree=ast.parse(source)\n    docs={}\n    stack=[]\n    class Visitor(ast.NodeVisitor):\n        def visit_ClassDef(self,node): self.visit_named(node)\n        def visit_FunctionDef(self,node): self.visit_named(node)\n        def visit_AsyncFunctionDef(self,node): self.visit_named(node)\n        def visit_named(self,node):\n            stack.append(node.name)\n            if node.body and isinstance(node.body[0],ast.Expr) and isinstance(node.body[0].value,ast.Constant) and isinstance(node.body[0].value.value,str):\n                docs[\".\".join(stack)]=node.body[0].value.value\n                node.body[0].value.value=\"<DOCSTRING>\"\n            self.generic_visit(node)\n            stack.pop()\n    Visitor().visit(tree)\n    return tree,docs\nbtree,bdocs=normalized(before)\natree,adocs=normalized(after)\nexpected={\"State.create\",\"StateMachine.quick_build\",\"StateMachine.add_listener\",\"quick_fsm\",\"condition_builder\"}\nchanged={name for name in bdocs.keys()|adocs.keys() if bdocs.get(name)!=adocs.get(name)}\nassert changed==expected,(changed,expected)\nassert ast.dump(btree,include_attributes=False)==ast.dump(atree,include_attributes=False),\"non-docstring AST changed\"\nprint(\"docstring-only change set:\",\", \".join(sorted(changed)))")'</automated>
    <automated>git diff --check -- src/fast_fsm/core.py</automated>
    <automated>uv run sphinx-build -b html docs docs/_build/html -W --keep-going</automated>
    <automated>for name in "State.create" "StateMachine.quick_build" "StateMachine.add_listener" "quick_fsm" "condition_builder"; do rg -Fq "$name" docs/_build/html/api/core.html || { printf 'missing rendered API name: %s\n' "$name"; exit 1; }; done</automated>
    <automated>uv run sphinx-build -b doctest docs docs/_build/doctest</automated>
    <automated>uv run pytest tests/test_state_machine_utils.py tests/test_basic_functionality.py tests/test_listeners.py tests/test_builder.py -x -q</automated>
  </verify>
  <done>The five public docstrings render cleanly under Sphinx 9.1 and doctest, the source guard proves no non-docstring AST change, focused runtime behavior is green, the scoped commit is recorded, and `fast_fsm-041` closes while the parent phase bead remains open.</done>
</task>

</tasks>

<threat_model>
## ASVS Target

OWASP ASVS Level 1 applies. This documentation-only task adds no authentication, request-data, archive, package-install, or network boundary; its relevant risks are accidental tampering of compiled runtime source and unsupported evidence claims.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docstring source -> Sphinx autodoc/Napoleon | Public API prose is parsed as reStructuredText and warnings must remain blocking. |
| documentation edit -> compiled `core.py` | A prose repair occurs inside the sole mypyc compilation unit and must not alter executable semantics. |
| local verification -> Phase 15 baseline | Plan 03 must consume an evidenced docs-clean commit, not an unverified worktree claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|--------------|---------------|
| T-15-29 | Tampering | `src/fast_fsm/core.py` executable semantics | high | mitigate | Restrict edits to the five named docstring literals, compare the normalized before/after AST, inspect the function-context diff, and commit only `core.py`. | Exact change-set AST guard, `git diff --check`, focused core tests, and scoped commit evidence. | AST equivalence does not prove compiled byte identity, but unchanged executable AST/signatures plus focused behavior tests make semantic drift observable at ASVS L1. |
| T-15-30 | Repudiation | documentation-gate completion claim | medium | mitigate | Require fresh warnings-as-errors HTML, rendered-name check, doctest, focused tests, recorded commit SHA, and bead closure only after success. | Exact automated commands and `15-07-SUMMARY.md`. | Sphinx runner internals can change later; the lock and summary preserve the observed tool context. |
| T-15-31 | Information Disclosure | public docstring examples | low | accept | Retain existing illustrative values only and do not add environment paths, credentials, or release secrets. | Scoped diff review. | Public API examples are intentionally published. |
</threat_model>

## Artifacts this phase produces

- Updated file: `src/fast_fsm/core.py`, limited to markup/indentation inside the docstrings for `State.create`, `StateMachine.quick_build`, `StateMachine.add_listener`, `quick_fsm`, and `condition_builder`.
- Tracking artifact: `fast_fsm-041` moves to closed only after verification; parent `fast_fsm-lw2` remains open for final Phase 15 verification.
- Evidence artifact: `15-07-SUMMARY.md` records the pre-edit/scoped commit SHAs, exact docstring-only change set, Sphinx HTML/doctest/focused-test results, Wave 3 gate handoff, and bead states.
- No new or changed executable symbol, signature, behavior, dependency, CLI, environment variable, build selector, schema field, generated release artifact, setup configuration, or mypyc boundary.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Maintainers can trust the repository quality-gate evidence before runtime semantics change. | Task 1 restores and proves the blocking documentation category without runtime changes. |
| REQ | REL-05 complete clean-checkout quality gate. | Task 1 clears the reproduced Sphinx gate and hands a verified commit to Plan 03. |
| RESEARCH | Sphinx HTML and doctest remain independently visible blocking categories. | Task 1 runs each command separately and the Wave 3 handoff runs the aggregate release gate. |
| CONTEXT | D-07 requires formatting, lint, mypy, tests, docs, doctest, coverage freshness, and regression checks on PR/release. | Task 1 implements the missing docs/doctest portion; other categories remain owned by Plans 02, 03, 04, and 06. |

Deferred Phase 20 compiled-wheel publication and installed parity work remains excluded. No source item is omitted by this gap plan.

<verification>
<automated>uv run sphinx-build -b html docs docs/_build/html -W --keep-going</automated>
<automated>uv run sphinx-build -b doctest docs docs/_build/doctest</automated>
<automated>uv run pytest tests/test_state_machine_utils.py tests/test_basic_functionality.py tests/test_listeners.py tests/test_builder.py -x -q</automated>
<automated>task release-gate</automated>
<human-check>Before the scoped commit, review `git diff --function-context -- src/fast_fsm/core.py` and confirm every changed line is inside exactly the five named docstring literals; after all Wave 3 plans finish, record the full `task release-gate` result.</human-check>
</verification>

<success_criteria>
- Sphinx HTML with warnings-as-errors, rendered API-name lookup, doctest, and focused runtime tests all pass.
- The source guard identifies exactly the five intended docstrings and no non-docstring AST delta.
- The implementation commit contains only `src/fast_fsm/core.py`; Plan 03 waits for this commit before clean baseline generation.
- The completed Wave 3 `task release-gate` includes this docs repair alongside sibling Ruff and durable-documentation work.
- `fast_fsm-041` closes only after verification; `fast_fsm-lw2` stays open through final Phase 15 verification.
</success_criteria>

<output>
Create `.planning/phases/15-release-baseline-evidence-harness/15-07-SUMMARY.md` when done.
</output>
