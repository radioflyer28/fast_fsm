---
phase: 15-release-baseline-evidence-harness
plan: 09
type: execute
wave: 6
depends_on:
  - 15-08
files_modified:
  - tools/release_evidence.py
  - tests/test_release_evidence.py
  - evidence/release-baseline.json
autonomous: true
gap_closure: true
requirements:
  - REL-05
  - REL-06
  - TEST-02
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Release evidence retains the exact Python patch version in the tracked manifest for audit context while freshness compares `toolchain.python` at the stable major.minor identity, so 3.12.10 and 3.12.3 agree but a different Python minor remains stale (REL-06, D-10)."
    - "Only Python patch-level comparison is normalized; exact uv and package-tool pins, release/artifact identity, recursive slots evidence, exact test inventory, source origin, and every other stable manifest field retain strict equality."
    - "Coverage regressions remain blocking and an exact test-count change remains stale until an explicit clean-archive baseline write records the new inventory (REL-05, D-06, D-11)."
    - "The authoritative tracked baseline is regenerated after all seven Plan 15-08 tests and Plan 15-09's contract tests are committed, records at least the 782-test hosted inventory or the exact larger current collected count, and passes read-only checking in a second clean pure archive."
    - "Plan 15-09 proves the portable freshness contract locally; Plan 15-05 remains responsible for the next pushed exact-SHA Actions run and keeps fast_fsm-bhn plus fast_fsm-6yg open until every required job succeeds."
  artifacts:
    - path: "tools/release_evidence.py"
      provides: "Fail-closed Python major.minor freshness normalization while exact patch evidence remains serialized"
    - path: "tests/test_release_evidence.py"
      provides: "TDD coverage for same-minor portability, different-minor staleness, exact audit serialization, strict test-count freshness, and coverage regression preservation"
    - path: "evidence/release-baseline.json"
      provides: "Clean-archive post-15-08/15-09 exact test and coverage inventory"
    - path: ".planning/phases/15-release-baseline-evidence-harness/15-09-SUMMARY.md"
      provides: "Run 33276055108 diagnosis, RED/GREEN evidence, two-clean-archive proof, scoped commit SHAs, and open-bead handoff"
  key_links:
    - from: "tools/release_evidence.py::_stable_manifest"
      to: "tools/release_evidence.py::compare_manifests"
      via: "a deep comparison copy normalizes only toolchain.python to parsed major.minor before the existing recursive equality walk"
      pattern: "toolchain.*python"
    - from: "tests/test_release_evidence.py"
      to: "tools/release_evidence.py::_stable_manifest"
      via: "paired same-minor/different-minor and strict-field mutation cases prove the portability boundary"
      pattern: "3.12.10|3.12.3|3.13"
    - from: "evidence/release-baseline.json"
      to: "tests/test_release_evidence.py"
      via: "the clean committed archive collects and records the exact current test inventory after all test additions"
      pattern: "quality_baseline.*tests"
    - from: "15-09-SUMMARY.md"
      to: "15-05-PLAN.md"
      via: "the second clean-archive success hands both open CI gap beads to the next exact-SHA hosted rerun"
      pattern: "fast_fsm-bhn|fast_fsm-6yg"
---

<objective>
Make release-evidence freshness portable across Python patch releases and refresh the exact clean baseline after Plan 15-08's seven added tests.

Purpose: Close the sole failing job from exact-SHA Actions run 33276055108 without weakening test-count, coverage, package-pin, or artifact freshness.
Output: A TDD-defined Python major.minor comparison contract, an exact audited patch-version manifest, and a two-clean-archive refreshed baseline ready for Plan 15-05's next hosted rerun.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-release-baseline-evidence-harness/15-CONTEXT.md
@.planning/phases/15-release-baseline-evidence-harness/15-RESEARCH.md
@.planning/phases/15-release-baseline-evidence-harness/15-PATTERNS.md
@.planning/phases/15-release-baseline-evidence-harness/15-VALIDATION.md
@.planning/phases/15-release-baseline-evidence-harness/15-03-SUMMARY.md
@.planning/phases/15-release-baseline-evidence-harness/15-08-SUMMARY.md
@.planning/phases/15-release-baseline-evidence-harness/15-05-PLAN.md
@tools/release_evidence.py
@tests/test_release_evidence.py
@evidence/release-baseline.json
@Taskfile.yml
@.github/copilot-instructions.md

<gap_evidence>
- GitHub Actions run: `33276055108` at exact SHA `8d1a4dca82c25cb107dee505002b559221099cab`.
- Complete result: all 29 jobs reached terminal conclusions; only `Pure coverage and release evidence freshness` failed.
- Strict inventory drift: tracked baseline expected 775 tests while the hosted run observed 782 after Plan 15-08 added seven tests.
- Non-portable toolchain drift: tracked `toolchain.python` was local `3.12.10` while the hosted Python 3.12 runner resolved `3.12.3`; both satisfy the intended stable Python 3.12 identity.
- Linked issue: `fast_fsm-bhn`. It and the prior `fast_fsm-6yg` remain open until Plan 15-05 records a completely successful corrective exact-SHA run.
</gap_evidence>

<flagged_assumptions>
- `toolchain.python` remains an exact serialized audit observation; only the stable comparison projection changes. Do not move the field into `measurement_environment`, delete the patch version, or rewrite stored versions to `3.12`.
- Python's major.minor identity is parsed from numeric components and fails closed on malformed or incomplete values. A different minor such as 3.11 or 3.13 remains a stable-field difference.
- The current inventory was 782 at SHA `8d1a4dca82c25cb107dee505002b559221099cab`. Because this plan adds contract tests, the authoritative refreshed count may be larger; independently collect the current committed suite and record exact equality rather than forcing the historical 782 value.
- Run 33276055108 is diagnostic evidence, not successful completion evidence. Plan 15-05 owns the next push/watch and both CI-gap bead closures.
</flagged_assumptions>
</context>

<execution_constraints>
- Execute on the existing `feat/release-baseline-evidence` branch. Before editing, `git branch --show-current` must print exactly `feat/release-baseline-evidence`; do not create a replacement branch and do not implement directly on `main`.
- Inspect `fast_fsm-bhn` and `fast_fsm-6yg` with `bd show ... --json`; claim only `fast_fsm-bhn` if it is open and unclaimed. Keep both beads open after every local check. Plan 15-05 closes both only after a successful exact-SHA Actions job table.
- Preserve unrelated dirty and staged work. Commit the RED tests with only `tests/test_release_evidence.py`, commit the GREEN implementation with only `tools/release_evidence.py`, then commit the refreshed baseline with only `evidence/release-baseline.json`; allow GSD to add only `15-09-SUMMARY.md` and scoped tracking metadata separately. Never use `git add .` or `git add -A`.
- Use `uv run` for every Python and pytest command. Keep `FAST_FSM_BUILD_MODE=pure` set before every clean-archive sync, preflight, Taskfile, test, coverage, and evidence command. Do not delete or certify any developer-checkout native shadow.
- Change only the stable projection used by manifest comparison. Preserve exact serialized `toolchain.python`, exact `measurement_environment.python_version`, schema version, deterministic JSON serialization, recursive comparison paths, and every non-Python stable value.
- Do not relax exact test outcome comparison, turn test counts into a floor/range, exclude `quality_baseline.tests` from freshness, suppress field-level differences, or weaken `validate_manifest_regressions`. Total and core coverage must still reject any two-decimal decrease before freshness is considered.
- Preserve exact uv `0.12.6`, pytest, pytest-cov, Ruff, mypy/mypyc, ty, Sphinx, setuptools, and wheel values. Do not introduce a dependency or change `uv.lock`, `pyproject.toml`, Taskfile commands, workflows, source-origin policy, artifact evidence, slots registry, or performance contract.
- Generate and check the refreshed baseline only from committed clean archives. The first archive intentionally writes, the developer reviews/copies only `evidence/release-baseline.json`, and a second archive at the baseline commit performs a read-only check. Plan 15-05 owns the later hosted rerun.
</execution_constraints>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Define and implement portable Python toolchain freshness</name>
  <read_first>
    - tools/release_evidence.py
    - tests/test_release_evidence.py
    - evidence/release-baseline.json
    - .planning/phases/15-release-baseline-evidence-harness/15-08-SUMMARY.md
    - .github/copilot-instructions.md
  </read_first>
  <files>tests/test_release_evidence.py, tools/release_evidence.py</files>
  <behavior>
    - Same-minor portability: expected `toolchain.python=3.12.10` and observed `3.12.3` produce no manifest difference while their original dictionaries and serialized exact patch values remain unchanged.
    - Minor-version staleness: observed Python 3.11.x or 3.13.x produces an actionable `toolchain.python` difference against a 3.12.x baseline.
    - Malformed identity: a missing minor or nonnumeric Python identity fails closed with an actionable evidence error rather than being silently excluded.
    - Exact inventory: changing any `quality_baseline.tests` outcome/count, including the 775-to-782 change, remains a field-level stale difference until an explicit baseline write.
    - Coverage and pins: lower total/core coverage still raises the existing regression error, and changing uv or any other tool/package stable field remains stale even when Python patches differ.
  </behavior>
  <action>
    RED: expand `_manifest_fixture()` with an exact `toolchain.python` patch value and add focused tests for same-minor portability with immutable input/serialization assertions, parametrized different-minor rejection, malformed-version failure, exact test-count staleness, coverage-regression preservation, and non-Python toolchain-pin staleness. Exercise `compare_manifests`, `_stable_manifest` through its public comparison seam, `serialize_manifest`, and `validate_manifest_regressions`; do not mock away the normalization. Run the focused portable/freshness tests and require the same-minor case to fail before production changes, then commit only `tests/test_release_evidence.py` with a `test(15-09)` commit.

    GREEN: add one narrow standard-library helper in `tools/release_evidence.py` that parses a Python version into canonical `major.minor` or raises `EvidenceError` on missing/nonnumeric components. In `_stable_manifest`, keep the existing deterministic deep copy and measurement-environment exclusion, then replace only the copied `toolchain.python` value with that parsed identity when the field exists. Do not mutate caller dictionaries or the serialized manifest, and do not normalize any other toolchain or stable field. Keep `compare_manifests`' recursive field-level reporting and `validate_manifest_regressions` unchanged. Run the focused tests, full `tests/test_release_evidence.py`, Ruff on the two touched Python files, mypy on the evidence tool, and a pure-source preflight; commit only `tools/release_evidence.py` with a `fix(15-09)` commit. Record RED/GREEN results and scoped SHAs for the summary.
  </action>
  <acceptance_criteria>
    - A same-minor 3.12.10/3.12.3 comparison is fresh while both exact patch strings remain present in their original serialized audit documents.
    - A different minor and a malformed/minorless identity fail with actionable evidence tied to `toolchain.python`.
    - Exact test-count/outcome changes, exact uv/package-pin changes, source/artifact/slots changes, and all other stable drift still produce field-level differences.
    - Existing total/core two-decimal coverage regressions still raise before ordinary freshness output.
    - The normalization changes only a deep comparison copy; no schema, manifest serialization, caller data, lockfile, workflow, or Taskfile changes.
    - The RED commit contains only the test file and the GREEN commit contains only the evidence tool; unrelated staged files remain untouched.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_release_evidence.py -x -q -k "python and (toolchain or manifest or freshness)"</automated>
    <automated>uv run pytest tests/test_release_evidence.py -x -q</automated>
    <automated>uv run ruff format --check tools/release_evidence.py tests/test_release_evidence.py</automated>
    <automated>uv run ruff check tools/release_evidence.py tests/test_release_evidence.py</automated>
    <automated>uv run mypy tools/release_evidence.py</automated>
    <automated>FAST_FSM_BUILD_MODE=pure uv run python tools/release_evidence.py verify-source --json</automated>
  </verify>
  <done>The evidence manifest preserves exact Python patch audit data while freshness portably compares only its major.minor identity, with strict tests, coverage, and all other stable fields unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh and prove the exact post-test baseline in two clean archives</name>
  <read_first>
    - tools/release_evidence.py
    - tests/test_release_evidence.py
    - evidence/release-baseline.json
    - Taskfile.yml
    - docs/dev/releasing.md
    - .planning/phases/15-release-baseline-evidence-harness/15-03-SUMMARY.md
    - .planning/phases/15-release-baseline-evidence-harness/15-08-SUMMARY.md
  </read_first>
  <files>evidence/release-baseline.json</files>
  <action>
    After Task 1's production and test commits exist, record the committed HEAD and extract that exact tree into an isolated clean archive. In the first archive, export `FAST_FSM_BUILD_MODE=pure` before environment creation, run `FAST_FSM_BUILD_MODE=pure uv sync --locked`, immediately run `FAST_FSM_BUILD_MODE=pure uv run python tools/release_evidence.py verify-source --json`, and stop before any test, coverage, build, inventory, or other collection if either step fails. Only after the preflight succeeds, independently collect the committed suite inventory; the count must be at least the hosted 782 and becomes the exact expected count for this refreshed baseline. Run `FAST_FSM_BUILD_MODE=pure task release-baseline-write`, assert the generated manifest has `collected == passed ==` the independently collected current count with zero failed/errors/skipped, `.py` source origin, exact uv/package pins, non-regressed total/core coverage, the complete slots inventory/two registered exceptions, and exact Python patch audit fields. Compare against the tracked baseline and require the intended diff to explain the post-15-08/15-09 exact test inventory plus any honestly observed coverage change; investigate any other changed stable field. Copy only `evidence/release-baseline.json` back, review its deterministic diff, and commit only that file with a `chore(15-09)` commit.

    Create a second isolated clean archive at the new baseline commit. Repeat pure mode before `uv sync --locked` and the immediate source preflight, then run `FAST_FSM_BUILD_MODE=pure task release-baseline-check` read-only and verify the manifest bytes remain unchanged. Run `FAST_FSM_BUILD_MODE=pure task release-gate` from the clean committed archive. Confirm the stored exact Python patch remains audit context, and separately exercise a fixture or focused test showing a hosted `3.12.3` observation compares fresh to a stored `3.12.10` baseline. Record both archive SHAs/commands, exact collected count, pass/fail/skipped/error counts, coverage, exact toolchain values, unchanged-field review, read-only byte check, and still-open fast_fsm-bhn/fast_fsm-6yg states in `15-09-SUMMARY.md`; do not close either bead or dispatch hosted CI.
  </action>
  <acceptance_criteria>
    - Baseline generation starts from a committed clean archive with pure mode established before locked sync and immediate source-origin preflight before collection.
    - The refreshed manifest records the exact current committed inventory, at least 782 tests, with collected and passed equal and zero failed/errors/skipped; it does not use a floor/range freshness policy.
    - Exact Python patch, uv 0.12.6, all package pins, source/artifact/slots/performance fields, and deterministic schema remain recorded; only Python patch comparison uses major.minor identity.
    - Total/core coverage does not regress; any changed coverage is generated evidence rather than a hand edit.
    - A second clean archive at the baseline commit passes read-only baseline checking and the complete release gate without changing manifest bytes.
    - The baseline commit contains only `evidence/release-baseline.json`; no developer native artifact or unrelated staged file is modified.
    - fast_fsm-bhn and fast_fsm-6yg remain open, and Plan 15-05 owns the next exact-SHA rerun and both closures.
  </acceptance_criteria>
  <verify>
    <automated>FAST_FSM_BUILD_MODE=pure uv run python tools/release_evidence.py verify-source --json</automated>
    <automated>FAST_FSM_BUILD_MODE=pure task release-baseline-check</automated>
    <automated>FAST_FSM_BUILD_MODE=pure uv run python -c "import json; d=json.load(open('evidence/release-baseline.json')); t=d['quality_baseline']['tests']; assert t['collected'] == t['passed'] and t['collected'] >= 782 and t['failed'] == t['errors'] == t['skipped'] == 0; assert d['toolchain']['uv'] == '0.12.6' and d['toolchain']['python'].count('.') >= 2; assert d['quality_baseline']['source']['core_origin'].endswith('.py'); print(t['collected'], d['toolchain']['python'])"</automated>
    <automated>uv run pytest tests/test_release_evidence.py -x -q -k "python and (toolchain or manifest or freshness)"</automated>
    <automated>FAST_FSM_BUILD_MODE=pure task release-gate</automated>
  </verify>
  <done>The tracked manifest contains the exact current clean pure test/coverage/toolchain evidence, and a second clean archive proves portable Python-patch freshness plus every unchanged strict contract before hosted rerun.</done>
</task>

</tasks>

<threat_model>
## ASVS Target

OWASP ASVS Level 1 applies. This maintainer-only evidence change handles deterministic local/hosted observations and clean-archive subprocesses; it adds no application authentication, request-data, runtime dependency, archive extraction from untrusted sources, or public API.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Python patch -> hosted Python patch | Exact audit observations differ even though both runners implement the supported 3.12 toolchain identity. |
| observed manifest -> stable comparison projection | Only the copied Python patch field may be normalized; all other evidence remains strict. |
| developer worktree -> authoritative baseline | Dirty/native-shadow state must not enter the tracked release baseline. |
| local clean proof -> GitHub Actions | Plan 15-05 must bind success to the next exact pushed SHA; local checks cannot close either CI gap bead. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|--------------|---------------|
| T-15-36 | Tampering | stable manifest comparison | high | mitigate | Normalize only a deep copy of `toolchain.python` to parsed major.minor; retain exact serialized patches and mutation-test all other strict fields. | Same-minor/different-minor, malformed, immutable-input, exact-pin, and exact-test-count tests. | Python patch-level behavior can differ; exact patches remain recorded in each run for audit even though patch drift is nonblocking. |
| T-15-37 | Tampering | test/coverage baseline refresh | high | mitigate | Explicit write from a committed clean pure archive, exact current inventory, blocking coverage regression check, reviewed diff, and second-archive read-only check. | Exact-count assertion, coverage guard tests, manifest diff, byte comparison, and release gate. | A future intentional test/coverage change still requires another reviewed write. |
| T-15-38 | Spoofing | pure-source baseline | high | mitigate | Establish pure mode before locked sync, immediately preflight source origin, and never collect from the developer checkout or delete native shadows. | Both archive preflights and `.py` manifest source assertion. | The local OS remains an environment observation excluded from stable freshness. |
| T-15-39 | Repudiation | hosted completion claim | high | mitigate | Record run 33276055108 only as diagnostic evidence, keep both beads open, and require Plan 15-05's terminal exact-SHA all-job table before closure. | `15-09-SUMMARY.md`, Plan 15-05 run URL/SHA/job conclusions, and bead closure reasons. | GitHub Actions remains the authoritative external execution record. |
</threat_model>

## Artifacts this phase produces

- Updated implementation: `tools/release_evidence.py` normalizes only the copied stable Python comparison identity to major.minor.
- Updated tests: `tests/test_release_evidence.py` covers exact-patch audit retention, same-minor portability, different-minor/malformed failure, exact test-count freshness, strict package pins, and unchanged coverage regression enforcement.
- Updated generated evidence: `evidence/release-baseline.json` records the exact post-Plan-15-08/15-09 collected count and clean pure coverage/toolchain observations.
- Evidence artifact: `15-09-SUMMARY.md` records run 33276055108's sole failure, RED/GREEN commits, two clean archive checks, exact baseline observations, and both open CI gap beads.
- No changed workflow, Taskfile command, dependency, lockfile, schema version, package/runtime source, public API, artifact publication rule, slots registry, performance floor, Git tag, or GitHub release body.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Maintainers can trust toolchain, quality-gate, and pure-source evidence across clean environments. | Tasks 1-2 separate exact audit context from portable stable identity, then prove the refreshed baseline twice from clean archives. |
| REQ | REL-05 complete clean-checkout quality gate and documented exact baseline. | Task 2 records the exact current test/coverage inventory and runs the full release gate without weakening freshness. |
| REQ | REL-06 reproducibly pinned release-producing tools. | Task 1 changes only Python patch comparison; exact uv and package pins remain blocking while Python 3.12 major.minor is portable. |
| REQ | TEST-02 pure source-origin and meaningful coverage proof. | Task 2 preflights immediately after each locked sync and preserves exact source/coverage evidence. |
| RESEARCH | Stable baseline fields must be deterministic while environment-specific observations do not churn across runners. | Task 1 defines Python major.minor as stable identity while retaining exact patch audit data; Task 2 validates local/hosted portability. |
| RESEARCH | CI freshness is read-only and baseline writes are explicit/reviewed. | Task 2 uses one explicit write archive and a separate read-only check archive. |
| CONTEXT | D-06 blocks coverage regression from the measured baseline. | Existing two-decimal total/core regression checks remain unchanged and are reasserted in TDD. |
| CONTEXT | D-07 runs the complete quality gate independently. | Task 2 runs the complete local release gate; Plan 15-05 later proves all hosted jobs. |
| CONTEXT | D-10 tracks exact tests, clean coverage, tool versions, artifact mode, and environment observations. | The refreshed manifest retains exact values, including Python patch, while only stable comparison uses major.minor. |
| CONTEXT | D-11 requires explicit reviewed generation and read-only CI freshness. | Task 2 separates intentional write, diff review, commit, and second-archive check. |
| GAP | Run 33276055108 observed 782 vs tracked 775 tests and Python 3.12.3 vs 3.12.10. | Tasks 1-2 preserve exact test freshness through regeneration and remove only irrelevant Python patch churn. |

Deferred Phase 20 compiled-wheel publication and installed-artifact parity remain excluded. Context decisions unrelated to Python freshness/baseline regeneration remain owned by prior Phase 15 plans and are not modified. No relevant source item is omitted.

<verification>
<automated>uv run pytest tests/test_release_evidence.py -x -q -k "python and (toolchain or manifest or freshness)"</automated>
<automated>uv run pytest tests/test_release_evidence.py -x -q</automated>
<automated>uv run ruff format --check tools/release_evidence.py tests/test_release_evidence.py</automated>
<automated>uv run ruff check tools/release_evidence.py tests/test_release_evidence.py</automated>
<automated>uv run mypy tools/release_evidence.py</automated>
<automated>FAST_FSM_BUILD_MODE=pure uv run python tools/release_evidence.py verify-source --json</automated>
<automated>FAST_FSM_BUILD_MODE=pure task release-baseline-check</automated>
<automated>FAST_FSM_BUILD_MODE=pure task release-gate</automated>
<human-check>Review the generated manifest diff and both clean-archive records: exact current test count and patch versions stay visible, only Python patch comparison is portable, coverage/pins remain strict, and neither fast_fsm-bhn nor fast_fsm-6yg closes before Plan 15-05's successful exact-SHA rerun.</human-check>
</verification>

<success_criteria>
- Same-minor Python patch drift is fresh without erasing exact audit versions; different-minor or malformed Python identities fail closed.
- Exact test counts/outcomes, coverage regression guards, uv/package pins, source/artifact identity, slots evidence, and all other stable fields remain strict.
- The refreshed manifest records at least 782—and exactly the current committed—passing tests with zero failures/errors/skips and non-regressed source coverage.
- One committed clean archive writes the reviewed baseline and a second committed clean archive passes read-only checking plus the complete release gate.
- Implementation, tests, and baseline are committed in separate scoped commits without unrelated files.
- fast_fsm-bhn and fast_fsm-6yg remain open; Plan 15-05 owns the next pushed exact-SHA run, terminal job proof, and both issue closures.
</success_criteria>

<output>
Create `.planning/phases/15-release-baseline-evidence-harness/15-09-SUMMARY.md` when done.
</output>
