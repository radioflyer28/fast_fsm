---
phase: 16-canonical-graph-dispatch-invariants
plan: 05
type: execute
wave: 4
depends_on:
  - 16-04
files_modified:
  - docs/dev/architecture.md
  - docs/dev/testing.md
  - evidence/release-baseline.json
  - .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md
autonomous: true
requirements:
  - GRAF-01
  - GRAF-02
  - GRAF-03
  - GRAF-04
  - GRAF-05
  - GRAF-06
  - GRAF-07
  - GRAF-08
  - LIFE-07
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Users receive one canonical topology and equivalent construction/dispatch behavior across sync, async, builder, and declarative APIs, with all nine Phase 16 requirements passing together."
    - "Pure and compiled module origins are asserted in distinct temporary contexts before semantic/performance collection; current working-tree Phase 16 files are overlaid explicitly, both modes pass the same Phase 16 suites, and compiled trigger remains at least 200,000 ops/sec."
    - "Before/after evidence records environment, tree identity, module origin, semantics, pure/compiled throughput, and history overhead before the Phase 16 design is frozen."
    - "Documentation records canonical identity, private structural snapshot, builder freeze, guard context, declarative exactly-once, and bounded history without publishing later-phase contracts."
  artifacts:
    - path: "docs/dev/architecture.md"
      provides: "Maintainer architecture contract for canonical topology and shared dispatch seams"
    - path: "docs/dev/testing.md"
      provides: "Pure/compiled Phase 16 verification procedure and edge-family matrix"
    - path: "evidence/release-baseline.json"
      provides: "Reviewed clean pure-source post-Phase-16 test/coverage/toolchain observation"
    - path: ".planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md"
      provides: "Paired before/after pure and compiled semantic/performance record"
  key_links:
    - from: "Phase 16 targeted suites"
      to: "pure and compiled core origins"
      via: "source-origin assertion before behavior execution"
      pattern: "core_origin|module origin"
    - from: "16-PERFORMANCE-EVIDENCE.md"
      to: "evidence/release-baseline.json"
      via: "environment-labeled observations and reviewed release-baseline regeneration"
      pattern: "200000|throughput"
    - from: "docs/dev/architecture.md"
      to: "src/fast_fsm/core.py"
      via: "exact private/public boundary and complexity contract"
      pattern: "canonical|graph version|structural"
  prohibitions:
    - "[UNVERIFIED — no SPEC] Do not claim installed-wheel or publication parity; Phase 20 owns installed artifact proof."
    - "[UNVERIFIED — no SPEC] Do not document Phase 17 callback/failure order, Phase 18 ownership, Phase 19 diagnostic budgets/migration, or a public topology format."
    - "[UNVERIFIED — no SPEC] Do not certify a developer checkout containing native shadows as clean pure-source evidence or delete those shadows."
---

<objective>
Freeze Phase 16 only after its complete semantic, documentation, and pure/compiled performance contract is proven from clean artifacts.

Purpose: Convert the individual expansion slices into one auditable phase outcome without weakening Phase 15 evidence or stealing later-phase scope.
Output: Updated architecture/testing guides, verified owning-task SPR history, reviewed baseline evidence, and paired pure/compiled final proof.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-CONTEXT.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-RESEARCH.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-PATTERNS.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-VALIDATION.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-01-SUMMARY.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-02-SUMMARY.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-03-SUMMARY.md
@.planning/phases/16-canonical-graph-dispatch-invariants/16-04-SUMMARY.md
@.planning/phases/15-release-baseline-evidence-harness/15-VERIFICATION.md
@docs/dev/architecture.md
@docs/dev/testing.md
@.specify/memory/spr-core-api.md
@tools/phase16_isolated_verify.py
@evidence/release-baseline.json
@Taskfile.yml
@setup.py

<interfaces>
- Phase-wide private symbols/fields are the exact inventory in the Artifacts section below; no public export is added.
- The targeted conformance command is the Validation quick set plus condition templates, mypyc guard, and performance cases.
- The pure authority is a temporary committed-HEAD export with a fixed explicit overlay of current working-tree Phase 16 files, `FAST_FSM_BUILD_MODE=pure` selected before setup, immediate source preflight, then isolated read/write gates.
- The compiled authority is a separate clean archive with `FAST_FSM_BUILD_MODE=compiled`, successful native build, native origin assertion, the same semantic tests, and the 200,000 ops/sec floor.
- The directly runnable authority is `uv run python tools/phase16_isolated_verify.py --suite phase16`; it owns creation of two temporary committed-HEAD contexts, fixed current-working-tree overlays for the exhaustive Phase 16 source/test/docs/evidence inventory, pre-setup mode selection, pre-semantic-import origin assertions, fail-closed native build, identical semantic commands, compiled trigger/history gates, isolated baseline freshness, and the full pure release gate.
</interfaces>

<flagged_assumptions>
- [ASSUMPTION — no SPEC] Structural snapshot immutability, graph version origin/copy semantics, wrapper-cycle ValueError, exact Unicode-name equality, declarative from/to matching, and non-bool positive history capacity are now the documented Phase 16 interpretations because Plans 01–04 prove them.
- [ASSUMPTION — no SPEC] Phase 16 requires clean pure and compiled source-tree semantic/performance proof, not installed-wheel parity; Phase 20 will independently prove installed artifacts.
- [ASSUMPTION — no SPEC] Single-owner graph snapshots are coherent only when topology is not concurrently mutated; this is a documented boundary, not a silent thread-safety claim.
</flagged_assumptions>
</context>

<execution_constraints>
- Run the full suite once at phase end, not repeatedly. Use targeted commands for any correction found during this plan.
- Regenerate the release baseline only through the helper's isolated `baseline-write` suite; inspect and commit only the expected manifest diff, then prove it read-only through the isolated `baseline-check` suite.
- Build native code separately and assert origin before tests. Do not install/publish artifacts or alter release workflows in this phase.
- Preserve all later-phase scope fences and every unrelated dirty/staged file.
</execution_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Document the resolved runtime contracts and scope fences</name>
  <read_first>
    - src/fast_fsm/core.py: final Phase 16 private seams and public methods
    - src/fast_fsm/conditions.py
    - src/fast_fsm/condition_templates.py
    - docs/dev/architecture.md
    - docs/dev/testing.md
    - .specify/memory/spr-core-api.md
    - .planning/phases/16-canonical-graph-dispatch-invariants/16-CONTEXT.md
    - .planning/phases/16-canonical-graph-dispatch-invariants/16-01-SUMMARY.md through 16-04-SUMMARY.md
  </read_first>
  <files>docs/dev/architecture.md, docs/dev/testing.md</files>
  <action>
    Update the maintainer architecture and testing guide to match the executed behavior: canonical exact-identity registry/endpoints, atomic batch/bidirectional/emergency mutation, monotonic topology-only version, declared initial state, private structurally immutable snapshot, builder identity-safe staging/publish-on-success/freeze, shared args/filter-then-cap context, supported built-in wrapper recursion, declarative exactly-once invocation including invocation-only error-edge counters, and positive-capacity deque history. State O(1) single-endpoint dictionary operations and the compiled floor. Record the chosen spec-less interpretations explicitly, including exact Python string equality and single-owner snapshot semantics. Preserve public API names/signatures and call out that snapshot consumers, lifecycle failures, ownership, diagnostics, public topology format, and installed artifacts belong to Phases 17–20/FUTR-05. Verify `.specify/memory/spr-core-api.md` already matches the owning implementation summaries; do not edit it here—route any discrepancy back to the implementation task whose commit changed that behavior. Use durable claims and link exact observations to `16-PERFORMANCE-EVIDENCE.md`/release baseline.
  </action>
  <threats>T-16-13, T-16-14, T-16-10</threats>
  <acceptance_criteria>
    - Documentation describes every GRAF-01..08 and LIFE-07 behavior without claiming deferred semantics.
    - Private graph snapshot is explicitly non-public and structurally immutable; single-owner scope is explicit.
    - Public symbols/signatures and the one-module/one-dependency/performance contracts are accurate.
    - SPR and docs agree with code/tests and use environment-labeled evidence rather than fabricated exact performance claims.
    - Git history shows each SPR behavior/signature edit in the same owning implementation task commit, with no deferred SPR change in Plan 16-05.
  </acceptance_criteria>
  <verify>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include docs/dev/architecture.md --include docs/dev/testing.md -- uv run sphinx-build -b html docs docs/_build/html -W --keep-going</automated>
    <automated>uv run python tools/phase16_isolated_verify.py --mode task --build-mode pure --include docs/dev/architecture.md --include docs/dev/testing.md -- uv run sphinx-build -b doctest docs docs/_build/doctest</automated>
    <automated>uv run ruff format --check src/fast_fsm tests</automated>
  </verify>
  <done>Maintainer and agent documentation accurately captures Phase 16 behavior, evidence, and later-phase boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the reviewed clean pure baseline after all semantic tests</name>
  <read_first>
    - Taskfile.yml: pure-source-check, release-baseline-write, release-baseline-check, release-gate
    - tools/release_evidence.py
    - evidence/release-baseline.json
    - .planning/phases/15-release-baseline-evidence-harness/15-VERIFICATION.md
    - .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md
    - tests/test_graph_invariants.py
  </read_first>
  <files>evidence/release-baseline.json, .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md</files>
  <action>
    Invoke `uv run python tools/phase16_isolated_verify.py --suite baseline-write --export-baseline evidence/release-baseline.json`. That suite must create a temporary committed-HEAD export, overlay the fixed explicit current-working-tree Phase 16 source/test set, select pure mode before locked sync, reject native files, assert `.py` origin, independently collect the exact suite inventory, run the release-baseline write, and atomically copy back only the generated manifest after successful collection. Review the resulting checkout diff: exact current tests/coverage/toolchain, `.py` origin, wheel identity, slots inventory, and environment-labeled performance may change only when honestly observed; package/build pins and schema stay strict. Commit the reviewed manifest, then invoke `uv run python tools/phase16_isolated_verify.py --suite baseline-check`; that suite creates a second asserted-pure temporary context, overlays the current working-tree manifest, runs read-only freshness, validates collected/passed equality and zero failures/errors plus `.py` core origin, and fails if the manifest bytes change. Append exact helper commands, SHAs, origin, inventory, coverage, and reviewed differences to `16-PERFORMANCE-EVIDENCE.md`. Never hand-edit measured fields or touch developer native shadows.
  </action>
  <threats>T-16-11, T-16-12, T-16-13</threats>
  <acceptance_criteria>
    - Pure mode is selected before environment setup and `.py` origin is proven before collection in both archives.
    - The manifest records exact current collected/passed equality with zero failures/errors and honest coverage/toolchain/slots observations.
    - A second archive passes read-only freshness without changing manifest bytes.
    - Only reviewed evidence files are modified; package pins, schema, workflows, dependencies, and developer artifacts are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run python tools/phase16_isolated_verify.py --suite baseline-check</automated>
  </verify>
  <done>The exact post-Phase-16 clean pure baseline is intentionally written, reviewed, committed, and independently proven fresh read-only.</done>
</task>

<task type="auto">
  <name>Task 3: Prove final pure/compiled semantic parity and performance before design freeze</name>
  <read_first>
    - .planning/phases/16-canonical-graph-dispatch-invariants/16-VALIDATION.md
    - .planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md
    - setup.py
    - Taskfile.yml
    - benchmarks/benchmark_fast_fsm.py
    - tests/test_performance_benchmarks.py
    - tests/test_graph_invariants.py
    - tests/test_builder.py
    - tests/test_async.py
    - tests/test_safety_kwargs.py
    - tests/test_advanced_functionality.py
    - tools/phase16_isolated_verify.py
  </read_first>
  <files>.planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md</files>
  <action>
    Run `uv run python tools/phase16_isolated_verify.py --suite phase16` as the canonical final blocking command. The committed helper must export committed HEAD into separate temporary pure and compiled contexts; overlay the fixed exhaustive current-working-tree Phase 16 source/test/docs/evidence inventory in each; set the respective build mode before each locked setup; immediately assert `.py`/native origin before any semantic import; stop with a nonzero exit if native build or origin proof fails; execute the exact same Phase 16 semantic pytest command in both modes; run compiled trigger-minimum and history-overflow/overhead gates; and run both release-baseline freshness and the full release gate inside the asserted-pure context. Record before/after deltas with environment, exact tree SHA plus overlay inventory/digests, origin, operations, repeats, and results. Require compiled `trigger()` >=200,000 ops/sec and no semantic difference; treat any regression/failure as blocking and correct it in the owning earlier plan surface before rerunning. Run blocking `task typecheck-mypy` separately. Then collect a separately named `Advisory typecheck-ty evidence` record by invoking `task typecheck-ty` through `subprocess.run` with stdout/stderr capture and `check=False`; append its complete stdout, stderr, and numeric exit status to the summary/evidence record, and do not turn a nonzero advisory status into task failure. This is source-tree native proof only: do not publish/install a release artifact or claim Phase 20 parity.
  </action>
  <threats>T-16-11, T-16-12, T-16-13, T-16-14</threats>
  <acceptance_criteria>
    - Clean pure origin passes the full release gate and all Phase 16 targeted tests.
    - Clean native origin passes the same semantic tests, mypyc/slots gates, and compiled trigger floor.
    - The helper fails closed on compiled setup/origin failure and runs both compiled trigger and history performance gates after shared semantics.
    - Before/after evidence is complete and environment-labeled; no fabricated count/throughput is asserted.
    - No developer native shadow is deleted, no installed-artifact/publication claim is made, and all later-phase fences remain intact.
  </acceptance_criteria>
  <verify>
    <automated>uv run python tools/phase16_isolated_verify.py --suite phase16</automated>
    <automated>task typecheck-mypy</automated>
  </verify>
  <done>Phase 16 semantics are identical in asserted pure/native source contexts, compiled throughput clears 200,000 ops/sec, and the reviewed before/after evidence freezes the design.</done>
</task>

</tasks>

<threat_model>
## ASVS Target

OWASP ASVS Level 1 applies to evidence integrity, build provenance, and accurate documentation. High-severity origin/performance threats are blocking.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dirty checkout -> clean pure evidence | Native shadows and uncommitted state must not enter the baseline. |
| source -> native extension | Compiled code must represent the same committed tree and semantics. |
| measured behavior -> documentation | Exact observations must remain environment-labeled and scope claims accurate. |
| Phase 16 -> later phases | This phase must not silently publish lifecycle, ownership, diagnostics, or installed-artifact contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Test Evidence | Residual Risk |
|-----------|----------|-----------|----------|-------------|-----------------|---------------|---------------|
| T-16-11 | Spoofing | module/artifact origin | high | mitigate | Separate clean archives, explicit build mode, origin assertion before behavior, non-destructive preflight. | Pure release gate and native-origin semantic suite. | Installed wheel identity remains Phase 20. |
| T-16-12 | Denial of Service | trigger/history performance | high | mitigate | Before/after measurements, fixed compiled floor, dictionary/deque/slots checks. | Performance tests and evidence record in both modes. | Environment variance remains documented; only the stable floor is policy. |
| T-16-13 | Repudiation/Tampering | evidence/docs drift | medium | mitigate | Reviewed explicit baseline write, second read-only archive, docs generated from executed summaries/tests. | Manifest diff/freshness, Sphinx, source audit, exact commands/SHAs. | Future intentional behavior changes require another reviewed evidence update. |
| T-16-14 | Tampering | concurrent snapshot interpretation | medium | transfer | Document single-owner structural semantics and assign ownership/locking to Phase 18 without claiming safety. | Docs assertions and absence of lock/ownership artifacts in Phase 16 diff. | Concurrent mutation remains unsupported until Phase 18 completes. |
</threat_model>

## Artifacts this phase produces

- New private core records: `_GraphTransition`, `_GraphSnapshot`, and `_PreparedTransition`.
- New private core seams: `_resolve_canonical_state`, `_normalize_transition_request`, `_commit_transition_plan`, `_graph_snapshot`, `_prepare_transition`, `_condition_children`, `_contains_async_requirement`, `_evaluate_condition_sync`, `_evaluate_condition_async`, `_resolve_declarative_handler`, `_invoke_declarative_handler`, and `_invoke_declarative_handler_async`.
- New private builder seams: `FSMBuilder._ensure_mutable` and `FSMBuilder._preflight_async_requirements`.
- New StateMachine field: `_graph_version`; existing `_history` changes to an optional bounded deque while its public property remains a list copy.
- New files: `tests/test_graph_invariants.py`, `tools/phase16_isolated_verify.py`, and `.planning/phases/16-canonical-graph-dispatch-invariants/16-PERFORMANCE-EVIDENCE.md`.
- Exhaustive named test/evidence inventory: `graph_fingerprint()`, `builder_staging_fingerprint()`, `RecordingCondition`, `RecordingAsyncCondition`, `ShortCircuitCondition`, `make_negated_cycle()`, `make_and_cycle()`, `make_or_cycle()`, `make_not_cycle()`, `make_shared_condition_dag()`, `DeclarativeInvocationCounter`, `AsyncDeclarativeInvocationCounter`, `_invoke_and_ignore_phase17_outcome()`, and working-tree-overlay task mode plus the `graph`/`baseline-write`/`baseline-check`/`phase16` suites in `tools/phase16_isolated_verify.py`.
- Modified public signatures without removals: Condition/FuncCondition/CompiledFuncCondition/NegatedCondition/AsyncCondition and built-in condition-template `check*` methods accept and forward `*args, **kwargs`.
- Modified evidence/docs: `evidence/release-baseline.json`, `docs/dev/architecture.md`, and `docs/dev/testing.md`; `.specify/memory/spr-core-api.md` is modified only by the owning implementation tasks in Plans 16-01 through 16-04.
- No new public exported symbol, public topology format, runtime dependency, schema, service integration, or installed artifact.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | One canonical topology and equivalent sync/async/builder/declarative construction/dispatch | 01-05 | COVERED | Tracer establishes topology; expansions converge guards, builder, declarative, history; final plan proves the set. |
| REQ | GRAF-01 | Canonical endpoints and atomic rejection | 01,05 | COVERED | Invalid/foreign/null/empty/duplicate/multi-source/batch plus bidirectional second-leg and emergency all-source families are explicit. |
| REQ | GRAF-02 | Identity-safe registration | 01,03,05 | COVERED | Runtime and builder staging both prove same identity no-op; distinct same-name/equality object rejects without cache/eventual topology drift. |
| REQ | GRAF-03 | Immutable deterministic versioned snapshot | 01,05 | COVERED | Structural immutability, declared initial, sorted rows, version, and single-owner semantics. |
| REQ | GRAF-04 | Builder freeze/idempotence | 03,05 | COVERED | Every mutator, empty/single content, repair, order, and repeated identity. |
| REQ | GRAF-05 | Recursive async detection | 02,03,05 | COVERED | Supported wrapper edges, cycles/DAGs, declarative metadata, callbacks, explicit modes. |
| REQ | GRAF-06 | Four-path guard context parity | 02,05 | COVERED | Positional identity, filter-then-cap mapping, empty/null-like values, no mutation/log leakage. |
| REQ | GRAF-07 | Declarative exactly once | 04,05 | COVERED | Successful canonical match plus false/invalid/raising invocation-count-only parity without Phase 17 result/state/order/history/commit assertions. |
| REQ | GRAF-08 | Shared seams and one compilation unit | 01-05 | COVERED | Exact private inventory, mypyc/slots guards, public-symbol preservation, pure/native proof. |
| REQ | LIFE-07 | Positive bounded O(1) history | 04,05 | COVERED | Invalid-preservation, deque FIFO, re-enable reset, copy, disabled, clone, performance. |
| RESEARCH | R-TOPOLOGY | Normalize/validate/commit canonical dictionary topology | 01 | COVERED | Implements recommended topology transaction and snapshot contract. |
| RESEARCH | R-GUARD | One preparation seam plus two evaluators | 02 | COVERED | Detection and runtime evaluation share child classification. |
| RESEARCH | R-BUILDER | Transactional publish-on-success builder | 03 | COVERED | Freeze guard and complete preflight precede local candidate publication. |
| RESEARCH | R-DECL | Declarative successful exactly-once integration | 04 | COVERED | Ordinary/helper routes converge without lifecycle-order scope leakage. |
| RESEARCH | R-HISTORY | Standard bounded deque and public list copy | 04 | COVERED | No hand-rolled buffer/dependency. |
| RESEARCH | R-EVIDENCE | Pure/compiled before-after gates and origin proof | 01,05 | COVERED | Committed isolation helper creates separate temporary committed-HEAD contexts, overlays only explicit current-working-tree Phase 16 files, selects mode before setup, asserts origin before semantic imports, fails native build closed, and runs shared semantics, isolated baseline/release gates, plus compiled performance. |
| CONTEXT | D-01 | Same-object idempotence/conflicting identity rejection | 01,03 | COVERED | Runtime tracer plus builder staging/cache/eventual-topology fingerprint coverage. |
| CONTEXT | D-02 | Canonical string/object endpoint resolution | 01 | COVERED | Exact registry identity preflight. |
| CONTEXT | D-03 | Complete multi-source validation before insertion | 01 | COVERED | Empty/duplicate/later-invalid/batch, two-leg bidirectional, and emergency all-source coverage. |
| CONTEXT | D-04 | Convenience constructors register declared set; ordinary addition does not | 01 | COVERED | Factory regressions plus strict ordinary path. |
| CONTEXT | D-05 | Monotonic topology-only graph version | 01 | COVERED | Success/no-op/rejection/current-state matrix. |
| CONTEXT | D-06 | Fresh immutable deterministic private snapshot | 01 | COVERED | Tuple records with canonical refs and sorted rows. |
| CONTEXT | D-07 | Internal tool contract only | 01,05 | COVERED | Public schemas preserved and docs fence Phase 19/FUTR-05. |
| CONTEXT | D-08 | Declared initial independent of current | 01 | COVERED | Snapshot/current movement tests. |
| CONTEXT | D-09 | Freeze only after complete success | 03 | COVERED | Every mutator matrix and failed repair. |
| CONTEXT | D-10 | Repeated build identity/local candidate | 03 | COVERED | Cache assignment last and no replay. |
| CONTEXT | D-11 | Recursive async requirements/explicit modes | 02,03 | COVERED | Classifier, evaluator, preflight, and real async execution. |
| CONTEXT | D-12 | Shared resolution for sync/async can/do | 02 | COVERED | `_prepare_transition` four-path matrix. |
| CONTEXT | D-13 | Positional args plus fresh deterministic sanitized kwargs | 02 | COVERED | Filter-before-cap, insertion order, fresh copy, no caller mutation. |
| CONTEXT | D-14 | Sanitize once per evaluation/no raw payload logging | 02 | COVERED | Wrapper object-identity and caplog tests. |
| CONTEXT | D-15 | Ordinary declarative handler exactly once | 04 | COVERED | One resolver/invocation seam, helper delegation, and count-only false/invalid/raising cases. |
| CONTEXT | D-16 | Do not lock lifecycle/failure model | 04,05 | COVERED | Successful-only assertions and explicit documentation fence. |
| CONTEXT | D-17 | Reject non-positive capacity/reset valid buffer | 04 | COVERED | Invalid values preserve old config; valid re-enable empties. |
| CONTEXT | D-18 | O(1) FIFO, disabled zero-cost, defensive list | 04,05 | COVERED | deque, None branch, list copy, performance evidence. |

Deferred Phase 17 lifecycle/failure ordering, Phase 18 ownership/locking, Phase 19 consumer migration/budgets/safe output, Phase 20 installed artifact publication, and FUTR-05 public topology format are explicit exclusions rather than gaps. All 12 spec-less probe prompts are resolved into the task behaviors/acceptance criteria or the flagged assumptions above; none is silently dropped.

<verification>
  <automated>uv run python tools/phase16_isolated_verify.py --suite phase16</automated>
  <automated>task typecheck-mypy</automated>
</verification>

<success_criteria>
- Every Phase 16 requirement and D-01..D-18 decision is covered by automated behavior and clean evidence.
- Pure and compiled source contexts agree semantically and compiled trigger clears 200,000 ops/sec.
- Documentation/evidence is auditable and no deferred phase contract is accidentally published.
</success_criteria>

<output>
Create `.planning/phases/16-canonical-graph-dispatch-invariants/16-05-SUMMARY.md` when done.
</output>
