---
phase: 19-bounded-diagnostics-safe-output
plan: 06
type: execute
wave: 3
depends_on: [19-02, 19-04]
files_modified:
  - src/fast_fsm/visualization.py
  - tests/test_output_safety.py
  - tests/test_visualization.py
  - .specify/memory/spr-visualization.md
autonomous: true
requirements: [DIAG-01, DIAG-04, DIAG-05, DIAG-06, DIAG-07, DIAG-08, OUT-01, OUT-02]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Per D-11, every state receives a unique deterministic `s{snapshot_position}` opaque ID independently of empty, Unicode, punctuation, or colliding labels."
    - "Per D-12, Mermaid, PlantUML, and Markdown each use a separate final-sink encoder that keeps every caller field inert and on one physical line."
    - "Per D-01/D-13, each renderer, fenced/document helper, and JSON call captures once, passes the same snapshot/graph/budget through private helpers, follows snapshot order, and is byte-stable for the same snapshot."
    - "JSON and documents reuse declared-initial/SCC/depth/sparse/status analysis; dense tables are explicit, preflighted, and caller-supplied matrices are equivalence-checked or rejected with a fixed error."
  artifacts:
    - {path: "src/fast_fsm/visualization.py", provides: "Opaque-ID grammar-specific renderers and one-snapshot JSON/Markdown adapters"}
    - {path: "tests/test_output_safety.py", provides: "Hostile source-containment, collision, deterministic byte, and snapshot mutation proof"}
    - {path: "tests/test_visualization.py", provides: "Intentional ordinary-output/schema compatibility expectations"}
    - {path: ".specify/memory/spr-visualization.md", provides: "Living diagram, JSON, and Markdown contract updated with each implementing task"}
  key_links:
    - {from: "public renderer/document/JSON boundary", to: "private *_from_snapshot helper", via: "exactly one snapshot and shared budget", pattern: "_from_snapshot"}
    - {from: "snapshot state position", to: "Mermaid/PlantUML ID", via: "opaque s0/s1/... allocation", pattern: "s[0-9]"}
    - {from: "caller text", to: "grammar output line", via: "target-specific final-step encoder", pattern: "_escape_mermaid|_escape_plantuml|_escape_markdown"}
---

<objective>
Render collision-free inert diagrams and snapshot-consistent JSON/Markdown under the shared diagnostic budget.

Purpose: Implement D-11 through D-13 and complete the visualization/JSON half of DIAG-01/04/05/06/07/08 without label-derived identity, syntax injection, nested recapture, or dense default allocation.
Output: Opaque IDs, three contextual encoders, private from-snapshot adapters, stable JSON analysis, and safe bounded Markdown documents.
</objective>

## Artifacts this phase produces

- New private helpers `_opaque_state_ids()`, `_escape_mermaid_text()`, `_escape_plantuml_text()`, `_escape_markdown_heading()`, `_escape_markdown_cell()`, `_to_mermaid_from_snapshot()`, `_to_plantuml_from_snapshot()`, `_to_json_from_snapshot()`, `_to_mermaid_fenced_from_snapshot()`, and `_to_mermaid_document_from_snapshot()`.
- Existing render/JSON/document functions gain additive keyword-only `limits: DiagnosticLimits | None`; `to_json()` gains `include_adjacency: bool = False`; `to_mermaid_document()` gains `include_adjacency: bool = False` while preserving `adjacency_matrix` compatibility through full snapshot-equivalence validation.
- JSON structured analysis exposes `diagnostic_status`, declared `initial`, separately reported `current`, sparse ordered topology, SCC membership, structural depth, and depth interpretation; dense adjacency is absent unless requested.
- No new public renderer name, runtime dependency, shared generic sanitizer, public topology snapshot schema, or parser-CLI requirement is added.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-CONTEXT.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-RESEARCH.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-PATTERNS.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-VALIDATION.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-04-SUMMARY.md

<interfaces>
- Existing public functions and positional behavior remain: `to_mermaid`, `to_plantuml`, `to_json`, `to_mermaid_fenced`, and `to_mermaid_document`; all controls are keyword-only.
- Plan 19-04 provides snapshot-backed structured validation and public `DiagnosticLimits`/`DiagnosticStatus`/`DiagnosticBudgetExceeded`; visualization imports diagnostics/validation lazily or one-way so `core.py` never imports it.
- Existing Markdown document accepts an optional plain-dict adjacency matrix with `states`, `transitions`, and `matrix` keys.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode Mermaid and PlantUML with opaque snapshot-order IDs</name>
  <reversibility rating="costly">D-12 makes deterministic target-specific encoding a user-visible output contract and golden snapshots may depend on it.</reversibility>
  <files>src/fast_fsm/visualization.py, tests/test_output_safety.py, tests/test_visualization.py, .specify/memory/spr-visualization.md</files>
  <read_first>src/fast_fsm/visualization.py Mermaid/PlantUML implementations; tests/test_output_safety.py rows owned by 19-06; tests/test_visualization.py ordinary/golden expectations; .specify/memory/spr-visualization.md; .github/copilot-instructions.md SPR anti-poisoning rules; 19-CONTEXT.md D-11/D-12/D-13; 19-RESEARCH.md official grammar findings; 19-VALIDATION.md Diagram and Markdown Sources matrix</read_first>
  <behavior>
    - Empty and old-colliding labels map to distinct stable `s0`, `s1`, ... IDs while labels remain visible only through encoding.
    - Hostile text in title/state/trigger/condition cannot create comments, directives, aliases, links, terminators, fences, or extra physical lines.
    - Mermaid and PlantUML use independent allowlist encoders and preserve deterministic Unicode meaning through visible escapes/entities.
    - One top-level call captures once and output from the same snapshot is byte-identical in snapshot state/edge order.
  </behavior>
  <action>Remove only Task 19-06 Mermaid/PlantUML markers and observe RED. Replace label-derived `_mermaid_id()` identity with `_opaque_state_ids()` that assigns `s0`, `s1`, and onward to every snapshot state, including already-safe names. Implement independent final-sink Mermaid and PlantUML encoders using strict safe-character allowlists and deterministic visible encodings for all other code points; normalize CR/LF/NUL/control characters into inert encoded text without changing the outer grammar line count. Encode title, state/source/target labels, trigger, and `Condition.name` separately at their sinks; never emit caller text into IDs, directives, comments, aliases, or delimiters and never call `str()`/`repr()` on conditions. Each public function captures exactly once, constructs one graph/budget, and calls its private renderer. Preserve outer Mermaid `stateDiagram-v2` and PlantUML start/end structure while intentionally updating legacy golden IDs/labels. In this same Task 1 atomic commit, update `.specify/memory/spr-visualization.md` with the exact opaque ID allocation, separate Mermaid/PlantUML encoders, covered caller-text sinks, one-capture private-renderer seam, and deterministic-byte contract; do not defer the living-memory change to Plan 19-07.</action>
  <acceptance_criteria>
    - Empty, punctuation-colliding, Unicode, control, directive, comment, URL, quote, and fence corpus cases pass for both grammars.
    - Every state ID matches its snapshot position and is unique; no state label is used as identity.
    - Caller-controlled text cannot change physical line count or introduce active grammar tokens; repeated same-snapshot bytes match.
    - Snapshot-call spy reports one capture per top-level render and no post-capture live dictionary read.
    - The visualization SPR records the exact opaque-ID and grammar-specific renderer contract and is staged in the same commit as the implementation and golden/security tests.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_output_safety.py tests/test_visualization.py -x -q -k 'opaque or mermaid or plantuml or escape or hostile or stable or snapshot' &amp;&amp; rg -n '_opaque_state_ids|_escape_mermaid_text|_escape_plantuml_text|snapshot|byte' .specify/memory/spr-visualization.md</automated>
  </verify>
  <done>OUT-01 and the diagram portion of OUT-02/DIAG-08 are complete with collision-free IDs, inert grammar-specific text, deterministic one-snapshot output, and an atomically current visualization SPR.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Share one bounded snapshot across JSON, fenced, and Markdown output</name>
  <reversibility rating="costly">D-06/D-13 publish structured completion, ordering, and encoding semantics across tool-facing output.</reversibility>
  <files>src/fast_fsm/visualization.py, tests/test_output_safety.py, tests/test_visualization.py, .specify/memory/spr-visualization.md</files>
  <read_first>src/fast_fsm/visualization.py JSON/fenced/document implementations after Task 1; tests/test_output_safety.py JSON/Markdown/snapshot rows; tests/test_visualization.py JSON/document expectations; src/fast_fsm/validation.py structured analysis/dense adapters from 19-04; .specify/memory/spr-visualization.md as updated by Task 1; .github/copilot-instructions.md SPR anti-poisoning rules; 19-CONTEXT.md D-01/D-02/D-06 through D-13</read_first>
  <behavior>
    - Fenced/document helpers capture once and call private renderers rather than public functions that recapture.
    - JSON initial/current/reachability/SCC/depth/sparse/status fields come from one graph and one aggregate budget; no broad exception converts failure to `quality=None`.
    - Markdown heading/table/cell/fenced content stays inert for the full hostile corpus.
    - Dense adjacency is absent by default; explicit generation preflights; supplied data must equal the captured snapshot or fail with a fixed non-leaking error.
  </behavior>
  <action>Remove the remaining Task 19-06 JSON/Markdown/snapshot markers and observe RED. Implement the private fenced/document/JSON helpers listed in Artifacts and have every public top-level call capture once then pass its snapshot, graph, and one ledger downward. Replace JSON's live BFS/DFS and broad quality fallback with Plan 19-04 analysis so declared initial, current metadata, SCC members, structural depth/interpretation, sparse rows, and diagnostic status are consistent; budget/correctness errors must remain explicit. Implement separate Markdown heading/cell encoders and ensure fenced content is produced from the already-rendered private Mermaid output. Keep `adjacency_matrix` accepted, but validate complete ordered state/transition/matrix equivalence against the captured graph under the same budget and raise a fixed mismatch error on stale/incompatible data; when `include_adjacency=True` creates data, reserve dense cells before allocation. Preserve existing outer document headings/table meanings and public callable names. Extend `.specify/memory/spr-visualization.md` in this same Task 2 atomic commit with the exact JSON analysis/status fields, fenced/document private composition, Markdown encoders, dense opt-in/preflight behavior, and full-equivalence stale-matrix rejection.</action>
  <acceptance_criteria>
    - Snapshot spy records one call for JSON, fenced, and document functions; barrier mutation cannot mix graph versions.
    - JSON exposes exact status/counters and corrected declared-initial/SCC/depth/sparse data in deterministic order; exhaustion is never hidden as `quality=None`.
    - Markdown hostile headings/cells/events/fences remain structurally contained on one encoded line.
    - Default document/JSON calls allocate no dense matrix; exact dense budget succeeds, one-less fails before allocation, and stale supplied matrices raise a fixed redacted error.
    - The visualization SPR matches the shipped JSON/fenced/Markdown/dense/stale-matrix contract and is committed atomically with those source and test changes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_output_safety.py tests/test_visualization.py tests/test_diagnostic_contracts.py -x -q -k 'json or markdown or document or fenced or dense or status or snapshot or stable' &amp;&amp; rg -n 'diagnostic_status|_to_json_from_snapshot|_to_mermaid_fenced_from_snapshot|_to_mermaid_document_from_snapshot|markdown|adjacency' .specify/memory/spr-visualization.md</automated>
  </verify>
  <done>DIAG-01/04/05/06/07/08 and OUT-02 are complete across JSON/fenced/Markdown output with one snapshot, one budget, safe encoding, sparse defaults, explicit failures, and same-commit living visualization memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller labels/titles → Mermaid/PlantUML/Markdown | Untrusted text can escape its data position into executable/rendering grammar. |
| mutable machine/adjacency input → JSON/document | Independent captures or stale caller data can combine incompatible graph versions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-25 | Tampering | Mermaid/PlantUML directives/comments | high | mitigate | Opaque IDs, independent final-sink encoders, physical-line containment, and hostile directive corpus. |
| T-19-26 | Tampering | Markdown headings/tables/fences | high | mitigate | Dedicated heading/cell encoding and private already-rendered fenced composition. |
| T-19-27 | Spoofing | label-derived identity collisions | high | mitigate | IDs derive only from snapshot position and collision tests include empty/punctuation variants. |
| T-19-28 | Tampering | mixed/stale JSON or adjacency data | high | mitigate | One capture/shared graph plus complete equivalence validation or fixed rejection. |
| T-19-29 | Denial of Service | dense JSON/document output | high | mitigate | Sparse default and reserve-before-allocation explicit dense opt-in. |
</threat_model>

<verification>
- Run grammar selection after Task 1, JSON/document selection after Task 2, then full output/visualization/diagnostic modules.
- Run Ruff and blocking mypy on the changed source/tests.
- Confirm Mermaid/PlantUML tools remain optional and no dependency files change.
</verification>

<success_criteria>
- Distinct states can never collide because of sanitization.
- All caller text remains inert in the correct target grammar.
- JSON, diagrams, fenced output, and Markdown describe one bounded immutable snapshot.
</success_criteria>

<output>
Create `.planning/phases/19-bounded-diagnostics-safe-output/19-06-SUMMARY.md` when done.
</output>
