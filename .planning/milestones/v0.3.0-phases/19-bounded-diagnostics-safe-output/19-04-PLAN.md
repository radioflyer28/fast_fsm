---
phase: 19-bounded-diagnostics-safe-output
plan: 04
type: execute
wave: 2
depends_on: [19-02, 19-03]
files_modified:
  - src/fast_fsm/validation.py
  - src/fast_fsm/__init__.py
  - tests/test_diagnostic_contracts.py
  - tests/test_validation.py
  - .specify/memory/spr-validation.md
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04, DIAG-05, DIAG-06, DIAG-07, DIAG-08]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Per D-03, batch and comparison outputs contain one ordered entry per input with explicit zero-based `position`; duplicate names remain labels and tied rankings break by ascending position."
    - "Per D-04, zero-machine comparison returns `entries=[]`, `rankings=[]`, `best_fsm=None`, `count=0`, `total_issues=0`, `avg_score=None`, and `score_range=None` without division."
    - "Per D-01/D-06, each input is captured once, every nested validator/report shares one graph/budget, and structured output carries deterministic completion/count metadata while legacy shapes fail closed."
    - "Validator reports use declared initial reachability, complete SCC membership/depth, and sparse topology by default; dense output is explicit and preflighted."
  artifacts:
    - {path: "src/fast_fsm/validation.py", provides: "Ordered comparison/batch schemas and snapshot-shared structured validation reports"}
    - {path: "src/fast_fsm/__init__.py", provides: "Intentional public diagnostic type exports without private graph exports"}
    - {path: "tests/test_diagnostic_contracts.py", provides: "Duplicate/empty/equality/order/snapshot/budget contract proof"}
    - {path: ".specify/memory/spr-validation.md", provides: "Living ordered-schema and structured validation contract updated with each implementing task"}
  key_links:
    - {from: "compare_fsms/batch_validate", to: "input sequence", via: "enumerated position records rather than name-keyed dicts", pattern: "position|entries|rankings"}
    - {from: "validator reports", to: "_DiagnosticGraph/_DiagnosticBudget", via: "one snapshot and aggregate budget per top-level call", pattern: "diagnostic_status|limits"}
    - {from: "package __all__", to: "public diagnostic types", via: "explicit import/export registry", pattern: "DiagnosticLimits|DiagnosticStatus|DiagnosticBudgetExceeded"}
---

<objective>
Publish position-safe batch/comparison results and complete snapshot-backed validation reports.

Purpose: Resolve D-03/D-04 and finish DIAG-01 through DIAG-08 at the public validation API without name collisions, zero-input errors, dense defaults, or nested snapshot/budget resets.
Output: Exact ordered schemas, one-capture per-input validation, sparse structured reports, and intentional public exports.
</objective>

## Artifacts this phase produces

- Existing `compare_fsms()` returns the exact `entries`, `rankings`, `best_fsm`, and `comparison_metrics` position-safe schema described below; existing `batch_validate()` returns `count` plus ordered `entries` records containing `position`, `name`, and `validator`.
- Comparison entry fields: `position`, `name`, `score`, `metrics`, `issue_count`, `diagnostic_status`; ranking fields: `position`, `name`, `score`; `best_fsm` is the winning `{position, name}` record or `None`.
- `comparison_metrics` fields: `count`, `avg_score`, `score_range`, `total_issues`, and `diagnostic_status`; zero input uses the exact D-04 values.
- Validator structured reports add `diagnostic_status`, `structural_depth`, `depth_interpretation`, `cyclic_components`, `states_in_cycles`, and `sparse_adjacency`; dense matrices appear only when explicitly requested.
- No new file or dependency is added; public diagnostic types from 19-01 are exported, while `_DiagnosticGraph`, `_DiagnosticBudget`, and private helpers remain unexported.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-CONTEXT.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-RESEARCH.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-PATTERNS.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-VALIDATION.md
@.planning/phases/19-bounded-diagnostics-safe-output/19-03-SUMMARY.md

<interfaces>
- Existing `compare_fsms(*fsms) -> dict` ranks by `overall_score`; existing `batch_validate(*fsms, show_summary=True)` currently returns a name-keyed dictionary and prints a summary.
- Plan 19-03 provides deterministic SCC/depth/sparse/dense/path adapters and status accounting on the snapshot-backed validator.
- Existing validator/report/scoring callable names and positional parameters remain available; all new controls are keyword-only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve duplicate inputs and define the zero-comparison schema</name>
  <reversibility rating="costly">D-03 and D-04 publish ordered result identity and empty-value semantics that downstream consumers will parse.</reversibility>
  <files>src/fast_fsm/validation.py, src/fast_fsm/__init__.py, tests/test_diagnostic_contracts.py, tests/test_validation.py, .specify/memory/spr-validation.md</files>
  <read_first>src/fast_fsm/validation.py compare_fsms/batch_validate/scoring implementations; src/fast_fsm/__init__.py; tests/test_diagnostic_contracts.py comparison rows owned by 19-04; tests/test_validation.py existing comparison/batch expectations; .specify/memory/spr-validation.md as updated by 19-03; .github/copilot-instructions.md SPR anti-poisoning rules; 19-CONTEXT.md D-03/D-04; 19-VALIDATION.md DIAG-02/03 evidence rows</read_first>
  <behavior>
    - Three same-name inputs produce three entries at positions 0, 1, and 2 in input order in both APIs.
    - Equal scores rank by ascending position; names never serve as identity or dictionary keys.
    - Zero comparison returns the exact documented empty values and never divides.
    - Each machine snapshot is captured once and completion/count status remains associated with its position.
  </behavior>
  <action>Remove only Task 19-04 duplicate/empty/comparison markers and observe RED. Per D-03, replace name-keyed accumulation with enumerated ordered records using the exact fields in Artifacts; keep names as display labels and sort rankings by descending overall score then ascending position. Make `best_fsm` a `{position, name}` record rather than an ambiguous duplicate label. Per D-04, branch before numeric aggregation and return the exact empty values; do not invent zero for undefined average/range. Add keyword-only `limits: DiagnosticLimits | None` to both APIs, capture each input exactly once, and preserve `show_summary` compatibility by iterating ordered entries. Update legacy expectations deliberately and document the costly schema in docstrings; do not add a name-keyed compatibility alias that would silently discard duplicates. In this same Task 1 atomic commit, update `.specify/memory/spr-validation.md` with the exact ordered entry/ranking/best/aggregate fields, positional tie behavior, batch cardinality rule, and zero-input values; do not defer the living contract to Plan 19-07.</action>
  <acceptance_criteria>
    - Duplicate-name input cardinality, order, identity, tied ranking, and per-entry status tests pass for comparison and batch.
    - Zero input returns exact D-04 fields: empty entries/rankings, no best, zero count/issues, and `None` average/range.
    - Snapshot-call count equals input count, including when scoring/report helpers run.
    - Existing callable names, positional FSM arguments, and `show_summary` behavior remain available.
    - The validation SPR records the exact duplicate-safe and empty schemas and is staged in the same commit as their implementation and regression tests.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_diagnostic_contracts.py tests/test_validation.py -x -q -k 'compare or batch or duplicate or empty or snapshot' &amp;&amp; rg -n 'position|entries|rankings|best_fsm|avg_score|score_range' .specify/memory/spr-validation.md</automated>
  </verify>
  <done>DIAG-02 and DIAG-03 are complete with explicit positional identity, stable tied ordering, one capture per input, the exact zero-machine result, and matching living validation memory in the same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unify validator, score, report, and JSON-ready analysis status</name>
  <reversibility rating="costly">D-06 makes completeness and exhaustion metadata a stable public contract across structured diagnostics.</reversibility>
  <files>src/fast_fsm/validation.py, src/fast_fsm/__init__.py, tests/test_diagnostic_contracts.py, tests/test_validation.py, .specify/memory/spr-validation.md</files>
  <read_first>src/fast_fsm/validation.py validators, reports, issue/scoring initialization, and compatibility functions; tests/test_diagnostic_contracts.py report/status rows owned by 19-04; tests/test_validation.py; .specify/memory/spr-validation.md as updated by Task 1; .github/copilot-instructions.md SPR anti-poisoning rules; 19-CONTEXT.md D-01/D-02/D-05 through D-10; 19-RESEARCH.md completeness/report migration guidance</read_first>
  <behavior>
    - Every validator/scoring/report top-level call derives initial/current/reachability/cycles/depth/topology from one graph and one aggregate budget.
    - Structured reports include complete/exhausted stage/dimension and all deterministic counter values.
    - Sparse adjacency is present by default; transition/adjacency matrices appear only by explicit opt-in and preflight.
    - Legacy set/list/scalar/printing helpers raise one redacted error before emitting partial output.
  </behavior>
  <action>Remove the remaining Task 19-04 report/status markers and observe RED. Route `FSMValidator`, `EnhancedFSMValidator`, `validate_fsm`, `enhanced_validate_fsm`, `validate_and_score`, quick reports, and lint/report assembly through private from-graph/from-analysis helpers so nested work cannot recapture or reset the ledger. Per D-02, source structural reachability only from the declared initial name and treat current name as reporting metadata. Per D-06/D-07/D-08/D-09, add the exact structured fields listed in Artifacts, make sparse rows default, make dense materialization explicit, propagate a single aggregate status into scores/comparisons, and remove broad fallbacks that hide diagnostic exhaustion. Keep public symbols and signatures compatible via additive keyword-only options. Export only the intended public limits/status/error types and add structural tests ensuring validation never reads live `_states`/`_transitions` after capture. Extend `.specify/memory/spr-validation.md` in this same Task 2 atomic commit with the exact report/status fields, one-capture/shared-ledger wiring, declared-initial/current separation, sparse default, dense opt-in, and legacy fail-closed behavior.</action>
  <acceptance_criteria>
    - Validation/report tests cover declared initial vs current, SCC members, depth interpretation, sparse default, dense opt-in, and exact status counters.
    - Aggregate exact-limit succeeds and one-less reports the nested exhausted stage/dimension without reset.
    - Printing/legacy helpers emit no partial lines or values after exhaustion.
    - AST/source assertions show post-capture validator analysis has no live topology dictionary access; private graph/budget symbols are absent from package `__all__`.
    - The validation SPR matches the shipped report/status/export contract and is committed with the source/test changes that establish it.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_diagnostic_contracts.py tests/test_validation.py tests/test_mypyc_guard.py -x -q -k 'report or status or exhaust or sparse or initial or snapshot or export' &amp;&amp; rg -n 'diagnostic_status|structural_depth|depth_interpretation|cyclic_components|states_in_cycles|sparse_adjacency' .specify/memory/spr-validation.md</automated>
  </verify>
  <done>All validation-side DIAG-01 through DIAG-08 behavior is snapshot-consistent, bounded, sparse-first, explicit on exhaustion, publicly documented in code, and reflected in the same-commit living validation SPR without dropping compatibility symbols.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller machine sequence → comparison/batch schema | Duplicate labels and ties must not overwrite or impersonate another input. |
| nested validation helpers → aggregate status | A nested reset or fallback can misrepresent incomplete work as complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-15 | Spoofing | duplicate-name comparison/batch | high | mitigate | Explicit positional identity, ordered entries, stable tie-break, and no name-keyed storage. |
| T-19-16 | Tampering | empty/aggregate metrics | medium | mitigate | Exact zero-input branch with `None` undefined aggregates and schema tests. |
| T-19-17 | Tampering | nested budget/status | high | mitigate | One ledger per top-level call, no reset, explicit status propagation, and exact aggregate boundary tests. |
| T-19-18 | Denial of Service | report dense output | high | mitigate | Sparse default and explicit preflighted dense option only. |
| T-19-19 | Information Disclosure | diagnostic exceptions/reports | medium | mitigate | Fixed error category and bounded metadata; no raw machine/payload/exception representations. |
</threat_model>

<verification>
- Run focused compare/batch tests after Task 1 and report/status tests after Task 2.
- Run full `tests/test_validation.py` plus diagnostic contracts before commit.
- Run Ruff and blocking mypy on modified production/tests; no full suite until the final gate.
</verification>

<success_criteria>
- Duplicate and empty inputs have exact stable public semantics.
- Every validation/report path describes one immutable snapshot and one budget.
- Structured completion is explicit and legacy partial output is impossible.
</success_criteria>

<output>
Create `.planning/phases/19-bounded-diagnostics-safe-output/19-04-SUMMARY.md` when done.
</output>
